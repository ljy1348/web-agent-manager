# 작업 기록

이 문서는 공개 저장소에서 공유할 수 있는 수준의 주요 결정과 검증 결과만 기록한다.
운영 도메인, 사용자명, 로컬 절대 경로, 채팅 번호, 인증 정보와 실제 데이터 조작 내역은 기록하지 않는다.

## 2026-07 Claude·Codex 연동 기반

- 사용자 요청에 따라 Claude Code와 Codex 세션을 프로젝트별로 조회하고 웹 터미널에서 이어서 작업할 수 있는 기반을 구현했다.
- CLI별 출력 형식과 상태 판정 차이를 각각의 어댑터에서 처리하고, 공통 채팅 모델에는 정규화된 상태만 저장하기로 결정했다.
- 작업 중인 여러 채팅의 선택 상태가 바뀌지 않도록 URL과 화면 상태의 소유권을 명확히 분리했다.

## 2026-07 파일 탐색·미리보기

- 채팅의 로컬 파일 링크를 파일 탭으로 연결하고 Markdown, HTML, 이미지, 영상, PDF와 텍스트 미리보기를 추가했다.
- 압축 기반 형식은 실행하거나 임의 해제하지 않고 압축 파일 안내만 제공하도록 제한했다.
- 파일 경로는 프로젝트 루트 내부로 정규화하고 업로드·다운로드 API의 경로 이탈을 거부하도록 했다.

## 2026-07 GitHub·프로젝트 관리

- GitHub 사용자·조직 저장소에서 프로젝트를 만들고 이미 연결된 저장소는 기존 채팅으로 이동하도록 구현했다.
- 새 프로젝트 생성은 모달로 제공하고 로컬 프로젝트에서 원격 저장소 생성·연결을 선택할 수 있게 했다.
- 큰 diff는 지연 로딩과 구간 렌더링을 사용하고, 통합·분할 보기를 전환할 수 있게 했다.

## 2026-07 설치·에이전트 위임

- 소스 설치, Docker 실행, 데스크톱 패키징 흐름을 추가하고 CLI 인증은 사용자가 직접 완료하는 방식으로 유지했다.
- Claude와 Codex가 다른 채팅의 문맥을 조회하거나 작업을 위임할 수 있도록 MCP 브리지와 스킬을 제공했다.
- 설치 후 CLI가 추가된 경우 웹 관리 화면에서 연동 상태를 확인하고 설정을 다시 적용할 수 있게 했다.

## 2026-07 접근 제어·운영 안정화

- 관리자 전용 터미널, 권한 요청, 도구 관리와 일반 사용자 화면을 역할 기반으로 분리했다.
- 내부망 여부는 신뢰할 수 있는 서버 측 요청 정보로 판정하고 숨김 파일 노출 정책에 적용했다.
- 종료된 터미널의 자동 재시도와 상태 감시를 중단하고, 로그·캐시·프로세스의 보존 범위를 제한했다.

## 2026-07-31 Codex - 공개 준비 점검

- 작업 시작: 사용자가 저장소를 공개해도 되는지 다시 점검해 달라고 요청했다.
- 점검 결정: 인증·인가, 네트워크 경계, 파일 처리, 로그, 의존성, 배포 산출물, 공개 문서와 저장소 이력을 함께 확인했다.
- 확인 결과: 로그인 제한의 메모리 상한, IP 단위 제한, WebSocket Origin 프로토콜 검사, 일부 운영 API 권한, 보안 헤더, 운영 로그 기본 레벨, 공개 자료의 운영 식별자 제거와 릴리즈 무결성 검증이 필요했다.
- 검증 결과: 수정 전 전체 단위 테스트와 프로덕션 의존성 감사를 통과했으며, 개발 의존성 경고와 서명되지 않은 데스크톱 패키지는 별도 공개 위험으로 분류했다.

## 2026-07-31 Codex - 공개 준비 차단 항목 해소

- 작업 시작: 사용자가 점검 결과를 실제로 처리하라고 요청했다.
- 구현 결정: 로그인 계정·IP 제한과 캐시 상한, 동일 출처 WebSocket 검사, 관리자 권한 누락, HTTP 보안 헤더, 운영 로그 기본 레벨을 먼저 보강한다.
- 공개 자료 결정: 개발 호스트는 환경변수로 옮기고 문서·테스트·스크린샷에서 운영 식별자를 제거한다. 현재 파일을 정리하는 것만으로 과거 Git 객체가 사라지지 않으므로 공개 전 이력 재작성 또는 새 공개 저장소 생성이 필요하다.
- 배포 결정: CI에서 검증을 강제하고 릴리즈 산출물에 체크섬과 SBOM을 포함한다. 라이선스 종류와 원격 저장소 공개·태그 변경은 사용자의 명시적 결정 전에는 수행하지 않는다.
- 구현 완료: 계정·IP 로그인 제한과 메모리 상한, 인증 실패 감사 로그 보존 제한, 계정 열거 완화, WebSocket Origin 호스트·프로토콜 검사, HTTP 보안 헤더, 운영 로그 레벨, 관리자 권한 누락을 보강했다.
- 공개 자료 완료: 개발 Host를 환경변수로 분리하고 테스트 경로와 공개 스크린샷을 샘플 데이터로 다시 만들었다. 운영 식별자가 있던 화면은 제거하고 현재 `history.md`는 공개 가능한 결정 요약으로 교체했다.
- 배포 완료: 버전을 기존 태그보다 높은 `0.2.0`으로 정리하고 PR CI·Dependabot·고정 Action SHA, Docker 증명·SBOM, 릴리즈 SBOM·체크섬·Release 자동 생성을 추가했다. Docker CLI 버전과 Electron asar 경계를 고정하고 데스크톱 로컬 포트 선점·외부 이동을 제한했다.
- 추가 발견 및 수정: 셸의 개발 환경변수가 프로덕션 Vite 빌드에 상속돼 절대 소스 경로가 번들에 포함되는 문제를 발견했다. 빌드에서 운영 환경을 강제하고 `dist` 절대 경로 검사를 추가해 공개 ZIP에 로컬 경로가 남지 않게 했다.
- 검증 완료: 타입 검사, 단위 테스트 286개, 프로덕션 빌드, Playwright UI 14개, 브라우저 리소스 누적 검사, Linux Electron asar 패키징, MCP 버전 응답, 전체 npm 감사와 패키지 서명 검사를 통과했다. `0.2.0` ZIP 4개와 CycloneDX SBOM의 SHA-256 체크섬도 모두 일치했다.
- 남은 사용자 결정: 라이선스 종류를 정하고, 운영 식별자가 남은 과거 Git 객체를 공개하지 않도록 새 공개 저장소를 만들지 현재 저장소 이력을 재작성할지 선택해야 한다. 이 결정 전에는 저장소 공개·강제 푸시·기존 태그 변경을 수행하지 않는다.

## 2026-07-31 Codex - MIT 공개 저장소 전환

- 작업 시작: 사용자가 MIT 라이선스와 새 공개 저장소 생성을 선택했다.
- 이력 결정: 운영 식별자가 포함됐던 과거 Git 객체를 공개하지 않기 위해 기존 저장소는 비공개 보관 저장소로 이름을 바꾸고, 현재 검증된 파일만 단일 커밋 이력으로 새 공개 저장소에 게시한다.
- 배포 결정: 네이티브 Node 모듈이 다른 CPU 아키텍처로 잘못 묶이지 않도록 macOS Intel과 Apple Silicon 설치 파일은 각각 x64·arm64 GitHub runner에서 빌드한다.
- 구현 완료: 표준 MIT 라이선스와 패키지 저장소·이슈·홈페이지 메타데이터를 추가하고 README·코드트리에 공개 이용 조건과 macOS 네이티브 빌드 구조를 반영했다.
- 검증 완료: 워크플로 YAML 파싱, 패키지 lockfile 정합성, 전체 npm 감사, 타입 검사, 단위 테스트 286개, 프로덕션 빌드와 공개 산출물 경로 검사를 통과했다.
- 원격 검증 발견: 새 공개 저장소의 최초 CI는 통과했지만 Docker builder의 일반 경로 `/app`가 `/approval` 같은 정상 번들 문자열과 겹쳐 절대 경로 검사에서 오탐됐다.
- 수정 결정: 공개 경로 검사 강도를 낮추지 않고 Docker builder만 고유한 전용 경로로 옮겨 정상 애플리케이션 경로와 빌드 머신 경로를 구분한다.
- 로컬 검증 제한: 현재 호스트에는 Docker CLI가 없어 컨테이너 build를 실행할 수 없으므로 공개 저장소 작업 브랜치의 GitHub Actions 다중 아키텍처 빌드로 실제 검증한다.
- GHCR 발견: 수정 브랜치의 amd64·arm64 빌드와 SBOM 생성은 통과했지만 기존 비공개 저장소가 같은 이름의 컨테이너 패키지를 소유해 새 공개 저장소 토큰의 업로드가 거부됐다.
- 패키지 결정: 기존 비공개 이미지를 삭제하지 않고 보존하며, 공개 저장소가 권한과 공개 범위를 처음부터 상속할 수 있도록 공개 이미지 이름을 `web-agent-manager-app`으로 분리한다.
- 원격 검증 완료: 공개 이미지 경로로 linux/amd64·linux/arm64 빌드, 공개 경로 검사, SBOM·provenance 생성과 GHCR 업로드를 모두 통과했다.
- 공개 릴리즈 점검 시작: `v0.2.0`의 운영체제별 설치 파일 빌드는 모두 성공했지만, 다운로드된 아티팩트의 하위 디렉터리를 `gh release`에 파일처럼 전달해 최종 게시가 실패했다.
- 수정 결정: 릴리즈 아티팩트의 일반 파일만 단일 디렉터리로 모으고 동명 파일을 거부한 뒤, 같은 디렉터리에 전체 체크섬을 생성해 실제 파일 목록만 게시한다.
- 로컬 수정 검증 완료: 중첩 아티팩트 평탄화·중복 검사와 체크섬 생성을 샘플 파일로 확인하고, 워크플로 YAML 파싱, 타입 검사, 단위 테스트 286개, 프로덕션 빌드와 공개 경로 검사를 통과했다.
- 공개 자산 추가 점검: 최종 Release 게시에는 성공했지만 GitHub가 Windows 설치 파일명의 공백을 점으로 정규화해, 업로드 전에 생성한 체크섬 목록의 파일명과 실제 다운로드 파일명 2개가 달랐다.
- 파일명 결정: 평탄화 단계에서 공백을 점으로 먼저 정규화하고 그 이름으로 충돌 검사와 체크섬 생성을 수행해 GitHub Release의 실제 자산 이름과 일치시킨다.
- 파일명 수정 검증 완료: 공백이 있는 Windows 설치 파일 샘플의 점 표기 변환과 체크섬 기록을 확인하고, 워크플로 YAML 파싱, 타입 검사, 단위 테스트 286개, 프로덕션 빌드와 공개 경로 검사를 다시 통과했다.
- 공개 전환 완료: 기존 이력은 비공개 보관 저장소에 유지하고, MIT 라이선스의 새 공개 저장소 `ljy1348/web-agent-manager`를 검증된 파일만 담은 정리된 이력으로 게시했다.
- 저장소 보호 완료: `main` 필수 PR·검증·최신화·대화 해결·선형 이력 규칙과 관리자 적용, 비밀 탐지·푸시 보호·취약점 알림·자동 보안 수정을 활성화했다.
- 배포 검증 완료: `v0.2.0`에서 Linux·macOS Intel·macOS ARM·Windows 설치 파일, 간단 설치 ZIP, CycloneDX SBOM과 SHA-256 목록을 게시했으며 체크섬 14개와 실제 대상 자산 14개의 이름이 모두 일치했다. linux/amd64·linux/arm64 공개 GHCR 이미지의 태그 빌드도 통과했다.

## 2026-07-31 Codex - 개발 허용 호스트 로컬 복구

- 작업 시작: 공개 자료 정리 후 현재 개발 서버에서 사용하던 외부 도메인이 Vite Host 검사에 차단되는 현상을 확인했다.
- 설정 결정: 개인 운영 도메인은 공개 코드 기본값으로 되돌리지 않고, Git에서 제외되며 `.env`보다 우선하는 `.env.local`에만 보관해 저장소 갱신 후에도 유지한다. IP 리터럴과 localhost는 Vite 기본 허용 정책을 그대로 사용한다.
- 적용 완료: 개발 watch 서버를 새 설정으로 재시작하고 지정 도메인, 루트·하위 도메인, VPN·사설 IP와 localhost Host 요청이 모두 정상 응답하는 것을 확인했다.

## 2026-08-01 Codex - 모바일 인증 팝업 동선 수정

- 작업 시작: 모바일 CLI 인증 버튼이 채팅 전송 버튼을 가리고, 모바일 전역 header 숨김 규칙 때문에 인증 팝업의 닫기 버튼이 보이지 않으며 브라우저 뒤로가기로 팝업을 닫을 수 없는 문제를 수정한다.
- 구현 결정: 인증 필요 상태를 상위 화면에 전달해 하나 이상의 설치된 CLI가 미인증일 때만 모바일 하단 탭바에 인증 버튼을 표시한다. 공통 dialog history 훅으로 모달을 브라우저 history에 쌓고 뒤로가기로 최상위 팝업부터 닫는다.
- 추가 점검: 원본 터미널을 열었을 때 검은 화면만 표시되는 현상을 함께 재현해 xterm 크기 계산, WebSocket 구독과 tmux 초기 화면 전달 경로를 확인한다.
- 원인 확인: 원본·인증 xterm이 구독 메시지를 먼저 보내고 출력 리스너를 나중에 등록해 즉시 도착한 첫 프레임을 놓칠 수 있었고, 정지된 tmux 화면은 구독 시 직접 전달하는 ANSI 초기 프레임이 없어 이후 출력 전까지 검은 화면으로 남았다.
- 적용 완료: 출력 리스너를 구독 전에 등록하고 headless 터미널의 현재 화면·커서를 ANSI 초기 프레임으로 즉시 전송했다. 모바일에서는 팝업 헤더를 숨기지 않고 인증 진입을 하단 탭바 안으로 옮겼으며, 미인증 CLI가 있을 때만 표시하고 관리 팝업을 브라우저 뒤로가기로 닫도록 통일했다.
- 검증 완료: 390×844 모바일 캡처에서 닫기 버튼과 공급자 이름·상태 배치를 확인하고, 원본 터미널 첫 출력·모바일 팝업 뒤로가기·인증 완료 후 버튼 숨김을 포함한 Playwright 3건, 타입 검사, 단위 테스트 287개, 프로덕션 빌드와 공개 경로 검사를 통과했다.

## 2026-08-01 Codex - 14003 개발 서버 프로세스 정리 및 재기동

- 작업 시작: 원본 터미널 수정이 사용자 브라우저에서는 반영되지 않고 web-agent-manager 관련 프로세스가 많아 보이는 상태를 점검하고, 14003 개발 서버를 한 세트로 정리해 다시 실행한다.
- 보존 결정: 기존 채팅 기록과 작업 상태를 잃지 않도록 tmux 세션 자체와 그 안의 Claude·Codex 프로세스는 종료하지 않는다. watch 서버 프로세스 트리만 정상 종료해 서버가 소유한 tmux attach·MCP 자식을 정리한 후 다시 구독한다.
- 점검 결과: 중복 서버는 없었고 14003 애플리케이션과 24678 Vite WebSocket을 같은 Node 서버가 소유했다. 많아 보인 프로세스는 npm·tsx watch 계층과 실행 중 채팅 7개의 tmux attach, 상태 조회용 Codex·Claude 자식이었다.
- 재기동 완료: 개발 서버 전용 프로세스 그룹을 SIGINT로 정상 종료해 포트와 attach 자식을 모두 정리하고, tmux 채팅 세션 7개는 보존한 채 `npm run dev`를 다시 실행했다. 새 서버 한 개가 `0.0.0.0:14003`과 24678을 점유하고 HTTP 200을 반환하며, 초기 동기화 후 CPU는 약 2%로 안정됐다.

## 2026-08-01 Codex - 외부 HTTPS 도메인 원본 터미널 연결 수정

- 작업 시작: `https://agent.qloug.com`에서는 일반 페이지가 열리지만 원본 터미널이 표시되지 않는 현상이 의도된 외부망 제한인지 확인한다.
- 원인 확인: 원본 터미널은 네트워크 위치가 아니라 관리자 권한으로 제한돼 외부 도메인에서도 동작해야 한다. 실제 HTTPS 페이지는 200이지만 WSS 업그레이드는 실패했고, 서버가 reverse proxy 뒤의 내부 HTTP 소켓만 보고 HTTPS Origin을 거부하는 조건과 일치했다.
- 구현 결정: Origin과 Host가 같은지 확인하는 기존 보안 경계는 유지하고, 프록시가 전달한 첫 번째 유효 `X-Forwarded-Proto`를 외부 프로토콜 판정에 반영한다. 현재 Host와 일치하는 명시적 `PUBLIC_URL`은 기존처럼 최우선으로 사용한다.
- 적용 완료: `isSameOrigin`이 직접 TLS, 유효한 전달 프로토콜, 현재 Host와 일치하는 `PUBLIC_URL` 순으로 외부 프로토콜을 판정하도록 수정하고 다중 Host·쉼표 전달값·잘못된 프로토콜·다른 Host 거부 회귀 테스트를 추가했다.
- 검증 완료: 실행 중 watch 서버가 새 코드로 재시작된 것을 확인했고 로컬 14003과 `https://agent.qloug.com`이 모두 200을 반환한다. 타입 검사, 단위 테스트 288개, 프로덕션 빌드와 공개 경로 검사를 통과했다.

## 2026-08-01 Codex - v0.3.0 선택형 터미널 채팅 모드 이슈 등록

- 작업 시작: 공개 저장소에 `v0.3.0` 이슈로 채팅 화면을 PTY 터미널 기반 입력·출력 화면으로 사용할 수 있는 기능을 등록한다.
- 범위 결정: 기존 채팅 화면을 완전히 교체하지 않고 설정 화면에서 `채팅 모드`와 `터미널 모드`를 사용자가 선택한다. 기본값은 기존 채팅 모드로 유지하며 터미널 모드에서도 완료 알림·승인·상태 및 사용량 동기화 같은 백그라운드 기능은 계속 동작해야 한다.
- 등록 완료: 공개 저장소에 `v0.3.0` 마일스톤을 만들고 enhancement 이슈 #20 `설정에서 채팅 화면과 터미널 화면 모드 선택 지원`을 등록했다. 계정별 설정 저장, 전체 PTY 화면·입력, 세션 보존, 재연결 초기 프레임, 관리자 권한 유지, 모바일 대응과 자동화 테스트를 수용 조건으로 명시했다.

## 2026-08-01 Codex - Claude 사용량 조회 CPU 순간 부하 점검

- 작업 시작: 사용량 새로고침 시 잠깐 나타난 Claude PID가 CPU 83.3%로 표시되는 원인을 확인하고, 자동 조회 주기·중복 실행·자식 프로세스 수집 경로를 점검한다.
- 초기 확인: 해당 PID는 점검 시 이미 종료된 일회성 프로세스였고 상시 Claude 상태 PTY는 낮은 CPU로 유지됐다. 사용량 조회는 공급자별 단일 PTY에 명령을 보내며 `busy` 플래그로 중복 실행을 차단하고 있지만, 자동 주기가 60초라 Claude CLI의 짧은 계산 부하가 자주 노출될 수 있다.
- 원인 정정: 실행 중 자식 프로세스를 다시 추적한 결과 높은 CPU를 사용한 명령은 상태 PTY의 `/usage`가 아니라 관리자 연동 알림이 60초마다 실행하던 `claude mcp get web-agent-manager`였다. 사용량 PTY는 기존대로 계속 살아 있는 단일 세션에 `/usage`만 전달한다.
- 변경 결정: Codex·Claude 연동 상태는 서버 시작 시 공급자별로 한 번만 확인한다. 정상 연동은 DB에 저장해 다음 서버 시작부터 실제 CLI 검사를 생략하고, 실패 상태만 메모리에 남겨 연결 버튼을 표시한다. 사용자가 연결을 누른 시점에는 현재 상태를 다시 확인해 이미 정상이라면 MCP를 제거·재등록하지 않고 성공으로 저장한다.
- 추가 원인: 실제 서버에서 반복 프로세스를 확인하던 중 핵심 데이터 재조회가 `/runtime` API를 호출할 때마다 별도의 `claude --version`도 실행하는 것을 확인했다. 연동 판정에서는 불필요한 버전 명령을 제거하고, 런타임 버전은 서버 라우터 생성 시 한 번 만든 결과를 프로세스 수명 동안 재사용하기로 했다.
- 구현 완료: `agent_integration_status`에 성공한 연동만 저장하고 서버 시작의 미저장 공급자 검사 이후 API 조회에서는 CLI를 실행하지 않도록 변경했다. 관리자 연결은 글로벌 스킬 설치 후 실제 MCP 상태를 먼저 확인해 이미 정상이면 그대로 성공 저장하며, 실패했을 때만 기존 MCP 정리·재등록과 최종 검증을 수행한다. 클라이언트의 60초·화면 복귀 연동 조회도 제거했다.
- 사용량 경량화: Claude 상태 조회 전용 PTY를 `--safe-mode --ax-screen-reader`로 실행해 인증과 `/usage` 같은 내장 명령은 유지하면서 훅·MCP·플러그인 로딩을 제외했다. 일반 채팅 실행 인자에는 영향을 주지 않는다.
- 검증 완료: 타입 검사와 연동·운영·사용량·Claude 파서 집중 테스트 83개, 전체 테스트 47개 파일 293개, 프로덕션 빌드를 통과했다. 개발 서버 안정 상태를 `top`으로 3초 연속 측정했을 때 Claude 상태 PTY는 0.0% CPU였고, 반복 생성되던 `claude mcp get`·`claude --version` 자식 프로세스가 없는 것을 확인했다.
- 후속 점검 시작: 첫 커밋 직후 프로세스 목록에서 인증 패널의 10초 폴링이 실행한 `claude auth status --json`이 CPU 87%를 순간 사용한 것을 추가로 확인했다. 인증 상태는 서버 시작 시 한 번 메모리에 캐시하고 로그인 PTY 종료 직후에만 해당 공급자를 재검사하며, 클라이언트 반복 폴링을 제거한다.
- 후속 구현 완료: `CliAuthManager`가 초기화 Promise와 공급자별 인증 상태를 메모리에 유지하도록 바꾸고, 상태 API는 로그인 PTY의 실행·종료 정보만 동적으로 합친다. 인증 PTY 종료 때 해당 공급자만 공식 상태 명령으로 한 번 재검사한 뒤 웹소켓 이벤트를 보내며, 클라이언트의 10초 타이머는 제거했다.
- 후속 검증: 타입 검사와 인증·연동·운영 집중 테스트 14개, 전체 테스트 48개 파일 295개와 프로덕션 빌드를 통과했다. 개발 서버가 96초 이상 실행되어 기존 10초 주기가 여러 번 지난 뒤에도 `claude auth status`·`claude mcp get`·`claude --version` 자식 프로세스가 다시 생성되지 않은 것을 확인했다.

## 2026-08-01 Codex - 대시보드 시스템 지표 수집 CPU 경량화

- 작업 시작: 개발 서버 `node`가 시스템 지표 수집 시 1코어 기준 순간 18~35%까지 사용하는 현상을 점검하고, PTY 작업 실행 부하와 서버 자체 주기 작업을 분리해 최적화한다.
- 측정 결과: 대량 출력 없는 10초 구간에서 서버 Node는 1코어 기준 평균 5.5%(4코어 전체 기준 약 1.4%)를 사용했다. `systeminformation`의 CPU·메모리·디스크·네트워크·프로세스·시간 조회를 개별 측정하면 한 묶음당 Node CPU 약 58ms가 5초마다 집중됐고, 200ms 표본에서는 18~35% 순간값으로 확대돼 보였다. PTY에서 실행되는 명령 자체는 Codex·Claude 자식 프로세스에 집계되지만, 작업 중 TUI 출력의 headless 화면 반영·WebSocket 중계는 서버 Node에 추가된다.
- 변경 결정: CPU·메모리·네트워크와 화면 브로드캐스트는 5초를 유지하고, 프로세스 목록·tmux PID 연결은 15초, 디스크는 60초로 낮춘다. 불필요한 `systeminformation.time()`은 Node 기본 uptime으로 대체해 대시보드 응답성과 프로세스 연결 기능을 유지하면서 주기적 집중 부하를 줄인다.
- 개발 모드 추가 원인: 20초·200ms 표본에서 지표 주기 사이에도 잦은 스파이크가 남아 실행 설정을 확인했다. `npm run dev`의 300ms Chokidar 폴링 설정이 같은 Node 프로세스 안의 Vite 파일 감시에도 전파돼, 작업이 없어도 프런트엔드 트리를 초당 세 번 이상 확인하고 있었다. 서버 코드 재시작용 tsx watch는 기존 감도를 유지하고 Vite HMR 폴링만 1초로 낮춰 개발 반영성과 유휴 CPU 사이 균형을 잡는다.
- 구현 완료: `SystemMetricsService`에 테스트 가능한 런타임·시계 경계를 두고 최초 전체 수집 뒤 빠른 지표만 5초마다 갱신하도록 분리했다. 프로세스 전체 조회와 tmux pane 연결은 15초 스냅샷, 디스크는 60초 스냅샷을 재사용하고 uptime은 `os.uptime()`으로 계산한다. Vite 개발 파일 감시는 Docker 폴링을 유지하되 명시적으로 1초 간격을 적용했다.
- 실측 결과: 동일하게 현재 Codex TUI가 작업 중인 10초 구간에서 서버 Node 평균은 1코어 기준 5.5%에서 4.0%로 약 27% 감소했다. 남은 부하는 작업 중 PTY 화면 반영·WebSocket 중계와 개발 Vite/HMR을 포함하며, 실제 명령 계산은 계속 Codex·Claude 자식 프로세스에 별도 집계된다.
- 검증 완료: 기존 tmux pane pid 직접·자식 연결·미연결 테스트 3개를 보존한 채 수집 주기·캐시 및 Vite 1초 폴링 테스트를 추가했다. 타입 검사, 전체 48개 파일 297개 테스트, 프로덕션 빌드와 공개 경로 검사를 통과했다.

## 2026-08-03 Codex - 채팅별 Git 브랜치·worktree 전환

- 작업 시작: 채팅마다 현재 연결된 Git 브랜치와 작업 경로를 확인하고, 채팅 화면과 GitHub 탭에서 브랜치를 변경할 수 있도록 한다. 같은 프로젝트의 여러 채팅이 서로 다른 브랜치를 동시에 사용할 수 있는 worktree 방식도 함께 지원 범위에 포함한다.
- 구현 원칙: 기존 프로젝트 경로를 공유하는 일반 checkout과 채팅 전용 worktree를 명확히 구분하고, 실행 중 터미널·미커밋 변경·경로 보안에 영향을 주는 전환은 서버에서 검증한 뒤 적용한다.
- 외부 worktree 결정: AI가 직접 만든 worktree도 `git worktree list --porcelain`로 발견하고, tmux 현재 경로가 일치하면 채팅에 연결한다. 생성 주체를 확정할 수 없는 worktree는 임의 배정하지 않고 미연결로 표시해 관리자가 채팅에 연결하도록 한다.
- 이슈 등록: 공개 저장소에 enhancement 이슈 #27 `채팅별 Git 브랜치와 worktree 관리 지원`을 만들고 공유 checkout·전용 worktree·외부 worktree·안전 차단·UI·테스트 수용 조건을 기록했다.
- 서버 구현 완료: `chats.git_branch/worktree_path` 마이그레이션과 `GitWorkspaceService`를 추가하고, 채팅 시작·복원 PTY와 Git·gh API가 선택 채팅의 실제 작업경로를 사용하도록 연결했다. 앱 관리 worktree는 데이터 디렉터리 아래에 만들고 깨끗할 때만 제거하며, 외부 worktree는 Git 등록 여부와 중복 연결을 검증한 뒤 DB 연결만 관리한다.
- 생명주기 결정: 공유 checkout 브랜치 변경은 공유 채팅 전체에 반영하고 전용 worktree는 채팅 하나에만 적용한다. 실행 중 채팅과 미커밋 작업공간은 전환·제거를 거부하며, 전용 worktree 채팅 첨부는 공유 프로젝트에 원본을 보존하면서 PTY가 같은 상대경로로 읽을 수 있게 worktree에도 복제한다.
- 클라이언트 구현 완료: 채팅 목록에 브랜치를 표시하고 채팅·GitHub 탭에 공용 브랜치 컨트롤을 연결했다. 최초 구현이 브랜치·경로·입력 전체를 상시 노출해 채팅 헤더를 과도하게 차지한다는 사용자 피드백과 첨부 이미지를 확인해, 현재 브랜치·공유/전용 배지·설정 아이콘만 한 줄로 유지하고 변경할 때만 상세 컨트롤을 펼치는 구조로 축소했다.
- 검증 완료: 실제 임시 Git 저장소에서 공유 `main` 보존, 두 채팅의 독립 브랜치, 외부 AI 생성 worktree 발견·연결, 실행 중·dirty 차단을 검증했다. 타입 검사, 전체 단위 테스트 49개 파일 302개, Playwright 15개 통과(실계정 로그인 1개 환경조건 건너뜀), 1440px·390px UI 넘침과 45px 미만 접힌 높이, 프로덕션 빌드와 공개 경로 검사를 통과했다.

## 2026-08-03 Codex - 커밋된 브랜치 diff 표시

- 작업 시작: AI가 별도 worktree에서 변경을 커밋한 뒤 프로젝트 checkout을 해당 브랜치로 전환했지만, GitHub 패널에 변경 파일과 diff가 보이지 않는 사례를 확인한다.
- 원인 확인: 기존 Diff 화면은 `git diff HEAD`로 미커밋 작업 트리만 조회해, 기준 브랜치 이후 이미 커밋된 변경은 정상적으로 빈 결과가 됐다. 실제 작업 브랜치에는 기준 브랜치 이후 커밋이 존재했다.
- 구현 결정: 기존 작업 파일 diff는 유지하고 원격 기본 브랜치 또는 `dev/develop/main/master`를 기준으로 한 `base...HEAD` 브랜치 diff를 별도 보기로 제공한다. 대용량 변경은 파일별 접힘 지연 렌더링을 재사용하며, 실제 저장소에서 사용하는 `feat/#567` 형식도 안전한 Git 인자 검증 범위에 포함한다.
- 구현 완료: 선택 채팅의 실제 worktree에서 비교 기준·브랜치 커밋·파일별 diff를 반환하는 API를 추가하고, Diff 패널에 `작업 파일/브랜치 변경` 전환과 비교 기준 선택을 연결했다. 커밋 diff가 있으면 채팅별 최초 진입에서 브랜치 변경 보기를 자동 선택하며, 모바일 비교 헤더는 긴 브랜치명과 보기 모드가 겹치지 않게 두 줄로 배치했다.
- 회귀 수정: Git 저장소가 아닌 프로젝트에서도 브랜치 조회 전 임시 상태 줄이 렌더링됐다가 사라지며 채팅 스크롤을 21px 이동시키는 현상을 전체 UI 테스트에서 발견했다. 실제 Git 작업공간 응답을 받은 뒤에만 컨트롤을 렌더링하도록 바꿔 레이아웃 이동을 제거했다.
- 검증 완료: 타입 검사, 전체 단위 테스트 49개 파일 304개, 프로덕션 빌드와 공개 경로 검사, Playwright 15개 통과(실계정 로그인 1개 환경조건 건너뜀)를 완료했다. 깨끗한 `feat/#567` 브랜치의 커밋 diff, 자동 브랜치 보기, 390px 화면의 문서·diff 가로 넘침과 기존 채팅 스크롤 보존을 포함한다.

## 2026-08-03 Codex - 브랜치 비교 롤백과 작업공간 메뉴 재설계

- 작업 시작: 사용자는 기존 최근 커밋 목록과 커밋 상세 diff로 커밋된 변경을 확인할 수 있으므로 별도 `base...HEAD` 브랜치 비교 기능을 제거하고, 첨부 화면에서 과도한 전체 폭 브랜치 행을 다시 설계해 달라고 요청했다.
- 변경 결정: GitHub Diff는 미커밋 작업 파일과 기존 커밋 상세 보기만 유지한다. 채팅의 브랜치 상태는 제목 아래 공급자·상태 메타 영역에 작은 메뉴로 통합하고, 브랜치·worktree 변경 입력은 메뉴를 눌렀을 때만 떠 있는 설정 패널로 표시해 대화 영역 높이를 점유하지 않도록 한다.
- 구현 완료: `/git/compare` API와 비교 기준·브랜치 변경 diff UI·관련 테스트를 제거했다. 채팅의 전체 폭 브랜치 행은 제목 메타 안의 아이콘·브랜치명·공유/전용 태그 메뉴로 교체하고, GitHub 화면도 같은 메뉴를 사용한다. 설정은 경로·브랜치·작업공간 방식·새 브랜치·외부 worktree를 정돈한 팝오버로 제공하며 모바일에서는 화면 안에 고정하고 명시적 닫기 버튼을 노출한다.
- 검증 완료: 타입 검사, 전체 단위 테스트 49개 파일 303개, 프로덕션 빌드와 공개 경로 검사, Playwright 15개 통과(실계정 로그인 1개 환경조건 건너뜀)를 완료했다. 1440px 채팅 제목 메타 배치와 390px 메뉴·Diff 화면의 가로 넘침, 모바일 팝오버 경계·닫기, 기존 채팅 스크롤 보존을 포함한다.

## 2026-08-04 Codex - 완료된 Claude 훅 승인 닫기 복구

- 작업 시작: 채팅 #160에서 AskUserQuestion 응답 뒤 Claude가 후속 작업과 최종 답변까지 완료했지만, PermissionRequest HTTP 훅 대기자가 메모리에 남아 `닫기`를 실제 대기 중 요청으로 오판하는 문제를 수정한다.
- 변경 결정: Claude 기록에서 턴 종료가 확인되면 해당 채팅의 남은 훅 승인을 공급자 입력 없이 정리한다. 기존에 완료된 요청은 채팅의 `busy=false`를 함께 확인해 웹 목록에서 닫을 수 있게 하고, 실제 작업 중인 훅 요청은 기존처럼 닫기를 차단한다.
- 구현 완료: 기록 동기화가 Claude `turn_end`를 확인하면 해당 채팅의 pending PermissionRequest 훅만 자동 거부·해제한다. 수동 닫기는 요청 뒤 채팅 기록이 갱신되고 `busy=false`가 된 경우에만 유실 대기로 판정하며, 훅 생성 직후 동기화 전 idle 구간과 실제 작업 중 요청은 계속 차단한다.
- 현장 확인: 개발 서버 반영 후 #160의 잔여 요청은 `dismissed`로 바뀌었고 pending 요청이 0건임을 확인했다.
- 검증 완료: 집중 테스트 11개와 타입 검사를 반복 통과했고, 최종적으로 전체 49개 파일 307개 테스트, 프로덕션 빌드와 공개 경로 검사를 통과했다.

## 2026-08-05 Codex - 공급자 사용량 세션 초기화 알림

- 작업 시작: Claude·Codex의 대표 사용량 창 초기화 시각이 지나면 실제 사용량을 다시 확인하지 않고 1분 뒤 자동 알림을 보내며, 정기 사용량 조회에서 그보다 먼저 새 창 전환이 감지되면 즉시 알림을 보내도록 한다.
- 변경 결정: 기존 rate-limit 대기 채팅 재개 알림과 분리된 공급자 단위 스케줄러를 둔다. 대표 창은 공급자 메타의 `usageWindowId`를 사용해 Claude는 현재 세션, Codex는 현재 정책대로 주간 한도를 추적하고, 발송 상태를 DB에 저장해 서버 재시작과 조기 감지·예정 타이머 경합에서도 한 번만 알린다.
- 구현 완료: `UsageResetNotifier`가 Claude 타임존·Codex 날짜 포함 초기화 문구를 절대 시각으로 변환해 공급자별 예약을 저장한다. 예정 시각 1분 뒤 타이머는 CLI나 사용량을 재확인하지 않고 Slack·ntfy·브라우저 알림을 보내며, UsageMonitor가 정상 결과에서 다음 창을 먼저 발견하면 즉시 알리고 이전 타이머를 취소한다.
- 중복·오탐 방지: DB의 `notified_at`을 먼저 선점하고 초기화 시각 기반 안정 이벤트 ID를 사용한다. 서버 시작 시 stale 사용량은 새 창으로 판정하지 않으며, 같은 창의 15분 이내 표기 오차와 대표 창 정책 변경도 초기화로 알리지 않는다.
- 현장 확인: 14003 개발 서버에서 Claude `session`과 Codex `weekly` 예약이 각각 미래 절대 시각으로 저장되고 서버 오류 없이 유지되는 것을 확인했다.
- 검증 완료: 초기화 시각+1분 무검사 발송, 조기 전환, 이전 타이머 취소, 재시작 중복 방지와 날짜·타임존 파싱 테스트를 포함해 전체 50개 파일 312개 테스트, 타입 검사, 프로덕션 빌드와 공개 경로 검사를 통과했다.
- 후속 작업 시작: 사용자 요청에 따라 Claude의 `Current week (all models)` 초기화도 알림 대상으로 추가한다. 기존 공급자당 단일 예약 구조를 창별 다중 예약으로 확장하되 기존 세션·Codex 주간 예약은 보존한다.
- 후속 구현 완료: 공급자 어댑터에 초기화 알림 대상 창 목록을 추가해 Claude는 `session`·`weekly_all`, Codex는 `weekly`를 추적한다. 예약 DB와 타이머를 공급자+창 복합 키로 확장하고 기존 단일 키 테이블은 행을 보존한 채 자동 마이그레이션한다. 알림 문구와 브라우저 제목도 세션·주간을 구분한다.
- 후속 현장 확인: 14003 서버의 실제 DB에서 복합 기본키와 Claude 세션·전체 모델 주간, Codex 주간 예약 3개가 모두 독립적으로 저장된 것을 확인했다.
- 후속 검증 완료: Claude 세션·주간 동시 발송과 기존 DB 마이그레이션 회귀 테스트를 추가해 전체 50개 파일 314개 테스트, 타입 검사, 프로덕션 빌드와 공개 경로 검사를 통과했다.

## 2026-08-05 Codex - diff 언어별 문법 색상

- 작업 시작: Git·GitHub·채팅 diff에 파일 언어별 문법 색상 팔레트를 적용하고 수정 후 커밋한다.
- 변경 결정: 파일 경로에서 언어를 판정하고 통합·분할 보기의 코드 본문만 토큰화한다. 기존 추가·삭제·hunk 배경과 대용량 파일별 지연 렌더링은 유지하며, 지원하지 않는 확장자와 바이너리 diff는 일반 텍스트로 대체한다.
- 구현 완료: Shiki JavaScript 정규식 엔진과 GitHub Light/Dark 테마를 추가하고 TypeScript·JavaScript·Python·Go·Rust 등 주요 파일 형식을 경로에서 판정한다. 통합 보기는 변경 기호를 제외한 코드, 분할 보기는 이전·이후 열을 각각 토큰화하며 밝은 Git 패널과 어두운 채팅 상세에 맞는 색상을 적용한다.
- 성능 결정: 기본 번들에 문법 엔진을 포함하지 않고 최초 diff에서 코어·테마를, 각 언어는 실제 사용 시 한 번만 동적 로드한다. 통합·분할 렌더러를 분리해 선택하지 않은 모드의 행 변환과 토큰화를 피하고, PR은 기존처럼 펼친 파일만 렌더링한다.
- 검증 완료: 파일·패치 언어 판정과 양 테마 토큰 단위 테스트를 포함해 전체 50개 파일 316개 테스트, 타입 검사, 프로덕션 빌드·공개 경로 검사, 런타임 의존성 감사 0건을 통과했다. Playwright는 15개 통과(실계정 로그인 1개 환경조건 건너뜀)했으며 통합·분할 토큰 색상, 긴 줄 열 경계와 모바일 가로 넘침을 실제 화면에서 확인했다.

## 2026-08-05 Claude - 내부망 접속 시 민감 경로 전면 노출

- 작업 시작: 사용자가 192.168.0.31로 접속했는데 파일 탭에 `.env`가 보이지 않는다고 보고했다. 원인을 조사한 결과 `assertNonSensitiveRelativePath`가 `.env*`와 `.git`·`.claude`·`.codex`·업로드 폴더·agents.md/claude.md를 `allowHidden` 검사보다 먼저 무조건 차단하고 있어, 내부망 예외는 그 외 일반 점 파일에만 적용되고 있었다. 기억하던 정책과 실제 구현이 어긋난 상태였다.
- 변경 결정: 사용자가 선택지 중 "`.env` + `.git`/`.claude`/`.codex` 전부 허용"과 "일반 파일과 동일하게 목록·미리보기·다운로드·업로드 전부 허용"을 선택했다. 따라서 무조건 차단 규칙을 없애는 대신 `allowHidden`(= `request.trustedNetwork`)이 참일 때 민감 경로 검사 전체를 건너뛰도록 바꾼다. 외부망 요청은 기존 2단 차단을 그대로 유지한다.
- 구현 완료: `assertNonSensitiveRelativePath` 맨 앞에서 `access.allowHidden`이면 즉시 통과시키고, 뒤따르던 `!access.allowHidden` 조건은 죽은 검사라 정리했다. 파일 라우터의 목록·미리보기·콘텐츠·다운로드·업로드·ZIP 경로가 모두 `fileAccess(request)`를 이미 넘기고 있어 이 한 곳만 고쳐도 일괄 적용된다. 프로젝트 경계 검증과 symlink 실제 경로 확인은 내부망에서도 그대로 유지된다.
- 테스트 갱신: 내부망 민감 경로 허용과 외부망 차단을 각각 고정하도록 `tests/path-security.test.ts`와 `tests/file-routes.test.ts`를 바꿨다. 기본값이 내부망이던 symlink 차단 테스트는 외부망 기준으로 옮기고, 내부망이어도 프로젝트 밖을 가리키는 symlink는 계속 차단하는 회귀 테스트를 새로 추가했다.
- 현장 확인: 임시 데이터 디렉터리와 검증 전용 관리자 계정으로 서버를 띄워 실제 HTTP로 확인했다. 내부망(127.0.0.1)에서는 로그인 응답 `networkTrusted:true`와 함께 `.env`·`.git`·`.claude`가 목록에 나오고 `.env` 미리보기·다운로드가 200으로 성공했으며, `WEB_AGENT_MANAGER_TRUSTED_NETWORKS`를 다른 대역으로 지정해 외부망으로 취급시킨 서버에서는 목록에 일반 파일만 남고 `.env` 다운로드와 `.gitignore` 미리보기가 400으로 거부됐다. 파일 탭 화면 캡처는 `artifacts/env-visible-internal.png`에 남겼다.
- 검증 완료: 50개 파일 317개 테스트, 타입 검사, 프로덕션 빌드와 공개 경로 검사를 통과했다.

## 2026-08-05 Claude - reverse proxy 뒤 내부망 오판정 수정

- 문제 발견: 직전 커밋(b84a45d)으로 내부망에 민감 경로를 연 뒤, 사용자가 외부 도메인 `agent.qloug.com`으로 접속해도 `.env` 등이 다 보인다고 보고했다. 확인 결과 `WEB_AGENT_MANAGER_TRUSTED_PROXIES`가 설정되어 있지 않아 `app.set("trust proxy", ...)`가 적용되지 않았고, `request.ip`가 방문자가 아니라 프록시 자신(loopback·172.17.x.x)으로 잡혀 기본 신뢰 대역과 항상 일치했다. 즉 프록시를 거친 모든 외부 접속이 내부망으로 판정되고 있었다. 이 오판정 자체는 이전부터 있었고(외부에서 일반 점 파일이 이미 보이던 상태) 직전 변경이 그 위에 민감 파일까지 얹은 것이다.
- 변경 결정: 운영 환경 설정을 바꾸라고 안내하는 대신 코드 기본값을 fail-closed로 바꾼다. 신뢰 프록시를 지정하지 않았는데 `X-Forwarded-For`·`Forwarded`·`X-Real-IP`·`CF-Connecting-IP`·`True-Client-IP` 중 하나라도 붙어 있으면 실제 출처를 알 수 없다고 보고 내부망 판정을 포기한다. 헤더가 없는 `192.168.x.x` 직접 접속은 그대로 내부망이고, 프록시를 거쳐도 실제 주소로 판정하려면 `TRUSTED_PROXIES`를 설정하면 된다. 민감 경로 노출 정책 자체(직전 커밋)는 사용자가 선택한 대로 유지한다.
- 구현 완료: `createNetworkCapability`에 `trustProxyConfigured` 인자를 추가하고 `index.ts`에서 `config.trustedProxies` 설정 여부를 넘긴다. 전달 헤더 판정은 `isUnverifiedProxyRequest`로 분리했다.
- 현장 확인: 재시작된 14003 서버에서 내부망 전용 엔드포인트(`/api/auth/setup`)가 헤더 없는 요청은 통과시키고 `X-Forwarded-For`를 붙인 요청은 "초기 설정은 내부망에서만 가능합니다"로 거부하는 것을 확인했으며, 실제 `https://agent.qloug.com` 경유 요청도 동일하게 외부망으로 판정됐다.
- 검증 완료: 프록시 헤더별 판정과 신뢰 프록시 설정 시 실제 클라이언트 주소 판정 테스트를 추가해 50개 파일 319개 테스트, 타입 검사, 프로덕션 빌드와 공개 경로 검사를 통과했다.
- 운영 주의: 이 수정 이후 `agent.qloug.com`으로는 민감 경로가 보이지 않는다. 도메인 경유로도 내부망 기능을 쓰려면 프록시 자체 주소를 `WEB_AGENT_MANAGER_TRUSTED_PROXIES`에 설정해야 하고, 그렇게 하면 실제 방문자 IP로 판정되므로 외부 IP 접속은 여전히 차단된다.
- 후속 작업(문서): 사용자가 신뢰 프록시 설정의 의미를 물어(“지정해두면 그 프록시 요청이 내부 IP와 동일 권한인가”) 오해 소지를 확인해, 설정 방법과 실제 동작을 문서에 명시하기로 했다. `TRUSTED_PROXIES`는 요청을 통과시키는 스위치가 아니라 전달된 방문자 주소를 신뢰하는 설정이라, 지정해도 도메인 접속은 방문자 주소가 공인 IP여서 여전히 외부망이다.
- 후속 완료: 지금까지 예시가 아예 없던 `.env.example`에 `WEB_AGENT_MANAGER_TRUSTED_NETWORKS`·`WEB_AGENT_MANAGER_TRUSTED_PROXIES`를 기본 동작과 오설정 위험(프록시 범위를 넓게 잡으면 `X-Forwarded-For` 위조로 내부망 판정을 얻을 수 있음) 주석과 함께 추가했다. README에는 두 변수의 역할 구분과 도메인 경유로 내부망을 열려면 둘 다 필요하지만 권장하지 않는 이유를, CODETREE에는 `config.ts` 설명 보강과 `.env.example` 행을 반영했다.

## 2026-08-05 Claude - 파일 탭 텍스트 편집

- 작업 시작: 사용자 요청으로 파일 탭 미리보기에서 텍스트 형식 파일(.txt·.env·.json 등)만 직접 편집·저장할 수 있게 한다. 이미지·영상·PDF·압축파일 등 비텍스트 형식은 편집 대상에서 제외한다.
- 변경 결정: 편집 가능 여부를 새 확장자 목록으로 따로 만들지 않고 기존 `previewKind` 판정을 그대로 재사용해 `markdown`·`text`일 때만 허용한다(중복 판정 방지). 미리보기가 256KiB에서 잘린 파일은 편집 화면에 전체 내용이 없어 저장 시 파일이 잘리므로 서버·클라이언트 양쪽에서 편집을 막는다. 권한은 기존 파일 업로드·다운로드와 같은 관리자 전용으로 두고, 민감 경로 정책은 직전 작업대로 내부망에서만 허용되는 `fileAccess`를 그대로 적용한다.
- 구현 완료: `PUT /projects/:id/files/content`를 관리자 전용으로 추가해 `previewKind`가 `text`·`markdown`인 파일만 저장하고, 디렉터리·이진 파일·256KiB 초과 파일은 거부한다. 지침 라우터에만 있던 원자적 쓰기 함수를 `helpers.ts`의 공용 `writeFileAtomic`으로 올려 두 곳이 같은 구현을 쓰게 했고, 기존 파일을 저장할 때 원래 권한 모드를 유지하도록 보강했다(지침 편집에도 함께 적용됨).
- 클라이언트 결정: 미리보기 패널 헤더에 연필 버튼을 두고 편집기는 같은 패널 안에서 열어 파일 탐색 맥락을 유지한다. 편집 중 다른 파일·폴더로 이동하거나 미리보기를 닫으면 변경을 버릴지 확인해 실수로 유실되지 않게 했고, 저장 후에는 미리보기와 목록의 크기·수정 시각을 함께 갱신한다.
- 현장 확인: 검증 서버에서 `.env`를 웹으로 저장해 실제 파일 내용이 바뀌는 것과 PNG 저장 요청이 거부되는 것을 확인했다. Playwright로 note.txt를 편집·저장해 목록 크기가 19B에서 54B로 갱신되는 화면(`artifacts/file-edit-mode.png`, `artifacts/file-edit-saved.png`)과 390×844 모바일에서 편집기·저장 버튼이 화면 안에 들어오고 가로 넘침이 없는 것(`artifacts/file-edit-mobile.png`)을 확인했다.
- 검증 완료: 편집 저장 회귀 테스트 2건을 추가해 50개 파일 321개 테스트, 타입 검사, 프로덕션 빌드와 공개 경로 검사를 통과했다.

## 2026-08-06 Claude - GitHub 탭 기본 Diff·조회 캐시·diff 가독성

- 작업 시작: 사용자 요청 3건을 한 번에 처리한다. (1) GitHub 탭의 기본 하위 탭을 `저장소`에서 `Diff`로 바꾼다. (2) 저장소 상태와 GitHub 조회가 매번 git·gh CLI를 기다려 탭 진입이 느리므로 결과를 캐시하고 1분 주기로 갱신한다. (3) diff 화면 폰트 가독성을 개선한다.
- 캐시 결정: 서버에 stale-while-revalidate 캐시를 두어 첫 조회만 실제 CLI를 기다리고 이후에는 저장된 값을 즉시 돌려주며, 60초가 지난 값은 응답을 막지 않고 뒤에서 갱신한다. 클라이언트는 Diff·현재 저장소 탭을 보고 있고 문서가 화면에 보이는 동안에만 60초 주기로 다시 조회한다(숨은 탭에서 gh 호출을 반복하지 않기 위함). 커밋·push·브랜치 전환·이슈/PR 쓰기 등 GET이 아닌 요청이 성공하면 해당 프로젝트 캐시를 즉시 버려 옛 상태가 1분간 남지 않게 한다.
- 폰트 결정: diff 본문 폰트를 UI 라벨용 DM Mono에서 시스템 코딩 폰트(`--font-code`)로 바꾸고 분할 보기 11px→12.5px, 통합 보기 12px→13px, 행간 1.55→1.65로 키웠다. 추가·삭제 줄은 GitHub처럼 배경색으로만 구분하고 글자는 진한 중립색으로 되돌렸다 — 기존에는 글자까지 초록·빨강이라 문법 색상이 없는 파일에서 코드 자체가 잘 읽히지 않았다.
- 구현 완료: `GitDataCache`를 추가하고 `/git`·`/git/changes`·`/github` 세 조회에 적용했다. 라우터 앞단 미들웨어가 GET이 아닌 요청의 성공 응답을 감지해 프로젝트 캐시를 버리므로 개별 쓰기 핸들러는 손대지 않았다. 응답에 `cachedAt`을 실어 화면 헤더에 기준 시각을 표시한다.
- 현장 확인: 실제 저장소를 등록해 GitHub 조회가 첫 882ms에서 캐시 3.7ms로 줄고 `?refresh=1`은 다시 973ms로 실제 CLI를 호출하는 것을 확인했다. 별도 임시 저장소에서 커밋 API 호출 직후 변경 목록에서 해당 파일이 즉시 사라져 무효화가 동작하는 것도 확인했다. Playwright로 GitHub 탭이 Diff로 열리고 캐시 기준 시각이 표시되며 분할 diff 폰트가 12.5px 시스템 코딩 폰트로 적용된 것을 확인했다(`artifacts/git-diff-default.png`, `artifacts/git-diff-split.png`, `artifacts/git-cached-header.png`).
- 검증 완료: 캐시 단위 테스트 6건을 추가해 51개 파일 327개 테스트, 타입 검사, 프로덕션 빌드와 공개 경로 검사를 통과했다.

## 2026-08-06 Claude - 대시보드 메모리 단위 수정·유휴 채팅 자동 종료

- 조사 결과: 사용자가 "대시보드에서 claude가 메모리를 적게 먹는 것으로 보인다"고 해서 확인한 결과, `systeminformation`의 `memRss`는 KiB 단위인데 클라이언트가 `bytes()`로 바이트처럼 포맷해 실제보다 1024배 작게 표시되고 있었다(claude 421MB가 0.4MB로 표시). 서버에서 바이트로 변환해 내보내도록 고쳤다.
- 작업 시작: 하루 동안 아무 활동이 없는 채팅 터미널을 주기적으로 자동 종료한다. 사용자가 선택지 중 "관리자 설정으로 켜고 끄기·기준 시간 조정"과 "알림 없이 감사 로그만 기록"을 선택했다.
- 안전 결정: 자동 종료는 되돌릴 수 없으므로 실제로 살아 있는 작업을 죽이지 않는 것을 최우선으로 한다. 대상은 `status = 'running'`인 채팅으로 한정하고, 작업 중(`busy = 1`), 리밋 재개 대기(`rate_limit_waits`에 있음), 응답 대기 중인 승인 요청(`approvals.status = 'pending'`)이 있으면 유휴 시간과 무관하게 제외한다. 마지막 활동 시각은 DB의 `chats.updated_at`과 실제 기록 파일(`history_file`)의 mtime 중 더 최근 값을 쓴다 — DB 동기화가 늦어져도 실제 대화가 있었으면 살아 있는 것으로 판정하기 위함이다.
- 구현 완료: `IdleChatReaper`가 10분 주기로 정책을 읽어 대상 채팅을 종료하고 `chat.idle_auto_stop` 감사 로그를 시스템 주체(user_id NULL)로 남긴다. `SessionManager.stop`의 사용자 인자를 `AuthUser | null`로 넓혀 시스템이 스스로 실행한 종료를 표현했다. 마지막 활동 시각 파싱에서 SQLite `CURRENT_TIMESTAMP`("YYYY-MM-DD HH:MM:SS" UTC)와 history-sync가 넣는 ISO 문자열이 섞여 있어, 전자를 그대로 파싱하면 시간대만큼 어긋나므로 UTC임을 명시해 해석한다. 관리자 설정 API(`/admin/idle-chat-settings`)와 대시보드 카드를 추가하고 기준 시간은 1~720시간으로 제한했다.
- 현장 확인: 검증 서버에서 기본 정책(켜짐·24시간) 조회, 48시간으로 변경, 0시간 거부를 확인했다. 대시보드 프로세스 표의 메모리가 claude 415.6MB처럼 실제 크기로 표시되는 것(수정 전에는 0.4MiB)과 설정 카드 화면을 확인했다(`artifacts/idle-chat-settings.png`, `artifacts/process-memory.png`).
- 검증 완료: 유휴 종료 회귀 테스트 9건을 추가해 52개 파일 336개 테스트, 타입 검사, 프로덕션 빌드와 공개 경로 검사를 통과했다.

## 2026-08-06 Claude - 긴 대화에서 채팅 입력 렉 제거

- 작업 시작: 사용자가 "긴 채팅에서 브라우저 렉이 심하다, 특히 채팅 입력이 렉이고 다른 건 괜찮다"고 보고했다. 가상 스크롤(@tanstack/react-virtual)은 이미 적용돼 있어 렌더 항목 수는 제한돼 있었다.
- 원인 분석: 입력창 텍스트가 ChatView의 state라 한 글자 칠 때마다 ChatView 전체가 리렌더되는데, `MessageCard`에 memo가 없어 화면에 보이는 모든 메시지가 함께 재렌더됐다. 카드마다 `splitMessageContent`(정규식 여러 개)와 ReactMarkdown 전체 파싱이 다시 돌고, 도구 상세가 열려 있으면 DiffView의 Shiki 토큰화까지 반복됐다. `splitMessageContent`는 visibleMessages 계산에서 한 번, MessageCard에서 또 한 번 중복 실행되고 있었다.
- 결함 발견·수정(자동 종료가 재시작 후 동작하지 않던 문제): 사용자가 "서버를 재시작했는데 왜 안 죽느냐"고 물어 실제 DB를 확인한 결과, 마지막 활동 시각을 `chats.updated_at`과 기록 파일 mtime 중 최신값으로 잡은 것이 역효과였다. 서버 시작 시 `sessions.restore()`가 tmux를 다시 붙이며 `setStatus`로 `updated_at = CURRENT_TIMESTAMP`를 찍어, 27시간 논 채팅이 재시작 직후 "21분 전 활동"으로 보였다(실측: id=148은 JSONL 08-05 03:16인데 DB는 08-06 04:17). 재시작을 반복하는 동안 자동 종료가 영영 발동하지 않는 구조였다. 실제 대화가 기록되는 JSONL mtime을 1순위로 쓰고 기록 파일을 못 읽을 때만 DB 시각으로 대체하도록 바꿨다. 또 `start()`가 주기만 걸어 첫 검사가 10분 뒤였던 것도 재시작 30초 뒤 1회 즉시 검사하도록 고쳤다(restore가 tmux를 붙이는 중에 끼어들지 않게 30초 지연).
- 현장 확인: 실제 운영 DB를 읽기 전용으로 열어 판정만 시뮬레이션한 결과, 수정 후에는 재시작 직후에도 각 채팅의 실제 유휴 시간(최대 18.3시간)이 정확히 계산됐다. tmux 9개가 모두 DB와 매칭돼 서버가 모르는 고아 세션은 없었다.
- 검증 완료: 재시작 회귀 테스트를 추가해 52개 파일 337개 테스트, 타입 검사, 프로덕션 빌드와 공개 경로 검사를 통과했다.

## 2026-08-06 Claude - 대시보드 프로세스 그룹 표시

- 작업 시작: 사용자 요청으로 대시보드 프로세스 표를 개별 나열에서 그룹 단위로 바꾼다. 한 채팅에 딸린 tmux·node·claude/codex 프로세스를 채팅 하나로 묶고, 채팅에 속하지 않는 web-agent-manager 시스템 프로세스(서버 본체·MCP 브리지·사용량 조회 PTY 등)도 구분해서 볼 수 있게 한다.
- 분류 결정: 서버가 이미 tmux pane pid에서 자손으로 채팅을 전파하는 `linkProcessesToChats`를 갖고 있으므로 같은 방식으로 서버 프로세스(process.pid) 자손을 시스템으로 전파한다. 채팅 매핑을 먼저 적용하고 남은 것 중 시스템을 판정해, 채팅에 속한 프로세스가 시스템으로 흡수되지 않게 한다. tmux 서버 데몬처럼 어느 쪽에도 안 붙지만 WAM이 띄운 프로세스는 시스템으로 본다.
- 구현 완료: 서버가 프로세스마다 `group`(kind: chat·system·other, key, label)을 계산해 스냅샷에 함께 내보낸다. `classifySystemProcesses`는 채팅 매핑에 쓰인 pid 집합을 먼저 받아 제외하므로 채팅 소속이 시스템으로 흡수되지 않는다. 클라이언트는 묶음 행을 기본 접힌 상태로 보여주며 CPU·메모리 합계와 프로세스 개수를 표시하고, 펼치면 개별 프로세스와 종료 버튼이 나온다. 묶음 순서는 채팅 → 시스템 → 기타로 고정하고 선택한 정렬 기준은 묶음 안과 동종 묶음 사이에만 적용한다.
- 현장 확인: 검증 서버에 실제 tmux 세션을 running 채팅으로 등록해 채팅 3개·시스템·기타 5개 묶음이 접힌 상태로 표시되고(`artifacts/process-groups-collapsed.png`), 시스템 묶음을 펼치면 claude·node·codex·tmux 14개가 종료 버튼과 함께 나오는 것을 확인했다(`artifacts/process-groups-expanded.png`).
- 검증 완료: 묶음 분류 테스트 4건을 추가해 52개 파일 341개 테스트, 타입 검사, 프로덕션 빌드와 공개 경로 검사를 통과했다.

## 2026-08-06 Claude - 기타 프로세스 정체 확인·묶음 일괄 종료

- 조사 요청: 사용자가 대시보드의 "기타 프로세스 9개"가 무엇이고 꺼도 되는지, 일괄 종료 버튼이 있는지 물었다. 실제 서버 PID로 분류를 재현한 결과 기타 9개는 (1) 터미널에서 직접 띄운 Claude Code 세션 4개(부모가 bash, 합계 1.4GB — 지금 이 대화 포함), (2) 그 세션들이 띄운 web-agent-manager MCP 브리지 4개, (3) `tsx watch` 부모 프로세스(pid 35)였다.
- 결함 발견: pid 35는 개발 서버를 띄운 web-agent-manager 자신인데도 기타로 분류됐다. 서버 본체의 *부모*라 자손 전파 방식으로는 잡히지 않았기 때문이다.
- 전파 실험: 부모를 SIGTERM으로 종료해도 자식은 고아로 살아남는 것을 두 가지 형태(단순 fork, stdio 파이프 연결)로 실측 확인했다. 따라서 "부모만 죽이면 자식도 죽는다"는 전제는 성립하지 않아, 묶음에 속한 프로세스를 모두 종료하는 일괄 버튼을 추가한다.
- 구현 완료: `classifySystemProcesses`가 서버 자손뿐 아니라 조상도 따라 올라가 시스템에 포함하되, 화면 표시 대상 이름(node·tmux·claude·codex)이 아닌 조상(셸 등)을 만나면 멈춰 무관한 프로세스를 삼키지 않게 했다. 기타 묶음 라벨을 "기타 프로세스 (앱이 띄우지 않음)"으로 바꿔 함부로 종료하면 안 된다는 것을 화면에서 알 수 있게 했다. 묶음 행에 일괄 종료·강제 종료 버튼을 추가했고, 확인창에 대상 프로세스 이름과 PID를 모두 나열한 뒤 자식(큰 pid)부터 순서대로 기존 개별 종료 API를 호출한다(전용 API를 새로 만들지 않음).
- 현장 확인: 묶음 종료 버튼이 "기타 프로세스 35개를 종료(SIGTERM)할까요?"와 claude·node PID 목록을 확인창에 보여주는 것을 확인하고 취소했다(`artifacts/process-group-kill.png`). 실제 종료는 하지 않았다.
- 검증 완료: 조상 포함·조상 중단 회귀 테스트를 추가해 52개 파일 343개 테스트, 타입 검사, 프로덕션 빌드와 공개 경로 검사를 통과했다.
- 보호 추가: 사용자 요청으로 시스템 묶음에서는 묶음·개별 종료 버튼을 모두 없애고 "앱 구동에 필요" 표시만 남겼다. 화면에서 버튼을 지우는 것만으로는 API 직접 호출 경로가 남으므로 `/system/processes/:pid/kill`도 최신 스냅샷에서 해당 pid의 묶음이 시스템이면 거부하도록 했다(기존 보호는 서버 자기 자신 pid 하나뿐이라 watch 프로세스·MCP 브리지·tmux 데몬은 막지 못했다).
- 현장 확인: 시스템 묶음 15개 행 전부에 종료 버튼이 없고 기타 묶음은 그대로 종료 가능한 것을 확인했으며(`artifacts/process-system-protected.png`), API로 tmux 데몬(38143) 종료를 직접 시도하자 거부 메시지가 돌아오고 프로세스가 그대로 살아 있는 것을 확인했다.
- 검증 완료: 시스템 거부·채팅 통과 회귀 테스트를 추가해 52개 파일 345개 테스트, 타입 검사, 프로덕션 빌드와 공개 경로 검사를 통과했다.

## 2026-08-06 Claude - 서버 프로세스 메모리 증가 원인 조사(코드 변경 없음)

- 조사 요청: 사용자가 대시보드 시스템 묶음 1.6GB, 특히 node 서버 본체가 878MB인 이유를 물었다.
- 방법: 서버를 건드리지 않고 `/proc/<pid>/smaps`의 매핑 종류별 RSS를 집계해, 9시간 실행 중인 서버와 같은 코드로 새로 띄운 서버를 비교했다.
- 결과: 증가분의 정체는 `[anon:mimalloc]`(Vite 8이 쓰는 Rust 번들러 rolldown의 네이티브 할당자)로, 기동 직후 14.9MB에서 9시간 뒤 432.8MB로 약 418MB 늘었다. 반면 V8 JS 힙을 포함한 일반 익명 매핑은 541MB에서 268MB로 오히려 줄어 JS 쪽 누수는 없었다. 즉 node·앱 코드 문제가 아니라 개발 모드에서 같은 프로세스에 내장한 Vite dev 서버가 파일 변경마다 재변환하며 네이티브 캐시를 쌓는 것이 원인이다.
- 결론: 프로덕션 빌드(`node dist/...`)로 실행하면 Vite 자체가 없어 이 영역이 통째로 사라진다(이전 측정에서 프로덕션 151MB vs 개발 419MB). 개발 모드를 유지한다면 서버를 이따금 재시작하는 것 외에 앱 코드로 줄일 여지는 없다. 코드 변경은 하지 않았다.

## 2026-08-07 Claude - GitHub식 diff 뷰 전면 리팩터링

- 작업 시작: 사용자 요청으로 Git/GitHub diff 화면을 GitHub와 같은 형태로 다시 만든다. (1) `diff --git a/... b/...`·`index ...`·`new file mode`·`--- /dev/null`·`+++ b/...` 같은 원문 헤더를 화면에서 없애고 구조화된 파일 헤더로 대체, (2) 변경되지 않은 부분(hunk 사이)을 펼치기/접기, (3) 커밋 내역을 누르면 그 커밋의 파일 목록과 파일별 diff만 보기, (4) 폰트·색·간격을 GitHub 기준으로 정리.
- 설계 결정: 문자열을 그때그때 정규식으로 훑던 방식을 버리고 `parseDiffFiles`로 파일·hunk·줄을 한 번에 구조화한 뒤 렌더러가 그 구조만 그리게 한다. 헤더 노이즈는 파싱 단계에서 상태(added·deleted·renamed·modified·binary)와 경로로 흡수해 화면에는 배지와 경로로만 남긴다. 컨텍스트 펼치기는 hunk 사이 빈 구간의 실제 원본 줄이 필요하므로 서버에 파일 내용 조회 API를 추가한다(커밋 diff는 해당 리비전, 미커밋 diff는 워킹트리 기준).
- 구현 완료: `src/client/lib/diff-parse.ts`를 새로 만들어 통합 diff를 파일→hunk→줄 구조로 파싱한다. 원문 헤더는 상태·경로·증감 통계로 흡수하고 따옴표·공백이 든 경로도 복원하며 이전·이후 줄 번호를 각각 매긴다. `diff-view.tsx`는 이 구조만 그리도록 다시 썼다 — 상태 배지·경로·+/− 통계가 있는 접이식 파일 카드, hunk 사이 `N줄 펼치기`(20줄씩 또는 전체), 통합·분할 보기. 서버에는 `/git/blob`(rev 선택)을 추가해 펼칠 원본 줄을 가져오고, 커밋 API는 메타(작성자·시각·제목·본문)와 패치를 분리해 내보낸다. GitView에서 문자열을 다시 쪼개던 `splitDiffSections`·`splitDiffByFile`·`diffPreamble`·`LazyDiffSection`은 모두 제거했다.
- 렌더링 수정: 처음에는 Shiki 토큰 span들이 flex 아이템으로 흩어져 긴 줄이 화면 오른쪽에서 세로로 흘러내렸다. 코드 텍스트를 `.diff-text` 한 요소로 감싸 줄바꿈이 정상 동작하게 고쳤다.
- 현장 확인: 미커밋 diff가 파일 카드 9개로 나오고 파일 헤더·hunk 헤더 어디에도 원문 헤더가 없는 것, `272줄 펼치기`를 눌러 8줄에서 28줄로 늘어나는 것, 커밋을 클릭하면 작성자·시각·본문과 변경 파일 목록이 먼저 나오고 파일별 diff가 이어지는 것을 확인했다(`artifacts/diff-github-file.png`, `artifacts/diff-github-expanded.png`, `artifacts/diff-github-commit.png`). `/git/blob`이 프로젝트 밖 경로를 거부하는 것도 확인했다.
- 검증 완료: diff 파싱 테스트 9건을 새로 추가하고 기존 분할 변환 테스트를 새 구조로 옮겨 53개 파일 354개 테스트, 타입 검사, 프로덕션 빌드와 공개 경로 검사를 통과했다.
- 모바일 개선(사용자 "모바일 화면도 보기 너무 불편" 피드백): 390×844에서 확인한 결과 Diff 탭이 변경 파일·커밋 메시지·최근 커밋 사이드바를 먼저 세로로 쌓아 정작 diff가 화면 한참 아래로 밀려 있었다. 좁은 화면에서만 사이드바를 접이식(`변경 파일 · 커밋` 토글, 변경 개수 요약 표시)으로 바꿔 diff부터 보이게 하고, 통합 보기의 줄 번호를 이후 파일 기준 하나만 남겨 코드 폭을 260px에서 322px로 넓혔다. 분할 보기는 찌그러지지 않도록 최소 폭 640px과 가로 스크롤을 주고, git status 출력이 diff를 밀어내지 않게 높이를 제한했다.
- 파일명 표시 수정: 경로가 길면 `src/client/features/git/GitVi…`처럼 앞부분만 남고 정작 파일명이 잘렸다. 경로를 디렉터리(축약 가능)와 파일명(고정 폭)으로 나눠 파일명이 항상 끝까지 보이게 했다.
- 현장 확인: 모바일에서 diff가 화면 상단에 바로 나오고(`artifacts/mobile-diff-after.png`), 토글로 사이드바를 펼치면 변경 파일·커밋 목록이 정상 표시되며(`artifacts/mobile-diff-sidebar.png`) 가로 넘침이 없는 것을 확인했다.
- 검증 완료: 53개 파일 354개 테스트, 타입 검사, 프로덕션 빌드와 공개 경로 검사를 통과했다.

## 2026-08-07 Claude - Diff 탭에서 브랜치·워크트리 선택 조회

- 작업 시작: 사용자 요청으로 GitHub 탭 Diff 화면에서 조회 대상 작업공간을 직접 고를 수 있게 한다. 지금은 선택된 채팅의 작업공간(chatId)에만 묶여 있어, 프로젝트 공유 checkout이나 다른 폴더로 빼둔 worktree의 커밋 내역·미커밋 변경을 보려면 채팅을 바꿔야 했다.
- 보안 결정: 조회 경로를 쿼리로 받되 임의 경로를 그대로 쓰지 않는다. `git worktree list`가 보고하는 그 프로젝트의 실제 워크트리 목록과 대조해 일치하는 것만 허용하고, 그 외에는 거부한다(경로를 직접 넘겨 프로젝트 밖을 읽는 것을 막기 위함).
- 구현 완료: `GitWorkspaceService`에 `listWorkspaces`(선택 목록)와 `resolveWorktreePath`(요청 경로를 `git worktree list` 결과와 대조해 검증)를 추가하고, `/git/workspaces` 조회 API를 새로 뒀다. 라우터의 `requestWorkspacePath`는 `worktree` 파라미터가 오면 검증된 그 경로를, 없으면 기존대로 chatId 기준 경로를 쓰도록 바꿨다(검증이 git 호출을 하므로 async로 전환, 호출부 23곳 반영). 캐시 키에 이미 작업경로가 들어가 있어 작업공간별 캐시는 자동으로 분리된다.
- 클라이언트: Diff 화면 상단에 작업공간 선택 드롭다운을 두고 상태·변경 목록·전체 diff·커밋 상세·컨텍스트 펼치기 조회가 모두 선택한 경로를 따르게 했다. 선택을 바꾸면 열어둔 커밋 상세와 파일 선택을 초기화해 이전 작업공간의 잔상이 남지 않게 했다.
- 현장 확인: 검증용 저장소에 다른 폴더(`wt-outside`) 워크트리를 만들어, 공유 checkout에서는 커밋 1개만 보이고 외부 워크트리를 고르면 `feature/side` 브랜치의 커밋 2개와 미커밋 파일 `uncommitted.txt`가 함께 나오는 것을 확인했다(`artifacts/worktree-selected.png`). `?worktree=/etc` 같은 프로젝트 밖 경로는 거부됐다.
- 검증 완료: 53개 파일 354개 테스트, 타입 검사, 프로덕션 빌드와 공개 경로 검사를 통과했다.
- 추가 요청 반영(사용자 "GitHub 탭은 확인용인데 터미널 열려 있다고 막히면 안 된다"): GitHub 탭에서 실제 브랜치를 바꾸는 UI를 없애고 현재 브랜치 표시만 남겼다(`canManage={false}`, 기존 읽기 전용 모드 재사용). 전환은 채팅 화면에만 두므로 실행 중 채팅의 `canSwitch=false`가 조회를 막지 않는다.
- 브랜치 조회 추가: `/git`에 `ref` 파라미터를 더해 실제 checkout은 그대로 두고 커밋 내역만 지정 브랜치 기준으로 읽는다(브랜치 이름은 기존 `assertGitBranch`로 검증, 캐시 키에도 ref를 포함). 화면에는 `작업공간`과 `커밋 내역` 두 선택과 "확인 전용 · 실제 브랜치는 바뀌지 않습니다" 안내를 함께 뒀다.
- 현장 확인: 실행 중(`canSwitch=false`) 채팅 기준에서도 커밋 조회가 정상 동작하고, `ref=feature/other`로 커밋 내역이 바뀌는 동안 저장소의 실제 HEAD는 `main` 그대로인 것을 확인했다. 화면에서도 전환 버튼이 사라지고 읽기 전용 표시만 남는 것을 확인했다(`artifacts/git-readonly-picker.png`).
- 검증 완료: 53개 파일 354개 테스트, 타입 검사, 프로덕션 빌드와 공개 경로 검사를 통과했다.
- 결함 발견(사용자 질문 "여기서 작업 브랜치를 바꾸면 어떻게 되냐"에서 확인): 조회 대상은 선택한 작업공간·브랜치를 따르는데 커밋·push는 여전히 `chatBody`로 채팅 기준 경로에 전송되고 있었다. 외부 워크트리를 골라 그 변경 목록을 보면서 "선택 파일 커밋"을 누르면 실제로는 다른 경로에서 커밋이 시도돼 실패하거나 엉뚱한 파일이 커밋될 수 있었다. GitHub 탭은 확인 전용이라는 결정에 맞춰, 기본값이 아닌 작업공간·브랜치를 고른 동안에는 커밋·push를 막고 이유를 화면에 알린다.
- 구현 완료: 기본값이 아닌 작업공간·브랜치를 고른 동안 커밋 메시지 입력·"선택 파일 커밋"·Push를 비활성화하고 "기본값으로를 누르세요" 안내를 띄운다. 되돌리면 즉시 다시 쓸 수 있다.
- 현장 확인: 워크트리 목록에 프로젝트 저장소의 checkout과 그 저장소에서 분기한 worktree만 나오고, 같은 디스크에 만든 무관한 저장소는 포함되지 않는 것을 확인했다. 기본 상태에서는 push가 활성, 외부 워크트리를 고르면 커밋·push가 모두 잠기고, "기본값으로"를 누르면 다시 풀리는 것을 화면에서 확인했다(`artifacts/git-viewonly-block.png`).
- 검증 완료: 53개 파일 354개 테스트, 타입 검사, 프로덕션 빌드와 공개 경로 검사를 통과했다.

## 2026-08-07 Claude - GitHub 탭 재구성과 이슈에서 워크트리·채팅 생성

- 작업 시작: 사용자 요청으로 (1) GitHub 탭의 하위 탭을 `로컬`(기존 Diff)·`깃허브`(기존 현재 저장소)·`저장소` 순으로 바꾸고, (2) 이슈·브랜치를 골라 새 워크트리를 만들 수 있게 한다.
- 결정(사용자 선택): 워크트리를 만들 때 그 작업 전용 채팅을 새로 만들어 연결한다(현재 채팅이 다른 워크트리에 묶여 있어도 영향 없음). 이슈에 GitHub가 연결한 브랜치가 있으면 그 브랜치를 그대로 쓰고, 없으면 `issue-<번호>-<제목 슬러그>` 형태를 제안하되 확인창에서 고칠 수 있게 한다. 연결 브랜치 조회는 `gh issue view`가 지원하지 않아 GraphQL `linkedBranches`로 읽는다(실측 확인).
- 구현 완료: 하위 탭을 `로컬`·`깃허브`·`저장소` 순으로 바꿨다. 서버에는 이슈 연결 브랜치 조회(`/github/issue/:number/branches`)와 worktree 전용 채팅 생성(`POST /chats/worktree`)을 추가했다. 후자는 세션을 먼저 띄우면 실행 중이라 작업공간 전환이 막히므로 채팅 행을 `stopped`로 만들어 worktree를 붙인 뒤 마지막에 세션을 시작하고, worktree 생성이 실패하면 만들어 둔 채팅 행을 지워 빈 채팅이 남지 않게 했다. 화면에서는 이슈 상세의 `새 작업공간에서 시작`과 로컬 탭의 `새 작업공간` 버튼이 브랜치 이름을 확인창으로 물어본 뒤 생성하고, 완료되면 그 채팅으로 이동한다.
- 현장 확인: 실제 저장소에서 `POST /chats/worktree`로 `issue-7-sample` 브랜치의 worktree(`/tmp/wamv/git-worktrees/24/22`)와 전용 채팅이 함께 만들어지고 메인 checkout의 HEAD는 `main` 그대로인 것을 확인했다. 화면에서 탭 순서가 `로컬·깃허브·저장소`이고 기본 선택이 `로컬`인 것, `새 작업공간` 버튼이 브랜치 이름을 묻는 것을 확인했다(`artifacts/git-tabs-order.png`, `artifacts/git-new-workspace.png`).
- 검증 완료: 브랜치명 생성 테스트 5건을 추가해 54개 파일 359개 테스트, 타입 검사, 프로덕션 빌드와 공개 경로 검사를 통과했다.

## 2026-08-07 Claude - 새 worktree에 로컬 지침 복사

- 작업 시작: 사용자 요청으로 채팅 전용 worktree를 만들 때 Claude·Codex의 로컬 지침이 함께 따라가게 한다. worktree에는 커밋된 파일만 체크아웃되므로 `CLAUDE.local.md`처럼 git에 올리지 않는 지침이 새 작업공간에서 사라져, 같은 프로젝트인데 에이전트가 다른 지침으로 동작하는 문제가 있었다.
- 결정: 복사 대상은 지침 편집 화면이 이미 관리 목록으로 쓰는 파일들과 같게 유지한다(목록이 갈라지면 한쪽만 늘어난다). 상수를 공용 모듈로 빼서 지침 라우터와 worktree 생성이 같은 목록을 참조하게 하고, worktree에 이미 있는 파일(= git에서 온 것)은 덮어쓰지 않는다.
- 구현 완료: 지침 파일 목록을 `src/server/core/instruction-files.ts`의 `PROJECT_INSTRUCTION_FILES`로 빼 지침 라우터와 공유하고, `switchBranch`가 전용 worktree를 새로 만든 직후 원본 checkout에만 있는 지침을 복사한다. worktree에 이미 있는 파일(git에서 따라온 버전)은 그대로 두고, 복사 실패는 경고만 남겨 worktree 생성 자체는 성공으로 둔다(지침 복사는 부가 작업이라 실패가 작업 시작을 막으면 안 됨).
- 검증: 실제 git 저장소로 `CLAUDE.local.md`·`AGENTS.override.md`·`.claude/CLAUDE.md`가 새 worktree에 복사되고, 커밋된 `AGENTS.md`는 원본에서 수정해도 worktree에는 커밋된 버전이 남는 것을 확인하는 회귀 테스트 2건을 추가했다. 54개 파일 361개 테스트, 타입 검사, 프로덕션 빌드와 공개 경로 검사를 통과했다.

## 2026-08-07 Claude - worktree 채팅 기록이 별도 프로젝트로 새던 문제 수정

- 증상: 사용자가 새 worktree로 채팅을 만들었더니 "폴더 이름이 다르니 답변 확인이 안 된다"고 보고했다. 실제 DB 확인 결과 `history-sync`의 `persist()`가 세션의 cwd(= worktree 경로)로 프로젝트를 자동 등록해, `name=193 path=.../git-worktrees/13/193`처럼 worktree마다 별도 프로젝트가 생기고 대화 기록이 그쪽으로 귀속되고 있었다. 채팅 자체는 원본 프로젝트(13)에 있어 원래 화면에서는 메시지가 보이지 않았다.
- 결정: worktree는 같은 프로젝트의 다른 작업공간이지 별도 프로젝트가 아니므로, 사용자가 고민한 "새 프로젝트로 분리" 대신 worktree 경로를 원본 프로젝트로 귀속시킨다. cwd가 등록된 worktree 경로와 일치하면 새 프로젝트를 만들지 않고 그 worktree를 쓰는 채팅의 프로젝트를 그대로 쓴다.
- 방향 확정(사용자 선택): worktree를 별도 프로젝트로 만들지 않고 같은 프로젝트에 두되, 파일 탭과 지침도 선택한 채팅의 작업공간을 보게 통일한다. 지금까지 파일·지침 API는 `getProjectPath(projectId)`만 써서 채팅이 worktree에 있어도 원본 checkout을 읽고 있었다.
- 구현 완료: `helpers.ts`에 `chatWorkspacePath`를 두어 파일·지침 API가 요청의 chatId로 그 채팅의 worktree를 보게 했다(연결이 끊긴 worktree는 프로젝트 경로로 되돌아가 화면이 비지 않게 함). 파일 목록·미리보기·콘텐츠·다운로드·업로드·편집·첨부와 지침 조회/저장/공통화가 모두 같은 기준을 쓴다. 클라이언트는 `FilesView`·`InstructionsView`에 현재 채팅을 넘기고 채팅이 바뀌면 다시 읽는다.
- 현장 확인: 워크트리 채팅을 만들고 그 폴더에만 파일을 하나 둔 뒤, `chatId` 없이 조회하면 원본 파일만(`only-main.txt`), `chatId`를 주면 워크트리 기준 목록(`only-main.txt`, `only-worktree.txt`)이 나오는 것을 확인했다.
- 검증 완료: 54개 파일 361개 테스트, 타입 검사, 프로덕션 빌드와 공개 경로 검사를 통과했다.

## 2026-08-07 Claude - worktree 공유(여러 채팅)와 정리 기능

- 작업 시작: 사용자 요청으로 (1) 하나의 worktree를 여러 채팅이 함께 쓰게 하고(Claude·Codex 각각 추가), (2) worktree 정리(미커밋 확인 후 삭제 또는 강제 삭제, 연결된 채팅도 함께 정리)를 추가한다.
- 현재 제약: 앱이 만드는 worktree 경로가 `git-worktrees/<projectId>/<chatId>`로 채팅 ID에 묶여 있고 `attachWorktree`가 다른 채팅이 쓰는 worktree를 거부해, 같은 폴더에서 Claude와 Codex를 나란히 돌릴 수 없었다. 정리도 채팅 삭제에 딸려서만 실행돼 worktree만 따로 치울 방법이 없었다.
- 설계 결정: 경로 기준을 채팅 ID에서 브랜치로 바꿔(`git-worktrees/<projectId>/<브랜치 슬러그>`) 같은 브랜치를 쓰는 채팅들이 자연히 한 폴더를 공유하게 한다. 이미 만들어진 채팅 ID 경로는 DB의 `worktree_path`를 그대로 따르므로 그대로 동작한다. 삭제는 마지막 사용 채팅이 떠날 때만 실제 worktree를 제거하고, 미커밋 변경이 있으면 기본적으로 거부하되 명시적 강제 옵션에서만 지운다.
- 구현 완료: `managedPath`를 브랜치 슬러그 기준으로 바꾸고, 같은 브랜치의 worktree가 이미 있으면 새로 만들지 않고 그대로 연결한다. 외부 worktree 연결의 중복 거부를 없애 여러 채팅이 한 폴더를 쓸 수 있게 했고, `detachWorktree`는 다른 채팅이 남아 있으면 연결만 끊고 폴더는 보존한다. 정리는 `removeWorktree`(+ `DELETE /projects/:id/git/worktree`)로 분리해 실행 중 채팅이 있으면 거부, 미커밋 변경이 있으면 force에서만 삭제하며 연결된 채팅들의 worktree 연결을 함께 끊는다(채팅 자체는 유지). 화면에서는 작업공간을 고른 상태에서 공급자별 `+ Claude`·`+ Codex` 추가 버튼과 `정리` 버튼이 나오고, 미커밋 변경으로 실패하면 "버리고 삭제할까요?"를 한 번 더 묻는다.
- 현장 확인: 같은 브랜치로 Claude·Codex 채팅을 만들자 둘 다 `git-worktrees/23/feature-team` 한 폴더를 쓰는 것을 확인했다. 정리는 (1) 실행 중 채팅이 있을 때 `실행 중인 채팅(#23, #24)이 있어...`로 거부, (2) 종료 후 미커밋 변경이 있으면 거부, (3) `force=1`에서 삭제되고 두 채팅의 연결만 끊긴 채 채팅은 남는 것을 단계별로 확인했다.
- 검증 완료: 공유·정리 회귀 테스트 5건을 추가해 54개 파일 366개 테스트, 타입 검사, 프로덕션 빌드와 공개 경로 검사를 통과했다.

## 2026-08-07 Claude - 채팅 목록을 작업공간별로 묶기

- 작업 시작: 사용자 요청으로 채팅 목록을 `프로젝트 채팅(n)`과 `<브랜치> 워크트리(n)`로 나눠 접었다 펼 수 있게 하고, 각 묶음에서 Claude·Codex 채팅을 바로 시작할 수 있게 한다. 워크트리 채팅의 백업 후 삭제는 기존과 같게 두되 그 워크트리를 쓰던 채팅이 모두 사라지면 worktree도 함께 정리하고 그 사실을 알린다.
- 구현 완료: `ChatView`가 `worktree_path` 기준으로 채팅을 묶어 `프로젝트 채팅`을 맨 위에 두고 worktree는 브랜치 이름순으로 정렬한다. 묶음 헤더를 누르면 접히고, 묶음마다 공급자별 시작 버튼이 있어 worktree 묶음에서 시작하면 `/chats/worktree`로 같은 폴더를 쓰는 채팅이 만들어진다. 채팅 삭제 API는 `removeChatWorktree` 결과(폴더까지 정리됐는지·경로·브랜치)를 응답에 실어, 마지막 채팅이 사라져 worktree가 정리되면 화면에서 알림창으로 알린다(백업 후 삭제·바로 삭제 두 경로 모두).
- 현장 확인: 프로젝트 채팅 1개와 `feature/group` 워크트리 채팅 2개가 각각 `프로젝트 채팅(1)`·`feature/group 워크트리(2)`로 묶이고, 묶음 헤더 클릭으로 2개가 접혔다 다시 펼쳐지는 것을 확인했다(`artifacts/chat-groups.png`). 첫 채팅을 지울 때는 `worktreeRemoved: false`로 폴더가 유지되고, 마지막 채팅을 지우자 `worktreeRemoved: true`와 함께 폴더가 정리되는 것을 확인했다.
- 검증 완료: 54개 파일 366개 테스트, 타입 검사, 프로덕션 빌드와 공개 경로 검사를 통과했다.
- 모바일 반영: 모바일 햄버거 메뉴의 채팅 목록도 같은 묶음 구조로 바꿨다(데스크톱 사이드바와 동일한 `chatGroups`·접힘 상태·공급자 시작 버튼을 공유하며, 모바일에서는 채팅을 시작하면 메뉴를 닫는다).
- 현장 확인: 390×844에서 메뉴를 열어 `프로젝트 채팅(1)`·`feature/mobile 워크트리(2)` 묶음과 묶음별 `+ Codex`·`+ Claude` 버튼이 나오는 것을 확인했다(`artifacts/mobile-chat-groups.png`). 검증 과정에서 모바일 폭은 전역 헤더가 숨겨져 로그인 확인 요소가 없다는 점 때문에 처음 스크립트가 실패했고, 로그인·프로젝트 선택까지는 데스크톱 폭에서 한 뒤 뷰포트만 바꾸는 기존 모바일 테스트 방식으로 맞춰 해결했다.
- 검증 완료: 54개 파일 366개 테스트, 타입 검사, 프로덕션 빌드와 공개 경로 검사를 통과했다.

## 2026-08-07 Claude - 전체 UI/UX 점검(개선안 도출, 코드 변경 없음)

- 요청: PC·모바일 전체 화면을 확인해 UI/UX 개선안을 만든다.
- 방법: 검증 서버에 실제 프로젝트를 등록하고 6개 탭(대시보드·채팅·파일·지침·GitHub·도구)을 1440px과 390px에서 각각 캡처하면서, 가로 넘침·넘침을 유발한 요소·32px 미만 터치 대상 수·문서 높이를 함께 수집했다(`artifacts/ux/`).
- 수집 결과: 페이지 자체의 가로 넘침은 모든 화면에서 0이었으나 모바일 대시보드의 프로세스 표(table/thead/tr/tbody)가 컨테이너보다 넓었다. 32px 미만 터치 대상은 파일 탭 25개(PC·모바일 동일), 채팅 9개, GitHub 7~9개였다. 문서 높이는 PC 도구 탭이 7269px로 가장 길었고(좌측 목록에 자체 스크롤이 없어 페이지 전체가 늘어남), 모바일 대시보드는 1965px였다.
- 결론: 코드는 바꾸지 않고 관찰 결과와 우선순위별 개선안만 사용자에게 전달했다.

## 2026-08-07 Claude - 잘못 귀속된 worktree 채팅 교정

- 증상: 워크트리 터미널에 들어가도 채팅이 여전히 안 보인다는 보고. 실제 DB 확인 결과 사용자가 만든 워크트리 채팅(193)은 `provider_session_id`가 비어 있고, 실제 대화는 워크트리 경로로 만들어진 프로젝트(47115)의 다른 채팅(194)에 붙어 있었다. 세션 cwd는 정확히 193의 worktree 경로였다.
- 원인: 귀속 수정 이전(옛 코드)에 만들어진 채팅은 이후 동기화에서 `provider_session_id`로 곧바로 찾아지기 때문에 프로젝트 귀속이 다시 계산되지 않았다. 즉 한 번 잘못 붙으면 영구히 그대로 남았다.
- 수정 방향: 이미 있는 채팅이라도 세션 cwd가 등록된 worktree 경로면 원본 프로젝트로 project_id를 교정하고, 그 결과 비게 되는 worktree 경로 프로젝트는 목록에서 감춘다.
- 구현 완료(우선순위 A + 요청 2건):
  - worktree 채팅 귀속 교정: 이미 있는 채팅도 매 동기화에서 올바른 프로젝트로 `project_id`를 되돌리고, worktree 경로로 만들어졌던 `discovered` 프로젝트는 `active=0`으로 감춘다. 서버가 최신 코드여도 옛 데이터가 고쳐지지 않던 원인이 여기 있었다.
  - 모바일 파일 탭: 다운로드 버튼 28px→44px, 행 높이 46→56px로 키우고 모바일에서 숨겨져 있던 크기·수정 시각을 파일명 아래 한 줄로 되살렸다(뒤쪽에 남아 있던 `display: none` 중복 규칙을 제거해야 적용됐다).
  - PC 도구 탭: 좌측 목록에 `max-height`를 줘 페이지 전체가 늘어나던 것을 멈췄다.
  - 모바일 대시보드: 프로세스 표를 카드형으로 바꾸고 각 셀에 `data-label`을 붙여 값 구분이 되게 했다.
  - 채팅 묶음 버튼: 전체 폭 버튼이 목록보다 눈에 띄어, 들여쓴 작은 칩(64×22)으로 바꿨다.
- 측정 결과(개선 전 → 후): PC 도구 문서 높이 7269px → 1000px, 모바일 대시보드 프로세스 표 넘침 요소 4개 → 0개, 모바일 파일 44px 미만 액션 버튼 전량 → 0개(44×44), 채팅 묶음 버튼 전체 폭 → 64×22 칩.
- 검증 완료: 54개 파일 366개 테스트, 타입 검사, 프로덕션 빌드와 공개 경로 검사를 통과했다.
- 사고와 수습(사용자 "프로젝트가 안 보여" 보고): 직전 커밋에서 넣은 "worktree 경로로 만들어진 프로젝트를 목록에서 감춘다"는 로직이 실제 프로젝트를 꺼버렸다. 과거 데이터에 `chats.worktree_path`가 그 프로젝트의 checkout 경로와 같은 행(chat 194 = `/home/ubuntu/myagent`)이 남아 있어, 일반 세션 cwd까지 worktree로 오판하고 `projects.active = 0`을 실행한 것이다. 프로젝트 13(myagent)이 목록에서 사라졌다.
- 조치: (1) 비활성화 로직을 완전히 제거했다 — 사용자가 직접 숨긴 프로젝트와 구분할 방법이 없어 자동으로 끄는 것 자체가 위험하다. (2) worktreeOwner 판정에 `c.worktree_path <> p.path` 조건을 넣어 프로젝트 경로와 같은 값은 worktree로 보지 않는다. (3) DB에서 프로젝트 13을 다시 활성화하고, 오판의 원인이던 잘못된 `worktree_path` 1건을 NULL로 정리했다.
- 후속 수정(사용자 "여전히 워크트리 채팅 안 보임"): 앞선 교정으로 프로젝트 귀속(47115 → 13)과 잘못된 프로젝트 감추기는 동작했으나, 정작 그 채팅의 `worktree_path`가 비어 있어 목록에서 worktree 묶음이 아니라 프로젝트 채팅 묶음에 섞였다. history-sync가 채팅을 새로 만들거나 기존 채팅을 찾을 때 세션 cwd가 등록된 worktree면 `worktree_path`·`git_branch`도 함께 남기도록 했다.
- 현장 확인: 실제 운영 DB에서 chat 194가 `wt=/home/ubuntu/myagent/data/git-worktrees/13/193`, `branch=feature/new-work2`로 채워져 chat 193과 같은 묶음에 들어가는 것을 확인했다.
- 데이터 정리(사용자 승인 후 실행): 193 터미널에서 물어본 답이 194에 나오던 문제를 확인했다. 193 tmux의 codex(pid 88147, cwd=worktree)가 실제로 쓰는 세션 파일이 194에 붙어 있었고, 이는 귀속 수정 이전(옛 코드)에 그 세션으로 194가 따로 만들어졌기 때문이다. 사용자가 "194 세션을 193으로 옮기기"를 선택해, 변경 전 상태를 JSON으로 백업한 뒤 194 행을 지우고 193에 세션 ID·기록 파일·제목을 넘겼다((provider, provider_session_id) UNIQUE 제약 때문에 삭제를 먼저 해야 했다). JSONL(63KB)은 건드리지 않아 대화가 그대로 유지된다.
- 확인: 동기화가 한 번 더 돈 뒤에도 193이 그 세션을 유지하고 194가 다시 생기지 않는 것을 확인했다. 앞으로 만드는 worktree 채팅은 `worktreeOwner` 매칭으로 처음부터 올바른 채팅에 붙는다.
- 확인 요청(GitHub 탭 정리가 실제 폴더도 지우는가): 검증 서버에서 `DELETE /projects/:id/git/worktree`를 호출해 worktree 폴더가 실제로 삭제되는 것을 확인했다. 다만 `git-worktrees/<projectId>/` 상위 폴더가 빈 채로 남는 것도 함께 확인했다(사용자 저장소에서도 `13/`이 빈 채 남아 있었다).
- 수정: `pruneEmptyParent`를 추가해 worktree 제거 후 앱이 만든 경로 안에서만, 비어 있을 때만 상위 폴더도 정리한다. 정리 버튼과 채팅 삭제(마지막 사용자) 두 경로 모두에 적용했다. 같은 프로젝트에 다른 worktree가 남아 있으면 상위 폴더를 지우지 않는 회귀 테스트도 넣었다.
- 부수 정리: 앞선 UX 점검에서 만든 검증 worktree가 사용자 저장소의 `.git/worktrees`에 `prunable` 상태로 남아 있어 `git worktree prune`으로 제거했고, 비어 있던 `data/git-worktrees/13/`도 지웠다.
- 검증 완료: 빈 폴더 정리 회귀 테스트 2건을 추가해 54개 파일 368개 테스트, 타입 검사, 프로덕션 빌드와 공개 경로 검사를 통과했다. 서버를 최신 코드로 재시작한 뒤 실제 API 호출에서 worktree 폴더와 상위 폴더가 모두 정리되는 것을 확인했다.

## 2026-08-07 Claude - 삭제 사전 확인과 불가역 작업의 내부망 제한

- 요청 1: 마지막 워크트리 채팅을 지울 때 "삭제 후 알림" 대신 "삭제 전 확인"으로 워크트리도 정리된다는 사실을 알려야 한다.
- 요청 2: 워크트리 삭제·채팅 삭제처럼 되돌릴 수 없는 작업은 파일 탭의 민감 경로 정책처럼 내부망(신뢰 네트워크)에서만 동작해야 한다.
- 결정: 확인은 클라이언트가 이미 가진 채팅 목록으로 "같은 worktree를 쓰는 채팅 수"를 세어 마지막인지 판단한다(서버 왕복 없이 확인창을 정확히 만들 수 있다). 내부망 제한은 라우트마다 조건문을 넣지 않고 `requireTrustedNetwork` 미들웨어를 만들어 불가역 작업에만 붙인다.
- 구현 완료: 삭제 확인은 `deleteConfirmMessage`가 같은 worktree를 쓰는 채팅 수를 세어, 공유 중이면 "폴더는 그대로 남습니다", 마지막이면 "작업공간 폴더도 함께 정리됩니다"와 브랜치·경로·미커밋 주의를 확인창에 함께 보여준다(사후 alert은 제거하고 상태 문구로 대체). 내부망 제한은 `requireTrustedNetwork` 미들웨어를 만들어 채팅 삭제·세션 백업 삭제·프로젝트 삭제·worktree 정리·프로세스 종료·MCP 삭제 6개 라우트에 붙였다.
- 현장 확인: 확인창 문구는 공유 중("다른 채팅 1개도 함께 쓰고 있어 폴더는 그대로 남습니다")과 마지막(브랜치·경로 포함) 두 경우 모두 삭제 전에 나오는 것을 확인했다. 내부망 제한은 `TRUSTED_NETWORKS`를 다른 대역으로 지정해 127.0.0.1을 외부망으로 취급시킨 서버에서 프로젝트 삭제·worktree 정리·프로세스 종료·채팅 삭제가 모두 403으로 거부되고 조회(GET /projects)는 200으로 정상인 것을, 다시 내부망 서버에서는 같은 작업이 통과하는 것을 확인했다.
- 검증 완료: 외부망 거부 회귀 테스트 2건을 추가해 54개 파일 370개 테스트, 타입 검사, 프로덕션 빌드와 공개 경로 검사를 통과했다.

## 2026-08-07 Claude - 다계정(Claude·Codex) 전환 기능 실현 가능성 조사

- 요청: 여러 계정을 미리 인증해두고 한도가 찰 때마다 전환하는 기능이 가능한지, `.claude`·`.codex` 인증을 백업해두는 방식으로 되는지 확인해달라(코드 변경 없는 조사 단계).
- 조사 방법: 인증 파일의 내용은 열지 않고(전역 규칙 16) 경로·구조와 실행 경로만 확인했다. CLI가 설정 디렉터리 분리를 지원하는지는 스크래치패드에 빈 디렉터리를 만들어 실제 명령으로 실증했다.
- 실증 결과: `CLAUDE_CONFIG_DIR`를 빈 디렉터리로 지정하면 `claude auth status`가 `loggedIn: false`를 반환하고 그 디렉터리에 `.claude.json`이 새로 생성된다. `CODEX_HOME`도 같은 방식으로 `Not logged in`을 반환한다. 즉 두 CLI 모두 계정별 설정 디렉터리 분리를 공식 지원한다.
- 결정(방식): 사용자가 제안한 "백업 후 파일 교체(스왑)" 대신 **계정 슬롯별 설정 디렉터리 + 실행 시 환경변수 주입** 방식을 권고한다. 스왑은 같은 `~/.claude`를 물고 있는 실행 중인 다른 채팅 세션 전체에 영향을 주고, 인증과 무관한 기록·캐시까지 함께 바뀌기 때문이다. 디렉터리 분리는 채팅마다 다른 계정을 동시에 쓸 수도 있다.
- 재활용 가능한 기존 자산: `session-manager.ts`가 이미 tmux `-e KEY=VALUE`로 세션별 환경변수를 주입하고 있고(`ProviderLaunch.env`), `cli-auth.ts`가 `claude auth login --claudeai`·`codex login --device-auth`를 관리자 전용 PTY로 돌리는 로그인 흐름을 갖고 있다. 한도 감지·재개는 `usage-monitor.ts`·`rate-limit-resume.ts`에 이미 있다.
- 확인된 제약(구현 시 반드시 손봐야 할 곳): (1) `claude.ts:408`·`codex.ts:275`의 `historyRoot`가 `os.homedir()` 고정이라 `history-sync.ts:83`이 한 경로만 스캔한다 — 계정을 나누면 다른 슬롯의 채팅이 목록에서 사라진다. (2) `cli-auth.ts:124`가 `HOME`을 고정해 슬롯별 로그인이 불가능하다. (3) `usage-monitor.ts:293`의 조회 실행에 env가 없어 계정별 사용량을 따로 볼 수 없다. (4) 슬롯을 바꾸면 이전 계정 디렉터리에 있는 세션 기록을 `--resume`으로 이어받을 수 없어, 전환 정책(새 세션 시작 또는 기록 복사)을 정해야 한다.
- 현재 상태: 조사만 완료했고 코드는 변경하지 않았다. 실제 구현 범위는 사용자 결정 대기 중이다.

## 2026-08-07 Claude - 계정 슬롯(다계정) 기능 구현 착수

- 요청: 앞선 조사 결과를 바탕으로, 채팅마다 계정을 할당하는 방식으로 다계정 전환을 구현한다. 사용량 조회 세션은 "대표 계정 하나만"과 "등록된 전 계정" 중 설정으로 고를 수 있게 한다.
- 설계 결정 1(전역 스위치 대신 채팅 귀속): 계정을 전역 스위치로 두면 전환하는 순간 이전 계정으로 만든 채팅 전부가 `--resume` 불가 상태가 된다. 채팅마다 자기가 태어난 계정을 기억하게 해(`chats.account_id`) 목록은 전부 보이고 재개는 각자 자기 계정으로 붙게 한다.
- 설계 결정 2(기본 계정은 건드리지 않는다): 기존 `~/.claude`·`~/.codex` 인증을 슬롯으로 복사·이동하지 않는다. 기본 계정은 설정 디렉터리를 지정하지 않는(환경변수 미주입) 특수 계정으로 등록해 기존 동작을 그대로 보존하고, 추가 계정만 `data/agent-accounts/<provider>/<slug>/`를 새로 만들어 쓴다. 인증 파일 내용은 어떤 경우에도 읽지 않는다(전역 규칙 16).
- 설계 결정 3(사용량 기본값은 대표 계정): 계정마다 상태 조회 PTY를 상시 띄우는 비용이 크므로 기본값은 대표 계정 하나만 조회하고, 설정에서 전 계정 조회로 바꿀 수 있게 한다.
- 구현 순서: (1) `agent_accounts` 테이블·계정 서비스, (2) 어댑터에 `configDirEnvVar`·`historyRootFor` 추가, (3) 세션 실행 시 계정 env 주입, (4) history-sync 다중 루트 스캔, (5) 계정별 CLI 로그인, (6) 사용량 범위 옵션, (7) API·UI, (8) 테스트·문서·검증.
- 브랜치: 전역 규칙 8(작업 브랜치 통일)에 따라 새 브랜치를 만들지 않고 현재 작업 브랜치 `feature/chat-git-worktrees`에서 이어서 진행한다(PR 미제출 상태).

- 구현 완료: (1) `agent_accounts` 테이블과 `AgentAccountService`를 만들고, 서버 시작 시 공급자별 기본 계정을 보장하며 기존 채팅(`chats.account_id`)과 사용량 행을 그 기본 계정으로 백필했다. `usage_status`는 계정마다 한도가 따로라 `(provider, account_id)` 복합 키로 재구성했다(기존 데이터 보존). (2) 어댑터에 `historyRootFor(configDir)`를 추가해 계정별 기록 루트를 계산한다. (3) 세션 실행은 `tmux new-session -e`로 계정 설정 디렉터리를 주입한다. (4) history-sync가 등록된 전 계정의 기록 루트를 스캔하고 발견 위치를 근거로 `account_id`를 기록한다. (5) CLI 로그인을 계정 단위로 바꾸고 터미널 식별자를 `claude:3` 형태 키로 통일했다. (6) 사용량 조회는 범위 설정(`default`/`all`)에 따라 계정별 PTY를 띄우고, 범위를 바꾸면 즉시 재구성한다. (7) 계정 CRUD·범위 설정·채팅 계정 변경 API와 CLI 인증 팝업의 계정 관리 UI, 계정별 새 채팅 버튼, 채팅 헤더의 계정 선택을 추가했다.
- 결정(계정 변경 시 세션 처리): 계정을 옮기면 이전 계정 폴더에만 있는 세션을 재개하려다 실패하므로 `provider_session_id`·`history_file`을 비워 다음 시작을 새 대화로 만든다. JSONL 자체는 지우지 않아 계정을 되돌리면 이전 대화를 다시 이어갈 수 있다. 실행 중인 채팅은 환경변수가 이미 고정돼 있어 변경을 거부한다.
- 남긴 제한(TODO 주석으로 명시): `usage_reset_schedules`가 `(provider, window_id)` 키라 계정을 구분하지 못해, 사용량 리셋 알림은 기본 계정 기준으로만 발송한다. 스케줄 테이블에 account_id를 추가하면 해제할 수 있다.
- 현장 확인: 검증 전용 DB·포트(14011)에서 기본 계정 2개 자동 등록과 기존 채팅 백필(Claude 14개·Codex 8개)을 확인했고, 계정을 추가하니 같은 CLI인데도 기본 계정은 `authenticated=true`, 새 계정은 `false`로 나와 인증이 실제로 분리되는 것을 확인했다. 사용량 범위를 `all`로 바꾸자 새 계정의 조회 PTY가 뜨며 `usage_status`에 행이 늘었고, 채팅 계정 변경 시 `account_id`가 바뀌고 세션 연결이 끊기며 감사 로그(`chat.assign_account`)가 남는 것을 확인했다. UI는 계정별 로그인 목록·`+ Claude · 회사 계정` 생성 버튼·채팅 헤더 계정 선택을 스크린샷으로 확인했고, 계정 라벨이 길어 채팅 목록 상단 버튼이 잘리던 것은 줄바꿈 허용으로 고쳤다.
- 사고 기록(검증 환경): 처음에 스크래치패드 경로로 검증 서버를 띄웠는데 UNIX 소켓 경로 108바이트 제한에 걸려 소켓 이름이 잘리며 기동이 실패했고, 그때 14003으로 돌아온 200 응답은 다른 세션의 서버였다(그래서 로그인이 401). 짧은 경로(`/tmp/wam-verify`)와 빈 포트(14011)로 다시 띄워 해결했으며, 정리할 때는 이름 패턴 검색 없이 이번 검증에만 쓴 고유 `WEB_AGENT_MANAGER_DATA_DIR` 값을 가진 프로세스만 골라 종료했다.
- 검증 완료: 계정 슬롯·다중 계정 기록 스캔 회귀 테스트 5건을 추가해 55개 파일 375개 테스트, 타입 검사, 프로덕션 빌드와 공개 경로 검사를 통과했다.
## 2026-08-08 Codex - 전체 UI/UX 재정비

- 작업 시작: 사용자 요청에 따라 현재 구현된 데스크톱·모바일 UI 전체를 다시 확인하고, 정보 위계·가독성·조작 피드백·반응형 사용성을 공통 디자인 체계 중심으로 개선한다.
- 담당과 의사결정: Codex가 기존 화면 구조와 E2E 시나리오를 보존하는 범위에서 최소 수정한다. 먼저 공통 토큰·레이아웃·컨트롤을 정리한 뒤 화면별 밀도와 모바일 동작을 확인하며, 이미 존재하는 컴포넌트와 스타일을 재사용해 중복 구현을 만들지 않는다.
- 작업 범위: `src/client`의 전역 셸, 대시보드, 채팅, 파일, 지침, GitHub, 도구, 로그인·다이얼로그·상태 UI를 점검하고 실제 브라우저 렌더링 및 기존 자동화 검증으로 회귀를 확인한다.
- 사용자 추가 지시: 코드만 읽어 판단하지 않고 앱을 실제로 띄워 화면을 파악한다. 이에 따라 검증용 로컬 환경에서 데스크톱·태블릿·모바일 화면과 주요 팝업을 직접 렌더링·캡처하고, 측정 결과를 개선 기준으로 삼는다.
- 실제 화면 확인: Vite 검증 서버와 Chromium을 띄워 로그인, 대시보드, 채팅·원본 터미널·모바일 메뉴, 파일·미리보기, 지침 편집기, 로컬 Git·GitHub PR diff, 도구 상세, 프로젝트 생성, CLI 인증, 서브 에이전트 서랍을 1440×1000과 390×844에서 직접 렌더링했다. 모바일 화면은 문서와 주요 내부 컨테이너의 가로 넘침도 함께 측정했다.
- 디자인 결정: 기존 기능 구조는 유지하고 공통 시각 체계를 네이비 셸·바이올렛 주요 동작·라임 활성 포인트로 통일했다. 탭에 아이콘과 명확한 활성 배경을 추가하고, 카드·입력·버튼·포커스·상태 배지의 반경·간격·그림자를 통일했다. 채팅은 선택 목록·대화·터미널·컴포저 위계를 강화하고 사용자 메시지를 주요 색상 말풍선으로 구분했다.
- 반응형 개선: 모바일 비채팅 화면에도 브랜드·실행 상태와 현재 프로젝트 선택을 복원했고, 하단 탭바를 아이콘+라벨 구조와 더 큰 조작 영역으로 바꿨다. 대시보드·파일·지침·GitHub·도구 카드에 모바일 여백과 높이 규칙을 적용하고, CLI 인증의 공급자·계정 선택과 추가 버튼이 좁은 화면에서도 보이도록 정리했다.
- 회귀 정리: 현재 `로컬·깃허브·저장소` 탭 이름과 공용 diff 컴포넌트 구조를 아직 예전 `Diff·현재 저장소`/옛 CSS 선택자로 찾던 E2E를 현재 UI 계약에 맞췄다. 지침·도구 상세과 로그인 화면의 데스크톱·모바일 렌더 테스트를 추가하고, 가상 목록 높이 측정이 안정된 뒤 과거 읽기 위치를 비교하도록 스크롤 테스트를 현실적인 브라우저 타이밍에 맞췄다.
- 문서 업데이트: README에 반응형 디자인 체계와 모바일 프로젝트 문맥을 추가하고, CODETREE의 최상위 셸·Git 탭·스타일·E2E 역할을 현재 구현 기준으로 갱신했다.
- 검증 완료: `npm run verify`에서 55개 파일 375개 테스트, 타입 검사, 프로덕션 빌드와 공개 경로 검사를 통과했다. 전체 Playwright는 테스트 계정 환경변수가 필요한 1건만 정상 skip되고 17건이 통과했으며, 스크롤 위치 보존 테스트는 3회 반복 실행도 모두 통과했다.

## 2026-08-08 Codex - 채팅 터미널 모드 구현

- 작업 시작: 사용자 요청에 따라 채팅 화면에서 대화 UI와 실제 CLI 터미널을 명확히 전환해 사용할 수 있는 터미널 모드를 구현한다.
- 담당과 조사 결정: Codex가 과거 계획·의사결정을 `history.md`와 프로젝트 문서, 세션 문맥에서 먼저 찾아 기존 방향을 복원한다. 현재 채팅·PTY·터미널 구현을 재사용하고, 별도 중복 터미널이나 세션을 만들지 않는 최소 변경을 원칙으로 한다.
- 검증 계획: 관련 단위·E2E 테스트를 갱신한 뒤 로컬 앱을 실제로 띄워 데스크톱과 모바일에서 모드 전환, 터미널 크기, 채팅 복귀와 입력 상태 보존을 직접 확인한다.
- 과거 계획 복원: `web-agent-manager-session-context` 스킬로 현재 프로젝트의 채팅 목록과 구조화된 문맥을 조회하고 로컬 기록을 교차 검색했다. `2026-08-01 Codex - v0.3.0 선택형 터미널 채팅 모드 이슈 등록`에서 기본은 기존 채팅 모드, 웹 계정별 선택 저장, 전체 PTY 화면·입력, 세션 보존, 재연결 초기 프레임, 승인·알림·상태·사용량 동기화 유지, 모바일 대응이라는 이슈 #20의 수용 조건을 확인했다.
- 구현 결정: 별도 PTY나 세션을 만들지 않고 기존 `TerminalPanel`과 현재 채팅의 tmux/WebSocket 구독을 그대로 재사용했다. 채팅 작업영역 상단에 `채팅 모드`·`터미널 모드` 선택을 두고, 터미널 모드에서는 기록 목록과 컴포저 대신 PTY가 남은 작업영역 전체를 사용한다. 채팅 헤더·모델/상태/사용량 바·인라인 승인·시작/종료 버튼은 계속 렌더링해 백그라운드 기능과 제어를 유지한다.
- 계정별 저장: `users.chat_view_mode`를 기본값 `chat`으로 추가하고 기존 DB용 경량 마이그레이션, 인증 사용자 응답, CSRF 보호 `PUT /api/auth/chat-view-mode`와 감사 로그를 연결했다. 화면은 저장을 낙관적으로 반영하고 실패하면 이전 모드로 복구하며, 새로고침·재로그인 뒤에는 `/auth/me` 값으로 복원한다.
- 터미널 입력 UX: 전체 터미널이 마운트되면 xterm에 즉시 포커스를 넘겨 별도 클릭 없이 현재 tmux 세션으로 입력할 수 있게 했다. 기존 고정 256×36 논리 그리드와 초기 ANSI 프레임, 선택 영역 Ctrl+C 복사 정책은 그대로 유지해 TUI 좌표와 세션 화면을 보존했다.
- 실제 화면 확인: Vite 검증 서버와 Chromium을 띄워 1440×1000 데스크톱과 390×844 모바일에서 모드를 직접 전환·캡처했다(`artifacts/ui-chat-terminal.png`, `artifacts/ui-mobile-chat.png`). 데스크톱은 PTY가 상태바 아래 남은 작업영역 전체를 사용하고, 모바일은 하단 탭바 바로 위까지 채우며 페이지 가로 넘침 없이 터미널 내부 스크롤만 사용하는 것을 확인했다. 새로고침 후 터미널 모드 복원, 채팅 모드 복귀, 포커스된 xterm의 실제 `terminal_input` WebSocket 전송도 자동화로 검증했다.
- 문서 업데이트: README의 기존 사이드 미리보기 설명을 계정별 전체 터미널 모드 동작으로 교체하고, CODETREE에 DB·인증 API·최상위 상태·채팅/터미널 컴포넌트·반응형 스타일·테스트 역할을 갱신했다.
- 검증 완료: `npm run verify`에서 55개 파일 376개 테스트, 타입 검사, 프로덕션 빌드와 공개 경로 검사를 통과했다. 전체 Playwright는 테스트 계정 환경변수가 필요한 로그인 1건만 정상 skip되고 17건이 통과했다.

## 2026-08-08 Codex - 모바일 터미널 UX 재설계

- 작업 시작: 사용자 피드백에 따라 모바일에서 터미널 창이 안정적으로 보이지 않는 문제, 위로 스와이프할 때 xterm 기록이 아니라 바깥 모바일 뷰가 움직이는 문제를 실제 브라우저에서 재현하고 수정한다.
- 담당과 의사결정: Codex가 기존 tmux 256×36 논리 그리드와 WebSocket 세션은 보존하면서, 터치 스크롤의 이벤트 소유권과 모바일 입력 수단을 터미널 컴포넌트 안에서 해결한다. 데스크톱 키보드 동작에는 영향을 주지 않고 모바일에 필요한 컨트롤·탐색 단축키만 명시적인 터치 툴바로 제공한다.
- 검증 계획: Chromium 모바일 에뮬레이션에서 터미널 가시 영역 크기, 문서/터미널 scrollTop 변화, 위·아래 스와이프, Ctrl·Esc·Tab·방향키·Enter 전송값, 화면 키보드 입력과 가로 넘침을 직접 측정하고 캡처한다.
- 원인 확인 1: 터미널 외곽 컨테이너가 xterm 내부 루트와 같은 `.terminal` 클래스를 사용해 전체 화면용 높이·여백·overflow 규칙이 xterm 내부에도 중복 적용되고 있었다. 외곽 클래스를 `.terminal-host`로 분리해 모바일에서 터미널 창이 잘리거나 사라지는 충돌을 제거했다.
- 원인 확인 2: 모바일 규칙 뒤에 선언된 공통 `.chat-layout { min-height: 560px; }`가 모바일의 `min-height: 0`을 덮어써, 키보드가 올라온 것과 같은 낮은 뷰포트에서 문서 전체가 스크롤되고 터미널은 제자리에 남았다. 채팅 앱 셸·레이아웃·작업영역을 `100dvh`와 `min-height: 0`으로 다시 고정해 바깥 문서 스크롤을 막았다.
- 터치 동작 결정: xterm이 일반 모드일 때 상하 스와이프는 xterm 스크롤백을 직접 이동하고, Codex·Claude TUI가 마우스 추적 모드일 때는 같은 제스처를 xterm 휠 이벤트로 전달해 실제 PTY 마우스 프로토콜 입력이 되게 했다. 좌우 스와이프는 256열 논리 화면만 이동하며 페이지에는 전달하지 않는다.
- 모바일 입력 구현: 터미널 아래에 키보드 호출, 1회성 Ctrl·Alt, Esc·Tab·Shift+Tab·방향키·PageUp·PageDown·Enter와 Ctrl+C·Ctrl+D를 전송하는 가로형 키 바를 추가했다. Ctrl/Alt는 다음 화면 키보드 문자에도 적용하며, 터치 단축키는 불필요하게 소프트 키보드를 다시 띄우지 않는다.
- 저높이 화면 대응: 390×520 이하에서는 중복 상태 정보와 터미널 헤더·도움말을 줄이고 터미널 본문과 필수 키 바를 우선 배치했다. 390×390에서도 바깥 문서 높이가 뷰포트를 넘지 않고 터미널·하단 내비게이션이 함께 보이도록 했다.
- 실제 화면 확인: Chromium에서 1440×1000, 390×844, 390×390 크기로 직접 렌더링했다. 데스크톱 전체 터미널, 모바일 터미널 창과 내부 스크롤바, 필수 키 바, 극단적으로 낮은 모바일 뷰에서의 하단 내비게이션 유지와 문서 무스크롤을 캡처해 확인했다(`artifacts/ui-chat-terminal.png`, `artifacts/ui-mobile-chat.png`, `artifacts/ui-mobile-terminal-compact.png`).
- 자동화 검증: 모바일 터치 이벤트로 xterm 행 내용만 이동하고 `window.scrollY`는 유지되는지, 마우스 추적 모드에서 SGR 휠 바이트가 PTY로 전송되는지, 좌우 이동이 문서 overflow를 만들지 않는지, 키보드 포커스와 Ctrl+C·Alt+X·PageUp·Shift+Tab·Enter의 정확한 바이트를 E2E로 검증했다.
- 문서 및 검증 완료: README와 CODETREE에 터치·단축키·저높이 레이아웃 계약을 반영했다. `npm run verify`에서 55개 파일 376개 테스트, 타입 검사, 프로덕션 빌드와 공개 경로 검사를 통과했고, 전체 Playwright는 인증 정보가 필요한 1건만 정상 skip되고 17건이 통과했다.

## 2026-08-08 Codex - v0.3.0 릴리스 및 배포

- 작업 시작: 사용자 요청에 따라 현재 작업 브랜치의 미커밋 변경과 v0.2.0 이후 전체 커밋을 다시 검토하고, v0.3.0 버전·변경 기록·배포 산출물을 정리한 뒤 공개 릴리스와 배포까지 진행한다.
- 담당과 의사결정: Codex가 보호 브랜치에 직접 수정하지 않고 현재 기능 브랜치에서 릴리스 준비 커밋을 만든다. 전체 로컬 검증과 패키지 무결성 확인을 통과한 커밋만 원격에 반영하고, 기존 CI 계약에 따라 공개 태그가 데스크톱 릴리스와 GHCR 다중 아키텍처 이미지를 만들도록 한다.
- 검증 계획: 변경 파일별 diff·문서·버전 일치 여부와 민감 정보 혼입 여부를 확인하고, 타입 검사·단위 테스트·프로덕션 빌드·전체 Playwright·릴리스 패키징 및 체크섬 검증을 수행한다. 원격 반영 후에는 GitHub Actions와 공개 Release·컨테이너 배포 결과를 실제로 조회해 완료 여부를 확인한다.
- 변경 검토: 공개 `main`은 운영 식별자가 있던 비공개 개발 이력과 공통 조상이 없는 정리 이력임을 재확인했다. 비공개 기능 브랜치를 공개 `main`에 직접 병합하지 않고, 공개 `main` 기반 별도 릴리스 브랜치에 검증된 현재 파일만 옮기며 공개 저장소의 최신 의존성·Dependabot·Docker Action 갱신은 보존하기로 했다.
- 버전·문서 준비: `package.json`과 lockfile 최상위 버전을 0.3.0으로 맞추고 README의 압축 배포 파일명을 갱신했다. CHANGELOG에는 Git worktree 작업공간, GitHub 화면, 다계정 슬롯, 전체 PTY 터미널·모바일 입력, UI 재정비, 사용량·프로세스 안정성 및 불가역 작업의 신뢰 네트워크 제한을 정리했다.
- 보안 감사 조치: 최초 `npm audit --audit-level=high`에서 간접 개발 의존성 `fast-uri 3.1.4`, `js-yaml 4.3.0`의 고위험 취약점 2건이 발견됐다. 호환 lockfile 업데이트로 각각 3.1.5와 4.3.1을 사용하게 하고 깨끗한 `npm ci` 후 전체·운영 의존성 감사 0건을 확인했다.
- 로컬 검증: 깨끗한 의존성 설치 후 타입 검사, 55개 파일 376개 단위 테스트, 프로덕션 빌드와 공개 절대 경로 검사를 통과했다. 별도 Vite 서버에서 전체 Playwright 17건이 통과하고 실계정 환경변수 필요 1건만 정상 skip됐으며, 브라우저 반복 탭 전환 후 heap 증가는 약 2.2MB, DOM은 6개 증가, 이벤트 리스너는 증가하지 않았다.
- 실제 CLI 검증: Codex·Claude 사용량 TUI 파싱은 실제 PTY에서 통과했다. 새 Codex 세션 생성·resume 검사는 현재 계정의 주간 한도가 100%라 세션 ID가 생성되지 않아 환경 조건상 완료하지 못했으며, CI 필수 검증이 아닌 보조 점검으로 분류했다.
- 로컬 배포 산출물: `npm run release:package`로 v0.3.0 Linux x64·macOS x64·macOS arm64·Windows WSL2 ZIP, CycloneDX SBOM과 SHA-256 목록을 생성했다. 5개 대상의 체크섬이 모두 일치하고 SBOM 최상위 컴포넌트 이름·버전이 `web-agent-manager 0.3.0`인지 확인했다.
- 공개 기준 동기화: 공개 `main`에서 별도로 검증·병합된 React·Vite·Vitest 등 호환 의존성 업데이트와 Dependabot 그룹 정책, 고정 Docker Action SHA를 비공개 기능 브랜치에도 역반영했다. v0.3.0 공개 브랜치가 최신 공개 유지보수 변경을 되돌리지 않도록 양쪽 파일 기준을 맞춘다.
- 릴리스 직전 경합 수정: 최신 의존성으로 브라우저 검증 서버를 재기동하는 과정에서 xterm이 정리된 직후 이전 터치 리스너가 `buffer`에 접근하는 콘솔 오류를 발견했다. effect 생성 당시의 호스트 요소를 고정해 같은 요소에서 리스너를 제거하고, 정리 시작 시 dispose 플래그와 제스처를 먼저 닫아 늦게 도착한 터치 이벤트가 폐기된 인스턴스에 접근하지 않게 했다.
- 공개 병합 완료: 공개 `main` 기반 `release/v0.3.0-public` 브랜치에 검증된 파일 차이만 옮겨 PR #30을 만들었다. 필수 원격 CI 통과 후 squash 병합했으며 공개 정리 이력을 유지한 병합 커밋은 `9cdd13ba7ef541e52a008c17fd1a1891e2be721d`다.
- Release 배포 완료: 병합 커밋에 주석 태그 `v0.3.0`을 게시했다. 태그 워크플로에서 버전 일치·감사·검증, Linux·macOS Intel·macOS ARM·Windows 네이티브 빌드, 자산 평탄화·중복 검사와 최종 체크섬 생성을 모두 통과하고 공개 GitHub Release를 생성했다.
- 공개 자산 검증: Release에는 AppImage·deb, macOS Intel/ARM dmg·zip, Windows NSIS·portable, OS별 간편 ZIP 4종, SBOM과 체크섬 2종 등 총 15개 자산이 올라갔다. `RELEASE-SHA256SUMS`의 14개 대상 이름이 체크섬 파일 자신을 제외한 실제 자산 14개와 일치하는 것을 확인했다.
- 컨테이너 배포 완료: 태그와 `main`의 GHCR 워크플로가 각각 linux/amd64·linux/arm64 이미지, provenance와 SBOM 게시에 성공했다. 태그 실행은 `v0.3.0`·커밋 SHA를, main 실행은 `latest`·커밋 SHA를 갱신한다. 현재 GitHub CLI 인증에는 `read:packages` 범위가 없어 패키지 API의 태그 목록을 직접 읽지는 못했지만 두 게시 워크플로의 성공 상태를 확인했다.
- 릴리스 관리 완료: PR의 `Closes #20`으로 선택형 채팅/터미널 모드 이슈가 닫혔고, 열린 이슈 0개·닫힌 이슈 1개 상태를 확인한 뒤 v0.3.0 마일스톤도 닫았다.

## 2026-08-08 Claude - PC 터미널 모드 높이·휠 스크롤 수정

- 작업 시작: 사용자가 PC 터미널 모드에서 (1) 터미널 내용이 절반쯤만 보이고 높이가 맞지 않는 문제, (2) 위로 스크롤하면 실제 CLI 터미널처럼 이전 내역이 아니라 화면(상자)만 움직이는 문제를 보고했다. 모바일은 이번 범위에서 제외한다.
- 담당과 의사결정: Claude가 담당한다. 서버 tmux pane과 클라이언트 xterm의 256×36 고정 논리 그리드 계약은 유지하고(절대 좌표 ANSI 시퀀스와 상태바 문제 재발 방지), 잘림은 행·열 변경 대신 상자 높이에 맞춘 글꼴 크기 조정으로 해결한다. 휠 스크롤은 tmux 자체 copy-mode 기록으로 연결해 실제 CLI에서 위로 스크롤한 것과 같은 결과를 보여준다.
- 계측 근거: Chromium에서 터미널 모드를 직접 렌더링해 재현했다. 고정 36행의 실제 내용 높이는 540px인데 표시 상자는 1440×1000에서 620px, 1440×900에서 520px, 1366×768에서 388px이었다. 즉 창이 조금만 작아져도 아래쪽 행이 잘리고, 1366×768에서는 36행 중 24행만 보였다.
- 검증 계획: 같은 계측 스크립트로 수정 후 각 크기에서 36행이 모두 보이는지, 휠 이벤트가 문서·상자 스크롤 대신 tmux 기록 요청으로 나가는지, copy-mode 진입/이탈과 입력 시 자동 복귀가 동작하는지 확인하고 스크린샷을 남긴다.
- 원인 1(높이): 클라이언트 xterm이 13px 고정이라 36행이 항상 540px을 차지하는데, 표시 상자는 창 높이에 따라 그보다 낮아지면 그대로 잘렸다. 행·열 수는 서버 tmux pane과 반드시 같아야 해(절대 좌표 ANSI·상태바 문제) 줄일 수 없으므로, 데스크톱(701px 이상)에서만 상자 높이에 맞춰 글자 크기를 8~14px 범위로 자동 조정하는 `fitFontSize`와 `ResizeObserver`를 넣었다. 모바일은 이번 범위가 아니라 기존 고정 크기 + 스와이프 동작을 그대로 뒀다.
- 원인 2(여백): `.chat-layout` 높이가 `100dvh - 184px`인데 실제로 위아래 쌓이는 높이는 188px(헤더 72 + 프로젝트 바 60 + main 여백 26/30)이라 문서에 4px짜리 스크롤이 남아 있었다. 188px로 맞춰 문서 스크롤을 없애고, 터미널 모드에서는 `main-chat-terminal` 클래스로 페이지 위아래 여백을 12/14px로 줄여(`100dvh - 158px`) 그만큼을 터미널에 넘겼다. 터미널 헤더·탭·호스트 여백도 조금씩 줄였다.
- 원인 3(스크롤): 웹 터미널은 `tmux attach-session` 클라이언트라 tmux가 대체 화면(`?1049h`)을 쓰고, 그래서 xterm 자체 스크롤백이 항상 비어 있다. 이 상태에서 휠은 xterm이 위·아래 방향키로 바꿔 CLI에 보내거나 바깥 상자·문서 스크롤로 새어나갔다. 실제 CLI에서 위로 스크롤한 것과 같은 이전 내역은 tmux 기록에만 있으므로, 휠을 `terminal_scroll` 요청으로 바꿔 서버가 `copy-mode -e` + `scroll-up/scroll-down`으로 tmux 기록을 옮기게 했다.
- 부작용 차단 결정: 기록 보기(copy-mode) 중에는 키 입력이 copy-mode 키 테이블에 먹히고 화면 스냅샷도 과거 내용이라 승인·작업중 판정이 어긋난다. 그래서 웹 키 입력·프롬프트 전송·응답 중단·준비 대기 시 자동으로 실시간 화면으로 되돌리고(실제 터미널에서 스크롤 중 타이핑하면 맨 아래로 돌아오는 것과 같은 동작), 기록 보기 중에는 상태 스캔을 건너뛴다. 한 번의 관성 스크롤이 tmux 명령 수십 번이 되지 않게 클라이언트에서 60ms로 모아 보내고 서버에서 200줄로 제한한다.
- 실제 확인: 임시 tmux 세션 + node-pty로 attach가 대체 화면을 쓰는 것, 스크롤 요청이 attach된 클라이언트에 과거 줄을 실제로 다시 그리는 것, 취소 시 최신 화면으로 복귀하는 것을 직접 재현했다. Chromium에서는 1440×1000·1440×900·1366×768 모두 36행 전체 표시(상자·문서 넘침 0)와, 휠이 페이지·상자를 움직이지 않고 기록 요청(+/-)으로 나가는 것을 확인했다(`artifacts/ui-terminal-height-before.png`, `artifacts/ui-terminal-height-after.png`, `artifacts/ui-chat-terminal.png`).
- 검증 완료: `npm run verify`(타입 검사, 56개 파일 383개 테스트, 프로덕션 빌드·공개 경로 검사) 통과. Playwright는 계정 환경변수가 필요한 1건만 정상 skip되고 17건 통과했다. 새 단위 테스트는 `tests/terminal-history-scroll.test.ts`와 `tests/tmux-input.test.ts`의 기록 스크롤 항목이다.
- 문서 업데이트: README에 PC 글자 크기 자동 맞춤과 휠→tmux 기록 동작을, CODETREE에 `tmux-input`·`realtime`·`session-manager`·`TerminalPanel`·스타일·테스트 항목을 반영했다.
- 실환경 재검증 요구(사용자): 목(mock) 기반 확인만으로는 부족하다는 지적에 따라, 격리한 실제 서버(포트 14099·별도 DATA_DIR·별도 HOME) + 내가 만든 실제 tmux 세션 + 실제 Chrome으로 다시 확인했다. 이 과정에서 목에서는 드러나지 않던 문제 두 가지를 찾아 추가로 고쳤다.
- 사고 기록(1차 시도): 첫 시도에서 HOME을 격리하지 않아, 새로 띄운 검증 서버가 공유 `~/.claude`의 실제 CLI 세션을 자동 임포트해 사용자님의 실행 중인 에이전트 tmux 세션에 붙었다. 그 상태에서 Playwright가 스페이스 키 1회와 휠 스크롤을 실제 세션에 보냈다. 사용자 DB(`data/*.sqlite`)는 격리되어 오염되지 않았음을 읽기 전용 조회로 확인했고(생성한 검증 계정 없음), 이후 HOME까지 격리해 내 검증용 tmux 세션 하나만 대상이 되도록 바꿨다. 검증 스크립트에도 채팅 제목 확인 가드를 넣었다.
- 추가 원인 4(페이지 스크롤): `.chat-layout` 높이를 `100dvh - 고정값`으로 잡고 있어, 헤더·프로젝트 바 외에 "에이전트 연동 필요" 배너(53px)가 뜨는 실제 환경에서는 딱 그만큼 문서 스크롤이 남았다(실측). 데스크톱 채팅 탭을 flex 레이아웃으로 바꿔 위에 무엇이 붙든 남은 높이를 그대로 쓰게 했다. 더불어 실제로 스크롤되는 요소인 `.xterm-viewport`에 `overscroll-behavior: contain`을 줘 스크롤 체이닝으로 페이지가 밀리는 경로도 막았다.
- 추가 원인 5(기록이 tmux까지 닿지 않음): 브라우저 xterm에 tmux 재그림 잔해가 스크롤백으로 쌓여 있어, 휠이 그 잔해 안에서만 맴돌고 실제 tmux 기록에 닿지 못했고 타이핑해도 맨 아래로 돌아오지 않았다. 서버 TerminalScreen과 같은 계약으로 클라이언트도 `scrollback: 0`으로 두어 기록의 정본을 tmux copy-mode 하나로 통일했다. 또 TUI가 mouse mode를 켠 동안에는 휠을 가로채지 않도록 가드를 넣었다(모바일 스와이프의 mouse 프로토콜 전달이 깨졌던 것을 E2E에서 확인해 수정).
- 예외 처리 보강: 아래로 굴려 tmux(-e)가 스스로 copy-mode를 끝낸 직후의 이동 명령이 "not in a mode"로 실패하던 것을 정상 흐름으로 처리하고, 이미 최신 화면이면 아래 방향 요청은 tmux를 건드리지 않게 했다. 승인 키 전송 경로에도 실시간 화면 복귀를 추가했다.
- 실환경 최종 결과: 1440×900·1366×768 모두 36행 전체 표시(상자·문서 넘침 0), 휠 위로 → tmux copy-mode 진입과 `[46/365]`→`[365/365]` 위치 이동으로 맨 첫 줄까지 실제 기록 표시, 페이지·상자 스크롤 0, 휠 아래로 → 최신 화면 복귀와 copy-mode 자동 종료, 기록 보기 중 키 입력 → 실시간 화면 자동 복귀를 확인했다(`artifacts/ui-terminal-real-live.png`, `artifacts/ui-terminal-real-history.png`). `npm run verify` 383개 테스트·빌드 통과, Playwright 17건 통과(계정 필요 1건 skip).
- 검증 환경 정리: 검증용 서버·tmux 세션·임시 데이터 디렉터리(/tmp/wamv)를 모두 제거했다. 사용자님의 운영 서버(14003)는 검증 내내 그대로 두었다.

## 2026-08-09 Codex - 선행 터미널 작업 재검증 및 이미지 첨부 경로 호환

- 작업 시작: 사용자 요청에 따라 선행 Claude 작업의 터미널 높이·스크롤 변경을 현재 파일 기준으로 독립 검증하고, 필요하면 최소 범위에서 보완한다.
- 추가 요청: Claude가 응답에 첨부한 이미지가 절대경로 기준일 때 보이지 않는 문제를 조사해, 프로젝트 상대경로와 허용된 절대경로를 모두 안전하게 표시할 수 있도록 경로 해석을 보완한다.
- 담당과 의사결정: Codex가 `web-agent-manager-session-context`로 선행 Claude 채팅의 마지막 결정과 검증 내용을 확인했다. 기존 미커밋 변경은 사용자 작업으로 간주해 보존하고, 현재 브랜치·코드트리·유사 첨부 유틸을 먼저 확인한 뒤 중복 없이 수정한다. 경로 호환은 임의 파일 노출을 넓히지 않도록 기존 파일 접근 정책과 프로젝트 작업공간 경계를 재사용한다.
- 검증 계획: 관련 단위 테스트와 타입 검사·빌드를 수행하고, 가능하면 실제 렌더링 경로에서 상대경로·절대경로 이미지가 모두 표시되는지 확인한다. 완료 전에 README와 CODETREE를 현재 동작에 맞춰 갱신한다.
- 선행 작업 검토 결과: 터미널 높이 보정, xterm `scrollback: 0`, WebSocket `terminal_scroll`, 서버의 tmux copy-mode 이동과 입력 전 실시간 복귀 흐름을 코드·테스트 기준으로 재검토했다. 격리한 실제 tmux 세션에서 5줄씩 연속 이동했을 때 기록 위치가 5→10으로 누적되고 copy-mode 취소 뒤 `pane_in_mode=0`으로 복귀해, 기존 구현에 추가 코드 결함은 발견하지 못했다. 다만 README·코드 주석 일부가 더 이상 존재하지 않는 xterm 로컬 스크롤백을 설명하고 있어 실제 동작(모바일 잘린 고정 화면 우선, 끝에서 tmux 기록 이동)에 맞게 바로잡았다.
- 이미지 원인과 수정: `[첨부: ...]` 표기가 경로 종류와 무관하게 숨김 업로드 전용 API로 치환되어, 해당 API가 허용하는 `.web-agent-manager-uploads/<chatId>/<file>` 외의 `artifacts/x.png`와 프로젝트 내부 절대경로가 모두 거부되고 있었다. 기존 `projectFilePathFromHref` 경계 검사를 재사용해 프로젝트·현재 worktree 내부의 상대/절대 경로를 상대경로로 정규화하고, 일반 파일 콘텐츠 API와 현재 `chatId`로 inline 이미지를 읽도록 했다. 기존 업로드 첨부는 소유권 검증 전용 API를 그대로 사용하며 프로젝트 밖 절대경로는 링크·파일 API로 노출하지 않는다.
- 중복 정리: 파일 화면에 있던 콘텐츠 URL 생성기를 `src/client/lib/file-links.ts`의 `projectFileContentUrl`로 옮겨 채팅과 파일 화면이 같은 경로 인코딩·worktree 쿼리 규칙을 사용하게 했다. 채팅 `MessageBody`와 Markdown 파일 미리보기에는 선택 채팅 ID와 worktree 경로를 전달했다.
- 브라우저 확인: mock API가 실제 1×1 PNG를 반환하는 Playwright 시나리오에서 프로젝트 상대경로와 절대경로 `[첨부: ...]` 두 장이 모두 자연 너비를 가진 이미지로 로드되고, 요청이 각각 현재 채팅의 `/files/content/...?...chatId=1`로 나가는 것을 확인했다. 같은 시나리오에서 36행 전체 표시, 페이지 넘침 0, 휠의 `terminal_scroll` 전송도 함께 통과했다.
- 최종 검증: 관련 5개 테스트 파일 36개 테스트와 타입 검사를 먼저 통과했고, `npm run verify`에서 56개 파일 388개 테스트·타입 검사·프로덕션 빌드·공개 절대 경로 검사를 모두 통과했다. 관련 Playwright 시나리오도 1건 통과했다. README와 CODETREE를 이미지 경로 호환·tmux 기록 동작·공용 URL 유틸 기준으로 갱신했다.
- 사용자 재피드백: 데스크톱 터미널이 화면의 남은 세로 공간을 채우지 못하고 중간 높이에 머무는 현상을 다시 확인한다. 앞선 검증이 36행의 상자 내부 수용 여부에 치우쳐 상자 자체 높이를 놓쳤을 가능성을 인정하고, 실제 스크린샷·DOM 높이와 flex/grid 제약을 다시 계측해 터미널 작업영역이 하단까지 확장되도록 보완한다.
- 첨부 화면 재분석: 검은 터미널 패널은 이미 화면 하단까지 정상 확장돼 있었고, 실제 문제는 고정 36행 xterm 격자가 최대 14px 글자 크기에서 중간 높이까지만 렌더링되어 프롬프트 아래로 큰 검은 빈 공간이 남는 것이었다. 상단 프로젝트·모델 바를 숨기는 방향은 문제를 잘못 짚은 변경이라 즉시 되돌렸다.
- 높이 보완 결정: 서버 tmux와 클라이언트의 256×36 논리 그리드는 ANSI 절대 좌표 호환을 위해 유지한다. 데스크톱에서 상자가 낮으면 기존처럼 글자 크기를 최소 8px까지 줄이고, 상자가 높으면 가로 폭을 넓히지 않는 xterm `lineHeight`만 최대 1.8까지 늘리는 `fitTerminalHeight`로 36행이 세로 영역을 채우게 했다. 창 크기·모델 바 변화 뒤 xterm 렌더 반영을 따라가도록 최대 4프레임 재계측한다.
- 실제 화면 확인: 사용자 첨부와 같은 2048×1114 뷰포트 및 기존 1440×1000 뷰포트에서 36행 전체가 보이고 마지막 프롬프트가 터미널 하단까지 내려오는 것을 Chromium으로 확인했다. 행 영역과 사용 가능 높이의 차이는 40px 미만, 호스트·문서 overflow는 0이며 휠→tmux 기록 요청과 모바일 동작도 기존 시나리오에서 함께 통과했다.
- 재검증 완료: 높이 회귀 기준을 추가한 관련 Playwright 시나리오가 통과했고, `npm run verify`에서 56개 파일 388개 테스트·타입 검사·프로덕션 빌드·공개 절대 경로 검사를 모두 통과했다. 검증용 Vite 서버는 종료했으며 갱신된 `artifacts/ui-chat-terminal.png`에서 마지막 36번째 행이 검은 패널 하단에 위치하는 것을 직접 확인했다.
- 사용자 지적 및 재작업 시작: 고정 36행을 유지한다는 이유로 `lineHeight`를 늘려 화면을 채운 변경은 실제 터미널 높이를 확장한 것이 아니라 행 간격을 인위적으로 벌린 잘못된 해결이었다. Codex가 이 판단 오류를 인정하고 해당 변경을 제거한다. 올바른 방향은 표시 영역에서 실제 행 수를 계산하고 클라이언트 xterm과 서버 tmux pane·화면 재구성기의 행 수를 같은 값으로 동기화하는 것이다. 기존 세션·다중 구독·모바일·ANSI 좌표에 미치는 영향을 먼저 재확인한 뒤 최소 범위의 resize 프로토콜과 회귀 테스트로 교체한다.
- 재작업 구현 완료: `fitTerminalHeight`와 글자 크기·`lineHeight` 확대 로직을 모두 제거하고 데스크톱 xterm을 13px·`lineHeight: 1`로 고정했다. 실제 렌더된 셀 높이와 패널 가용 높이로 12~120 범위의 행 수를 계산하며, 가로는 스크롤백 재줄바꿈을 피하려고 기존 256열을 유지한다. 모바일은 키보드 출현에 따른 잦은 서버 리사이즈를 피하고 기존 256×36 스와이프 동작을 유지한다.
- 동기화 결정 및 구현: WebSocket 구독에 현재 행 수를 싣고 이후 `terminal_resize`로 변경분을 보낸다. 서버는 관리자 요청만 받아 `SessionManager.resizeTerminal`에서 copy-mode를 종료한 뒤 node-pty, 실제 tmux 창, `TerminalScreen`을 동일한 256×행 크기로 바꾸고 현재 ANSI 화면을 다시 전송한다. 행 요청은 12~120으로 제한해 비정상 크기 요청의 영향을 막았다.
- 실제 검증: 격리한 tmux 세션을 256×36에서 256×58로 바꿔 window·pane 모두 정확히 256×58이 되는 것을 확인하고 세션을 제거했다. Chromium 1440×1000 및 사용자 첨부와 같은 2048×1114에서 글자·줄 간격을 늘리지 않은 채 논리 행 수가 화면 높이에 따라 증가하고 xterm 행 영역과 재전송된 마지막 줄이 패널 하단까지 내려오는 것을 확인해 `artifacts/ui-chat-terminal.png`를 갱신했다.
- 재작업 검증 완료: `npm run verify`에서 타입 검사, 56개 파일 392개 테스트, 프로덕션 빌드와 공개 경로 검사가 모두 통과했다. Playwright는 변경 시나리오를 재실행해 통과했고 전체 18개 시나리오도 계정 환경변수가 필요한 1개만 정상 skip, 나머지 17개가 통과했다. README와 CODETREE는 고정 36행·행간 확대 설명을 제거하고 실제 동적 행 동기화 동작과 테스트 기준으로 갱신했다.

## 2026-08-09 Codex - v0.3.0 누적 변경 갱신 릴리즈

- 작업 시작: 사용자가 현재 작업 트리의 모든 누적 변경을 v0.3.0에 반영해 다시 릴리즈하도록 요청했다. Codex가 현재 브랜치의 미커밋 변경 전체, 기존 v0.3.0 태그·GitHub Release·공개 저장소 상태, 릴리즈 워크플로와 문서를 다시 확인한 뒤 검증·커밋·공개 반영·배포까지 담당한다.
- 릴리즈 안전 결정: 이미 공개된 v0.3.0 태그를 임의로 이동하거나 기존 자산을 덮어쓰면 설치 재현성과 체크섬 신뢰가 깨질 수 있으므로, 먼저 원격 릴리즈 상태와 프로젝트의 기존 배포 관례를 확인한다. 기존 태그 유지가 필요한 상태라면 같은 0.3.0 계열의 명시적인 후속 태그를 사용하고, 사용자가 요구한 v0.3.0 변경 누적이라는 의미를 릴리즈 노트에 분명히 기록한다.
- 작업 계획: 변경 파일 전체를 기능 단위로 재검토하고 CHANGELOG·README·CODETREE를 실제 동작과 검증 결과에 맞춘다. 전체 단위·브라우저·패키징 검증 후 별도 릴리즈 브랜치에 논리적 커밋을 만들고, 공개 저장소 PR과 필수 CI를 통과시킨 뒤 태그·GitHub Release·멀티플랫폼 자산·컨테이너 게시 상태까지 확인한다.
- 원격 확인 및 배포 결정: 공개 `main`과 기존 `v0.3.0`은 같은 커밋이고 GitHub Release에는 15개 자산, 태그 기준 데스크톱·Docker 워크플로 성공 이력이 있다. 이번 사용자 요청은 새 기능 버전이 아니라 v0.3.0에 현재 수정 전체를 갱신하라는 명시적 요청이며, 릴리즈 워크플로도 기존 동일 태그 자산의 `--clobber` 갱신을 지원한다. 따라서 공개 PR과 CI가 통과한 뒤에만 v0.3.0 태그를 새 병합 커밋으로 이동하고, 모든 설치 파일·체크섬·Docker 태그·릴리즈 노트를 같은 소스 기준으로 다시 게시한다. 이동 전 기존 태그 커밋과 자산 목록은 이 기록에 남겨 추적 가능하게 한다.
- 의존성 감사 보완: 릴리즈 직전 CI와 같은 `npm audit --audit-level=high`에서 새로 공개된 `nanoid <3.3.17` 고위험 권고가 검출됐다. 직접 기능 의존성이 아니라 `tsup → postcss → nanoid` 전이 경로임을 확인하고 호환 범위 안의 3.3.18로 lockfile 한 항목만 갱신했다. 재감사 결과 취약점 0건이며 이 변경도 v0.3.0 갱신에 포함한다.
- 로컬 릴리즈 검증: 갱신된 lockfile로 `npm ci`를 새로 수행한 뒤 의존성 감사 0건, `npm run verify`의 타입 검사·56개 파일 392개 테스트·프로덕션 빌드·공개 절대경로 검사를 모두 통과했다. 실제 Chromium 변경 시나리오도 다시 통과했으며, `npm run release:package`로 v0.3.0 플랫폼 ZIP 4종·CycloneDX SBOM·SHA256SUMS를 새로 생성해 모든 체크섬이 일치함을 확인했다.
- 문서 정리: CHANGELOG의 0.3.0 아래에 2026-08-09 후속 안정화 항목을 추가해 동적 터미널 행, tmux 기록, 상대·절대 이미지, 회귀 테스트와 전이 의존성 보안 갱신을 명시했다. README·CODETREE와 터미널 CSS 주석도 최종 구현인 13px 정상 줄 간격·256열 고정·동적 행 동기화 기준으로 재확인했다.
- 기능 브랜치 반영: 이미지 경로 호환, 터미널 동적 행·tmux 기록, v0.3.0 문서·보안 갱신을 논리 단위 커밋 `a5f0d21`, `dadf50b`, `5ace7b8`로 나누고 `origin/feature/chat-git-worktrees`에 게시했다.
- 공개 병합 완료: 공개 `main` 기반 `release/v0.3.0-update-public` 브랜치에서 동일 변경을 독립적으로 `npm ci`, 고위험 이상 감사 0건, `npm run verify`로 재검증했다. PR #31의 필수 CI가 성공한 뒤 squash 병합했으며 새 공개 커밋은 `d562619bace0993756aea5d12c9f9f677b69f4e0`이다. 병합 뒤 main 검증 실행 `31296690331`과 `latest` Docker 실행 `31296690338`도 성공했다.
- 태그 갱신 완료: 사용자 요청과 사전 기록한 결정에 따라 주석 태그 `v0.3.0`을 기존 공개 커밋 `9cdd13ba7ef541e52a008c17fd1a1891e2be721d`에서 새 병합 커밋 `d562619bace0993756aea5d12c9f9f677b69f4e0`으로 강제 이동했다. 원격 태그 객체 `325ae5c374130afce323f2a933f097cdab7611da`의 peeled commit이 새 병합 커밋과 일치함을 확인했다.
- 재배포 완료: 태그 기준 데스크톱 실행 `31296906108`에서 버전 일치, 감사, 전체 검증, Linux·macOS Intel·macOS ARM·Windows 빌드, 자산 중복 검사, 최종 체크섬과 기존 Release 자산 갱신이 모두 성공했다. 태그 Docker 실행 `31296906101`도 linux/amd64·linux/arm64 게시에 성공했다.
- 공개 릴리즈 최종 검증: GitHub Release는 정식 `v0.3.0` 상태를 유지하며 2026-08-09에 새로 생성된 설치 파일·간편 ZIP 4종·SBOM·체크섬 등 총 15개 자산을 제공한다. `RELEASE-SHA256SUMS`의 14개 대상 이름이 자기 자신을 제외한 공개 자산과 일치하고, 그 안에 기록된 `SHA256SUMS` 해시도 실제 다운로드 파일의 SHA-256과 일치함을 확인했다. 릴리즈 노트에는 `2026-08-09 후속 안정화` 내용을 반영했다.

## 2026-08-09 Codex - v0.3.0 이후 이슈·PR 정리

- 작업 시작: 사용자가 v0.3.0 갱신 릴리스 뒤 공개 저장소의 이슈와 PR을 정리하도록 요청했다. Codex가 열린 이슈·PR, 마일스톤, 병합·CI·릴리스 반영 여부를 현재 원격 상태에서 다시 확인한다.
- 정리 원칙: 이미 v0.3.0에 반영된 이슈·중복 PR은 근거가 되는 병합 커밋과 릴리스 링크를 남기고 닫는다. 아직 필요하고 안전하게 병합 가능한 유지보수 PR은 diff와 CI를 검토해 처리하며, 불확실하거나 별도 제품 판단이 필요한 항목은 임의로 닫지 않는다.
- 이슈 정리 완료: 열린 이슈 #27의 수용 조건이 v0.3.0 공개 코드와 문서·테스트에 반영된 것을 다시 확인했다. 닫힌 v0.3.0 마일스톤에 연결하고 PR #30·#31 및 릴리스 링크가 포함된 완료 댓글을 남긴 뒤 `completed`로 닫았다. v0.3.0 마일스톤은 열린 이슈 0개·완료 이슈 2개가 됐다.
- PR 검토·병합: 유일한 열린 PR #29는 직접 운영 의존성 `ws`만 8.21.1에서 8.21.2로 올리는 semver patch이며 `package.json`·lockfile 외 변경이 없고 필수 CI가 성공한 상태였다. 제품 기능 변경이 없는 릴리스 후 유지보수 변경으로 판단해 squash 병합했으며 공개 `main` 커밋은 `2b88c312415d16fc9e812c8587206b0c13010c89`이다. 이미 게시한 v0.3.0 태그는 `d562619bace0993756aea5d12c9f9f677b69f4e0`에 그대로 유지했다.
- 병합 후 검증: 공개 main 검증 실행 `31297665872`와 linux/amd64·linux/arm64 `latest` Docker 게시 실행 `31297665884`가 모두 성공했다. 공개 저장소의 열린 이슈와 열린 PR이 각각 0개인지 재확인했다.
- 비공개 기능 브랜치 동기화: 다음 공개 반영에서 의존성을 다시 낮추지 않도록 현재 기능 브랜치의 `package.json`·lockfile도 `ws 8.21.2`로 맞췄다. `npm ci`, 고위험 이상 감사 0건, 타입 검사, 56개 파일 392개 테스트, 프로덕션 빌드와 공개 경로 검사를 통과했다.

## 2026-08-09 Codex - GitHub 무료 저장소 용량 진단

- 작업 시작: 사용자가 GitHub 무료 저장소 용량이 가득 찼다는 알림을 전달했다. Codex가 공개 저장소의 Actions 아티팩트·캐시, GitHub Release 자산, GHCR 패키지 버전별 용량과 보존 필요성을 읽기 전용으로 조사한다.
- 안전 원칙: Release 설치 파일과 현재 배포 태그는 사용자가 내려받는 공개 산출물이므로 임의로 삭제하지 않는다. 진단 결과로 복구 가능한 CI 중간 산출물·캐시·오래된 untagged 컨테이너처럼 안전한 정리 대상을 정확히 특정한 뒤 삭제 범위를 사용자에게 알린다.
- 원인 계측: 공개 저장소에 Actions 아티팩트 37개 5,309,108,264바이트와 캐시 206개 7,815,861,441바이트가 누적되어 합계 약 13.13GB를 차지했다. 데스크톱 빌드 한 번의 중간 아티팩트가 약 1.05~1.08GB이며, 태그 빌드는 이를 Release 자산으로 다시 게시한 뒤에도 기본 90일 동안 중간 사본을 보존하고 있었다. v0.2.0·v0.3.0 Release 자산은 각각 약 1.06GB·1.09GB지만 공개 다운로드 원본이므로 정리 대상에서 제외했다.
- 원격 정리 완료: Release·태그·소스·GHCR 이미지는 보존하고, Release와 중복된 Actions 아티팩트 37개와 빌드에서 재생성 가능한 Actions 캐시 206개만 삭제했다. 삭제 후 두 API의 항목 수와 사용량이 모두 0인지, v0.3.0 Release 자산 15개가 그대로인지 재확인했다. 계정 전체 청구량과 GHCR 버전 목록은 현재 GitHub CLI 권한에 `user`·`read:packages` 범위가 없어 조회하지 않았고, 민감 권한 확대나 토큰 접근을 요청하지 않았다.
- 재발 방지 구현: 데스크톱 교차 플랫폼 중간 아티팩트 보존 기간을 1일로 제한했다. Docker 공식 Action의 지원 환경변수로 Buildx 기록 아티팩트 업로드를 끄고, 모든 중간 레이어를 저장하던 `mode=max` 캐시를 최종 이미지에 포함되는 레이어 중심의 `mode=min`으로 축소했다. README와 CODETREE에도 저장 공간 정책을 반영했다.
- 로컬 검증: `npm run verify`에서 타입 검사, 56개 파일 392개 테스트, 프로덕션 빌드와 공개 경로 검사를 통과했고 `git diff --check` 대상 변경도 확인한다. 공개 저장소에는 별도 브랜치와 PR의 필수 CI를 거쳐 반영한다.
- 청구 알림 해석: 사용자가 전달한 실제 알림은 GitHub Free의 월 0.5GB Actions 아티팩트 저장 허용량 중 0.45GB(90%)가 이번 주기에 누적됐고 2026-09-01 초기화 예정이라는 내용이다. GitHub 공식 청구 정책을 재확인해, 아티팩트 삭제는 현재 저장량과 이후 시간당 누적을 즉시 멈추지만 이미 이번 주기에 누적된 0.45GB는 줄이지 않으며 사용량 표시는 최대 6~12시간 늦게 갱신될 수 있음을 사용자에게 정정 안내했다. Actions 캐시는 아티팩트와 별도인 저장소당 10GB 무료 범위다.
- 공개 반영 완료: 공개 `main` 기반 PR #32에서 별도 `npm ci`, 감사 0건, 392개 테스트·타입 검사·빌드를 통과한 뒤 squash 병합했다. 병합 커밋은 `28f3ba4608512d8fc9ba272e1b6ebaadb3a1c48f`이며 main 검증 실행 `31307364612`와 새 저장 정책을 실제 사용한 Docker 실행 `31307364617`이 모두 성공했다.
- 재발 방지 실측: 새 Docker 빌드 뒤 Buildx 기록을 포함한 Actions 아티팩트는 0개를 유지했고, 처음부터 다시 생성한 `mode=min` 캐시는 31개 977,747,806바이트로 기존 7,815,861,441바이트보다 약 87.5% 줄어 별도 10GB 무료 범위 안에 있다. v0.3.0 Release 자산 15개와 약 1.09GB 공개 다운로드는 그대로 보존했다.

## 2026-08-10 Codex - Android WebView 앱 및 홈 화면 위젯 구현 착수

- 작업 시작: 사용자 요청에 따라 기존 web-agent-manager를 Android WebView 앱으로 감싸고, FCM 푸시 알림 연동 기반과 Claude·Codex 사용량/CPU/메모리를 표시하는 여러 크기의 홈 화면 위젯을 추가해 설치 가능한 APK를 산출한다.
- 담당과 의사결정: Codex가 기존 웹 서버·모바일 UI·사용량 및 시스템 지표 API를 먼저 재확인하고 가능한 기존 기능을 재사용한다. Android 코드는 별도 `android/` 프로젝트로 추가하며 보호 브랜치가 아닌 `feature/android-webview-app`에서 작업한다.
- 보안 결정: 외부망에서 내부망 전용 권한을 우회하거나 `.env` 내용을 APK에 포함하는 기능은 만들지 않는다. 앱은 사용자가 설정한 HTTPS 서버 주소와 기존 로그인/서버 권한을 그대로 사용하고, 외부 접속이 필요하면 VPN·Tailscale 같은 보안 터널 또는 서버의 신뢰 프록시/네트워크 설정을 관리자가 별도로 구성하는 구조로 둔다. 인증서 검증과 WebView 안전 설정은 유지한다.
- 구현 계획: Android 빌드 환경과 서버 API 계약을 확인한 뒤 (1) 서버 주소 설정·WebView 세션/파일 업로드/다운로드/뒤로가기, (2) FCM 토큰 등록을 위한 서버·앱 인터페이스와 인증 설정이 없을 때의 안전한 비활성 동작, (3) 반응형 WebView 설정, (4) 1×1·2×1·1×2·2×2 위젯 레이아웃과 주기/수동 새로고침, (5) 문서·코드트리·테스트·APK 빌드 검증 순으로 진행한다.
- 서버 구현 완료: 기존 `UsageMonitor`·`SystemMetricsService` 스냅샷에서 위젯에 필요한 Claude·Codex 사용률과 CPU·RAM만 추린 `GET /api/mobile/widget`을 추가했다. 로그인한 관리자 기기의 FCM 토큰 등록·해제·테스트 API, `push_devices`·`fcm_deliveries` 스키마와 이벤트/기기별 중복 방지·폐기 토큰 비활성화를 구현하고 `NotificationHub`의 Slack·ntfy 옆 채널로 연결했다. FCM은 기본 비활성이며 Google 기본 자격증명과 HTTP v1을 사용하므로 서비스 계정 원문을 앱·DB·로그에 전달하지 않는다.
- Android 구현 완료: Android 6(API 23) 이상 Java WebView 앱을 `android/`에 추가했다. 첫 실행 서버 주소 설정, 동일 origin 안의 기존 로그인/웹소켓/파일 선택·다운로드, 외부 링크 분리, 인증서 검증 유지, Android 13+ 알림 권한, 선택적 Firebase 토큰 동기화와 전경 알림을 구현했다. 1×1은 StackView로 Claude·Codex를 스와이프하고, 2×1은 좌우 두 모델, 1×2는 상하 두 모델, 2×2 이상은 두 모델+CPU+RAM을 표시하며 30분 주기·수동·앱 페이지 로드 시 갱신한다.
- 외부망 결정 확정: 앱이 내부망 capability를 위조하지 않으며 서버의 실제 요청 주소·관리자·CSRF 판정을 그대로 따른다. 외부에서 `.env` 등 내부망 전용 기능이 필요하면 공개 IP를 신뢰 처리하는 대신 Tailscale/VPN 사설 주소로 접속하고 필요한 CIDR만 서버 신뢰 목록에 추가하도록 README와 Android 문서에 명시했다.
- 의존성 결정: 처음 검토한 최신 Firebase Admin SDK가 운영 `npm audit`에서 취약한 하위 `uuid`를 끌어와, 강제 구버전 설치 대신 취약점 0건인 최신 `google-auth-library`와 공식 FCM HTTP v1 호출로 교체했다. Android Firebase Messaging SDK는 `google-services.json`이 있을 때만 Google Services 플러그인을 적용해 인증 파일 없는 기본 APK도 빌드되게 했다.
- 문서 업데이트: README에 Android 설치·위젯 크기·외부 접속 보안·FCM 흐름을 추가하고, `android/README.md`에 빌드/서명/자격증명 배치 절차를 분리했다. CODETREE에는 모바일 라우터·FCM 서비스·DB·Android 클래스/리소스/빌드 및 회귀 테스트 역할을 추가했고 `.env.example`에는 값 없는 FCM 변수명만 문서화했다.
- 검증 완료: 운영 의존성 `npm audit --omit=dev` 0건, `npm run verify`에서 57개 파일 395개 테스트·타입 검사·프로덕션 빌드·공개 경로 검사를 통과했다. JDK 17·AGP 9.3·Gradle 9.5·SDK/API 36에서 `assembleRelease`와 `lintRelease`, 설치용 `assembleDebug`를 실제 실행했다. APK는 v1/v2 서명 검증과 manifest(package `com.webagentmanager.android`, minSdk 23, targetSdk 36), APK 내 `.env`·`google-services.json` 부재를 확인했다.
- 산출물: 개발 서명된 설치 가능 APK를 `release/android/web-agent-manager-android-0.1.0.apk`로 만들었고 SHA-256은 `d00755067fae38b35e32208ee6a385911b3e870a4ed05c12844282f0df28e869`다. 실제 기기/에뮬레이터가 이 환경에 없어 설치 후 터치·런처별 위젯 렌더링은 수행하지 않았으며, Android lint와 리소스/Java 컴파일·APK 패키징으로 검증했다.

## 2026-08-10 Codex - Android 시스템 바 겹침 수정 착수

- 작업 시작: 사용자가 제공한 실제 기기 화면에서 상단 상태바가 웹 헤더를 덮고 하단 Android 내비게이션바가 채팅 입력창·웹 하단 탭바를 덮는 현상을 확인했다. Codex가 보호 브랜치와 분리한 `fix/android-system-bar-insets`에서 원인을 수정하고 새 설치 APK를 만든다.
- 원인과 결정: targetSdk 36의 edge-to-edge 화면에서 `MainActivity`가 시스템 `WindowInsets`를 반영하지 않은 채 WebView를 전체 창 크기로 배치한 것이 원인이다. 모바일 CSS에 기기별 고정 여백을 중복 추가하지 않고, 네이티브 루트가 상태바·디스플레이 컷아웃·내비게이션바·화면 키보드의 실제 inset을 받아 WebView 가용 영역을 제한하도록 최소 수정한다.
- 검증 계획: Android debug/release 빌드와 lint를 실행하고 APK 서명·버전·체크섬을 확인한다. 실제 기기 재현 환경은 없으므로 시스템 inset 계산은 Android API 계약과 lint/컴파일로 검증하고, 사용자가 설치해 최종 화면을 확인할 수 있도록 버전을 올린 별도 APK를 제공한다.
- 구현 완료: Android 11(API 30) 이상에서 edge-to-edge 창을 명시하고 `MainActivity.applySystemInsets`가 상태바·디스플레이 컷아웃·하단 내비게이션바의 실제 여백과 화면 키보드 높이 중 큰 하단 값을 루트에 적용하도록 했다. WebView와 서버 설정 버튼이 모두 안전 영역 안에서 측정되므로 CSS의 `100dvh`와 고정 하단 탭바도 실제 사용 가능 화면을 기준으로 배치된다. inset을 직접 처리한 뒤 자식에는 소비해 웹 safe-area와 이중 적용되지 않게 했고, API 23~29는 기존 플랫폼의 decor 맞춤 동작을 유지했다.
- 문서·버전: README와 Android README에 시스템 UI·키보드 겹침 방지 계약을 기록하고 CODETREE에 `applySystemInsets`의 역할을 추가했다. 수정 APK를 기존 설치본과 구분할 수 있도록 versionCode를 2, versionName을 0.1.1로 올렸다.
- 검증 완료: SDK/API 36에서 clean debug/release APK 빌드와 debug/release lint를 모두 통과했다. 설치용 debug APK가 package `com.webagentmanager.android`, versionCode 2, versionName 0.1.1, minSdk 23, targetSdk 36인지 확인했고 v1·v2 서명 검증도 통과했다. `git diff --check`와 두 Android APK의 체크섬 검증도 성공했다.
- 산출물: 개발 서명된 설치 가능 APK는 `release/android/web-agent-manager-android-0.1.1.apk`이며 SHA-256은 `38659fd8abdebb2c9a0c6969991e019a0975c63f99537c7ad6f00428e72d0f6d`다. 이 환경에는 실제 Android 기기·에뮬레이터가 없어 사용자가 제공한 기기에서의 최종 육안 확인은 새 APK 설치 후 진행해야 한다.

## 2026-08-10 Codex - Android 설정 버튼 조작 및 앱 기기 인증 착수

- 작업 시작: 사용자 요청에 따라 화면을 가리는 네이티브 설정 버튼을 이동·숨김·복원할 수 있게 하고, 내부망에서 앱 기기를 최초 인증한 뒤에는 외부망의 같은 앱 로그인 세션도 내부망 전용 기능을 사용할 수 있게 한다. Codex가 `feature/android-device-trust-controls` 브랜치에서 서버·Android·테스트·문서를 함께 수정한다.
- UI 결정: 설정 버튼은 터치 드래그 후 가장 가까운 좌우 가장자리에 고정하고 위치를 앱 전용 저장소에 보존한다. 길게 누르면 작은 가장자리 복원 탭으로 접고, 복원 탭을 누르면 같은 위치에 버튼을 다시 표시해 숨긴 뒤 설정 진입 경로가 사라지는 문제를 막는다.
- 보안 결정: 외부 IP를 내부망으로 위조하거나 영구 bearer 토큰을 WebView에 주입하지 않는다. 내부망의 로그인된 관리자만 Android Keystore 공개키를 서버에 등록할 수 있고, 이후 서버의 일회성 challenge를 기기 내보내기 불가 개인키로 서명한 경우에만 현재 웹 로그인 세션을 신뢰 capability로 승격한다. 기기 등록·challenge·서명은 사용자 로그인 및 기존 CSRF 검증을 함께 요구하고, 로그아웃·세션 만료 뒤에는 다시 서명해야 한다.
- 구현 계획: 실제 네트워크 신뢰와 앱 세션 신뢰를 요청에서 구분하고, SQLite에 기기 공개키·일회성 challenge·세션의 인증 기기 관계를 추가한다. 모바일 API와 서명 검증 테스트를 만든 뒤 Android 설정 UI·키 생성/등록/자동 인증을 연결하고, README·Android README·CODETREE를 갱신해 전체 서버/Android 검증과 새 APK 산출을 수행한다.
- 설정 UI 구현 완료: 기존 고정 톱니를 `FloatingSettingsController`로 분리했다. 터치 이동 뒤 가까운 좌우 가장자리에 붙이고 세로 비율을 앱 전용 설정에 저장하며, 화면 회전·상하 시스템 inset·키보드 변화 때 새 안전 영역 안으로 다시 제한한다. 길게 누르거나 설정 대화상자의 숨기기 버튼을 선택하면 폭 16dp의 가장자리 탭으로 접고 탭을 누르면 복원한다.
- 서버 기기 신뢰 구현: 실제 접속망 판정 `networkOriginTrusted`와 최종 기능 capability `trustedNetwork`를 분리했다. 실제 내부망의 로그인된 관리자만 P-256 공개키를 `mobile_trusted_devices`에 등록할 수 있고, `mobile_trust_challenges`의 웹 세션별 2분·1회 challenge 서명을 검증하면 그 `web_sessions` 행만 등록 기기와 연결한다. 세션 loader가 활성 기기 관계를 확인해 기존 숨김 파일·삭제·프로세스 종료 등의 공통 판정을 그대로 재사용하므로 기능별 우회 코드는 추가하지 않았다.
- Android 기기 인증 구현: 앱 설치 단위 P-256 개인키를 Android Keystore에 생성하고 공개키만 내부망 등록 API로 보낸다. 등록 뒤와 이후 외부 로그인 페이지 로드 때 네이티브 공통 쿠키·CSRF 클라이언트가 challenge를 받아 `SHA256withECDSA`로 서명하고, 성공하면 WebView를 한 번 다시 불러 실제 UI 권한 상태도 갱신한다. 인증 해제는 현재 서버의 해당 기기·모든 연결 세션·미사용 challenge를 비활성화하고 현재 주소의 기기 식별자를 삭제한다.
- 보안 검증: 외부망 최초 등록 거부, P-256 외 키 거부, 잘못된 서명 거부, 성공 challenge 재사용 거부, 다른 로그인 세션에는 권한이 전파되지 않는 세션 단위 승격, 기기 해제 후 capability 회수를 실제 EC 키와 SQLite/HTTP 미들웨어로 검증했다. 기존 관리자 역할·로그인 쿠키·CSRF가 모든 등록/서명/해제 요청에 함께 필요하며 외부 앱 주소는 기존 정책상 HTTPS만 허용된다.
- 문서 업데이트: README와 Android README에 설정 버튼 조작, 내부망 최초 기기 등록, 외부 로그인 자동 서명, 인증 해제 절차와 HTTPS 요구를 기록했다. CODETREE에는 실제 망·앱 신뢰 결합, 새 DB 스키마·서비스·라우터·Android 컨트롤러/Keystore 클라이언트·회귀 테스트 역할을 추가했다.
- 검증 완료: `npm run verify`에서 타입 검사, 58개 파일 398개 테스트, 프로덕션 클라이언트/서버 빌드와 공개 경로 검사를 통과했다. Android SDK/API 36에서 clean debug/release 빌드와 양쪽 lint를 통과했으며 APK package `com.webagentmanager.android`, versionCode 3, versionName 0.2.0, minSdk 23, targetSdk 36과 v1·v2 서명을 확인했다.
- 산출물: 개발 서명된 설치 가능 APK는 `release/android/web-agent-manager-android-0.2.0.apk`이며 SHA-256은 `e851c490d8e96a3fcf48001deedd98eba3905d804a2be0170c5c6f1da960e632`다. 서버에도 이번 공개키/서명 API와 DB 마이그레이션 코드가 배포·재시작돼야 앱 인증이 동작한다. 실제 Android 기기·에뮬레이터가 없는 환경이라 드래그·축소 탭·Keystore의 최종 실기기 조작은 사용자가 새 APK 설치 후 확인해야 한다.
- 주소 전환 보완 착수: 최초 등록 때 쓴 내부망 IP/VPN URL과 이후 외부 HTTPS URL이 서로 다르면 주소별 Keystore 별칭·기기 ID 저장만으로는 같은 서버 등록을 찾지 못하는 경계를 최종 검토에서 확인했다. Codex는 기기 개인키를 앱 설치 단위로 유지하고, 로그인 사용자 범위에서 공개키 지문으로 기존 서버 기기를 안전하게 resolve한 뒤 현재 주소에 기기 ID를 연결하도록 보완한다. 공개키·기기 ID는 비밀이 아니며 서명 challenge의 세션 결합·1회성·CSRF 조건은 그대로 유지한다.
- 주소 전환 보완 완료: Android는 앱 설치 단위 Keystore 키를 유지하고 주소별로 확인된 기기 ID만 캐시한다. 새 외부 주소에서 ID가 없거나 오래됐으면 `/api/mobile/trust/resolve`가 로그인 사용자와 공개키 지문이 모두 맞는 활성 기기를 찾아 현재 주소에 다시 연결한다. 다른 서버·다른 사용자는 같은 지문을 조회할 수 없고 서명 전에는 capability가 생기지 않는다. resolve HTTP 계약을 회귀 테스트에 추가한 뒤 `npm run verify` 398개 테스트·프로덕션 빌드와 Android clean debug/release·양쪽 lint를 다시 통과했고 최종 APK와 체크섬을 재생성했다.

## 2026-08-10 Codex - 과거 Claude 리밋 문구 반복 감지 수정 착수

- 작업 시작: 사용자가 #190 채팅에서 이미 해소된 Claude 세션 리밋 문구가 기록 중간에 남아 있어, 터미널 실행 때 이를 현재 리밋으로 다시 감지하고 실제 사용량이 남은 상태에서 `계속` 호출을 반복하는 현상을 보고했다.
- 담당과 의사결정: Codex가 `web-agent-manager-session-context`로 #190의 현재 기록을 재확인하고, 리밋 감지·대기 등록·사용량 회복 재개 흐름을 추적한다. 과거 문구 뒤에 사용자 입력과 Claude 응답이 이어진 경우를 현재 리밋으로 취급하지 않도록 기존 파서와 상태 흐름을 최소 수정하며, 아직 해소되지 않은 최신 리밋 감지는 유지한다.
- 검증 계획: #190과 같은 `리밋 문구 → 사용자 계속 → 후속 도구/최종 답변` 기록을 회귀 테스트로 추가하고, 관련 리밋 감지·자동 재개 테스트와 전체 로컬 검증을 실행한다. 완료 전 README와 CODETREE에서 변경된 동작과 코드 역할을 최신화한다.
- 원인 확인: Claude의 다른 선택/승인 화면은 프롬프트 뒤에 새 입력·응답이 이어졌는지 검사했지만, 선택 메뉴 없는 `session_limit_notice`만 현재 pane 전체에서 `⎿ You've hit your session limit` 마지막 일치 문구를 찾은 즉시 대기로 등록했다. 세션 resume가 #190 기록 중간의 8월 8일 리밋 문구를 다시 그리면, 그 뒤에 `계속`·도구 실행·최종 답변이 이미 있어도 과거 배너를 새 리밋으로 오판할 수 있었다.
- 구현 완료: `hasOnlyIdleChromeAfter`를 추가해 선택 메뉴 없는 리밋 배너 뒤에는 Claude의 유휴 입력 프롬프트, 작업 시간, 모드·캐시 상태줄, 구분선만 남은 경우에만 현재 리밋으로 감지한다. 사용자 입력·도구 결과·후속 답변 등 실질적인 대화가 하나라도 이어지면 해소된 과거 배너로 제외한다. 기존 최신 배너와 예전·최신 선택 메뉴 감지 및 실제 사용량 회복 후 자동 재개 흐름은 유지했다.
- 문서 업데이트: README의 자동 재개 설명에 선택 메뉴 없는 Claude 배너의 최신성 조건을 추가하고, CODETREE의 `ClaudeAdapter` 관련 함수·회귀 테스트 역할을 갱신했다.
- 검증 완료: #190 형태의 `과거 배너 → 계속 → 도구 실행·최종 답변 → 새 입력`은 감지하지 않고 최신 배너+유휴 프롬프트는 계속 `session_limit_notice`로 감지하는 테스트를 추가했다. 관련 집중 테스트 72개와 `npm run verify`의 타입 검사, 58개 파일 400개 테스트, 프로덕션 클라이언트·서버 빌드 및 공개 경로 검사를 통과했다. 개발 서버는 watch 모드에서 변경 소스를 자동 재기동해 반영했으며, 구조화 문맥상 #190은 이미 stopped이고 tmux pane도 종료돼 실제 pane 재검사는 할 수 없었다.

## 2026-08-10 Codex - Android 외부 주소 전환 인증 유지 재점검 착수

- 작업 시작: 사용자가 내부망에서 앱 기기를 인증한 뒤 외부망 URL로 바꾸면 인증이 풀린다고 실기기 결과를 알려왔다. Codex가 기존 공개키 resolve 구현뿐 아니라 URL 저장·WebView 쿠키 생성·페이지 완료 자동 서명 순서를 다시 추적해 재현 가능한 원인을 찾고 최소 수정한다.
- 보안 결정: 내부망 인증을 외부 주소의 로그인 쿠키로 복사하지 않는다. 대신 등록 기기 ID·공개키가 일치하는 경우에만 외부 주소에서 짧은 일회성 challenge를 발급하고, 설치 단위 Keystore 개인키 서명이 맞으면 해당 관리자·기기에 직접 연결된 새 만료 웹 세션과 capability를 함께 발급한다. 영구 bearer 토큰은 만들지 않고 명시적 로그아웃 뒤에는 자동 재로그인하지 않는다.
- 검증 계획: 주소 전환 시나리오의 Android 로직과 서버 resolve API 계약을 회귀 검증하고, README·Android README·CODETREE를 현재 동작에 맞춘 뒤 서버 전체 검증과 Android 빌드·lint를 실행해 새 APK를 생성한다.
- 원인 확인: 기존 `/mobile/trust/resolve`는 외부 주소에서도 같은 등록 기기를 찾을 수 있게 했지만 호출 자체가 이미 로그인된 웹 세션을 요구했다. WebView 쿠키는 origin별로 분리되므로 내부 IP에서 외부 도메인으로 URL을 바꾸는 순간 외부 주소에는 로그인 쿠키가 없고, 자동 서명 단계까지 진입하지 못해 사용자가 본 것처럼 인증이 풀렸다.
- 서버 구현 완료: 로그인 전용 `mobile_trust_login_challenges`와 공개 bootstrap 라우터를 추가했다. 활성 관리자 기기 ID와 P-256 공개키가 맞아야 2분 challenge를 발급하며, challenge·요청 origin을 함께 서명한 값을 한 번 검증한 뒤에만 해당 사용자·기기가 연결된 만료 웹 세션과 기존 HttpOnly 쿠키를 만든다. 서명을 origin에 결합해 다른 서버의 challenge 중계를 거부하고, 기기 해제는 로그인용 challenge까지 함께 삭제한다.
- Android 구현 완료: 기존 0.2.0 설치가 주소별로 저장한 기기 ID도 모두 찾아 새 외부 origin에서 challenge를 서명하고, 응답의 `Set-Cookie`를 WebView CookieManager에 설치한 뒤 한 번 새로고침한다. origin별 자동 로그인은 한 번만 시도해 명시적 로그아웃 뒤 무한 재로그인을 막고, 설정의 `연결`을 누르면 사용자가 현재 주소에서 재시도할 수 있다. 버전은 versionCode 4·versionName 0.2.1로 올렸다.
- 문서 업데이트: README와 Android README의 외부 주소 절차를 재로그인이 필요 없는 기기 서명 세션 발급 방식으로 바로잡고, 쿠키 비복사·origin 결합·명시적 로그아웃 유지 조건을 기록했다. CODETREE에는 공통 세션·쿠키 생성 함수, 공개 bootstrap 라우터, 두 challenge 테이블과 Android 주소 전환 흐름 및 회귀 테스트 역할을 반영했다.
- 검증 완료: 집중 인증 테스트 12개와 `npm run verify`의 타입 검사, 58개 파일 401개 테스트, 프로덕션 클라이언트·서버 빌드 및 공개 경로 검사를 통과했다. Android API 36에서 clean debug/release 빌드와 양쪽 lint를 통과했고 APK의 package `com.webagentmanager.android`, versionCode 4, versionName 0.2.1, minSdk 23, targetSdk 36 및 v1·v2 서명을 확인했다. 실제 origin 미결합 서명과 challenge 재사용이 거부되는 것도 HTTP 회귀 테스트로 검증했다.
- 산출물: 설치 APK는 `release/android/web-agent-manager-android-0.2.1.apk`이며 SHA-256은 `f99e85c82015dfadedc759b6b8370cd1000c74075137b8b12241ae7bc67bc68b`다. 현재 14003 개발 서버는 watch 재기동 후 `/health`가 계속 `{"ok":true}`를 반환한다. Android 실기기·에뮬레이터는 이 환경에 없어 최종 URL 전환 동작은 사용자가 새 APK를 덮어 설치해 확인해야 한다.

## 2026-08-10 Codex - Android 다운로드 저장 위치 수정 착수

- 작업 시작: 사용자가 앱의 다운로드 버튼을 누르면 시작 안내는 뜨지만 실제 다운로드 파일을 찾을 수 없다고 실기기 결과를 알려왔다. Codex가 WebView `DownloadListener`와 Android `DownloadManager` 대상 경로·권한을 재확인하고, 완료 파일이 시스템 다운로드 화면에 보이도록 수정한다.
- 원인과 결정: 현재 `setDestinationInExternalFilesDir`는 파일을 앱 전용 `Android/data/.../files/Download` 아래에 저장해 최신 Android 파일 앱의 일반 다운로드 목록에서 찾기 어렵다. Android 10 이상은 공용 `Downloads`에 직접 저장하고, Android 9 이하는 저장소 권한을 명시적으로 받은 뒤 같은 공용 위치에 저장하도록 한다. enqueue 성공만으로 완료라고 오인하지 않도록 안내 문구도 실제 저장 위치를 포함한다.
- 검증 계획: 다운로드 권한·공용 경로 분기와 기존 동일-origin 쿠키 전달을 Android 컴파일·debug/release 빌드·lint로 확인하고, README·Android README·CODETREE를 갱신한 뒤 버전을 올린 새 설치 APK와 체크섬을 만든다.
- 구현 완료: `MainActivity`의 다운로드 요청은 기존과 동일하게 설정 서버 내부 URL만 허용하고 WebView 로그인 쿠키·User-Agent·MIME을 `DownloadManager`에 전달한다. 저장 대상만 앱 전용 외부 디렉터리에서 시스템 공용 `Downloads`로 바꿨고 안내 문구에 `내 파일 > 다운로드 > 파일명`을 표시한다. Android 10 이상은 scoped storage 계약으로 별도 권한 없이 저장하며, Android 6~9는 maxSdkVersion 28의 `WRITE_EXTERNAL_STORAGE` 권한을 승인받은 뒤 보류 요청을 재개한다.
- 문서·버전: README와 Android README에 공용 다운로드 위치와 구버전 권한 동작을 추가하고 CODETREE의 `MainActivity` 역할을 갱신했다. 기존 설치본과 구분하도록 versionCode 5·versionName 0.2.2로 올렸다.
- 검증 완료: `npm run verify`의 타입 검사, 58개 파일 401개 테스트, 프로덕션 클라이언트·서버 빌드와 공개 경로 검사를 통과했다. Android API 36에서 clean debug/release 빌드와 양쪽 lint를 통과했으며, APK의 package `com.webagentmanager.android`, versionCode 5, versionName 0.2.2, minSdk 23, targetSdk 36, v1·v2 서명과 Android 9 이하로 제한된 저장 권한을 확인했다.
- 산출물: 설치 APK는 `release/android/web-agent-manager-android-0.2.2.apk`이며 SHA-256은 `6595792e837473b34c65de1e7fd284bf8ac3dfba18d5a3266cf3b46d79c40219`다. 실제 Android 기기가 없는 환경이라 공용 Downloads 노출의 최종 육안 확인은 사용자가 새 APK를 덮어 설치한 뒤 진행해야 한다.

## 2026-08-10 Codex - 파일 미리보기 다운로드 버튼 추가 착수

- 작업 시작: 사용자가 파일 목록 행뿐 아니라 파일 미리보기 창에서도 현재 파일을 바로 내려받을 수 있는 버튼을 요청했다. Codex가 기존 파일 다운로드 API와 채팅 worktree `chatId` 전달 방식을 재사용해 미리보기 헤더 액션에 다운로드 버튼을 최소 추가한다.
- UI 결정: 편집 가능 여부나 미리보기 성공 여부와 무관하게 선택된 실제 파일은 항상 다운로드할 수 있으므로, 미리보기 헤더의 편집 버튼 앞에 동일한 `Download` 아이콘 링크를 둔다. 기존 헤더 버튼 크기·hover 스타일과 모바일 전체화면 sticky 헤더를 그대로 재사용하고 파일 경로 기반 접근성 이름을 제공한다.
- 검증 계획: Playwright 파일 미리보기 시나리오에 링크 URL·접근성 이름을 검증하는 회귀 단언을 추가하고, README·CODETREE를 갱신한 뒤 전체 로컬 검증을 실행한다.
- 구현 완료: `projectFileDownloadUrl`을 추가해 목록 행과 미리보기 헤더가 같은 `/files/download` URL 생성 규칙을 공유하도록 중복을 제거했다. 미리보기 헤더에는 파일 경로 기반 접근성 이름을 가진 다운로드 아이콘 링크를 편집·닫기 앞에 배치했고, 현재 채팅 ID를 함께 보내 전용 worktree의 원본 파일을 내려받는다. 미리보기 실패·압축파일 안내·편집 모드에서도 선택 파일 다운로드는 계속 노출된다.
- 반응형 확인: 기존 헤더 버튼 CSS를 링크에도 적용해 데스크톱 분할 미리보기와 390×844 모바일 전체화면 sticky 헤더에서 다운로드·편집·닫기 버튼의 크기와 간격이 일치함을 갱신된 두 검증 스크린샷으로 확인했다.
- 문서 업데이트: README의 파일 미리보기 보안·동작 설명에 헤더 다운로드를 추가하고, CODETREE의 `FilesView`·파일 URL 유틸·회귀 테스트 역할을 최신화했다.
- 검증 완료: 파일 URL 단위 테스트 5개와 14003 개발 서버를 사용한 집중 Playwright 미리보기 시나리오 1개를 통과했다. `npm run verify`의 타입 검사, 58개 파일 402개 테스트, 프로덕션 클라이언트·서버 빌드와 공개 경로 검사도 모두 통과했다.

## 2026-08-10 Codex - 14003 서버 장애 진단 착수

- 작업 시작: 사용자가 14003 포트의 웹 서버가 다시 종료됐다고 알려와 Codex가 현재 프로세스·포트 리스너·최근 서버 로그와 실행 구성을 읽기 전용으로 확인한다.
- 진단 원칙: 민감 설정값이나 `.env` 내용은 열지 않고, 프로세스 명령·서비스 상태·애플리케이션 오류만 확인한다. 원인을 확인한 뒤 안전한 기존 실행 방식이 명확한 경우에만 서버를 복구하고 `/health`로 검증한다.
- 추가 요청과 범위 결정: 사용자가 현재 세션을 제외한 프로세스 전체 정리를 요청했다. 시스템 프로세스 전체 종료로 확대하지 않고 web-agent-manager가 생성한 서버·채팅 tmux·MCP/에이전트 프로세스만 대상으로 하며, 현재 대화의 업로드 경로로 확인되는 채팅 `#205`는 보존한다.
- 정리 계획: tmux pane PID와 관련 프로세스 트리를 먼저 매핑해 정확한 종료 대상을 확정하고, 다른 채팅 tmux에는 정상 종료 신호를 보낸 뒤 남은 고아 MCP/에이전트 프로세스를 정리한다. 현재 `#205`와 이 진단 명령의 실행 기반은 건드리지 않는다.
- 장애 확인 완료: 14003 리스너와 서버 프로세스가 없고 로그는 11:33:28에 정상 HTTP 요청을 처리한 직후 끊겼다. 종료 직전 120줄은 모두 DEBUG였으며 예외·미처리 Promise·OOM 흔적은 없었다. 이 환경에는 systemd가 없고 서버 전용 tmux도 없었으므로 애플리케이션 크래시보다 이전 임시 실행 세션의 외부 종료 가능성이 높다고 판단했다.
- 재기동 시도 결과: 14003 전용 tmux 세션으로 개발 서버를 시작했으나 애플리케이션 로거가 시작되기 전에 세션이 종료돼 헬스체크가 열리지 않았다. 사용자의 후속 전체 정리 요청을 우선해 추가 재기동은 중단했다.
- 프로세스 정리 완료: `web_agent_manager_7fbbadaede8f8b4c`, 채팅 `#202`, `#203`, `#206` tmux를 정상 종료했고 그 자식 Claude·Codex·MCP·code-mode-host 및 `#202`의 14008 개발 서버가 함께 종료됐다. 고아 자식은 남지 않았다.
- 최종 상태: tmux에는 현재 채팅 `#205` 하나만 남겼고, 이를 유지하는 tmux 서버 데몬과 현재 응답을 수행하는 Codex 실행 계층만 보존했다. 14003은 사용자의 정리 요청에 맞춰 중지 상태로 유지한다.

## 2026-08-10 Codex - 14003 서버 재실행 착수

- 작업 시작: 사용자가 프로세스 정리 후 웹 서버를 `0.0.0.0:14003`으로 다시 실행해 달라고 요청했다.
- 실행 결정: 현재 채팅 `#205`와 분리된 `web_agent_manager_server_14003` tmux 세션을 만들고, tmux의 작업 디렉터리·환경 지정 옵션으로 호스트와 포트를 직접 전달한다. `/health` 응답과 tmux·Node 프로세스 존속을 함께 확인한다.
- 첫 실행 원인 확인: 오래전에 생성된 tmux 서버 환경에서 Volta 기본 Node 선택 정보가 없어 `npm` shim이 `Node is not available`로 즉시 종료됐다. 애플리케이션 코드나 14003 포트 충돌은 아니었다.
- 재실행 완료: 유지되는 bash pane에서 설치된 Node 22.23.1과 npm CLI의 실제 경로를 지정하고 `WEB_AGENT_MANAGER_HOST=0.0.0.0`, `WEB_AGENT_MANAGER_PORT=14003`으로 개발 watch 서버를 시작했다.
- 검증 완료: `/proc/net/tcp`에서 `00000000:36B3 LISTEN`을 확인해 모든 IPv4 인터페이스 바인딩을 검증했고, `http://127.0.0.1:14003/health`가 반복해서 `{"ok":true}`를 반환했다. tmux pane과 tsx watch Node 프로세스도 계속 실행 중이다.
- 네트워크 검증 한계: 실행 컨테이너에서 호스트 LAN 주소 `192.168.0.31`로 되돌아가는 요청은 타임아웃됐지만, 컨테이너에는 인터페이스 조회 도구가 없어 hairpin/호스트 라우팅을 더 판별하지 못했다. 서버 자체의 `0.0.0.0:14003` 리스너와 로컬 응답은 정상이다.

## 2026-08-10 Claude - 릴리즈 상태와 서버 자동 재시작 가능성 점검

- 작업 시작: 사용자가 현재 릴리즈 상태 확인과 "서버가 죽었을 때 자동 재시작"을 설정할 수 있는지/없는지 점검을 요청했다. Claude가 읽기 전용으로 조사만 수행하고, 실제 재시작 수단 설치·설정 변경은 사용자 결정 전까지 하지 않기로 했다.
- 조사 원칙: 이전 사고 기록에 따라 이름 패턴으로 프로세스를 찾거나 종료하는 방식(`pkill`·`pgrep`·`ps | grep`)은 쓰지 않고, `/health` 응답과 tmux 세션 목록으로만 서버 생존을 확인했다. `.env` 등 민감 설정값은 열지 않았다.
- 릴리즈 확인 결과: 공개 저장소 `ljy1348/web-agent-manager`의 최신 릴리즈는 `v0.3.0`(Latest, 태그 peeled commit `d562619b`)로 정상 게시 상태다. 반면 로컬 `feat/file-preview-download`는 공개 `main` 대비 앞서 있고, private `origin/feat/web-agent-manager` 기준으로도 87개 커밋이 푸시되지 않았다. 안드로이드 WebView 앱·기기 인증·다운로드 개선·파일 미리보기 다운로드 등 v0.3.0 이후 작업 전부가 로컬에만 존재한다.
- 미게시 산출물 확인: `release/android`의 APK 0.1.0~0.2.2와 `release/archives`의 v0.3.0 ZIP 4종·SBOM은 로컬 파일로만 있고, 안드로이드 APK는 어떤 원격 릴리즈에도 자산으로 올라가 있지 않다.
- 자동 재시작 환경 판정: 이 실행 환경은 PID 1이 `docker-init`인 컨테이너이고 `systemctl is-system-running`이 `offline`이라 준비된 `deploy/web-agent-manager.service`(`Restart=on-failure`)를 이 안에서는 쓸 수 없다. `crontab` 미설치로 `@reboot`도 불가하고, `/var/run/docker.sock`이 없어 컨테이너 안에서 `restart: unless-stopped` 같은 도커 재시작 정책을 다룰 수도 없다. pm2·forever·supervisor는 설치돼 있지 않다.
- 현재 구동 방식 확인: 14003 서버는 `web_agent_manager_server_14003` tmux 세션의 bash pane에서 `tsx watch` 개발 서버로 떠 있고, 점검 시점 `/health`는 `{"ok":true}`를 반환했다. pane이 종료되면 아무도 되살리지 않는 구조라 자동 복구 수단이 전혀 없다.
- 판단: 컨테이너 안에서 가능한 선택지는 (1) tmux pane의 감시 루프(의존성 없음), (2) pm2 설치 두 가지이며, 둘 다 프로세스 크래시·외부 종료는 복구하지만 컨테이너 자체 재시작은 복구하지 못한다. 컨테이너 재기동까지 포함한 자동 복구는 호스트 쪽 docker 재시작 정책이나 호스트 systemd로만 가능하다. 어떤 수단을 적용할지는 사용자 결정을 기다린다.

## 2026-08-10 Claude - 14003 운영 서버 프로덕션 전환과 감시 루프 적용 착수

- 작업 시작: 위 점검 결과를 받은 사용자가 (1) 개발 `tsx watch` 대신 `npm start` 기준의 프로덕션 빌드로 전환하고 (2) 선택지 1번인 tmux 감시 루프로 자동 재시작을 적용하도록 결정했다. Claude가 수행한다.
- 설계 결정 - 실행 명령: 감시 루프 안에서는 `npm start`를 그대로 부르지 않고 그 실체인 `NODE_ENV=production node dist/server/src/server/index.js`를 node 절대경로로 직접 실행한다. 이전 재실행 기록에서 tmux 서버 환경에 Volta 기본 Node 선택 정보가 없어 `npm` shim이 `Node is not available`로 즉시 죽은 사고가 있었고, npm을 한 단계 끼우면 자식 프로세스 PID 추적과 종료 신호 전달도 불투명해지기 때문이다.
- 설계 결정 - 중지 플래그: 감시 루프가 의도적 중지까지 되살리면 서버를 내릴 방법이 없어지므로, 중지 플래그 파일이 있을 때만 루프를 끝내도록 한다. "닫기처럼 보이는 동작이 실제 살아있는 작업을 조용히 죽이면 안 된다"는 기존 원칙의 반대 방향으로, 되살리기도 명시적 신호로만 멈추게 하려는 의도다.
- 설계 결정 - 종료 대상 한정: 기존 개발 서버는 이름 패턴 프로세스 검색 없이 Codex가 만든 정확한 tmux 세션 `web_agent_manager_server_14003`만 종료해 교체한다. 감시 루프는 자신이 띄운 자식 PID를 pidfile에 기록해, 이후 검증에서도 그 PID만 대상으로 삼는다.
- 검증 계획: 프로덕션 빌드 후 `/health` 200을 확인하고, pidfile의 PID만 종료해 감시 루프가 자동으로 새 프로세스를 띄우는지, 중지 플래그를 세우면 루프가 실제로 멈추는지까지 확인한다.
- 구현 완료: `scripts/run-server-supervised.sh`를 추가했다. 앱 폴더에서 `NODE_ENV=production`으로 `dist/server/src/server/index.js`를 실행하고(`npm start`와 같은 명령), 자식 PID를 `data/supervisor/server.pid`에 기록하며, 로그는 프로세스 치환으로 pane과 `data/supervisor/server.log`에 동시에 남긴다. 서버 설정이 `process.cwd()`를 rootDir로 삼아 데이터·프로젝트 경로를 계산하므로 실행 전 앱 폴더로 이동하게 했다.
- 전환 완료: 기존 개발 watch 서버는 이름 패턴 프로세스 검색 없이 tmux 세션 `web_agent_manager_server_14003`만 종료해 교체했고, 같은 이름의 새 세션에서 유지되는 bash pane을 만든 뒤 supervisor를 실행했다. 프로덕션 빌드는 사전에 다시 수행해 최신 커밋 기준 산출물로 갱신했다.
- 검증 중 발견한 결함: 첫 재시작 시험에서 SIGTERM을 보냈더니 서버가 죽지 않고 리스너만 닫힌 채 프로세스가 매달렸다. `src/server/index.ts`의 `shutdown`이 `server.close(cb)` 콜백에서 정리를 끝내는데, 열린 WebSocket 때문에 콜백이 호출되지 않는 것이 원인이다. 이 상태에서는 서비스가 죽었는데도 PID가 살아 있어, 프로세스 생존만 보는 감시 루프는 복구하지 못한다. 이전 장애 기록의 원인이 "외부 종료 추정"이었던 만큼 실제로 마주칠 수 있는 경로라 판단했다.
- 보완 결정: 살아있음의 기준을 프로세스 생존이 아니라 `/health` 응답으로 바꿨다. 5초 간격으로 헬스를 확인해 한 번 정상 응답한 뒤 3회 연속 실패하면 장애로 보고, 정상 종료를 시도한 다음 10초 안에 끝나지 않으면 강제 종료해 재시작한다. 기동 중에는 60초까지 기다린다. 감시 루프 자신이 종료 신호를 받을 때도 같은 강제 종료 폴백을 쓴다.
- 검증 완료: (1) 강제 종료 시 `exit=137` 기록 후 3초 뒤 새 PID로 재기동하고 `/health`가 `{"ok":true}`를 반환했다. (2) SIGTERM으로 hang을 유발한 시험에서도 헬스 3회 실패를 감지해 강제 정리 후 재시작했고, 감지부터 복구까지 약 30초가 걸렸다. (3) 중지 플래그를 세우고 서버를 종료하니 "재시작하지 않고 감시를 끝낸다"를 남기고 루프가 실제로 멈췄다.
- 프로덕션 회귀 확인: `NODE_ENV=production`에서 CSP `script-src`가 `'self'`로 좁아지지만 빌드 산출물에 인라인 스크립트가 없어 문제가 없었다. 메인 페이지와 `/assets` 번들이 200이고, Chromium으로 실제 로그인 화면이 정상 렌더되는 것을 스크린샷으로 확인했다. 남은 콘솔 오류 2건은 이번 전환과 무관하다 - 비로그인 상태의 `/api/auth/me` 401과, `style-src`가 개발·프로덕션 동일하게 외부 Google Fonts 스타일시트를 차단하는 기존 동작이다.
- 문서 업데이트: README에 "systemd를 쓸 수 없는 환경의 자동 재시작" 절을 추가해 실행 방법·조정 환경변수·중지 절차와 컨테이너 재시작은 호스트에서만 복구 가능하다는 한계를 명시했고, CODETREE에 스크립트 항목을 추가했다.
- 남은 한계: 이 감시 루프는 컨테이너 자체가 재시작되면 함께 사라져 자동 부팅되지 않는다. 거기까지 필요하면 호스트의 도커 재시작 정책이나 호스트 systemd가 필요하며 이는 컨테이너 안에서 설정할 수 없다. 또한 `shutdown`이 WebSocket 때문에 완료되지 않는 문제 자체는 애플리케이션 결함으로 남아 있어, 별도 수정 대상이다(현재는 supervisor의 강제 종료로 우회).

## 2026-08-10 Claude - 종료 신호에서 서버가 매달리는 결함 수정 착수

- 작업 시작: 위 supervisor 작업에서 발견한 "SIGTERM을 받으면 리스너만 닫히고 프로세스가 남는" 결함의 원인 파악과 수정을 사용자가 요청했다. Claude가 수행한다.
- 원인 확정: `src/server/index.ts`의 `shutdown`은 `server.close(cb)` 콜백 안에서 `agentBridge.close()`·`database.close()`·`process.exit(0)`을 수행하는데, `server.close`는 새 연결만 막고 이미 열린 연결이 전부 끊겨야 콜백을 부른다. `RealtimeHub`는 `httpServer.on("upgrade")`로 승격한 WebSocket을 `clients` Set에 들고 있을 뿐 종료용 정리 메서드가 없어서, 웹 화면이 하나라도 붙어 있으면 그 연결이 영원히 남아 콜백이 호출되지 않는다. HTTP keep-alive 연결도 같은 방식으로 종료를 붙잡는다.
- 수정 방침: (1) `RealtimeHub`에 종료용 정리 메서드를 추가해 열린 실시간 연결과 업그레이드 서버를 먼저 닫고, (2) `shutdown`에서 그 정리를 호출한 뒤 유휴 keep-alive 연결도 함께 닫으며, (3) 그래도 끝나지 않을 때를 대비해 시간 제한을 두고 남은 연결을 정리한 뒤 종료하도록 한다. 종료 경로에 단일 실행 가드를 둬 정상 경로와 시간 초과 경로가 겹쳐도 자원 정리가 중복 실행되지 않게 한다.
- 검증 계획: 실제 WebSocket과 keep-alive 연결을 붙인 상태에서 SIGTERM을 보내 프로세스가 매달리지 않고 종료되는지 확인하고, 회귀 방지를 위한 단위 테스트를 추가한다.
- 구현 완료 - RealtimeHub: `close()`를 추가해 열린 클라이언트 소켓을 모두 끊고 업그레이드 서버를 닫는다. 종료 경로에서는 상대의 close 프레임 응답을 기다리는 정상 핸드셰이크가 끝나지 않을 수 있어 `terminate()`로 즉시 끊는 쪽을 선택했다.
- 구현 완료 - shutdown: 기존 자원 정리 뒤 `realtime.close()`와 `server.closeIdleConnections()`를 호출해 실시간 연결과 유휴 keep-alive 연결을 먼저 정리하고, `server.close(finalize)`로 정상 종료를 기다린다. 처리 중이던 요청이 끝나지 않는 경우를 대비해 `SHUTDOWN_GRACE_MS`(5초) 뒤 `closeAllConnections()`로 남은 연결까지 끊고 마무리한다. 정상 경로와 시간 초과 경로가 겹쳐도 `agentBridge.close()`·`database.close()`가 두 번 실행되지 않도록 `finalized` 가드를 뒀다.
- 유예 시간 근거: systemd 유닛의 `TimeoutStopSec=20`, 감시 스크립트의 강제 종료 유예 10초보다 짧은 5초로 잡아, 외부 감시자가 강제로 죽이기 전에 애플리케이션이 항상 먼저 스스로 정리를 끝내도록 했다.
- 회귀 테스트 추가: `tests/realtime-shutdown.test.ts`에 실제 SQLite 세션으로 인증한 WebSocket을 붙인 HTTP 서버를 만들고, (1) 허브 정리 없이는 `server.close` 콜백이 불리지 않는다는 결함 자체, (2) `RealtimeHub.close()` 뒤에는 정상적으로 닫힌다는 점, (3) 허브를 닫으면 클라이언트 연결도 끊긴다는 점을 검증한다.
- 검증 완료: 타입 검사, 59개 파일 405개 단위 테스트(기존 402개 + 신규 3개), 프로덕션 빌드와 공개 경로 검사를 모두 통과했다. 실제 14003 운영 서버를 새 빌드로 교체한 뒤 SIGTERM을 보내니 **1초 만에 정상 종료**됐고 supervisor 로그에도 강제 종료 없이 `서버 종료 exit=0`만 남았다. 수정 전 같은 시험에서는 프로세스가 매달려 헬스 3회 실패 감지와 강제 종료까지 약 30초가 걸렸다.
- 문서 업데이트: CODETREE에 `RealtimeHub.close`의 역할과 새 회귀 테스트 항목을 추가했고, README의 자동 재시작 설명에서 이번 수정으로 사라진 특정 증상 대신 "프로세스가 남아도 응답하지 못하는 상태"라는 일반적인 근거로 문구를 다듬었다. 헬스 기반 감시는 교착 등 다른 원인에도 유효하므로 그대로 유지한다.

## 2026-08-10 Claude - Git 화면 커밋·PR 목록이 앞부분만 나오는 문제 수정 착수 (이슈 #14)

- 작업 시작: 사용자가 "커밋이랑 PR이 일부만 나온다"고 보고했다. Claude가 원인 조사 후 수정을 수행한다.
- 원인 확정: 화면 렌더링이 자르는 것이 아니라 서버가 애초에 앞부분만 조회하고 있었다. `src/server/routes/git-routes.ts`에서 `git log -10`(커밋 10개), `gh issue list --limit 50`, `gh pr list --limit 50`, `gh run list --limit 20`이 모두 상수로 고정돼 있고, 클라이언트에는 그 뒤를 이어 불러올 수단이 전혀 없다. 실제 데이터와 비교하면 WSS-Server는 커밋 2505개 중 10개, PR 317개 중 50개, 이슈 277개 중 50개만 보이는 상태다. myagent 저장소는 PR 7개·이슈 5개라 전부 보이지만 커밋은 238개 중 10개만 보인다.
- 설계 결정 - 방식 선택: 사용자에게 (1) "더 보기" 버튼, (2) 상수만 크게 상향, (3) 커밋만 먼저 수정 세 가지를 제시했고 사용자가 (1)을 선택했다. 상수 상향은 WSS-Server처럼 큰 저장소에서 첫 화면 로딩이 그만큼 느려지는데도 결국 그 이상은 못 보는 한계가 남아 채택하지 않았다.
- 설계 결정 - 초기 묶음 크기: 사용자 선택에 따라 커밋 30 / PR·이슈 50 / 워크플로 20으로 한다. 커밋만 10에서 30으로 올려 첫 화면을 채우고, 나머지는 기존 값을 유지한 채 이어 불러오기로 확장한다. 모바일 스크롤 부담을 고려한 값이다.
- 설계 결정 - 범위: 사용자가 지목한 것은 커밋과 PR이지만 이슈·워크플로도 같은 엔드포인트의 동일한 결함이고, PR 탭에만 더 보기 버튼이 생기면 같은 화면의 다른 탭과 동작이 어긋난다. 네 목록 모두 같은 방식으로 처리한다.
- 설계 결정 - "더 있는지" 판단: `git log`도 `gh list`도 전체 개수를 주지 않아, 요청 개수보다 하나 더 읽어 초과분이 있으면 더 있다고 보고 응답에서는 잘라서 돌려준다. 총 개수를 따로 세는 추가 CLI 호출을 넣지 않으려는 선택이다.
- 검증 계획: 타입 검사와 단위 테스트를 돌리고, 실제 14003 서버에서 커밋이 많은 프로젝트(WSS-Server)를 열어 더 보기 동작을 Playwright로 확인하고 스크린샷을 남긴다.
- 구현 완료 - 서버: `git-routes.ts`에 `listLimit`(쿼리 값 정규화, 기본값 복구와 상한 500)과 `withHasMore`(하나 더 읽은 목록을 잘라 내며 다음 페이지 유무 판단)를 추가하고, `/projects/:id/git`은 `commits`, `/projects/:id/github`은 `issues`·`pulls`·`runs` 쿼리를 받도록 바꿨다. 늘어난 개수는 `GitDataCache` 키에도 넣어, 앞 페이지만 담긴 옛 캐시가 그대로 돌아오지 않게 했다.
- 구현 완료 - 클라이언트: `GitView.tsx`에 `commitLimit`·`githubLimits` 상태와 각 목록 끝의 "더 보기" 버튼을 추가했다. 조회는 `loadGit`(로컬)과 `loadGithub`(GitHub)으로 나눠, 커밋 더 보기가 GitHub 목록까지 다시 읽지 않게 했다. GitHub 결과를 비우는 처리는 조회 대상이 바뀔 때만 하고 개수만 늘어난 경우에는 하지 않아, 더 보기를 눌러도 보고 있던 목록이 사라졌다 다시 나타나지 않는다.
- 구현 결정 - 개수 되돌리기 위치: 프로젝트·채팅·작업공간·조회 브랜치가 바뀔 때 늘려 둔 개수를 기본값으로 되돌리는 처리를 effect가 아니라 렌더 단계에서 한다. effect에서 하면 새 대상을 옛 개수로 한 번 조회한 뒤 기본 개수로 또 조회하는 낭비가 생기기 때문이다.
- 구현 결정 - 쿼리 조립: 파라미터가 늘면서 기존의 `?`/`&` 문자열 이어붙이기가 읽기 어려워져 `withQuery` 하나로 모으고, 같은 일을 하던 `chatPath` 내부도 그걸 쓰도록 정리했다(중복 구현 방지).
- 검증 완료: 타입 검사, 단위 테스트 411개(기존 405개 + 신규 6개), 프로덕션 빌드와 공개 경로 검사를 통과했다. 회귀 테스트로 `tests/git-commit-paging.test.ts`(실제 임시 저장소로 라우터를 띄워 기본 30개·경계값·상한·잘못된 값 처리 검증)와 `tests/e2e/git-list-paging.spec.ts`(커밋 30→45, PR 50→70으로 이어 불러오고 남은 항목이 없으면 버튼이 사라지며, 커밋 더 보기가 GitHub을 다시 읽지 않는지 확인)를 추가했다. Playwright 전체 19개 중 18개 통과·1개는 테스트 계정 환경변수가 없어 기존과 동일하게 skip이다.
- 검증 환경 정리: UI 확인용 vite 서버는 4399에 직접 띄우고, 이름 패턴 프로세스 검색 없이 `/proc`에서 그 포트의 리스닝 소켓 inode로 PID를 역추적해 내가 띄운 두 프로세스만 종료했다(`npx`가 만든 자식 프로세스가 남아 있었다). 운영 14003 서버는 작업 내내 건드리지 않았고 `/health` 200을 유지했다.
- 문서 업데이트: README의 GitHub 화면 설명에 초기 묶음 개수와 더 보기 동작을, CODETREE에 `listLimit`·`withHasMore`·`withQuery`·`loadGit`·`loadMoreGithub`와 새 테스트 2건을 추가했다.

## 2026-08-11 Claude - 모바일에서 커밋 상세 접근성과 설명 본문 스크롤 개선 착수 (이슈 #15, #16)

- 작업 시작: 이슈 #14의 "더 보기"를 적용한 뒤 사용자가 앱(모바일)에서 두 가지를 지적했다. (1) 커밋 목록이 길어지니 실제 내역(커밋 상세)이 한참 밑에 있어 보기 힘들다, (2) 설명문이 별도 스크롤이라 보기 싫다. Claude가 수행한다.
- 원인 확정 (1): 좁은 화면(<=700px)에서는 `.git-layout`이 1열이 되어 사이드바(변경 파일 + 최근 커밋) 전체가 커밋 상세 위에 쌓인다. 목록을 30개, 60개로 늘릴수록 상세가 그만큼 아래로 밀린다. 접이식 토글은 이미 있지만 커밋을 골라도 접히지 않아 사용자가 직접 접거나 스크롤해야 했다.
- 원인 확정 (2): 커밋 설명은 `pre.commit-body`인데 `.commit-body` 자체에는 높이 제한이 없다. 이 요소가 `.git-box` 안의 `pre`라서 `.git-box pre { max-height: 64vh; overflow: auto; }`에 걸리고, 명시도가 `.git-box pre`(0,1,1) > `.commit-body`(0,1,0)라 제한과 자체 스크롤이 그대로 적용됐다. 의도해서 준 제한이 아니라 공용 규칙에 딸려 들어간 것이다.
- 설계 결정 (1): 좁은 화면에서 커밋을 고르면 사이드바를 접고 커밋 상세 위치로 이동한다. 판단은 CSS 브레이크포인트와 같은 `matchMedia("(max-width: 700px)")`로 하고, 넓은 화면은 2열이라 상세가 이미 옆에 보이므로 동작을 바꾸지 않는다. 커스텀 오버레이나 모달을 새로 만들지 않고 이미 있는 접이식 사이드바와 브라우저 기본 `scrollIntoView`만 쓰는 쪽을 택했다 - 모바일에서는 OS·브라우저가 제공하는 동작을 우선한다는 기존 방침에 맞춘다.
- 설계 결정 (2): 커밋 설명의 높이 제한과 자체 스크롤을 없애 페이지 흐름을 따라 이어지게 한다. 상태 출력(`.git-status pre`, 150px)과 전체 diff(64vh)의 제한은 그대로 둔다 - 사용자가 지적한 것은 설명 본문이고, 그 둘은 길이가 예측되지 않아 제한이 실제로 필요하다.
- 검증 계획: Playwright로 390px 모바일 뷰포트에서 커밋 선택 시 사이드바가 접히고 상세가 화면에 들어오는지, 긴 커밋 설명이 잘리거나 안쪽에서 스크롤되지 않는지 확인하고 스크린샷을 남긴다.
- 구현 완료 (1): `GitView.tsx`에 `revealCommitDetail`을 추가해 커밋 선택 시 좁은 화면에서만 사이드바를 접고 `commitDetailRef`가 가리키는 커밋 상세로 `scrollIntoView`한다. 접기와 이동을 같은 프레임에 하면 목록이 아직 펼쳐진 상태의 위치로 스크롤돼 어긋나므로, `requestAnimationFrame`으로 레이아웃이 확정된 다음 프레임에 이동한다.
- 구현 완료 (2): `styles.css`에 `.git-box pre.commit-body { max-height: none; overflow: visible; }`을 추가해 공용 규칙에서 딸려 온 높이 제한과 자체 스크롤을 되돌렸다. 선택자를 `.git-box pre.commit-body`로 잡은 건 `.commit-body`만으로는 명시도가 낮아 공용 규칙을 이기지 못하기 때문이다.
- 검증 완료: 타입 검사, 단위 테스트 411개, Playwright 20개 중 19개 통과(1개는 테스트 계정 환경변수가 없어 기존과 동일하게 skip). `tests/e2e/git-list-paging.spec.ts`에 390px 모바일 테스트를 추가해 (1) 커밋 선택 후 목록이 접히고 상세가 뷰포트 안에 들어오는 것, (2) 60줄짜리 설명이 `scrollHeight <= clientHeight`이고 `overflow-y: visible`이라 잘리거나 따로 스크롤되지 않는 것을 확인했다. 스크린샷은 `artifacts/ui-git-commit-detail-mobile.png`.
- 검증 환경 정리: vite 검증 서버는 포트로 PID를 역추적해 내가 띄운 프로세스만 종료했고, 운영 14003은 `/health` 200을 유지했다.
- 문서 업데이트: README의 GitHub 화면 설명에 좁은 화면 동작을, CODETREE에 `revealCommitDetail`과 모바일 테스트 항목을 추가했다.

## 2026-08-11 Claude - GitHub 탭에서 프로젝트 변경 시 흰 화면 되는 버그 수정 착수 (이슈 #17)

- 작업 시작: 사용자가 앱에서 "GitHub 탭의 깃허브 화면에서 프로젝트를 바꾸면 흰 화면이 뜬다"고 보고했다. Claude가 원인 조사 후 수정한다.
- 재현 경위: 개발 서버에서 프로젝트 전환을 목킹해 돌렸을 때는 재현되지 않았고, 프로덕션 번들에서도 렌더 예외가 없었다. "프로젝트를 바꾸는 순간", 즉 그 프로젝트의 캐시가 비어 있을 때만 나는 증상이라는 점에 착안해 `GitDataCache`를 직접 두드려 재현했다.
- 원인 확정: `GitDataCache.refresh`는 조회를 시작하면서 `{ value: undefined, updatedAt: 0, pending }` 자리표시 엔트리를 먼저 저장한다. 이 상태에서 같은 키로 두 번째 요청이 오면 `read`는 `!entry`가 거짓이라 대기 경로로 가지 않고, `stale`이지만 `entry.pending`이 있어 갱신도 걸지 않은 채 `entry.value`(= `undefined`)를 그대로 반환한다. 라우터가 `response.json({ ...cached.value, cachedAt })`로 내보내므로 실제 응답이 `{"cachedAt":"1970-01-01T00:00:00.000Z"}`만 남고, 클라이언트 `GitHubTab`이 `github.repository.url`을 검사 없이 접근하다 TypeError로 죽어 화면 전체가 사라진다. 300ms loader에 50ms 뒤 두 번째 `read`를 넣어 실제로 `value === undefined`임을 확인했다.
- 내 이전 작업과의 관계: 캐시 자체의 결함은 원래 있었지만, 이슈 #14에서 `loadGithub` 호출 경로를 늘리고(대상 변경 effect와 표시 개수 effect) 캐시 키에 조회 개수를 넣어 키가 세분화되면서, 프로젝트 전환 직후 빈 캐시에 동시 요청이 겹칠 확률이 올라갔다. 사용자가 "갑자기" 겪게 된 이유로 판단한다.
- 수정 방침: (1) 근본 원인은 캐시다 - `read`에서 아직 성공한 값이 없는 진행 중 엔트리는 빈 값을 돌려주지 말고 진행 중인 조회에 합류해 결과를 기다린다. (2) 클라이언트도 방어한다 - 저장소 정보가 없는 응답이면 흰 화면 대신 안내를 표시한다. 서버가 어떤 이유로든 불완전한 응답을 주더라도 화면 전체가 사라지지는 않아야 한다.
- 검증 계획: 캐시 동시 요청에 대한 단위 테스트와, 프로젝트 전환 중 응답이 지연될 때 화면이 살아 있는지 확인하는 E2E를 추가한다.
- 구현 완료 (근본): `GitDataCache.read`에 "아직 성공한 값이 없는(`updatedAt === 0`) 진행 중 엔트리"를 처리하는 분기를 넣어, 저장된 빈 값 대신 `entry.pending`에 합류해 결과를 기다리도록 했다. pending이 없는 예외적 상태면 새로 조회한다. 두 요청이 같은 조회를 공유하므로 CLI 실행 횟수는 늘지 않는다.
- 구현 완료 (방어): `GitHubTab`의 가드를 `!github`에서 `!github?.repository`로 넓히고 `issues`·`pullRequests`·`runs`를 기본값 있는 지역 변수로 받아 렌더에 쓰도록 했다. 서버가 어떤 이유로든 불완전한 응답을 주더라도 에러 경계가 없는 이 화면이 통째로 사라지지 않고 안내를 보여준다.
- 검증 완료: 타입 검사, 단위 테스트 415개(기존 411개 + 신규 4개), Playwright 21개 중 20개 통과(1개는 테스트 계정 환경변수가 없어 기존과 동일하게 skip). 회귀 테스트로 `tests/git-cache-pending.test.ts`(진행 중 조회 합류·실제 저장 시각·실패 전파·완료 후 즉시 반환, CLI 1회 실행)와 `tests/e2e/git-list-paging.spec.ts`의 "저장소 정보 없는 응답" 시나리오(프로젝트를 바꿔가며 `pageerror` 없이 안내 표시)를 추가했다.
- 수정 전후 대조: 같은 재현 스크립트에서 수정 전에는 두 번째 요청이 `value = undefined`(응답 `{"cachedAt":"1970-01-01T00:00:00.000Z"}`)를 받았고, 수정 후에는 첫 요청과 동일한 정상 값을 받는다.
- 문서 업데이트: CODETREE의 `GitDataCache` 설명에 진행 중 조회 합류 동작과 이 버그의 증상을 남기고, 새 테스트 2건을 표에 추가했다.

## 2026-08-11 Claude - 좁은 화면에서 이슈·PR 상세로 이동 (이슈 #18)

- 작업 시작: 사용자가 "이슈를 클릭했는데 상세를 보려면 여전히 스크롤을 쭉 내려야 한다"고 지적했다. 이슈 #15에서 커밋 목록만 처리하고 GitHub 탭의 이슈·PR 목록은 같이 보지 않은 누락이다. Claude가 수행한다.
- 원인: `.github-split`은 `@media (max-width: 1100px)`에서 1열이 되어 목록(기본 50개, 더 보기로 그 이상) 전체가 상세 위에 쌓인다. 커밋 쪽과 같은 구조의 문제인데 브레이크포인트만 다르다(커밋 사이드바는 700px, GitHub 목록/상세는 1100px).
- 설계 결정: 커밋에 넣었던 이동 로직을 `scrollToDetail(element, maxWidth)` 공용 헬퍼로 빼고 커밋·이슈·PR이 함께 쓴다. 브레이크포인트를 인자로 받는 이유는 두 레이아웃의 CSS 기준이 실제로 다르기 때문이다. 모바일 전용 모달이나 목록/상세 전환 UI를 새로 만들지 않고 브라우저 기본 스크롤만 쓰는 방침은 그대로 유지한다.
- 구현 방법: React 19라 함수 컴포넌트에 `ref`를 일반 prop으로 넘길 수 있어 `IssueDetail`·`PullRequestDetail`에 `detailRef`를 전달하고, 선택이 바뀔 때 effect에서 이동한다(클릭 직후에는 상세가 아직 렌더되지 않아 ref가 비어 있다).
- 구현 완료: `scrollToDetail(element, maxWidth)` 공용 헬퍼를 만들어 커밋(700px)과 이슈·PR(1100px)이 함께 쓴다. `IssueDetail`·`PullRequestDetail`에 `detailRef`를 넘기고, 선택이 바뀔 때 effect에서 상세로 이동한다.
- 구현 보완 - 목록 접기: 처음에는 스크롤 이동만 넣었는데, 상세가 문서의 마지막 요소라 아무리 내려도 화면 하단에 걸쳐서 실제로는 여전히 읽기 불편했다(스크린샷으로 확인). 커밋 사이드바와 같은 접이식 토글을 GitHub 목록에도 넣어, 항목을 고르면 목록을 접고 상세가 화면 위로 올라오게 했다. 토글에는 선택 중인 번호나 항목 수를 함께 표시하고, 다시 눌러 목록으로 돌아갈 수 있다.
- 함께 고친 결함 (이슈 #19): 검증 스크린샷에서 이슈 상세의 "새 작업공간에서 시작" 버튼과 "GitHub에서 열기" 링크가 겹쳐 보이는 것을 발견했다. `.github-detail-actions`에 스타일이 아예 없어 정렬·간격이 없었던 것으로, flex와 줄바꿈·간격을 줘서 해결했다. 지금 손대는 화면에서 눈에 보이는 결함이라 함께 처리했다.
- 검증 완료: 타입 검사, 단위 테스트 415개, Playwright 22개 중 21개 통과(1개는 테스트 계정 환경변수가 없어 기존과 동일하게 skip). 모바일 이슈 테스트는 (1) 처음에는 상세가 화면 밖이고, (2) 항목을 고르면 목록이 접히며 상세가 화면 안으로 들어오고, (3) 토글로 목록을 다시 펼 수 있음을 확인한다. 스크린샷은 `artifacts/ui-git-issue-detail-mobile.png`.

## 2026-08-11 Claude - 목록 접힘과 스크롤이 겹쳐 상세 중간으로 가는 문제 수정 (이슈 #20)

- 작업 시작: 사용자가 "선택하니 목록이 접히면서 스크롤이 움직이는데 상세 위가 아닌 중간으로 가버린다"고 지적했다. 직전 이슈 #18 수정의 후속 결함이다. Claude가 수행한다.
- 원인: `revealDetail`이 `setListOpen(false)`로 접기를 예약한 뒤 `requestAnimationFrame`에서 부드러운 스크롤을 시작한다. 접힘으로 문서 높이가 줄어드는 것과 스크롤 애니메이션이 겹쳐, 목표 위치가 접히기 전 레이아웃 기준으로 계산된다. 스크롤이 끝날 즈음에는 위쪽 콘텐츠가 사라져 있어 상세 중간이 화면 상단에 온다. 커밋 쪽(`revealCommitDetail`)도 사이드바 접기와 같은 구조라 같은 문제를 갖고 있었다.
- 테스트가 놓친 이유: E2E가 `toBeInViewport`만 확인해 상세가 화면에 걸치기만 하면 통과했다. 상세 위쪽이 화면 상단 근처에 오는지는 보지 않았다. 단언이 느슨해 "동작은 하지만 쓸모없는" 상태를 잡지 못한 것이다.
- 설계 결정: 접기와 이동을 두 단계로 나눈다. 선택 시에는 접기만 하고 이동 대상을 예약해 두었다가, 접힘이 DOM에 반영된 뒤 `useLayoutEffect`에서 실제로 이동한다. `useLayoutEffect`는 DOM 변경 후 페인트 전에 실행되므로 접힌 레이아웃 기준으로 위치가 계산된다. 부드러운 스크롤은 유지한다 - 레이아웃이 이미 확정된 뒤에 시작하므로 더는 어긋나지 않는다.
- 검증 계획: 상세 상단이 화면 상단 근처에 오는지를 좌표로 확인하는 단언을 추가하고, 커밋·이슈 두 경로 모두 검증한다.
- 구현 완료: 선택 시에는 목록 접기와 "이동 대상" 예약만 하고, `useLayoutEffect`에서 접힘이 반영된 뒤 실제로 이동하도록 두 단계로 나눴다. 커밋·이슈·PR 세 경로 모두 같은 구조다. 폭 판정은 `isStackedLayout(maxWidth)`로 모으고, `scrollToDetail`은 이동만 담당하게 단순화했다.
- 검증 중 발견한 추가 조건: 강화한 단언으로 이슈는 통과했지만 커밋만 실패했다. 커밋 상세는 이슈·PR과 달리 본문·diff를 나중에 받아 높이가 크게 늘어나는데, 로딩 중에 이동하면 그 시점에는 문서가 짧아 상세가 화면 위까지 올라오지 못한다. 이동 조건에 `commitLoading`을 추가해 내용이 채워진 뒤 옮기도록 했다.
- 테스트 보강: `toBeInViewport`만으로는 "화면에 걸치기만 한" 상태를 걸러내지 못해, 상세 위쪽의 화면 좌표가 상단 200px 안에 들어오는지를 커밋·이슈 양쪽에 추가했다. 커밋 스크린샷도 `fullPage`를 빼서 실제 스크롤 위치가 보이도록 바꿨다.
- 검증 완료: 타입 검사, 단위 테스트 415개, Playwright 22개 중 21개 통과(1개는 테스트 계정 환경변수가 없어 기존과 동일하게 skip). 스크린샷으로 커밋·이슈 모두 상세가 화면 맨 위에서 시작하는 것을 확인했다.

## 2026-08-11 Claude - 이슈·PR 본문 자체 스크롤 제거 (이슈 #21)

- 작업 시작: 사용자가 "깃허브 이슈 상세는 왜 또 스크롤이 따로 노냐"고 지적했다. 이슈 #16에서 커밋 설명만 고치고 GitHub 이슈·PR 본문은 같이 보지 않은 누락이다. Claude가 수행한다.
- 원인: `styles.css`의 `.github-body { max-height: 220px; overflow: auto; ... }`. 커밋 설명은 공용 규칙(`.git-box pre`)에 딸려 들어간 것이었지만 이쪽은 명시적으로 준 제한이었다. 어느 쪽이든 페이지 스크롤과 분리돼 읽기 불편하다는 문제는 같다.
- 수정: 높이 제한과 `overflow: auto`를 없애 본문이 페이지 흐름을 따라 이어지게 했다. 테두리·배경·안쪽 여백은 본문 영역 구분을 위해 유지한다. 커밋 설명(`.commit-body`)과 같은 기준이다.
- 검증 완료: 이슈 본문을 40문단으로 늘린 E2E에서 `scrollHeight <= clientHeight`이고 `overflow-y: visible`임을 확인했다. 타입 검사, 단위 테스트 415개, Playwright 22개 중 21개 통과(1개는 테스트 계정 환경변수가 없어 skip).
- 테스트 단언 수정: 상세 위치 검사에서 `y >= 0`이 지나치게 엄격해 전체 병렬 실행에서 `-0.40625`로 실패했다. 상세가 화면 맨 위에 딱 붙을 때 생기는 서브픽셀 오차이고 동작은 정확하므로, "위로 크게 밀려나지 않았는지"(> -20)로 완화했다. 제품 결함이 아니라 단언 문제였다.

## 2026-08-11 Claude - GitHub 조회 속도 개선: 상세 캐시와 Actions 분리 (이슈 #22, #23)

- 작업 시작: 사용자가 "PR 조회가 왜 이렇게 오래 걸리는지 아느냐"고 물어 원인을 측정했고, 결과를 보고받은 사용자가 앱 코드로 해결되는 두 가지(상세 캐시, Actions 분리)를 진행하도록 결정했다. Claude가 수행한다.
- 측정 결과 (WSS-Server, PR 317개): 순수 HTTPS 왕복은 ~70ms로 네트워크는 병목이 아니다. `gh repo view` 489ms, `gh pr list --limit 51` 668ms, `gh issue list --limit 51` 830ms, `gh run list --limit 21` 1441ms, `gh pr view` ~600ms로, `gh`를 한 번 부르는 것 자체가 최소 0.5초다(프로세스 시작 + GraphQL 처리). 간헐적인 5~6초 스파이크는 DNS 질의 유실로, `dns.lookup` 120회 중 1회가 정확히 5013ms였다(`resolv.conf` 기본 타임아웃 5초). nameserver 두 개는 각각 9~11ms로 정상이라 서버 장애가 아니라 UDP 패킷 유실이다.
- 범위 결정: DNS 타임아웃은 `/etc/resolv.conf`의 `options timeout:1 attempts:2`로 완화할 수 있지만 앱이 아니라 컨테이너 시스템 설정이라 손대지 않고 원인만 보고했다. 사용자도 앱 코드 두 가지만 진행하도록 지시했다.
- 설계 결정 (#22): 상세 조회도 목록과 같은 `GitDataCache`를 태운다. 쓰기 작업 뒤에는 라우터 앞단 미들웨어가 이미 프로젝트 캐시를 통째로 버리므로 댓글·병합 후 오래된 상세가 남지 않는다. 별도 무효화 로직을 추가하지 않는 이유다.
- 설계 결정 (#23): 워크플로 조회를 `/projects/:id/github/runs`로 분리해 Actions 탭을 실제로 열 때만 부른다. `/github` 응답에서 `runs`를 빼므로 헤더의 "워크플로 N" 표시는 조회한 뒤에만 보여준다. 목록 전체를 다시 부르는 방식(파라미터로 0개 요청) 대신 엔드포인트를 나눈 건, 워크플로만 갱신할 때 이슈·PR까지 함께 딸려오지 않게 하려는 것이다.
- 검증 계획: 상세 캐시는 같은 항목을 두 번 조회할 때 CLI가 한 번만 실행되는지, Actions 분리는 GitHub 탭 첫 진입에서 워크플로를 부르지 않고 Actions 탭을 열 때만 부르는지 확인한다.
- 구현 완료 (#22): `/github/issue/:number`와 `/github/pr/:number`를 `GitDataCache`에 태웠다(키는 `github-issue:<번호>`·`github-pr:<번호>`). 응답에 `cachedAt`을 함께 실어 목록과 형식을 맞췄다.
- 구현 완료 (#23): `/projects/:id/github/runs`를 새로 만들고 `/github` 응답에서 `runs`와 `hasMore.runs`를 뺐다. 클라이언트는 `githubRuns` 상태를 따로 두고 Actions 탭을 열 때, 그리고 workflow 재실행 뒤 Actions를 보고 있을 때만 조회한다. 헤더의 "워크플로 N"은 조회한 뒤에만 표시한다.
- 측정 결과 (WSS-Server, 개선 후): GitHub 탭 첫 진입 969ms(이전에는 워크플로 1441ms에 맞춰졌음) → 재진입 4ms, PR 상세 첫 조회 573ms → 다시 열기 10ms, Actions는 열 때만 1233ms → 재조회 6ms.
- 회귀 테스트 추가: `tests/github-detail-cache.test.ts`. 가짜 `gh` 실행 파일을 PATH 앞에 두고 호출 횟수를 세는 방식으로, 같은 이슈·PR을 두 번 열어도 `gh`가 한 번만 실행되는지·다른 번호는 각각 조회하는지·목록 조회가 `run list`를 부르지 않는지·Actions 엔드포인트는 워크플로만 읽는지 검증한다.
- 검증 완료: 타입 검사, 단위 테스트 420개(기존 415개 + 신규 5개), Playwright 22개 중 21개 통과(1개는 테스트 계정 환경변수가 없어 기존과 동일하게 skip).
- 문서 업데이트: README에 워크플로 지연 조회와 상세 캐시를, CODETREE에 분리 근거와 새 테스트를 추가했다.

## 2026-08-11 Codex - 대시보드 채팅 종료 명칭 및 Codex 초기화권 표시 개선 착수

- 작업 시작: 사용자가 대시보드에서 채팅 항목의 `일괄종료` 표현을 `터미널 종료`로 바꾸고, Codex 사용량에 초기화권의 잔여량과 기한도 함께 표시해 달라고 요청했다. Codex가 기존 사용량 수집·표시 구조와 종료 동작의 실제 범위를 먼저 확인한 뒤 최소 수정한다.
- 구현 원칙: 기존 Codex CLI 사용량 화면에 이미 제공되는 초기화권 정보를 파싱·전달하는 경로가 있는지 우선 확인해 중복 구현을 피한다. 데이터가 없는 경우에는 임의 값을 만들지 않고 기존 사용량 상태의 선택 필드로 안전하게 확장한다.
- 검증 계획: 관련 파서·API 타입·대시보드 렌더링 테스트를 보강하고 실제 로컬 테스트 및 타입 검사를 수행한다. 커밋 전 README와 CODETREE도 현재 동작에 맞춰 갱신한다.
- 근거 확인: 로컬 Codex CLI 0.146.0의 `/usage` 첫 화면에서 초기화권 1개가 있음을 확인했지만 당시 `Redeem usage limit reset` 상세까지 들어가지 않아 TUI 기한 표시를 놓쳤다. 실제 app-server `account/rateLimits/read` 응답의 `rateLimitResetCredits.availableCount`와 사용 가능한 상세의 `expiresAt`을 구조화 소스로 선택했다. 인증 파일이나 토큰 값은 직접 읽지 않고 기존 CLI 인증을 통한 읽기 전용 메서드만 호출했다.
- 구현 완료 - 초기화권: `codex-rate-limits.ts`가 계정별 `CODEX_HOME` 환경을 유지한 단기 app-server에서 잔여 개수와 가장 이른 기한을 읽고 즉시 정리한다. `UsageMonitor`는 이 값을 기존 `details_json`에 합치며, app-server만 일시 실패하면 사용량 전체를 실패 처리하지 않고 직전 초기화권 값을 유지한다. 대시보드 Codex 카드에는 `초기화권 N개 · 기한 ...`을 로컬 시각으로 표시한다.
- 구현 완료 - 종료 동작: 프로세스 묶음에서 채팅 행은 `묶음 종료`·`강제` 버튼 대신 `터미널 종료` 하나를 표시하고 `/chats/:id/stop`을 호출한다. 따라서 개별 PID에 신호만 보내는 대신 SessionManager가 `/exit`, tmux 정리, 채팅 상태 변경, 리밋 재개 대기 삭제를 한 경로에서 처리한다. 시스템·기타 묶음과 펼친 개별 프로세스 제어는 기존 동작을 유지했다.
- 회귀 테스트: app-server 응답 파서 3개, 사용량 상세 병합·직전 캐시 보존 2개를 추가했다. Playwright는 초기화권 개수·기한 표시와 채팅 행에 `묶음 종료`가 없고 `터미널 종료`가 채팅 stop API만 호출하는 것을 검증한다.
- 문서 업데이트: README의 사용량·프로세스 대시보드 설명과 CODETREE의 새 app-server 조회 유틸·UsageMonitor·Overview·관련 테스트 역할을 현재 구현에 맞게 갱신했다.
- 검증 완료: 실제 로컬 app-server 조회가 초기화권 1개와 만료 ISO 시각을 반환했고, `npm run verify`의 타입 검사, 63개 파일 425개 단위 테스트, 프로덕션 클라이언트·서버 빌드와 공개 경로 검사를 모두 통과했다. 집중 Playwright 시나리오 1개도 14003 프로덕션 빌드에서 통과했다.

## 2026-08-11 Codex - 14003 운영 서버 최신 빌드 재실행 착수

- 작업 시작: 사용자가 대시보드 개선 커밋을 실제 운영 서버에 반영하도록 서버 재실행을 요청했다. Codex가 기존 `web_agent_manager_server_14003` tmux supervisor는 유지하고, pidfile로 확인한 현재 서버 자식 하나에만 SIGTERM을 보내 자동 재기동시킨다.
- 안전 원칙: 중지 플래그나 tmux 세션 자체는 건드리지 않아 supervisor의 정상 재시작 경로를 사용한다. 다른 채팅 tmux와 에이전트 프로세스는 종료하지 않으며, 재기동 전후 PID 변경·supervisor 로그·`/health` 응답을 함께 확인한다.
- 재실행 완료: pidfile의 기존 PID `2791050`이 실제 `/home/ubuntu/myagent/dist/server/src/server/index.js` 실행 프로세스임을 `/proc` 명령줄로 대조한 뒤 SIGTERM을 보냈다. 애플리케이션은 강제 종료 없이 `exit=0`으로 정상 종료했고 supervisor가 3초 뒤 최신 빌드를 PID `3042614`로 재기동했다.
- 검증 완료: 새 PID의 실행 진입점과 pidfile 값이 일치하고 supervisor 로그에 새 서버 시작·헬스 확인이 기록됐다. `http://127.0.0.1:14003/health`는 `{"ok":true}`를 반환했으며, `web_agent_manager_server_14003` supervisor와 채팅 `#205`, `#207`, `#208`, `#209` tmux는 모두 그대로 유지됐다.

## 2026-08-11 Codex - Codex `/usage` 초기화권 상세 화면 재확인 착수

- 작업 시작: 사용자가 첨부 화면으로 `/usage`의 `Usage limit resets` 상세에 `Full reset · Expires 02:28 on 13 Aug 2026`이 표시된다고 바로잡았다. Codex가 이미지를 직접 확인했으며, 앞서 “TUI에는 기한이 나오지 않는다”고 기록한 잘못된 조사 결론을 수정한다.
- 확인 계획: 첨부 TUI 시각과 app-server의 `expiresAt`이 같은 절대 시각인지 대조하고, 재실행된 서버가 실제 `usage_status.details_json`에 초기화권 개수·기한을 저장했는지 읽기 전용으로 확인한다. 구현이 이미 동일 데이터를 정확히 표시한다면 불필요한 수집 경로 변경은 하지 않고 설명·history만 바로잡는다.
- 중간 확인: 별도 Codex 조회 터미널에서 `/usage` → `Redeem usage limit reset`을 선택해 사용자 첨부와 같은 `Full reset · Expires 02:28 on 13 Aug 2026` 화면을 재현했다. 이 시각은 app-server가 한 차례 반환한 `2026-08-12T17:28:18.000Z`와 한국 시각 기준 정확히 일치한다.
- 결함 확인: 운영 DB에는 초기화권 개수 1개만 저장되고 `expiresAt`은 `null`이었다. app-server가 같은 계정에서도 상세 배열을 주는 경우와 개수만 주는 경우가 있어, 60초 조회 중 개수 전용 응답이 기한을 덮어쓴 것이 원인이다.
- 수정 방침: 사용자가 초기화권 상세 조회는 하루 1회면 된다고 정했다. 일반 사용량은 기존 60초 주기를 유지하되, Codex 초기화권 상세 메뉴 탐색과 app-server 보조 조회만 서버 시작 후 1회 및 이후 24시간 간격으로 수행하고, 그 사이에는 마지막 정상 기한을 보존한다.
- 구현 완료 - 상세 조회: `ProviderAdapter.usageDetails`에 저빈도 상세 명령의 진입·완료 판정·이탈 계약을 추가하고, Codex는 하루 1회 `/usage`에서 `Redeem usage limit reset`을 열어 `Full reset · Expires ...`를 파싱한다. 상세 목록이 비동기로 오래 `Loading`일 수 있음을 실제 재현해 최대 30초 동안 200ms 간격으로 완료를 확인하며, Full reset을 선택하는 추가 Enter는 보내지 않고 `Esc`로 메뉴를 닫는다.
- 구현 완료 - 기한 보존: app-server 보조 조회도 같은 하루 1회 경로에서만 실행한다. TUI 또는 app-server가 개수만 반환하면 같은 개수의 다른 신규 소스나 직전 저장값에서 정상 기한을 이어받고, 개수가 달라졌으면 옛 기한을 가져오지 않는다. 따라서 일반 60초 사용량 갱신이 초기화권 기한을 `null`로 다시 덮지 않는다.
- 회귀 테스트: Full reset 화면의 로컬 시각→ISO 변환, Loading 완료 판정, 24시간 경계, 같은 개수의 기한 보존과 개수 변경 시 옛 기한 폐기를 추가했다.
- 문서 업데이트: README에 일반 사용량 60초·초기화권 상세 하루 1회 주기와 정상 기한 보존을 명시했고, CODETREE에 새 상세 메뉴 계약·파서·병합 역할과 테스트를 반영했다. 앞선 작업 기록의 잘못된 “TUI에는 기한이 나오지 않았다” 문장도 실제로 상세 메뉴를 놓친 사실에 맞춰 정정했다.
- 로컬 검증 완료: `npm run verify`에서 타입 검사, 63개 파일 432개 단위 테스트, 프로덕션 클라이언트·서버 빌드와 공개 경로 검사를 모두 통과했다. 테스트용 Vite 서버를 직접 띄운 집중 Playwright 시나리오도 통과한 뒤 해당 임시 서버만 종료했다.

## 2026-08-11 Codex - 초기화권 상세 조회 수정 운영 반영 착수

- 작업 시작: 사용자의 기존 서버 재실행 요청 범위에서, 새 커밋 `9223dbb`의 하루 1회 Full reset 상세 조회를 14003 운영 서버에 반영한다. 기존 supervisor는 유지하고 현재 서버 자식 하나만 정상 종료해 자동 재기동시킨다.
- 검증 계획: 새 PID·헬스 응답을 확인한 뒤 하루 1회 최초 상세 조회가 끝날 때까지 운영 DB를 읽기 전용으로 관찰해 `rateLimitResetCredits.expiresAt`이 실제 기한으로 저장되는지 확인한다. 다른 채팅 tmux와 에이전트 프로세스는 건드리지 않는다.
- 재실행 완료: pidfile의 기존 PID `3042614`가 실제 프로덕션 서버 진입점을 실행하는지 `/proc`에서 대조한 뒤 SIGTERM으로 정상 종료했다. supervisor 로그에 `exit=0`이 기록됐고 3초 뒤 최신 빌드가 PID `3074569`로 시작됐으며 `/health`는 `{"ok":true}`를 반환했다. 서버 supervisor와 채팅 `#205`, `#207`, `#208`, `#209` tmux는 모두 유지됐다.
- 운영 데이터 검증: 새 서버의 최초 상세 조회 완료 시각 `2026-08-11T02:20:34.326Z`에 운영 DB가 초기화권 `1개`, 기한 `2026-08-12T17:28:18.000Z`를 저장했다. 이는 사용자 첨부의 한국 시각 `13 Aug 2026 02:28`과 동일하다.
- 보존 검증: 이어진 일반 60초 조회가 `2026-08-11T02:20:51.163Z`에 성공한 뒤에도 같은 기한이 유지됐다. 상세 조회가 아닌 주기 갱신에서는 app-server를 다시 호출하지 않고 직전 정상 기한을 보존하는 새 동작이 실제 운영 환경에 반영됐음을 확인했다.

## 2026-08-11 Codex - 사용자 요청 운영 서버 재실행 착수

- 작업 시작: 사용자가 14003 서버 재실행을 다시 요청했다. Codex가 현재 pidfile의 서버 자식과 실제 실행 진입점을 대조한 뒤, 기존 `web_agent_manager_server_14003` supervisor를 유지한 채 해당 자식 하나만 SIGTERM으로 정상 종료해 자동 재기동시킨다.
- 안전·검증 계획: 중지 플래그와 tmux 세션은 건드리지 않고 채팅 프로세스를 보존한다. 재기동 전후 PID 변경, supervisor의 정상 종료·새 시작 로그, `/health` 응답과 채팅 tmux 목록을 확인한다.
- 재실행 완료: 기존 PID `3074569`가 `/home/ubuntu/myagent/dist/server/src/server/index.js`를 실행 중임을 확인하고 SIGTERM을 보냈다. 애플리케이션은 `exit=0`으로 정상 종료했고 supervisor가 3초 뒤 같은 최신 빌드를 PID `3080358`로 재기동했다.
- 검증 완료: 새 PID의 실행 진입점과 pidfile 값이 일치하고 supervisor 로그에 새 서버 시작과 헬스 응답 확인이 기록됐다. `http://127.0.0.1:14003/health`는 `{"ok":true}`를 반환했으며, 서버 supervisor와 채팅 `#205`, `#207`, `#208`, `#209` tmux가 모두 유지됐다.

## 2026-08-11 Codex - 대시보드 초기화권 사용 기능 추가 착수

- 작업 시작: 사용자가 대시보드에서 Codex 초기화권을 실제 사용할 수 있게 해 달라고 요청했고, 여러 초기화권이 있으면 `/usage`의 `Full reset` 목록 맨 위 항목을 사용하도록 정했다. Codex가 기존 하루 1회 상세 조회 PTY와 사용량 API·대시보드 구조를 재확인해 최소 수정한다.
- 안전 원칙: 초기화권 소모는 되돌릴 수 없으므로 대시보드에서 개수·기한을 포함한 확인창을 거친 뒤 관리자 전용 서버 API를 호출한다. 서버는 저장된 잔여량과 실제 TUI 상세 화면을 다시 확인하고, 맨 위 `Full reset`이 선택된 상태에서만 확정 Enter를 보내며 중복 요청을 막는다.
- 검증 계획: 실제 초기화권은 검증 중 소모하지 않는다. TUI 상태 판정·API 거부 조건·클라이언트 요청을 단위/E2E 테스트에서 모의하고, 전체 타입 검사·테스트·빌드를 통과한 뒤 운영 서버를 재실행한다.
- 구현 경로 정정: 설치된 Codex 0.146.0이 공식 app-server 메서드 `account/rateLimitResetCredit/consume`을 제공하고, 프로토콜 생성 타입에 `idempotencyKey`, 선택적 `creditId`, `reset`·`nothingToReset`·`noCredit`·`alreadyRedeemed` 결과 계약이 있음을 프로그램 코드에서 확인했다. TUI에 확정 Enter를 보내는 방식은 화면 타이밍에 의존하므로 쓰지 않고, 상세 목록이 있으면 응답 순서상 첫 `available` ID를 명시하며 목록이 생략되면 backend의 “다음 사용 가능 권리” 선택 계약을 사용한다.
- 구현 완료 - 서버: `codex-rate-limits.ts`가 공식 consume 메서드에 UUID 멱등성 키와 응답 순서상 첫 available ID를 보내고 사용 뒤 잔여량을 다시 읽는다. 사용 응답만 유실돼도 실제 잔여량 감소가 확인되면 성공으로 복구한다. `UsageMonitor`는 저장 잔여량, 요청한 계정, 계정별 동시 소모를 검증하고 결과를 DB와 실시간 화면에 반영하며, 관리자 전용 API는 계정 ID·결과·잔여량을 감사 로그에 남긴다.
- 구현 완료 - 화면: Codex 초기화권이 남은 관리자 카드에 `사용하기` 버튼을 추가했다. 누르면 해당 계정 카드의 맨 위 Full reset 1개, 표시된 기한, 되돌릴 수 없다는 점을 확인한 뒤에만 API를 호출하며 완료 후 대시보드를 다시 읽는다. 일반 사용자에게는 소모 버튼이 보이지 않는다.
- 재확인 보강: 최초 구현을 다시 검토하며 다중 계정에서 보조 계정 카드를 눌러도 기본 계정이 선택될 수 있는 위험을 발견했다. Codex가 카드의 `account_id`를 요청 본문에 포함하고 서버가 해당 계정 모니터만 선택하도록 수정해, 사용자가 누른 카드의 권리 외에는 소모하지 않게 했다.
- 문서 업데이트: README에 해당 계정 카드의 맨 위 권리를 사용한다는 동작과 서버 검증을 반영했고, CODETREE에 app-server 소비 유틸·서비스·관리자 API·화면·회귀 테스트 역할을 갱신했다.
- 검증 완료: 실제 초기화권은 소모하지 않고 app-server 요청을 모의했다. 집중 단위 테스트 50개와 계정 ID가 포함된 집중 Playwright 1개가 통과했고, 최종 `npm run verify`에서 타입 검사, 63개 파일 441개 단위 테스트, 프로덕션 클라이언트·서버 빌드 및 공개 경로 검사가 모두 통과했다.

## 2026-08-11 Codex - 초기화권 사용 기능 운영 반영 착수

- 작업 시작: 사용자의 서버 재실행 요청 범위에서 커밋 `04dda71`의 관리자용 초기화권 사용 기능을 14003 운영 서버에 반영한다. Codex가 기존 `web_agent_manager_server_14003` supervisor를 유지하고 pidfile과 실제 명령줄이 일치하는 서버 자식 하나만 SIGTERM으로 정상 종료해 자동 재기동시킨다.
- 안전·검증 계획: 초기화권 사용 API는 호출하지 않아 실제 권리를 소모하지 않는다. 재기동 전후 PID·프로덕션 진입점·supervisor 정상 종료 로그·`/health` 응답을 확인하고 기존 채팅 tmux가 유지되는지 대조한다.
- 재실행 완료: pidfile의 기존 PID `3080358`이 `/home/ubuntu/myagent/dist/server/src/server/index.js`를 실행 중임을 확인한 뒤 SIGTERM으로 정상 종료했다. supervisor 로그에 `exit=0`이 기록됐고 3초 뒤 새 프로덕션 빌드가 PID `3100296`으로 시작됐다.
- 운영 검증 완료: 새 PID·진입점·pidfile이 일치하고 `/health`가 `{"ok":true}`를 반환했다. 운영 서버가 제공한 최신 JS에서 `사용하기`, `맨 위 Full reset`, `/usage/codex/reset-credit/redeem` 문자열을 확인했으며, 재기동 전 존재하던 서버 supervisor와 채팅 `#209` tmux가 모두 유지됐다. 실제 초기화권 사용 API는 호출하지 않았다.

## 2026-08-11 Codex - 채팅 #210 웹 전송 누락 진단 착수

- 작업 시작: 사용자가 채팅 #210에서 웹 메시지 전송 직후 상태는 `작업중`으로 바뀌었지만 실제 터미널에는 입력이 전달되지 않았다고 보고했다. Codex가 해당 채팅의 DB 상태·서버 요청 로그·SessionManager 전송 경로·현재 tmux pane과 프로세스를 같은 시각 기준으로 대조한다.
- 진단 원칙: 현상을 보존하기 위해 메시지를 재전송하거나 터미널 키 입력·세션 재시작 같은 상태 변경은 하지 않는다. 인증 파일이나 토큰 값은 읽지 않고 앱 상태·프로세스·마스킹된 운영 로그만 확인한다.
- 현상 확인: 클라이언트는 `2026-08-11T03:32:46.105Z`에 채팅 #210의 116자 전송을 시작했고 402ms 뒤 HTTP 202를 받았다. 감사 로그에도 같은 길이의 `chat.prompt`가 남았으며 tmux pane에는 116자 본문이 Codex 입력창에 그대로 들어가 있었다. 그러나 제출 뒤 생겨야 할 `provider_session_id`·`history_file`은 여전히 없고 pane은 유휴 프롬프트 상태라, 본문 붙여넣기는 성공했지만 Enter 제출만 적용되지 않은 것으로 확정했다.
- 원인: `sendPromptNow`는 `paste-buffer`와 `send-keys Enter`의 tmux 명령 성공만 확인한 직후 실제 Codex 수신 여부와 무관하게 `busy=true`를 저장하고 HTTP 202를 반환한다. tmux가 Enter 명령을 받아도 Codex TUI가 프롬프트를 제출하지 않은 이번 경우를 검출·재시도·busy 원복하는 확인 절차가 없어 웹 상태만 `작업중`으로 남았다. Enter가 Codex 내부에서 유실된 더 낮은 단계의 이유는 현재 로그에 애플리케이션 수신 확인이 없어 단정할 수 없다.
- 진단 완료: 채팅 #210의 현 상태를 보존했고 키 입력이나 메시지 재전송은 하지 않았다. 수정한다면 전송 뒤 pane 또는 JSONL에서 제출을 확인하고, 입력창에 같은 본문이 남은 경우에만 Enter를 제한적으로 재시도하며 확인 실패 시 `busy`를 원복하는 경로와 실제 tmux 통합 회귀 테스트가 필요하다.

## 2026-08-11 Codex - 채팅 제출 확인 및 장문 파일 전환 개선 착수

- 작업 시작: 사용자가 #210의 Enter 유실 하위 원인을 더 확인하고, 웹 채팅의 긴 본문은 터미널에 통째로 붙여넣지 말고 텍스트 파일로 변환해 전달하는 개선을 시작하도록 요청했다. Codex가 tmux/Codex 입력 이벤트 계약과 기존 첨부 저장 경로를 재확인한 뒤 전송 확인·제한 재시도·장문 파일화를 함께 구현한다.
- 구현 원칙: 짧은 메시지는 기존 UX를 유지하되 tmux 명령 종료 코드가 아니라 TUI 화면 또는 공급자 기록 변화로 제출을 확인한다. 같은 본문이 입력창에 남아 있을 때만 Enter를 한 번 제한적으로 재시도하고, 끝내 확인되지 않으면 `busy`를 원복해 웹이 거짓 작업중 상태로 남지 않게 한다. 긴 본문은 채팅 작업공간 아래 앱 전용 업로드 폴더에 안전한 `.txt`로 저장하고 짧은 파일 읽기 지시만 TUI에 보낸다.
- 검증 계획: 실제 #210에는 키를 보내지 않고 현상을 보존한다. tmux/Codex 입력 타이밍은 격리된 로컬 테스트와 순수 판정 테스트로 재현하며, 장문 임계값·파일명·원문 보존·경로 경계·제출 실패 상태 원복을 회귀 테스트한 뒤 전체 타입 검사·테스트·빌드를 수행한다.
- 하위 원인 확정: web-agent-manager가 `tmux paste-buffer`를 bracketed paste 표시 없이 사용해 Codex가 본문을 개별 고속 키 입력 burst로 받았다. Codex 0.146.0의 paste burst 판정은 마지막 고속 입력 뒤 120ms 안의 Enter를 제출이 아닌 붙여넣기 내부 줄바꿈으로 바꾸며, #210 입력창 끝에 본문과 빈 줄이 남은 실제 화면이 이 동작과 일치했다. tmux 3.4 소스도 `paste-buffer -p`일 때만 bracketed paste 모드 pane에 시작·끝 표식을 붙이는 계약임을 확인했다.
- 구현 완료 - 입력 전달: `pastePromptToTmux`를 `paste-buffer -p`로 바꾸고 Codex에는 160ms 안정화 지연을 선언했다. 유휴 일반 메시지는 Enter 뒤 화면이 실제 작업중 또는 빈 입력창으로 바뀌었을 때만 제출 성공으로 인정한다. 본문이 남아 있으면 Enter를 한 번만 재시도하고, 두 번 모두 실패하면 Unicode 문자 수만큼 터미널 초안을 지운 뒤 생성 파일과 `busy`를 원복해 웹 입력창 복구와 중복되지 않게 했다. 작업 중 후속 입력과 슬래시 명령의 기존 큐·자동완성 동작은 유지했다.
- 구현 완료 - 장문 전환: 1,000자를 넘는 일반 텍스트 붙여넣기는 브라우저에서 기존 첨부 API를 통해 UTF-8 `.txt`로 올리고 입력창에는 `[첨부: 경로]`만 남긴다. 브라우저를 거치지 않고 API에 긴 본문이 들어와도 서버가 프로젝트와 전용 worktree의 `.web-agent-manager-uploads/<chatId>/`에 원문을 원자 저장하고 짧은 파일 읽기 지시만 TUI에 전달한다. 슬래시·셸 명령은 길어도 원문을 유지하고, 경로 경계와 symlink 탈출을 기존 유틸로 차단했다.
- 검증 완료: 실제 #210에는 키 입력·재전송·재시작을 하지 않았다. 집중 단위 테스트 26개와 장문 multipart 업로드를 확인하는 Playwright 1개가 통과했고, 최종 `npm run verify`에서 타입 검사, 65개 파일 450개 단위 테스트, 프로덕션 클라이언트·서버 빌드 및 공개 경로 검사가 모두 통과했다.

## 2026-08-11 Codex - 채팅 제출·장문 첨부 개선 운영 반영 착수

- 작업 시작: 사용자의 앞선 서버 재시작 요청에 따라 커밋 `e17a1fc`의 Enter 유실 방지와 장문 텍스트 첨부 전환을 14003 운영 서버에 반영한다. Codex가 pidfile과 실제 프로덕션 진입점을 대조한 뒤 기존 supervisor는 유지하고 서버 자식 하나만 SIGTERM으로 정상 종료해 자동 재기동시킨다.
- 안전·검증 계획: 입력 초안이 보존된 채팅 #210을 포함한 채팅 tmux에는 키를 보내거나 세션을 재시작하지 않는다. 서버 재기동 전후 PID·진입점·헬스 응답·배포 JS 문자열과 tmux 목록을 확인한다.
- 재실행 완료: pidfile의 기존 PID `3100296`이 `/home/ubuntu/myagent/dist/server/src/server/index.js`를 실행 중임을 확인한 뒤 SIGTERM으로 정상 종료했다. supervisor가 같은 프로덕션 진입점을 새 PID `3133199`로 자동 재기동했다.
- 운영 검증 완료: `http://127.0.0.1:14003/health`가 `{"ok":true}`를 반환했고, 운영 클라이언트 빌드에서 장문 `pasted-text-` 업로드 코드와 서버 빌드에서 Codex 제출 실패 처리 문자열을 확인했다. 재기동 전후 `web_agent_manager_chat_209`, `web_agent_manager_chat_210`, `web_agent_manager_server_14003` tmux가 모두 유지됐으며 #210에는 어떤 키 입력도 보내지 않았다.

## 2026-08-11 Codex - 사용량 리셋 후 최소 세션 활성화 착수

- 작업 시작: 사용자가 실제 채팅이 없는 동안 Claude 5시간 창의 초기화 시각이 계속 뒤로 밀리는 문제를 막기 위해, 사용량 조회 전용 세션에서 리셋 직후 토큰을 거의 쓰지 않는 단답 프롬프트를 한 번 보내도록 요청했다. 대상은 Claude 세션 창이 조회에서 사라졌거나 0%인 경우와 Codex 사용률이 직전 양수에서 0%로 바뀐 경우다.
- 구현 원칙: 고정 프롬프트는 `1`만 답하도록 짧게 만들고 사용량 조회 PTY 안에서만 보낸다. 이 프롬프트로 생긴 JSONL은 일반 채팅 목록에서 숨기며, 공급자·계정별 마지막 전송 시각을 DB에 저장해 서버 재시작과 1분 조회 반복에도 5시간 안에는 중복 전송하지 않는다. 인증 정보는 읽지 않고 기존 계정별 PTY 환경만 그대로 사용한다.
- 검증 계획: 실제 운영 계정에 테스트 프롬프트를 보내지 않고 조건 판정·DB 중복 선점·내부 기록 숨김·PTY 입력 순서를 모의 테스트한다. README와 CODETREE를 갱신하고 전체 타입 검사·테스트·빌드 후 운영 서버를 정상 재기동한다.
- 구조 결정: 기존 세션 초기화 알림과 같은 정상 사용량 파싱 결과와 DB 중복 방지 원칙을 쓰되, 알림 타이머가 최소 턴을 직접 보내지는 않게 분리했다. 알림은 예정 시각+1분에 재조회 없이 울릴 수 있지만 토큰을 쓰는 최소 턴은 실제 조회에서 세션 없음/0% 전환이 확인된 뒤에만 보내 조기·오검출 전송을 막는다.
- 구현 완료: `UsageMonitor.finishUsage`가 stale·후퇴로 거부되지 않은 결과에서 Claude `session` 창 없음/0%와 Codex 임의 창의 양수→0%를 판정한다. 조건이 맞으면 사용량 메뉴를 닫고 `[WAM usage] 1이라고만 답해.` 본문과 Enter를 안정화 간격을 두고 전달하며 최대 15초 동안 단답 완료를 기다린다. `usage_keepalive_prompts`가 공급자·계정별 마지막 전송과 사유를 저장해 5시간 내 반복·서버 재시작 중복을 막고, 전송 자체가 실패하면 선점 기록을 지워 다음 조회에서 재시도한다.
- 내부 기록 처리: Claude는 고정 최소 턴 이외의 실제 user 메시지가 있는지 mtime마다 다시 검사하고, Codex는 프로젝트 지침과 `/usage` 같은 슬래시 명령을 건너뛴 첫 실제 프롬프트를 검사한다. 두 공급자 모두 최소 턴과 `1` 응답만 있는 상태 조회 JSONL을 일반 채팅 목록에서 숨긴다.
- 검증 완료: 실제 운영 계정에는 테스트 프롬프트를 보내지 않았다. 집중 테스트 4개 파일 111개가 통과했고, 최종 `npm run verify`에서 타입 검사, 66개 파일 457개 테스트, 프로덕션 클라이언트·서버 빌드와 공개 경로 검사가 모두 통과했다.
- 추가 요청 착수: 사용자가 최소 단답을 언제 남겼는지 확인할 수 있게 해 달라고 요청했다. Codex가 새 상태를 중복 저장하지 않고 기존 `usage_keepalive_prompts.sent_at`을 사용량 응답에 합쳐 계정별 대시보드 카드에 마지막 전송 시각과 사유를 짧게 표시하도록 확장한다.
- 추가 구현 완료: `UsageMonitor.list`가 사용량 상태와 최소 단답 기록을 계정 키로 결합하고, 전송 성공 직후 `usage_updated`를 방송한다. 대시보드 카드는 기록이 있을 때만 `세션 유지 단답 · 마지막 전송 ...`과 Claude 세션 창 없음·0% 또는 Codex 0% 전환 사유를 로컬 시각으로 표시한다.
- 추가 검증 완료: 최소 단답 DB 결합·중복 제한·공급자 기록 숨김을 포함한 집중 테스트 114개와 사용량 카드 Playwright 시나리오가 통과했다. 최종 전체 검증에서도 66개 파일 457개 테스트, 타입 검사, 프로덕션 클라이언트·서버 빌드와 공개 경로 검사가 모두 통과했다.

## 2026-08-11 Codex - GitHub 로컬 diff 명시 선택 방식 개선 착수

- 작업 시작: 사용자가 GitHub 탭의 로컬 보기에서 파일을 선택하지 않았는데도 현재 diff 파일이 자동으로 보이는 동작을 수정해, 파일을 직접 선택했을 때만 diff 본문을 표시해 달라고 요청했다.
- 구현 원칙: Codex가 기존 로컬 변경 파일 선택 상태와 diff 조회·컨텍스트 펼치기 흐름을 다시 확인한 뒤, 로컬 보기의 초기 자동 선택만 제거한다. 커밋 상세나 PR처럼 항목 선택 자체가 상세 진입을 뜻하는 다른 diff 화면은 유지하고, 선택 파일이 삭제·갱신됐을 때의 안전한 상태 정리도 함께 확인한다.
- 구현 완료: `selectedFiles`가 비어 있으면 로컬 diff API를 호출하지 않고 이전 본문을 즉시 비우며 선택 안내만 표시한다. 파일을 하나 이상 체크한 경우에만 반복 `file` 쿼리로 해당 범위를 조회하고, 선택이 바뀐 뒤 늦게 도착한 이전 응답은 폐기한다. 기존 `전체 diff` 버튼은 실제 의미에 맞게 `선택 해제`로 바꿨고 커밋 상세·PR diff 흐름은 건드리지 않았다.
- 검증 완료: Playwright에서 변경 파일이 있어도 초기 로컬 diff 요청이 0회이고 안내만 보이는지 확인한 뒤, 파일 체크 시 해당 경로의 `file` 쿼리 1회와 diff 본문이 나타나는지 검증했다. 이 시나리오와 사용량 카드 시나리오 2개가 통과했고, 최종 `npm run verify`의 타입 검사·457개 테스트·프로덕션 빌드도 모두 통과했다.

## 2026-08-11 Codex - 최소 세션 활성화·로컬 diff 운영 반영 착수

- 작업 시작: 사용자의 서버 재시작 요청 범위에서 커밋 `d0f6fe6`, `63083c9`의 사용량 최소 단답·마지막 전송 표시와 로컬 선택 파일 diff를 14003 운영 서버에 반영한다.
- 안전·검증 계획: Codex가 pidfile의 서버 자식과 실제 프로덕션 진입점을 대조한 뒤 기존 supervisor는 유지하고 해당 자식 하나만 SIGTERM으로 정상 종료한다. 재기동 전후 PID·헬스·배포 문자열·tmux 목록을 확인하며, 초안이 남아 있을 수 있는 채팅 #210에는 어떤 키도 보내지 않는다. 최초 사용량 조회가 조건을 만족하면 사용자가 요청한 최소 단답이 실제 조회 PTY에서 한 번 전송될 수 있으며, 그 경우 민감 정보 없이 공급자·계정 ID·사유·전송 시각만 확인한다.
- 운영 반영 보류·추가 진단 착수: 사용자가 로컬 diff에서 파일을 여러 개 선택하면 다시 본문이 나오지 않는다고 지적했다. 앞선 Playwright가 단일 파일만 검증해 다중 선택 계약을 놓친 것이므로, Codex가 반복 `file` 쿼리·서버 diff 결합·클라이언트 비동기 응답 순서를 2개 파일 시나리오로 재현하고 수정·검증한 뒤 서버를 재시작한다.
- 추가 원인 확인·수정: 반복 `file` 쿼리 자체는 양쪽 모두 지원했지만, 선택 목록에 삭제 파일이 하나라도 있으면 서버가 모든 경로에 현재 파일 존재를 요구해 요청 전체를 실패시켰고 클라이언트가 그 오류를 빈 diff처럼 숨겼다. 삭제 파일은 Git pathspec으로 정상이라는 계약에 맞춰 symlink·프로젝트 경계 검사는 유지하면서 존재 요구만 풀었고, 클라이언트는 조회 오류 문구를 직접 표시한다.
- 추가 검증 완료: 실제 임시 Git 저장소에서 수정 파일과 삭제 파일을 반복 `file` 쿼리로 함께 선택해 두 diff와 HTTP 200을 확인했다. Playwright도 첫 파일 선택 후 삭제 파일을 추가했을 때 `선택 파일 diff (2)`와 두 본문, 두 경로 쿼리를 검증했다. 최종 `npm run verify`에서 타입 검사, 66개 파일 458개 테스트, 프로덕션 클라이언트·서버 빌드와 공개 경로 검사가 모두 통과했다.
- 재실행 완료: pidfile의 기존 PID `3133199`가 `/home/ubuntu/myagent/dist/server/src/server/index.js`를 실행하는지 대조한 뒤 SIGTERM으로 정상 종료했다. supervisor가 `exit=0`을 기록하고 최신 빌드를 PID `3264622`로 재기동했으며 `/health`는 `{"ok":true}`를 반환했다.
- 운영 기능 확인: 배포 클라이언트 번들에 `세션 유지 단답`과 선택 전 diff 안내 문구가 포함됐고, 새 서버의 최초 정상 조회에서 Claude 계정 ID 2가 0% 세션을 감지해 최소 단답을 한 번 전송했다. `usage_keepalive_prompts`에는 `claude_session_zero`, 전송 시각 `2026-08-11T08:43:00.981Z`(한국 시각 17:43:00)가 저장돼 대시보드의 마지막 전송 표시와 5시간 중복 제한에 사용된다.
- 운영 보존 확인: 서버 supervisor와 `web_agent_manager_chat_209`, `web_agent_manager_chat_210` tmux가 재기동 전후 모두 유지됐고 #210에는 어떤 키 입력도 보내지 않았다.

## 2026-08-11 Codex - Claude 세션 초기화 시각 10분 차이 원인 추적

- 작업 시작: 사용자가 Claude 초기화 시각이 예상보다 10분 늦어진 이유와 최소 단답 전송이 기존에 없던 세션을 생성해 시각을 확정한 것인지 확인해 달라고 요청했다. Codex가 사용량 상태·초기화 알림·최소 단답 DB 시각, 사용량 처리 순서, 상태 조회 전용 Claude 기록을 다시 대조한다.
- 진단 원칙: 인증 정보나 토큰은 읽지 않고 사용량 시각·고정 최소 프롬프트 기록·애플리케이션 코드와 마스킹된 운영 기록만 확인한다. 추가 프롬프트나 터미널 키는 보내지 않아 현재 세션 창을 변경하지 않는다.
- 순서 확인: 초기화 알림 전송 기록은 한국 시각 `17:43:00`, 최소 단답 DB 선점은 `17:43:00.981`, Claude의 실제 프롬프트 기록은 `17:43:01.409`, 응답 `1`은 `17:43:02.962`였다. `finishUsage`도 정상값 저장과 초기화 창 관찰을 먼저 수행한 뒤 최소 단답을 보내므로, 새 `22:40` 시각과 조기 전환 판정은 단답 입력 전에 이미 결정됐다.
- 상태 확인: 최소 단답 사유는 `claude_session_missing`이 아니라 `claude_session_zero`였다. 따라서 당시 처리 결과에는 이미 `Current session` 창과 초기화 시각이 있었고 사용률만 0%였으며, 단답이 없던 세션 행을 새로 만든 경우가 아니다. 단답은 그 창에 실제 모델 턴을 아주 작게 남겨 활성 상태를 고정하는 역할만 했다.
- 10분 차이 해석: 단답은 `17:43`에 전송됐고 현재 초기화는 `22:40`이라 실제로는 전송 후 약 4시간 57분이다. 애플리케이션에는 10분을 더하거나 10분 단위로 올림하는 로직이 없고, `22:40`은 단답 전에 사용량 처리에서 확보된 시각이다. 운영 로그가 info 레벨이라 최초 `/usage` 원문이 남지 않아 Claude가 직접 `22:40`을 반환했는지, 만료된 `17:40` 스냅샷을 기존 5시간 보정이 `22:40`으로 바꿨는지는 사후에 구분할 수 없다.
- 진단 완료: 직전 예약은 `20:30`, 새 예약은 `22:40`이며 `17:43`에 직전 예약보다 먼저 새 창이 관찰돼 조기 초기화 알림이 발생했다. 단답 때문에 10분이 늦어진 것은 아니며, `17:30`이 아니라 `17:40` 경계로 잡힌 이유의 원천만 원문 로그 부재로 확정할 수 없다.

## 2026-08-11 Codex - Claude 초기화 후 최소 단답 시각 미갱신 진단

- 작업 시작: 사용자가 Claude 세션 초기화 뒤에도 대시보드의 최소 단답 마지막 전송 시각이 `17:43:00`에서 갱신되지 않았다고 보고했다. Codex가 현재 사용량·초기화 예약·최소 단답 DB 기록, 5시간 중복 제한과 운영 로그·Claude 내부 고정 프롬프트 기록을 다시 대조해 감지 누락과 전송 제한 중 어느 쪽인지 확인한다.
- 진단 원칙: 현재 세션을 바꿀 수 있는 프롬프트·터미널 입력은 보내지 않고 읽기 전용 상태만 확인한다. 인증 정보나 토큰은 읽지 않는다.
- 원인 확인: Claude 초기화 예약은 `22:40`이었지만 직전 최소 단답이 `17:43:00.981`에 전송돼, 전송 시각 기준 정확히 5시간인 중복 제한이 `22:43:00.981`까지 다음 전송을 막고 있었다. 초기화 시각과 직전 단답 시각이 3분 어긋나 초기화 직후 대시보드에는 이전 시각이 잠시 유지된 것이다.
- 후속 동작 확인: `22:42:53` 정기 조회가 시작돼 제한 만료 직후인 `22:43:01.759`에 사유 `claude_session_missing`으로 DB 시각을 갱신했다. Claude 내부 기록에도 `22:43:02.177` 고정 프롬프트와 `22:43:05.146` 응답 `1`이 남았고 운영 로그에 전송 성공이 기록됐다. 따라서 감지나 화면 갱신 고장이 아니라 현재의 5시간 중복 제한에 따른 약 3분 지연이었다.
- 진단 완료: 대시보드가 받는 `usage_updated` 방송도 성공 전송 직후 발생했으므로 현재 마지막 전송 시각은 `2026-08-11 22:43:01`로 갱신됐다. 즉시 전송이 필요하다면 후속 변경에서 단순 `sent_at + 5시간`이 아니라 새 reset 창 식별자를 기준으로 중복을 막도록 정책을 바꿔야 한다.

## 2026-08-11 Codex - 최소 단답 중복 방지를 초기화 창 기준으로 변경 착수

- 작업 시작: 사용자가 초기화 직후에도 직전 단답 시각 기준 5시간 제한 때문에 전송이 늦어지는 조건을 수정해 달라고 요청했다. Codex가 단순 시간 간격 대신 공급자·계정의 실제 새 사용량 창을 식별해, 같은 창에서는 중복을 막고 다른 창이면 즉시 전송하도록 최소 수정한다.
- 구현 원칙: Claude는 세션 창의 초기화 시각, Codex는 0%로 전환된 창들의 ID·초기화 시각을 안정적인 창 키로 사용한다. 서버 재시작 후에도 같은 창 중복을 막도록 DB에 마지막 창 키를 함께 저장하되 기존 전송 시각·사유 표시는 보존하고, 이전 스키마 행은 안전하게 마이그레이션한다.
- 검증 계획: 같은 창 0% 반복은 한 번만 전송, 초기화 시각이 달라진 새 창은 5시간 미만이어도 즉시 전송, 기존 행 마이그레이션과 전송 실패 시 선점 원복을 단위 테스트로 확인한다. README·CODETREE를 갱신하고 전체 타입 검사·테스트·빌드를 통과한 뒤 논리적 단위로 커밋한다.
- 구현 완료: 정상 사용량에서 최소 단답 사유와 초기화 절대시각 기반 창 키를 함께 계산하고 `usage_keepalive_prompts.window_key`에 저장한다. 같은 창은 Claude 표기 시각이 15분 안에서 흔들려도 중복으로 보며, 새 창 키면 직전 전송 후 5시간이 지나지 않았어도 즉시 전송한다. 세션 블록이 사라진 Claude는 직전 리셋 시각을 5시간씩 넘겨 현재 창의 다음 리셋을 복원하고, 창을 전혀 식별할 수 없는 예외에만 기존 5시간 제한을 안전장치로 남겼다.
- 마이그레이션·실패 처리: 기존 DB에는 nullable `window_key`를 자동 추가한다. 기존 Claude 전송 시각이 계산된 현재 5시간 창 안에 있으면 추가 프롬프트 없이 창 키만 승계하고, 새 창 전송이 실패하면 덮어쓰기 전 사유·시각·창 키를 복원해 다음 조회가 잘못 막히지 않게 했다.
- 문서 업데이트: README의 계정별 5시간 1회 설명을 새 초기화 창마다 1회로 정정했고, CODETREE에 창 키 계산·동일 창 허용 오차·기존 행 승계와 DB 컬럼 역할을 반영했다.
- 검증 완료: 같은 창 반복, 15분 이내 표시 오차, 5시간 미만 새 창 즉시 허용, Claude 세션 없음 시 다음 창 복원, 기존 행 승계·스키마 마이그레이션, 실패 원복을 포함한 집중 37개 테스트와 타입 검사를 통과했다. 최종 `npm run verify`에서도 66개 파일 464개 테스트, 타입 검사, 프로덕션 클라이언트·서버 빌드와 공개 경로 검사가 모두 통과했다.

## 2026-08-11 Codex - 초기화 창 기준 최소 단답 운영 반영 착수

- 작업 시작: 사용자 요청으로 커밋 `0de1c82`의 초기화 창 기준 중복 방지를 14003 운영 서버에 반영한다. Codex가 pidfile과 실제 프로덕션 진입점을 대조한 뒤 기존 supervisor와 채팅 tmux는 유지하고 서버 자식 하나만 SIGTERM으로 정상 종료해 자동 재기동시킨다.
- 안전·검증 계획: 재기동 전후 PID·진입점·헬스 응답·tmux 목록을 확인한다. 시작 시 DB에 `window_key` 컬럼이 추가되고 기존 `22:43` 단답이 현재 `03:40` 창 안의 기록으로 승계돼 불필요한 추가 프롬프트가 나가지 않는지, 운영 로그와 민감정보 없는 DB 필드만으로 확인한다.
- 재실행 완료: pidfile의 기존 PID `3264622`가 프로덕션 진입점을 실행 중임을 확인한 뒤 SIGTERM으로 정상 종료했다. supervisor가 `exit=0`을 기록하고 3초 뒤 최신 빌드를 PID `3404023`으로 재기동했으며 `/health`는 `{"ok":true}`를 반환했다.
- 마이그레이션·동작 확인: 운영 DB에 `usage_keepalive_prompts.window_key`가 추가됐고 기존 `22:43:01` 전송 시각·사유는 그대로 보존됐다. 현재 Claude 세션은 이미 1% 사용 중이라 최소 단답 조건이 아니어서 창 키 승계와 추가 프롬프트 모두 발생하지 않았다. 다음 초기화에서 0% 또는 세션 없음이 감지되면 새 초기화 시각으로 창 키를 만들고 기존 null 키와 다르다고 판단해 즉시 전송한다.
- 운영 검증 완료: 새 PID·진입점·헬스가 정상이고 서버 supervisor 및 채팅 `#209`, `#210` tmux가 모두 유지됐다. 재기동 이후 운영 로그에 추가 keepalive가 없어 현재 창에 불필요한 모델 턴을 만들지 않은 것도 확인했다.

## 2026-08-12 Codex - 현재 myagent 구현 구조 설명 착수

- 작업 시작: 사용자가 현재 myagent가 어떻게 구현되어 있는지 기능과 내부 흐름을 하나씩 설명해 달라고 요청했다. Codex가 최신 README·CODETREE만 요약하지 않고 실제 서버·클라이언트 진입점, 라우터, 세션·기록·사용량·Git·보안 서비스를 다시 읽어 현재 브랜치 기준 구조를 설명한다.
- 설명 원칙: 코드를 수정하지 않고 읽기 전용으로 확인한다. 먼저 전체 요청 흐름과 데이터 정본을 잡은 뒤, 서버 시작·인증·프로젝트·tmux 채팅·JSONL 동기화·승인·사용량·파일·GitHub·알림·클라이언트 순으로 파일 위치와 역할을 연결한다. 토큰·API 키·비밀번호 등 민감정보는 조회하지 않는다.
- 작업 완료: Codex가 `README.md`, `CODETREE.md`, 서버 부트스트랩과 설정·DB·보안 코드, 공급자 어댑터, 세션·기록·승인·사용량·워크트리·알림 서비스, REST·WebSocket 라우트, React·xterm 클라이언트 및 Electron 진입점을 다시 확인했다. 현재 구현은 API가 답변을 생성하는 채팅 앱이 아니라, 계정별 실제 Claude Code·Codex CLI를 tmux에서 실행하고 브라우저가 REST와 WebSocket으로 조종·관찰하는 관리자라는 점을 중심으로 설명을 구성했다.
- 확인 결과: 메시지 본문은 SQLite에 복제하지 않고 공급자 JSONL을 정본으로 사용하며, 터미널 상태는 tmux가 정본이고 브라우저 xterm은 실시간 표시기 역할을 한다. SQLite는 사용자·프로젝트·채팅 연결·상태·승인·사용량·알림·감사 등 관리 메타데이터를 보관한다. 설명 전용 작업이므로 제품 코드는 변경하지 않았고, 민감정보도 조회하지 않았다.

## 2026-08-12 Codex - Claude 짧은 첫 턴 사용량 확인

- 작업 시작: 사용자가 `ㅎㅇ` 한 번에도 Claude 세션 사용률이 2%로 표시된다고 관찰했다. Codex가 사용량 파서와 Claude 실행·지침 주입 경로를 다시 확인해, myagent 자체 계산인지 Claude CLI 원본 값인지와 짧은 입력 외에 포함되는 비용을 구분한다.
- 확인 완료: myagent는 Claude `/usage`의 `Current session` 퍼센트를 가공 계산하지 않고 그대로 파싱한다. 따라서 2%는 두 글자의 크기가 아니라 Claude Code의 내장 시스템 문맥·도구 정의·프로젝트 지침·응답 등 전체 턴 비용이 반영된 값이며, 별도 최소 단답이 같은 사용량 창에서 실제 전송됐다면 그 턴도 합산될 수 있다. 사용량 조회용 `/usage` 자체는 모델 답변 턴이 아니고, 프로젝트 `AGENTS.md`와 `CLAUDE.md`는 현재 합계 823바이트라 프로젝트 지침만으로 2%가 됐다고 보기는 어렵다. 제품 코드는 변경하지 않았다.
- 사용자 정정 반영: 사용자가 해당 초기화 창에서 다른 Claude 사용이 전혀 없었음을 확인했다. 따라서 이번 2%는 일반 채팅과 합산됐을 가능성이 아니라, `usage-monitor`가 조회 PTY에 실제 모델 턴으로 제출한 `[WAM usage] 1이라고만 답해.` 세션 유지 요청 하나가 발생시킨 사용량으로 판단하는 것이 맞다. Codex가 앞서 원인을 여러 가능성으로 분산해 설명한 부분을 바로잡았으며, 최소 출력이어도 Claude Code의 모델 요청 고정 문맥 비용까지 없어지는 것은 아니라는 결론이다.

## 2026-08-12 Codex - Claude 사용량 조회 세션 컨텍스트 누적 조사 착수

- 작업 시작: 사용자가 세션 유지용 최소 턴 자체보다 `usage-monitor`의 Claude 조회 PTY를 계속 재사용하면서 이전 턴 컨텍스트가 누적되는 것이 2% 사용의 원인일 수 있다고 지적했고, 매 유지 턴마다 세션을 폐기·재생성하거나 JSONL 토큰 수치로 검증해 달라고 요청했다.
- 조사 계획: Codex가 `UsageMonitor`의 PTY 생성·반복 조회·유지 턴 이후 생명주기를 다시 추적하고, Claude 내부 JSONL에서 대화 내용은 출력하지 않은 채 `input_tokens`·캐시 생성/읽기·`output_tokens` 숫자만 추출해 누적 여부를 확인한다. 누적이 확인되면 유지 턴 응답 직후 기존 조회 PTY를 종료하고 새 세션으로 교체하는 최소 변경을 적용하며, 기존 주기 조회·창 중복 방지·다계정 정리에 미치는 영향을 테스트한다.
- 원인 확인: 기본 계정의 내부 JSONL 두 개에서 내용은 노출하지 않고 usage 숫자만 비교했다. 같은 세션의 첫/다음 유지 턴이 각각 `cache_creation_input_tokens 6,519 → 27,013`, 다른 세션은 `26,025 → 46,447`로 증가해 두 경우 모두 5시간 동안 약 20,400토큰의 조회 문맥이 추가됐다. 반면 `input_tokens=2`, `cache_read_input_tokens=13,740`, `output_tokens=3`은 동일해 출력 문구가 아니라 장수 조회 세션 재사용이 증가분의 원인임을 확인했다.
- 구현 결정: 60초 조회 PTY 자체를 매분 재시작하면 불필요한 프로세스 기동과 파일 증가가 생기므로, 모델 턴이 필요한 순간에만 동일 계정 환경의 새 임시 PTY를 생성하는 방식으로 범위를 최소화했다. 기존 조회 PTY에는 유지 프롬프트를 보내지 않으며, 임시 PTY가 준비된 뒤 고정 프롬프트와 Enter를 보내고 작업중 또는 정확한 `1` 응답을 확인한 다음 즉시 종료한다. 다음 창도 항상 새 세션이므로 이전 `/usage`와 유지 턴 컨텍스트가 입력에 포함되지 않는다.
- 문서·테스트 반영: README와 CODETREE의 '같은 조회 PTY' 설명을 임시 PTY 수명주기로 정정하고, 단위 테스트가 기존 조회 터미널 미사용·새 PTY 생성·프롬프트 분리 입력·응답 후 종료를 검증하도록 갱신했다. 집중 테스트 6개와 TypeScript 타입 검사를 통과했으며, 실제 Claude 턴을 추가로 소모하는 운영 실험은 하지 않았다.
- 전체 검증·운영 반영: `npm run verify`에서 66개 파일 464개 테스트, 타입 검사, 프로덕션 클라이언트·서버 빌드와 공개 경로 검사가 모두 통과했다. 커밋 `75d8264`를 만든 뒤 pidfile과 실제 프로덕션 진입점이 일치하는 기존 서버 자식 PID `3404023`만 SIGTERM으로 정상 종료했고, supervisor가 `exit=0`을 기록한 다음 새 빌드를 PID `3665619`로 재기동했다. `/health`는 `{"ok":true}`를 반환했고 서버 supervisor와 채팅 `#209`, `#210` tmux가 유지됐으며, 재기동 중 추가 keepalive는 발생하지 않았다.

## 2026-08-12 Codex - 채팅 메시지 토큰 사용량 표시 착수

- 작업 시작: 사용자가 채팅창에 토큰 사용량을 작게 표시해 달라고 요청했다. Codex가 공통 기록 메시지 타입, Claude·Codex JSONL 파서와 API 직렬화, React 채팅 말풍선 렌더링을 다시 확인해 공급자가 실제로 기록한 usage 숫자만 노출한다.
- 구현 원칙: assistant 응답별 usage가 있는 경우에만 입력·캐시 생성·캐시 읽기·출력과 합계를 작은 보조 텍스트로 표시한다. 값이 없는 과거 기록이나 공급자 이벤트는 기존 화면을 유지하고, 사용량을 추정하거나 중복 합산하지 않는다. 두 공급자의 JSONL 구조 차이는 각 어댑터 파서에서 공통 타입으로 정규화한다.
- 구현 완료: `HistoryMessage.tokenUsage` 공통 타입을 추가했다. Claude assistant의 `message.usage`는 입력·캐시 생성·캐시 읽기·출력과 이들의 실제 합계로 변환하고, Codex의 별도 `token_count.info.last_token_usage`는 직전 현재 턴 assistant 메시지에 입력·캐시·캐시 쓰기·출력·추론·공급자 합계로 연결한다. Codex 캐시·추론은 각각 입력·출력의 부분집합이므로 합계를 다시 더하지 않고 JSONL `total_tokens`를 사용한다.
- 화면 결정: `MessageCard`가 usage가 있는 응답 아래에 `토큰 20.3K · 입력 2 · 캐시 생성 6.5K · 캐시 읽기 13.7K · 출력 3` 형태의 작은 monospace 보조 줄을 표시한다. Codex는 `캐시`·`추론` 항목도 값이 있을 때만 표시한다. 현재 채팅 API가 커서 페이지네이션을 쓰므로 불완전한 로드 구간을 전체 세션 누계로 오해시키지 않게 응답별 사용량만 표시한다.
- 문서·검증 완료: README와 CODETREE에 공급자별 파싱 소스와 채팅 표시 원칙을 반영했다. Claude·Codex 파서와 표시 포맷 집중 테스트 85개를 통과했고, 최종 `npm run verify`에서 67개 파일 469개 테스트, 타입 검사, 프로덕션 클라이언트·서버 빌드와 공개 경로 검사가 모두 통과했다.
- 운영 반영: 기능 커밋 `3749d34` 생성 후 pidfile과 프로덕션 진입점이 일치하는 서버 자식 PID `3665619`만 SIGTERM으로 정상 종료했다. supervisor가 최신 빌드를 PID `3672882`로 재기동했고 `/health`는 `{"ok":true}`를 반환했으며, 서버 supervisor와 채팅 `#209`, `#210` tmux는 그대로 유지됐다.

## 2026-08-12 Codex - 채팅 토큰 수치 의미 재확인

- 확인 요청·결론: 사용자가 채팅에 표시된 토큰이 누적인 것 같다고 관찰해 Codex가 현재 파서와 표시 코드를 다시 확인했다. 화면은 세션의 과거 응답을 모두 더한 누계가 아니라 각 assistant 모델 호출의 JSONL usage를 표시하지만, 그 호출의 입력에는 그 시점까지의 대화 컨텍스트가 다시 포함되므로 입력·캐시 수치는 대화가 길수록 누적처럼 커진다. 출력은 해당 응답에서 새로 생성한 양이고, Codex `cached_input_tokens`·`reasoning_output_tokens`는 각각 입력·출력의 부분집합이라 총량에 별도로 더하지 않는다. 제품 코드는 변경하지 않았다.

## 2026-08-12 Codex - Claude·Codex 채팅 토큰 항목 설명 착수

- 작업 시작: 사용자가 채팅 말풍선에 표시되는 Claude와 Codex의 토큰 항목을 각각 설명해 달라고 요청했다. Codex가 현재 파서·표시 코드를 다시 읽고, OpenAI `openai-docs` 지침에 따라 Codex 공식 매뉴얼과 필요한 공식 문서를 확인하며 Claude는 Anthropic 공식 문서로 교차검증한다.
- 설명 원칙: `총 토큰`, `입력`, `캐시`, `캐시 생성`, `캐시 읽기`, `출력`, `추론`이 총량에 포함되는 관계와 이번 호출/세션 누계 차이를 공급자별로 구분한다. 공식 문서에 공개되지 않은 CLI JSONL 내부 필드는 현재 설치본에서 실측한 구조임을 명시하고, 제품 코드는 변경하지 않는다.
- 확인 완료: 최신 Codex 공식 매뉴얼의 per-turn `total/input/cached_input/output/reasoning_output` 구분과 JSONL 예시, Anthropic Messages API·prompt caching 공식 문서의 usage 정의를 현재 WAM 파서와 대조했다. Claude 총 입력은 `input + cache_creation + cache_read`이고 출력까지 더한 값이 WAM의 `토큰`이며, Codex `cached_input`은 input의 부분집합이고 `reasoning_output`은 output의 부분집합이라 WAM은 공급자 `total_tokens`를 그대로 사용한다.
- 설명상 주의점: 두 공급자 모두 말풍선 수치는 세션 전체 단순 누계나 대시보드 퍼센트 차감량이 아니다. Claude는 assistant API 호출별 값이고, Codex WAM은 공개 `turn.completed`가 아니라 현재 CLI rollout의 내부 `token_count.info.last_token_usage`를 직전 assistant에 연결하므로 도구를 여러 번 쓴 복합 턴에서는 최종 말풍선 숫자가 전체 작업 합계가 아니라 해당 마지막 모델 호출량일 수 있음을 명시한다. 제품 코드는 변경하지 않았다.

## 2026-08-12 Codex - 삭제 채팅 포함 토큰 사용량 통계 착수

- 작업 시작: 사용자가 삭제한 채팅까지 포함해 토큰 사용량을 프로젝트별·채팅별·일자별 등으로 확인할 수 있게 해 달라고 요청했다. Codex가 현재 DB 스키마, JSONL 기록 동기화, 세션 백업·원본 삭제 경로, 사용량 라우트와 대시보드 구조를 다시 확인한다.
- 설계 방향: 살아 있는 JSONL을 매번 집계하면 원본 삭제 순간 과거 사용량을 잃으므로, assistant message ID를 멱등 키로 하는 영구 토큰 사용 이벤트 원장을 SQLite에 추가한다. 현재 채팅은 기록 동기화에서 계속 upsert하고, 백업·삭제 직전에도 다시 보존하며, 서버 시작 시 기존 현재 채팅과 백업 JSONL을 역수집한다. 프로젝트·채팅 이름과 ID를 스냅샷으로 남겨 원본 row가 삭제돼도 통계 차원을 유지한다.
- 조회 원칙: 기간 필터와 프로젝트·채팅·일자·공급자·계정별 그룹을 지원하고 총 토큰뿐 아니라 입력·캐시·캐시 생성/읽기·출력·추론을 함께 합산한다. 캐시·추론은 공급자에 따라 입력·출력의 부분집합이므로 `total_tokens`와 별개 항목으로 보여주고 다시 합산하지 않는다. 기존 백업 없이 이미 완전 삭제돼 원본 JSONL도 없는 채팅은 복구할 근거가 없다는 한계를 구분한다.
- 구현 완료: `token_usage_events`와 `TokenUsageLedger`를 추가해 공급자·세션·assistant 메시지 ID를 복합 안정 키로 숫자형 usage와 프로젝트·채팅·계정·모델 스냅샷을 멱등 upsert한다. `HistorySynchronizer`가 변경 JSONL을 동기화할 때 계속 갱신하고, `SessionBackupService`는 백업·백업 없는 삭제 직전에 최신 JSONL을 다시 보존한 뒤 삭제 표시만 남긴다. 복원 시에는 같은 이벤트를 새 현재 채팅 정보로 되돌리며 백업 사본 삭제는 원장을 건드리지 않는다.
- 기존 자료 처리: 서버 시작 때 `session-backups/*/session.jsonl`을 다시 파싱해 원장 도입 전에 백업 후 삭제된 채팅도 역수집한다. 옛 백업에 새 계정·프로젝트 ID 필드가 없으면 프로젝트 경로와 공급자 기본 계정으로 기존 DB row를 찾아 보완하고, 손상된 백업 하나는 서버 시작을 막지 않게 격리한다. 원장 도입 전에 백업 없이 JSONL까지 완전 삭제된 채팅은 근거가 없어 복원할 수 없다는 한계를 화면에 명시했다.
- API·화면 완료: 인증된 `/api/token-usage`가 최근 7·30·90일·1년·전체 기간과 프로젝트·채팅·일자·공급자·계정·모델 그룹을 허용 목록으로 검증해 합산한다. 대시보드에 전체·입력·캐시·출력·추론 요약, 응답·채팅·프로젝트 수와 분류 표를 추가했고 채팅별 화면에는 삭제된 항목 배지를 표시한다. 일자 경계는 브라우저가 보낸 시간대 오프셋을 적용한다.
- 문서·검증 완료: README와 CODETREE에 메시지 본문은 계속 DB에 복제하지 않고 숫자형 usage만 영구 보존하는 경계, 관련 서비스·라우트·화면·테스트 역할을 반영했다. `npm run verify`에서 68개 파일 471개 테스트, TypeScript 타입 검사, 프로덕션 클라이언트·서버 빌드와 공개 경로 검사가 모두 통과했다.
- 운영 반영: 기능 커밋 `c0376b6` 생성 후 pidfile과 실제 프로덕션 진입점이 일치하는 기존 서버 자식 PID `3672882`만 SIGTERM으로 정상 종료했다. supervisor가 `exit=0`을 기록한 뒤 새 빌드를 PID `3686852`로 재기동했고 `/health`와 supervisor 헬스 확인이 통과했으며 채팅 `#209`, `#210` tmux는 유지됐다. 시작 역수집 결과 메시지 본문을 조회하지 않고 숫자만 확인했을 때 토큰 이벤트 21,041개·세션 65개가 원장에 들어왔고, 그중 삭제 채팅은 Claude 23개·Codex 19개였다.

## 2026-08-12 Claude - 노트북 화면 세로 공간 압축 착수

- 작업 시작: 사용자가 노트북에서 채팅 화면이 너무 꽉 차 답답하다고 스크린샷과 함께 지적했다. Claude가 `styles.css`의 브레이크포인트를 확인한 결과 `1100px`·`700px` 두 개뿐이라 폭 1400px대 노트북이 대형 데스크톱과 완전히 동일한 고정 치수를 사용하고 있었다.
- 원인 진단: 채팅 화면에 세로로 쌓이는 고정 크롬이 header 64 + project-bar 52 + main 상하 여백 44 + workspace-head 66 + 화면 모드 탭 46 + 2줄로 접힌 model-bar 78 + composer 78로 약 430px이다. 노트북 뷰포트 세로가 700~800px이므로 실제 대화 영역이 전체의 40% 수준으로 줄어든다. 여기에 `.chat-layout`의 `min-height: 560px`이 남아 있어 짧은 화면에서는 문서 자체에 스크롤까지 생길 수 있었다.
- 결정한 방향: 폭이 아니라 세로가 병목이므로 `@media (min-width: 701px) and (max-height: 900px)` 조건으로 노트북 구간을 잡는다. 모바일 규칙(700px 이하)과 겹치지 않게 `min-width`를 함께 건다.
- 적용 범위(사용자가 3개 항목 전부 진행 승인): (1) header·project-bar·main 상하 여백·workspace-head·화면 모드 탭·composer의 세로 치수 압축, (2) 이미 모바일용으로 존재하는 `.model-bar-summary` 요약 + `자세히 ▾` 펼치기 패턴을 노트북에도 재사용해 2줄짜리 model-bar를 1줄로 축소(중복 구현을 만들지 않기 위해 새 컴포넌트나 상태를 추가하지 않고 CSS 표시 규칙만 재사용), (3) 채팅 사이드바 280→240px으로 가로 여유 확보.
- 가로 여백 판단: `.main`의 좌우 24px는 줄이지 않는다. 좌우까지 좁히면 오히려 더 꽉 차 보이므로 세로 여백만 줄이고 가로 여유는 사이드바 축소로 확보한다.
- 검증 계획: Playwright로 노트북 해상도(1440×900) 전후 스크린샷을 찍어 대화 영역 증가를 비교하고, `npm run verify`로 전체 테스트·타입 검사·빌드를 확인한다. 검증 서버는 운영 포트(14003)를 건드리지 않고 내가 띄운 PID만 정리한다.
- 구현 완료: `styles.css` 끝에 `@media (min-width: 701px) and (max-height: 900px)` 블록을 추가해 헤더 64→52, 프로젝트 바 52→44, `.main` 상하 여백 20/24→12/14, workspace-head 66→52, 모드 탭 46→37, 컴포저 여백을 압축하고 채팅 사이드바를 280→240px, 채팅 항목을 64→56px로 줄였다. `.chat-layout`의 `min-height: 560px`은 짧은 화면에서 문서 스크롤을 만들 수 있어 0으로 되돌렸다.
- 중복 방지 결정: model-bar 접기는 새 컴포넌트·상태를 만들지 않고 모바일용으로 이미 있는 `.model-bar-summary`(마크업과 `modelBarExpanded` 상태가 이미 공용)를 그대로 재사용했다. 다만 그 시각 스타일이 `≤700px` 미디어쿼리 안에만 있어 노트북에서 쓸 수 없었으므로, 스타일을 미디어쿼리 밖 기본 규칙으로 올리고 각 구간에서는 `display`만 바꾸도록 정리했다. React 코드는 변경하지 않았다.
- 가로 여백 결정: `.main`의 좌우 24px는 유지했다. 좌우까지 좁히면 오히려 더 꽉 차 보이므로 세로만 압축하고 가로 여유는 사이드바 축소로 확보했다.
- 검증 결과: 운영 서버(14003)와 `dist`를 건드리지 않도록 별도 포트(4399) vite dev 서버를 띄워 Playwright로 측정했다. 1440×1000에서 대화 영역 574px(고정 크롬 426px)이던 것이 1440×900에서 581px(고정 크롬 319px)로, 뷰포트가 100px 더 짧은데도 대화 영역이 넓어졌다(크롬 107px 압축). 문서 스크롤은 0이다. 사용자 실제 화면처럼 model-bar가 두 줄로 접히는 경우에는 절감폭이 더 크다.
- 검증 중 정정: 처음에는 앞 단계에서 2048×1114로 바꿔둔 뷰포트를 데스크톱 기준값으로 잘못 측정해 압축 효과가 없는 것처럼 보였다. 같은 폭(1440)에서 규칙 경계인 1000 대 900으로 비교하도록 기준을 바로잡아 확인했다.
- 회귀 방지: `tests/e2e/ui.spec.ts`의 채팅 테스트에 노트북 구간 검증을 추가했다. 요약바 표시·model-bar 접힘, 1440×1000 대비 대화 영역 증가, 문서 스크롤 0, '자세히 ▾'로 모델·권한 컨트롤이 다시 펼쳐지는지를 확인하고 `artifacts/ui-chat-laptop.png`를 남긴다.
- 문서·전체 검증 완료: README에 노트북 상태바 접힘과 세로 압축을, CODETREE에 노트북 브레이크포인트 근거와 요약바 스타일 배치 원칙, e2e 검증 항목을 반영했다. `npm run verify`에서 68개 파일 471개 테스트, 타입 검사, 프로덕션 빌드와 공개 경로 검사가 모두 통과했다. 검증용 dev 서버는 내가 띄운 PID만 종료했고 운영 서버 `/health`가 200인 것을 확인했다. 운영 반영(재기동)은 사용자 판단에 맡기고 이번 작업에서는 하지 않았다.

## 2026-08-12 Codex - #212 종료 후 WSS 어드민 웹 복구 착수

- 작업 시작: 사용자가 채팅 #212의 터미널을 종료한 직후 WSS 어드민 웹에 접속할 수 없다고 알렸다. Codex가 앞서 Claude Code의 `run_in_background` 프로세스가 터미널 종료 뒤에도 남을 가능성이 높다고 잘못 안내한 점을 정정하고, 14007·14008 리스너와 #212 tmux가 모두 사라진 것을 확인했다.
- 복구 결정: 채팅 터미널 수명주기와 다시 결합되지 않도록 백엔드(14007)와 프론트엔드(14008)를 별도의 이름 있는 tmux 세션으로 기동한다. 기존 프로젝트 코드와 `.env` 값은 변경하거나 노출하지 않고, #212에서 적용한 Vite 프록시 설정에 `VITE_API_PROXY_TARGET=http://127.0.0.1:14007`만 실행 환경으로 전달한다.
- 검증 계획: 두 tmux 세션·포트 리스너·백엔드 직접 응답·프론트 경유 `/api` 프록시 응답을 확인하고, 작업 완료 뒤 실제 PID와 종료 방법을 기록한다.
- 복구 완료: 백엔드는 `wss_admin_backend_14007`(pane PID 3769614), 프론트엔드는 `wss_admin_frontend_14008`(pane PID 3769620) tmux 세션으로 기동했다. 백엔드의 ts-node-dev 임시 파일은 `/tmp/package.json`의 ESM 선언을 상속하지 않도록 프로젝트 `node_modules/.cache/wss-tsdev`를 사용했다.
- 검증 완료: 백엔드 `GET 127.0.0.1:14007/api/novels`, 프론트 루트 `GET 127.0.0.1:14008/`, 프론트 프록시 `GET 127.0.0.1:14008/api/novels`가 모두 HTTP 200을 반환했다. 이 서버들은 web-agent-manager 채팅 tmux와 분리돼 채팅 터미널을 종료해도 유지되며, 필요할 때 `tmux kill-session -t wss_admin_backend_14007`과 `tmux kill-session -t wss_admin_frontend_14008`로 각각 종료한다.

## 2026-08-12 Claude - 노트북 요약바에 사용량·초기화 시각 표시 착수

- 작업 시작: 앞선 노트북 압축 작업으로 model-bar가 요약 한 줄로 접히면서, 사용자가 실제로 계속 보던 `주요 사용량 2% · 초기화 1:40pm (Asia/Seoul)` 정보가 펼치기 전에는 보이지 않게 됐다. 사용자가 사용량과 초기화 시각은 접힌 상태에서도 남아 있어야 한다고 요청했다.
- 현재 상태 확인: 요약바(`ChatView.tsx`)는 `{usedPercent}%`만 라벨 없이 표시하고 초기화 시각(`resetAt`)은 아예 넣지 않았다. 전체 model-bar에만 `주요 사용량 <b>N%</b> · 초기화 …`가 있다.
- 결정: 요약바 표기를 `사용량 N% · 초기화 …`로 바꿔 무슨 숫자인지 알 수 있게 한다. 요약바 마크업은 모바일과 공용이라 390px에서는 초기화 시각까지 넣으면 한 줄을 넘기므로, 초기화 부분만 별도 요소로 감싸 `≤700px`에서는 숨기고 노트북 이상에서만 보여준다(모바일은 기존처럼 퍼센트 중심 유지).
- 구현 완료: 요약바 표기를 `사용량 N%`로 바꾸고 `resetAt`이 있으면 `· 초기화 …`를 이어 붙였다. 초기화 부분만 `.summary-reset`으로 감싸 `≤700px`에서는 숨기고, 사용량 전체는 `.summary-usage { flex: none }`으로 고정해 모델명 ellipsis에 밀려 줄어들지 않게 했다.
- 검증 완료: 채팅 e2e mock의 `/api/usage`가 빈 배열이라 요약바에 사용량이 아예 나오지 않아 확인이 불가능했으므로, codex `weekly` 창(12%, `1:40pm (Asia/Seoul)`)을 넣었다. 노트북 1440×900에서 `사용량 12% · 초기화 1:40pm (Asia/Seoul)`이 접힌 상태로 보이는 것과, 390px에서는 `사용량 12%`만 남고 `.summary-reset`이 숨겨지며 요약 높이가 50px 미만(한 줄 유지)인 것을 검증했다.
- 문서·전체 검증: README와 CODETREE에 접힌 요약의 사용량·초기화 시각 유지와 모바일 예외를 반영했다. `npm run verify`에서 68개 파일 471개 테스트, 타입 검사, 프로덕션 빌드, 공개 경로 검사가 모두 통과했다. 검증용 dev 서버는 4399 LISTEN 소켓의 소유 PID를 역추적해 내가 띄운 프로세스만 종료했고(이름으로 프로세스를 찾지 않았다), 운영 서버 `/health` 200을 확인했다.

## 2026-08-12 Claude - 채팅 없는 워크트리도 채팅 목록에 표시 착수

- 작업 시작: 사용자가 WSS-admin-web에 새로 만든 워크트리(`/home/ubuntu/websoso/WSS-admin-web-admin-platform`, `feat/admin-security-observability-deploy`)가 채팅 목록에 안 뜬다고 지적했다. 확인 결과 `ChatView.tsx`의 `chatGroups`가 `chats` 배열만 순회해 `worktree_path` 기준으로 그룹을 역산하는 구조라, 그 워크트리를 쓰는 채팅이 0개면 그룹 자체가 생기지 않았다.
- 사용자 판단: 채팅이 없어도 워크트리는 목록에 보여야 한다. 워크트리 존재 여부와 채팅 존재 여부는 별개 이슈이며, "채팅이 사라졌으니 워크트리도 안 보인다"는 건 워크트리 정리 문제와 뒤섞여 혼란스럽다는 것이 근거다.
- 확인한 제약: 기존 `/chats/worktree` API는 브랜치만 받아 `switchBranch(mode:"worktree")`를 호출하고, 그 경로는 앱 관리 경로(`data/git-worktrees/<projectId>/<branch>`)만 본다. 밖에서 만든 외부 워크트리는 이 경로에 없으므로 빈 그룹에서 새 채팅을 시작하면 `git worktree add`가 "이미 다른 워크트리에 체크아웃됨"으로 실패한다.
- 결정한 방향: (1) 서버 `/chats/worktree`가 선택적으로 `worktreePath`를 받아, 값이 있으면 이미 존재하는 `attachWorktree`로 새 채팅을 그 폴더에 연결한다(새 유틸을 만들지 않고 기존 함수 재사용). (2) 클라이언트는 이미 있는 `GET /projects/:id/git/workspaces`로 워크트리 목록을 받아, 채팅이 없는 워크트리도 빈 그룹으로 만들어 목록에 표시하고 그 그룹의 `+ 채팅` 버튼이 `worktreePath`를 넘기게 한다. 공유 checkout(main)은 기존 "프로젝트 채팅" 그룹과 중복되므로 제외한다.
- 검증 계획: `npm run verify`와 Playwright로 빈 워크트리 그룹이 목록에 뜨는지 확인하고 스크린샷을 남긴다. 운영 서버(14003)는 건드리지 않고 내가 띄운 PID만 정리한다.
- 구현 완료: 서버 `/chats/worktree`에 선택적 `worktreePath`를 추가해, 값이 있으면 기존 `attachWorktree`로 이미 있는 폴더에 새 채팅을 붙이고 없으면 기존 `switchBranch` 경로를 그대로 쓴다(브랜치 없이 경로만 온 detached worktree도 폴더명으로 제목을 만든다). 클라이언트 `ChatView`는 `/projects/:id/git/workspaces`를 30초 주기로 읽어 채팅이 0개인 worktree도 빈 묶음으로 만들고, 그 묶음의 새 채팅 요청에 `worktreePath`를 함께 보낸다. 빈 묶음에는 `채팅 없는 작업공간입니다.` 안내(`title`에 실제 경로)를 둬 빈 자리가 오류로 보이지 않게 했다.
- 중복 방지 결정: 외부 worktree 연결용 함수를 새로 만들지 않고 `GitBranchControl`이 이미 쓰던 `attachWorktree`와 목록 API `listWorkspaces`를 그대로 재사용했다. 공유 checkout(main)은 기존 `프로젝트 채팅` 묶음과 같은 폴더라 빈 묶음 생성에서 제외했다.
- 회귀 방지: `tests/git-workspaces.test.ts`에 "외부 worktree가 쓰는 브랜치는 브랜치 지정만으로 붙일 수 없고 경로 지정으로 공유한다"를 추가해 라우트가 `worktreePath` 분기를 두는 근거를 못박았다. `tests/e2e/ui.spec.ts`의 worktree 테스트에는 워크트리 목록 mock을 넣어 채팅 0개 묶음의 개수·안내 표시와 새 채팅 요청 본문에 `worktreePath`가 실리는지를 검증한다.
- 검증 완료: `npm run verify`에서 68개 파일 472개 테스트, 타입 검사, 프로덕션 빌드, 공개 경로 검사가 모두 통과했다. 별도 포트(4399) vite dev 서버로 Playwright e2e 22개 전부 통과했고 `artifacts/ui-chat-branch-compact.png`에 `feature/agent 워크트리 0` 빈 묶음이 안내 문구와 함께 남았다. 검증 서버는 4399 리스너의 소유 PID를 `fuser`로 역추적해 내가 띄운 프로세스만 종료했고(이름으로 프로세스를 찾지 않았다) 운영 서버 `/health` 200을 확인했다.
- 남은 사항: 운영 서버(14003) 재기동은 사용자 판단에 맡겼다. 재기동 전까지 실제 화면에는 반영되지 않는다. 사용자가 지적한 `/home/ubuntu/websoso/WSS-admin-web-admin-platform`(`feat/admin-security-observability-deploy`)은 반영 후 `feat/admin-security-observability-deploy 워크트리 0` 묶음으로 나타난다.

## 2026-08-13 Codex - 다중 실행 조건 비교 실험 기능 기획 착수

- 작업 시작: 사용자가 하나의 명령을 동일 모델의 스킬 유무뿐 아니라 하네스·멀티 에이전트·루프 에이전트 등 미리 정의한 여러 실행 조건으로 반복 실행하고, 토큰 사용량과 결과 차이를 비교할 수 있는 기능을 기획해 달라고 요청했다. Codex가 현재 토큰 원장·채팅 실행·공급자 연동 구조를 재확인해 기존 기능과 중복되지 않는 제품 모델과 단계별 구현 범위를 설계한다.
- 기획 원칙: 비교의 최소 단위는 동일 입력과 평가 기준을 공유하는 실험이며, 각 실행 조건은 모델·추론 강도·지침·스킬·하네스·에이전트 구성·반복 정책을 명시적으로 고정한 변형으로 다룬다. 비용·토큰·시간 같은 계측값과 결과 품질 평가는 분리해 저장하고, 재현성·격리·동시 실행 제한·중단 가능성을 우선한다.
- 평가 원칙: 각 실행 결과는 복수의 평가 에이전트가 다른 결과의 조건과 가능하면 정체를 가린 채 독립적으로 점수·판단 근거·치명적 결함·선호 순위를 제출하게 한다. 사람의 최종 판단, 평가자 간 합의도와 이견, 평가 자체에 든 토큰·시간도 함께 보존해 단일 자동 점수를 진실처럼 취급하지 않는다.
- 작업 범위: 이번 단계에서는 기존 코드와 문서를 근거로 사용자 흐름, 도메인 모델, 실행·계측·평가 방식, UI 초안, 위험 요소와 단계별 로드맵을 제안한다. 실제 기능 코드나 DB 마이그레이션은 사용자의 후속 구현 결정 전에는 변경하지 않는다.
- 추적 등록: 기획과 후속 구현의 완료 조건을 GitHub 이슈 #24 `동일 명령을 스킬·하네스·에이전트 구성별로 실행하고 결과·비용·평가자 판단을 비교하는 실험 기능`으로 개별 등록했다. 기존 통합 작업 브랜치를 유지하고 보호 브랜치나 원격 머지는 건드리지 않았다.
- 기존 구조 확인: 메시지별 `TokenUsageLedger`는 입력·캐시·출력·추론 토큰을 안정 키로 영구 보존하지만 실험 실행 ID는 없고, `SessionManager`는 계정·worktree별 tmux 실행과 모델/추론 강도 적용을 제공하지만 모델 적용이 대화형 TUI 메뉴에 의존한다. `AgentBridge`는 새 자식 채팅·멱등 위임·완료 결과 회수를 제공하지만 실행 조건 고정, 반복, 블라인드 평가, 실행별 예산은 없다. 프로젝트/전역 스킬은 symlink로 자동 연결돼 현재 경로를 그대로 공유한 채 스킬 없음 조건을 만들면 다른 채팅에 영향을 줄 수 있다.
- 제품 모델 결정: 동일 입력·공통 평가 기준을 가진 `experiment` 아래 사전 설정 가능한 `variant`를 두고, 변형의 스냅샷에서 반복별 `run`을 만든다. 변형은 공급자·계정·모델·추론 강도·권한·지침·스킬 집합·MCP/도구 집합·하네스 유형(single/multi/loop)·에이전트 그래프·중단 조건·예산을 명시한다. 실행은 별도 채팅과 격리된 worktree/설정 overlay를 사용하고 최종 응답·변경 diff·테스트 결과·도구 호출·실행 시간·토큰을 산출물로 고정한다.
- 평가 모델 결정: 결정적 검사(테스트·lint·파일/문자열 규칙), 결과별 독립 rubric 점수, 결과 간 pairwise 선호를 분리한다. 복수 평가 에이전트에는 변형 이름·모델·토큰을 가린 익명 산출물을 무작위 순서로 주며, 같은 비교의 A/B 순서를 바꾼 재판정으로 위치 편향을 표시한다. 각 판단은 평가자·모델·기준별 점수, 신뢰도, 근거, 치명적 결함, 선호와 평가 토큰·시간을 원문 그대로 남기고 합의율·이견·사람의 최종 판정은 별도 필드로 보존한다.
- 구현 경계 결정: 기존 토큰 이벤트를 다시 계산하지 않고 `run_id` 귀속 또는 실행 시작/종료 메시지 키 범위로 연결하는 실험 집계 계층을 추가한다. 일반 채팅 `delegations`는 실행 하위 작업 추적에 재사용할 수 있지만 실험/평가의 원장으로 쓰지 않는다. 스킬·MCP 조건은 실제 사용자 전역 디렉터리를 변경하지 않고 실행 전용 공급자 설정 overlay와 읽기 전용 스킬 manifest를 만들어 격리한다. 동일 저장소 쓰기 실행은 독립 worktree를 기본으로 하고 공정한 비교를 위해 각 반복을 같은 기준 커밋에서 시작한다.
- 단계 결정: 내부 기반 검증은 single 하네스의 스킬 on/off, 모델·추론 강도, 반복 실행, 토큰·시간·diff·테스트 비교부터 만든다. 그러나 사용자용 MVP 완료 조건에는 single·orchestrator-worker 멀티 에이전트·evaluator-optimizer 루프의 세 내장 하네스, 실행별 예산/중단 정책, 복수 자동 평가자의 블라인드 rubric/pairwise 판단과 합의/이견 UI를 모두 포함한다. 이후 실험 템플릿·회귀 기준선·통계적 비교와 내보내기로 확장한다.
- 기획 완료: 결과 화면은 승자 하나만 강조하지 않고 변형별 성공률·품질 중앙값·토큰/시간 중앙값과 분산·반복 수, 품질 대비 토큰 효율, 평가자별 판단 근거와 불일치를 함께 보여주도록 제안한다. 자동 평가 결과는 사람의 최종 선택을 덮어쓰지 않으며, 승인 대기·사용량 한도·실행 실패·예산 초과는 서로 다른 종료 사유로 남긴다. 이번 단계에는 기능 코드·DB·README·CODETREE 변경이나 실행 테스트가 없고 `history.md`와 이슈만 갱신했다.

## 2026-08-13 Codex - 최신 에이전트 하네스·루프·훅 구현 가능성 검토 착수

- 작업 시작: 사용자가 앞서 기획한 비교 실험 안에서 하네스, 반복형 에이전트, 훅 등 현재 널리 쓰이는 에이전트 기술을 web-agent-manager 자체에 구현할 수 있는지 물었다. Codex가 현재 세션 관리자·공급자 어댑터·위임 브리지·승인·토큰 원장 구조와 최신 공식 Codex/Claude 확장 지점을 다시 대조해 구현 가능 범위와 구조적 한계를 판단한다.
- 검토 원칙: 특정 공급자의 CLI 화면 문구에 종속된 기능과 web-agent-manager가 공급자 중립적으로 소유해야 할 오케스트레이션 기능을 분리한다. 가능 여부만 낙관적으로 답하지 않고, 기존 구조 재사용 범위, 필요한 신규 추상화, 운영 위험, 단계별 우선순위를 함께 제시한다. 이번 단계는 타당성 검토이며 기능 코드는 변경하지 않는다.
- 공식 기능 확인: 최신 Codex는 생명주기 command hooks, 기본 활성화된 subagent, 사용자 정의 agent, `codex exec --json`, App Server, TypeScript/Python SDK를 제공한다. Claude Code는 command·HTTP·MCP·prompt·agent hooks, subagent, 실험적 agent teams, `claude -p --output-format stream-json`, Agent SDK를 제공한다. 양쪽 모두 구조화된 비대화형 실행이 가능하므로 실험 실행기를 TUI 화면 파싱 위에만 만들 필요는 없다.
- 현재 구조 판단: WAM의 `SessionManager`와 `ProviderAdapter`는 인터랙티브 채팅에 최적화돼 모델 변경·승인·완료 일부를 TUI 문구와 키 입력으로 처리한다. 반면 `AgentBridge`의 멱등 위임·깊이 제한·완료 회수, worktree 격리, 승인 서비스, 토큰 원장과 realtime 방송은 새 오케스트레이터에서 재사용할 수 있다. 현재 공통 hook event bus, 실행 그래프, 체크포인트, 실행별 예산·재시도·무진전 판정은 없다.
- 아키텍처 결정: `AgentRuntime`이 공급자별 SDK/구조화 CLI 이벤트를 공통 실행 이벤트로 변환하고, `HarnessEngine`이 single·router·parallel fan-out/fan-in·orchestrator-worker·debate/consensus 그래프를 수행하게 한다. `LoopController`는 evaluator-optimizer·reflection·test-fix 반복을 최대 횟수, 토큰/시간 예산, 동일 실패 반복, 최소 개선폭, 사람 승인 조건으로 종료한다. `HookBus`는 실행 전후 정책·감사·컨텍스트 주입만 담당하고 루프 상태의 원천으로 쓰지 않는다.
- 공급자 경계 결정: 첫 버전은 WAM 소유 그래프로 Codex와 Claude를 동일하게 조정한다. Codex native subagent와 Claude agent teams는 별도 `native_multi_agent` 프로필로 노출해 공급자 자체 하네스의 효과를 비교하되, Claude agent teams는 실험 기능이고 팀원이 같은 worktree를 공유하므로 기본 실행기로 삼지 않는다. 공급자 native hook은 있는 그대로 수집·표시하고, WAM hook은 `before_run`, `before_node`, `before_tool`, `after_tool`, `after_node`, `before_evaluate`, `after_evaluate`, `on_budget`, `on_error`, `after_run`의 안정 이벤트로 별도 제공한다.
- 최신 기술 적용 범위: 스킬·MCP/도구 레지스트리, 역할 기반 subagent·handoff, 병렬 실행, 상태 그래프·체크포인트/재개, human-in-the-loop 승인, 정책/guardrail hooks, 메모리·컨텍스트 주입, tracing·record/replay, evaluator/judge·합의/이견, 예산·circuit breaker, 실험 반복과 회귀 비교는 모두 구현 가능하다. 장기 기억은 원문 대화를 무제한 주입하지 않고 출처·만료·범위를 가진 명시적 memory artifact로 제한한다.
- 우선순위 결정: 1차 기반은 구조화 `AgentRuntime`·공통 이벤트·run/checkpoint 원장·취소/예산이다. 2차는 WAM hooks와 single/parallel/router harness, 3차는 evaluator-optimizer loop와 복수 평가자, 4차는 native subagent/agent-team 비교 및 사용자 정의 그래프다. 이 순서 없이 시각적 그래프 편집기나 native team부터 붙이면 현재 TUI 파싱 복잡도 위에 또 다른 공급자 종속 상태가 쌓인다.
- 검토 완료: 기술적으로 구현 가능하고 현재 프로젝트의 worktree·위임·토큰 기반도 유리하다. 가장 큰 선행 과제는 기존 인터랙티브 TUI 경로를 유지하면서 실험용 구조화 실행 경로를 병렬 추가하는 이중 런타임 구조, 이벤트 정규화, 공급자 설정/스킬 격리다. 이번 검토에서는 기능 코드·README·CODETREE를 변경하지 않았고 `history.md`만 갱신했다.

## 2026-08-13 Codex - v0.4.0 선행 릴리즈와 실험실 전체 구현 착수

- 작업 시작: 사용자가 실험실과 최신 에이전트 기술 구현 전에 현재 프로젝트의 마지막 릴리즈를 확인하고, 누적 변경을 새 버전으로 정리한 다음 개요·구현 계획·레퍼런스·한계·실제 구현 범위를 파일로 남겨 전체 구현을 시작하도록 요청했다. Codex가 공개/비공개 원격의 릴리즈 상태와 v0.3.0 이후 차이를 확인한 뒤 릴리즈 준비, 설계 문서, 런타임·하네스·훅·평가·UI 구현을 순서대로 진행한다.
- 릴리즈 기준 확인: 실제 배포 기준인 공개 `ljy1348/web-agent-manager`의 최신 정식 릴리즈는 `v0.3.0`이며 15개 설치·체크섬·SBOM 자산이 게시돼 있다. 현재 브랜치는 v0.3.0 이후 277개 커밋, 118개 파일, 약 7,818줄 추가로 Android 앱·토큰 원장·사용량 안정화·GitHub 조회 성능·워크트리 UX 등을 포함하므로 새 minor 버전 `0.4.0`으로 올린다.
- 저장소별 배포 결정: 사용자의 추가 판단에 따라 private 저장소는 GitHub Release나 설치 자산을 만들지 않되 `package.json`·lockfile 등 소스 버전은 공개 저장소와 동일한 `0.4.0`으로 갱신한다. 멀티플랫폼 설치 파일·체크섬·SBOM·Docker 태그와 GitHub Release는 공개 저장소에서만 생성한다.
- 이슈 분리: 릴리즈 #26, 설계 문서 #25, 구조화 런타임 #27, HookBus #31, 하네스·루프 #28, 복수 평가 #29, 실험실 API·UI #30으로 개별 등록했다. 전체는 기존 상위 이슈 #24 아래 하나의 통합 작업 브랜치에서 순차 구현한다.
- 보호 브랜치·머지 원칙: private/public 보호 브랜치에는 직접 작업하지 않고 통합 기능 브랜치에서 검증·커밋한다. 공개 릴리즈 PR은 Codex가 생성·상태 확인까지만 하며 직접 머지하지 않는다. 사용자가 GitHub에서 병합한 뒤에만 공개 태그·릴리즈 워크플로 결과를 검증하고, 작업 브랜치는 삭제하지 않는다.
- v0.4.0 버전·문서 준비: `npm version 0.4.0 --no-git-tag-version`으로 package와 lockfile 버전을 함께 올리고 CHANGELOG에 Android 앱/기기 인증·토큰 원장·사용량 최소 턴/초기화권·채팅 제출·GitHub 캐시/페이징·빈 worktree UX를 정리했다. README의 압축 파일명을 v0.4.0으로 맞추고 Android 서명 APK는 별도 signing 환경이 필요해 공개 Release 자동 자산에서 제외한다는 경계를 추가했으며 CODETREE의 릴리즈 역할도 동일하게 갱신했다.
- 웹·서버 검증: 깨끗한 `npm ci` 후 전체·고위험 감사가 모두 취약점 0건이었고 `npm run verify`에서 타입 검사, 68개 파일 472개 테스트, production 클라이언트·서버 빌드와 공개 절대경로 검사가 통과했다.
- Android 검증 제약: 현재 호스트에는 Android SDK 경로·`adb`·API 플랫폼 디렉터리가 남아 있지 않아 Gradle이 `SDK location not found`로 Java 컴파일 전에 중단됐다. 코드·lint 실패가 아니며 현재 Android 변경은 앞선 API 36 debug/release·lint 검증 이후 추가 수정되지 않았다. 저장소에 `local.properties`나 비밀 signing 설정을 만들지 않았고, 공개 PR/릴리즈에서는 서명되지 않은 APK를 자산으로 게시하지 않는다.
- 로컬 패키징 검증: v0.4.0 간편 ZIP 4종·CycloneDX SBOM·SHA256SUMS를 생성했고 모든 체크섬이 실제 파일과 일치했다. SBOM 최상위와 Linux ZIP 내부 package의 이름·버전이 `web-agent-manager 0.4.0`임을 확인했다. 이후 README가 갱신됐으므로 최종 커밋 전 패키징을 다시 생성해 문서까지 최신으로 맞춘다.
- 최종 릴리즈 준비 검증: README 갱신 뒤 `npm run verify && npm run release:package`를 다시 실행해 타입 검사, 68개 파일 472개 테스트, production 빌드와 ZIP 4종·SBOM·체크섬 생성을 모두 통과했다. 최종 `SHA256SUMS`의 5개 파일이 전부 일치하고 SBOM 최상위 버전, Linux ZIP 내부 `package.json`, 압축에 포함된 README의 배포 버전이 모두 `0.4.0`으로 맞는 것을 재확인했다.
- 공개 브랜치 분리 결정: private 개발 브랜치와 공개 `main`은 공개 정리 과정에서 서로 독립된 Git 이력을 사용해 공통 조상이 없다. private 브랜치를 공개 원격에 그대로 올리지 않고 공개 `main`에서 별도 `release/v0.4.0-public` 브랜치를 만들어 검증된 최종 트리 차이만 옮기며, 공개 PR 병합은 사용자에게 맡긴다.
- 사용자 추가 운영 규칙: 이후 작업 단위마다 Claude의 5시간 `Current session` 사용량을 구조화 `/usage` 검증으로 확인하고 80% 이하일 때 Claude에도 독립 구현·검토를 맡긴다. 토큰·인증 값은 읽거나 출력하지 않고 사용률만 판단하며, 같은 작업 파일을 동시에 수정하지 않도록 역할을 분리한다.
- Claude 위임 착수: 현재 Claude 5시간 세션 사용량은 0%(주간 32%)로 조건을 충족한다. Codex가 공개 브랜치 생성과 정확한 트리 이전을 담당하는 동안 Claude는 공개 v0.4.0 후보의 민감정보 노출·private 전용 설정·릴리즈 누락·검증 위험을 읽기 전용으로 독립 감사하고 근거 파일과 권고를 보고한다.
- supervisor 이식성 수정 착수: Codex의 공개 차이 사전 점검에서 `scripts/run-server-supervised.sh`가 기본 앱 경로를 `/home/ubuntu/myagent`, Node를 `/root/.volta/...`로 고정해 다른 설치 경로에서 실패하는 릴리즈 차단 위험을 발견했다. private #32와 public #34로 분리 등록하고, 기본 앱 경로는 스크립트 위치의 상위 폴더에서 계산하며 Node는 `WAM_NODE_BIN`을 우선하되 미지정 시 현재 `PATH`에서 실제 실행 파일을 찾도록 최소 수정한다. 경로 공백·고정 호스트 문자열 회귀 테스트와 README·CODETREE 갱신 후 전체 검증한다.
- Claude 감사 완료·반영 결정: 자식 채팅 #220은 공개 차이 117개 파일을 독립 검토해 private 운영 경로가 포함된 `history.md` 신규 531줄을 공개 후보에서 제외해야 한다고 판정했고, Codex의 supervisor 이식성 수정도 확인했다. 추가 중요 결함으로 무인증 모바일 기기 로그인 부트스트랩 요청 제한 부재와 Codex app-server `clientInfo.version`의 `0.3.0` 고정을 발견했으며, Android 버전·wrapper 검증, ZIP README 자산, Playwright CI 누락을 함께 보고했다. Codex가 현재 파일·테스트와 대조해 실제 문제임을 확인하고 private #33~#36, public #35~#38로 개별 등록했다.
- 요청 제한 구현 결정: 새 제한기를 만들지 않고 로그인 API가 이미 사용하는 `LoginRateLimiter`를 challenge/activate 라우트가 공유한다. 클라이언트 주소 전체 창과 `deviceId`별 실패 잠금을 적용하고 429·`Retry-After`·중복 억제 감사 로그를 남기며, 성공 시 device 실패 기록만 초기화해 IP 전체 요청량은 우회되지 않게 한다.
- 버전 동기화 결정: Codex app-server 초기화 버전은 별도 상수를 또 하드코딩하지 않고 실행 루트의 `package.json`을 읽어 사용한다. package/lockfile과 실제 전송값을 테스트에서 대조해 다음 릴리즈에서 하나만 바뀌는 회귀를 막는다.
- 릴리즈 보완 완료: supervisor 기본 경로 이식성, 모바일 기기 로그인 요청 제한, Codex app-server package 버전 동기화, Android 0.4.0 버전·공식 Gradle 체크섬, 자체 기동 Playwright와 Android CI, 릴리즈 ZIP의 전체 화면 자산 포함을 구현했다. 타입 검사와 관련 회귀 20개, 전체 Vitest 473개, production 빌드·공개 경로 검사, Playwright 23개 시나리오, Gradle 9.5.0 wrapper 기동이 통과했다. Android API 36 실제 컴파일은 현재 호스트 SDK 부재로 공개 CI에서 수행하도록 남겼다.
- 사용자 최종 릴리즈 결정: v0.4.0은 중간 단계이므로 package·Android 버전과 소스 변경을 기능 브랜치에 커밋하는 데서 마친다. 공개 PR·태그·GitHub Release·Docker/설치 자산 배포와 private Release는 모두 생략하고, 별도 릴리즈 대기 없이 실험실 설계·구현으로 바로 넘어간다.

## 2026-08-13 Codex·Claude - 에이전트 실험실 명세 문서 작성 착수

- 작업 시작: v0.4.0 소스 커밋을 완료하고 공개 배포는 생략한다는 사용자 결정에 따라, 실험실의 개요·구현 계획·공식 레퍼런스·한계·이번 실제 구현 범위를 `docs/agent-lab.md`에 먼저 고정한 뒤 코드 구현으로 진행한다.
- 역할 분담: 새 작업 단위에서 Claude 5시간 `Current session` 사용량은 12%로 사용자 기준 80% 이하이다. Codex는 현재 DB·세션·위임·토큰 원장 구조와 공식 Codex 매뉴얼을 바탕으로 주 명세를 작성하고, Claude에는 공급자 중립 도메인 모델, 평가자 편향, 실행 격리, Claude 구조화 실행 경계를 읽기 전용으로 독립 검토하도록 맡긴다.
- 문서 원칙: 기능 이름만 나열하지 않고 실험/변형/반복 실행/노드/이벤트/평가/사람 판정의 저장 계약, single·multi-agent·loop 하네스의 상태 전이, HookBus 이벤트와 실패 정책, 토큰·시간·품질 비교 지표, 단계별 구현 파일과 수용 조건을 명시한다. 공급자 native 기능은 비교 프로필로 취급하고 WAM 공통 원장을 상태의 기준으로 둔다.
- Claude 검토 전달 실패: 사용량 조건을 충족해 자식 채팅 위임을 시도했으나 WAM MCP와 CLI fallback이 모두 `read ECONNRESET`으로 종료돼 작업 자체가 전달되지 않았다. 인증 정보나 설정 파일은 확인하지 않았고, 연결이 복구된 이후 구현 단계의 독립 검토 단위에서 다시 맡긴다. 이번 명세에는 완료되지 않은 Claude 검토 결과를 반영한 것처럼 기록하지 않는다.
- 명세 완료: `docs/agent-lab.md`에 제품 목표와 MVP, 실행 격리·재현성 원칙, 실험/변형/run/node/event/checkpoint/artifact/evaluation/judgment/human verdict 모델, 상태·종료 이유, 공통 `AgentRuntime`, 공급자별 구조화 CLI 경계, 스킬 on/off 방식, 세 하네스, HookBus, 블라인드 복수 심사와 쌍대 순서 교차, 비용·품질 지표, API·UI, 단계별 예정 파일, 실제 구현 범위, 한계와 검증 전략을 확정했다.
- 구현 경계 결정: 기존 대화형 `SessionManager`·일반 자식 채팅 `AgentBridge`·채팅 JSONL용 `TokenUsageLedger`는 그대로 유지한다. 실험은 별도 비대화형 `AgentRuntime`과 append-only 실행 원장을 사용하고, MVP는 공식 CLI 자식 프로세스로 시작해 새로운 SDK 의존성을 만들지 않는다. Claude 스킬-off는 다른 기능까지 함께 끄는 safe mode가 아니라 `--disable-slash-commands`, Codex는 전역 설정을 바꾸지 않는 실행별 `skills.config` overlay를 사용한다.
- 평가 결정: 결정적 검증과 LLM 심사를 분리하고, 심사 입력에서 변형·공급자·모델을 숨긴다. A/B와 B/A를 모두 평가해 위치 편향을 드러내며, 작업 usage와 심사 usage, 심사자별 점수·근거·신뢰도·계열 편향 표시, 합의·불일치, 사람의 최종 선택을 각각 보존한다. 심사 실패는 작업 결과를 실패로 덮어쓰지 않고 부분 평가로 재시도할 수 있게 한다.
- 문서 연결 완료: README에는 아직 완료 기능으로 오해하지 않도록 단계적 구현임을 명시해 설계 문서를 연결했고, CODETREE에는 해당 문서의 책임을 등록했다. 공개 릴리즈·태그·PR은 사용자 결정대로 만들지 않는다.

## 2026-08-13 Codex - 에이전트 실험 실행 원장·체크포인트 구현 착수

- 작업 시작: 기준 명세 커밋 `6b92bf8`에 따라 이슈 #27의 공통 타입, SQLite 스키마, 실행·이벤트·체크포인트 저장소와 회귀 테스트를 먼저 구현한다. 실제 Codex·Claude 프로세스 실행이나 API·UI는 다음 논리 단위로 분리한다.
- 중복 확인: 기존 `TokenUsageLedger`는 공급자 대화 JSONL의 메시지별 usage, `AgentBridge`는 일반 자식 채팅 위임을 소유한다. 둘에 실험 상태를 덧붙이지 않고 별도 `ExperimentRepository`를 두되, SQLite 연결·transaction·UTC 문자열 관례는 그대로 재사용한다.
- 저장 원칙: 실험 설정과 실행 시점 스냅샷을 분리하고, 실행 이벤트는 `run_id + idempotency_key`로 중복 삽입을 막는 append-only 원장으로 만든다. 현재 실행 상태는 허용 전이만 갱신하며, 체크포인트는 완료 노드 경계의 순번이 뒤로 가지 않을 때만 교체한다.
- 사용자 Winner promotion 결정: 실험 우승 Variant를 실제 WAM Agent preset으로 승격해 `실험 → 측정 → 선택 → 실제 사용` 순환을 완성한다. #37로 개별 등록하고, 편집 가능한 Variant가 아니라 선택한 완료 run의 설정 스냅샷을 새 preset 불변 버전으로 복사하며 출처 실험·변형·run, 승격 당시 성공률·품질·토큰·시간 증감, 호환성 검증, 활성 버전 전환·롤백을 남기는 구조로 정했다.
- 사용자 평가 원칙 수정: 판정 모델과 피험 모델의 계열 분리를 강제하지 않는다. Claude·Codex 결과를 Claude·Codex·사람이 교차 심사하게 해 동일 계열과 타 계열 점수 차이 자체를 self-bias 연구 대상으로 삼는다. 대신 피험·평가자 provider/model/family, `same_family`, 블라인드 라벨·제시 순서·루브릭 버전을 철저히 보존하고 기본 UI에서 동일 계열 경고를 표시한다.
- 공정성 정의 수정: 공급자 native 차이를 무조건 제거하는 대신 실험마다 통제 변수와 평가 변수를 명시한다. 모델 효과 실험은 도구·권한을 통제하고 하네스 전체 실험은 native 도구·기본 컨텍스트도 평가 변수로 둘 수 있다. 선언되지 않은 조건 차이는 실행 금지가 아니라 비교 유효성 경고의 근거가 된다.
- 첨부 독립 리뷰 반영: 실행 시점 CLI·실제 모델·도구·권한·스킬 해시를 담을 환경 스냅샷, 반복 수·순서 무작위화, 단일 실행 경고와 분산·신뢰구간, 하드 예산, 기본 observe 훅, 256KiB 이벤트 본문 상한과 큰 payload의 artifact/blob 분리 원칙을 명세·기반 계약에 보강했다.
- 구현 완료: `src/shared/experiments.ts`에 실험 설계와 변형 설정 검증, `database.ts`에 실험/변형/run/node/event/checkpoint/artifact/evaluation/judgment/human verdict 및 draft preset·불변 preset version 스키마, `ExperimentRepository`에 생성·조회·run 시도 번호·설정/환경 스냅샷·허용 상태 전이·멱등 이벤트·최신 체크포인트를 구현했다. 기존 `SessionManager`, `AgentBridge`, `TokenUsageLedger`는 변경하지 않았다.
- 무결성 결정: 이벤트는 run별 멱등 키와 연속 순번을 갖고 256KiB 초과 본문은 artifact 저장을 요구한다. 체크포인트는 해당 run 소유 노드와 실제 존재하는 최신 이벤트 이하만 참조하고, 더 낮은 순번 저장은 기존 지점을 되돌리지 않는다. preset은 버전 없는 active 상태가 생기지 않도록 draft/NULL active version에서 시작해 후속 승격 transaction이 버전 삽입과 활성화를 함께 수행한다.
- Claude 교차검토 실패: 이 작업 단위에서 Claude 현재 세션 사용량은 17%(주간 34%)로 80% 이하였으나 `delegate_and_wait`가 다시 `read ECONNRESET`으로 작업 전달 전에 실패했다. 완료되지 않은 Claude 판단은 반영하지 않았으며 Codex가 타입·SQLite 무결성과 테스트를 직접 검토했다.
- 대상 검증 완료: `npm run typecheck`와 `tests/experiment-repository.test.ts` 8개 테스트가 통과했다. 중간에 다른 run 노드와 미래 이벤트가 동시에 잘못된 체크포인트의 오류 우선순위가 테스트 기대와 달라, 더 구체적인 노드 소유권을 먼저 검증하도록 순서만 조정한 뒤 재통과했다. 전체 회귀·production 빌드는 커밋 직전 별도로 수행한다.
- 전체 검증 완료: README·CODETREE 갱신 뒤 `npm run verify`에서 타입 검사, 71개 파일 485개 테스트, production 클라이언트·서버 빌드와 공개 절대경로 검사가 모두 통과했다. 이번 단위는 DB·공통 타입·서비스 로직만 변경해 Playwright 화면 검증 대상은 없으며, 실제 실험 실행기·API·UI는 다음 단계에 남아 있다.

## 2026-08-13 Codex - 공통 AgentRuntime·HookBus 구현 착수

- 작업 시작: 기반 원장 커밋 `f1f08c0` 위에서 이슈 #31과 #28의 선행 계약인 공급자 중립 `AgentRuntime`, 하드 `BudgetPolicy`, WAM `HookBus`를 구현한다. 실제 Codex·Claude 자식 프로세스 인자와 SingleHarness는 이 계약이 검증된 다음 논리 단위로 분리한다.
- 구현 원칙: 런타임은 구조화 이벤트만 방출하고 DB나 UI를 직접 알지 않는다. HookBus는 등록 순서가 안정적인 실행별 인스턴스로 만들며 기본 `observe` 훅에는 깊게 복제·동결한 payload만 제공한다. 입력 변환·검증·차단은 각각 명시적 mode로 opt-in하고, timeout 및 `fail_run`/`warn`/`ignore` 실패 정책을 공통 결과로 남긴다.
- 예산 원칙: 공급자 CLI의 자체 제한과 별개로 WAM이 벽시계·누적 토큰·비용을 매 이벤트 경계에서 판정하는 하드 상한을 둔다. 미보고 usage는 0으로 추정하지 않고 알려진 값만 누적하며, 초과 차원과 관측값·상한을 구조화 결과로 반환한다.
- 계약 구현 완료: `AgentRuntime`에 준비·실행·재개·취소와 started/message/tool/usage/artifact/completed/failed 공통 이벤트를 정의했다. usage 필드는 nullable로 두어 실제 0과 공급자 미보고를 구분하고, `RuntimeBudgetPolicy`는 시간·토큰·비용의 첫 하드 상한 도달을 원장의 `*_budget` 종료 이유로 반환한다.
- HookBus 구현 완료: 실행별 등록 순서와 중복 ID·100개 상한·1~60000ms timeout을 강제했다. observe 훅에는 `structuredClone` 뒤 재귀 동결한 payload를 전달해 반환값과 객체 변조가 실행 상태를 바꾸지 못하게 하고, transform·validate·block만 명시적 효과를 낸다. 일반 실행 오류는 `fail_run`/`warn`/`ignore`에 따라 구분하며 정책 거부는 별도 오류 타입으로 유지한다.
- 대상 검증 완료: `npm run typecheck`와 `tests/experiment-runtime.test.ts` 7개 테스트가 통과했다. 실제 공급자 CLI를 호출하지 않고 미보고 usage, 세 예산 차원, 중첩 payload 불변성, transform 순서, validate/block, timeout 실패 정책을 검증했다.
- 전체 검증 완료: README·CODETREE 갱신 뒤 `npm run verify`에서 타입 검사, 72개 파일 492개 테스트, production 클라이언트·서버 빌드와 공개 절대경로 검사가 통과했다. 운영 Codex usage도 KST 17:02:44에 다시 `fresh 32%`로 갱신돼 단일 supervisor와 상태 PTY 복구가 지속됨을 확인했다.

## 2026-08-13 Codex·Claude - 공급자 구조화 CLI 런타임 구현 착수

- 작업 시작: 런타임 계약 커밋 `9c58a72` 위에서 공통 JSONL 자식 프로세스 드라이버와 Codex `exec --json`, Claude `-p --output-format stream-json` 어댑터를 구현한다. 실제 모델 호출 대신 CLI 도움말과 기록 fixture로 인자·이벤트·usage·취소 계약을 검증한다.
- 역할 분담: Claude 현재 세션 사용량은 17%로 사용자 기준 이하이고 WAM 연결이 복구돼, 자식 채팅 #222에 Claude stream-json 인자·이벤트·usage·resume·취소 경계의 읽기 전용 독립 설계를 맡겼다. Codex는 공통 프로세스 드라이버와 Codex JSONL 런타임 구현을 담당하고, Claude 결과를 현재 파일과 대조한 뒤 Claude 어댑터에 반영한다.
- Claude 제출 복구: 사용자가 실제 #222 터미널에서 프롬프트가 입력창에만 남고 Enter가 제출되지 않았음을 확인했다. Codex가 pane 끝의 전체 프롬프트와 유휴 `auto mode on` 상태를 재확인한 뒤 Enter를 정확히 한 번 보냈고, 입력창이 비워지며 `thinking with high effort · esc to interrupt`로 전환된 것을 확인했다. 중복 Enter는 보내지 않았다.

## 2026-08-13 Codex - Codex 대시보드 사용량 불일치 읽기 전용 진단

- 사용자 관찰: 실제 Codex 터미널의 주간 표시는 70%인데 대시보드는 25%로 남아 있다고 보고했다. 사용자는 지금 usage 기능을 수정하는 것이 아니라 이유를 확인해 달라고 했다.
- 진단 결과: Codex TUI의 70%는 `70% left`이고 WAM은 `100 - remaining`으로 계산하므로 현재 사용량 의미로는 30%다. 독립 `npm run test:tui`도 현재 `Weekly limit=30%`로 정상 파싱했다.
- stale 근거: 운영 DB의 기본 Codex 계정은 마지막 성공값 25%, `last_success_at=2026-08-13T07:29:20.352Z`(KST 16:29:20), `last_checked_at=2026-08-13T07:30:08.347Z` 이후 `data_status=unavailable`이다. 계정은 하나뿐이어서 계정 선택 오류가 아니며, 오류 시 마지막 정상 수치를 보존하는 정책 때문에 25%가 화면에 남았다. 이번 작업에서는 원인만 기록하고 사용량 코드·운영 프로세스는 변경하지 않았다.
- 상태 PTY 복구 요청: 사용자가 갱신 TUI 생존을 확인하고 죽었으면 살리도록 요청했다. 프로세스·supervisor 로그를 대조한 결과 정식 supervisor 외에 16:08 이슈 생성 명령 본문의 셸 백틱이 의도치 않게 실행한 supervisor 두 개(PID 415030, 415050)가 남아, 기존 14003 서버를 자기 자식의 health로 오인하며 `EADDRINUSE`와 `database is locked` 재시작을 반복한 것이 상태 초기화 실패 원인이었다.
- 운영 복구 완료: accidental supervisor 두 개를 SIGTERM으로 정상 종료해 자식까지 정리하고, 정식 supervisor의 단일 서버 PID 487201만 SIGTERM으로 재시작했다. 정식 supervisor가 새 서버 PID 556121을 띄웠고 Codex 상태 PTY 556294, Claude 상태 PTY 556296이 생성됐다. Codex 원장은 `last_success_at=2026-08-13T08:01:25.946Z`, `data_status=fresh`, `used_percent=32`로 갱신돼 터미널의 약 68% left와 일치한다. 코드·DB 값을 수동 수정하지 않았다.

## 2026-08-13 Codex - Claude 위임 프롬프트 제출 누락 수정 착수

- 재부팅 복구: 사용자 컴퓨터 재시작 뒤 기존 tmux supervisor와 실행 중이던 Claude 자식 채팅이 사라졌다. Codex가 README의 운영 절차와 실제 프로세스를 다시 확인하고, 키 입력 전송에 의존하지 않는 tmux 시작 명령으로 공식 supervisor 한 개를 복구했다. 새 서버 PID 5564와 `/health` 정상 응답을 확인했다.
- 작업 시작: 자식 채팅 #222에서 위임 프롬프트가 Claude TUI 입력창에 붙여넣어졌지만 Enter 제출은 되지 않았는데도 delegation이 `sent`로 기록된 원인을 이슈 #38로 등록했다. `AgentBridge`의 전송 성공 판정, `SessionManager`의 새 터미널 준비 대기와 제출 검증, Claude 공급자 어댑터의 입력 초안 판별을 함께 추적한다.
- 원인 가설: 새 자식 채팅은 terminal map에 먼저 등록되므로 `sendPromptNow`가 이미 실행 중인 세션으로 오인해 TUI 준비 대기를 건너뛸 수 있다. 또한 제출 후 화면 검증은 Codex에만 켜져 있어 Claude에서는 붙여넣기와 Enter 전송 호출이 반환되기만 하면 실제 입력창 잔류 여부와 무관하게 성공으로 처리된다.
- 완료 조건: 새 Claude 세션은 준비된 프롬프트를 확인한 뒤 전송하고, Enter가 누락돼 입력 초안이 남으면 이를 성공으로 보지 않고 제한 횟수만 재시도한다. busy 전환 또는 빈 입력창 복귀를 확인한 경우에만 성공 처리하며, 끝까지 제출되지 않으면 입력 초안을 정리하고 구조화 오류를 반환한다. 실제 #222 형태의 화면 fixture와 세션 회귀 테스트로 재현·수정을 검증한다.
- 원인 확정: `AgentBridge.createTargetChat()`은 `SessionManager.start()` 직후 곧바로 `sendPrompt()`를 호출한다. `start()` 내부 `attach()`가 Claude TUI 준비 전 terminal map과 DB `running` 상태를 먼저 만들기 때문에 기존 `alreadyRunning` 분기가 `waitUntilReady()`를 건너뛰었다. 여기에 Claude의 `promptQuirks`에는 Codex와 달리 붙여넣기 안정화 지연과 `verifyPromptSubmission`이 없어, 긴 bracketed paste 직후 Enter가 소비되지 않아도 tmux 명령 자체가 성공한 것만으로 delegation을 `sent` 처리했다.
- 수정 완료: 이미 terminal map이 있어도 현재 화면이 busy도 ready도 아니면 초기화 중으로 보고 실제 입력 프롬프트까지 기다린다. Claude에도 160ms 붙여넣기 안정화와 공통 제출 검증을 적용해, 첫 Enter 뒤 작업중 또는 본문이 사라진 빈 입력창을 확인한다. 본문이 남으면 Enter를 정확히 한 번 재시도하고, 두 번 모두 실패하면 해당 Unicode 문자 수만큼 Backspace로 초안을 지운 뒤 `busy=false`와 공급자 이름이 포함된 오류를 반환한다.
- 검증 완료: `tests/session-prompt-submission.test.ts`에 Claude 첫 Enter 누락 재현, 새 terminal map의 준비 대기, 두 번 실패 시 초안 정리 회귀를 추가하고 기존 Codex fixture도 실제 `ready → draft → busy` 순서로 바로잡았다. 대상 75개 테스트와 타입 검사가 통과했고, README·CODETREE 갱신 뒤 `npm run verify`에서 타입 검사, 73개 파일 501개 테스트, production 클라이언트·서버 빌드와 공개 절대경로 검사까지 모두 통과했다. UI 마크업 변경은 없어 Playwright 스크린샷 대상은 아니다.

## 2026-08-13 Codex - supervisor Volta shim PID 추적 수정 착수

- 작업 시작: 제출 수정 빌드를 운영 반영하려고 PID 파일의 서버 PID를 종료했지만 실제 14003 리스너가 남고 새 서버가 `EADDRINUSE`로 반복 종료됐다. 이 재부팅 환경의 재현을 이슈 #39로 분리 등록하고 supervisor의 Node 실행 파일 해석을 최소 수정한다.
- 원인 확정: tmux 환경에서 `command -v node`가 `/root/.volta/bin/node` shim을 반환했다. supervisor는 shim PID를 `$!`와 PID 파일에 기록했지만 shim이 실제 `/root/.volta/tools/image/node/22.23.1/bin/node`를 자식으로 실행하므로, shim만 종료하면 실제 서버가 PID 1 아래 고아로 남았다. 헬스 검사는 고아 서버의 응답을 새 자식의 성공으로 오인해 중복 재시작과 `EADDRINUSE`를 만들었다.
- 운영 복구: 전용 PGID 5550에 supervisor·로그 tee·고아 서버·감시 sleep만 있음을 확인한 뒤 그 그룹을 종료했다. `node -p process.execPath`로 확인한 실제 Node 경로를 `WAM_NODE_BIN`에 명시해 새 tmux supervisor를 시작했고, PID 파일과 실제 서버가 동일한 PID 13307이며 `/health`가 정상임을 확인했다.
- 구현 범위: 사용자가 `WAM_NODE_BIN`을 명시한 경우는 그대로 존중한다. 미지정 기본값만 PATH의 node 명령을 한 번 실행해 `process.execPath`를 얻고, 결과가 유효하지 않을 때 기존 명령 경로로 안전하게 대체한다. Volta 같은 shim 환경의 실제 PID 추적을 회귀 테스트와 README·CODETREE에 반영한다.
- 구현 완료: `WAM_NODE_BIN` 미지정 시 PATH의 node 명령을 `-p process.execPath`로 한 번 실행해 실제 바이너리를 선택하고, 유효한 실행 파일을 얻지 못한 경우에만 기존 명령 경로로 대체한다. 명시적 override와 기존 앱·run 경로 계약은 유지했다. fake node shim이 실제 경로를 돌려주는 회귀 테스트를 추가하고 README·CODETREE의 기본값 설명을 수정했다.
- 대상 검증 완료: `bash -n scripts/run-server-supervised.sh`, supervisor 이식성 테스트 2개, 타입 검사가 통과했다. 실제 운영에서도 기존 supervisor를 중지 플래그로 정상 종료한 뒤 override 없는 README 기본 명령으로 재기동했고, PID 파일 16269가 실제 `/root/.volta/tools/image/node/22.23.1/bin/node` 서버를 직접 가리키며 서버 프로세스가 한 개이고 `/health`가 정상임을 확인했다.
- 전체 검증 완료: `npm run verify`에서 타입 검사, 73개 파일 502개 테스트, production 클라이언트·서버 빌드와 공개 절대경로 검사가 모두 통과했다. UI 변경은 없어 Playwright 스크린샷 대상은 아니다.

## 2026-08-13 Codex·Claude - 구조화 CLI 런타임 구현 재개 및 장문 위임 회수 수정 착수

- 작업 재개: 재부팅 전 남은 공통 JSONL 프로세스 드라이버와 Codex `exec --json` 초안을 다시 읽고 대상 테스트를 실행했다. Claude 5시간 세션 사용률은 KST 17:46 기준 21% `fresh`로 사용자 기준 80% 이하라, 중단된 자식 채팅 #222에 새 멱등 키로 읽기 전용 설계를 다시 맡겼다.
- 위임 제출 실검증: 수정된 SessionManager 경로가 #222의 준비된 빈 입력창을 기다린 뒤 장문 prompt를 첨부 안내문으로 붙여넣고 실제 `Choreographing… · esc to interrupt`로 전환했다. delegation은 `sent`, 채팅은 `busy=1`이 됐으며 pane에서 입력 초안 잔류가 없음을 확인해 이슈 #38 수정이 실제 위임에서도 동작함을 검증했다.
- Codex 런타임 보완: 현재 Codex CLI 0.146.0 도움말과 최신 공식 Codex 매뉴얼을 대조했다. resume 하위 명령에는 없는 `--color`를 제거하고, 공식 `codex exec -` stdin 계약으로 prompt를 전달해 argv 노출과 길이 제한을 피했다. file change/plan update를 도구 이벤트로 보존하고, SIGTERM 처리기가 exit 0을 반환해도 취소를 성공으로 오판하지 않도록 공통 runner를 보강했다.
- 원장 선행 계약: SingleHarness가 DB를 직접 수정하지 않도록 준비 단계 환경 provenance 병합, 최초 provider thread/session ID 고정, nullable 누적 usage의 단조 증가 저장 메서드를 `ExperimentRepository`에 추가했다. 저장소·Codex 런타임 17개 대상 테스트와 타입 검사가 통과했다.
- Claude 독립 검토: #222는 Claude CLI 2.1.229의 `--verbose` 필수, 숨은 `--max-turns`, init/result/usage/session 필드와 현재 공통 계약의 캐시 생성·읽기 토큰 및 tool correlation 한계를 보고했다. 파일은 수정하지 않았으며 Codex가 현재 도움말·소스와 대조해 Claude 어댑터 설계에 반영한다.
- 장문 결과 회수 버그 발견: #222는 `turn_end`와 `busy=false`까지 정상 완료했지만 delegation은 `sent/result=null`로 남았다. 이슈 #40으로 분리 등록했다. 원인은 `resolveDelegationResult()`가 DB의 원본 prompt와 history user 메시지를 완전 일치 비교하는 반면, 1,000자 초과 prompt는 `prepareChatPrompt()`가 임의 첨부 경로를 가진 안내문으로 바꿔 TUI와 history에 전달하기 때문이다.
- 수정 방향: `SessionManager.sendPrompt()`가 실제 전달한 `terminalText`를 반환하고 `delegations.history_prompt`에 저장한다. 완료 판정은 이 전달 문자열을 우선 사용하되 기존 행은 원본 prompt로 대체한다. 원본 prompt는 감사·조회용으로 그대로 보존하며, 새 컬럼은 민감정보를 추가 복제하지 않고 이미 history에 기록된 안내문만 담는다.
- 장문 위임 회수 수정 완료: `SessionManager.sendPrompt()`의 직렬 큐가 `prepareChatPrompt()` 적용 뒤 실제 터미널에 보낸 문자열을 반환하도록 바꾸고, `AgentBridge`가 이를 nullable `delegations.history_prompt`에 저장한다. 완료 판정은 새 값이 있으면 이를 사용하고 이전 DB 행은 기존 원문으로 대체하므로 호환성을 유지한다.
- 회귀 검증: 1,000자를 넘는 원본이 첨부 안내문으로 바뀌는 fixture에서 history에는 변환된 문구만 넣고 assistant 완료 응답을 회수하도록 테스트를 강화했다. AgentBridge·채팅 프롬프트·제출 검증 18개 테스트와 타입 검사가 통과했다.
- 재부팅 후 서버 확인: 공식 supervisor와 실제 Node 서버 PID 16269가 살아 있었고 `http://127.0.0.1:14003/health`가 정상이다. 최초 3000 포트 확인 실패는 프로젝트 기본 운영 포트가 14003인 점을 놓친 진단 명령 오류였으며 서버 장애는 아니었다.
- 운영 반영 및 실검증: 전체 73개 파일 505개 테스트와 production 빌드를 통과한 커밋 `642415f`를 만든 뒤 supervisor가 새 서버 PID 30528로 재기동했고 `/health`와 `delegations.history_prompt` 마이그레이션이 정상이다. #222 재사용은 입력창에 소유권이 불명확한 기존 초안이 남아 준비 단계에서 안전하게 거부됐으며 초안을 지우거나 제출하지 않았다. 대신 새 Claude 자식 #223에 1,000자 초과 지시를 보내 첨부 변환·Enter 제출·Read 도구·idle 판정·`장문 회수 확인 완료` 응답과 delegation `completed` 회수까지 실제 경로로 확인했다.
- 런타임 계약 의사결정: Claude 검토를 받아 하네스 `maxIterations`와 공급자 `runtime.maxTurns`를 분리하고, cache creation/read, `totalTokensSource`, tool/parent call ID, user message와 세분화 실패 이유를 공통 Runtime 계약에 추가했다. 기존 캐시 집계 필드는 호환을 위해 유지하고 run·node·judgment DB에 분리 컬럼을 경량 마이그레이션한다.
- Claude print 런타임 구현: 설치된 Claude Code 2.1.231의 실제 `--help`와 모델 호출 없는 `--max-turns 1 --help` capability 검사를 기준으로 `-p --output-format stream-json --verbose`, session resume, stdin prompt, model/effort/native permission, max budget, JSON schema, all/none 스킬 조건을 argv로 구성했다. init·message·tool use/result·cache usage·result subtype을 공통 이벤트로 변환하며 개별 스킬 selected는 native 격리가 불가능해 명시적으로 거부한다.
- 프로세스 취소 보강: 공통 JSONL runner는 POSIX에서 별도 프로세스 그룹을 만들고 TERM 후 grace 동안 후손 정리 핸들러를 보장한 뒤 KILL한다. 첫 회귀 fixture가 손자 준비 전 취소되는 경합과, 부모 종료 직후 finally가 grace를 무시해 손자 정리를 끊는 문제를 각각 드러내 fixture ready 동기화와 타이머 유지로 수정했다. Windows는 직접 자식 단계 종료를 유지한다.
- 대상 검증: 타입 검사와 Runtime·저장소·Codex·Claude fixture 31개 테스트가 통과했다. 실제 공급자 호출은 앞선 #223 장문 위임 검증 외에는 사용하지 않았고 Claude 런타임 자체는 fixture/argv 계약으로 검증했다.
- SingleHarness 착수 및 저장소 확장: 단일 실행도 향후 evaluator/optimizer·orchestrator와 같은 그래프 원장을 쓰도록 `ExperimentRepository`에 순서·부모 관계가 있는 node 생성, queued→running→terminal 상태 전이, 256KiB 입출력 상한, cache/cost/total provenance usage 저장을 추가했다. run/node/judgment에 nullable `cost_usd`와 node/judgment `total_tokens_source` 경량 마이그레이션을 함께 적용한다.
- SingleHarness 구현: queued single run만 받아 격리 작업 디렉터리와 공급자 Runtime을 검증하고, `before_run` transform 뒤 runtime snapshot을 환경에 고정한다. 단일 worker node와 모든 Runtime 이벤트를 append-only 원장에 기록하며 provider session ID·run/node usage를 함께 투영한다. 시간 타이머와 토큰·비용 관측 초과, 외부/명시 취소는 Runtime 취소로 연결하고 완료 node 경계에서 체크포인트를 저장한다.
- 종료·훅 의사결정: 먼저 발생한 취소/예산 이유를 고정해 경합으로 종료 분류가 뒤집히지 않게 했다. `before/after_run`, `before/after_node`, `on_budget`, `on_checkpoint`, `on_error` 훅 결과·경고도 이벤트로 남기며 block/validate는 `policy_blocked`, fail_run 훅은 `hook_error`, 공급자 `max_turns`는 별도 실패 이유로 보존한다. 구조화 완료 이벤트가 없으면 성공으로 추정하지 않는다.
- SingleHarness 대상 검증: 가짜 Runtime으로 prompt transform, snapshot·node·tool/usage/cost·checkpoint 성공, token budget 취소, 사용자 취소와 중복 실행 차단, 정책 훅 차단, 공급자 max turn 실패를 검증했다. repository 포함 21개 대상 테스트와 타입 검사가 통과했다.
- 실험 API·UI 작업 착수: SingleHarness를 실제 프로젝트에서 실행할 수 있도록 프로젝트별 실험/Variant/run 생성·조회·취소 API와 관리자 실험실 화면을 연결한다. 실행 준비 시 현재 Git HEAD에서 분리한 detached worktree를 만들고, 공급자별로 발견한 스킬의 scope·경로·SHA-256 manifest를 환경 snapshot에 고정해 같은 설정 이름이 나중에 바뀌어도 실행 조건을 추적할 수 있게 한다.
- MVP 경계 결정: 첫 UI에서는 이미 구현된 `single` 하네스와 Codex·Claude의 `all/none` 스킬 비교를 실제 실행 대상으로 노출한다. 등록되지 않은 hook 이름을 조용히 무시하지 않고 실행 전에 거부하며, multi-agent·loop·복수 evaluator는 다음 논리 단위에서 같은 run/node/evaluation 원장 위에 추가한다.
- 평가 provenance 표시 결정: 동일 계열 evaluator를 금지하지 않는 사용자 원칙에 맞춰 run 상세가 심사자 provider·model·family, 피험 family, blind label·제시 순서, `same_family`, 점수·신뢰도·근거를 반환하게 한다. 실험실 UI는 동일 계열 심사에 경고를 붙이되 결과를 숨기거나 무효화하지 않는다.
- 재부팅 후 운영 확인: supervisor tmux와 실제 Node 자식 프로세스가 살아 있고 14003 `/health`가 정상 응답한다. 서버를 중복 기동하지 않고 현재 감독 루프를 유지한 채 새 production build가 준비된 시점에만 검증된 PID를 정상 종료해 자동 재기동시킨다.
- API·UI 구현 완료: 관리자 전용 프로젝트 실험/Variant 생성, SingleHarness run 시작·취소·상세 API를 서비스 생명주기에 연결했다. `실험실` 탭에서 가설·통제/평가 변수, Codex/Claude·모델·reasoning·sandbox·skills all/none·예산 Variant를 만들고 성공률·표본 수·상태·토큰·비용, run node·이벤트·종료 이유와 evaluator/subject provenance를 비교한다. 동일 계열 evaluator는 경고만 표시하고 연구 데이터를 보존한다.
- Claude 독립 검토 및 반영: 5시간 세션 사용률 32%로 기준 이하라 #224에 API·worktree·manifest·동시성·UI를 읽기 전용 검토시켰다. Codex가 현재 코드와 대조해 run 상세 judgment 누락은 새 `listJudgments()`로 해소했고, 재시작 고아 run, 준비 실패 worktree 누수, 계정별 config 스킬 경로 불일치, 서버 전체 환경 상속, 무제한 동시 실행, 공급자 sandbox 의미 차이와 위험 권한 노출을 실제 결함으로 수용했다.
- 실행 안전성 보강: 서비스 생성 시 이전 프로세스의 비terminal run을 `runtime_error`로 종결하고, 준비 중 요청까지 포함해 전역 동시 실행을 1개로 제한해 429와 `Retry-After`를 반환한다. 계정을 worktree보다 먼저 검증하고 CLI는 PATH·HOME·locale·temp와 선택 계정 config 변수만 받는다. user-scope manifest는 실제 `CODEX_HOME`/`CLAUDE_CONFIG_DIR`의 `skills`를 해시하며 파일 목록을 정렬한다.
- worktree 보존·정리 결정: 준비 도중 실패하거나 worktree 안 작업 경로가 symlink로 밖을 가리키면 즉시 `git worktree remove --force`로 정리한다. 완료 결과는 후속 evaluator와 산출물 확인을 위해 24시간 보존하고 snapshot에 HEAD 기준·만료 시각을 남긴 뒤, 주기 정리가 앱 관리 root와 원본 Git 저장소를 재검증한 경우에만 제거한다. 원본 Git object/ref까지 격리하는 local clone과 artifact/diff 영구 보존은 후속 과제로 남긴다.
- 비교 한계 UI 반영: Claude native 승인 모드와 Codex OS sandbox가 같은 의미가 아님을 화면에 표시하고 `danger-full-access`는 저장소 밖 경계가 없어 UI에서 제거·API에서 거부한다. 토큰의 reported/derived 출처와 공급자 비용 미보고를 표에 드러내며 반복/무작위화는 아직 자동 실행이 아닌 설계 provenance라고 명시했다.
- 검증 완료: 타입 검사와 Codex/Claude Runtime, 환경 비상속, worktree 생성·제거·계정별 skill manifest, 고아 run 종결, API 429·비활성 프로젝트 거부, SingleHarness를 포함한 대상 6개 파일 36개 테스트가 통과했다. 이어 `npm run verify`에서 전체 77개 파일 524개 테스트, production 클라이언트·서버 build와 공개 절대경로 검사를 통과했다. 전체 Playwright는 23개 통과, 실계정 로그인 1개 환경 조건 건너뜀이며 Agent Lab의 Variant 비교·judgment blind/order·동일 계열 경고와 데스크톱·390px 가로 넘침을 검증해 `artifacts/ui-agent-lab.png`, `artifacts/ui-agent-lab-mobile.png`를 남겼다.
- 복수 evaluator 구현 착수: 완료된 run의 결과를 공급자/Variant 이름이 없는 블라인드 입력으로 Codex·Claude evaluator 각각에 전달하고, evaluator별 provider·model·family와 피험 provider·model·family, 무작위 blind label·제시 순서, 점수·신뢰도·근거·usage·비용을 독립 judgment로 저장한다. 피험 모델과 같은 계열도 금지하지 않고 `same_family`로 보존해 self-bias 분석에 포함한다.
- 평가 경계 결정: 작업 run의 성공/실패 상태를 평가 실패로 덮어쓰지 않는다. 평가 라운드는 queued→running→completed/partial/failed/cancelled로 별도 전이하며, 일부 evaluator만 성공하면 partial로 남겨 재실행과 사람 판정이 가능하게 한다. 첫 구현은 완료 run의 단독 rubric 심사로 제한하고 pairwise A/B·B/A 교차는 다음 확장 단위에서 같은 평가 원장에 추가한다.
- Claude 독립 평가 설계 검토: 최신 Claude 5시간 세션 사용률이 47%로 사용자 기준 80% 이하임을 확인하고 #226에 현재 코드를 읽기 전용 검토시켰다. 장문 prompt는 실제 제출되어 도구 실행과 최종 응답까지 완료됐고 delegation `3ef3ea2e-4420-42f1-8fcb-a4c4b60020ff`도 `completed`로 회수했다. Claude는 node raw output·도구 어휘·worktree 직접 접근의 블라인드 누출, judgment 재실행 중복, 평가 고아 상태, 작업 run과 evaluator의 CPU 경합, pairwise 시 judgment별 usage 복제 문제를 지적했다.
- Claude 검토 반영 결정: 원본 run/node/event 객체를 evaluator에 넘기지 않고 마지막 assistant 답변과 tracked diff만 allowlist 패킷으로 새로 조립한다. 공급자·모델·세션 지문과 길이 절단 건수를 남기고, evaluator는 피험 worktree가 아닌 별도 빈 Git cwd에서 read-only로 순차 실행한다. 판단별 멱등 키와 별도로 evaluator 호출 원장을 추가해 usage·비용·실패를 호출에 귀속하며, pairwise는 후속 구현에서도 호출 비용을 복수 judgment에 중복 합산하지 않는다.
- 복수 rubric evaluator 구현 완료: 완료 run에서 Codex·Claude 1~4명을 선택해 블라인드 평가를 백그라운드 실행한다. 작업 run과 평가는 전역 동시 실행 한도를 공유하고, 평가별 무작위 blind label·subject 순서, evaluator 실행 순서 옵션, 계정·모델·family·CLI·usage·비용·오류를 저장한다. strict JSON 점수·신뢰도·verdict·항목 점수를 검증하며 한 evaluator 실패는 성공 judgment를 보존한 `partial`, 전부 실패는 `failed`, 일부 성공 후 사용자 취소도 `partial`로 종결한다. 서버 재시작의 queued/running 평가·호출은 실패로 명시 종결한다.
- 평가 API·UI 연결: 관리자 run 상세에서 Codex·Claude evaluator와 모델을 선택해 평가를 시작하고 평가 호출 상태·토큰·비용·오류를 자동 갱신한다. 판단에는 피험/평가 provider·model·family, blind label·제시 순서·prompt 버전·마스킹 provenance를 표시하며 동일 계열은 금지하지 않고 경고한다. 진행 중 평가 API는 run ID blind map을 숨기고 시작·취소는 감사 로그에 남긴다.
- 복수 evaluator 검증 완료: 실제 운영 SQLite를 backup한 임시 DB에 `openDatabase`를 적용해 기존 judgment 테이블에 `call_id`·`idempotency_key`, evaluation `error`, 호출 테이블과 고유 인덱스가 정상 마이그레이션되는지 확인했다. `npm run verify`에서 타입 검사, 78개 파일 529개 테스트, production 클라이언트·서버 빌드와 공개 경로 검사를 통과했다. Agent Lab Playwright는 복수 평가 폼·호출별 usage/partial 오류·동일 계열 경고와 데스크톱·390px 가로 넘침을 통과하고 `artifacts/ui-agent-lab.png`, `artifacts/ui-agent-lab-mobile.png`를 갱신했다.
- 평가 안전성 추가 보강: evaluator에도 WAM `RuntimeBudgetPolicy`를 적용해 시간 타이머와 보고 토큰·비용 상한이 CLI 완료 이벤트보다 우선하도록 했다. 빈 cwd에서도 Codex 사용자 스킬 manifest를 발견해 `skills:none` override를 실제 전달하고 Claude는 native disable을 사용한다. 별도 cwd는 피험 경로를 프롬프트에서 숨기지만 OS 읽기 namespace는 아니므로 `read-access-not-isolated` provenance와 문서 한계로 명시했다. 최종 `npm run verify`는 78개 파일 530개 테스트와 production build를 통과했다.
- 그래프 하네스·루프·내장 훅 구현 착수: `orchestrator_worker`는 조정자 계획→격리된 역할별 worker→통합 노드를, `evaluator_optimizer`는 초안→구조화 평가→개선을 같은 run/node/event/checkpoint 원장에 기록한다. primary runtime과 별도의 secondary runtime을 Variant snapshot에 포함해 Codex 작업→Claude 검토 같은 조합을 재현 가능하게 하며, worker 수·최소 점수·최대 반복·무개선 종료·공통 예산을 강제한다. 훅은 서버에 등록된 ID만 허용해 임의 명령 실행을 막고 첫 내장 훅은 diff 통계·결과 크기 검증으로 제한한다.
- 그래프 하네스 구현 완료: Variant harness에 worker 수와 secondary provider·account·model·reasoning을 추가했다. OrchestratorWorker는 primary read-only 계획, secondary workspace-write worker 1~8명, primary 통합을 독립 Runtime node로 실행한다. EvaluatorOptimizer는 primary 초안/개선과 secondary read-only JSON 점수 평가를 최소 점수·최대 반복·무개선 조건까지 반복한다. 첫 구현은 파일 충돌·CPU 측정 오염을 피하려고 같은 detached worktree를 순차 공유하며 역할별 filesystem 격리·병렬 속도 비교는 후속 한계로 남겼다.
- 그래프 원장·예산 결정: 독립 호출 usage는 node 합계를 run에 다시 투영하고 한 node라도 비용을 보고하지 않으면 run 비용을 미집계로 유지한다. 모든 node는 부모 관계·Runtime snapshot·message/tool/usage·완료 이벤트와 after/before hook을 남기고 최종 node 경계에 체크포인트를 저장한다. WAM 시간·토큰·비용 예산과 사용자 취소가 공급자 종료보다 우선하며 max_iterations·no_improvement를 별도 종료 이유로 보존한다.
- 내장 훅·UI 구현 완료: 임의 hook 이름은 거부하고 `diff_stats`가 tracked diff 파일·추가·삭제 줄 수를 after_node 이벤트에 더하며 `git_diff_check`가 whitespace/conflict 오류를 validate 차단한다. Variant 폼에서 세 하네스, secondary 공급자/모델, worker·반복·점수·무개선·내장 hook을 선택하고 비교표에서 하네스 유형을 확인할 수 있다.
- 그래프 하네스 검증 완료: 가짜 Codex·Claude Runtime으로 계획→두 worker→통합의 node 부모 관계와 4회 usage 합산, 낮은 평가→개선→최소 점수 도달 루프를 검증했다. 내장 hook Git fixture와 함께 `npm run verify`에서 타입 검사, 80개 파일 534개 테스트, production build·공개 경로 검사를 통과했다. Agent Lab Playwright는 하네스/secondary/worker 폼과 기존 복수 평가, 데스크톱·390px overflow를 통과하고 `artifacts/ui-agent-lab-harness-form.png`을 추가했다.
- Winner promotion 구현 착수: 관리자가 완료 run을 선택하고 preset 이름·판정 메모를 확인하면 accepted human verdict와 서버 계산 지표를 남기고 run의 불변 config snapshot을 `agent_preset_versions` 새 버전으로 복사한다. 같은 이름 재승격은 버전을 증가시키며 활성 버전 전환을 transaction으로 처리하고, 표본 부족·평가 없음/partial·미보고 비용 등은 차단 대신 compatibility 경고로 보존한다.
- Winner promotion 구현 완료: Codex는 완료 run만 받아 같은 Variant의 terminal 표본 성공률, 완료 표본 중앙 토큰/비용, 점수 있는 judgment 평균을 서버에서 계산하고 accepted human verdict·compatibility warning·불변 config snapshot을 한 transaction으로 preset v1/v2에 누적했다. 활성 승격은 새 버전으로 원자 전환하며 비활성 후보 버전 생성은 기존 활성 버전을 해제하지 않는다. 관리자 API는 프로젝트 preset 목록과 승격 감사 로그를 제공하고, Agent Lab은 이름·선택 근거 확인 뒤 활성 버전·성공률·표본·경고를 표시한다.
- 마감 안전성 결정: 커밋 전 재검토에서 GraphHarness의 Runtime/훅 예외가 현재 node를 queued/running으로 남길 수 있음을 Codex가 발견했다. `runNode`가 모든 예외에서 현재 node를 failed/cancelled로 종결하고 Runtime 포인터를 finally에서 정리하도록 보강했으며, 두 번째 worker 예외 회귀 테스트로 run과 node 원장이 함께 terminal임을 고정했다. 승격 서비스의 비활성 새 버전이 기존 활성 버전을 지우던 경계도 회귀 테스트와 함께 수정했다.
- Winner promotion 최종 검증: 대상 3개 파일 11개 테스트와 타입 검사가 통과했고, `npm run verify`에서 81개 파일 539개 테스트, production 클라이언트·서버 build와 공개 경로 검사를 통과했다. Agent Lab Playwright는 프리셋 승격 폼 제출, 활성 v1·성공률·표본·호환성 경고 표시와 화면 overflow를 통과해 `artifacts/ui-agent-lab.png`를 갱신했다. 문서와 상단 UI는 승격/활성 전환 완료와 pairwise·자동 반복·일반 채팅 preset 소비 미구현 경계를 명시한다.
- 운영 반영 완료: Codex는 PID 파일과 `/proc` 명령행이 이 프로젝트 production server임을 확인하고 supervisor `web-agent-manager-server`가 살아 있는 상태에서 기존 PID 87223에만 SIGTERM을 보냈다. 감시 스크립트가 최신 v0.4.0 build를 새 PID 109735로 재기동했으며 `http://127.0.0.1:14003/health`가 정상 응답한다. 중간 릴리즈와 태그는 사용자 지시에 따라 생성하지 않았다.
- 스킬셋 overlay 격리 작업 착수: 사용자 요청과 이슈 #41에 따라 Agent Lab 비교군을 `현재 설치 스킬`, `현재 설치+선택 추가`, `깨끗한 기본`, `깨끗한 기본+선택 추가`로 확장한다. Codex가 모델·reasoning·권한·MCP·훅·지침·환경·Git 기준점은 동일하게 고정하고, 실행별 스킬 bundle과 공급자 native 주입만 평가 변수로 만든다. 선택 스킬은 `SKILL.md`만이 아니라 같은 스킬 디렉터리 전체의 상대 경로·파일 해시를 snapshot에 고정하며, 공급자가 요구한 비교군을 정확히 만들지 못하면 암묵적으로 근사하지 않고 실행을 거부한다.
- 격리 경계 결정: 이 기능의 목표는 보안 컨테이너가 아니라 인과 비교를 위한 treatment 격리다. 같은 실험의 Variant는 기준 commit·작업 경로·프로세스 환경 key·계정·Runtime·하네스·훅·예산·지침 fingerprint를 비교해 스킬 외 조건 차이를 경고 또는 차단하고, 실제 적용된 baseline/overlay manifest와 effective skill set을 provenance로 남긴다. 호스트 읽기 namespace와 모델 내재 지식까지 제거하는 것으로 표현하지 않는다.
- Claude 독립 스킬 격리 검토: 사용자 기준에 따라 마지막으로 확인된 Claude 5시간 사용률 47%가 80% 이하인 상태에서 #228에 읽기 전용 CLI capability·누출 경로 검토를 위임했고 delegation `9f50f233-6234-48c1-9fb9-6f52ce17e9b2`를 `completed`로 회수했다. Claude 2.1.231에서 `--disable-slash-commands`는 공급자 기본 스킬까지 전부 제거하고, `--setting-sources`와 `--plugin-dir` 조합은 인증을 유지한 채 사용자·프로젝트 스킬 기준선과 plugin 추가를 구분한다는 실측을 현재 CLI 도움말과 재대조했다. Codex 0.146.0은 `--ignore-user-config`와 `--strict-config`를 제공하며 `skills.config.path`에는 `SKILL.md` 파일이 아니라 디렉터리를 전달해야 한다.
- 격리 구현 수정 결정: Claude의 네 비교군은 모든 실행에 setting source를 명시하고 추가 스킬을 native plugin bundle로 주입한다. Codex는 strict config와 디렉터리 기반 skill override를 사용하고 clean arm에서 사용자 config를 제외한다. detached worktree에서 Git ignore된 프로젝트 스킬이 사라지는 문제를 막기 위해 원본 프로젝트의 설치 스킬을 실행별 worktree로 고정 복제하고 해시를 남긴다. CLI가 모르는 옵션을 무음 수용할 수 있으므로 미지원 Claude max-turns 감지를 exit code에 의존하지 않으며, bundle 검증은 성공·실패 종료 모두에서 수행한다.
- 최종 통제 방식 결정: 초기 검토안을 더 대조한 결과 Codex `--ignore-user-config`와 Claude의 baseline별 setting source 차이는 MCP·hook·기타 설정까지 바꿔 스킬 외 변수를 만들 수 있어 사용하지 않는다. Codex는 모든 arm에서 사용자 config를 유지하고 `--strict-config`의 디렉터리 기반 `skills.config`만 교체한다. Claude는 모든 strict arm에 동일한 `--setting-sources ""`·`--strict-mcp-config`를 적용하고, 현재 project/user custom skills와 선택 additions를 같은 native plugin bundle 형식으로 재구성한다. 모델·reasoning 명시와 single harness를 강제하며 graph/loop의 공급자별 overlay는 근사 구현하지 않고 후속 범위로 남긴다.
- 스킬 격리·비교 UI 구현 완료: 후보 API가 공급자 installed project/user/system, 프로젝트 `.agent-lab/skills`, WAM 라이브러리를 경로 노출 없이 반환한다. 실행별 bundle은 스킬 전체 디렉터리를 500파일·10MiB 상한과 내부 symlink 차단 아래 복사하고 파일 상대경로·크기·mode·SHA-256 digest를 실행 전후 검증한다. Git에서 제외된 project skills도 detached worktree에 고정 복제한다. 같은 comparisonId는 baseline/additions 외 Runtime·계정·모델·reasoning·권한·하네스·훅·예산이 달라지면 거부하고, 같은 실험의 Git HEAD 변경도 차단한다. 비교표와 run 상세에는 성공률·유효 evaluator 평균 품질·벽시계·토큰 출처·비용·종료 상태와 control/bundle digest를 표시한다.
- 스킬 격리 최종 검증·운영 반영: 타입 검사, 81개 파일 546개 전체 테스트, production 클라이언트·서버 build와 공개 경로 검사가 통과했다. Agent Lab Playwright는 installed 중복 후보 비활성화, clean/installed 조건, 선택 addition, graph 전환 시 strict overlay 제거, 91점 품질·1분 wall-clock·토큰·비용, 상세 판단·승격과 390px overflow를 통과해 `artifacts/ui-agent-lab-skill-form.png`, `artifacts/ui-agent-lab.png`, `artifacts/ui-agent-lab-mobile.png`를 갱신했다. supervisor의 기존 production PID 109735에만 SIGTERM을 보내 새 PID 145625로 재기동했고 14003 `/health`가 정상이다. v0.4.0은 유지하며 중간 Release·tag는 만들지 않는다.
- Ponytail 실험 착수: 사용자 요청에 따라 Claude의 `clean` 기준선과 `clean + Ponytail`을 같은 모델·reasoning·권한·예산·Git fixture에서 실행해 품질·벽시계·토큰·비용·diff 크기를 비교한다. 현재 로컬 skill catalog에는 Ponytail이 없어 공식 `DietrichGebert/ponytail` 소스를 실험 전용 후보로만 고정하며 전역 Claude 설정에는 설치하지 않는다. 공개 레퍼런스상 Ponytail은 단순 skill auto-discovery가 아니라 SessionStart hook으로 규칙을 주입해야 실제 적용이 보장되므로, WAM overlay가 hook을 누락한 상태로 허위 treatment를 기록하지 않는지 먼저 검증한다.
- Ponytail 적용 누락 원인 확정: Codex가 공식 Ponytail v4.9.0 commit `2ed6c52c9d7e5e56942508591085fd45dea277d3`의 plugin manifest·SessionStart hook·agentic benchmark를 확인했다. 기존 WAM은 선택한 스킬 디렉터리를 Claude plugin bundle에 복사했지만 plugin hook은 구성하지 않아, 자동 발견되지 않은 Ponytail을 실제로 켰다는 보장이 없었다. 이 상태의 실행은 허위 treatment가 되므로 유료 비교 전에 차단하고 일반화된 활성화 방식을 보완했다.
- 무상태 SessionStart 격리 결정: strict Claude single에 `activation: native|session_start`를 추가했다. `session_start`는 대조군과 처리군 모두 동일한 실행별 read-only hook과 공통 머리말을 거치고 처리군에만 선택 additions의 frontmatter 제거 본문을 붙인다. 생성 hook은 bundle의 정적 파일만 stdout으로 내보내며 Ponytail 원본 plugin의 mode flag·statusline 설정 등 `CLAUDE_CONFIG_DIR` 쓰기는 실행하지 않는다. 따라서 이번 실험은 “Ponytail v4.9.0 지시 본문 효과”를 측정하고 plugin UI·상태 부작용은 제외한다.
- 공정성·provenance 보강: 활성화 방식을 같은 `comparisonId`의 비스킬 fingerprint에 포함하고 Claude 외 provider의 SessionStart 선택을 거부한다. run snapshot과 UI 격리 증명에 활성화 방식을 표시하며, Claude Variant 폼에서 자동 발견과 강제 주입을 명시적으로 고른다. 기존 `native` 기본값과 Codex 동작은 유지했다.
- 활성화 모드 검증 완료: 대조군·처리군 plugin manifest가 같은 hook 경로를 갖고 대조군은 공통 머리말만, 처리군은 추가 본문만 더하며 hook script에 파일 쓰기가 없음을 회귀 테스트로 검증했다. `npm run verify`에서 타입 검사, 81개 파일 547개 테스트, production 클라이언트·서버 build와 공개 경로 검사가 통과했다. Agent Lab Chromium 시나리오도 Claude `SessionStart 강제 주입` 선택과 화면 overflow를 통과해 `artifacts/ui-agent-lab-skill-form.png`를 갱신했다.
- Claude evaluator capability 수정 착수: 실제 Ponytail 정식 run 두 개와 Codex blind judgment는 완료됐지만 Claude Sonnet evaluator가 `현재 Claude CLI는 runtime.maxTurns를 지원하지 않습니다`로 실패했다. Codex가 `ExperimentService.startEvaluation()`을 재확인한 결과 evaluator 공통 설정만 provider와 무관하게 `maxTurns: 3`을 강제해, 일반 Claude 작업 run에서 이미 지키는 capability 경계를 평가 경로가 우회한 것이 원인이었다. Claude evaluator에는 `null`, Codex에는 기존 3을 유지하는 최소 수정과 회귀 테스트 후 실패 run에 새 label의 Claude-only blind 평가를 추가한다.
- Claude evaluator capability 수정 완료: provider별 순수 설정 builder를 추가해 Claude evaluator는 `maxTurns:null`, Codex evaluator는 기존 `maxTurns:3`을 사용하게 했다. WAM의 300초·10만 토큰·$2 evaluator 예산은 둘 다 유지한다. 타입 검사와 rubric/API 대상 2개 파일 11개 테스트가 통과했으며, UI 마크업 변경은 없다.
- Ponytail 단건 정식 실험 완료: 공식 `cache` 과제와 fixture HEAD `1abdf4567a40c22ca69cdee774ee90a57fd072dd`, Claude Haiku 4.5 medium, clean baseline, 동일 무상태 SessionStart hook, 300초·20만 토큰·$3 상한에서 무작위 순서(Ponytail→control)로 각 1회 실행했다. 두 arm 모두 공식 deterministic scorer의 correct/safe를 통과했다. Ponytail은 29초·134,259토큰·$0.04612, control은 30초·103,942토큰·$0.0407478이었다. tracked source diff는 둘 다 3줄 추가였으나 untracked 테스트는 Ponytail 59줄, control 87줄로 Ponytail이 28줄(32.2%) 적었다.
- 단건 evaluator 판단: 공급자 분리 Codex gpt-5.5는 Ponytail 0.82/control 0.90, 동일 계열 Claude Sonnet 4.6은 Ponytail 0.78/control 0.85로 둘 다 control을 높게 봤다. control의 `maxsize=None`이 반복 호출을 무제한 보장한 반면 Ponytail은 임의 `maxsize=128`이라 128개 초과 후 재계산 가능성이 지적됐다. 반대로 control 테스트는 fixture에 없는 pytest를 import하고 87줄, Ponytail 테스트는 stdlib assert 59줄이었다. 현재 blind packet·diff_stats가 untracked 새 파일을 누락해 judge가 이 차이를 보지 못한 측정 한계도 확인했다.
- 목표 기반 멀티턴 비교 착수: 사용자 추가 요청과 이슈 #42에 따라 Codex가 “하나의 goal과 완료 기준을 고정하고 각 Variant가 turn별로 얼마나 도달했는지 단건과 함께 비교”하는 기능을 시작한다. 첫 완결 범위는 동일 작업 runtime의 multi-turn resume, 매 turn 별도 evaluator 점수·개선량·남은 gap·usage·duration checkpoint, 목표 점수/최대 turn/무개선/예산 종료, 단건 대비 UI다. 기존 evaluator_optimizer와 중복되는 node·checkpoint·예산 로직을 먼저 재사용하고, 새 파일을 포함한 평가 packet 범위도 목표 진척률의 정확성을 위해 함께 보완한다.
- 단건 산출물 실행 검증·범위 조정: 사용자 지시에 따라 목표형 멀티턴 구현은 지금 보류하고 앞선 Ponytail 단건 테스트만 마감했다. 공식 deterministic `score_cache`는 두 arm 모두 통과했지만 각 agent가 만든 `test_compute.py`를 실제 `python3`로 실행하니 Ponytail의 stdlib assert 테스트는 통과하고 control은 fixture에 설치되지 않은 `pytest`를 새 의존성으로 import해 `ModuleNotFoundError`로 실패했다. tracked-only blind packet 때문에 두 judge가 이 새 파일 실패를 보지 못했으므로, 자동 judge 평균만으로 control을 우승 처리하지 않고 “정답 함수는 동률, 실행 가능한 검증·작은 테스트는 Ponytail 우세, 토큰·비용·judge 점수는 control 우세”로 보고한다. 이슈 #42는 후속 범위로 유지한다.
- 운영 반영: production build가 최신 두 수정 커밋을 포함한 상태에서 supervisor의 기존 서버 PID 145625에만 SIGTERM을 보냈다. supervisor가 새 PID 495919를 기동했고 `http://127.0.0.1:14003/health`가 정상이다. 릴리즈·태그·푸시는 하지 않았다.
- Ponytail 멀티턴 실험 시작: 사용자 정정에 따라 기존 단건 cache 비교를 반복하지 않고, 앱 구현도 보류한 채 새 목표 하나를 Claude clean과 동일 clean+Ponytail 조건에서 각각 3개 연속 session turn으로 수행한다. Codex는 동일 Git HEAD·Claude Haiku 4.5 medium·workspace-write·무상태 SessionStart hook·외부 의존성 없는 고정 scorer를 통제하고, turn별 기준 충족률·증분·벽시계·토큰·비용과 최종 Claude/Codex 블라인드 판단을 비교한다.
- Ponytail 멀티턴 정식 결과: 목표는 기존 `compute.py`에 bounded LRU·TTL·동일 key single-flight·설정/통계 API·stdlib 테스트를 추가하는 것으로 고정했다. 최초 scorer가 후보 테스트의 `SystemExit`를 격리하지 못한 Ponytail pilot과 채점 timeout 전에 usage를 저장하지 못한 clean pilot은 정식 표본에서 제외하고, scorer subprocess 격리와 turn별 선행 checkpoint를 보완한 새 worktree만 비교했다. 무작위 arm 순서는 Ponytail→clean이었다.
- Turn별 측정: Ponytail은 품질 `100→100→95`, 시간 `256.987→158.188→126.831초`, 파생 토큰 `650,942→831,068→725,638`, 비용 `$0.2647425→$0.2668245→$0.2664372`였다. clean은 품질 `100→100→100`, 시간 `103.304→61.592→126.544초`, 파생 토큰 `265,769→229,638→342,771`, 비용 `$0.1234143→$0.1033268→$0.1160959`였다. 누적은 Ponytail 2,207,648토큰·542.006초·$0.7980042, clean 838,178토큰·291.440초·$0.342837로 Ponytail이 2.63배 토큰·1.86배 시간·2.33배 비용을 사용했다.
- 평가자 provenance: 최종 tracked diff와 새 Python 파일을 모두 넣은 blind packet을 judge별 독립 A/B 순서로 제시했다. Codex gpt-5.5 medium은 Ponytail 0.67/clean 0.74, Claude Sonnet 4.6 medium은 Ponytail 0.61/clean 0.72로 둘 다 clean을 선택했다. Codex가 지시한 0~1 대신 10점 척도를 반환한 값은 10으로 나눠 정규화했다. 두 judge가 공통 지적한 Ponytail의 in-flight `configure_cache()` waiter deadlock은 별도 daemon-thread probe에서 `waiter_alive=true`로 재현됐고 clean은 두 worker가 모두 종료됐다. Ponytail 자체 테스트도 최종 exit 1, clean은 exit 0이었다.
- 종합 점수 결정: 사용자 요청대로 토큰 사용량을 승부 점수에 포함했다. `품질 70%(최종 deterministic 60%+두 blind judge 평균 40%) + 토큰 효율 20% + 속도 효율 10%`를 사용하고 효율은 두 arm 중 최소 사용량 대비 비율로 정규화했다. 최종 점수는 clean 92.44, Ponytail 70.79로 clean 승리다. 가중치 밖의 비용도 원자료로 별도 보존한다. 이번 결과는 각 arm 1세션뿐이므로 통계적 일반화나 Ponytail 전체 효용 판정이 아니라 이 목표·모델·순서의 사례 결과다.
- 실험실 후속 계획 추가 착수: 사용자 지시에 따라 Claude가 지금 구현하지 않고 `docs/agent-lab.md`의 기존 계획에만 후속 범위를 추가한다. 근거는 채팅 #219 Ponytail 비교의 측정 한계 재검토와 사용자가 새로 요구한 두 축이다. 하나는 실험실을 스킬·MCP로 노출해 에이전트가 직접 관리·실행하게 하는 것이고, 다른 하나는 실험 전용 fixture를 새로 만들지 않고 등록된 기존 프로젝트를 대상으로 지정해 실험하는 것이다. 코드·이슈·커밋은 이번 범위에서 만들지 않는다.
- 실험실 후속 계획 추가 완료: `docs/agent-lab.md` 15장에 `7단계 — 실험 설계와 측정 정확도 보강`과 `8단계 — 에이전트 셀프서비스와 기존 프로젝트 대상 실험`을 추가했다. 7단계는 과제 해법 자유도를 실험 변수로 명시, untracked 산출물 포함과 `새 의존성 도입`·`실제 실행 가능` rubric 항목 추가, arm당 3회 이상과 순서 교차 강제, 출력·추론 토큰과 답변 길이 표면화, 스킬 강도 인자 arm 지원의 다섯 항목이다. 8단계는 MCP·스킬을 통한 에이전트의 실험 관리·실행과 기존 등록 프로젝트를 대상으로 한 실험 지정이다. 근거 수치는 #219 원자료를 다시 집계해 확인했으며 출력 토큰 1.80배, 추론 2.01배, 최종 답변 길이 4.40배로 입력측 누적 2.63배와 별개의 역효과가 확인됐다. 코드·테스트·이슈·커밋은 사용자 지시대로 만들지 않았고 기존 5·6단계와 16~18장은 중복을 피해 수정하지 않았다.
- 코드 품질 비교와 계획 보강: Claude가 #219 두 산출물을 직접 읽고 재실행·probe로 검증했다. Ponytail은 `ttl_seconds=True` 거부와 TypeError/ValueError 구분에서 우세했고, clean은 in-flight `configure_cache()` deadlock 부재, 예외 미캐싱, `_calls == 1` 엄격 단언에서 우세했다. Ponytail 테스트 실패는 코드 결함이 아니라 `test_exception_caching`이 wrapper 자신의 호출 수를 캐시 적중으로 착각한 논리 오류이며 해당 테스트는 예외를 한 번도 발생시키지 않는다. Ponytail은 스킬이 요구한 `ponytail:` 주석을 0건 남기고 파일 수 최소 규칙도 어겼다. 결론은 스킬 지시를 따라서 진 것이 아니라 지키지 못해 졌다는 것이므로, 사용자 승인을 받아 `docs/agent-lab.md` 7단계에 스킬 규칙 준수율 독립 측정을 추가했다.
- 채팅 프로젝트 오귀속 원인 규명: 사용자가 이 채팅(#257)이 myagent에서 myagent/artifacts로 이동하고 채팅 모드 입력이 막힌다고 보고했다. Claude가 조사한 결과 `HistorySyncService.persist()`가 Claude 세션 JSONL의 마지막 `cwd` 레코드로 프로젝트를 매번 재계산하고, 해당 경로 프로젝트가 없으면 `source='discovered'`로 새로 만든 뒤 기존 채팅의 `project_id`까지 강제로 갱신하는 것이 원인이었다. 직접 트리거는 Claude가 산출물 조사 중 실행한 `cd /home/ubuntu/myagent/artifacts`로, Bash 도구의 작업 디렉터리가 호출 간 유지되어 이후 세션 기록 70건의 cwd가 artifacts로 남았다. 같은 메커니즘으로 실험 worktree와 evaluation workspace 10여 개도 프로젝트로 자동 등록되어 목록을 오염시켰다. 셸 cwd는 원복했으나 채팅은 자동 복귀하지 않아 캐시 또는 재귀속 조건을 추가로 확인해야 한다. 수정은 사용자 승인 전까지 하지 않았다.
- 채팅 오귀속 수정 착수: 사용자 승인으로 Claude가 이슈 #43·#44·#45를 등록하고 `feat/agent-lab-v04` 한 브랜치에서 순서대로 진행한다. #43은 세션의 마지막 cwd로 채팅 프로젝트를 매번 재귀속시키는 로직과 worktreeOwner 자기참조 고착을 고친다. 추가 조사에서 `#257`의 `worktree_path`가 프로젝트 경로와 같아 `c.worktree_path <> p.path` 방어를 통과하고 자기 자신을 worktree 소유자로 찾는 것이 복구 불능의 직접 원인임을 DB 조회로 확정했다. #44는 이미 등록된 프로젝트의 하위 경로에서 발견된 세션이 새 `discovered` 프로젝트를 만들지 않고 상위 프로젝트에 귀속되게 한다. #45는 오염된 `#257` 행을 복구한다. 오염 범위는 조회 결과 `#257` 1건이다.
- 채팅 오귀속 수정 완료: `HistorySynchronizer`의 프로젝트 결정을 `세션 cwd와 정확히 같은 등록 프로젝트 → 그 경로를 쓰는 worktree 소유 채팅의 프로젝트 → 가장 가까운 상위 등록 프로젝트 → 신규 discovered 등록` 순서로 바꿨다(#43, #44). 첫 단계가 자기참조 고착을 원천 차단하고, 세 번째 단계가 실험 worktree·평가 작업공간·artifacts 같은 하위 경로를 상위 프로젝트로 귀속시켜 목록 오염을 막는다. 기존 채팅의 `project_id` 재귀속은 worktree 소유자가 확인된 경우로 한정해, 에이전트가 `cd`만 해도 채팅이 튕겨나가던 동작을 없앴다. worktree 채팅의 원래 교정 의도는 같은 분기에서 그대로 유지한다. `tests/history-sync-project.test.ts` 3개를 추가했고 수정을 되돌리면 앞의 두 개가 실패하고 worktree 보존 테스트만 통과하는 것으로 회귀 방어를 확인했다. `npm run verify`에서 타입 검사, 82개 파일 551개 테스트, production 빌드와 공개 경로 검사가 통과했다. 다른 세션이 작업 중인 `experiments/*` 변경분은 건드리지 않았다.
- 운영 반영·데이터 복구 완료(#45): production build가 위 수정을 포함한 상태에서 supervisor PID 파일의 기존 서버 PID 495919에만 SIGTERM을 보냈다. `/proc/495919/cmdline`과 cwd로 이 프로젝트 production server임을 먼저 확인했고 프로세스 이름 검색은 쓰지 않았다. supervisor가 새 PID 543118로 재기동했으며 `http://127.0.0.1:14003/health`가 200이다. 이어서 채팅 `#257`의 `project_id`를 13으로, `worktree_path`를 NULL로 되돌렸다. 3·6·10·15초 뒤 재확인에서 값이 그대로 유지돼 자기고착 루프가 끊긴 것을 실측했다. `status='error'`는 긴 도구 실행 중 채팅 모드 입력이 15초 readiness를 넘겨 남은 값이며 `SessionManager.waitUntilReady()`가 다음 정상 입력에서 running으로 되돌린다. 릴리즈·태그·푸시는 하지 않았다. 남은 범위로 채팅이 하나도 없는 discovered 프로젝트 11개(`artifacts`, `data/git-worktrees/13/193`, Java 소스 하위 경로 등)와 채팅이 붙은 실험 worktree 프로젝트들의 정리는 파괴적이라 사용자 확인 전까지 하지 않았다.
- 오류 채팅 입력 교착 수정(#46): 사용자 요청에 따라 오류 상태에서 busy를 0으로 유지하고 채팅 입력을 받도록 고쳤다. `HistorySynchronizer`가 `stopped`뿐 아니라 `error` 상태에서도 busy를 올리지 않게 해 `setStatus`가 내린 busy를 곧바로 되올리던 경로를 끊었고, `SessionManager.sendMessage()`는 실행 중인 터미널이 있으면 `status='error'`일 때도 idle 프롬프트를 기다리지 않고 바로 전송하게 했다. 기다리던 기존 동작에서는 매 전송이 15초 뒤 실패하고 다시 오류로 덮어써 사용자가 스스로 복구할 수단이 없었다. 전송이 성공하면 이미 있던 `sendMessage`의 복구 경로가 상태를 running으로 되돌린다. 회귀 테스트를 추가해 수정을 되돌리면 실패하는 것을 확인했고 `npm run verify`에서 82개 파일 552개 테스트와 빌드가 통과했다.
- 정리 유지 수정(#47): 채팅·프로젝트 정리에 앞서 `persist()`가 프로젝트의 `active`를 보지 않아, 프로젝트를 비활성화하고 채팅을 지워도 다음 스캔에서 같은 JSONL로 곧바로 재등록되는 것을 확인했다. 비활성 프로젝트의 세션은 새 채팅을 만들지 않도록 고치고 이후 갱신·알림도 건너뛰게 했다. 회귀 테스트를 추가해 수정을 되돌리면 실패함을 확인했으며 `npm run verify`에서 82개 파일 553개 테스트가 통과했다.
- 채팅·프로젝트 정리 실행: 사용자 지시("이미 삭제 기능이 있으니 그걸 활용해")에 따라 DB에 직접 DELETE를 쓰지 않고 앱의 기존 삭제 경로를 그대로 호출했다. 채팅은 `SessionBackupService.backupChat()`으로 백업을 남긴 뒤 `GitWorkspaceService.removeChatWorktree()`와 `SessionBackupService.deleteChat()`을 순서대로 실행했고, 프로젝트는 `project-routes`의 DELETE와 동일하게 `active=0` 갱신과 `project.delete` 감사 로그를 남겼다. 대상 계산에서 사람이 쓰는 프로젝트 7개(myagent, WSS-Server, WSS-admin-web, geulmeok-frontend, geulmeok-scrap, resume, Ponytail fixture)는 화이트리스트로 보호했다. 프로젝트 16개(artifacts와 그 하위 judge·ponytail worktree, experiment-worktrees 4, evaluation-workspaces 4, WSS-admin-web/backend)와 채팅 14개를 정리했으며 모두 stopped 상태여서 세션 중지 경로는 타지 않았다. 정리 후 활성 프로젝트는 35개에서 7개가 되었고, 12초 뒤 재확인에서 삭제한 채팅 재등록은 0건으로 #47 수정이 실제로 동작함을 확인했다. 채팅 `#257`은 `project_id=13`, `status='running'`으로 정상 복구됐다. `WSS-admin-web/backend`는 채팅이 없는 자동 등록 항목이라 포함했고 soft delete이므로 필요하면 되돌릴 수 있다. `/tmp` 계열 채팅 3건은 이미 비활성 프로젝트 소속이라 목록에 노출되지 않아 남겨두었다.
- 승인 닫기 교착·채팅 입력 덮어쓰기 수정 착수: 사용자가 채팅 `#257`의 채팅 모드 입력이 계속 오류로 실패하고, 진행 중 채팅 `#259`에서는 권한 요청 카드의 "닫기"가 전혀 동작하지 않는다고 보고했다. Claude가 조사해 두 개의 서로 다른 원인을 확정했다. (1) `#257`은 tmux 입력창에 미전송 텍스트("backend 복구해줘")가 남아 있어 `isClaudePromptLine`이 빈 프롬프트만 프롬프트로 인정하는 탓에 `isReady`와 `isBusy`가 동시에 false가 되고, `waitUntilReady()`가 15초마다 타임아웃하며 상태를 다시 error로 덮어쓰는 영구 실패에 빠졌다. 실제 화면 스냅샷을 어댑터에 넣어 초안이 있을 때 false, 초안을 지운 가정에서 true가 되는 것을 실측해 확정했다. (2) `#259`의 닫기 실패는 그 승인이 이 세션의 `AskUserQuestion` 훅 요청이었고, 질문형 카드에는 "답변 전송"과 "닫기"뿐이라 취소 경로가 없는데 `ApprovalService.dismiss()`가 살아있는 훅에 대해 무조건 예외를 던져 사용자가 9분 타임아웃까지 갇힌 것이다. 사용자 결정에 따라 채팅 모드 입력은 입력창의 대기 텍스트를 지우고 새 입력으로 덮어쓰되, 승인·메뉴처럼 입력창이 아닌 화면에서는 덮어쓰지 않고 입력을 막는다. 닫기는 살아있는 요청이라도 사용자를 가두지 않고 취소로 전달한다.
- 승인 닫기 교착·채팅 입력 덮어쓰기 수정 완료: `ApprovalService.dismiss()`는 이제 살아있는 요청에도 예외를 던지지 않고 `cancel` 결정으로 종료해, 훅에는 취소 응답을, 터미널 승인에는 `approvalInput`의 취소 입력을 전달한다. 닫기가 반드시 카드를 닫으므로 사용자가 답변을 강요당해 갇히지 않고, 살아있는 작업도 조용히 끊기는 대신 취소를 명시적으로 전달받는다. 채팅 입력 쪽은 어댑터에 `readPromptDraft` 선택 훅을 새로 두고(빈 입력창은 빈 문자열, 입력창 자체가 없는 승인·선택 메뉴 화면은 null) `SessionManager`가 이를 사용한다. `waitUntilReady()`는 초안이 남은 입력창도 입력 가능으로 인정하고, `sendPromptNow()`는 붙여넣기 직전 남은 초안을 그 길이만큼 Backspace로 지워 새 입력으로 덮어쓴다. 사용자 지시대로 입력창이 아닌 화면에서는 덮어쓰지 않고, 승인 화면이면 15초 대기 없이 곧바로 이유를 알리며 막는다. `isReady`는 제출 확인(`waitForPromptSubmission`)이 "빈 입력창만 준비"라는 성질에 의존하므로 건드리지 않고 별도 판정으로 분리했다. 회귀 테스트는 초안 읽기·빈 입력창·메뉴 화면 구분 3건, 초안 덮어쓰기·빈 입력창 무동작·승인 화면 거부 3건, 살아있는 훅의 닫기가 취소로 풀리는 것 1건을 추가·수정했고 수정을 되돌리면 초안 덮어쓰기 테스트가 실패하는 것을 확인했다. `npm run verify`에서 타입 검사, 82개 파일 559개 테스트, production 빌드와 공개 경로 검사가 통과했다. 다른 세션이 작업 중인 `experiments/*` 변경분은 건드리지 않았다.
- Agent Lab 채팅 정리: 사용자 요청으로 랩 작업 중 생성된 myagent 채팅 21건을 앱의 기존 삭제 경로(`backupChat` → `removeChatWorktree` → `deleteChat`)로 정리했다. 실제 작업 기록이 있는 7건(#220 릴리즈 감사, #222 AgentRuntime 설계, #223·#228 첨부 처리, #224 보안·동시성 검토, #225 #222 디버깅, #226 evaluator 설계)은 백업을 남기고 삭제했고, 기능 확인용 더미 14건(#229~#242의 "ok", "2+2 계산 확인", "메시지 응답 확인")은 사용자 지시대로 백업 없이 삭제했다. 삭제 전 하드 가드로 보호 목록(#257 현재 대화, #258·#259 사용자가 방금 띄운 세션, #219 실행 중)이 계획에 섞이면 예외로 중단하도록 했고, `stopped`가 아닌 채팅은 자동으로 건너뛰게 해 살아 있는 세션을 끊지 않도록 했다. #222는 실행이 끝난 뒤 stopped로 바뀌어 뒤이어 정리했다. myagent 채팅은 34건에서 13건이 되었고 재등록은 0건이다. #219는 실행 중이고 Codex goal이 active라 임의로 종료하지 않고 사용자 확인 대상으로 남겼다.
- 닫기의 결정 종류 정정: 사용자가 "닫기 눌렀을 때 Esc하는 게 채팅 상황을 확인하고 하는 것이냐, 질문창이 맨 아래에 있을 때만 취소해야지 안 그러면 질문을 닫았는데 작업이 취소될 수 있다"고 지적했다. 확인 결과 터미널 승인의 키 입력은 `SessionManager.isTerminalApprovalLive()`가 현재 화면 스냅샷에 `detectApproval`을 돌려 같은 요청이 실제로 떠 있을 때만 나가고, `detectApproval` 자체가 화면 하단 위치까지 검증하므로 지나간 화면에는 아무 키도 보내지 않는 것이 맞았다. 그러나 Claude 훅 경로에는 실제 결함이 있었다 — `cancel`은 `denyClaude(message, interrupt=true)`로 나가 진행 중인 턴 자체를 중단시키므로, 질문 하나를 닫았을 뿐인데 작업 전체가 취소된다. 그래서 `dismiss()`의 살아있는 요청 처리를 `cancel`에서 `decline`("사용자가 이 요청을 닫았습니다.")으로 바꿨다. 이제 그 요청만 거부되고 작업은 이어지며, 터미널 승인도 Esc가 아니라 요청유형별 거부 입력이 나간다. 회귀 테스트로 화면에서 지나간 터미널 승인은 닫아도 키가 전혀 나가지 않는 것과, 살아있는 승인은 `decline`으로 전달되는 것을 고정했다.
- 다중 저장소·과제 유형 비교 스위트 계획 정리 착수: 사용자가 "실험실에 여러 비교군을 넣어 한 번에 결과를 받고 싶다. GitHub 공개 저장소를 대형·중형·소형으로 받아 에러 유지보수/새로 만들기/신규 기능 추가/보안 취약점 네 관점에서 완성도·소모 토큰·소요 시간을 비교하고 싶다"고 요구했고, 기존 실험실 기획과 대조해 다시 정리해 달라고 지시했다. Claude가 `docs/agent-lab.md`와 `src/shared/experiments.ts`·`experiment-service.ts`·`experiment-workspace.ts`·`experiment-routes.ts`·`ExperimentsView.tsx`·`builtin-hooks.ts`·`blind-subject-packet.ts` 현재 상태를 읽고 갭을 확정한 뒤 계획 문서에만 반영한다. 코드·이슈·커밋은 이번 범위에서 만들지 않는다.
- 다중 저장소·과제 유형 비교 스위트 계획 정리 완료: `docs/agent-lab.md` 15장에 `9단계 — 다중 저장소·과제 유형 비교 스위트`를 추가하고 17장 한계에 세 항목을 더했다. 현재 구현과 대조해 확정한 갭은 다섯 개다. (G1) `ExperimentWorkspaceService.create()`가 등록 프로젝트의 현재 HEAD에서만 worktree를 만들어 외부 공개 저장소를 특정 commit에 고정할 경로가 없다. (G2) 과제가 `experiments.command` 자유 텍스트 하나뿐이라 네 관점의 시작 상태·정답 기준·금지 사항을 구분할 자리가 없다. (G3) 실행이 Variant마다 수동 POST이고 `design.repetitions`는 저장만 될 뿐 소비되지 않아 배치 실행이 없다. (G4) 내장 훅이 `diff_stats`·`git_diff_check` 둘뿐이라 완성도를 LLM rubric에만 의존하고 테스트 통과 여부를 재지 못한다. (G5) 비교 화면이 실험 하나 안의 Variant만 보여 규모×유형×비교군 매트릭스를 볼 상위 개체가 없다. 대응으로 저장소 fixture(bare mirror 캐시 + pinnedCommit + 규모는 용량이 아닌 LOC 기준), `taskKind` 4종(greenfield는 빈 worktree이므로 저장소 규모 대신 명세 규모로 대체, security는 정적 식별·수정까지만), arm 교차 순서의 실행 계획 큐(전역 동시 1개는 CPU 경합에 의한 시간 지표 오염을 막으려 유지), fixture 선언 `testCommand`만 실행하는 결정적 검사 훅, `suite → experiment(fixture×taskKind) → variant → run` 계층과 집계표를 정의했다. 조합은 `4 유형 × 3 규모 × 4 비교군 × 3 반복 = 144 run`으로 순차 실행 시 평가를 빼고도 40시간을 넘기므로 전면 실행을 목표로 두지 않고 중형 1개 파일럿 → 규모 확장 → 본실험 3단계로 나눴고, 기본 예산 1,800초가 대형에서 대부분 `time_budget`으로 끝나는 문제 때문에 규모별 1,800/3,600/7,200초를 출발점으로 제시했다. 한계에는 공개 저장소가 학습 데이터에 포함돼 upstream 수정 재현이 능력 측정이 아닐 수 있다는 오염 위험, 대형 저장소에서 탐색 비용이 조건 차이를 덮는 문제, 보안 관점의 실행 금지 범위를 명시했다. `blind-subject-packet`의 untracked 산출물 포함은 다른 세션이 이미 작업 중이라 9단계에서 다시 만들지 않고 선행 조건으로만 참조했다. 사용자 지시대로 계획 문서만 고쳤고 코드·이슈·커밋은 만들지 않았다.
- 비교군 다변화·조건부 권고 산출로 실험 설계 재조정 착수: 사용자가 "지금 비교군이 너무 단일한데 실제로는 다양한 상황에서 쓰인다. 그 다양한 상황을 제한적으로나마 재현해서 '어디엔 뭐가 좋더라, 일반적으론 이 스킬셋을 쓰면 좋더라'가 나와야 한다"고 목표를 정정했고, 세부 조절은 Claude에게 위임했다. 또 "대형 레포라고 다 되는 건 아니다"라고 fixture 선정의 현실 제약을 지적했다. 이에 따라 9단계를 (1) fixture 적격성 게이트, (2) 상황 축과 부분요인 설계, (3) 구성 프로필(비교군) 카탈로그, (4) 결과를 조건부 권고로 바꾸는 산출 규칙 중심으로 다시 정리한다. 코드·이슈·커밋은 이번에도 만들지 않는다.
- 비교군 다변화·조건부 권고 산출로 실험 설계 재조정 완료: `docs/agent-lab.md` 9단계를 8개 소절로 다시 짰다. 서두에 이 단계의 산출물이 실행 표가 아니라 "어떤 상황에 어떤 구성이 낫고 일반적으로 권할 기본 스킬셋은 무엇인가"를 적은 플레이북임을 명시하고, 갭 표에 G6(결과를 조건부 권고로 바꾸는 규칙 부재)을 추가했다. 사용자의 "대형 레포라고 다 되는 건 아니다" 지적은 9-1 적격성 게이트 6종(오프라인 재현, 기준선 녹색, 검사 10분 이내, 도구 무관성, 라이선스, 과제 성립)으로 명문화하고 대형 칸은 "가장 유명한 저장소"가 아니라 "게이트를 통과하는 것 중 가장 큰 것"으로 채우도록 순서를 고정했다. 9-3에 상황 축 네 개(과제 유형·규모·명세 명확도·생태계)를 두되 완전요인 48셀이 성립하지 않으므로 S1×S2 12셀을 격자로 삼고 S3·S4는 균형 배치하는 부분요인으로 줄였다. 9-4에 구성 프로필 카탈로그 6종(P0 clean, P1 installed, P2 task-kit, P3 self-review, P4 decompose, P5 more-thinking)을 정의했고, 특히 P5를 비용 대조군의 중심으로 삼았다 — 스킬·루프를 얹은 구성은 거의 항상 토큰을 더 쓰므로 대조군 없이 이기면 구성의 효과가 아니라 추가 토큰의 효과를 측정한 것이 되며, 근거는 7단계에 기록된 #219의 2.63배 토큰 패배 사례다. `parseExperimentVariantConfig()`가 isolated_overlay를 single 하네스로만 허용하고 같은 comparisonId의 비스킬 조건 동일성을 강제하므로 프로필을 스킬 축 그룹과 하네스 축 그룹으로 나누고 P0를 양쪽에 넣어 상대값으로만 잇도록 했으며, 그룹을 가로지르는 직접 순위는 확증 등급으로 올리지 않게 제한했다. 9-7에는 셀 승자 결정 순서(결정적 검사 → rubric → 비용 효율 → 무차별)와 권고 등급 3단계(확증·잠정·무차별), 일반 권고를 낼 수 있는 세 조건을 넣고 "상황에 따라 순위가 뒤집히면 일반 권고를 내지 않는다"를 규율로 고정했다. 9-8은 전면 실행 대신 스크리닝 28 run → 격자 32 run → 확증 54 run의 단계별 축소 설계로 바꾸고 각 단계의 중단 기준과 "대형은 3단계에서만 사용"을 명시했다. 사용자 지시대로 계획 문서만 고쳤고 코드·이슈·커밋은 만들지 않았다.
- 실험 기본 모델 등급 정책 확정: 사용자가 "아까 돌려보니 모델이 하이쿠더라. 기본적으로 가성비라 불리는 모델을 쓰도록. 하이쿠는 좀 아니지 않냐"고 지적했다. Claude가 코드를 확인한 결과 Haiku는 하드코딩이 아니라 실험 UI의 Model 칸이 빈 값이면 계정 CLI 기본값을 그대로 상속하는 구조였고(`ExperimentsView.tsx`의 model 초기값은 빈 문자열, evaluator 모델 칸도 동일), `claude-print-runtime.ts`는 `--model`·`--effort`를, `codex-exec-runtime.ts`는 `--model`·`model_reasoning_effort`를 설정값 그대로 CLI에 넘긴다. 모델 등급·가격은 기억으로 답하지 않고 claude-api 스킬을 로드해 확인했다. `docs/agent-lab.md` 9-4에 모델 정책을 추가했다 — 모델은 통제 변수로 여섯 프로필이 모두 동일 모델을 쓰고 P5만 추론 강도를 올리며, 기본은 가성비 등급(Claude `claude-sonnet-5`, Codex는 frontier 기본값 gpt-5.5 아래 중간 등급을 `/model` 메뉴 실측으로 확정 — 추측하지 않고 TODO로 남김), 최상위 `claude-opus-5`는 품질 상한 확인용 별도 축으로만 쓴다. Haiku 4.5를 피험 모델에서 제외한 근거는 두 가지다. (1) 컨텍스트가 200K로 Sonnet 5·Opus 5의 1M 대비 5분의 1이라 9-3의 규모 축(S2)에서 중형·대형 저장소를 다룰 때 구성의 차이가 아니라 모델의 상한을 측정하게 되어 규모 축 자체가 무의미해진다. (2) effort 파라미터를 지원하지 않아 P5 more-thinking 대조군이 성립하지 않고, `parseExperimentVariantConfig()`가 isolated_overlay에 모델·추론 강도 명시를 강제하는데 Haiku에는 넣을 값이 없다. 가격표(Opus 5 $5/$25, Sonnet 5 $3/$15·2026-08-31까지 인트로 $2/$10, Haiku 4.5 $1/$5)를 함께 넣어 인트로 기간 중 Sonnet은 Haiku 대비 비용 2배에 컨텍스트 5배라는 교환을 명시했다. evaluator 모델도 빈칸이면 CLI 기본값을 상속해 공급자가 기본 모델을 바꾸면 같은 rubric 점수가 조용히 달라지므로 고정하고 provenance에 남기도록 했다. 계획 문서만 고쳤고 UI 기본값 코드 변경은 Codex 중간 등급 ID 확정 후로 미뤘다.
- 한도 대기 재사용·실작업 시간 지표 반영: 사용자가 "WAM에는 한도일 때 기다렸다 재시작하는 게 있으니 그걸 활용해라. 테스트 기준 중 시간은 토큰 대기 시간을 제외하라"고 지시했다. Claude가 기존 구현을 확인해 `RateLimitResumeService`(`src/server/services/rate-limit-resume.ts`)와 `usage-monitor.ts`가 계정별 60초 주기로 갱신하는 `usage_status` 테이블을 재사용 대상으로 확정했다. 그대로 쓰는 것은 `usage_status`(reset_at·remaining_percent의 정본), `parseResetTime()`, `isRateLimitRecovered()`(예정 시각 경과 또는 잔여 10% 이상) 세 가지이고, 바꿔 끼우는 것은 `resumeChat()` 하나다 — 이 함수는 tmux 채팅에 "계속"을 보내는데 실험 run은 비대화형 자식 프로세스라 해당 경로가 없어, 이미 양쪽 런타임에 구현돼 있는 `AgentRuntime.resume()`과 원장의 provider session ID로 대체한다. 별도 예약 타이머 없이 폴링으로 판단하는 원래 방식은 서버 재시작에 강해서 그대로 따랐고, run 상태도 새로 만들지 않고 6장의 `running → paused → running` 전이를 재사용했다. `docs/agent-lab.md`에 9-6 `공급자 한도 대기와 재개`를 신설하고 기존 9-6~9-8을 9-7~9-9로 밀었으며 갭 표와 본문의 상호 참조 4곳을 함께 맞췄다. 설계에서 확인한 두 전제 조건도 명시했다. (1) 지금은 한도·컨텍스트 초과·실행 오류가 모두 `runtime_error`로 뭉개져 대기 대상을 골라낼 수 없으므로 `provider_limit`·`context_exceeded` 분리가 선행돼야 한다. (2) 대기 중인 run이 전역 동시 1개 슬롯을 점유하면 큐가 몇 시간 멈추므로 대기 진입 시 슬롯을 반납하고 재개 시 다시 잡되 worktree·체크포인트는 보존하며, 같은 계정 후속 항목은 어차피 같은 한도라 함께 보류하고 다른 계정·공급자는 진행시킨다. 시간 지표는 `실작업 시간 = 벽시계 - 누적 한도 대기 시간`으로 정의해 9-8 비교표의 기본 열로 올렸고, 대기 시간은 운영 정보로만 남기고 셀 승자 결정에서 제외했다 — 대기를 포함하면 그날 계정 사용량에 따라 "밤에 돌린 arm이 느린 arm"이 되어 순위가 뒤집히기 때문이다. 같은 정의를 예산에도 적용해야 함을 전제 조건으로 명시했다. 현재 `RuntimeBudgetPolicy`는 `Date.now() - startedAtMs`를, `SingleHarness`는 실행 시작 시점의 `setTimeout(maxSeconds × 1000)`을 쓰므로 대기를 모르고, 고치지 않으면 한도 대기 중 `time_budget`으로 run이 죽어 대기 기능 자체가 성립하지 않는다. 17장 한계에는 실패 원인이 뭉개지는 문제와 예산 검사가 사후 판정이며 `totalTokens`가 null이면 토큰 예산을 조용히 건너뛴다는 점을 추가했다. 계획 문서만 고쳤고 코드·이슈·커밋은 만들지 않았다.
- untracked 산출물 평가 포함 구현 검수·커밋: 다른 세션이 작업하던 `docs/agent-lab.md` 7단계 첫 항목(평가 packet의 tracked 전용 한계 해소)이 구현된 채 커밋되지 않고 남아 있었고, 사용자가 "다른 세션 다 죽었으니 변경사항 확인해서 커밋하라"고 지시해 Claude가 내용을 검수한 뒤 커밋했다. 변경은 일관됐다 — `git-change-snapshot.ts`를 새로 만들어 `collectGitChangeSnapshot()`으로 tracked diff와 untracked 일반 파일을 한 스냅샷으로 모으고, 중복 구현이던 `blind-subject-packet.ts`의 인라인 `git diff`와 `builtin-hooks.ts`의 자체 `parseNumstat()`을 모두 이 함수로 대체했다. 안전장치로 untracked 파일 200개·총 4MiB 상한, symlink·비일반 파일 거부, binary 파일의 추가 줄 수 제외, `git diff --no-index`의 차이 있음 exit 1을 정상 결과로 변환하는 처리가 들어 있다. `diffCoverage`는 `tracked-only`에서 `tracked-and-untracked`로 바뀌었다. 이로써 `#219`에서 확인된 측정 한계 — control이 fixture에 없는 pytest를 새로 도입해 실행에 실패했는데도 두 judge가 그 새 파일을 보지 못한 것 — 이 해소된다. `npm run verify`에서 타입 검사, 82개 파일 561개 테스트, production 빌드와 공개 경로 검사가 모두 통과했다. 커밋 전 규칙대로 `CODETREE.md`에 `git-change-snapshot.ts` 행을 새로 넣고 `builtin-hooks.ts`·`blind-subject-packet.ts`·`tests/builtin-hooks.test.ts` 설명을 tracked·untracked로 갱신했다. 남은 사소한 불일치로 `BlindSubjectPacket.trackedDiff` 필드명이 이제 untracked 내용까지 담는데 이름은 그대로다 — `diffCoverage`가 실제 범위를 명시하고 있고 이름 변경은 rubric 프롬프트·테스트까지 파급되므로 이번 커밋에서는 바꾸지 않았다.
- 실험 예산을 실작업 시간 기준으로 전환: 사용자 지시("미구현인 것들 진행해")에 따라 9-6 한도 대기의 선행 조건을 구현했다. `ActiveClock`을 `agent-runtime.ts`에 새로 두고 `pause()`/`resume()`으로 대기 구간을 누적해 `activeSeconds()`에서 제외한다. `RuntimeBudgetPolicy`는 두 번째 인자로 기존 `startedAtMs` 숫자와 `ActiveClock`을 모두 받게 해 기존 호출부와 테스트를 그대로 유지했고, `observe()`의 `elapsedSeconds`를 실작업 시간으로 바꿨으며 타이머 재무장을 위한 `remainingActiveMs()`를 추가했다. `SingleHarness`와 `GraphHarness`는 run마다 `ActiveClock` 하나를 실행 상태에 들고 예산 타이머를 `budget.remainingActiveMs()`로 건다. 이 변경 전에는 `Date.now() - startedAtMs`와 실행 시작 시점의 `setTimeout(maxSeconds × 1000)`이라 한도 대기를 붙이면 대기 중에 `time_budget`으로 run이 죽어 대기 기능 자체가 성립하지 않았다. 회귀 테스트로 10초 작업 후 1시간 대기하고 재개해도 실작업 시간이 10초로 유지되고 남은 예산이 50초 그대로이며 상한은 실작업 60초에 닿을 때만 발화하는 것, `pause()` 중복 호출이 대기를 이중 계산하지 않고 멈춘 적 없는 `resume()`이 무효인 것을 고정했다. 아직 `pause()`를 호출하는 곳은 없으므로 현재 동작은 이전과 동일하며, 실제 대기·재개 연결은 다음 단계다.
- 공급자 한도 실패를 provider_limit으로 분리: 9-6 한도 대기의 두 번째 선행 조건을 구현했다. 그동안 한도·컨텍스트 초과·실제 실행 오류가 모두 `runtime_error`로 뭉개져 무엇을 대기시킬지 고를 수 없었다. `ExperimentTerminationReason`에 `provider_limit`을 추가하고 두 하네스에 선택적 `isProviderLimited` 훅을 주입해, 종료 이유가 `runtime_error`일 때만 계정 상태를 확인해 좁히도록 했다. 판정 근거로 CLI 오류 문구를 새로 파싱하지 않은 것이 핵심 결정이다 — 기존 한도 감지(`session-manager.ts`의 `registerRateLimitWait`)는 tmux 화면 스크래핑 기반이라 비대화형 실험 런타임에는 적용할 수 없고, 새 문자열 패턴을 지어내면 오탐이 난다. 대신 usage-monitor가 계정별 60초 주기로 갱신하는 `usage_status`를 그대로 보고, 잔여 사용량이 완전히 0이면서 `isRateLimitRecovered()`가 거짓일 때만 한도로 판정한다. 잔여가 null이거나 0보다 크면 한도로 보지 않는데, `reset_at`은 정상 계정에서도 미래 시각이라 `isRateLimitRecovered()`만으로는 건강한 계정을 한도로 오분류하기 때문이다. 회귀 테스트로 한도일 때만 `provider_limit`으로 바뀌고, 아닐 때는 `runtime_error`가 유지되며, 한도 조회 자체가 예외를 던져도 run 종결을 막지 않고, 정책 차단·사용자 취소는 한도 조회를 아예 하지 않는 것을 고정했다. 알려진 한계로 graph 하네스는 primary 계정만 조회하므로 secondary 계정에서 난 한도는 분류되지 않는다. `context_exceeded`는 CLI가 실제로 내보내는 신호를 아직 확인하지 못해 추측 매핑을 만들지 않고 남겨두었다. `npm run verify`에서 82개 파일 565개 테스트와 빌드가 통과했다.
- 공급자 한도 대기·재개 구현: 사용자가 "토큰 오염 감수하고 진행하라"고 결정해 9-6 본체를 구현했다. 앞서 제시한 A안(같은 공급자 세션에 "계속"을 보내 이어붙이기)과 B안(대기 후 처음부터 재실행) 중 A안을 택한 것이며, 이어붙인 run은 한 번에 끝낸 run과 프롬프트 캐시·턴 구조가 달라 토큰 지표가 오염된다는 것을 사용자가 알고 감수했다. 그래서 오염 여부를 사후에 판별할 수 있도록 `experiment_runs`에 `waited_seconds`·`wait_count` 컬럼을 추가하고(기존 DB는 ALTER TABLE 마이그레이션), `run.waiting`·`run.resumed`·`run.wait_abandoned` 이벤트를 원장에 남긴다. `SingleHarness`의 실행부를 재시도 루프로 바꿔, 종료 이유가 `provider_limit`이고 대기 정책과 저장된 공급자 session ID가 모두 있을 때만 `ActiveClock.pause()` → run `paused` → 대기 → `resume()` → run `running` → `runtime.resume(providerRunId, prompt:"계속")` 순서로 이어간다. 대기 상한은 run당 3회이며 넘으면 `provider_limit`으로 끝낸다. 대기 구간은 예산 타이머를 해제했다가 재개 시 `budget.remainingActiveMs()`로 다시 걸어, 4시간을 기다려도 `time_budget`으로 죽지 않는다. `ExperimentService`는 `usage_status`를 60초 간격으로 최대 6시간까지 폴링하며 회복되면 true를 반환하고, 대기 중인 run을 `waitingOnLimit`에 넣어 `operationCount()`에서 빼므로 전역 동시 1개 슬롯을 점유하지 않는다 — 대기하는 run 때문에 다른 계정·공급자 작업까지 몇 시간 멈추는 것을 막기 위해서다. 취소 신호가 오면 폴링이 즉시 깨고 재개하지 않는다. 회귀 테스트로 한도 실패가 대기 후 같은 session ID에 "계속"으로 재개돼 완료되는 것, `waitCount`가 1이고 `run.waiting`·`run.resumed`가 남는 것, 대기 정책이 없거나 회복되지 않으면 `provider_limit`으로 끝나는 것을 고정했다. graph 하네스는 이번 범위에서 제외했다(노드별 재개 지점이 달라 별도 설계가 필요). `npm run verify`에서 82개 파일 567개 테스트와 빌드가 통과했다.
- 저장소 fixture·과제 유형 데이터 모델과 mirror 작업공간 구현: 9단계 G1·G2를 구현했다. `experiment_fixtures` 테이블(이름·URL·40자 고정 commit·규모·언어·라이선스·LOC·준비/검증 명령·게이트 상태·mirror 경로)과 `experiments.task_kind`·`experiments.fixture_id`를 추가했고 기존 DB는 ALTER TABLE로 마이그레이션한다. 검증에서 내린 결정 두 가지가 있다. (1) 준비·검증 명령을 shell 문자열이 아니라 **argv 배열**로만 저장한다 — 9-7 결정적 검사 훅이 shell 없이 execFile로 실행해야 하고, 문자열로 받으면 `pytest -q && rm -rf /` 같은 값이 그대로 들어온다. (2) 기준 commit은 40자 전체 SHA만 받는다 — 짧은 SHA는 저장소가 커지면 모호해져 재현성이 깨진다. URL은 https·file만 허용해 ssh·git 프로토콜을 배제했다. `ExperimentFixtureStore`는 fixture별 bare mirror를 한 번만 clone해 캐시하고, 기준 commit이 없으면 한 번 fetch한 뒤에도 없으면 거부하며, run마다 그 mirror에서 고정 commit의 detached worktree를 만든다. greenfield용 빈 Git 작업공간과 앱 관리 경로 밖 제거 차단도 같은 서비스에 뒀다. 검증은 가정하지 않고 실제 Git 저장소(file:// 원본)를 만들어 mirror 생성·재사용, worktree의 HEAD가 고정 commit과 일치하는지, 파일 내용이 실제로 체크아웃됐는지, 없는 commit 거부, 중복 키 거부, 관리 경로 밖 제거 차단을 모두 돌려 확인했다.
- 비교 화면의 시간 지표를 실작업 시간으로 교체: 사용자가 "예상하지 말고 실제 화면이랑 확인해서 진행하라"고 지시해 Playwright로 실험실 화면을 실제로 띄워 확인했고, 속도 열과 DURATION 카드가 여전히 `wall-clock`을 표시하고 있어 9-8 정의와 어긋나는 것을 발견했다. `formatRunDuration()`이 벽시계에서 `waitedSeconds`를 빼도록 바꾸고, 대기가 있었던 run은 `실작업 · 한도 대기 N회 M초 제외`로 근거를 함께 보여준다 — 대기 표시는 토큰 지표 오염 가능성(이어붙인 run)을 판단하는 단서도 된다. e2e 목 데이터에 벽시계 60초·대기 20초 run을 넣어 화면에 40초로 나오는 것을 실제 렌더링으로 확인했다. `npm run verify`에서 83개 파일 573개 테스트와 빌드가 통과했다.
- 결정적 검사와 fixture 실행 경로 연결: 9단계 G4를 구현하고 fixture를 실제 run에 물렸다. `runDeterministicCheck()`는 fixture가 선언한 argv 명령만 `shell: false`로 실행하며, 결과를 네 상태로 나눈다 — 통과, 실패(종료 코드 보존), 미실행(명령 없음), error. error를 따로 둔 이유는 실행 파일이 없거나 시간 초과로 죽은 것은 fixture 환경 문제이지 에이전트 산출물의 결함이 아니어서, 이를 실패로 집계하면 구성 비교가 오염되기 때문이다. 결과는 `experiment_runs`의 `check_status`·`check_exit_code`·`check_duration_ms`·`check_output` 컬럼에 저장해 이후 집계에서 조회할 수 있게 했다. `ExperimentService.startVariant()`는 이제 실험 대상을 셋으로 나눈다 — fixture가 지정되면 고정 commit의 mirror worktree, `taskKind`가 greenfield면 빈 Git 작업공간, 둘 다 아니면 기존처럼 등록 프로젝트 HEAD. 적격성 게이트를 통과하지 않은(ready가 아닌) fixture는 실행을 거부하고, 빈 작업공간은 기준 commit이 없으므로 commit 동일성 검사를 건너뛴다. run이 completed로 끝나면 fixture의 `testCommand`를 worktree에서 실행해 결과를 기록하며, 검사 자체가 실패해도 run 결과는 뒤집지 않는다. 검증은 실제 명령 실행으로 확인했다 — 종료 코드 0/3에 따른 통과·실패, 없는 명령의 error, 300ms 타임아웃의 시간 초과 error, 그리고 `&&`·`rm`을 인자로 넘겨도 shell이 없어 해석되지 않고 canary 파일이 살아남는 것까지 확인했다. 화면은 Playwright로 실제 렌더링해 비교표에 `검증` 열(통과 12초 / - 검증 없음)과 run 상세의 CHECK 타일이 나오는 것을 확인했다. `npm run verify`에서 84개 파일 577개 테스트와 빌드가 통과했다.
- 실행 계획 큐와 스위트 집계·권고 구현: 9단계 G3·G5·G6을 구현했다. `experiment_run_plans`·`experiment_run_plan_items` 테이블을 두고, 계획을 만들 때 `Variant × 반복`을 **회전 교차** 순서로 미리 펼친다 — 반복 회차마다 시작 arm을 한 칸씩 밀어 A B C / B C A / C A B가 되며, 무작위가 아니라서 순서 효과를 없애면서도 순서를 재현할 수 있다(8장 공정성 규칙의 "교차"를 코드로 고정). `ExperimentService.startRunPlan()`이 큐를 백그라운드에서 하나씩 소비하며, 전역 동시 실행 1개 제한에 막히면 항목을 queued로 되돌리고 5초 뒤 다시 시도하고, 중간 실패는 항목에만 기록해 큐를 멈추지 않는다. 집계는 `suite-summary.ts`에 순수 함수로 뒀다. 검사 통과율 분모에서 `skipped`(명령 없음)와 `error`(fixture 환경 문제)를 빼는 것이 핵심 결정이다 — 환경 문제를 실패로 세면 구성 비교가 오염된다. 시간 중앙값은 벽시계가 아니라 대기를 뺀 실작업 시간을 쓴다. 권고는 9-8의 순서를 코드로 고정해 `결정적 검사 → rubric(0.05 여유) → 토큰(10% 여유) → 무차별`로 승자를 정하고, 양쪽 모두 완료 3회 이상일 때만 `확증`, 그 아래는 `잠정`으로 등급을 낮춰 관찰값임을 문장에 명시한다. API는 `GET /api/experiments/:id/summary`, 실행 계획 생성·조회·취소, fixture 등록·조회를 관리자 전용·감사 로그로 추가했다. 화면은 Playwright로 실제 렌더링해 권고 패널(`확증` 배지 + 승자 + `기준 결정적 검사 · 기준선 대비 토큰 1.27배` + 근거 문장)이 비교표 위에 나오는 것을 확인했다. 권고 패널이 승자 이름을 표시하면서 기존 e2e 단언과 충돌해 표 범위로 한정했다. `npm run verify`에서 85개 파일 586개 테스트와 빌드가 통과했다.
- 실험실 화면에 과제 유형·대상 저장소·계획 실행 노출과 배너 정정: 새 실험 폼에 과제 유형(유지보수·새로 만들기·기능 추가·보안)과 대상 저장소 선택을 추가했다. 저장소 목록은 적격성 게이트를 통과한(ready) fixture만 보여 준비되지 않은 대상을 고를 수 없게 했고, 선택하지 않으면 기존처럼 등록 프로젝트를 쓴다. 실험 목록 항목에 과제 유형과 `대형 · django` 같은 규모·이름 태그를 달아 어떤 상황의 결과인지 목록에서 바로 보이게 했다. 설계 카드에 `계획 실행 · N회 교차` 버튼을 붙여 설계에 적어둔 반복 횟수만큼 arm 회전 교차 큐를 시작할 수 있게 했다. 상단 배너는 실제 구현과 어긋나 있어 실제 화면을 보고 고쳤다 — `현재 실행 가능`에 fixture·과제 유형·검증 명령·교차 실행 계획·한도 대기 재개를 넣고, `다음 단계`에서 이미 구현한 자동 반복을 빼고 graph 한도 대기와 신뢰구간 통계를 넣었으며, `비교 주의`에 "시간은 한도 대기를 뺀 실작업 기준이며 대기 후 재개한 run은 토큰 지표가 오염될 수 있다"는 경고를 추가해 사용자가 감수하기로 한 오염을 화면에서 드러냈다. Playwright로 실제 렌더링해 태그·버튼·경고가 모두 보이는 것을 확인했다. `docs/agent-lab.md` 16장의 구현 결과에 9단계에서 완료한 여섯 항목을 추가하고 남은 범위를 graph 한도 대기·`context_exceeded` 감지·pairwise·신뢰구간 통계 등으로 갱신했다. `npm run verify`에서 85개 파일 586개 테스트와 빌드가 통과했다.
- 남은 실험실 기능 일괄 구현(신뢰구간·graph 한도 대기·컨텍스트 초과·쌍대 비교)과 스킬 노출: 사용자가 "남은 것도 다 하라", "해당 기능을 스킬에도 포함해 에이전트가 쓸 수 있게 하라"고 지시해 9단계 잔여 항목을 모두 구현했다. (1) 신뢰구간: `wilsonInterval()`을 넣고 확증 등급에 구간 비겹침 조건을 추가했다. 이 과정에서 계산으로 확인한 사실이 있다 — 표본 3회는 3승 0패 대 0승 3패여도 95% Wilson 구간이 0.438 대 0.562로 겹쳐 확증이 원천적으로 불가능하다. 완전 분리가 가능한 최소가 4회라 `CONFIRMATION_MIN_SAMPLES`를 4로 올리고 문서 9-8의 "3회 이상"과 9-9의 확증 단계 표본 수(54→72 run), 완전 격자 계산(252→336 run)을 함께 고쳤다. 문서의 기존 규칙이 자기모순이었던 것을 계산으로 잡아낸 사례다. (2) graph 한도 대기: node 단위로 providerRunId를 잡아 한도 실패 시 대기 후 같은 세션을 "계속"으로 재개하도록 single과 같은 패턴을 넣었고, node별 `node.waiting`·`node.resumed`·`node.wait_abandoned` 이벤트를 남긴다. (3) `context_exceeded`: 종료 이유를 추가하고 `failure-classification.ts`에서 오류 문구의 보수적 표식 목록으로 분류하되, 한도보다 먼저 본다(확정적 신호이므로). 표식이 실제 초과 상황의 라이브 출력이 아니라는 점을 코드 주석과 문서에 명시했다. (4) 쌍대 비교: A/B와 B/A 두 순서를 모두 물어 순서를 바꿔도 같은 후보를 고른 쌍만 승리로 세고, 두 순서에서 각각 앞자리를 고른 쌍은 위치 편향으로 분리해 승패를 주지 않는다. 한쪽 순서만 있는 판단과 무승부도 승패에서 제외한다. (5) 스킬 노출: `AgentBridge`에 `experiment.summary`·`experiment.list`·`experiment.plans`·`experiment.plan_start`·`experiment.plan_cancel`·`experiment.fixtures`·`experiment.fixture_create` 메서드를 추가하고 같은 이름의 MCP 도구 5종을 등록했으며, `skills/web-agent-manager-experiment/SKILL.md`를 새로 써 `AGENT_SKILL_NAMES`에 등록해 실제로 설치되게 했다. 스킬에는 확증에 최소 4회가 필요한 이유, 등급을 임의로 올리지 말 것, 대기한 run의 토큰 오염과 검사 통과율 분모에서 빠지는 표본을 해석 시 주의할 것, 실험 생성·프리셋 승격은 관리자 확인이 필요해 스킬로 하지 않는다는 경계를 명시했다. 스킬 추가로 깨진 설치 테스트 두 개는 하드코딩된 스킬 이름 목록과 개수를 `AGENT_SKILL_NAMES`에서 파생하도록 고쳐 다음 스킬 추가에도 깨지지 않게 했다. `npm run verify`에서 87개 파일 599개 테스트와 빌드가 통과했다.
- 최종 잔여 항목 구현(CLI 실측·graph overlay·preset 고정): 사용자 지시로 남은 세 항목을 모두 처리했다. (1) `context_exceeded` 표식 실측: 추측 대신 설치된 CLI 번들을 직접 뒤져 확정했다. Claude Code 2.1.232에서 `context_window_exceeded`, `model_context_window_exceeded`, `prompt is too long`, ``input length and `max_tokens` exceed context limit``을, Codex 0.146.0에서 `context_window_exceeded`, `context length`를 확인해 표식 목록을 실측값으로 교체했다. **이 과정에서 실제 버그를 하나 찾았다** — Claude 번들의 error 계열 result subtype은 `error`, `error_during_execution`, `error_max_budget_usd`, `error_max_structured_output_retries`, `error_max_turns` 다섯인데, `claude-print-runtime.ts`는 존재하지 않는 `error_max_budget`을 찾고 있어 CLI 자체 예산 초과가 `cost_budget`이 아니라 `runtime_error`로 기록되고 있었다. 정확 문자열 검색으로 `error_max_budget` 0건, `error_max_budget_usd` 1건임을 확인한 뒤 두 이름을 모두 받도록 고치고 실측 subtype 다섯 개를 회귀 테스트로 고정했다. (2) graph 공급자별 overlay: 제약의 실제 원인은 `startVariant()`가 primary 공급자로 만든 overlay 하나를 primary·secondary 런타임에 그대로 공유하던 것이었다. 공급자별로 별도 UUID 키의 bundle을 만들어 각 런타임에 자기 것만 주도록 바꾸고, native 디렉터리 수집도 공급자별로 분리했으며, 정리 경로와 환경 스냅샷(`providerBundles`)도 함께 고쳤다. `parseExperimentVariantConfig()`의 single 전용 제약은 SessionStart 주입에만 남겼다 — 그것만 Claude 전용 plugin hook이기 때문이다. (3) 채팅 preset 고정: `chats`에 `preset_version_id`·`preset_config_json`을 추가하고 채팅 생성 시 `presetId`(활성 버전) 또는 `presetVersionId`로 고른 버전의 설정 스냅샷을 행에 복사한다. 다른 프로젝트의 preset과 공급자가 다른 preset은 거부한다. 회귀 테스트로 고정 후 활성 버전을 v2로 올려도 이미 만든 채팅이 v1을 유지하는 것을 확인했다. 검증 완화로 깨진 기존 테스트(graph+strict overlay 거부)는 새 동작에 맞춰 SessionStart 조합 거부와 native overlay 허용을 함께 검증하도록 바꿨다. `npm run verify`에서 88개 파일 607개 테스트와 빌드가 통과했다.
- 스위트 롤업 구현과 caveman 상황별 실측: 사용자가 "애초에 랩 요구조건이 그거 감안해서 여러 조건으로 돌려보는 것"이라고 지적했다. 맞는 지적이었다 — 앞선 caveman 테스트를 한 과제 × 두 arm × 1회, 즉 셀 하나만 돌려놓고 "과제를 바꿔 다시 돌릴까요"라고 되물은 것은 9-3 상황 축을 세운 이유 자체를 무시한 실행이었다. 확인해보니 더 근본적으로, 셀을 여러 개 돌려도 묶어 볼 수단이 없었다 — `summary`는 실험 하나 단위이고 문서 9-7의 `experiment_suite`는 구현돼 있지 않았다. 그래서 `experiment_suites` 테이블과 `experiments.suite_id`, `rollupSuite()`, `suiteSummary()`, 브리지 `experiment.suite_summary`를 추가했다. 롤업은 확증 등급만 우열 근거로 세고, 한 arm이 어떤 셀에서 이기고 다른 셀에서 지면 일반 권고를 내지 않고 역전 지점을 그대로 적는다. 그 위에서 같은 두 arm(clean, clean+caveman)을 산출물 형태가 다른 두 상황에서 각각 2회 반복 교차로 총 8 run 실행했다. 결과는 대화형 설명(파일 없음)에서 clean 57,160 / caveman 59,374 총토큰에 출력 토큰 1,348 대 631(-53%)로 `indistinguishable`, 파일 산출에서 clean 182,834 / caveman 121,833 총토큰(-33%)에 출력 2,656 대 1,951(-27%)로 `tentative`였다. 앞선 1회 실행에서 "caveman이 토큰을 더 쓴다"고 본 것은 표본 1의 잡음이었고, 2회로 늘리자 파일 산출 셀에서 반대 방향이 나왔다. 일관된 것은 출력 토큰 감소뿐이며 총 토큰은 변동이 커서, 랩은 두 셀 모두 확증을 주지 않고 일반 권고도 내지 않았다 — 설계대로 동작한 것이다.
- 실험실 실행이 채팅 목록을 덮는 문제 수정: 사용자가 "랩에서 돌린 건 채팅창에 안 나오게 해달라"고 요청했다. 확인 결과 랩 run 10건이 모두 채팅으로 등록돼 있었다(#262~#271). 원인은 `HistorySynchronizer`가 실험 worktree에서 돈 Claude 세션 JSONL을 일반 세션과 똑같이 스캔해 채팅을 만들던 것이다. 실행마다 worktree가 하나씩 생기므로 비교 한 번에 `arm 수 × 반복 수`만큼 채팅이 쌓여 사람이 연 채팅이 묻힌다. `isLabWorkspace()`를 새로 두고 세션 cwd가 앱 관리 경로(`dataDir/experiment-worktrees`, `dataDir/evaluation-workspaces`) 안이면 채팅을 만들지 않고 건너뛰게 했다. 결과는 실험 원장에 이미 남으므로 채팅으로 중복 보존할 이유가 없다. 채팅 전용 worktree(`data/git-worktrees/...`)는 사람이 여는 작업이라 계속 채팅으로 남는 것을 테스트로 고정했다. 이미 등록된 10건은 앱의 기존 삭제 경로(`SessionBackupService.deleteChat`)로 정리했고, 재빌드·재시작 뒤 45초를 기다려 재등록이 0건임을 실측했다. 채팅은 43개에서 33개가 되었다. `npm run verify`에서 88개 파일 611개 테스트와 빌드가 통과했다.
- 실험 작업공간 자동 정리와 과제 규모 확대: 사용자가 "테스트 끝났으면 워크트리도 정리해야지", "테스트 끝나면 자동 정리가 되어야지", "테스트가 너무 짧아, 분석·생성 전부 너무 짧아"라고 연달아 지적했다. 셋 다 맞는 지적이었다. (1) 정리를 일회성 스크립트로 때우지 않고 기능으로 만들었다 — `ExperimentService.cleanupWorkspaces({experimentId, suiteId})`가 끝난 run의 worktree와 스킬 bundle(공급자별로 나뉜 것 포함)을 즉시 지우고, Git worktree로 등록되지 않은 fixture·빈 작업공간은 앱 관리 경로 제거로 넘어간다. **블라인드 평가가 걸린 run은 제외한다** — 평가가 피험 worktree의 diff를 읽기 때문이다. 실제로 정리했을 때 12개가 지워지고 평가가 붙은 2개만 남아 62MB가 148KB가 되었고, `git worktree list`에도 실험 worktree가 남지 않았다. (2) `startRunPlan`에 `cleanup` 옵션(기본 true)을 넣어 계획의 모든 항목이 끝나면 자동으로 정리하게 했다. 평가를 붙일 계획만 `cleanup:false`로 시작해 보존한다. 브리지 `experiment.cleanup`과 MCP 도구, 스킬 문서에도 이 규칙을 적었다. (3) 과제가 20~30초짜리라 스킬 효과가 잡음에 묻히던 문제는 읽을 양과 쓸 양을 모두 키워 해결했다 — 심층 분석(실험 모듈 전체를 읽고 의존 관계·실패 경로 추적·간섭 분석·취약점 3건과 재현 시나리오를 답변으로)과 대규모 생성(코드를 읽고 8개 절 이상의 운영 가이드 문서 작성) 두 셀을 arm당 4회(확증 최소 표본)로 총 16 run 구성했다. run당 소요가 20~30초에서 약 5분으로 늘었다. 함께 `experiment.suite_summary` MCP 도구와 스킬의 "셀 하나로 결론 내지 말 것" 지침도 추가했다.
- 실행 계획의 기준 commit 고정: 대규모 스위트 16 run 중 13건이 실패했고 원인은 Claude 자신이었다. 실행이 도는 동안 `953ed5d`를 커밋해 브랜치 HEAD가 바뀌자, 같은 실험의 기준 commit 동일성 검사가 남은 항목을 모두 거부했다("기존 6aa6aad18cf2, 현재 953ed5dcff16"). 검사 자체는 비교 조건을 지키는 옳은 동작이지만, 몇십 분 걸리는 계획이 커밋 하나에 통째로 깨지는 것은 설계 결함이다. `experiment_run_plans.baseline_commit`을 추가해 계획을 시작할 때 기준 commit을 고정하고(기존 run의 baseline이 있으면 그것, 없으면 현재 HEAD), `ExperimentWorkspaceService.create()`가 그 고정값으로 worktree를 만들도록 했다. 이제 실행 도중 브랜치에 커밋이 쌓여도 같은 계획의 모든 항목이 동일 조건에서 돈다. 회귀 테스트로 고정값 저장·조회와, 고정하지 않은 계획이 기존처럼 실행 시점 HEAD를 쓰는 것을 함께 확정했다.
- 비대화형 실행에서 Bash가 전부 거부되던 실험 오염 수정: 사용자가 "스킬을 직접 요청에 추가하는 건 아니지, 실제 스킬만 추가한 환경으로 테스트하는 거지"라고 물어 `activation` 설정을 다시 확인하다가 더 큰 결함을 찾았다. 실제 구현 과제(fixture의 실패 테스트 통과시키기)를 돌린 run들의 최종 답변이 모두 "테스트 실행 승인이 필요합니다"였고, 도구 이벤트를 열어보니 `node --test`가 매번 `This command requires approval`로 거부되고 있었다. 원인은 Claude `workspace-write`가 `acceptEdits` 권한 모드로 매핑되는데, 이 모드는 파일 편집만 자동 승인하고 Bash는 승인을 요구하며 `-p` 비대화형에는 승인할 주체가 없다는 것이다. 결과적으로 에이전트는 구현만 하고 검증은 전혀 못 한 채 같은 명령을 3~4회 반복 시도하다 끝났고, 그 재시도 낭비가 arm별로 달라 토큰 비교까지 오염됐다. `claude --help`와 번들 문자열로 `--allowedTools`와 `Bash(cmd:*)` 패턴 문법을 실측한 뒤, `RuntimePrepareInput.allowedCommands`를 추가해 fixture가 선언한 검증 명령만 도구 허용 목록에 올리도록 했다. 임의 Bash를 여는 것이 아니라 선언된 명령 하나만 여는 것이 핵심이다. 실측 검증 결과 승인 거부가 3건에서 0건이 되었고, 에이전트가 `cat`으로 코드를 읽고 `node --test`를 직접 돌려 TAP 출력을 받았으며, 토큰이 169K~200K에서 111K로 줄었다(재시도 낭비 제거). 17장 한계에 이 실측 내용을 추가했다. 사용자 질문에 대한 답은 "그동안 `session_start`를 써서 스킬 지시문을 주입하고 있었다"이며, 환경에만 설치하는 `native` 셀을 따로 돌려 비교하도록 스위트에 추가했다.
- 스킬 활성화 3경로 진단 실행과 결과: 사용자의 "스킬을 어떻게 주입해야 하나"라는 질문에 답하기 위해 `activation: "explicit"`(슬래시 명령 호출)을 구현하고, 트리거 문구를 처리군이 아니라 **공유 과제**에 넣은 3-셀 스위트를 arm당 4회, 총 24 run 실행했다. 트리거를 공유 과제에 넣은 이유는 처리군에만 넣으면 스킬과 프롬프트 두 변수가 동시에 바뀌기 때문이다. 결과는 활성화 강도가 단계적으로 드러났다 — 처리군/대조군 토큰비가 native 1.008, explicit 1.062, session_start 2.104였고, 최종 답변 문체도 native는 대조군과 동일, explicit은 백틱·조사가 빠지기 시작, session_start는 영어 전환과 콜론 구조로 완전히 압축됐다. 즉 caveman은 환경에 설치만 해서는 간결함 요구가 과제에 있어도 사실상 켜지지 않고, 슬래시 호출로는 약하게, 주입하면 확실히 켜진다. 그런데 켜질수록 비싸진다는 역설이 나왔다. session_start 셀의 토큰 구성을 분해하니 출력은 1,148 → 1,391(+21%)로 소폭인데 캐시 읽기 입력이 119,156 → 249,580으로 2.1배였고 도구 호출 수가 5회 → 9회로 늘었다. 스킬 본문이 매 턴 컨텍스트에 실리고 턴 자체가 늘어난 것이 원인이며, 답변을 줄여 아낀 양보다 주입 비용이 훨씬 크다. 완성도는 세 경로 모두 검사 통과 100%로 차이가 없었다. 결론적으로 이 코딩 과제에서 caveman은 어느 경로로도 쓸 이유가 없고, 랩은 native·explicit을 `indistinguishable`, session_start를 `confirmed`(clean 우세)로 판정했으며 롤업 일반 권고는 clean이다. 사용자 질문에 대한 실증적 답은 "주입 방식을 고르기 전에 무엇을 재려는지 정해야 하고, 발견을 재려면 과제에 작용점이 있어야 하며, 주입은 효과의 상한을 재는 것이지 실사용 효과가 아니다"이다.
- Claude 사용량 표시 갇힘 진단 착수: 사용자가 "클로드 한도 파싱이 안 된다"고 보고해 Claude(claude-opus-5)가 조사했다. 확인 결과 `usage_status`의 claude 행이 2026-08-15 11:33 KST 이후 `data_status=stale`로 굳어 `100% / Resets 11:39am`(이미 지난 시각)을 계속 표시하고 있었고, `last_checked_at`은 매분 갱신되는데 `last_success_at`만 멈춰 있었다. 모니터와 동일 조건(`claude --safe-mode --ax-screen-reader`, 120x40 PTY, `/usage` 후 8초)으로 화면을 재현해 `ClaudeAdapter.parseUsage`에 넣으면 `session 12% / 4:40pm`, `weekly 19% / Aug 22 1am`이 `fresh`로 정상 파싱돼, 파싱 정규식(신버전 Claude Code v2.1.233의 Settings 탭형 `/usage` 화면 포함)은 문제가 아님을 확인했다. 코드상 해당 DB 조합이 나오는 경로는 `usage-monitor.ts`의 거부 분기(`detectUsageRegression` 또는 `isImplausibleClaudeSessionReset`이 참일 때 마지막 정상값을 지키고 stale만 남기는 처리)뿐이다. 다만 저장된 직전값과 실제 화면값으로 두 판정을 재현하면 모두 거짓이라 서버 내부 조회 PTY가 보는 화면이 다른 것으로 보이며, 원문(`lastSnapshot`)은 인증이 필요한 API로만 볼 수 있고 파서 입력 debug 로그는 production 레벨이라 남지 않았다. 사용자 의사결정으로 세 방안(갇힘 해제 / 스냅샷 원문 확인 / debug 재시작) 중 "갇힘부터 풀기"를 선택해, 캐시성 데이터인 `usage_status`의 claude 행만 삭제해 직전값 의존 거부를 해제하고 다음 조회 주기의 실제 저장값을 관찰하기로 했다. 서버 재시작이나 다른 세션에 영향을 주는 조치는 하지 않는다.
- Claude 사용량 표시 갇힘 해제 완료: Claude(claude-opus-5)가 사용자 선택에 따라 `usage_status`의 claude 행 1건만 삭제했고, 다음 조회 주기에서 `2026-08-15T03:00:09Z`에 `Current session 16% / Resets 4:39pm`, `Current week (all models) 20% / Aug 22 12:59am`이 `data_status=fresh`로 정상 저장되며 갇힘이 풀렸다. 삭제 즉시 회복된 점으로 보아 직전 저장값(100% / 11:39am)에 의존하는 거부 처리에 갇혀 있었을 가능성이 크지만, 저장값과 실측 화면값으로 `detectUsageRegression`·`isImplausibleClaudeSessionReset`을 재현하면 둘 다 거짓이라 어떤 입력에서 참이 됐는지는 확정하지 못했다(허용 오차는 15분인데 11:39am과 4:39pm은 300분 차이라 동일 창으로 묶이지 않는다). 확정을 위해서는 인증이 필요한 `/api/usage/:provider/snapshot` 원문이나 production 레벨에서 빠지는 `usage-check` debug 로그가 필요하다. 남은 문제로, 거부 처리는 마지막 정상값을 무기한 지키기 때문에 이번처럼 27분(11:33~12:00 KST) 동안 이미 지난 리셋 시각의 100%를 계속 노출했다. "다음 주기에 최신 값이 오면 자동 복구된다"는 기존 주석의 가정이 실제로 깨진 사례이므로, 거부가 일정 횟수·시간 이상 연속되면 최신값을 강제 채택하는 안전장치가 필요하다고 판단했다. 다만 최소 수정 원칙에 따라 사용자 확인 전까지 코드는 수정하지 않았고, 이번 작업의 변경은 DB 캐시 행 삭제와 이 기록뿐이다.
- 사용량 거부 갇힘 탈출구 구현 착수: 바로 앞 항목에서 확인한 결함(거부 처리에 탈출구가 없어 이미 지난 리셋 시각의 옛 값이 27분간 굳음)을 사용자가 수정하라고 승인해 Claude(claude-opus-5)가 착수한다. 설계 판단은 다음과 같다. 거부가 연속으로 이어지는 동안 조회 주기(60초)마다 같은 값이 계속 돌아온다면 그것이 실제 최신값이므로, 연속 거부가 임계치를 넘으면 최신값을 강제로 채택하고 경고 로그를 남긴다. 기존 거부 처리의 목적(옛 스냅샷 1~2주기 오염 차단)은 임계치 아래에서 그대로 유지되며, 오염값을 잘못 채택하더라도 다음 주기에 진짜 최신값이 증가 방향으로 들어와 자동 복구되는 반면 지금의 영구 갇힘은 스스로 복구되지 않는다는 비대칭이 근거다. 판정은 기존 순수 함수들과 같은 방식으로 export해 테스트한다.
- 사용량 거부 갇힘 탈출구 구현 완료: Claude(claude-opus-5)가 `src/server/services/usage-monitor.ts`에 상수 `REJECTED_ADOPT_STREAK = 5`와 순수 함수 `shouldAdoptRejectedUsage`를 추가하고, `MonitorState`에 연속 거부 횟수 `rejectedStreak`를 두어 `finishUsage`에서 거부가 5회(60초 주기 기준 약 5분) 연속되면 최신값을 그대로 채택하며 `usageLog.warn("rejected-adopted", …)`로 경고를 남기도록 했다. 정상 반영이나 강제 채택이 일어나면 연속 횟수는 0으로 초기화돼 옛 스냅샷 차단은 그대로 유지된다. 검증은 `tests/usage-monitor.test.ts`에 임계값 경계 단위 테스트를, `tests/usage-rejection-adopt.test.ts`(신규)에 실제 `UsageMonitor`와 임시 DB로 `finishUsage`를 5회 태워 4회까지는 stale·마지막 정상값 90%가 유지되고 5회째에 fresh·최신값 10%와 `last_success_at` 갱신이 일어나는지, 거부가 끊기면 횟수가 0이 되는지 확인하는 통합 테스트를 추가했다. 테스트가 실제 동작을 잡는지 확인하려고 임계값을 999로 바꿔 5회째 단언이 실패하는 것까지 역검증한 뒤 5로 되돌렸다. `npx tsc --noEmit` 무오류, 전체 `vitest run` 89파일 617테스트 통과다. UI 변경이 없는 백엔드 로직이라 스크린샷은 남기지 않았다. 이 변경은 아직 빌드·서버 재시작 전이라 실행 중인 서버에는 반영되지 않았고, 재시작은 다른 세션 작업을 끊을 수 있어 사용자 판단에 맡긴다. 문서는 `CODETREE.md`의 usage-monitor 행과 테스트 표, `README.md` 운영 설명을 갱신했다.

## 2026-08-18 Claude - Grok 공급자(채팅 코어) 추가 착수

- 배경/요청: 사용자가 "grok도 추가하자"고 요청했고, 로컬에 `grok 1.0.3`이 설치되어 grok.com 계정으로 로그인까지 완료된 상태다. 기존 web-agent-manager는 `Provider = "codex" | "claude"` 두 공급자만 지원한다.
- 사전 조사(Claude, claude-opus-5): Grok CLI는 Claude Code와 거의 동형의 인터페이스를 가진다 — `-r/--resume <ID|title>`, `-s/--session-id`, `--permission-mode`, `-p/--single`, `--model`, `--reasoning-effort`, `--output-format`. 모델은 `grok-4.6`(기본)/`grok-4.5`. 기록은 `~/.grok/sessions/<URL인코딩 cwd>/<세션UUID>/chat_history.jsonl`이며 같은 디렉터리의 `summary.json`에 제목(`generated_title`/`session_summary`)·모델(`current_model_id`)·시각(`created_at`/`updated_at`)·`reasoning_effort`가 있다. `chat_history.jsonl` 레코드 타입은 system/user/assistant/reasoning/tool_result/backend_tool_call이고, 레코드 단위 토큰 사용량 필드는 없다(세션 누적치만 `signals.json`의 `contextTokensUsed`에 있음).
- 영향 범위 측정: `Provider`에 `"grok"`을 임시로 추가해 `tsc --noEmit`을 돌린 결과 에러 16건. 이 중 채팅 코어는 `session-manager.ts` 1건, `agent-accounts.ts` 1건뿐이고 나머지 14건은 실험실(`experiment-service.ts`/`experiment-repository.ts`/`graph-harness.ts`/`single-harness.ts`/`rubric-evaluation.ts`)이 `Provider` 대신 `"codex" | "claude"` 리터럴 유니온을 자체적으로 쓰고 있어서 생긴 것이다.
- 범위 결정(사용자 선택): 이번 작업은 **채팅 코어까지만** 한다. 즉 GrokAdapter 신규 구현 + `Provider` 타입 확장 + 채팅 생성·히스토리 동기화·승인·사용량·계정 슬롯 연결까지다. 실험실(Agent Lab)은 Grok을 실험 대상/평가자로 쓰는 것까지 포함하면 DB 스키마와 실험 UI까지 손대야 해 작업량이 크게 늘어나므로, 기존 `"codex" | "claude"` 리터럴을 그대로 유지해 실험실 동작을 건드리지 않고 TODO로 남긴다. 사용자가 큰 요청을 한 번에 다 처리하기보다 범위를 좁혀 확실히 검증하는 쪽을 선호한다는 점을 반영한 결정이다.
- 진행 방침: TUI 화면 판정(`isReady`/`isBusy`/`detectApproval`/`detectModel`)은 추측으로 정규식을 쓰면 오탐이 나므로, 실제 grok TUI를 tmux로 띄워 캡처한 화면을 근거로 작성한다.
- Grok 공급자(채팅 코어) 구현 완료: Claude(claude-opus-5)가 `src/server/providers/grok.ts`에 `GrokAdapter`를 새로 만들고 `Provider` 타입에 `"grok"`을 추가해 채팅 코어 전체를 연결했다. 주요 설계 판단은 다음과 같다. (1) **턴 종료 판정**: Grok은 도구 호출 직전에도 설명 문장을 assistant 메시지로 남겨 "내용 있는 assistant"를 완료로 보면 턴 중간마다 완료 알림이 나간다. 실제 완료는 `events.jsonl`의 `turn_ended`뿐이라 그것만 인정하되, `chat_history.jsonl`의 mtime만 감시하면 마지막 메시지 기록과 `turn_ended` 기록 사이의 짧은 순간에 스캔이 걸렸을 때 턴 종료를 영영 놓쳐 작업중 표시가 안 풀린다 — 그래서 `events.jsonl`도 같은 세션의 입구로 받아들여(historyFile은 항상 `chat_history.jsonl`로 고정) 그 파일이 갱신될 때 반드시 한 번 더 동기화되게 했다. (2) **사용자 발화 구분**: user 레코드에는 사람이 친 `<user_query>` 말고도 `<user_info>`·`<git_status>`·`<system-reminder>` 주입이 같은 타입으로 섞여 들어와, 태그가 있는 블록만 사람 발화로 인정했다. (3) **승인 입력**: 실제 승인 화면을 tmux로 재현해 확보한 결과 선택지 구성이 도구마다 달라(`1 (●) Yes, and don't ask again` / `2 (○) ...this session` / `3 (○) Yes` / `4 (○) No, reject`) 고정 번호 매핑이 성립하지 않았다. `ProviderAdapter`에 화면 기반 `resolveApprovalInput` 선택 훅을 추가해(기존 `resolveRateLimitInput`의 일반화) 실제 화면 문구에서 번호를 찾게 하고, `approvalInput`은 빈 문자열을 돌려 화면을 못 본 상태에서는 아무 키도 보내지 않게 했다 — 번호를 추측해 보내면 승인 화면이 아닌 입력창에 그 숫자가 그대로 채팅 메시지로 전송되는 사고가 나기 때문이다.
- Grok 구현의 부수 결정: (1) `grok` CLI에는 사용량·한도 조회 명령이 아예 없어 `usageCommands`를 비우고 `parseUsage`가 새 `error_code: "unsupported"`를 돌려주게 했다. 그대로 두면 `UsageMonitor`가 아무 명령도 보내지 않는 조회 전용 TUI를 계속 띄우므로, 사용량 명령이 없는 공급자는 모니터 대상에서 제외하고 상태 행만 미지원으로 남기도록 고쳤다. 화면에는 코드 문자열 대신 "이 CLI는 사용량 조회를 지원하지 않습니다"를 보여준다(`usageErrorLabel`). (2) 계정 슬롯은 `GROK_HOME`으로 나눈다. (3) `grok`은 로그인하지 않아도 상태 명령이 정상 종료해 종료 코드만 보면 항상 "로그인됨"이 되므로, `cli-auth`에 출력 기반 판정 경로(`commandOutput` + `unauthenticatedPattern`)를 선택적으로 추가했다. (4) `tool-routes`의 `provider === "claude" ? "claude" : "codex"` 삼항식은 새 공급자가 전부 codex로 떨어져 엉뚱한 CLI를 실행하므로 공급자 id를 그대로 쓰도록 고쳤고, Grok 내장 슬래시 명령 16개를 도구 카탈로그에 추가했다. (5) 기본 계정 라벨 생성이 삼항식이라 grok이 "기본 Codex 계정"을 받던 버그를 공급자별 라벨 맵으로 고쳤다.
- 실험실 경계 처리: 사용자가 정한 범위대로 Agent Lab은 Grok을 지원하지 않는다. `Provider`를 넓히자 실험실이 쓰던 `"codex" | "claude"` 리터럴에서 타입 에러 14건이 났는데, 캐스팅으로 덮지 않고 `ExperimentProvider`(+`isExperimentProvider`)를 `shared/experiments.ts`에 정의해 "실험실이 실제로 돌릴 수 있는 공급자"를 타입으로 명시했다. 승인 서비스(`approval.ts`)는 실험실이 아니라 채팅 코어라 반대로 `Provider` 전체를 받도록 넓혔다.
- DB 마이그레이션: 채팅 코어 6개 테이블(`chats`, `usage_reset_schedules`, `agent_integration_status`, `agent_accounts`, `usage_keepalive_prompts`, `token_usage_events`)의 `CHECK(provider IN ('codex','claude'))`에 `'grok'`을 추가했다. SQLite는 제약만 바꾸는 ALTER가 없어 테이블 재생성이 필요한데, 스키마 상수를 그대로 쓰면 그동안 ADD COLUMN으로 늘어난 컬럼이 빠지므로 현재 DB에 저장된 CREATE 문을 읽어 제약 문구만 치환했다. 재생성 중에는 외래키를 꺼서 `chats`를 ON DELETE CASCADE로 참조하는 승인·토큰 행이 함께 지워지지 않게 하고, DROP으로 사라진 인덱스를 원래 정의대로 되살린 뒤 `foreign_key_check`로 검증한다. 실험실 테이블(`experiment_*`)은 Grok 미지원이라 제외했다. 운영 DB 스냅샷 복사본으로 실제 실행해 chats 46행·token_usage_events 25,997행·approvals 46행이 그대로 보존되고 인덱스 전체 복원, `foreign_key_check` 0건, `integrity_check` ok를 확인했다(운영 DB는 건드리지 않음).
- 검증: 타입 검사 0건, 90개 파일 639개 테스트 전체 통과(신규 `tests/grok-history.test.ts` 21개 + `cli-auth.test.ts`에 grok 출력 기반 판정 테스트 추가), production build 통과. TUI 화면 판정 정규식은 추측하지 않고 실제 `grok` TUI를 tmux로 띄워 캡처한 화면(유휴/응답중/초안/승인/권한모드)을 fixture로 고정했다. 별도 포트(14099)·별도 DATA_DIR로 검증 서버를 띄워 운영 서버(14003)와 분리한 채 실제 동작을 확인했다 — Grok 공급자·기본 계정 등록, 기존 `~/.grok` 세션 5개 자동 발견과 대화 파싱(사람 발화만 남고 주입 블록·reasoning 제외), 웹에서 만든 Grok 채팅에 프롬프트를 보내 응답·메시지 동기화·완료 시 작업중 해제(busy=0)·모델 표시(grok-4.6)·권한 모드(default) 표시·Grok이 만든 제목 자동 반영까지 통과했다. 화면은 `artifacts/ui-grok-overview.png`, `artifacts/ui-grok-chat.png`에 남겼다. 검증 중 `npx` 래퍼만 종료되고 실제 node 자식이 포트를 계속 점유해 옛 코드가 서빙되는 문제를 겪어, 소켓 inode로 점유 프로세스를 특정해 정리하고 이후에는 래퍼 없이 실행했다(운영 서버·다른 세션은 건드리지 않음).
- 남은 범위(TODO): Agent Lab에서 Grok을 실험 대상·평가자로 쓰기(실험실 테이블의 provider CHECK 제약과 실험 생성 UI, 하네스 타입까지 함께 넓혀야 함), 자식 채팅 위임(`web-agent-manager-delegate`)의 Grok 대상 지원, 전역 스킬·MCP 자동 연동(`agent-integration`)에 Grok 추가, `grok` CLI가 사용량 조회 수단을 제공하면 `parseUsage` 실제 구현으로 교체.

## 2026-08-18 Claude - Grok 사용량 조회 지원(직전 "미지원" 결론 정정)

- 정정 배경: 사용자가 grok TUI의 `/usage` 화면 스크린샷("Weekly limit (SuperGrok) 16% / Resets: August 21, 13:23")을 보내 "이건 장식이냐"고 지적했다. 직전 작업에서 Claude(claude-opus-5)가 "grok CLI에는 사용량 조회 명령이 없다"고 결론 내린 것은 **잘못**이었다. 근거로 삼은 `~/.grok/README.md`의 슬래시 명령 표에 `/usage`가 없었으나 그 표 자체가 불완전했고(실제 TUI 자동완성에는 `/usage`·`/guides`·`/resume-cursor` 등 표에 없는 명령이 있음), 문서 표만 보고 실제 TUI에서 확인하지 않은 것이 원인이다. 문서보다 실제 CLI 동작을 먼저 확인해야 한다는 교훈.
- 확인한 실제 동작: `/usage`만 보내면 `show`(View usage)/`manage`(Manage billing) 하위 선택에서 멈춘다. 인자까지 붙인 `/usage show`를 보내면 자동완성이 show로 확정돼 Enter 한 번에 사용량 화면이 열린다(`manage`는 결제 관리라 자동 조회가 절대 선택하면 안 되는 항목이다). 화면은 `Context usage`/`Usage limit`/`Session info` 탭을 가진 모달이며 기본 탭에 `Weekly limit (<플랜명>)` 제목, 막대와 퍼센트, `Resets: <시각>`, 그리고 `Session usage` 블록(Input/Output/Total tokens, Model calls, Cost)이 있다.
- 구현: `GrokAdapter.usageCommands`를 `["/usage show"]`로, `usageWindowId`를 `weekly`로 바꾸고 `parseUsage`를 실제 파싱으로 교체했다. 세션 토큰·호출·비용 줄은 한도가 아니라 참고 수치라 퍼센트 창이 아닌 activity 목록으로 남긴다. 직전 작업에서 넣었던 "사용량 조회 명령이 없는 공급자" 처리(`UsageMonitor.monitorAdapters`/`markUnsupported`, `error_code: "unsupported"`)는 해당하는 공급자가 없어져 죽은 코드가 되므로 되돌렸다 — 다만 상태 코드를 한국어 문구로 바꾸는 `usageErrorLabel`은 그 자체로 유효한 개선이라 `unsupported` 항목만 빼고 유지했다.
- 실사용에서만 드러난 문제 2건과 대응: (1) **모달이라 닫아야 한다** — 사용량 화면이 입력창을 덮어서, 파싱 뒤 닫지 않으면 다음 주기의 조회 명령이 입력창이 아니라 모달 단축키(Tab 전환·`c` 세션ID 복사 등)로 먹힌다. `ProviderAdapter`에 `usageScreenCloseInput` 선택 훅을 추가하고 화면을 읽은 **뒤** Esc를 보내도록 했다. (2) **`/model`을 보내면 안 된다** — 검증 서버 2회차 조회가 `Unknown model: /usage show`로 실패했다. `UsageMonitor.modelOptions()`가 목록을 읽으려고 `/model\r`을 보내는데, Grok의 `/model`은 선택 메뉴가 아니라 인자 입력 대기(`/model <model> [effort]`)라서 Esc 뒤에도 입력이 남고 다음 명령이 모델 이름으로 먹혔다. Grok은 CLI 고정 목록과 화면 테두리의 현재 값만으로 목록을 만들 수 있으므로 `promptQuirks.modelOptionsWithoutMenu`를 추가해 명령 전송 자체를 건너뛰게 했다.
- 파싱 견고성: 첫 실사용 조회에서 제목이 `Weekly  imit (SuperGrok)`으로 캡처돼(tmux 화면 캡처가 글자를 흘림) 제목을 조건으로 걸었던 정규식이 통째로 실패했다. 제목 대신 막대·퍼센트 줄과 `Resets:` 키워드를 앵커로 삼아 창을 찾고 제목은 위쪽에서 표시용으로만 주워오도록 바꿨다. 제목에 `limit`이 성하지 않으면 캡처 손상으로 보고 대표 창의 표시명만 보정한다(플랜 이름은 화면에서 읽은 값을 유지). 이 손상 케이스는 회귀 테스트로 고정했다.

## 2026-08-18 Grok - Grok CLI 자체 토큰 표시 확인

- 작업 시작: 사용자가 "그록 자체를 확인해줘, 코드가 아닌 그록 CLI 자체를 뜯어서 확인해줘"라고 요청했다. web-agent-manager 코드가 아니라 설치된 `grok 1.0.5` 바이너리·세션 파일·공식 문서를 근거로 답하기로 했다.
- 확인 대상: `/root/.grok/downloads/grok-1.0.5-linux-x86_64`(ELF 160MB), 현재 세션의 `updates.jsonl`/`chat_history.jsonl`/`events.jsonl`/`signals.json`, `~/.grok/docs/user-guide/`. `auth.json`은 읽지 않았다.
- 의사결정: 프로젝트 코드를 수정하지 않고 조사만 한다. 이전 기록의 "Grok 기록에 응답별 토큰이 없다"는 `chat_history.jsonl` 기준으로는 맞지만, CLI가 실제로 어디에 usage를 남기는지까지 다시 확인한다.
- 확인 결과:
  - TUI 채팅 말풍선에는 응답별 토큰을 그리지 않는다. pager에 `usage_modal.rs`·`context_bar.rs`는 있고, 메시지 타임라인에 `Show token`/`message usage` 설정은 없다. 메시지 크롬은 `/timestamps`뿐이다.
  - `/usage`(모달)에 주간·월간 한도와 `Session usage`(Input/Output/Total tokens, cached, reasoning, Model calls, API time, Cost)가 있다. `/session-info`·`/context`는 컨텍스트 창 사용량이다. 대시보드 행은 `tools · tok · turns` 수준이다.
  - 데이터는 있다. `updates.jsonl`의 `turn_completed.usage`에 턴 단위 `inputTokens`/`outputTokens`/`totalTokens`/`cachedReadTokens`/`cacheCreationTokens`/`reasoningTokens`/`modelCalls`/`costUsdTicks`가 남는다. `chat_history.jsonl` assistant 레코드와 `events.jsonl`에는 usage 필드가 없다. `signals.json`은 컨텍스트 창 사용량만 갖는다.
  - headless JSON 스트림은 응답 경계 `type: usage`를 내보낸다. 인터랙티브 TUI 채팅과는 별개다.
- 이전 기록 정정: "레코드 단위 토큰이 없다"는 `chat_history.jsonl`에 한정해서만 참이다. 턴 단위 원본은 `updates.jsonl`에 이미 있다. 다만 TUI가 그걸 메시지 아래에 붙이지 않고, WAM도 그 파일을 토큰 원장에 쓰지 않는다.

## 2026-08-18 Grok - Grok 턴 단위 토큰을 Claude·Codex와 같은 말풍선/원장 경로에 연결

- 작업 시작: 사용자가 "여기 구현 보면, 턴단위 토큰만 있으면 돼 클로드, 코덱스"라고 했다. 현재 WAM은 Claude assistant `message.usage`와 Codex `token_count.last_token_usage`를 마지막 assistant에 붙여 말풍선·원장에 쓴다. Grok도 `updates.jsonl`의 `turn_completed.usage`면 같은 경로로 충분하다.
- 구현 결정: `chat_history.jsonl`은 그대로 두고 `turn_completed`를 사용자 턴 순서대로 그 턴의 마지막 내용 있는 assistant에 붙인다. 중간 도구 설명 문장에는 붙이지 않는다. Grok 숫자는 Codex와 같이 `inputTokens`에 캐시가 포함되고 `totalTokens = input + output`이라 `cachedReadTokens`는 `cachedInputTokens`(부분집합)로, `reasoningTokens`는 `reasoningOutputTokens`로 옮기고 합계는 다시 더하지 않는다.
- 동기화 결정: `updates.jsonl`도 events처럼 세션 입구로 받아 `historyFile`은 `chat_history.jsonl`로 고정한다. 채팅 API는 `chat_history.jsonl` 캐시를 읽으므로, `updates.jsonl`이 바뀌면 그 캐시를 버려 턴이 끝난 뒤에야 붙는 usage가 화면에 나오게 한다. events.jsonl은 턴 중에 수백 줄이 쌓여 같은 처리를 하면 파싱이 폭주하므로 대상에서 뺀다.
- 구현 완료: `parseGrokTurnUsage`/`readGrokTurnUsages`/`attachGrokTurnUsages`를 추가하고, 원장 집계 표시명이 grok를 Claude로 묶지 않게 SQL을 고쳤다. 이 세션 `chat_history.jsonl`을 직접 파싱해 인사 턴(17,061)·토큰 질문 턴(170,079)·CLI 조사 턴(1,482,493)만 각 턴의 마지막 assistant에 붙고 중간 도구 설명에는 안 붙는 것을 확인했다.
- 검증: `tsc --noEmit` 무오류. `grok-history`·`history-cache`·`token-usage-ledger`·`token-usage-display`·`codex-history`·`claude-history` 6파일 121테스트 통과. UI 변경은 기존 `MessageCard` 경로를 그대로 쓰므로 새 화면은 없다.
- Claude(claude-opus-5) 검토·보완: Grok이 구현한 `turn_completed.usage` 연결을 실제 `~/.grok` 세션 7개로 검증했다. 파싱 결과가 Grok이 제시한 수치와 일치하고(17,061 / 170,079 / 1,482,493), 각 턴의 마지막 내용 있는 assistant 말풍선에만 붙는 것을 확인했다. `updates.jsonl`이 `events.jsonl`보다 15ms 나중에 기록되는 것도 실측해(mtime 05:22:46.882 vs .897) `updates.jsonl`을 세션 입구로 여는 판단이 옳음을 확인했다 — turn_ended만 트리거로 쓰면 그 사이에 스캔이 걸렸을 때 턴 토큰을 놓친다. 앞선 조사에서 "응답별 토큰이 없다"고 단정한 것은 Claude의 잘못으로, `updates.jsonl`을 `"[a-z_]*token[a-z_]*"` 소문자 패턴으로만 grep해 camelCase 필드명(`inputTokens`)을 통째로 놓친 것이 원인이다.
- Claude 성능 보완 2건: (1) `readGrokTurnUsages`가 모든 줄을 `JSON.parse`했는데, `updates.jsonl`은 턴 중 도구 진행·사고 조각이 계속 쌓여 실측 410줄 중 `turn_completed`는 4줄뿐이다. `readLastTurnEndedAt`과 같은 방식으로 해당 문자열이 있는 줄만 파싱하게 바꿨다. (2) 사이드카가 바뀔 때마다 본문 캐시를 버리면(파일 크기 실측: updates.jsonl 2.9MB, chat_history.jsonl 717KB) 응답이 진행되는 내내 2초 주기로 본문을 반복 재파싱하게 된다. 무효화 조건을 "토큰이 붙은 메시지 수가 실제로 늘었을 때"로 좁혔고, 그 덕에 `history-sync`에 있던 `updates.jsonl` 파일명 하드코딩도 없애 공급자 중립으로 되돌렸다. `token-usage-ledger`의 공급자 라벨에서 grok이 'Claude'로 표시되던 것을 Grok이 함께 고친 것도 확인했다(직전 작업에서 Claude가 놓친 부분).

## 2026-08-18 Claude - 테스트 세션 정리 중 조회 전용 기록 노출 버그 발견·수정

- 요청: 사용자가 "테스트 세션들 정리"를 요청해 Claude(claude-opus-5)가 검증 산출물을 정리했다. 삭제 대상은 경로로 구분했다 — `scratchpad/grokprobe`·`groktrust`·`grokusage`와 `/tmp/wamgrok/proj`에서 만든 grok 세션 5개, 그에 대응하는 채팅 3건(#292~294)과 토큰 원장 7건(314,017 tokens), 검증용 작업 디렉터리. 실제 작업 세션(`/home/ubuntu/resume`, `/home/ubuntu/myagent`)과 실행 중이던 채팅 #296은 건드리지 않았다.
- 정리 중 발견한 버그: 채팅 목록에 제목 없는 "Grok 세션"이 7개 쌓여 있었다. 확인해 보니 사용량 조회 전용 PTY도 일반 grok 세션 디렉터리를 만드는데, 거기엔 `/usage show` 같은 슬래시 명령만 오가서 `<user_query>`가 한 줄도 남지 않는 빈 기록이 된다. Claude·Codex 어댑터는 `isHiddenHistoryFile`에서 내용 기반으로 이런 조회 PTY 기록을 걸러내는데, Claude가 만든 GrokAdapter는 파일 이름만 보고 판정해 이 경우를 놓쳤다. 서버를 재시작할 때마다 새 조회 세션이 생기므로 그대로 두면 목록이 계속 오염된다.
- 수정: `isHiddenHistoryFile`이 본문(`chat_history.jsonl`)에 실제 사람 발화가 있는지까지 확인하도록 바꿨다. 사이드카(`events.jsonl`·`updates.jsonl`)로 들어와도 판정 기준은 본문 하나로 통일한다. 판정에 파일을 읽어야 하므로 Claude와 같은 방식으로 mtime 캐시를 두고, 사람 대화가 한 번이라도 확인된 기록은 이후 그 대화가 사라질 수 없으므로 영구히 노출로 확정한다. 웹에서 새로 만든 채팅이 첫 메시지 전에 숨김 판정으로 지워질 위험은 없다 — 숨김이면 `persist`를 건너뛰어 `history_file`이 연결되지 않고, `removeHiddenChat`은 `history_file`이 일치하는 행만 지우기 때문이다.
- 정리·검증: 조회 전용 세션 중 이번 검증이 만든 myagent 7개는 파일까지 지우고, 8/14에 만들어진 resume의 1개(#291)는 사용자 기록이라 파일을 남긴 채 목록에서만 빠지는지로 수정 동작을 검증했다 — 서버 재시작 후 #291이 자동으로 사라졌고, 재시작이 새로 만든 조회 세션(01a0136b)도 채팅으로 등록되지 않았다. 최종적으로 대화가 있는 세션 2개(현재 Grok 대화, resume 성과평가)만 목록에 남는다. 651개 테스트·빌드 통과.

## 2026-08-20 Claude - Ponytail 벤치마크 산출물 전면 정리 착수

- 작업 시작: 사용자가 "포니테일 벤치마크 프로젝트와 그 하위 워크트리들 전부 정리해줘"라고 요청했다. Claude(claude-sonnet-5)가 앞서 완료된 Ponytail 단건·멀티턴 비교 실험(종합 점수로 clean 승리, 후속 범위는 문서에만 반영)의 잔여 산출물을 정리 대상으로 확인했다.
- 조사 결과: fixture 원본 저장소 1개, 그 `git worktree` 5개(clean/clean-formal/clean-formal-2/treatment/treatment-formal), blind judge용 독립 저장소 2개(judge-claude-*, judge-codex-*), 그리고 실행 스크립트·judge 결과 jsonl/json 파일들이 artifacts 아래 남아 있었다. DB 조회 결과 연결된 채팅은 0건이었고, 워크트리 5개 중 4개와 judge 저장소 2개는 이미 프로젝트 목록에서 비활성 처리(active=0)되어 있었으나 파일은 남아 있었다. fixture 원본 저장소 프로젝트 1건만 active=1로 남아 있었다.
- 범위 확정: 사용자에게 "워크트리+fixture 원본만" 대 "관련 스크립트·judge 결과까지 전부"를 물어 후자로 확정했다.
- 실행 방식: 로그인 세션 쿠키가 없어 프로젝트 삭제 API(관리자 인증 필요)는 호출하지 못했다 — DB에 직접 UPDATE/DELETE를 쓰지 않는다는 기존 원칙에 따라 우회하지 않고, 파일시스템 정리(`git worktree remove` + 저장소·파일 삭제)만 수행한다. 남은 fixture 원본 프로젝트(active=1) 1건의 목록 비활성화는 사용자가 웹 UI에서 직접 처리하도록 안내한다.
- 실행 완료: fixture 원본 저장소에서 `git worktree remove --force`로 5개 워크트리를 정식 해제한 뒤(대상 프로젝트 연결 채팅 0건, 열린 프로세스 없음을 먼저 확인) fixture 원본 저장소 디렉토리, judge용 독립 저장소 2개, 실행 스크립트 5개(runner·race-probe·goal-score·blind-judge 등), judge 결과 json/jsonl 5개를 삭제했다. 코드베이스 안의 `tests/experiment-foundations.test.ts`(재사용 가능한 테스트 픽스처)와 `docs/agent-lab.md`(향후 실험 계획 문서)는 산출물이 아니라 삭제 대상에서 제외했다. DB의 fixture 원본 프로젝트 레코드(active=1) 1건은 관리자 인증이 필요해 남겨뒀으므로, 사용자가 웹 UI에서 삭제하면 정리가 완결된다.

## 2026-08-20 Claude - Android 앱 채팅 첨부 업로드 실패·대용량 파일 무응답 수정 착수

- 작업 시작: 사용자가 "여기 파일 업로드 제대로 동작 안하더라 특히, 모바일 버전"이라고 보고했다. 처음엔 모바일 브라우저를 의심했으나 사용자가 "브라우저가 아니라 앱"이라고 정정해, `android/app`의 WebView 셸(`MainActivity.java`) 문제로 범위를 좁혔다. 이어서 사용자가 "100mb짜리 올리니까 30분 기다려도 pending상태이고, 새로고침 후에는 데이터가 안 올라가 있다"는 대용량 파일 무응답 증상도 추가로 제기했다.
- 원인 진단(코드 리딩만으로 확인, 실기기 재현은 못함):
  1. **Android 파일 선택 콜백 유실**: `MainActivity`의 `fileChooserCallback` 필드는 인스턴스 필드라 `onSaveInstanceState`로 보존되지 않는다. 📎 버튼으로 갤러리 앱을 열면 `MainActivity`가 백그라운드로 밀려나는데, 이때 시스템이 메모리 회수로 프로세스를 종료했다가 파일 선택 완료 후 액티비티를 새로 만들면 새 인스턴스의 `fileChooserCallback`이 `null`이라 `onActivityResult`가 `requestCode != FILE_CHOOSER_REQUEST || fileChooserCallback == null` 가드에서 조용히 return한다. 웹 쪽 `<input>`에는 아무 값도 전달되지 않아 `onChange`조차 안 걸리므로 화면에 성공도 실패 메시지도 안 뜬다. `onSaveInstanceState`/`webView.saveState()` 같은 상태 복원 로직도 이 앱에 전혀 없었다.
  2. **대용량 파일 업로드 시 서버 응답 없음**: `src/server/core/uploads.ts`의 `processMultipartFiles()`는 `assertUploadCapacity()`가 `Content-Length` 기준으로 총량 제한(채팅 첨부 50MB) 초과를 감지하면 즉시 `throw`하지만, 이 시점에 요청 바디 스트림을 전혀 소비(`resume`/`destroy`)하지 않는다. 100MB 요청이 들어오면 서버는 에러를 던져 라우트가 `next(error)`로 넘어가 응답을 시도하지만, 클라이언트가 아직 보내고 있는 요청 바디가 소켓에 정체된 채로 응답 왕복이 막혀 클라이언트 쪽에서는 요청이 그냥 pending으로 보인다. 동시성 초과(`MAX_CONCURRENT_UPLOAD_REQUESTS`) 경로도 같은 결함이 있다.
  3. **부수 원인**: 클라이언트(`ChatView.tsx`)에 파일 크기 사전 검증이 전혀 없어 100MB 파일도 그대로 전송을 시도하고, 업로드는 `fetch`라 진행률도 전혀 표시하지 않는다. 그래서 큰 파일은 사전 차단도, 진행 상황 안내도 없이 "그냥 느리다"로 느껴진다.
- 수정 계획(사용자 승인): (1) Android WebView 콜백 유실 시 Toast+로그로 명시적 실패 안내, WebView 상태 저장/복원 추가. (2) 서버 조기 거부 시 요청 스트림 `destroy()`로 즉시 정리. (3) 클라이언트에 서버와 동일한 제한(파일당 25MB·총 50MB·5개)을 공유 상수로 사전 검증해 초과 시 요청 자체를 보내지 않고 바로 안내. (4) 업로드를 `XMLHttpRequest`로 바꿔 진행률(%)을 표시.
- 구현 완료:
  1. `src/server/core/uploads.ts`의 `processMultipartFiles()`: 동시성 초과·`assertUploadCapacity()` 실패(총량 초과) 시 `throw` 전에 `request.destroy()`를 먼저 호출해 요청 소켓을 즉시 끊도록 고쳤다. 회귀 방지로 `tests/uploads.test.ts`에 100MB Content-Length 요청이 50MB 제한에서 즉시 실패하며 `destroy()`가 호출되는지 확인하는 테스트를 추가했다.
  2. 채팅 첨부 제한(파일당 25MB·총 50MB·5개)을 `src/shared/chat-attachments.ts` 공유 상수로 뽑아 `project-routes.ts`(서버)와 `ChatView.tsx`(클라이언트)가 같은 값을 참조하게 했다. 서버의 하드코딩된 리터럴을 상수로 치환했을 뿐 제한값 자체는 바꾸지 않았다.
  3. `ChatView.tsx`의 `uploadAttachments()`에 업로드 전 사전 검증(개수·파일당 크기·총 크기)을 추가해 제한을 넘는 파일은 서버로 보내지 않고 바로 이유를 안내한다.
  4. `src/client/api.ts`에 `uploadFile()`을 추가했다(`fetch`는 업로드 진행률 이벤트가 없어 `XMLHttpRequest`의 `upload.onprogress` 사용). `uploadAttachments()`가 이걸로 바뀌면서 첨부 상태 메시지에 실시간 퍼센트가 뜬다.
  5. `android/app/.../MainActivity.java`: `onActivityResult`에서 `fileChooserCallback`이 null인 경우(파일 선택기 대기 중 프로세스가 재생성된 경우) `Log.w`로 원인을 Logcat에 남기고 Toast로 "앱이 재시작되어 파일 선택이 처리되지 못했다, 다시 시도하라"를 안내하도록 바꿨다 — 이전에는 조용히 return이라 사용자가 원인을 알 방법이 없었다. 또한 `onSaveInstanceState`에서 `webView.saveState()`로 URL·스크롤 상태를 저장하고 `onCreate`에서 `state != null`이면 `webView.restoreState()`로 복원해, 프로세스 재생성 후에도 로그인 화면이 아니라 원래 보던 채팅 화면으로 돌아가게 했다. 콜백 자체(진행 중이던 파일 선택)는 프로세스가 죽으면 원천적으로 복구 불가능해 재시도 안내로 대응했다.
- 검증: `tsc --noEmit` 무오류, `vite build` 성공. 전체 652개 테스트(신규 1건 포함) 통과. Android 쪽은 이 환경에 Android SDK가 없어(`ANDROID_HOME` 미설정) `gradlew compileDebugJavaWithJavac`이 SDK 위치 오류로 실패했다 — 실제 컴파일·기기 검증은 못 했고 코드 리뷰 수준으로만 확인했다.

## 2026-08-20 Claude - 채팅 입력창에 다른 채팅·프로젝트 파일 링크 멘션 추가 착수

- 작업 시작: 사용자가 "채팅에서 다른 채팅 링크 거는거랑, 파일 링크 거는거 편의 기능 넣어줘. 파일은 현재 프로젝트, 채팅은 전체 프로젝트로"라고 요청했다. Claude(claude-sonnet-5)가 기존 코드를 조사해 이미 있는 구조를 재사용하는 방향으로 설계했다.
- 조사 결과: 메시지 렌더링(`src/client/lib/attachments.tsx`의 `MessageBody`)이 이미 마크다운 링크 중 현재 프로젝트 파일을 가리키는 링크를 감지해 새 탭 대신 파일 탭 미리보기로 여는 기능(`onOpenProjectFile`)을 갖고 있다. 채팅 목록 API(`GET /chats`, projectId 생략 시 전체 프로젝트 최대 300건)와 단건 조회(`GET /chats/:id`)도 이미 프로젝트 무관하게 존재한다. 다만 프로젝트 전체 파일을 이름으로 찾는 검색 API는 없었다(디렉터리 단위 목록 조회만 있음).
- 설계 결정: 입력창에서 캐럿 기준으로 `@`(파일)·`#`(채팅) 트리거를 감지해(직전이 공백/문장 시작이어야 함, 기존 슬래시 명령 메뉴와 동일한 UX 패턴 재사용) 자동완성 드롭다운을 띄우고, 선택하면 `[라벨](경로)` / `[라벨](chat:번호)` 마크다운 링크를 삽입한다. `chat:` 링크는 새 pseudo-scheme으로, `MessageBody`의 링크 렌더러가 이를 감지해 `onOpenChat` 콜백으로 프로젝트를 넘나들며 해당 채팅으로 이동시킨다(파일 링크가 `onOpenProjectFile`로 파일 탭을 여는 것과 대칭 구조). 파일 검색은 서버에 `GET /projects/:id/files/search`를 신설해 프로젝트 루트를 재귀 탐색하되(민감 경로는 기존 `assertNonSensitiveRelativePath` 재사용, node_modules 등 무거운 디렉터리는 별도 스킵 목록으로 제외) 결과·스캔 수 상한을 둔다.
- 작업 중 추가 요청: 사용자가 "파일 미리보기에서, 줄에서 바로 채팅이랑 연결 or 채팅 생성 바로 할 수 있게 해줘"를 이어서 요청했다. 파일 미리보기(`FilesView.tsx`)의 텍스트 표시를 `<pre>` 한 덩어리에서 줄 단위 grid(`LineNumberedPreview`)로 바꾸고, 줄마다 호버 시 나타나는 링크 버튼을 눌러 `LineChatLinkMenu`(전체 프로젝트 채팅 검색 + 공급자별 "새 채팅" 버튼)를 띄운다. 선택하면 `App`의 `linkFileLineToChat`이 기존 채팅이면 `openChatById`로, 새 채팅이면 `createChat`으로 이동한 뒤 `composerPrefill` state로 `[경로:줄](경로#L줄)` 참조를 그 채팅 입력창에 채워 넣는다. 반대 방향(메시지 속 파일 링크 클릭 시 그 줄로 스크롤·강조)은 `onOpenProjectFile`에 `line` 인자를 추가하고, `href`의 `#L줄` 프래그먼트를 `MessageBody`가 원본에서 직접 파싱해(경로 판정용 `cleanLinkedPath`는 프래그먼트를 먼저 잘라내므로 별도로 읽어야 함) 전달하는 방식으로 구현했다.
- 검증 중 발견·수정한 버그: 실제 브라우저(Playwright)로 확인하던 중 `chat:` 링크가 클릭 가능한 링크로 렌더링되지 않고 `href=""`로 비는 것을 발견했다 — react-markdown v10의 기본 `defaultUrlTransform`이 XSS 방지 목적으로 허용 목록에 없는 URI scheme(예: `chat:`)의 href를 빈 문자열로 지우기 때문이었다. `MessageBody`에 커스텀 `urlTransform`을 추가해 `chat:` 패턴만 예외로 통과시키고 나머지는 기존 기본 정책을 그대로 따르게 고쳤다. 이 문제는 `renderToStaticMarkup` 기반 단위 테스트(신규 3건, `tests/attachments.test.ts`)로 재현·확인했다.
- 검증: `tsc --noEmit` 무오류, `vite build` 성공, 전체 655개 테스트(신규 4건: 파일 검색 1건 + MessageBody 링크 3건) 통과. 별도 격리 인스턴스(임시 DATA_DIR·PORT, 운영 DB와 분리)를 띄우고 Playwright로 로그인 → `@파일`/`#채팅` 멘션 자동완성 → 파일 미리보기 줄 링크 → 다른 채팅으로 이동 및 입력창 프리필까지 실제 화면에서 확인했다.
  - 검증 중 실수: 첫 시도에서 서버 환경변수 이름을 `WEB_AGENT_MANAGER_` 접두사 없이 잘못 지정해, 격리된 것으로 착각한 인스턴스가 실제로는 운영 데이터 디렉터리(`data/`)와 포트 4317을 그대로 사용하며 잠깐 기동했다. 포트 충돌(4317 사용 중)로 곧바로 크래시했고 로그를 확인한 결과 실행한 동작은 채팅 상태 재확인(모두 "running"으로 일치, 상태 오손 없음)뿐이었으며 Slack·ntfy 알림도 발생하지 않았다. 이후 정확한 환경변수 접두사로 완전히 격리된 인스턴스(별도 데이터 디렉터리·포트)로 다시 진행했다.
- 배포 사고와 복구: 검증용으로 이 저장소에서 반복 실행한 `npm run build`가 실제 운영 서버(`scripts/run-server-supervised.sh`가 tmux에서 감시하는 포트 14003 프로세스)가 디스크에서 직접 서빙하는 `dist/client`를 그대로 덮어썼다. `NODE_ENV=production`은 `express.static(dist/client)`으로 정적 파일을 요청마다 새로 읽으므로 프론트엔드는 즉시 새 버전으로 바뀌었지만, 백엔드 프로세스는 재시작 전까지 옛 코드를 메모리에 그대로 들고 있어 새 프론트가 호출하는 `GET /projects/:id/files/search`가 없었다. 그 결과 사용자가 실제로 `@` 멘션을 입력했을 때 파일 목록이 비어 보이는 문제가 발생했다(`파일목록이 안뜨는뎅` 실사용 보고로 발견). 원인 확인 후 `data/supervisor/server.pid`에 기록된 PID(216055)만 종료하는 문서화된 정상 재시작 절차를 안내했고(Claude의 직접 `kill` 시도는 auto mode 분류기가 차단해, 사용자가 `!` 접두사로 직접 실행), 감시 루프가 3초 뒤 새 PID(348099)로 정상 재기동해 헬스체크를 통과했다. tmux 기반 CLI 세션은 별도 프로세스라 영향받지 않았다.
- 후속 수정: 사용자가 "폴더는 안 나옴?"이라고 확인 요청해, `searchFiles`가 지금까지 디렉터리는 재귀 대상에서만 쓰고 결과에는 전혀 포함하지 않던 것을 확인했다. 이름이 일치하는 폴더도 `{ path, directory: true }`로 결과에 포함하도록 고치고(계속 재귀는 유지), 클라이언트 멘션 목록은 파일 검색 결과 타입을 문자열 배열에서 `{ path, directory }` 객체 배열로 바꿔 "파일"/"폴더" 배지로 구분해 표시한다. `tsc --noEmit` 무오류, 전체 655개 테스트(폴더 매칭 케이스 추가) 통과.
- 추가 UX 요청: 사용자가 "자동완성처럼 타고들어갈수있어야지 폴더만선택할수도있고"·"현재하위폴더파일 전부보여주든가"라고 이어서 요구해, 폴더를 "링크로 바로 삽입"하는 대신 셸 tab 자동완성처럼 그 폴더 안으로 들어가 하위 항목을 계속 보여주도록 다시 바꿨다. `drillIntoFolder`가 멘션 토큰을 `@폴더경로/`로 치환해 검색어를 그 접두사로 좁히며(전체 경로 부분일치 검색이라 "폴더경로/"가 곧 그 폴더 하위 전체를 자연히 보여줌 — 별도 prefix 검색 API 없이 세 번째 요청까지 함께 충족), 메뉴는 닫지 않고 유지한다. 폴더 자체를 링크로 넣고 싶을 때를 위해 각 폴더 행에 별도 "+" 버튼(마우스)과 Enter 키(키보드, Tab은 항상 드릴인)를 남겨 "폴더만 선택"도 가능하게 했다. `tsc --noEmit` 무오류, 전체 655개 테스트 통과(이 변경은 클라이언트 전용이라 백엔드 API 계약은 안 바뀜).
- 배포 노트: 폴더 지원(백엔드 응답 형태 변경)과 드릴인 UX(클라이언트 전용)를 위해 총 두 번 `npm run build`를 실행했고, 앞선 배포 사고의 교훈에 따라 재빌드마다 즉시 사용자에게 백엔드 재시작 필요 여부를 안내했다. 이 대화 시점까지 사용자는 아직 재시작(`kill 348099`)을 실행하지 않아, 두 클라이언트 빌드가 겹쳐 반영된 채로 재시작을 한 번만 안내해 마쳤고, 사용자가 직접 `! kill 348099`를 실행해 새 PID(385069)로 정상 재기동됐다.
- 실사용 버그(재현·수정): 사용자가 재시작 뒤 모바일에서 `@` 멘션 스크린샷을 보내왔는데, "폴더" 배지와 "+" 버튼은 보이는데 정작 폴더 이름 텍스트가 안 보였다. 데스크톱 폭 Playwright 검증에서는 `innerText`로 텍스트가 잡혀 통과로 오판했으나, 이는 `overflow:hidden`으로 폭이 0이어도 `textContent`는 그대로 읽히기 때문에 생긴 착시였다 — 실제로는 desktop 폭에서도 같은 버그가 있었는데 스크린샷 없이 텍스트만 확인해 놓쳤다. 격리 인스턴스에서 `getBoundingClientRect`·`getComputedStyle`로 직접 측정해 원인을 확정했다: `.mention-row-pick`(+버튼, 30px 의도)에 `width: 30px`을 `!important` 없이 줬는데, `.slash-menu button`의 `width: 100%`가 특이도(class+element vs class 하나)로 이겨 그 버튼이 행 전체 폭을 차지했고, 옆의 `.mention-row-main`(flex:1, flex-basis:0)이 사실상 0폭으로 밀려나 있었다(실측 18px → strong 요소 실폭 0px). `width: 30px !important; flex: 0 0 30px !important;`로 고쳐 `.mention-row-main`이 정상 폭(실측 206px)을 되찾는 것을 같은 방법으로 재확인했다. CSS 전용 수정이라 `tsc --noEmit`·전체 655개 테스트는 이전과 동일하게 통과(회귀 없음 확인용 재실행).
- 교훈: 이후 유사한 flex/grid 레이아웃 버그를 검증할 때는 `innerText`/`textContent`만으로 "텍스트가 존재한다"를 확인하지 말고, 반드시 스크린샷이나 `getBoundingClientRect`로 실제 렌더 크기까지 확인한다.
- 추가 버그 신고·수정: 사용자가 "@ 모달에서 화살표로 내리면 하이라이트가 화면 밖으로 쭉 내려가는데 스크롤은 안 따라옴, 그 뒤로는 화살표를 눌러도 선택이 안 움직이는 것처럼 보인다"고 보고했다. 코드를 보니 `mentionIndex`/`commandIndex`가 바뀔 때 해당 행을 스크롤 컨테이너 안으로 당겨오는 처리가 아예 없었다("안 움직이는 것처럼 보임"은 실제로는 인덱스가 바뀌지만 하이라이트된 행이 화면 밖에 있어 안 보이는 것과 같은 현상으로 판단). 이 버그는 새로 만든 `#채팅`/`@파일` 멘션 메뉴뿐 아니라 기존 슬래시 명령 메뉴에도 원래 있던 것이라(전에는 아무도 목록을 그렇게 길게 스크롤할 일이 없어 안 걸림) 둘 다 같은 방식으로 고쳤다. `mentionIndex`/`commandIndex` 변경 시 하이라이트된 행 ref로 `scrollIntoView({ block: "nearest" })`를 호출한다. 격리 인스턴스에서 폴더 30개를 만들어 두고 실제 모바일 폭(390px)에서 화살표를 20번 눌러 하이라이트가 항상 스크롤 뷰포트 안에 있는지 좌표까지 측정해 확인했다(이전 실수를 반복하지 않기 위해 텍스트 존재가 아니라 `getBoundingClientRect` 좌표 비교로 검증). `tsc --noEmit` 무오류, 전체 655개 테스트 통과. 클라이언트 전용 변경이라 백엔드 재시작은 불필요.
- 커밋: 위 채팅 링크 멘션·파일 줄 연결·발견한 버그 3건을 커밋 `2ca02ec`(`feat/agent-lab-v04`)로 묶어 반영. push는 하지 않음. repo 루트에 남아있던 사용자 메시지 첨부(`Screenshot_20260820_200819.jpg`, harness가 대화 첨부를 cwd에 저장한 것으로 추정)는 무관한 파일이라 커밋에서 제외.

## 2026-08-20 Claude - 파일 삭제·GitHub 탭 롤백 추가, 브랜치 선택 버그 2건 수정

- 작업 시작: 사용자가 세 가지를 요청했다 — (1) 파일 탭에 파일 삭제 기능 추가, (2) GitHub 탭에서 파일·폴더를 선택해 롤백(미커밋 변경 되돌리기)하는 기능 추가, (3) "현재 작업 브랜치 선택이 제대로 안 된다"는 버그 확인. (3)은 GitHub 탭의 "작업공간" 드롭다운을 말하는 것으로 사용자에게 직접 확인받았다.
- 브랜치 선택 버그 원인: `GitView.tsx`의 60초 자동 새로고침 `useEffect`가 `[project?.id, chat?.id, tab]`만 의존성으로 갖고 `workspacePath`·`commitRef`를 빠뜨려서, 작업공간을 선택해도 effect가 재실행되지 않아 `setInterval` 콜백이 선택 전 시점의 `load()` 클로저를 계속 들고 있었다. 1분 뒤 그 오래된 클로저가 실행되며 예전 작업공간 데이터로 되돌아가 방금 고른 선택이 "안 먹는" 것처럼 보였다. 의존성 배열에 두 값을 추가해 고쳤다.
- 이어서 사용자가 "채팅에서 AI한테 브랜치 새로 만들어 스위치해도 GitHub 탭은 이전 브랜치를 보고 있다"는 관련 증상을 추가로 제기했다. 원인은 `GitBranchControl.tsx`(GitHub 탭에 `canManage=false`로 읽기 전용 표시만 하는 위젯)가 `[projectId, chat?.id]` 변경 시에만 조회하고 별도 폴링이 전혀 없었던 것 — 터미널 안에서 AI가 이 앱의 `/git/branch` API를 거치지 않고 직접 `git checkout -b`로 브랜치를 바꾸면 이 위젯은 그 사실을 알 방법이 없어 브랜치 배지가 예전 이름에 무기한 머물렀다. 20초 주기 폴링을 추가하되, 메뉴를 펼쳐 고르는 중이거나 전환 요청이 진행 중이면 화면이 발밑에서 바뀌지 않도록 건너뛴다.
- 파일 삭제 구현: 서버에 `DELETE /projects/:id/files`(관리자·신뢰 네트워크 전용)를 신설했다. 다른 파괴적 API와 같은 정책이면서도 한 단계 더 보수적으로, 요청이 신뢰 네트워크라도 `access.allowHidden`을 강제로 비워 `.git` 등 민감 경로는 이 API로 절대 지울 수 없게 했다(목록·미리보기는 신뢰 네트워크에서 민감 경로도 보여주는 게 편의지만, 삭제는 실수 한 번의 대가가 회복 불가능하게 크기 때문). 클라이언트(`FilesView.tsx`)는 목록 행과 미리보기 헤더 양쪽에 휴지통 버튼을 추가하고 확인창 뒤 삭제, 지우던 파일을 미리보고 있었으면 미리보기도 닫는다.
- GitHub 탭 롤백 구현: 서버에 `POST /projects/:id/git/discard`(관리자·신뢰 네트워크 전용)를 신설했다. 선택 경로를 다시 `git status`로 나눠 추적 파일(수정·삭제·스테이지된 신규 파일 포함)은 `git restore --source=HEAD --staged --worktree`로 HEAD 기준 복원하고, 한 번도 add하지 않은 순수 untracked 파일은 되돌릴 원본이 없어 `git clean -fd`로 삭제한다 — `git checkout`이 아니라 `git restore`를 쓴 이유는 스테이지된 신규 파일(HEAD에 없는 경로)도 예외 없이 통일된 방식으로(index+worktree 동시) 제거하기 위함이다. "폴더 선택"은 git status가 파일 단위로만 나오는 한계를 UI에서 흡수했다 — 변경 파일 목록을 바로 위 폴더 하나로 묶어(`changeGroups`) 폴더 헤더 체크박스로 그 폴더 아래 파일 전부를 한 번에 선택하고(부분 선택은 인디터미네이트로 표시), 기존 "선택 파일 커밋" 옆에 "선택 파일 롤백" 버튼을 추가했다(같은 `selectedFiles`를 공유하므로 커밋과 동일하게 다른 작업공간·브랜치를 보는 중에는 비활성화).
- 검증: `tsc --noEmit` 무오류. 신규 테스트 `tests/git-discard.test.ts`(실제 임시 git 저장소로 수정/삭제된 추적 파일 복원·스테이지 신규 파일과 untracked 파일 삭제·폴더 단위 다중 선택·외부망 403·빈 선택 400 검증, 감사 로그의 `users` FK 때문에 harness에 관리자 사용자 행을 미리 넣어둠 — 기존 `git-diff-untracked.test.ts` harness에는 없던 부분이라 처음엔 FK 오류로 전부 실패했다가 원인을 찾아 고쳤다)와 `tests/file-routes.test.ts`(삭제 API의 외부망 차단·민감 경로 차단) 추가. 전체 663개 테스트 통과. 격리 인스턴스에서 실제 git 저장소(추적 파일 수정 2개 + untracked 폴더/파일)를 만들어 Playwright로 파일 탭 폴더 삭제, GitHub 탭 폴더 그룹 체크박스 선택→롤백까지 실제 화면과 실제 git 상태(`git status`)로 확인했다 — 선택한 파일만 원복되고 선택 밖 파일은 그대로, untracked 파일은 삭제되는 것을 확인.

- 추가 요청: 사용자가 "현재 브랜치를, 현재 작업중인 브랜치가 아닌 명시적인 브랜치명으로 보이도록 수정"을 요청했다. GitHub 탭의 작업공간·커밋 내역 드롭다운 기본 옵션이 각각 "선택한 채팅 기준"·"현재 작업공간 브랜치"라는 서술형 문구만 보여주고 실제 브랜치 이름은 드러내지 않던 것을 실제 이름으로 바꿨다. 두 옵션의 의미가 달라(커밋 내역 쪽은 "지금 보고 있는 대상", 작업공간 쪽은 "항상 기본/채팅 worktree로 돌아가기") 데이터 출처도 나눴다 — 커밋 내역 기본값은 이미 60초 주기로 갱신되는 `git.status` 첫 줄에서 브랜치명을 파싱(`currentBranchFromStatus`, 지금 보고 있는 워크스페이스가 어디든 항상 정확)하고, 작업공간 기본값은 별도로 유지하던 `/git/workspace`(채팅 기준, worktree override 없음) 조회 결과를 `loadWorkspaceMeta`로 함수화해 같은 60초 자동 새로고침 사이클에 포함시켰다(전에는 이 조회가 프로젝트 전환 때만 한 번 실행돼, 마찬가지로 오래되면 낡은 값을 보여줄 수 있었다). 클라이언트 전용 변경. `tsc --noEmit` 무오류, 전체 663개 테스트 통과. 격리 인스턴스에 `feature/explicit-branch-test` 브랜치로 저장소를 만들어 두 드롭다운 첫 옵션 텍스트를 Playwright로 직접 읽어 실제 브랜치명이 나오는지 확인(스크린샷도 함께 확인). 서버 API는 안 바뀌어 백엔드 재시작은 불필요.

## 2026-08-20 Claude - v0.5.0 공개 릴리즈 준비(systemd 루트 설치 안내 겸용)

- 작업 시작: 사용자가 "systemd에 루트 권한으로 설치"하는 방법 안내와, 지금까지의 변경사항을 v0.5.0으로 공개 레포(`public` 리모트, github.com/ljy1348/web-agent-manager)에 배포해달라고 요청했다. systemd 설치는 기존 `deploy/web-agent-manager.service`·`packaging/install.sh`·README `## systemd 배포` 절을 그대로 안내(서비스 자체는 보안 하드닝을 위해 전용 비루트 사용자로 구동하는 기존 설계를 유지하고, "루트 권한"은 `sudo systemctl`/`/etc/systemd` 등록 절차로 해석)했다.
- 공개 릴리즈 범위 확인: 공개 태그가 `v0.3.0`에서 멈춰 있고 `v0.4.0`은 태그가 없음(`release/v0.4.0-public` 브랜치가 `public/main`과 동일 — 이전 세션에서 0.4.0 공개 준비가 시작됐지만 완료되지 못하고 중단된 것으로 보임)을 발견했다. `public/main` 대비 사설 main(`feat/web-agent-manager`)이 106커밋, 현재 체크아웃된 `feat/agent-lab-v04`(Agent Lab 실험실 기능이 주력)가 거기서 232커밋 더 앞서 있어 총 338커밋 차이. AskUserQuestion으로 사용자에게 (1) agent-lab-v04까지 전부 포함할지 (2) 정제 브랜치를 먼저 만들지 확인받아 둘 다 "예"로 확정했다.
- 보안 점검: fork 서브에이전트로 `public/main..feat/agent-lab-v04` diff(205파일, +23085/-423) 전체를 시크릿·PII·로컬경로 기준으로 스캔시켰다. 실제 시크릿/API키/`.env` 실값 노출은 없었으나, `history.md`의 신규 구간에 사용자의 무관한 다른 비공개 프로젝트 경로(`/home/ubuntu/websoso/WSS-admin-web-admin-platform`, `/home/ubuntu/resume`)가 그대로 노출된 것을 확인했다. 흥미롭게도 `history.md` 1083번째 줄 자체에 과거(0.4.0 준비 당시) 같은 문제를 감사로 발견해 "신규 531줄을 공개 후보에서 제외해야 한다"고 판단한 기록이 남아 있었다 — 그 결정이 실제로 반영되지 못한 채 0.4.0 공개가 중단됐던 것으로 보인다. LAN IP(`192.168.0.31`)·`/root/.volta`·`/home/ubuntu/myagent` 등 이 프로젝트 자체를 가리키는 경로는 과거 공개 릴리즈에도 이미 실려 있던 선례가 있어(공개 레포 `history.md`에서 확인) 이번엔 건드리지 않고, 무관한 타 프로젝트를 특정하는 두 항목만 정제하기로 했다.
- 정제 브랜치 작업: `release/v0.5.0-public`을 이 브랜치에서 분기해 `history.md`의 무관한 타 프로젝트 경로 2건을 `[다른 프로젝트 경로]`로 치환했다(이 사설 브랜치의 원본 `history.md`는 그대로 둔다 — 정제는 공개 브랜치에만 적용).
- `npm run build`(공개 빌드 경로 검사 포함)·`npm run typecheck` 로컬 검증 통과.
- push 시도 중 발견: `public` 리모트의 `main`과 사설 `feat/web-agent-manager`가 공통 조상이 없었다(사설 main이 과거 어느 시점에 히스토리 재작성됨 — `release/v0.4.0-public`은 옛 히스토리 그대로라 `public/main`과 동일했지만, 지금의 사설 main엔 그 커밋들이 없다). `git branch -f`로 기존 브랜치를 재구성하려던 시도는 auto mode 분류기가 차단해, 사용자에게 이유를 설명하고 AskUserQuestion으로 진행 방식을 확인받았다(공개 main 기준 재구성 선택).
- `release/v0.5.0-public-squashed`를 `public/main`에서 새로 분기해 준비한 파일 상태(버전 0.5.0 + history.md 정제)를 커밋 하나로 올렸다. 이 과정에서 `.agents/skills`·`.claude/skills`의 심볼릭 링크 2개가 이 머신의 절대경로(`/home/ubuntu/myagent/...`)를 가리키는 것을 추가로 발견해 상대경로로 고쳤다(다른 환경에서 clone하면 깨지는 이식성 문제 — 사설 브랜치엔 이미 있던 기존 버그라 이번 정제에서만 고치고 사설 쪽은 건드리지 않음).
- 공개 레포에 push 후 PR #41(`release/v0.5.0-public-squashed` → `main`) 생성. `main`은 브랜치 보호(PR 필수, CI `verify` 통과 필수, force push 금지)가 걸려 있어 직접 push는 불가능했다. Claude는 전역 규칙(PR 자체 머지 금지)에 따라 머지는 시도하지 않고 사용자에게 PR 링크를 전달해 리뷰·머지를 맡긴다. CI 결과 확인과 머지 후 `v0.5.0` 태그 생성은 후속 작업으로 남긴다. 이전에 push했다가 히스토리 단절 문제로 못 쓰게 된 `release/v0.5.0-public` 브랜치(공개 레포)는 정리하지 않고 남겨뒀다.
- CI 1차 실행 결과: 필수 체크 `verify`가 `tests/experiment-foundations.test.ts`에서 `EACCES: permission denied, rmdir '.../.claude-plugin'`로 실패했다. 원인 확인: `ExperimentSkillBundleService.prepare()`(`src/server/experiments/skill-bundle.ts:187`)가 번들 디렉터리를 `chmodSync(bundleRoot, 0o555)`로 읽기전용화하는데, 이 테스트 파일의 `afterEach` 임시 디렉터리 정리(`fs.rmSync(root, {recursive:true})`)가 삭제 전 권한 복원 없이 바로 지우려 해 root가 아닌 GitHub Actions 러너에서 실패했다. 이 컨테이너는 root라 로컬에서는 재현되지 않아, `sudo -u ubuntu`로 비루트 사용자를 만들어 동일한 chmod 0o555 디렉터리를 실제로 재현·검증한 뒤 원인을 확정했다. `unlockForCleanup()` 헬퍼(정리 전 재귀적으로 0o700 복원)를 테스트에 추가해 사설 브랜치(`feat/agent-lab-v04` 커밋 `33b56ba`)와 공개 PR 브랜치 양쪽에 동일하게 반영하고 push했다(공개 레포에 처음 CI를 돌려서 드러난, 로컬 root 환경 때문에 지금까지 아무도 못 본 실제 버그였다).
- `android` job(Android API 36 설치의 `sdkmanager: command not found`, 워크플로 자체에 SDK 설치 단계 없이 PATH만 가정한 기존 버그)도 실패했지만, 브랜치 보호의 필수 상태 검사 목록엔 `verify`만 있어 머지를 막지 않는다. 범위 밖 별도 이슈로 남기고 이번 릴리즈에서는 고치지 않았다.
- 2차 CI 실행에서 `verify` 통과 확인. 사용자에게 PR #41 링크와 머지 대기 상태를 전달했다. 머지·태그·GitHub Release 발행은 사용자 확인 후 진행한다.
- 사용자가 android까지 마저 조사·수정해달라고 요청해 두 건을 추가로 고쳤다. (1) android job: `android-actions/setup-android@40fd30f`(v4.0.1, 다른 스텝처럼 커밋 해시 고정)를 `setup-gradle` 다음·`sdkmanager` 호출 전에 추가해 PATH 문제를 해소했다(GitHub API로 실제 릴리스 커밋 해시를 조회해 고정, 기존 `gradle/actions/setup-gradle` 스텝을 실수로 지웠다가 즉시 복구). (2) `ui` job: `tests/e2e/ui.spec.ts:1372` 테스트를 로컬에서 재현(30초 타임아웃 그대로 재현됨 — CI만의 일시적 문제가 아니라 실제 회귀)하고 실패 시점 `error-context.md`의 접근성 트리를 읽어, GitHub 탭 폴더 그룹핑 추가(`GitView.tsx`, 커밋 `e2ef697`) 이후 체크박스 접근성 이름이 `src/selected.ts`에서 `selected.ts 수정`(폴더 프리픽스를 상위 그룹 헤더로 분리)으로 바뀐 게 원인임을 확인했다. 옛 전체경로 기준 셀렉터 2곳을 새 이름 규칙(`/^selected\.ts/`, `/^deleted\.ts/`)으로 고치고 해당 테스트와 전체 `ui.spec.ts` 20개(1개 skip) 모두 로컬에서 재통과를 확인했다. 두 수정 모두 사설 브랜치(`feat/agent-lab-v04` 커밋 `49b7947`)와 공개 PR 브랜치에 동일하게 반영해 push.
- 3차 CI에서 `verify`·`android`·`ui` 세 체크 모두 통과 확인. PR #41은 머지 가능 상태이며, 전역 규칙(PR 자체 머지 금지)에 따라 머지는 사용자에게 맡긴다. 머지 확인 후 `v0.5.0` 태그·GitHub Release 발행이 남은 작업이다.
- 사용자가 머지를 완료했다고 확인해줘서, 머지 커밋(`26259c2`, public `main`)에 `v0.5.0` 태그를 만들어 push하고 CHANGELOG.md의 0.5.0 항목을 릴리즈 노트로 그대로 사용해 GitHub Release를 발행했다: https://github.com/ljy1348/web-agent-manager/releases/tag/v0.5.0
- 마무리: `release/v0.5.0-public-squashed`는 머지 시 자동 삭제됐다. 처음에 히스토리 단절 문제로 못 쓰게 됐던 `release/v0.5.0-public`(공개 레포, 커밋 `d7a979e`)은 사용자 확인 후 삭제했다. v0.5.0 공개 릴리즈 작업 완료. (참고: 이번 릴리즈는 `feat/agent-lab-v04`를 기준으로 했고, 그 위에서 만든 버전 올림·정제·CI 수정 커밋들은 사설 `feat/web-agent-manager`(main)엔 아직 병합되지 않았다 — 이번 요청 범위는 공개 배포까지였고 사설 main 병합은 별도 요청이 없어 진행하지 않았다.)

## 2026-08-20 Claude - Codex 사용량이 오래 갱신되지 않는 문제(실제로는 Claude 사용량) 조사·수정

- 작업 시작: 사용자가 "코덱스 사용량 업데이트 안된다, 터미널 모드에서는 19%인데"라고 보고했다. Claude(claude-sonnet-5)가 조사했다.
- 1차 확인: DB(`usage_status`)를 직접 조회해 Codex 2%·Claude 65%·Grok 19% 세 값을 확인, 사용자가 본 19%는 Codex가 아니라 Grok 계정 값임을 안내했다. 사용자가 스크린샷(Claude CLI 실제 `/usage` 화면: 세션 20%, 주간 60%)을 첨부하며 "대시보드/헤더 배지가 코덱스라고 되어 있는데 실제로는 클로드 값"이라고 정정 — 실제 불일치 대상은 Claude였다.
- 원인 조사 1단계(프로세스 중복): `ps`·`/proc/net/tcp`·`/proc/[pid]/fd`로 포트 14003 리스닝 소켓 소유자를 추적해, supervisor가 추적하는 정상 프로세스(PID 588477) 외에 **다른 세션이 `npm start`로 직접 띄우고 정리 안 한 고아 프로세스(PID 215945, 7시간 넘게 생존, HTTP 포트는 못 잡았지만 내부 사용량 폴링 타이머는 계속 돌며 같은 SQLite DB에 동시에 씀)**를 발견했다. 이 프로세스는 이번 세션이 띄운 게 아니라(사설 CLAUDE.md·메모리 규칙상 임의 종료 금지) 사용자에게 확인받고 종료했다. 종료 직후에도 Claude 값(65%)은 그대로였다 — 프로세스 중복은 있었지만 이번 정체(freeze)의 근본 원인은 아니었다.
- 원인 조사 2단계(실제 CLI 재현): 사용자가 "검증 터미널부터 확인해봐"라고 요청해, 앱과 동일한 실행 방식(`claude --safe-mode --ax-screen-reader`, `/usage` 전송)을 별도 tmux 세션으로 격리 재현했다. 첫 `/usage`는 실제 최신값(세션 25%, 주간 60%, "Resets 3am")을 정상적으로 가져왔지만, **이미 `/usage` 화면이 열린 상태에서 `/usage`를 다시 보내도 화면이 완전히 그대로였다**("you: /usage" 에코 줄조차 새로 안 찍힘) — 반면 Esc로 닫은 뒤 다시 보내면 "Total duration (wall)"이 증가하며 진짜로 재조회됐다. 원인 확정: `src/server/providers/claude.ts`의 `ClaudeAdapter`에 Grok 어댑터(`grok.ts`)에는 있는 `usageScreenCloseInput`(Esc)이 없어서, 사용량 조회용 PTY가 오래 살아있는 동안 최초 1회 조회 이후로는 영원히 같은 화면만 재파싱하고 있었다. `detectUsageRegression`은 "값이 줄었을 때"만 거부하는데 이 경우는 값이 아예 안 바뀌니(동일 스냅샷 반복) 거부 로직도 안 걸리고 `data_status: "fresh"`로 계속 저장돼 겉으로는 정상처럼 보였다.
- "아까까지는 잘 됐는데 갑자기 왜 그러냐"는 사용자 질문에는, 이 버그 자체는 원래 있었지만 오늘 v0.5.0 릴리즈 작업 중 `npm run build`로 서버가 여러 번 재시작되면서 그때마다 조회용 PTY도 새로 떠 우연히 매번 최신값을 받아왔고, 릴리즈 작업이 끝나고 재시작 없이 3시간 넘게 안정적으로 떠 있는 동안 그 사이 딱 한 번 열린 `/usage` 화면이 그대로 굳어버린 것이라고 설명했다.
- 수정: `ClaudeAdapter`에 `readonly usageScreenCloseInput = "";`를 추가(Grok과 동일 패턴). 처음 Edit 시도에서 두 번이나 리터럴 `` 대신 실제 ESC 제어문자(0x1b)가 파일에 그대로 박혀 들어가는 실수를 했고(터미널·Read 도구에는 안 보여서 빈 문자열처럼 착각), `od -c`로 원인을 확인한 뒤 Python으로 바이트 단위 치환해 가독성 있는 이스케이프 표기로 고쳤다. `tests/claude-history.test.ts`에 회귀 테스트(`usageScreenCloseInput`이 Esc인지) 추가.
- 검증: `tsc --noEmit` 무오류, 관련 테스트 3개 파일 89개 전부 통과, `npm run build`(공개 빌드 경로 검사 포함) 통과. 사용자 승인 하에 운영 서버를 supervisor PID(`data/supervisor/server.pid`) 기준 정상 재시작 절차로 재기동(고아 프로세스처럼 임의 kill이 아니라 문서화된 절차 사용)했고, 재기동 뒤 첫 폴링부터 Claude 사용량이 28%(reset 3am)로 실제 값과 일치해 갱신됐으며 이후 여러 주기 동안 안정적으로 유지되는 것을 DB 직접 조회로 확인했다.
- 남은 작업: 이 수정은 사설 브랜치(`feat/agent-lab-v04`)에만 커밋했고, 아직 v0.5.0 공개 릴리즈에는 반영 안 됨(패치 릴리즈로 낼지는 사용자 판단 필요). 고아 프로세스가 왜 생겼는지(다른 세션이 정리를 안 했는지)는 근본 원인까지는 추적하지 않았다.

## 2026-08-20 Claude - systemd 설치 절차를 git clone·완전 자동 설치까지 지원하도록 정비

- 작업 시작: 사용자가 호스트에 새로 만든 systemd+Docker 컨테이너에서 myagent를 git clone으로 설치하다가 연속으로 문제를 겪었다: (1) `packaging/install.sh`가 "Cannot find module './package.json'"로 실패, (2) `npm install @anthropic-ai/claude-code`가 `Tracker "idealTree" already exists`로 실패(원인: `-g` 없이 루트 디렉터리에서 실행), (3) `npm ci`가 `node-pty` 네이티브 빌드 중 `make: not found`로 실패. 사용자가 "이렇게 하나하나 해야 하냐, 자동화를 했어야지" · "의존성 설치도 자동으로 되어야지" · "curl\|sh 한 줄처럼 되게" 순서로 요구 수위를 높였다.
- 원인 진단: `packaging/install.sh`·`create-admin.sh`·`run.sh` 세 스크립트 모두 `dirname "${BASH_SOURCE[0]}"`를 앱 루트로 가정했다. 이건 `npm run release:package`가 만드는 배포 zip에서만 참이다(archiver가 packaging/ 내용물을 zip 최상위로 평평하게 풀기 때문) — git 저장소를 그대로 쓰면 이 파일들이 `packaging/` 서브폴더 안에 있어 가정이 깨졌다. 또한 `install.sh`는 이미 빌드된 `dist/`가 있다는 것과 production 의존성(`npm ci --omit=dev`)만 있으면 된다는 것도 전제했는데, git clone은 소스 상태라 devDependencies로 직접 빌드해야 한다.
- 수정 1(경로 자동 판정): 세 스크립트 모두 `$SCRIPT_DIR/package.json` 존재 여부로 압축 zip(평평해진 경우)과 git 저장소(packaging/ 안에 있는 경우)를 자동 구분하도록 고쳤다. 로컬에서 두 시나리오를 각각 시뮬레이션(zip은 임시 폴더에 스크립트+더미 package.json, git clone은 저장소 안에서 직접 실행)해 둘 다 올바른 루트를 찾는 것을 확인했다.
- 수정 2(소스 자동 빌드): `dist/server/src/server/index.js` 존재 여부로 "이미 빌드된 배포본"과 "소스 체크아웃"을 구분해, 후자는 `npm ci`(devDependencies 포함) + `npm run build`를 자동으로 돌리도록 분기를 추가했다.
- 수정 3(빌드 도구 자동 설치): `node-pty` 같은 네이티브 모듈은 이 환경용 prebuild가 없으면 그 자리에서 컴파일하는데 `python3`·`make`·`g++`가 없으면 실패한다. `require_command`에 apt 패키지명을 받는 인자를 추가해, Linux·root·apt-get이 모두 갖춰진 경우에만 `apt-get install -y`로 자동 설치하고, 아니면 기존처럼 안내만 하고 중단하도록 했다(macOS·비root 환경을 임의로 건드리지 않기 위함). Node.js 자체는 버전 관리 방식이 사용자마다 달라(nvm·volta·nodesource) 자동 설치 대상에서 제외했다.
- 신규 `packaging/install-systemd.sh`: 빌드부터 전용 시스템 사용자 생성·`/etc/web-agent-manager/web-agent-manager.env` 준비·systemd 유닛 등록(`/opt/web-agent-manager` 하드코딩을 `sed`로 실제 설치 경로로 치환)·`daemon-reload`·`enable --now`까지 한 스크립트로 묶어, `sudo bash packaging/install-systemd.sh` 한 번으로 systemd 서비스가 뜨게 했다. root 권한 없으면 바로 안내 후 중단한다.
- 검증: 세 스크립트 `bash -n` 문법 검사 통과. 이 저장소에서 실제로 `bash packaging/install.sh`를 실행해 정상 경로(이미 빌드됨 → `--omit=dev`)가 안 깨지는 것을 확인했는데, 이 과정에서 devDependencies가 지워져(`npm ci --omit=dev`가 실제로 지움) `npm ci`로 즉시 복구하고 `vitest` 재실행으로 개발 환경이 정상임을 재확인했다. `install-systemd.sh`는 systemd 컨테이너가 없는 이 샌드박스에서는 직접 실행 검증을 못 했다(root라 `[[ "$(id -u)" -ne 0 ]]` 체크는 통과하지만 systemd 자체가 없음) — 사용자의 실제 호스트 컨테이너에서 실행 결과를 기다린다.
- 사용자가 curl\|sh 한 줄 설치를 요구했으나, 아직 이 브랜치가 공개·사설 원격 어디에도 push되지 않아(로컬 커밋만 있음) 인터넷에서 바로 받아 실행하는 형태는 당장 제공할 수 없다고 판단, 대신 이미 clone된 저장소 안에서 쓸 수 있는 단일 명령(`sudo bash packaging/install-systemd.sh`)과 최신 파일을 직접 붙여넣는 방법을 안내했다. 커밋(`a6828a6`) 후 사설 원격 `origin`(web-agent-manager-private)에 `feat/agent-lab-v04`를 push해 호스트에서 `git pull`로 받을 수 있게 했다.
- 사용자가 이어서 "위에서 수동으로 설치했던 git·gh·node도 없으면 자동으로 설치돼야 한다"고 요구해, 앞서 "Node는 버전 관리 방식이 제각각이라 자동화하지 않는다"고 판단했던 것을 뒤집었다. `git`·`gh`는 tmux와 같은 방식(apt 패키지명)으로 자동 설치 대상에 추가했다. Node는 Debian/Ubuntu 기본 apt 저장소 버전이 보통 22 미만이라(Ubuntu 24.04 기본 18) 단순 `apt install nodejs`로는 버전 요구사항을 못 맞춰, NodeSource 공식 22.x 설치 스크립트(`curl ... deb.nodesource.com/setup_22.x | bash -` 후 `apt-get install nodejs`)를 Linux·root·apt 조건에서만 자동 실행하는 `ensure_node()`로 만들었다. 이미 22 이상이 있으면 아무것도 안 하고 바로 통과한다.
- 검증: `bash -n` 문법 통과. 이 저장소(Node 22 이미 있음)에서 다시 `bash packaging/install.sh`를 실행해 `ensure_node`가 즉시 통과하고 기존 정상 경로가 그대로 동작하는 것을 확인, devDependencies가 또 지워져 `npm ci`로 재복구했다. NodeSource 자동 설치 분기 자체(Node 없는 상태)는 이 샌드박스에 이미 Node가 있어 직접 실행 검증은 못 했다 — 로직은 기존에 사용자에게 안내했던 수동 명령과 동일해 실패 가능성은 낮다고 판단.
