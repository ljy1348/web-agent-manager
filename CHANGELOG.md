# 변경 기록

## 0.6.0 - 2026-09-19

### 프로젝트·예약 자동화

- 예약에 `실행 날짜`를 추가했다(#99). 날짜를 넣으면 매일 반복 대신 그 날짜·시각에 한 번만 실행하고 자동으로 꺼진다. 서버가 그때 꺼져 있었다면 다시 켜진 뒤 한 번 실행하고, 이미 지난 날짜·시각은 저장하지 않는다.

- 프로젝트 추가 화면을 `새 프로젝트`·`기존 폴더`·`GitHub 저장소`로 분리했다. 새 프로젝트는 설정된 워크스페이스 아래에 폴더를 만들고 `main` 브랜치 Git 저장소로 초기화하며, 선택적으로 Private·Public·Internal GitHub 저장소를 만들고 `origin`까지 연결한다.
- 서버 재시작 뒤에도 유지되는 매일 예약 입력을 추가했다. 일정마다 새 Codex·Claude·Grok 채팅을 생성하거나 지정한 기존 채팅을 이어서 쓸 수 있고, 프로젝트·계정·현지 시각·시간대·입력 문구를 지정해 일시정지·수정·즉시 실행·삭제할 수 있다. 실행일을 DB에서 먼저 선점해 같은 날 중복 실행하지 않으며, 예약 시각에 서버가 꺼져 있었다면 같은 날 재기동 시 한 번 보충 실행한다.

### UI·설정

- 활성 채팅의 기록 갱신마다 브라우저가 대시보드 전체 API를 다시 호출하던 CPU 회귀를 고쳤다. 연속 갱신은 채팅 목록·현재 메시지 요청 하나씩으로 합치고, 사용량·승인은 해당 API만 갱신한다. 서버의 shell 기반 네트워크·프로세스 상세 수집도 각각 30초·60초로 낮추되 전체 CPU·메모리는 5초 갱신을 유지한다.
- 작업 탭에 같은 현재 채팅이 여러 `작업 중` 카드로 쌓이던 문제를 고쳤다. 카드 하나는 웹 입력 1회를 뜻하며, provider turn 종료·실패·중단과 상태를 맞추고 새 입력 시작 시 앞선 입력을 완료 처리한다. 재시작 때도 idle 채팅의 과거 started task만 보수적으로 정리한다. 화면에는 이 카드 단위와 preview·브라우저 증거 수집이 수동 동작임을 명시했다.
- 웹 화면 확인에서 raw loopback URL과 긴 기능 설명을 먼저 보여주지 않는다. 프로젝트·연결 worktree에서 실제 실행 중인 HTTP listener를 자동 탐지해 선택하게 하고, 찾지 못했을 때만 개발 서버 실행 후 로컬 주소를 입력하라는 한 줄 안내를 표시한다. 화면 크기는 접힌 보조 설정으로 옮겼다.
- 채팅 입력 중 WAM의 다른 탭이나 다른 채팅으로 이동하면 작성하던 텍스트가 사라지던 문제를 고쳤다. 초안은 현재 로그인 화면의 메모리에 채팅별로 유지하고 전송·로그아웃 때 지우며, 민감한 입력을 영구 저장하지 않도록 새로고침·브라우저 종료 뒤에는 남기지 않는다.
- Claude가 작업 중일 때 보낸 후속 입력이 실제로 대기열에 들어갔는데도 제출 실패로 뜨고, 진행 중인 채팅이 `대기중`으로 바뀌던 문제를 고쳤다(#103). Claude JSONL의 대기열 기록을 제출 증거로 인정하고, 원래 작업 중이던 채팅은 실패해도 작업중 표시를 유지한다.
- 다른 프로젝트 채팅을 연 뒤 아무 채팅이나 갱신되면 옛 프로젝트의 방금 갱신된 채팅으로 화면이 넘어가던 문제를 고쳤다(#102). 채팅 링크·실행 중 패널로 연 채팅도 마지막 세션으로 저장한다.
- 채팅 응답의 코드 블록 오른쪽 위에 `복사` 버튼을 추가했다(#101). 빌드·재시작 같은 명령을 드래그 없이 그대로 복사하고, 모바일에서는 코드 폭을 덜 잡아먹도록 아이콘만 보인다.
- 채팅 목록 맨 위에 `실행 중 · 전체 프로젝트` 묶음을 추가했다(#100). 여러 프로젝트를 동시에 돌릴 때 프로젝트를 옮기지 않고도 실행 중·작업 중·권한 요청·리밋 대기 채팅을 프로젝트 이름과 함께 한눈에 보고, 누르면 그 프로젝트로 전환해 연다. 일반 채팅 카드 모양이라 자리를 많이 차지하고 아래 목록과 구분되지 않던 첫 모양을, 테두리 패널 안 프로젝트별 한 줄 행으로 다시 만들었다.
- 웹 입력 전송 확인을 다시 설계했다(#98). Codex가 대기열·기록 지연으로 user 기록을 늦게 쓰면 실제로 전송된 질문을 지우고 "제출을 확인하지 못했습니다"를 띄워 재전송·중복을 부르던 문제를 고쳤다. 이제 붙여넣은 글이 입력창에 보인 뒤 Enter를 보내고, 입력창이 비워짐(작업 중이면 대기열 진입)·UserPromptSubmit 훅·JSONL 기록 중 하나로 제출을 확인한다. 글이 입력창에 계속 남아 있을 때만 Enter를 최대 3번 재시도하고 실패로 판정한다. 작업 중 후속 입력도 같은 방식으로 확인한다.
- Grok 채팅도 훅으로 턴 상태를 즉시 반영한다(#96). 서버 시작 시 `~/.grok/hooks/web-agent-manager.json` 전역 훅을 설치해 SessionStart로 세션 ID를 연결하고 UserPromptSubmit·Stop·StopFailure·StopCancelled로 작업중 표시와 동기화를 앞당긴다. 채팅 ID 환경변수가 없는 Grok 세션(사용자 직접 실행·조회 PTY)에서는 아무것도 하지 않는다. Grok에는 PermissionRequest가 없어 승인은 계속 화면 감지로 처리한다.
- Codex 명령 실행·파일 수정 승인을 `PermissionRequest` 훅으로 받아 웹 승인 카드로 처리한다(#95). Codex 훅은 허용/거부만 돌려줄 수 있어 Codex 카드에는 세션 허용 버튼을 두지 않는다. WAM 서버가 응답하지 못하면 결정을 내지 않아 Codex 기본 승인 화면이 뜨고 기존 화면 감지가 이어받는다. 한도·모델 전환·디렉터리 신뢰 화면은 계속 화면 감지로 처리한다.
- Claude가 사용량 한도로 턴을 멈추면 보내는 `StopFailure(rate_limit)` 훅으로 리밋 대기를 보완 등록한다(#94). 화면 감지가 5초 안에 먼저 등록했으면 아무것도 하지 않아 알림이 두 번 가지 않는다. 리셋 문구 파서가 "resets 3am"의 "ets 3"을 날짜로 오인해 정각 리셋 시각을 비우던 문제도 고쳤다(화면 감지 경로에도 적용).
- 훅이 붙은 Claude·Codex 채팅은 UserPromptSubmit·Stop 훅으로 작업중 표시를 바로 바꾼다(#93). Codex async 훅은 최신 턴의 종료일 때만 해제한다. 폴링이 찾은 턴 종료마다 Stop 훅 도착 여부를 대조하는 관찰 통계(`GET /api/admin/hook-stats`, 누락은 경고 로그)를 추가했다. 폴링 주기 완화와 Codex 추측 귀속 삭제는 이 통계로 누락이 없음을 확인한 뒤 진행한다.
- Claude 웹 채팅에서 `/clear`하면 새 대화가 목록에 안 보이는 숨김 채팅으로 빠지고 원래 채팅이 옛 대화에 멈추던 문제를 고쳤다(#91). `SessionStart` 훅이 새 session ID를 알려 주면 같은 채팅을 새 대화에 옮겨 붙이고, `/clear` 전 대화는 다음 스캔에서 목록에 안 보이는 외부 채팅으로 보존한다(파일은 삭제하지 않음).
- 웹에서 띄운 Codex 채팅에 WAM 훅을 도입했다(#92). `SessionStart`로 첫 기록을 session ID로 정확히 연결해 첫 질문 대조·생성 순서 추측에 기대지 않고, `UserPromptSubmit`·`Stop`으로 턴 상태를 즉시 반영한다. Codex는 비관리 훅을 해시 신뢰 없이 실행하지 않아 사용자 결정에 따라 WAM 채팅 실행에만 `--dangerously-bypass-hook-trust`를 붙인다. 새로 시작·재개한 채팅부터 적용된다.
- Claude 훅 1단계(#90): 승인 훅이 tmux의 채팅 ID를 함께 보내 같은 프로젝트의 다른 Claude 채팅에 승인 카드가 붙지 않는다. 웹에서 띄운 Claude는 `UserPromptSubmit`·`Stop`·`StopFailure` HTTP 훅으로 턴 시작·종료를 알려 작업중·완료 반영이 2초 폴링을 기다리지 않는다. 훅 실패는 Claude 작업을 막지 않으며 기존 폴링이 fallback으로 남는다. 새 설정은 새로 시작·재개한 Claude 세션부터 적용된다.
- Codex처럼 신규 session ID를 미리 지정할 수 없는 공급자는 웹에서 보낸 첫 질문 claim과 실제 JSONL 첫 user 메시지를 정확히 연결한다. 빈 채팅 생성 순서나 busy 반영 타이밍으로 다른 터미널의 기록을 가져오지 않으며, 실제 대화 기록 없는 터미널이 종료되면 그 빈 채팅은 목록에서도 자동 제거한다. 기록 없는 Claude·Grok 예약 UUID는 재개하지 않고 새 세션으로 교체한다.
- 대시보드 런타임 카드에 Codex·Claude·Grok CLI 업데이트를 추가했다. 관리자가 확인하고 업데이트하면 해당 공급자의 실행 중 채팅과 모델 목록·사용량 조회 PTY를 모두 새 바이너로 재시작하고 버전 캐시를 갱신한다.
- 사용량 카드에 `터미널 재시작` 버튼을 추가했다. 플랜을 바꾸면 CLI가 조회 전용 PTY 시작 시점의 값을 계속 돌려줘 `새로고침`으로는 갱신되지 않으므로, 해당 공급자의 조회 전용 PTY만 즉시 새 프로세스로 교체한다. CLI 업데이트와 달리 실행 중인 채팅 세션은 재시작하지 않는다.
- 터미널 프로세스 종료 자체에 대한 외부 알림을 제거했다. 작업 완료·승인·사용량 한도 알림은 유지하며, 관리자의 프로세스 종료는 외부망에서도 허용하되 서버 자신·PID 1·WAM 시스템 프로세스 보호는 유지한다.
- 모바일 하단 내비게이션을 자주 쓰는 `대시보드`·`채팅`·`파일`·`GitHub`·`더보기` 5개로 정리하고, 지침·예약·실험실·도구·설정은 설명이 붙은 더보기 시트로 이동했다. 보조 화면의 활성 상태, CLI 인증 필요 표시, 안전 영역과 브라우저 뒤로가기를 유지한다.
- 별도 `설정` 탭을 추가해 브라우저 알림, Slack·ntfy, CLI 계정·인증, 유휴 채팅 자동 종료와 일회용 로그인을 대시보드에서 분리했다. 대시보드는 사용량·호스트 자원·런타임·토큰 원장·프로세스 운영에 집중한다.
- 대시보드 상단에 임시로 추가했던 `운영 요약`을 사용자 결정에 따라 제거했다. 사용량·호스트 자원·런타임·토큰 원장·프로세스 카드는 그대로 유지한다.

### 사용량 안정화

- Codex Pro 플랜 `/status`의 모델 전용 한도(`GPT-5.3-Codex-Spark limit:` 헤더 아래 5h·주간)를 플랜 한도로 오인하던 문제를 고쳤다. Pro 플랜에는 5시간 창이 없는데 모델 전용 5시간 값을 대표 사용량·초기화 시각으로 보여줬다. 이제 값 없는 모델 전용 헤더 이후는 모델명과 무관하게 읽지 않고, 대표값은 주간으로 폴백한다.
- Claude `/usage`가 밤 시간대에 날짜 없이 `Resets 1am`처럼 표시한 5시간 창을 오늘 새벽의 만료 값으로 오판하던 문제를 고쳤다. 다음 발생 시각이 5시간 안이면 익일 리셋으로 해석해 실제 40% 같은 최신값을 stale로 버리지 않는다.
- Claude `/usage`의 상세 조회 제한을 사용자 5시간·주간 quota 초과와 분리했다. seeded headers·seeded persisted·갱신 실패·로딩 화면의 숫자는 마지막 직접 확인값을 덮지 않으며, 대시보드에는 오류 대신 캐시 출처를 표시한다.
- 같은 낮은 값이 5회 반복되면 강제로 채택하던 경로를 제거했다. 다섯 번째에는 조회 PTY를 재생성하고 reset 시각이 전진한 fresh 새 창만 채택해, 반복되는 세션 로컬 1% fallback이 정상값을 오염시키지 않는다.
- Claude usage 상세 fallback이 반복되면 자동 조회를 2분부터 최대 15분까지 백오프한다. fallback·로딩에서는 초기화 알림과 pending keepalive를 실행하지 않는다.
- Claude `/usage`가 `Current session 0%`만 있고 Resets가 없으면 숫자는 채택하지 않지만 초기화 고정 턴은 보낸다. 저장된 세션 리셋이 이미 지났으면 대시보드를 0%·5시간 뒤로 보정한다.
- Codex keepalive는 첫 요구를 1500자로 하고 최대 10회 만에 10000자까지 올린다. 10회를 다 쓰면 현재 창을 기록하고, Slack·ntfy·FCM·브라우저로 세션 유지 실패 알림을 보낸다.
- Codex 5시간 창이 이미 1% 이상이면 고정 턴을 더 보내지 않고 현재 창을 성공으로 남긴다. 0% 확인 실패 뒤에 사용량만 오르면 성공 기록이 비던 구멍을 막는다.
- Codex keepalive는 대화형 TUI 대신 격리 `codex exec --json`으로 보낸다. TUI는 스레드만 열고 모델 턴을 제출하지 못했다. exec는 완결된 `agent_message`와 `turn.completed`를 확인한 뒤에만 사후 사용량 검증으로 넘긴다.
- 같은 리셋 날짜라는 이유만으로 0%를 거절하지 않는다. 같은 창에서 0%로 떨어지면 2회 연속 관측 뒤 조회 터미널을 재시작하고, 재시작 후 조회도 0%면 확정한다. 리셋 시각이 바뀐 5시간 창은 바로 반영하고, 0이 아닌 값으로 줄어든 같은 창만 즉시 거절한다.
- 실제 TUI 검증을 Codex·Claude·Grok 세 공급자로 확대하고, Claude fallback에서는 숫자 미채택 계약을 확인한다.

### 인증

- 사용 중 세션이 만료되면 다음 API 401 또는 실시간 연결 종료 확인 즉시 로그인 화면으로 전환한다. 서버 재시작·네트워크 장애와 일반 403은 로그아웃으로 오인하지 않는다.
- 관리자가 설정 화면에서 숫자 8자리 일회용 로그인 코드를 발급할 수 있다. 코드는 10분 안에 한 번만 사용할 수 있고, 교환된 세션은 1시간 동안 일반 사용자 권한만 가지며 내부망 capability를 얻지 못한다.
- 사용한 일회용 코드의 임시 사용자가 만료된 후 다음 코드를 발급하면 감사 로그 외래키 때문에 실패하던 문제를 고쳤다. 감사·승인 기록은 보존하고 만료 사용자와 세션만 정리한다.
- 임시 로그인 세션은 권한 요청을 결정할 수 없고 승인 목록·실시간 승인 이벤트도 받지 않는다. 도구 이름과 인자가 담긴 요청 내용이 손님에게 노출되지 않게 했다.
- 동시 발급으로 활성 코드가 상한을 넘으면 가장 오래된 코드가 올바른 입력으로도 교환되지 않던 문제를 고쳤다. 상한 초과 응답도 500이 아니라 429와 재시도 시각을 준다.
- 일반 사용자가 터미널 화면 모드를 저장할 수 없게 했다. 서버가 터미널 구독을 관리자에게만 허용하므로 화면에 반영되지 않는 값이었다.
- 임시 로그인 세션은 조회는 그대로 할 수 있지만 변경 작업은 할 수 없다. 실행 중인 CLI에 입력을 보내는 모델 새로고침과 사용량 새로고침을 막았다.
- 일회용 코드 검증이 활성 코드 수만큼 느린 해시를 돌던 것을 서버 시크릿 HMAC 한 번의 조회로 바꿨다. 인증 없이 호출되는 로그인 요청이 서버 CPU를 과도하게 쓰던 문제를 없앤다.
- 모든 로그인 요청을 성공·실패 구분 없이 접속 IP·프록시 체인·User-Agent·Origin 등 식별자와 함께 30일간 남긴다. 비밀번호와 토큰, 일회용 코드 원문은 기록하지 않는다.

## 0.5.2 - 2026-08-21

### 버그 수정

- `claude auth status --json`이 로그인 여부와 무관하게 항상 정상 종료(exit 0)하는데 종료 코드만 보고 있어 항상 "인증됨"으로 오판하던 문제를 고쳤다(JSON 본문의 `loggedIn` 값으로 판정).
- codex 기기 코드 로그인 완료 후 CLI가 스스로 종료하지 않으면 인증 상태가 갱신되지 않던 문제를 완화했다 — 로그인 진행 중에도 5초 주기로 실제 로그인 여부를 다시 확인한다.
- CLI 인증 화면(Codex·Claude·GitHub 로그인 팝업)에 포커스 설정이 빠져 있어 키 입력이 씹힐 수 있었고, Ctrl+C로 선택 영역을 복사하는 대신 로그인 프로세스가 취소될 수 있던 문제를 고쳤다.

### 설치 자동화

- `packaging/install.sh`가 GitHub CLI(`gh`)도 버전을 확인해, 오래된 apt 기본 버전이 최신 플래그(`--skip-ssh-key`)를 지원하지 않으면 GitHub 공식 저장소로 자동 업그레이드한다.

## 0.5.1 - 2026-08-21

### 버그 수정

- Claude 사용량 조회용 PTY가 `/usage` 화면을 한 번 연 뒤로는 다음 조회에서 화면이 갱신되지 않고 최초 값에 영구히 고정되던 문제를 고쳤다(Grok과 같은 방식으로 조회 전 화면을 닫도록 함).

### 설치 자동화

- `packaging/install.sh`·`create-admin.sh`·`run.sh`가 압축 배포본뿐 아니라 git 저장소를 그대로 실행해도 앱 루트를 올바르게 찾도록 고쳤다. 빌드된 `dist/`가 없으면 소스에서 자동으로 빌드한다.
- Node.js(NodeSource 22.x)·git·GitHub CLI·tmux·네이티브 모듈 빌드 도구(`python3`·`make`·`g++`)가 없으면 Linux·root·apt 환경에서 자동으로 설치한다.
- 신규 `packaging/install-systemd.sh`: 빌드부터 전용 시스템 사용자 생성·환경 파일 준비·systemd 유닛 등록·기동까지 한 번에 처리한다.
- README의 압축 배포 파일명과 `docker-compose.yml`의 이미지 태그에 남아있던 예전 버전 번호 하드코딩을 제거했다.

## 0.5.0 - 2026-08-20

### Agent Lab(실험실)

- Claude·Codex 실행 하네스를 나란히 비교하는 실험 런타임을 추가했다. 스킬셋·모델·프리셋 조합별로 예산을 두고 단일 실행·그래프·스위트 단위로 돌리며, 반복 훅과 복수 블라인드 평가로 결과를 채점한다.
- 실행 계획 큐와 스위트 집계로 여러 저장소·과제 유형을 한 번에 비교하고, 신뢰구간·쌍대 비교·컨텍스트 초과 판정을 더해 권고를 산출한다. 우승 run은 accepted 사람 판정을 거쳐 불변 Agent preset으로 승격한다.
- 공급자 사용량 한도를 실패 대신 대기 후 재개로 처리하고, 대기 시간은 실작업 시간 예산에서 제외해 판정이 왜곡되지 않게 했다.
- 실험 작업공간·채팅을 자동 정리하는 도구를 추가하고, 정리 후 세션이 다시 채팅으로 등록되거나 다른 프로젝트로 옮겨 붙는 오귀속 문제들을 수정했다.
- 승인 카드를 닫아도 진행 중인 작업이 취소되거나 답변을 강요당하지 않게 하고, 오류 상태 채팅이 busy로 남아 입력이 막히는 교착과 입력창 잔여 초안으로 채팅 모드 입력이 영구 실패하는 문제를 해소했다.
- 전체 구조와 한계는 [에이전트 실험실 설계 문서](docs/agent-lab.md)에 정리했다.

### Grok 공급자 지원

- Grok을 세 번째 채팅 공급자로 추가했다. CLI 로그인·상태 확인, 도구 화면의 내장 명령, 응답별 토큰 표시와 사용량 조회를 Claude·Codex와 같은 구조로 지원한다.
- 조회 전용 기록이 채팅 목록에 쌓이지 않게 하고, 사용량 조회를 미지원으로 잘못 판단했던 부분을 정정했다.

### 채팅·파일

- 채팅 입력창에서 `@파일`·`#채팅` 멘션으로 파일과 다른 채팅을 바로 연결하고, 파일 미리보기와 줄 단위로 연결했다.
- 첨부 업로드에 크기 사전 검증과 진행률 표시를 추가하고, 대용량 첨부가 조기 거부될 때 요청 소켓이 정리되지 않아 무응답하던 문제를 고쳤다.
- 파일 삭제와 GitHub 탭 롤백 기능을 추가하고, 브랜치 표시·선택 버그 3건을 수정했다.
- 사용량 조회가 연속으로 거부되면 옛 값을 유지하지 않고 최신값을 채택하도록 했다.

### Android

- 파일 선택기 대기 중 프로세스가 재생성되면 콜백이 유실되던 문제를 수정했다. 앱 버전은 서버와 같은 0.5.0으로 맞췄다.

## 0.4.0 - 2026-08-13

### Android·외부 접속

- Android 6 이상 WebView 앱과 런처 크기에 따라 바뀌는 Claude·Codex 사용량/CPU/RAM 홈 화면 위젯을 추가했다. 상태바·내비게이션바·키보드 inset을 반영하고, 로그인 쿠키를 사용한 공용 Downloads 저장과 설정 버튼 접기·위치 보존을 지원한다.
- Android Keystore의 P-256 공개키와 origin 결합 일회성 challenge로 외부 HTTPS 주소에 새 웹 세션을 발급하는 기기 인증을 추가했다. 개인키·비밀번호·내부망 쿠키는 서버나 다른 origin으로 전달하지 않으며, 기기 해제 시 연결 세션의 내부망 capability를 회수한다.
- 외부에 공개되는 기기 서명 로그인 challenge·activate 요청을 클라이언트 주소와 기기별로 제한하고 반복 요청에는 `Retry-After`를 반환한다. Android 앱 버전은 서버와 같은 0.4.0으로 맞췄다.
- 선택적으로 Firebase Cloud Messaging을 연결해 기존 작업 완료·승인·한도 알림을 Android 푸시로 전달한다. Firebase 자격증명이 없으면 WebView와 위젯만 정상 동작한다.

### 토큰·사용량 관리

- Claude·Codex JSONL의 assistant 응답별 입력·캐시·출력·추론 토큰을 채팅에 표시하고, 공급자·세션·메시지 안정 키로 SQLite 원장에 멱등 저장한다. 삭제된 채팅도 당시 프로젝트·계정·모델 스냅샷과 토큰 합계를 유지한다.
- 대시보드에서 최근 7·30·90일·1년·전체 기간을 일자·프로젝트·채팅·공급자·계정·모델별로 집계하고 응답 수·입력·캐시·출력·추론·전체 토큰을 비교한다.
- Claude 세션 창과 Codex 사용량 창이 초기화된 뒤 최소 단답 턴을 새 임시 상태 세션에서 창마다 한 번만 보내 초기화 시각을 실제 활동에 고정한다. 반복 조회 문맥이 다음 턴의 캐시 입력을 키우지 않도록 실행 직후 임시 세션을 폐기한다.
- Codex Full reset 초기화권의 잔여 개수·기한을 구조화 app-server 응답과 사용량 화면에서 확인하고, 관리자가 되돌릴 수 없다는 확인을 거쳐 공식 사용 경로를 호출할 수 있게 했다.
- Codex app-server 초기화의 client 버전을 현재 package 버전에서 읽어 릴리즈 이후에도 서버와 일치하게 했다.

### 채팅·작업공간 안정성

- Codex 프롬프트 전달에 bracketed paste와 제출 확인·제한 재시도를 적용했다. 1,000자를 넘는 일반 텍스트는 프로젝트·worktree 안의 UTF-8 첨부로 보존하고 TUI에는 짧은 파일 읽기 지시만 전달한다.
- 채팅이 하나도 없는 Git worktree도 작업공간 묶음으로 표시하고 해당 폴더에 Claude·Codex 채팅을 바로 연결한다. 외부 worktree는 새 폴더를 만들지 않고 실제 Git 등록 경로를 검증해 공유한다.
- 세로가 짧은 노트북에서 채팅 헤더·입력 영역을 압축하고 접힌 상태바에도 사용량과 초기화 시각을 유지한다. 모바일에서는 한 줄을 지키기 위해 사용량만 표시한다.
- 서버 supervisor 실행 스크립트와 WebSocket 정상 종료 처리를 추가해 서버 자식 재기동과 배포 종료가 열린 연결 때문에 멈추지 않게 했다. supervisor의 앱·Node 기본 경로는 설치 위치와 PATH에서 찾아 특정 호스트에 종속되지 않는다.

### GitHub·파일·검증

- GitHub 커밋·이슈·PR·Actions 목록을 단계별로 더 불러오고, Actions 조회를 탭 진입에서 분리했다. PR·이슈 상세과 Git 상태 조회는 진행 중 요청을 공유하는 캐시로 중복 CLI 실행을 줄였다.
- untracked 파일과 삭제 파일을 포함한 선택 diff를 보강하고, 로컬 diff는 사용자가 파일을 고른 경우에만 조회한다. 조회 실패는 빈 diff로 숨기지 않고 원인을 표시한다.
- 파일 미리보기 원본 다운로드와 외부 worktree 기준 파일 경로 처리를 보강했다. 모바일·토큰 원장·사용량 초기화·GitHub 캐시/페이징·프롬프트 제출·서버 종료 회귀 테스트를 추가했다.
- PR CI에서 Chrome Playwright 전체 시나리오와 공식 Gradle wrapper 검증, Android API 36 debug/release 빌드·lint를 실행한다. 간편 ZIP에는 README가 가리키는 화면을 포함해 전체 검증 스크린샷을 담는다.

## 0.3.0 - 2026-08-08

### 2026-08-09 후속 안정화

- PC 전체 터미널 모드가 글자 크기나 줄 간격을 늘리지 않고 남은 화면 높이만큼 실제 행 수를 확장하도록 개선했다. 브라우저 xterm·PTY·tmux·서버 화면 크기를 함께 동기화하며 가로 256열은 유지해 기존 스크롤백의 줄바꿈을 보호한다.
- 터미널 휠·모바일 세로 스와이프를 tmux copy-mode 기록과 연결하고, 기록을 보는 중 입력·질문·승인·중지를 보내면 자동으로 최신 화면으로 복귀한다. TUI mouse mode의 휠 입력과 페이지 스크롤 차단도 유지한다.
- AI 응답의 `[첨부: 경로]`와 Markdown 이미지가 현재 프로젝트 또는 채팅 worktree 안의 상대경로·절대경로를 모두 표시하도록 보완했다. 프로젝트 밖 절대경로는 파일 API로 노출하지 않는다.
- 동적 터미널 행 동기화, tmux 기록 이동, 관리자 권한 경계, 상대·절대 이미지 경로를 단위 테스트와 실제 Chromium 회귀 시나리오에 추가했다.
- 새 보안 권고에 대응해 빌드 도구의 전이 의존성 `nanoid`를 안전한 3.3.18로 고정하고 고위험 이상 의존성 감사를 다시 0건으로 정리했다.

### 주요 기능

- 채팅별 Git 브랜치와 전용 worktree를 지원하고, 채팅 목록을 프로젝트·worktree 작업공간별로 묶어 같은 작업 문맥에서 대화·파일·지침·diff를 다룬다.
- GitHub 화면을 로컬 변경·GitHub 활동·저장소 관리로 재구성하고, 파일별 지연 렌더링 diff와 문법 강조, 이슈에서 worktree 채팅 시작, 조회 전용 작업공간 보호를 제공한다.
- Claude·Codex 계정 슬롯을 추가해 채팅마다 인증 계정을 지정하고, 계정별 기록·세션 실행·CLI 로그인·사용량 조회를 분리한다.
- 웹 사용자별로 채팅 모드와 실제 PTY 전체 터미널 모드를 전환·저장한다. 모바일 터미널은 화면 키보드, Ctrl·Alt와 탐색 키 바, xterm 스크롤백·TUI 마우스 휠·가로 화면 이동을 지원한다.
- 전체 화면을 공통 네이비·바이올렛 디자인 체계와 모바일 하단 내비게이션으로 재정비하고, 파일 형식별 미리보기·텍스트 편집, 대시보드 프로세스 묶음과 원본 사용량 화면을 개선한다.

### 안정성·보안

- 사용량 세션·주간 한도 초기화 알림, 유휴 채팅 자동 종료, 완료된 승인 요청 정리와 서버 재시작 후 상태 복구를 보강한다.
- reverse proxy의 실제 방문자 주소를 기준으로 신뢰 네트워크를 판정하고, 채팅·worktree·프로젝트·백업·프로세스·MCP 삭제 같은 불가역 작업을 신뢰 네트워크로 제한한다.
- 마지막 worktree 채팅 삭제 전 폴더 정리 범위를 명시하고, 공유 중인 worktree는 보존하며 비게 된 앱 관리 상위 폴더까지 안전하게 정리한다.
- 모바일·데스크톱 주요 화면, 터미널 원시 입력, Git 작업공간, 계정 분리와 삭제 경계를 단위·브라우저 회귀 테스트로 검증한다.

## 0.2.0 - 2026-07-31

### 주요 기능

- Claude Code와 Codex 채팅, 실제 TUI 터미널, 사용량·상태·승인 요청을 한 웹 화면에서 관리한다.
- 파일 탐색·형식별 미리보기, GitHub 저장소 프로젝트 생성, 대용량 diff 지연 렌더링과 통합·분할 보기를 제공한다.
- 다른 채팅 문맥 조회, Claude·Codex 상호 작업 위임과 서브 에이전트 관리를 MCP·스킬로 연결한다.
- Linux·macOS·Windows WSL2 압축 설치, Electron 패키지와 다중 아키텍처 Docker 이미지를 제공한다.

### 보안·운영

- 로그인 계정·IP 제한에 메모리 상한과 만료 정리를 적용하고 인증 실패 감사 로그 보존량을 제한했다.
- 미등록 계정 로그인도 동일한 scrypt 검증 비용을 사용하도록 해 계정 존재 여부의 시간 차이를 줄였다.
- WebSocket Origin의 호스트·프로토콜 검증, HTTP 보안 헤더와 Slack·ntfy 테스트 전송의 관리자 권한을 추가했다.
- 운영 기본 로그를 `info`로 낮추고 채팅 상태 원문 추적은 `debug`에서만 기록한다.
- 개발 호스트를 환경변수로 분리하고 공개 화면·문서·테스트에서 운영 식별자를 제거했다.
- PR 검증 CI, Dependabot, 고정 GitHub Action SHA, Docker provenance·SBOM, 릴리즈 CycloneDX SBOM·SHA-256 체크섬을 추가했다.
- 운영·개발 의존성 감사 결과를 0건으로 정리하고 Docker의 Codex·Claude CLI 버전을 고정했다.
