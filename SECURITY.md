# 보안 정책

## 지원 범위

보안 수정은 기본 브랜치와 최신 GitHub Release를 우선 대상으로 한다. 오래된 릴리즈에서 문제가 발견되면 먼저 최신 버전에서도 재현되는지 확인한다.

## 취약점 제보

공개 Issue에 취약점 세부 정보, 토큰, 로그 원문 또는 재현 가능한 운영 주소를 올리지 않는다. 저장소의 **Security > Report a vulnerability**에서 비공개로 제보한다. 해당 메뉴가 보이지 않으면 저장소 소유자가 Private vulnerability reporting을 활성화할 때까지 공개 재현 정보 없이 최소한의 영향 범위만 알린다.

제보에는 다음 정보를 포함한다.

- 영향을 받는 버전과 실행 환경
- 공격에 필요한 권한과 예상 영향
- 비밀 값을 제거한 최소 재현 절차
- 가능한 경우 수정 방향 또는 우회 방법

접수 후 7일 안에 최초 상태를 회신하는 것을 목표로 한다. 수정과 공개 시점은 제보자와 조율하며, 이미 노출된 인증 정보는 애플리케이션 수정과 별개로 즉시 폐기·재발급해야 한다.

## 기본 보안 경계

- 외부 공개 시 HTTPS reverse proxy와 정확한 `WEB_AGENT_MANAGER_PUBLIC_URL`을 사용한다.
- 서비스 계정은 필요한 프로젝트와 데이터 디렉터리에만 접근 권한을 부여한다.
- Docker socket, 호스트 root 권한과 광범위한 bind mount를 컨테이너에 제공하지 않는다.
- 공급자 토큰과 환경 파일은 Issue, 로그, 스크린샷, 진단 자료에 포함하지 않는다.
