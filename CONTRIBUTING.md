# 기여 안내

## 개발 흐름

1. `main`, `dev`, `develop`이 아닌 별도 브랜치에서 작업한다.
2. `npm ci`로 lockfile 기준 의존성을 설치한다.
3. 기존 모듈과 유틸을 먼저 확인하고 요청 범위에 필요한 최소 변경과 회귀 테스트를 작성한다.
4. 새로 작성하거나 수정한 함수·클래스에는 역할을 설명하는 짧은 한국어 주석을 남긴다.
5. `README.md`, `CODETREE.md`, `history.md`를 변경 내용에 맞게 갱신한다.
6. `npm run verify`와 `git diff --check`를 통과한 뒤 Pull Request를 연다.

## Pull Request 기준

- 문제, 선택한 해결 방식, 사용자 영향과 검증 결과를 설명한다.
- UI 변경은 운영 계정·경로·도메인·사용량이 없는 재현용 데이터로 검증한다.
- 토큰, 쿠키, 환경 파일, 셸 기록, 개인 경로와 실제 채팅 내용을 커밋하지 않는다.
- 의존성이나 배포 흐름을 바꾸면 lockfile, SBOM·체크섬 생성과 지원 플랫폼 영향을 함께 확인한다.
- 큰 기능과 무관한 포맷 변경이나 리팩터링을 같은 Pull Request에 섞지 않는다.

보안 문제는 Pull Request나 공개 Issue 대신 [보안 정책](SECURITY.md)의 비공개 제보 절차를 사용한다.
