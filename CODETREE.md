# 코드트리

중복 구현을 피하기 위해 주요 파일과 역할을 정리한다.

| 경로 | 주요 구성요소 | 역할 |
|---|---|---|
| `src/server/index.ts` | `main` | HTTP, WebSocket, 서비스 생명주기와 라우터 연결. 시작 시 서버 로깅·요청별 신뢰 네트워크 capability, 등록 프로젝트 에이전트 스킬, 소유자 전용 Unix 소켓 브리지를 준비하고 종료 시 함께 정리 |
| `src/server/core/logger.ts` | `defaultLogLevel`, `initServerLogging`, `createLogger`, `createRequestLogger`, `createClientLogHandler` | 운영 기본 `info`·개발 기본 `debug`, 최소 레벨 미만 console·클라이언트 로그 제외, 요청·프로세스 오류 기록, 날짜별 저장과 14일 초과 파일 정리 |
| `src/server/core/config.ts` | `loadConfig`, `readProductEnv`, `parseAllowedRoots`, `parseList`, `resolveHomeDir` | `WEB_AGENT_MANAGER_*` 우선·기존 `MYAGENT_*` fallback 환경변수 기반 서버·경로·알림 설정과 신뢰 네트워크 목록 구성. 프로젝트 등록 허용 루트는 기본 `/`이며, `projectsDir`은 GitHub clone 기본 경로로 사용 |
| `src/server/core/network.ts` | `DEFAULT_TRUSTED_NETWORKS`, `isTrustedNetworkAddress`, `createNetworkCapability` | loopback·RFC1918·CGNAT `100.64/10`·IPv6 ULA/link-local 및 사용자 CIDR을 Node `BlockList`로 검사해 요청별 신뢰 네트워크 capability 주입 |
| `src/server/core/database.ts` | `openDatabase` | SQLite 스키마와 경량 마이그레이션. 채팅·사용량·알림·감사·리밋 대기 외에 7일 문맥 스냅샷 `context_snapshots`와 기준 메시지 수·완료 결과·완료 시각을 저장하는 멱등 작업 전달 `delegations` 포함 |
| `src/server/core/auth.ts` | 세션 loader, `login`, CSRF middleware, `requireAdmin` | 실제·더미 해시의 동일 scrypt 검증 경로, 로그인 세션 요청 인증, 관리자 전용 라우트 차단, 요청별 `trustedNetwork` capability 전달 |
| `src/server/core/security.ts` | 비밀번호/토큰/경로/파일명 유틸, `dummyPasswordHash` | scrypt와 계정 열거 완화용 고정 더미 해시, 쿠키·토큰 해시, 타이밍 세이프 비교, 허용 루트와 파일명·ANSI 정규화 |
| `src/server/core/login-rate-limit.ts` | `LoginRateLimiter` | IP 전체 요청 창과 IP+계정 연속 실패 잠금, 잠금별 단일 감사 결정, TTL 정리와 메모리 항목 상한 적용 |
| `src/server/core/http-security.ts` | `createHttpSecurityHeaders` | CSP·프레임·MIME·권한·referrer·교차 출처 정책과 HTTPS HSTS를 적용하며 Vite 개발 인라인 모듈만 별도 허용 |
| `src/server/core/audit.ts` | `writeAudit` | 민감 원문을 제외한 감사 이벤트 저장 |
| `src/server/core/uploads.ts` | `processMultipartFiles`, `streamToFile` | multipart의 파일당·총량·개수·시간·동시 요청·디스크 여유 제한과 중단 정리. `0600` 임시 파일 후 hard link no-replace 또는 명시적 overwrite로 원자 반영하고 크기·해시 반환 |
| `src/server/core/chat-busy.ts` | `setChatBusy` | 채팅별 "작업중" 여부를 `chats.busy` 컬럼에 영속화하고 `chat_busy` 웹소켓으로 알림(단일 진실 공급원 — 클라이언트는 값을 추측하지 않고 `/chats` 응답을 그대로 표시) |
| `src/server/providers/provider.ts` | `ProviderAdapter`, `TmuxIO`, `HistorySyncContext`, `HistorySyncDecision` | 공급자 실행·기록·승인·TUI 상호작용 공통 계약, 표시 라벨·대표 사용량 창·CLI 버전 명령, 준비/작업중 판정, 사용량 파싱, 모델 적용, 별도 추론 강도 명령, 공급자별 기록 동기화 정책(작업중 시작·해제·완료 알림), 공급자 내부용 기록 숨김 판정 선택 훅 |
| `src/server/providers/codex.ts` | `CodexAdapter` | Codex 실행/resume, JSONL 변환(event_msg 중복 제거, AGENTS.md 자동 주입 지침 분리, 내부 권한 상승 검토 세션 숨김, turn_context 기반 실제 모델·추론강도 감지, task_complete/turn_aborted 기반 턴 종료 시각 기록), Codex TUI 준비/작업중 판정, Codex 기록 동기화 정책(task_complete/turn_aborted 기반 완료·busy 해제), `/usage`·`/status` 파싱, `/model` 모델·effort 목록 파싱(Codex가 추천 모델에 붙이는 `(default)` 표시도 `(current)`·`(selected)`와 함께 인식하되 현재 선택값으로는 취급하지 않음 — 이 표시를 못 알아채던 예전 정규식은 해당 줄 전체가 파싱 실패해 그 모델이 목록에서 통째로 빠졌음)와 적용, slash 명령 자동완성 Enter 특례 선언, 번호 선택 메뉴가 실제로 있을 때만 승인으로 보는 TUI 승인 감지·입력(한도 임박 시 뜨는 경량 모델 전환 3지선다, 선택 메뉴 없이 `try again at`만 찍는 사용량 한도 화면 — 시각만 있는 기존 형식과 "Jul 13th, 2026 12:14 AM"처럼 요일 없는 전체 날짜까지 있는 새 형식을 둘 다 인식, 새 디렉터리 신뢰 확인 2지선다는 각각 별도 요청유형으로 감지·전용 키 매핑), tmux pane 캡처의 끝쪽 빈 줄을 먼저 제거한 뒤 마지막 줄만 검사, 시작 배너·하단 상태줄 모델 감지, 대표 사용량 구간은 Plus 플랜 계정엔 5시간 한도 자체가 없어 weekly로 표시 |
| `src/server/providers/claude.ts` | `ClaudeAdapter` | Claude 실행/resume, JSONL 변환(도구 결과·/exit 등 로컬 명령 user 턴을 tool/system으로 재분류, `/compact`·컨텍스트 한도 자동 압축이 CLI가 API 프로토콜상 어쩔 수 없이 "user" 역할 턴으로 주입하는 요약 전체(`isCompactSummary` 플래그로 식별)도 system(`compact_summary`)으로 재분류해 실제 사람이 그 긴 요약문을 직접 친 것처럼 채팅 말풍선에 보이지 않게 함, assistant content에 `tool_use` 블록이 있으면 `stop_reason`과 무관하게 완료가 아닌 `tool_call`로 표시, `stop_reason=end_turn` assistant를 완료 판정용 `turn_end`로 표시, 실제 사용자 메시지가 없는 상태 조회·local-command 전용 JSONL 제외 — 내용 기반 숨김 판정은 파일 전체를 읽어야 해서 mtime 캐시로 재검사를 최소화(실제 대화 확인된 파일은 영구 false), assistant 메시지 기반 실제 모델 감지), Claude screen-reader·일반 TUI 준비/작업중 판정, Claude 기록 동기화 정책(`turn_end` assistant 기반 완료·busy 해제), 진행 막대형 `/usage` 파싱과 stale 판정, 정렬형 `/model` 모델·effort 목록 파싱(`/model` 메뉴 선택 설명에서 현재 모델명을 보완), 모델 적용 시 채팅의 실제 최신 `/model` 메뉴를 다시 파싱해 캐시된 모델 ID와 일치하는 현재 번호를 선택(메뉴 순서가 바뀌어도 다른 모델을 고르지 않음), 최신 TUI의 `/effort <강도>` 별도 적용, hook 환경 연결, 훅 밖 TUI rate-limit 선택 화면 감지(스크롤백에 남은 문구가 아니라 "Enter selection [1-2]" 같은 한도 선택 화면 전용 표시로만 판정, 대형 세션 resume 선택 화면은 `Enter selection [1-3]` 또는 최신 `Enter to confirm · Esc to cancel` 푸터와 "Resume from summary" 문구를 함께 요구, 디렉터리 신뢰 확인 등 y/n 확인 화면은 "Enter y/n:"으로 별도 요청유형(confirm_yn) 감지 — 이 문구 자체가 기능을 설명하는 코드 주석·문서에 리터럴로 있어 grep/cat 도구 출력이나 이를 설명하는 assistant 텍스트에도 같은 화면에 찍혀 오탐했던 적이 있어, resume_session_prompt·generic_permission_prompt와 같은 방식으로 그 뒤에 esc to interrupt·"$" 프롬프트로 이미 넘어갔는지까지 확인하는 것에 더해, CODETREE.md 표의 매우 긴 한 줄(claude.ts 설명 자체)이 pane에서 여러 줄로 접혀 나오며 그 안의 "Enter y/n:"이 staleIndex보다 뒤로 밀리는 재오탐도 실제로 겪어 그 줄이 이 문구 하나만으로 이뤄져 있는지까지 요구하도록 좁힘, claude-in-chrome 등 브라우저 도구 권한 화면은 제목 문구 대신 실제 선택지 3개(Install extension/Not now/Don't ask again)의 구조와 화면 하단 위치로 판정해 `browser_permission_prompt`로 감지·전용 키 매핑(제목 문구 매칭 방식은 문구가 바뀌면 놓칠 수 있어 구조 기반으로 교체))·요청유형별 입력 매핑, tmux pane 캡처의 끝쪽 빈 줄을 먼저 제거한 뒤 마지막 줄만 검사, 시작 배너 모델 감지(대화가 길어져 배너가 화면 밖으로 스크롤되면 `/model` 변경 직후 뜨는 "Set model to X ..." 확인 메시지로 대체 감지), 표시용 제목(`displayTitle`)은 `claude --resume`·`claude agents --json`이 보여주는 것과 같은 소스인 `~/.claude/sessions/<pid>.json`의 `name`(문서화 안 된 내부 파일이라 실패하면 조용히 무시, sessionId당 가장 최근 것만, `kind:"bg"`는 세션 이름이 아니라 작업 설명이라 제외, `nameSource:"derived"`는 실제 `/rename`이 아니라 "작업디렉터리명-무작위2자" 자동 기본값이라 제외 — 진짜 `/rename`을 실행하면 이 필드 자체가 사라지는 것으로 확인함)을 최우선으로, 없으면 대화 중 계속 갱신되는 JSONL의 `aiTitle`로 대체 |
| `src/server/providers/claude.ts` | `rateLimitPromptIndex` | 최신 Claude 리미트 메뉴에서 공통 푸터만 신뢰하지 않고 `What do you want to do?` 제목과 wait/upgrade 두 번호 선택지, 화면 하단 위치를 함께 검증해 `rate_limit_options`로 판정. 메뉴 뒤에 idle·응답 생성 표시가 있으면 지나간 화면으로 제외하며 예전 `Enter selection [1-2]` 형식은 `ClaudeAdapter.detectApproval`의 기존 경로로 병행 지원 |
| `src/server/providers/history-utils.ts` | `extractContent`, `fallbackId` | 공급자 기록의 표시 텍스트와 안정 ID 생성 |
| `src/server/providers/usage-utils.ts` | `isExpiredResetTime`, `todayResetTime` | `todayResetTime`은 날짜 없는 "시:분" 리셋 문구를 "오늘 그 시각"의 실제 Date로 환산(이미 지났어도 내일로 안 미룸 — rate-limit-resume의 `parseResetTime`은 반대로 항상 다음 도래 시각으로 미루므로 용도가 다름), `isExpiredResetTime`은 그 결과가 이미 지났는지만 판정하는 공급자 공용 유틸 |
| `src/server/services/session-manager.ts` | `SessionManager` | tmux/PTY 생성(고정 크기 `DEFAULT_COLS`×`DEFAULT_ROWS`, 상태바 끔)·웹에서 수동 시작, 입력 직렬화(system 자동화는 `user=null`로 호출 가능), 일반 전송의 자동 resume와 실행 중 후속 입력 TUI 큐 전달, 종료·busy·실제 터미널 유실 시 자동 시작하지 않는 시스템 입력 전용 `sendPromptIfRunning`, 사용자 종료 시 리밋 대기 취소, 중지 복구용 원래 질문 보존, 어댑터 기반 준비 대기·작업중 판정·모델/추론 강도 적용, tmux 이름 있는 키 기반 응답 중단, 권한 모드 전환, PTY 출력을 headless 현재 화면으로 재구성하고 출력 폭주를 500ms로 합치며 정적 화면은 5초 안전망으로 승인·busy·권한 모드를 한 스냅샷에서 판정, `renameSession`, 작업 중 모델·이름 변경 즉시 거부 |
| `src/server/services/history-sync.ts` | `HistorySynchronizer` | 전역 JSONL 스캔, 공급자 내부용 숨김 기록 제외와 과거 등록 row 정리, 프로젝트·채팅 upsert, 어댑터 기록 동기화 정책에 따른 완료 알림·작업중 표시 갱신(외부에서 발견한 `stopped` 채팅은 완료 레코드 없는 user 메시지만 있어도 busy를 올리지 않고 옛 값도 정리), 등록된 알림 채널(Slack·ntfy) + 브라우저 알림용 `task_completed` 브로드캐스트, 메시지는 DB 미러링 없이 history-cache로 그때그때 파싱, JSONL에 기록된 실제 모델명으로 채팅 모델 갱신·브로드캐스트, 채팅 제목은 `chats.title_source`가 'manual'(SessionManager.renameSession으로 사람이 직접 바꿈)이 아닌 한 매 동기화마다 공급자 `displayTitle`(있으면)·없으면 첫 메시지 기반 제목으로 계속 갱신 |
| `src/server/services/history-cache.ts` | `HistoryCache` | 파일 mtime·크기 기준 세션 파싱 결과 캐시. append-only JSONL은 새 바이트의 완성 레코드만 공급자 증분 파서에 전달하고 파일 축소·덮어쓰기·불완전 레코드는 전체 파싱으로 복구, 메시지 조회의 단일 소스, 삭제·복원 시 캐시 무효화 |
| `src/server/services/session-backups.ts` | `SessionBackupService` | Codex·Claude JSONL 세션과 채팅 메타데이터 백업, 원본 삭제, 공급자 기록 저장소로 복원, `deleteBackup`으로 백업 사본(JSONL·메타데이터)만 지우고 원본 채팅·공급자 기록은 건드리지 않음 |
| `src/server/services/approval.ts` | `ApprovalService` | 공급자·요청유형별 승인 저장, 웹 결정(터미널 결정 콜백에 요청유형 포함 전달), 승인 알림에 요청 요약 포함, timeout 기본 거부, `dismiss`(AI가 아직 응답을 기다리는 살아있는 요청이면 막는 안전한 웹 목록 정리)와 `decide`를 분리, `autoResolve`(사람 개입 없이 시스템이 즉시 결정, `notify=false`로 만들면 웹·알림 채널 없이 조용히 생성) |
| `src/server/services/runtime-files.ts` | `prepareRuntimeFiles` | 앱 소유 Claude hook 설정과 내부 토큰 파일 생성 |
| `src/server/services/usage-monitor.ts` | `UsageMonitor`, `detectUsageRegression`, `reconcileStaleClaudeSessionWindow` | 상태 전용 TUI, 주기(60초) 조회, 어댑터 `usageCommands`·`promptQuirks`·`parseUsage` 기반 사용량 파싱. 날짜 없는 리셋 시각 만료 판정은 어댑터 공용 유틸이 맡고, 리셋 시각이 아직 미래인데 같은 창의 퍼센트만 이전 저장값보다 줄어든 경우(실측: 실제 56% 시점에 1시간 전 값 26%를 최신인 척 반환)는 옛 스냅샷으로 보고 마지막 정상값을 덮지 않고 stale 표시만 남김(`detectUsageRegression`, CLI가 같은 창의 리셋 시각을 "2:09pm"↔"2:10pm"처럼 1분 다르게 표기하는 것도 실측돼 몇 분 이내 표기 오차는 같은 창으로 취급하고, 리셋 시각이 실질적으로 달라졌으면 창이 넘어간 것이므로 감소 허용). Claude는 세션 리셋 시각이 지나도 "Current session" 블록이 사라지지 않고 리셋 전 마지막 스냅샷을 그대로 계속 돌려주는 경우가 실측됨 — `reconcileStaleClaudeSessionWindow`가 리셋 시각이 2분 이상 지난 세션 창을 0%·5시간 뒤 재리셋으로 직접 채워 넣고 stale을 fresh로 되돌리되, 밤 시간대의 다음날 새벽 리셋처럼 5시간 세션 창 안에 있는 다음 발생 시각은 stale로 보지 않음(Claude 전용, Codex는 미적용) — "12:40am"처럼 자정을 넘겨 곧 돌아올 새벽 시각을 어댑터 공용 유틸이 "오늘 자정 직후"로 계산해 반나절 넘게 지난 옛 스냅샷으로 오판, 방금 받은 진짜 최신 값(예: 47% 사용)까지 0%로 덮어쓰던 버그가 실사용 재현됨 — 그 리셋 시각을 24시간 뒤로 다시 계산했을 때도 5시간 윈도우 안에 들어오면(=자정 넘김 오판) 보정하지 않도록 고침. `isImplausibleClaudeSessionReset`은 세션 리셋 시각이 지금부터 5.5시간 넘게 남은 것처럼 나오면(5시간 롤링 윈도우로는 물리적으로 불가능) `detectUsageRegression`과 같은 방식(stale만 남기고 마지막 정상값 유지)으로 걸러냄 — 상태 조회 전용 PTY가 간헐적으로 완전히 동떨어진 리셋 시각의 옛 화면을 새 값인 척 그대로 돌려주는 게 실측됨. 모델 옵션과 사용량 상세는 마지막 성공값을 유지해 일시 파싱 실패·상태 PTY busy 때 UI가 비지 않게 함. 모델·effort 목록은 서버 시작 시 1회 조회해 캐시하고 이후엔 수동 새로고침(`POST /models/:provider/refresh`)에서만 재조회, `snapshot(provider)`으로 매 조회마다 파서에 실제로 넘어간 원본 화면 텍스트(`lastSnapshot`)를 메모리에 남겨 대시보드에서 직접 확인 가능(숫자만으로 파싱 이상 원인을 알기 어려워 실사용 요청으로 추가, DB에 영속화하지 않음) |
| `src/server/services/rate-limit-resume.ts` | `RateLimitResumeService`, `parseResetTime`, `zonedTimeToUtc` | `rate_limit_waits`를 60초마다 확인해 예정 시각 도달 시 최신 `usage_status`로 실제 회복 여부를 재검증하고, 예고가 부정확하면 다음 리셋 시각으로 대기를 연장. 회복 뒤에는 DB가 running이고 busy가 아니며 SessionManager가 실제 관리 중인 터미널에만 "계속"을 보내고 전송 성공 때만 리셋 알림을 발송하며, 종료·오류·터미널 유실 채팅은 대기만 정리해 자동 시작하지 않음. `parseResetTime`은 타임존 유무·12/24시간·요일 없는 전체 날짜 형식을 실제 UTC 시각으로 환산 |
| `src/server/services/terminal-screen.ts` | `TerminalScreen` | 용도별 크기·scrollback으로 ANSI TUI를 headless 화면에 재구성하고 전체 기록 또는 현재 viewport 텍스트 제공 |
| `src/server/services/tmux-input.ts` | `pastePromptToTmux`, `sendTmuxEnter`, `sendTmuxEscape`, `sendTmuxBackspace`, `sendTmuxShiftTab`, `sendTmuxText` | 웹 채팅 프롬프트를 tmux 버퍼로 붙여 넣고 실제 Enter 키 이벤트로 제출, Escape·Backspace·Shift+Tab(Claude Code의 기본·auto-accept edits·plan mode 순환)과 짧은 리터럴 키 입력도 tmux send-keys로 전달(attach된 클라이언트에 raw 제어 바이트를 직접 쓰면 이스케이프 시퀀스 파서에 걸려 실제로 반영되지 않을 수 있음) |
| `src/server/services/system-metrics.ts` | `SystemMetricsService`, `linkProcessesToChats` | 5초 간격 호스트·프로세스·채팅 상태 지표 순환 수집과 중첩 실행 차단, tmux pane pid(및 그 자식 프로세스)를 실제 채팅·프로젝트에 연결 |
| `src/server/services/realtime.ts` | `isSameOrigin`, `RealtimeHub` | Origin 호스트·프로토콜과 reverse proxy 외부 URL을 검증한 인증 WebSocket, 관리자 전용 채팅·CLI 인증 터미널 구독과 raw 입력 중계 |
| `src/server/services/github-projects.ts` | `GithubProjectService`, `githubRepositoryName` | `gh api`로 로그인 사용자와 소속 조직을 조회하고 소유자별 구조화 저장소 목록을 중복 제거해 프로젝트 origin 연결 상태와 결합, 기존 프로젝트 재활성화, `소유자/저장소` 기본 경로의 허용 경계 clone, 로컬 Git 초기화와 `gh repo create --source --remote origin` 실행 |
| `src/server/services/cli-auth.ts` | `CliAuthManager` | Codex device auth·Claude 계정 로그인·GitHub web auth 공식 CLI를 관리자 전용 PTY로 실행하고 종료 코드 기반 설치·인증 상태와 제한된 출력 스냅샷 중계 |
| `src/server/services/notifier.ts` | `Notifier`, `NotificationHub` | 알림 채널 공통 계약과, 등록된 채널 전부에 동시에 보내는 허브(한 채널 실패가 다른 채널에 영향 없음) — 각 서비스는 이 허브 하나만 주입받으면 됨 |
| `src/server/services/slack.ts` | `SlackNotifier` | bot token 기반 중복 방지 Slack 알림, DB에 저장된 관리자 설정을 우선 쓰고 없으면 환경변수로 대체 |
| `src/server/services/ntfy.ts` | `NtfyNotifier` | ntfy(topic 기반 푸시) JSON 발행 API로 중복 방지 알림, DB에 저장된 topic·서버 URL을 우선 쓰고 없으면 환경변수로 대체, 이벤트 유형별 제목·이모지 태그·우선순위 매핑 |
| `src/server/routes/auth-routes.ts` | `createAuthRouter` | 입력 길이 검증, IP 전체·IP+계정 로그인 제한과 상한 있는 실패 감사 로그, 로그아웃, 현재 사용자·마지막 세션 API. 사용자가 없을 때만 신뢰 네트워크에서 트랜잭션으로 첫 관리자 생성 |
| `src/server/routes/project-routes.ts` | `createProjectRouter`, `visibleChats` | 프로젝트·채팅 조회와 생명주기, GitHub 저장소 목록·clone 프로젝트 생성, 로컬 프로젝트의 GitHub 저장소 생성, JSONL 메시지 페이지네이션, 첨부 업로드, 백업 API. 프로젝트 등록 시 중앙 스킬 링크 자동 설치 |
| `src/server/routes/cli-auth-routes.ts` | `createCliAuthRouter` | 관리자 전용 CLI 인증 상태 조회와 공급자별 로그인 PTY 시작·중단, 감사 로그 기록 |
| `src/server/routes/file-routes.ts` | `createFileRouter`, `previewKind`, `parseAttachmentPath` | 요청별 내부망 정책을 적용한 파일 목록·업로드·다운로드·형식별 미리보기. 목록 symlink 제외, 새 `.web-agent-manager-uploads`와 기존 `.myagent-uploads`의 프로젝트·채팅 소유권을 검증하는 전용 첨부 API, 매직 바이트·CSP·nosniff·no-store와 비활성 ZIP 다운로드 포함 |
| `src/server/routes/instruction-routes.ts` | `createInstructionRouter`, `ensureClaudeImportsAgents` | 허용된 프로젝트/전역 지침 조회와 관리자 전용 편집 API, `CLAUDE.md`가 공통 `AGENTS.md`를 import하도록 보장하는 지침 통합 API |
| `src/server/routes/git-routes.ts` | `createGitRouter`, `parseStatus`, `parseLog`, `issueNumber`, `ghJson` | Git 상태·변경 파일·선택 diff·커밋 상세, GitHub 이슈·PR 상세/댓글/닫기/다시 열기/리뷰/병합과 부분 실패 허용 gh 조회, 관리자 전용 Git/GitHub 쓰기 API. `/git/diff`는 상태를 다시 조회해 untracked(`??`) 파일만 따로 `git diff --no-index`(exit code 1은 "차이 있음"이라 정상 처리하는 `runNoIndexDiff`)로 "전부 추가된" diff를 만들어 tracked 변경분과 합침(`git diff HEAD`는 untracked 파일을 원천적으로 못 보여줘 파일 목록엔 뜨는데 diff만 항상 비어 보였음) — 선택 파일이 전부 untracked라 tracked 목록이 우연히 비어도 `git diff --` 가 "제한 없음"으로 해석되지 않게 그 경우엔 호출 자체를 생략, 두 diff 호출 모두 `core.quotePath=false`로 비ASCII(한글) 파일명이 8진 이스케이프·따옴표로 깨지지 않게 함. `gh pr list`/`gh pr view`의 `statusCheckRollup` 필드는 PR마다 체크 상태를 추가로 조회해 응답이 느려지는데 화면 어디에도 안 써서 뺌(조회 속도 개선, 2026-07-23) |
| `src/server/routes/tool-routes.ts` | `createToolRouter`, `listClaudeMcp`, `listCliMcp`, `parseCliMcpOutput`, `listCodexMcp`, `writeClaudeMcp`, `writeCodexMcp` | Claude/Codex commands·skills·marketplace·MCP 카탈로그 API와 관리자 전용 MCP 추가·수정·삭제·토글 API. `claude/codex mcp list` CLI 조회는 비동기 `execFile`로 실행하고 provider·경로별 60초(실패 시 10초) 캐시로 이벤트 루프 블로킹과 반복 실행을 방지. Claude는 프로젝트 `.mcp.json`의 `mcpServers`와 `claude mcp list`가 보고하는 연결 상태를 함께 보여주고, 공식 설정에 없는 enabled 토글은 거부. Codex는 `~/.codex/config.toml`과 프로젝트 `.codex/config.toml`의 `mcp_servers` 블록 및 `codex mcp list` 상태를 함께 읽으며 `enabled=false` 토글을 지원. env/header 값은 목록에 노출하지 않고 키 이름만 반환하며, 빈 입력으로 저장하면 기존 비밀 필드를 보존 |
| `src/server/routes/agent-integration-routes.ts` | `createAgentIntegrationRouter` | 관리자 전용 Codex·Claude CLI/전역 스킬/web-agent-manager MCP 상태 조회와 명시적 설치 API, 설치 감사 로그 |
| `src/server/routes/agent-delegation-routes.ts` | `createAgentDelegationRouter` | 관리자 전용 프로젝트 위임 목록과 새 자식 세션 생성 API. 대상 채팅의 실제 상태를 위임 기록과 함께 반환하고 생성은 `AgentBridge`를 통해 감사 로그로 기록 |
| `src/server/routes/operations-routes.ts` | `createOperationsRouter` | 승인, 공급자 메타·사용량·자원 조회, 관리자 전용 Slack·ntfy 테스트·설정, CLI 버전 API, 관리자 전용 프로세스 종료(SIGTERM/SIGKILL, 자기 자신·pid 1 보호) |
| `src/server/routes/helpers.ts` | 경로 유틸 | 프로젝트 경계와 symlink 실제 경로 검증, 네트워크와 무관하게 차단할 민감 경로 및 외부망 점 경로 정책 |
| `src/server/services/agent-bridge.ts` | `AgentBridge` | `0600` Unix 소켓의 줄 단위 JSON API. 프로젝트·채팅 목록, 번호 기반 제한 문맥 조회, 7일 스냅샷, 멱등 작업 전달·상태, 공급자별 새 자식 생성, 완료 대기·assistant 결과 영속화를 제공하고 모든 쓰기를 `SessionManager`와 감사 로그로 통과시킴 |
| `src/server/services/agent-skill-installer.ts` | `installProjectAgentSkills`, `installGlobalAgentSkills`, `globalAgentSkillsInstalled` | 등록 프로젝트와 공급자 사용자 전역 경로에 중앙 스킬 symlink를 설치하고 기존 항목은 보존. 이 저장소의 이름 변경 전 원본을 정확히 가리키는 옛 symlink만 정리하고 현재 원본 연결 상태 판정 |
| `src/server/services/agent-integration.ts` | `AgentIntegrationManager` | PATH에서 Codex·Claude CLI를 셸 없이 찾고 공식 `mcp get/add/remove`로 web-agent-manager MCP 상태를 판정·설치. 설정 파일이나 비밀값을 읽지 않고 production/개발 브리지 진입점을 선택 |
| `src/client/main.tsx` | `App`, `navigationFromLocation`, `writeNavigation`, `selectChat`, `selectTab`, `refetchChats`, `rememberLastSession`, `openProject`, `openProjectFile` | 최상위 상태와 인증·WebSocket·세션 백업 조정. 탭·프로젝트·채팅·파일 위치를 URL history에 기록·복원하고, 생성·재활성화한 GitHub 프로젝트를 최신 목록에서 선택해 채팅으로 이동하며 이전 비동기 응답의 선택 덮어쓰기를 차단 |
| `src/client/api.ts` | `api`, `setCsrfToken`, `getCsrfToken` | CSRF 토큰 보관과 인증 JSON API 호출 공통 유틸 |
| `src/client/lib/logger.ts` | `initClientLogging`, `createLogger` | 클라이언트 상세 로깅. console 티로 기존 콘솔 로그와 전역 오류(error·unhandledrejection)를 배치(5초/30건)로 `/api/logs/client`에 전송, 숨김/이탈 시 keepalive flush, 로그인 전(CSRF 없음)에는 전송 보류 |
| `src/client/types.ts` | `Tab`, `Json`, `ApprovalAction`, `ChatActivity`, `ChatScrollState` | 클라이언트 전용 공통 타입 |
| `src/client/components/Login.tsx` | `Login` | 사용자 0명 상태 확인, 첫 관리자 생성과 일반 로그인 폼·웹 세션 생성 |
| `src/client/components/ProjectDialog.tsx` | `ProjectDialog` | `document.body` portal 팝업에서 로컬 절대 경로 등록과 선택적 GitHub 저장소 생성·origin 연결, GitHub 저장소 clone 프로젝트 생성을 탭으로 제공 |
| `src/client/components/GithubRepositoryList.tsx` | `GithubRepositoryList` | 개인·조직 소유자 필터와 저장소 검색·새로고침, 연결 프로젝트 채팅 이동, 미연결 저장소의 clone 경로 확인 팝업과 프로젝트 생성 |
| `src/client/components/CliAuthPanel.tsx` | `CliAuthPanel` | 세 CLI 설치·인증·PTY 실행 상태를 주기적으로 확인하고 미인증 CLI 발견 시 `document.body` portal의 관리자 인증 팝업 자동 표시 |
| `src/client/components/LoadingState.tsx` | `LoadingState` | 도구·GitHub 등 최초 서버 응답을 기다리는 영역의 공용 회전 스피너와 접근성 상태 문구 |
| `src/client/components/AgentIntegrationNotice.tsx` | `AgentIntegrationNotice` | 관리자 화면에서 60초 주기·화면 복귀 시 공급자 연동 상태를 재감지하고, 설치됐지만 준비되지 않은 Codex·Claude의 전역 스킬+MCP 연결 버튼 제공 |
| `src/client/components/SubagentManager.tsx` | `SubagentManager`, `delegationActivity` | 관리자용 서브 에이전트 작업 서랍. 프로젝트 작업 수·실행 상태 요약, 접이식 새 작업 위임 폼, 상태 중심 대상 목록과 채팅 열기·응답 중단·터미널 종료·재시작 제공 |
| `src/client/features/chat/ChatView.tsx` | `ChatView`, `MessageCard` | 공급자 메타 기반 새 채팅 버튼·라벨·대표 사용량 표시·모드 전환 지원 여부, 채팅 목록·모바일 메뉴·모델/사용량/추론 강도 선택, 채팅 이름 변경, 모바일 상태바 요약, wheel·touch·pointer 입력 시 하단 정렬을 즉시 중단하는 자동 추적/과거 위치 보존 가상 스크롤, 관리자 서브 에이전트 패널, 입력 길이 자동 조절 컴포저와 첨부, 도구·diff 상세·인라인 승인. 작업 중에도 후속 질문·명령 전송과 현재 응답 중지를 동시에 제공하고 중지 복구 질문은 큐 입력으로 덮지 않으며, 데스크톱 권한 요청 패널은 현재 프로젝트에 pending 요청이 있을 때만 렌더링 |
| `src/client/features/terminal/TerminalPanel.tsx` | `TerminalPanel` | 선택 채팅의 tmux PTY를 xterm 화면과 WebSocket으로 연결하고 크기를 동기화 |
| `src/client/features/terminal/AuthTerminal.tsx` | `AuthTerminal` | 공급자 로그인 PTY를 xterm으로 표시하고 출력 조각에서 최근 인증 URL을 찾아 새 탭 링크 제공 |
| `src/client/features/overview/Overview.tsx` | `Overview`, `SlackSettingsCard` | 공급자 메타 기반 사용량 라벨, 호스트 자원·프로세스 대시보드, 관리자 Slack 설정과 프로세스 종료 UI. 사용량 카드마다 마지막 파싱 시각(stale일 땐 마지막 성공 시각도 함께)과 개별 새로고침 버튼 표시, "터미널 보기" 버튼으로 파서에 실제로 넘어간 원본 화면 텍스트를 어두운 터미널풍 블록으로 펼쳐 확인(파싱이 이상해도 숫자만으로는 원인을 알기 어려워 추가) |
| `src/client/features/files/FilesView.tsx` | `FilesView`, `PreviewContent`, `fileContentUrl`, `fileDate` | 이름·크기·수정 시각·다운로드 열의 조밀한 프로젝트 파일 목록과 앱 스타일 파일 선택·업로드, 행 전체 단일 클릭과 키보드 폴더 진입·파일 선택, 채팅에서 전달된 경로 드러내기와 폴더·미리보기 탐색 이력 통지. 데스크톱은 선택 시 분할 미리보기, 모바일은 즉시 보이는 전체 오버레이를 사용하며 Markdown·sandbox HTML·이미지·영상·오디오·PDF·일반 텍스트를 렌더링하고 ZIP·EPUB은 압축파일 안내만 표시 |
| `src/client/features/instructions/InstructionsView.tsx` | `InstructionsView` | 프로젝트·전역 지침 파일 편집, `CLAUDE.md`에 `AGENTS.md` import를 추가하는 공통 지침 버튼 |
| `src/client/features/git/GitView.tsx` | `GitView`, `GitHubTab`, `IssueDetail`, `PullRequestDetail`, `LazyDiffSection`, `splitDiffSections`, `diffPreamble` | 저장소 프로젝트 생성·Diff·현재 저장소 탭, 로컬 변경·커밋 상세·PR 통합/분할 diff, 이슈·PR·Actions 관리. 대형 PR은 펼친 파일의 줄 DOM만 지연 렌더링하고 GitHub 로딩·인증 필요·빈 상태를 구분 |
| `src/client/features/tools/ToolsView.tsx` | `ToolsView`, `McpEditor`, `parseJsonField` | 공급자별 commands·skills·marketplace·MCP 카탈로그와 MCP 추가·수정·삭제·Codex 활성화 토글. 최초 응답 전 로딩과 성공 후 빈 필터 결과를 구분하고 재조회 중 기존 결과를 유지하며, CLI 상태 전용 MCP는 read-only로 표시하고 기존 env/header 값은 노출하지 않음 |
| `src/client/lib/approvals.ts` | `approvalSummary`, `approvalActions`, `chatActivity` | 승인 요청 요약(Claude PermissionRequest 훅의 tool_name/tool_input에서 실제 명령·경로 추출)·버튼 라벨·리밋 대기 포함 채팅 활동 상태 계산 |
| `src/client/lib/format.ts` | `usageWindows`, `bytes` | 사용량 세부 JSON 파싱과 바이트 단위 표시 |
| `src/client/lib/notifications.ts` | `notificationPermission`, `requestNotificationPermission`, `showNotification` | 브라우저 Notification API 권한 조회·요청, 탭이 보이는 중엔 팝업을 띄우지 않고 조용히 무시 |
| `src/client/message-display.ts` | `splitMessageContent`, `mergeMessages`, `reconcileOptimisticMessages` | 일반 응답과 접힌 도구·diff 상세 분리, 페이지 병합·중복 제거, 작업 중 큐 입력은 실제 동일 user 기록이 새로 확인된 항목만 하나씩 낙관적 말풍선에서 확정 |
| `src/client/lib/attachments.tsx` | `isImagePath`, `attachmentUrl`, `AttachmentImage`, `MessageBody` | 일반 프로젝트 링크는 파일 탭으로 연결하고 `.web-agent-manager-uploads` 첨부는 전용 소유권 검증 API로 inline/다운로드. 이미지 실패는 고정 크기 안내로 가상 스크롤 변동 방지 |
| `src/client/lib/file-links.ts` | `projectFilePathFromHref` | 채팅·Markdown의 절대/상대 링크에서 현재 프로젝트 내부 경로만 추출하고 에디터 줄 번호·URL 조각 제거, 프로젝트 밖 경로와 외부 URL 거부 |
| `src/client/lib/diff-view.tsx` | `DiffView`, `DiffModeToggle`, `parseSplitDiffRows`, `looksLikeDiff` | 기존 통합 diff 색상 보기와 unified hunk의 이전/이후 줄 번호·삭제/추가 블록을 좌우 정렬하는 GitHub식 분할 보기. 긴 본문은 각 열 경계 안에서 개행하며 공용 segmented mode control 제공 |
| `src/client/styles.css` | 반응형 스타일 | 데스크톱 관리 화면과 모바일 채팅 중심 레이아웃, 작업 상태 배지·인라인 승인 카드·에이전트 연동 알림·서브 에이전트 관리 서랍·모바일 파일 미리보기 오버레이·Slack 설정 폼·세션 백업 목록 스타일 |
| `scripts/create-admin.ts` | 관리자 CLI | 환경변수로 관리자 생성/갱신 |
| `scripts/claude-permission-bridge.ts` | hook bridge | Claude stdin 요청을 내부 승인 API로 전달 |
| `scripts/web-agent-manager-agent.ts` | 로컬 브리지 CLI·MCP | Unix 소켓 요청 CLI와 프로젝트·채팅·문맥·스냅샷·비동기 전달, 새 자식 생성 후 완료 대기, 기존 전달 결과 대기 도구를 제공하는 줄 단위 stdio MCP 서버 |
| `scripts/myagent-agent.ts` | 기존 MCP 경로 호환 wrapper | 이름 변경 전 등록된 production MCP 명령이 새 `web-agent-manager-agent` 브리지로 계속 진입하도록 연결 |
| `scripts/install-agent-integrations.ts` | 공급자 연동 설치 진입점 | 설치된 Codex·Claude CLI를 감지해 기존 사용자 파일을 덮어쓰지 않고 전역 스킬 링크와 사용자 범위 `web-agent-manager` MCP를 설치·보정 |
| `electron/main.cjs`, `electron/preload.cjs`, `electron/setup.html` | Electron 데스크톱 셸 | sandbox 창과 제한 preload, 외부 이동 차단, 임의 localhost 포트 사용. 셸은 asar로 묶고 외부 Node 백엔드만 unpack해 최초 관리자 생성·연동 수행 |
| `scripts/package-release.ts` | `createArchive`, `createSbom`, `createChecksums`, `main` | production 빌드·중앙 스킬·lockfile·플랫폼 진입점·Docker 구성과 README 공개 화면을 지원 플랫폼 ZIP으로 묶고 CycloneDX SBOM·SHA-256 목록 생성 |
| `scripts/create-release-checksums.mjs` | `writeChecksums` | 릴리즈 디렉터리의 일반 파일을 이름순으로 정렬해 SHA-256 무결성 목록 생성 |
| `scripts/check-public-build.mjs` | `checkPublicBuild` | 프로덕션 `dist`의 텍스트 산출물에 빌드 머신 절대 경로가 남으면 빌드를 실패 처리 |
| `packaging/*` | 간단 설치·관리자 생성·실행 진입점 | Linux `setup.sh`, macOS `setup.command`, Windows `setup-windows.cmd` 단일 진입점과 단계별 설치·관리자 생성·실행 스크립트. Windows는 WSL2로 동일 Linux 런타임 사용 |
| `.github/workflows/ci.yml`, `.github/dependabot.yml` | PR 검증·의존성 갱신 | 전체 npm 감사·타입·테스트·빌드를 강제하고 npm 운영·개발 패키지와 Docker Actions를 호환 범위별 그룹 PR로 매주 제안. Node Docker 이미지는 22 계열 주요 버전을 유지 |
| `.github/workflows/release-desktop.yml` | Electron 교차 플랫폼 빌드 | 고정 SHA Action으로 사전 검증 후 Linux·Windows와 네이티브 Intel·Apple Silicon runner별 macOS 설치 파일, 간단 설치 ZIP을 GitHub 자산 이름으로 정규화·중복 검사하고 태그 Release에 SBOM·전체 체크섬과 함께 첨부 |
| `Dockerfile`, `docker-compose.yml`, `docker/entrypoint.sh` | Docker 배포 | 전용 builder 경로에서 공개 산출물을 검사하고 Node.js 22·tmux·Git·gh·고정 버전 Codex·Claude와 production 앱을 비루트 UID 10001로 실행하며 데이터·프로젝트·CLI 홈 볼륨을 영속화 |
| `.github/workflows/docker-image.yml` | GHCR 다중 아키텍처 이미지 | `main`·태그·수동 실행에서 linux/amd64·linux/arm64 이미지를 공개 `web-agent-manager-app` 패키지에 provenance·SBOM 증명과 함께 게시 |
| `skills/web-agent-manager-session-context/SKILL.md` | 세션 조회 스킬 | “채팅 160 참고” 같은 번호·프로젝트 요청을 구조화 문맥 조회로 변환하고 MCP가 없으면 CLI 사용 |
| `skills/web-agent-manager-delegate/SKILL.md` | 작업 전달 스킬 | 원본 문맥을 확인하고 대상 채팅 또는 새 반대 공급자 자식 채팅에 멱등 전달한 뒤 완료 결과를 회수·검토. 명시적 위임과 이점이 분명한 복잡한 교차검증에만 제한적으로 사용 |
| `scripts/check-usage-tui.ts` | 실제 TUI 검증 | Codex·Claude 사용량 화면 통합 테스트 |
| `scripts/check-session-lifecycle.ts` | 실제 resume 검증 | 새 세션 저장·종료·동일 ID resume 테스트 |
| `scripts/check-browser-resources.ts` | 브라우저 리소스 검증 | 주요 화면 120회 전환 전후 Chromium GC 기준 JS heap·DOM 노드·이벤트 리스너 누적 상한 검사 |
| `vite.config.ts` | `parseAllowedHosts`·Vite 개발 서버 설정 | 환경변수의 추가 허용 Host 목록을 정규화하고 React 플러그인·클라이언트 루트·빌드 출력을 구성 |
| `LICENSE`, `SECURITY.md`, `CONTRIBUTING.md`, `CHANGELOG.md` | 공개 운영 문서 | MIT 이용 조건, 비공개 취약점 제보, 기여·검증 기준과 버전별 사용자 영향 기록 |
| `tests/login-rate-limit.test.ts`, `tests/http-security.test.ts`, `tests/realtime-origin.test.ts` | 공개 경계 회귀 테스트 | 로그인 캐시·잠금 상한, HTTP 헤더·미리보기 예외, WebSocket 호스트·프로토콜 검증 |
| `tests/release-checksums.test.ts`, `tests/vite-config.test.ts`, `tests/logger.test.ts` | 배포 설정 회귀 테스트 | 재현 가능한 체크섬, 개발 허용 Host 파싱과 운영·개발 로그 기본값 검증 |
| `TODO_LIST.md` | CLI/데스크톱 앱 parity TODO | Codex CLI/App, Claude Code CLI/Desktop 대비 남은 구현 후보와 우선순위 |
| `tests/usage-monitor.test.ts` | 파서 회귀 테스트 | 실측 축약 화면과 인증 오류, 파싱 실패 시 사용량 상세 캐시 보존 계약, 세션 리셋 시각 만료(stale) 판정 검증, 자정 넘겨 곧 돌아올 새벽 리셋을 옛 스냅샷으로 오판해 0%로 덮어쓰지 않는지·진짜 오래 지난 리셋은 여전히 0%로 보정하는지 검증 |
| `tests/rate-limit-resume.test.ts` | 리셋 시각 파싱 테스트 | `parseResetTime`의 24/12시간·타임존 유무·자정 경계·이미 지난 시각의 다음 날 처리·파싱 실패 검증, 요일 없는 전체 날짜 형식("Jul 13th, 2026 12:14 AM")을 오늘/내일 추측 없이 그대로 신뢰하는지 검증 |
| `tests/rate-limit-resume-tick.test.ts` | 리밋 자동 재개 테스트 | 최신 사용량 회복 재검증과 다음 시각 연장, 정상 실행 터미널 재개, 종료·실제 터미널 유실 채팅의 자동 시작·잘못된 리셋 알림 차단 검증 |
| `tests/session-manager-rate-limit-stop.test.ts` | 리밋 대기 종료 테스트 | 사용자 터미널 종료 시 `rate_limit_waits` 삭제와 종료된 채팅의 시스템 `계속` 입력 거부 검증 |
| `tests/system-metrics.test.ts` | 프로세스-채팅 연결 테스트 | tmux pane pid 직접 연결·자식 프로세스 전파·미연결 케이스 검증 |
| `tests/slack-settings.test.ts` | Slack 설정 테스트 | 환경변수 대체, DB 저장값 우선, 빈 값 갱신 시 기존값 유지 검증 |
| `tests/ntfy-settings.test.ts` | ntfy 설정·알림 허브 테스트 | 환경변수 대체, DB 저장값 우선, 빈 값 갱신 시 기존값 유지, topic 미설정 시 전송 안 함, 이벤트 ID 중복 방지, `NotificationHub`의 다중 채널 동시 전송·한 채널 실패 격리 검증 |
| `tests/process-kill.test.ts` | 운영 API 테스트 | 공급자 메타 응답, 자기 자신·pid 1 종료 거부, 이미 종료된 프로세스 안내 검증 |
| `tests/message-display.test.ts` | 메시지 표시 테스트 | 도구·diff 분리와 상세 라벨, 페이지 병합, 실제 기록 전까지 작업 중 큐의 낙관적 입력 보존 검증 |
| `tests/diff-view.test.ts` | diff 표시 테스트 | git/unified/Codex 패치 판정과 분할 변환의 삭제·추가 블록 좌우 정렬 및 이전·이후 줄 번호 계산 검증 |
| `tests/codex-history.test.ts` | Codex 기록 파싱 테스트 | event_msg 중복 제거, AGENTS.md 지침 분리, 내부 권한 상승 검토 세션 숨김과 일반 언급 오탐 방지, turn_context·하단 상태줄 모델 감지, `/model` 모델·effort 파싱, 턴 종료 시각 기록, 승인 감지 오탐 방지, 경량 모델 전환·디렉터리 신뢰 확인 화면 감지·입력 매핑 회귀 검증, 요일 없는 전체 날짜까지 찍는 새 사용량 한도 문구("try again at Jul 13th, 2026 12:14 AM")도 리밋 대기로 감지하는지 검증 |
| `tests/history-cache.test.ts` | JSONL 증분 캐시 테스트 | Codex·Claude 추가 레코드만 누적해 메시지·모델·제목을 갱신하고 쓰는 중인 불완전 레코드는 전체 파싱으로 복구하는지 검증 |
| `tests/terminal-screen.test.ts` | headless 화면 테스트 | ANSI 커서 이동과 scrollback을 적용한 뒤 현재 viewport만 분리해 반환하는지 검증 |
| `tests/claude-history.test.ts` | Claude 기록 파싱 테스트 | 도구 결과 user 턴의 tool 재분류, 도구 호출 assistant 턴을 완료로 보지 않는지, 제목 계산, 사용자 대화 없는 상태 조회·local-command 전용 JSONL 제외, 더미 식별자 기반 assistant 모델 감지, `/model` 모델·effort 파싱, `/effort` 명령 생성·확인 메시지 파싱, 대형 세션 resume 선택 화면 감지, 예전·최신 rate-limit 선택 화면 감지와 지나간 메뉴 오탐 방지, confirm_yn 감지·입력 매핑 회귀 검증 |
| `tests/path-security.test.ts` | 경로 보안 테스트 | traversal, symlink, 민감 경로, symlink로 해석된 민감 경로, 업로드 파일명 정규화 차단 검증 |
| `tests/file-routes.test.ts` | 파일 API 테스트 | 형식별 미리보기, 외부망 점 파일 차단·내부망 일반 점 파일 허용, 민감 파일 상시 차단, symlink 제외, 첨부 소유권, 원자적 overwrite, 빈 업로드·위장 파일 차단 검증 |
| `tests/uploads.test.ts` | 업로드 저장 테스트 | no-replace·명시 overwrite와 파일 크기 초과 시 임시·대상 파일 정리 검증 |
| `tests/network.test.ts` | 신뢰 네트워크 테스트 | 사설망·CGNAT·IPv6·mapped 주소 경계와 잘못된 CIDR 거부 검증 |
| `tests/agent-bridge.test.ts` | 에이전트 연동 테스트 | 번호 기반 문맥 조회, 멱등 전달, 새 자식 작업의 assistant 완료 응답 대기·영속화, 소켓 `0600`·정리, Codex·Claude 프로젝트 스킬 링크와 기존 파일 보존 검증 |
| `tests/agent-delegation-routes.test.ts` | 서브 에이전트 API 테스트 | 관리자 위임 상태 조회, 대상 채팅 상태 결합, 새 자식 생성 브리지 호출과 감사 로그 검증 |
| `tests/agent-integration.test.ts` | 공급자 설치 관리 테스트 | Codex·Claude CLI 감지, 공급자별 공식 MCP 명령 인자, 사용자 전역 스킬 링크, CLI 미설치 시 무변경 실패 검증 |
| `tests/github-projects.test.ts` | GitHub 프로젝트 테스트 | HTTPS·SSH origin 정규화, 저장소 연결 상태, 비활성 프로젝트 재사용, 기본 경로 clone, 로컬 저장소 생성 명령 계약 검증 |
| `tests/history-sync.test.ts` | 히스토리 동기화 테스트 | Codex·Claude 어댑터별 기록 동기화 정책이 중간 assistant 조각은 완료 알림으로 보지 않고 각 공급자 완료 신호 기준으로만 완료 처리하는지 검증 |
| `tests/auth-routes.test.ts` | 인증 라우트 테스트 | 로그인 실패 감사·시도 제한, 세션 발급, 로그인·`/me` 신뢰 네트워크 capability, 마지막 프로젝트·채팅 저장, 타이밍 세이프 비교 검증 |
| `tests/rbac.test.ts` | RBAC 테스트 | 일반 사용자 관리자 전용 HTTP 변경 작업 거부, 채팅 프롬프트 허용, WebSocket 터미널 구독·raw 입력 거부 검증 |
| `tests/instruction-routes.test.ts` | 지침 파일 API 테스트 | `CLAUDE.md`가 `AGENTS.md`를 import하도록 생성하고 반복 호출 시 import를 중복하지 않는지 검증 |
| `tests/chat-attachments.test.ts` | 채팅 첨부 API 테스트 | 업로드 파일이 프로젝트 폴더 전용 디렉터리에 저장되고 전용 디렉터리 symlink 탈출은 거부되는지 검증 |
| `tests/attachments.test.ts` | 첨부 미리보기 유틸 테스트 | SVG 제외 이미지 확장자 판정, 다운로드 URL 인코딩, 비이미지 첨부의 파일 탭 링크 렌더링 검증 |
| `tests/file-links.test.ts` | 프로젝트 파일 링크 테스트 | 절대·상대 경로와 줄 번호 변환, Markdown 기준 디렉터리 해석, 외부 URL·프로젝트 밖 경로 거부 검증 |
| `tests/message-pagination.test.ts` | 메시지 페이지네이션 테스트 | DB 없이 JSONL을 커서 기반으로 정확히 나누어 반환하는지 검증 |
| `tests/approvals.test.ts` | 승인 요약·버튼 라벨 테스트 | Claude PermissionRequest 훅(permission) 요약 추출, confirm_yn·trust_directory 2택 액션과 resume_session_prompt 3택 액션 검증 |
| `tests/session-backups.test.ts` | 세션 백업 테스트 | Codex·Claude JSONL 세션을 각각 백업, 삭제, 복원할 수 있는지 검증, 백업 사본만 지우고 원본 채팅은 그대로 남는지·존재하지 않는 백업 삭제 시 실패하는지 검증 |
| `tests/git-diff-untracked.test.ts` | Git diff untracked 파일 테스트 | 실제 임시 git 저장소로 `createGitRouter`를 직접 띄워, untracked 새 파일이 전체 추가 diff로 나오는지·특정 untracked 파일만 선택해도 무관한 tracked 변경이 안 섞이는지·tracked·untracked가 섞였을 때 둘 다 나오는지 검증 |
| `tests/tmux-input.test.ts` | tmux 입력 테스트 | 프롬프트 붙여넣기와 Enter 키 제출, 실패 시 버퍼 정리 검증 |
| `tests/tui-readiness.test.ts` | TUI 준비 상태 테스트 | Codex 입력 프롬프트, 순환 placeholder 문구, 프롬프트 위/아래 작업중 상태 영역, Claude 프롬프트·생성 중 판정, 오래된 버퍼에 남은 문구 무시 검증 |
| `tests/e2e/ui.spec.ts` | Playwright 테스트 | 데스크톱·모바일 채팅 UI, 자동 스크롤·작업 중 후속 입력, 채팅 선택 경합·history 복원, 에이전트 연동/서브 에이전트 제어·권한 패널 조건부 표시, 도구·GitHub 지연 응답 로딩과 응답 후 빈 상태, 모바일 파일 미리보기, PR 통합/분할 diff·병합 오류, 사용량 원본 화면 검증 |
