# 코드트리

중복 구현을 피하기 위해 주요 파일과 역할을 정리한다.

| 경로 | 주요 구성요소 | 역할 |
|---|---|---|
| `docs/agent-lab.md` | 에이전트 실험실 기준 명세 | 스킬·하네스·루프·훅·멀티 에이전트 조건 비교, 실행 원장·체크포인트, 복수 심사·사용자 판정의 구조와 구현 단계·한계를 정의 |
| `src/shared/experiments.ts` | `parseExperimentDesign`, `parseExperimentVariantConfig`, `parseExperimentEvaluators`, `parseExperimentOutputContract`, `parseFindingReport`, `parseExperimentFixtureInput`, `omitFixtureGroundTruth`, 실험 공통 타입 | 통제/평가 변수와 primary/secondary 공급자·모델, worker 수, 하네스 반복·최소 점수·무개선 한도, native/strict 스킬 기준선·추가 목록·비교 그룹, 훅·예산·복수 evaluator를 버전 1 계약으로 검증. `EXPERIMENT_OUTPUT_CONTRACTS`는 실험 산출물(`code_change`\|`finding_report`)이며 fixture의 `reviewTarget`·`findingTaxonomy`·`groundTruth`와 에이전트 `FindingReport` 파서를 포함한다. `finding_report`는 single 하네스만 저장한다 — graph는 채점 대상 node가 정해지지 않아 점수가 빠진 completed run이 된다. `category`는 자유 문자열이고 빈 findings는 유효하다. 파일 경로는 `\`를 `/`로 바꾼 뒤 절대·탈출 세그먼트를 검사한다. `omitFixtureGroundTruth`는 피험 에이전트 응답에서 정답 필드 자체를 빼 존재·개수 힌트까지 가린다. `EXPERIMENT_PROVIDERS`·`ExperimentProvider`·`isExperimentProvider`는 실험실이 실제로 돌릴 수 있는 공급자(codex·claude·grok)를 채팅용 `Provider`와 구분해 경계에서 좁힌다 — 지금은 세 공급자가 같지만, 채팅만 되고 하네스·평가자 Runtime이 없는 공급자가 새로 붙어도 실험실 입구에서 걸러지도록 별도 타입을 유지한다 |
| `src/server/index.ts` | `main` | HTTP, WebSocket, 서비스 생명주기와 라우터 연결. 시작 시 서버 로깅·요청별 신뢰 네트워크 capability, 등록 프로젝트 에이전트 스킬, Git 작업공간 복원, 소유자 전용 Unix 소켓 브리지를 준비하고 종료 시 함께 정리 |
| `src/server/core/logger.ts` | `defaultLogLevel`, `keepDays`, `pruneLogsIn`, `initServerLogging`, `createLogger`, `createRequestLogger`, `createClientLogHandler` | 운영 기본 `info`·개발 기본 `debug`, 최소 레벨 미만 console·클라이언트 로그 제외, 요청·프로세스 오류 기록, 날짜별 저장과 14일 초과 파일 정리 보존은 기본 3일이며 `WEB_AGENT_MANAGER_LOG_KEEP_DAYS`로 조절한다(`keepDays`). 정리는 시작 시 1회에 더해 6시간 주기로도 돈다 — 예전에는 `initServerLogging`에서 한 번만 돌아 오래 켜둔 서버에서 계속 쌓였고, 디버그 레벨에서는 조회 화면 전체가 매분 쌓여 하루 수백 MB까지 커진다(실측 200MB). `pruneLogsIn`은 디렉터리와 기준 시각을 받아 날짜 형식 로그만 지우므로 로그 디렉터리에 섞인 다른 파일은 건드리지 않는다. |

| `src/server/core/config.ts` | `loadConfig`, `readProductEnv`, `parseAllowedRoots`, `parseList`, `resolveHomeDir` | `WEB_AGENT_MANAGER_*` 우선·기존 `MYAGENT_*` fallback 환경변수 기반 서버·경로·Slack·ntfy·FCM 알림 설정과 신뢰 네트워크·신뢰 프록시 목록 구성(`trustedProxies`는 설정했을 때만 `trust proxy`로 적용되고, 미설정이면 전달 헤더 요청의 내부망 판정을 포기하는 근거가 됨). 프로젝트 등록 허용 루트는 기본 `/`이며, `projectsDir`은 GitHub clone 기본 경로로 사용 |
| `src/server/core/network.ts` | `DEFAULT_TRUSTED_NETWORKS`, `isTrustedNetworkAddress`, `createNetworkCapability` | loopback·RFC1918·CGNAT `100.64/10`·IPv6 ULA/link-local 및 사용자 CIDR을 Node `BlockList`로 검사해 실제 접속망 `networkOriginTrusted`와 최종 capability를 주입. 신뢰 프록시를 지정하지 않았는데 전달 헤더가 붙은 요청은 내부망으로 판정하지 않으며, `requireTrustedNetwork`가 실제 내부망 또는 서명 인증된 앱 세션만 되돌릴 수 없는 작업에 허용 |
| `src/server/core/database.ts` | `openDatabase` | SQLite 스키마와 경량 마이그레이션. 웹 세션의 인증 기기 관계와 Android 공개키·웹 세션 승격 및 새 origin 로그인용 일회성 challenge, 사용자별 화면 상태, 채팅 작업공간, 사용량·알림·감사·리밋 대기, 숫자형 토큰 원장, 공급자 연동·위임, 영구 일일 프롬프트 일정에 더해 실험/변형/run/node/event/checkpoint/artifact/evaluation/judgment/human verdict와 불변 Agent preset 버전 기반을 생성. `experiments.output_contract`는 기본값 `code_change`로 기존 행을 보존하고, `experiment_fixtures`는 리뷰 대상·분류 체계·정답 JSON을 선택 컬럼으로 둔다. `migrateProviderCheckConstraint`는 나중에 추가된 공급자(grok)를 기존 DB의 `CHECK(provider IN ...)`에 반영한다 — SQLite는 제약만 바꾸는 ALTER가 없어 테이블을 다시 만들어야 하는데, 스키마 상수 대신 현재 DB에 저장된 CREATE 문을 읽어 제약 문구만 치환해 그동안 ADD COLUMN으로 늘어난 컬럼 구성을 보존하고, 재생성 중에는 외래키를 꺼서 `chats`를 CASCADE로 참조하는 행이 함께 지워지지 않게 하며 DROP으로 사라진 인덱스를 원래 정의대로 되살린 뒤 `foreign_key_check`로 검증한다(실험실 테이블 `experiment_nodes`·`experiment_evaluation_calls`·`experiment_judgments`도 대상에 포함된다) |
| `src/server/services/experiment-repository.ts` | `ExperimentRepository` | 실험·변형·run/node/event/checkpoint와 평가 subject·호출·judgment를 관리한다. 실험 `outputContract`와 fixture `reviewTarget`·`findingTaxonomy`·`groundTruth`를 검증해 저장·조회하고, evaluator 호출별 usage·오류와 judgment 멱등 키를 분리하며, 작업/평가 상태 전이·서버 재시작 고아 종결·최신 assistant allowlist 조회·피험/심사 provenance를 SQLite 원장에 보존 |
| `src/server/experiments/agent-runtime.ts` | `AgentRuntime`, `RuntimeBudgetPolicy`, `ActiveClock` | Codex·Claude 비대화형 실행의 준비·실행·재개·취소, cache creation/read·총 토큰 출처·tool/parent 상관관계·세분화 실패 이유의 nullable 이벤트 계약을 공유하고, 한도 대기를 제외한 실작업 시간·보고된 토큰·비용의 WAM 하드 상한을 구조화 종료 이유로 판정 |
| `src/server/experiments/jsonl-process.ts` | `JsonlProcessRunner`, `JsonlProcessExitError` | 구조화 CLI를 shell 없이 stdin/JSONL로 실행하고 run 중복·stderr 상한·취소를 관리. POSIX는 별도 프로세스 그룹 전체에 TERM→grace→KILL, Windows는 직접 자식에 같은 단계적 종료를 적용 |
| `src/server/experiments/os-sandbox.ts` | `detectOsSandboxSupport` | Linux에서 `unshare --user`로 유저 네임스페이스 생성이 실제로 되는지 판정한다. PATH의 bwrap 존재만 보면 Codex가 내부 bwrap으로 읽기까지 거부하는 환경을 놓친다 |
| `src/server/experiments/codex-exec-runtime.ts` | `CodexExecRuntime`, `buildCodexExecArgs`, `normalizeCodexExecEvent` | Codex `exec --json --strict-config`를 stdin prompt와 실행별 모델·sandbox·reasoning, 디렉터리 기반 installed/clean/addition skill override로 구성하고 thread/message/tool/usage/종료 이벤트를 공통 Runtime 계약으로 변환. OS 샌드박스를 쓸 수 없으면 요청한 read-only를 걸지 않고 `danger-full-access`로 달리며 `permissionProfile.enforcement`에 남긴다. 도구 이벤트는 command_execution·file_change·mcp_tool_call·web_search·plan_update만 `tool_started`로 남기고 그 밖의 item type은 버려 finding_report의 `toolCallCount`가 과소 집계될 수 있다 |
| `src/server/experiments/claude-print-runtime.ts` | `ClaudePrintRuntime`, `buildClaudePrintArgs`, `normalizeClaudePrintEvent` | Claude `-p --output-format stream-json --verbose`를 stdin prompt, native 권한·예산·schema와 도움말 기반 capability로 구성한다. strict single은 모든 arm에 동일 setting source·MCP 경계와 실행별 native plugin을 적용하고 init/message/tool/cache usage/result를 공통 이벤트로 변환 |
| `src/server/experiments/grok-print-runtime.ts` | `GrokPrintRuntime`, `buildGrokPrintArgs` | Grok `-p <프롬프트> --output-format streaming-messages-json`을 실행한다. 이 출력이 Claude `stream-json`과 **같은 wire format**(실측: `system/init` → `assistant` → `result`, usage·`total_cost_usd` 포함)이라 파서를 새로 만들지 않고 `normalizeClaudePrintEvent`를 공유하며, 오류 문구의 공급자 이름만 인자로 바꾼다. Claude와 다른 점만 여기서 처리한다 — 프롬프트는 stdin 파이프가 불가(실측 `No such device or address`)해 argv로 넘기고, `--max-turns`는 네이티브 지원이라 capability 탐지가 없으며, `--max-budget-usd`가 없어 비용 상한은 WAM `RuntimeBudgetPolicy`만 강제한다(`toolProfile.costBudgetEnforcement: "wam-only"`로 표시). 발견된 스킬을 끄거나 고정하는 수단이 없어(`--setting-sources` 상당 없음, `--plugin-dir`는 `grok agent` 전용이라 headless가 `unexpected argument`로 거부 — 실측) `skills.mode: none`·`selected`와 스킬 overlay는 baseline과 무관하게 전부 거부한다. `--sandbox`는 존재하지만 Linux에서 유저 네임스페이스를 못 만들면(bwrap 부재 또는 `No permissions to create a new namespace`) 샌드박스 초기화가 실패하므로 `detectOsSandboxSupport`가 강제 가능할 때만 걸고, 무엇으로 통제했는지는 `permissionProfile.enforcement`에 남긴다 |
| `src/server/experiments/single-harness.ts` | `SingleHarness` | queued single run을 준비 snapshot→before/after HookBus→단일 worker node→Runtime 이벤트/usage/cost 원장→예산·사용자 취소→node 경계 체크포인트→구조화 종료 상태로 실행. `finding_report`면 outputSchema를 준비하고, 세 공급자에 같은 계약 문구를 과제 뒤에 붙이며(처리군 `promptPrefix`는 과제 앞) 그 문구를 run 스냅샷에 남긴다. 형식 실패는 completed+malformed, diff와 `toolCallCount`(`tool_started`만)는 관찰 열만 남긴다. 도구 횟수의 비교 신호는 0 vs >0이다. run별 중복 실행과 격리 작업 디렉터리 누락을 차단한다. 일반 실행 오류는 계정 사용량 상태로 `provider_limit` 여부를 확인해 좁히고, 한도면 run을 `paused`로 두고 회복을 기다린 뒤 같은 공급자 세션을 이어 재개하며 대기 시간을 실작업 시간에서 제외한다 |
| `src/server/experiments/graph-harness.ts` | `GraphHarness` | `orchestrator_worker`의 계획→순차 worker→통합과 `evaluator_optimizer`의 초안→JSON 평가→개선을 primary/secondary Runtime node로 실행한다. 독립 호출 usage 합산, 공통 예산·취소, 점수·반복·무개선 종료, 예외 node 종결, HookBus·체크포인트를 원장에 투영 |
| `src/server/experiments/builtin-hooks.ts` | `createBuiltinExperimentHookBus` | 허용된 `diff_stats`, `git_diff_check`만 Git을 shell 없이 실행해 node 후 tracked·untracked diff 통계를 이벤트에 추가하거나 whitespace/conflict 오류를 validate 차단 |
| `src/server/experiments/hook-bus.ts` | `ExperimentHookBus`, 훅 오류 타입 | 실행 경계 훅을 안정된 등록 순서로 호출하며 observe에는 재귀 동결 복사본만 제공하고 transform·validate·block 효과, timeout과 fail/warn/ignore 정책을 명시적으로 처리 |
| `src/server/experiments/skill-manifest.ts` | `ExperimentSkillManifestService` | Codex·Claude·Grok의 프로젝트/사용자/시스템 `SKILL.md`를 공급자 scope별로 발견하고(Grok은 네이티브 `.grok/skills`·`~/.grok/skills`에 더해 실제로 함께 읽히는 Claude 호환 경로 `.claude/skills`·`~/.claude/skills`까지 포함해야 실행 시 보이는 집합과 스냅샷이 일치한다) realpath 중복을 제거한 뒤 파일 SHA-256을 실행 환경 provenance로 제공 |
| `src/server/experiments/skill-bundle.ts` | `ExperimentSkillBundleService` | 현재 installed baseline과 선택 additions의 전체 디렉터리를 파일 수·크기·symlink 제한 아래 실행별 Claude plugin/Codex skill bundle로 복사해 digest를 고정한다. Claude 선택 모드에서는 대조군·처리군 공통 무상태 SessionStart 훅과 처리군 지시 본문을 bundle에 고정하고, Git에서 제외된 project skills도 detached worktree에 복제해 실행 전후 변조를 검증 |
| `src/server/experiments/failure-classification.ts` | `classifyFailureReason`, `looksLikeContextOverflow` | 일반 실행 오류를 `context_exceeded`로 좁혀 규모 축에서 "구성이 나빴는지"와 "컨텍스트가 모자랐는지"를 구분한다. 표식은 설치된 Claude·Codex CLI 번들에서 실측한 문자열이다 |
| `src/server/experiments/pairwise-evaluation.ts` | `buildPairwisePrompt`, `parsePairwiseJudgment`, `summarizePairwise` | 같은 두 후보를 A/B와 B/A 두 순서로 물어 순서를 바꿔도 같은 후보를 고른 쌍만 승리로 세고, 두 순서 모두 앞자리를 고른 쌍은 위치 편향으로 분리한다 |
| `src/server/experiments/suite-summary.ts` | `summarizeVariant`, `recommend`, `rollupSuite`, `wilsonInterval` | Variant별 종료 표본을 검사 통과율·rubric 중앙값·토큰/실작업 시간 중앙값으로 접고, `결정적 검사 → rubric → 비용 → 무차별` 순서로 승자를 정한 뒤 표본 4회 이상이고 95% Wilson 구간이 겹치지 않을 때만 확증 등급을 준다. 여러 상황(셀)을 가로질러 묶고 순위가 뒤집히면 일반 권고를 내지 않는다 |
| `src/server/experiments/deterministic-check.ts` | `runDeterministicCheck` | fixture가 선언한 argv 검증 명령만 shell 없이 실행해 완성도의 1차 지표를 만든다. 통과·실패는 종료 코드로, 실행 불가·시간 초과는 산출물 결함과 구분되는 error로 분류하고 출력 뒷부분을 남김 |
| `src/server/experiments/finding-score.ts`, `finding-synonyms.ts`, `finding-output.ts` | `scoreFindingReport`, `compareFindingScores`, `extractFindingReport`, `FINDING_REPORT_OUTPUT_SCHEMA`, `FINDING_REPORT_CONTRACT_INSTRUCTION`, `composeFindingReportPrompt` | `finding_report` 정답 대조. 상위 20개만 채점하고, 같은 파일·40행 이하·보고 span의 50% 이상 겹침·동의어 분류만 자동 매칭한다. 동의어 표는 한 집합이며 상위 범주(security)는 그 가족하고만 맞는다. 함수 단위 span은 정답을 거의 포함하면 인정한다. 재심 큐와 관찰에 보고 원문(파일·줄·분류·제목)을 남긴다. 승자는 비가중 mustFind 재현율 → 오탐 수 → 위치·선택 항목 → 비용 순이며 F1은 관찰 지표다. `manualDecisions`로 사람 재심을 점수에 반영한다. 하네스는 native JSON schema를 넘긴다. Codex OpenAI strict를 위해 모든 object에 additionalProperties:false와 선택 필드의 nullable required를 두고, 형식 실패는 `malformed` 관찰로 남긴다. 계약 문구는 세 공급자에 같고, 스키마 description과 프롬프트·스냅샷이 같은 상수를 쓴다. 중간 계획을 findings에서 빼는 채점 필터는 두지 않는다. `toolCallCount`는 관찰이며 승자 규칙에 넣지 않는다 |
| `src/server/experiments/fixture-store.ts` | `ExperimentFixtureStore` | 외부 공개 저장소 fixture의 bare mirror를 한 번만 받아 캐시하고 run마다 고정 commit의 detached worktree를 앱 관리 경로에 만든다. greenfield용 빈 Git 작업공간과 관리 경로 밖 제거 차단을 함께 처리 |
| `src/server/experiments/experiment-workspace.ts` | `ExperimentWorkspaceService` | 등록 프로젝트 Git HEAD에서 run 전용 detached worktree를 데이터 디렉터리 아래 만들고 monorepo 하위 프로젝트의 상대 작업 디렉터리를 보존 |
| `src/server/experiments/git-change-snapshot.ts` | `collectGitChangeSnapshot` | index를 바꾸지 않고 tracked diff와 untracked 일반 파일을 하나의 산출물 스냅샷으로 모은다. 파일 200개·총 4MiB 상한과 symlink·비일반 파일 거부로 범위를 제한하고 binary는 추가 줄 수에서 제외 |
| `src/server/experiments/blind-subject-packet.ts` | `buildBlindSubjectPacket` | 최종 assistant 답변과 tracked·untracked diff만 평가 입력으로 allowlist 투영하고 공급자·모델·세션 지문 마스킹, 길이 제한·절단 provenance를 적용 |
| `src/server/experiments/rubric-evaluation.ts` | `RubricEvaluationRunner` | Codex·Claude evaluator를 순차 실행해 strict JSON 점수·신뢰도·항목 근거를 검증하고 호출별 usage·실패와 독립 judgment를 저장하며 completed/partial/failed/cancelled 상태를 확정 |
| `src/server/services/experiment-service.ts` | `ExperimentService`, `buildEvaluatorVariantConfig`, `createExperimentRuntime` | Variant 계정 환경·원본/project skill snapshot·실행별 overlay·Runtime·세 하네스를 조립한다. Runtime 선택은 `createExperimentRuntime` 한곳에서만 분기한다 — 예전 `provider === "codex" ? Codex : Claude` 삼항식은 새로 추가된 공급자를 전부 Claude로 떨어뜨려 엉뚱한 CLI를 실행했다. `permissionSemantics`도 셋을 구분한다 — Codex `codex-sandbox`, Grok `grok-sandbox-and-permission-mode`, Claude만 OS 샌드박스 없는 native 권한 모드다. `buildEvaluatorVariantConfig`의 평가자 스킬 모드도 Grok만 `all`이다(Grok은 스킬을 끌 수 없어 `none`이면 매 평가가 실행 전에 거부된다). strict comparisonId의 비스킬 fingerprint와 실험 Git HEAD를 고정하고, evaluator provider별 max-turn capability를 보존한 블라인드 복수 평가, run·평가 전역 직렬 한도와 상세 조회·취소·재시작/종료 정리를 관리 |
| `src/server/services/agent-preset-service.ts` | `AgentPresetService` | 완료 run의 불변 config snapshot과 accepted 사람 판정을 새 Agent preset version으로 transaction 저장한다. Variant 표본의 성공률·중앙 토큰/비용·평균 심사 점수와 표본/평가/비용 호환성 경고를 서버 계산하고 활성 버전을 원자 전환 |
| `src/server/routes/experiment-routes.ts` | `createExperimentRouter` | 관리자 전용 실험/Variant, 경로를 숨긴 스킬 후보, run·복수 평가, 프로젝트 Agent preset 목록과 Winner promotion API를 감사 로그·429 계약에 연결 |
| `src/client/features/experiments/ExperimentsView.tsx` | `ExperimentsView` | 과제 유형·대상 저장소 fixture를 지정해 실험을 만들고, strict single의 installed/clean+선택 스킬과 native graph/loop Variant를 만든다. 성공률·결정적 검사·평가 품질·실작업 시간(한도 대기 제외)·토큰·비용·종료를 비교하고 조건부 권고와 arm 교차 실행 계획을 제공한다. 복수 evaluator provenance와 완료 run의 preset 승격 지표·경고를 표시 |
| `src/server/routes/mobile-routes.ts` | `createMobileRouter`, `buildMobileWidgetSnapshot` | 로그인 세션을 그대로 사용하는 Android 전용 API. 위젯에 Claude·Codex·Grok 사용량과 CPU·RAM만 축약해 제공하고, 어댑터의 대표 창과 실제 상세 창을 대조한 `windowLabel`(`5시간`·`주간`)을 함께 내려 Android 모든 크기·스택 카드에서도 창 종류를 명시한다. 관리자 기기의 FCM 토큰 등록·해제·테스트도 처리 |
| `src/server/routes/mobile-trust-routes.ts`, `src/server/services/mobile-device-trust.ts` | `createMobileTrustBootstrapRouter`, `createMobileTrustRouter`, `MobileDeviceTrustService` | 실제 내부망 관리자만 P-256 앱 공개키를 등록한다. 로그인된 세션은 공개키 지문 resolve와 1회성 서명으로 승격하고, 로그인 쿠키가 없는 새 앱 origin은 기기 ID·공개키가 일치할 때 받은 2분 challenge 서명으로 기기 연결 HttpOnly 세션을 발급한다. 기기 해제 시 모든 연결 세션 신뢰와 두 종류의 미사용 challenge를 회수 |
| `src/server/services/fcm.ts` | `FcmNotifier`, `fcmTitle` | Google 기본 자격증명과 FCM HTTP v1 API로 기존 운영 알림을 등록 Android 기기에 최대 500개 단위로 전송하고 이벤트·기기별 중복을 방지하며 폐기 토큰을 자동 비활성화. 설정이 없으면 네트워크·초기화 없이 비활성 |
| `src/server/services/credential-vault.ts` | `CredentialVault` | WAM 관리 Slack/MCP 비밀을 owner·purpose·version AAD의 AES-256-GCM으로 암호화하고 0600 master key, 30초 내부 lease 메타데이터, 프로젝트·공급자 범위의 CLI 환경 주입을 관리. Claude/Grok `${VAR}`, Codex `env_vars`·`env_http_headers` 참조를 생성하고 Codex stdio env 충돌을 저장 전에 차단 |
| `src/server/services/full-backup.ts` | `FullBackupService`, `restoreFullBackup` | 온라인 SQLite snapshot과 WAM 관리 key를 scrypt+AES-256-GCM 단일 package로 스트리밍하고 manifest/전체·항목 hash·mode를 검증. 복구는 sibling staging에서 DB/key를 검증하고 로그인·challenge·lease·runtime 상태를 폐기한 뒤 존재하지 않거나 빈 dataDir로만 원자 rename |
| `src/server/routes/full-backup-routes.ts` | 전체 백업 관리자 API | 최근 재인증과 신뢰 네트워크 아래 생성·다운로드·정확한 package 삭제를 제공하고 passphrase 없이 metadata만 응답·감사 |
| `scripts/create-full-backup.ts`, `scripts/restore-full-backup.ts`, `scripts/drill-full-backup.ts` | pre-migration 생성·offline restore·정기 drill CLI | 구버전 DB를 migration 없는 read-only 연결로 snapshot·암호화한 뒤 임시 빈 환경 실제 복구 검증에 성공한 package만 남긴다. passphrase를 환경변수로만 받아 복구·건수·vault/MFA key 훈련을 실행하고 안전한 요약만 출력 |
| `src/server/services/task-board.ts`, `src/server/routes/task-board-routes.ts` | `TaskBoardService`, 목표 보드·추천·queue API | task/verification/rate-limit/schedule 원장을 6개 열과 active/idle/wait/queue로 투영하고 계획 metadata·verified checkpoint를 보존. 사용량/reset·capability·profile model·세 범위 동시 실행 근거의 멱등 추천은 승인 전 불변이며, 적용 시 점유를 재검증하고 priority queue를 5초마다 조정 |
| `src/server/services/task-command-service.ts` | `TaskCommandService` | 웹 입력 1회마다 command와 task를 멱등 생성하고 provider turn ID로 시작·완료·실패·중단 이벤트를 대응시킨다. 새 입력이 시작되면 같은 채팅의 이전 started task를 완료 처리하고, 서버 재시작 시 idle 채팅에 남은 started task만 보수적으로 완료해 현재 응답 하나만 Working에 남긴다. 아직 전달 전인 received/queued task는 자동 완료하지 않는다 |
| `src/client/features/tasks/TaskBoardView.tsx` | 작업 보드 UI | 데스크톱 6열·모바일 lane 가로 탐색으로 목표/상태/reason/checkpoint와 추천 근거를 표시하고 지연 추천 진행 상태 및 명시 확인 후 적용을 제공. 카드가 채팅 전체가 아닌 웹 입력 1회를 뜻함을 화면에서 설명한다. 웹 화면 확인은 프로젝트에서 실행 중인 HTTP listener 후보를 우선 선택하게 하고 탐지 실패 때만 로컬 주소 입력을 한 줄로 요청하며, viewport는 보조 설정으로 접는다 |
| `src/server/services/live-preview.ts`, `src/server/routes/live-preview-routes.ts` | `LivePreviewService`, preview/visual/a11y API | 프로젝트와 연결 worktree 아래 프로세스의 `/proc` listener 중 실제 loopback HTTP 응답이 있는 port만 관리자용 preview 후보로 탐지한다(전체 host port와 WAM 자체 port 제외). 프로젝트별 query 없는 loopback URL·viewport를 검증하고 실제 Chrome의 외부 HTTP/WS를 차단한 뒤 viewport PNG, redacted console, query 없는 network metadata와 제한된 axe rule 결과를 task event/0600 hash artifact로 연결. 같은 task의 명시 기준선과 동일 크기 PNG는 pixelmatch diff·변경 비율을 만들고 크기 불일치는 비교 불가로 기록. 관리자·최근 재인증·신뢰 네트워크 mutation과 관리자 hash 검증 다운로드 제공 |
| `src/server/services/remote-worker.ts`, `src/server/routes/remote-worker-routes.ts` | `RemoteWorkerService`, remote worker API | pinned host key·vault private key·workspace root, root 내부 project mapping을 저장하고 forwarding/password/TOFU 없는 고정 worker probe/start/status만 실행. probe 선언 capability 재검증, 제한 JSON base64url, task 멱등 원장·append-only event, ACK 불명/재시작 unknown 비재전송, host 1개·전역 4 SSH 상한과 관리자·최근 재인증·신뢰망 경계를 제공 |
| `scripts/web-agent-manager-worker.ts` | 원격 host worker CLI | owner-only config/state, root realpath, exact task schema와 설정된 capability별 absolute executable·고정 argv만 허용. detached 멱등 runner, 동시성, timeout TERM/KILL, 최소 환경, 출력 상한/redaction, 재시작 비재실행 status를 제공 |
| `scripts/codex-app-server-canary.ts` | 격리 Codex structured acceptance harness | owner-only 전용 credential seed와 고정 candidate CLI만 사용해 실제 stdio initialize/account/thread start·resume/고정 marker turn/approval decline/interrupt/rate-limit 10단계를 실행. prompt·delta는 메모리에서만 확인하고 제한 evidence·latency만 generic canary 원장에 반환하며 timeout 시 process group을 정리 |
| `src/server/services/deployment-readiness.ts`, `scripts/check-deployment-readiness.ts` | read-only 배포 재시작 preflight | 현재 release의 table/column 계약과 운영 SQLite를 migration 없이 비교하고 quick-check, busy/transitional chat, 검증 중 task·ACK 전 prompt·verification·canary·rollout·remote/structured dispatch, production build와 supervisor 세대를 JSON으로 판정. workload 재시작 가능성과 migration 전 백업 필요를 별도 필드로 내고 blocker가 있으면 종료 코드 2이며 DB·task·process를 변경하지 않음 |
| `scripts/drill-deployment.ts` | 실제 운영 snapshot migration drill | 운영 SQLite를 read-only online backup으로 임시 0700 dataDir에 복제하고 current migration, integrity/foreign key, durable count, 전체 table/column과 임시 test_only scrypt credential을 검증한 뒤 복제본을 삭제. source schema·server·tmux·project를 변경하지 않음 |
| `src/client/features/settings/SettingsView.tsx` profile card | 프로젝트 profile 검토 UI | 활성 프로젝트의 provider/task kind별 초안을 분석해 경고와 불변 JSON을 검토하고 draft/version 저장과 특정 version 명시 활성화를 분리. mutation은 최근 재인증·신뢰망·관리자 경계를 사용 |
| `src/server/services/git-workspaces.ts` workspace validation | worktree profile·지침 생성 증거 | worktree 채팅 생성 직후 immutable profile pin, 요구 지침 존재, 원본↔worktree SHA-256, Claude `@AGENTS.md` import를 검사해 valid/needs_review JSON을 채팅에 저장하고 자동 수정·실행은 하지 않음 |
| `src/server/services/codex-structured-shadow.ts`, `src/server/services/codex-structured-transport.ts` | Codex 구조화 관찰 gate·limited transport | 실제 `starting/running/resuming` 채팅의 `thread/read(includeTurns:false)`와 metadata-only turns를 7일 독립 판정. 두 gate+flag+cohort+quota 뒤의 신규·무기록 채팅만 app-server thread/turn이 소유하며 provider ACK를 task evidence로 연결. 전송 전 실패만 TUI fallback, ACK 유실·재시작은 비재전송 확인 상태, 본문 없는 receipt/provenance와 UI transport 표시를 유지 |
| `src/server/services/agent-preset-service.ts` batch readiness | 활성 프로젝트 profile 승인 준비표 | 모든 활성 프로젝트의 analysis/implementation active version, verification/protected policy와 현재 지침/import/script 경고를 본문·경로·명령 원문 없이 read-only 집계하고 설정 카드에서 개별 검토로 연결 |
| `src/server/services/project-profile-recommendations.ts` | 등록 프로젝트 맞춤 profile catalog | 현재 8개 프로젝트별 reasoning·verification argv·조건 경로·보호 작업을 실제 wrapper/package script가 있을 때만 제안하고 누락은 경고로 반환 |
| `src/server/services/project-profile-launch.ts`, `src/server/providers/provider.ts`의 `ProviderLaunchProfile` | profile 실행 정책 정규화·강제 | 불변 snapshot의 task kind/sandbox/approval/model/reasoning/추가 경로/tool 정책을 저장·활성화·승격·채팅 시작에서 검증하고, adapter가 지원할 수 없는 안전 정책은 실행 전에 거부 |
| `src/server/services/github-auto-merge.ts`, `src/server/routes/git-routes.ts`의 PR API | `GithubAutoMergeService`, check monitor·native auto-merge | 선택 PR의 head/check 집계/review/conflict/native auto-merge를 구조화하고 동일 head·non-draft·mergeable·check 존재·failed/unavailable 0·changes requested 없음 조건을 실행 직전 재검증. 임의 인자/`--admin` 없이 `--auto`/`--disable-auto`와 `--match-head-commit`만 호출하며 관리자·최근 재인증·신뢰망, 최소 감사/task event 요약을 적용 |
| `src/server/services/webhook.ts`, `src/server/routes/webhook-routes.ts` | `WebhookNotifier`, signed outbound webhook API | endpoint 전체와 HMAC secret을 vault에 분리 저장하고 host/configured 상태만 공개. HTTPS 공개 DNS의 모든 IP를 검사해 선택 IP를 실제 TLS lookup에 pin하고 redirect/private·loopback·link-local·documentation 대역을 차단. 고정·길이 제한 JSON과 timestamp-bound HMAC, 10초 timeout, 이벤트별 중복 원장, 관리자·최근 재인증·신뢰망 경계를 제공 |
| `src/server/services/git-hunk-review.ts`, `src/server/routes/git-routes.ts`의 review/hunk API | `GitHunkReviewService`, hunk snapshot·결정 | index→worktree text patch를 파일/hunk SHA-256으로 고정하고 stale·binary·rename/copy·5MiB 초과를 거부. 선택 hunk만 `git apply --cached` 승인 또는 reverse 거부하며 관리자·최근 재인증·신뢰 네트워크와 원문 없는 감사를 적용 |
| `src/server/core/auth.ts` | 세션 loader, `login`, `createWebSession`, `setWebSessionCookie`, CSRF middleware, `requireAdmin` | 비밀번호와 기기 서명 로그인이 공유하는 만료 세션·HttpOnly 쿠키 생성, 실제·더미 해시의 동일 scrypt 검증 경로, 요청 인증과 관리자·CSRF 차단, 활성 앱 기기 세션의 `appTrusted`를 최종 `trustedNetwork` capability에 병합. 세션별 `network_access_allowed=0`은 실제 접속망보다 우선해 임시 로그인의 내부망 capability를 차단 |
| `src/server/core/one-time-login.ts` | `issueOneTimeLoginCode`, `consumeOneTimeLoginCode`, `OneTimeCodeLimitError` | 관리자가 발급한 8자리 숫자 코드의 해시·10분 만료·원자적 1회 소비를 관리하고, 1시간 만료 일반 사용자와 내부망 비허용 웹 세션을 생성. 활성 코드 상한(10개)은 개수 재확인과 INSERT를 한 트랜잭션에 묶어 지키며(넘겨 저장되면 그 코드는 영영 교환되지 않는다), 코드는 서버 시크릿 HMAC-SHA256으로 저장해 `code_hash` UNIQUE 인덱스 한 번의 조회로 검증한다(시크릿은 32바이트 이상 환경변수 우선, 없으면 데이터 디렉터리에 0600 파일로 배타 생성). 같은 숫자가 30일 보존 기간 안에 다시 뽑히면 UNIQUE에 걸리므로 다른 숫자로 다시 뽑는다. 상한 초과는 `OneTimeCodeLimitError`로 구분해 라우트가 500이 아닌 429와 가장 이른 만료까지의 `Retry-After`를 준다 |
| `src/server/core/secret-input.ts` | `readSecretInput` | 백업 passphrase·test-only password를 직접 env 또는 absolute owner-only 0600 일반 파일 하나에서만 읽고 symlink, 열린 권한, 다른 owner, NUL/내부 개행, 크기 초과와 두 입력 동시 설정을 거부 |
| `scripts/create-test-user.ts` | test-only 계정 발급 CLI | owner-only password file을 받아 이미 `access_scope` migration이 적용된 DB에만 user/test_only 계정을 생성·회전. 구버전 schema는 자동 migration하거나 쓰지 않음 |
| `src/server/core/security.ts` | 비밀번호/토큰/경로/파일명 유틸, `dummyPasswordHash` | scrypt와 계정 열거 완화용 고정 더미 해시, 쿠키·토큰 해시, 타이밍 세이프 비교, 허용 루트와 파일명·ANSI 정규화 |
| `src/server/core/login-rate-limit.ts` | `LoginRateLimiter` | IP 전체 요청 창과 IP+계정 연속 실패 잠금, 잠금별 단일 감사 결정, TTL 정리와 메모리 항목 상한 적용 |
| `src/server/core/http-security.ts` | `createHttpSecurityHeaders` | CSP·프레임·MIME·권한·referrer·교차 출처 정책과 HTTPS HSTS를 적용하며 Vite 개발 인라인 모듈만 별도 허용 |
| `src/server/core/audit.ts` | `writeAudit`, `loginRequestIdentifiers` | 민감 원문을 제외한 감사 이벤트 저장. `loginRequestIdentifiers`는 로그인 요청의 추적 식별자(원격 주소·`X-Forwarded-For`·`X-Real-IP`·User-Agent·Origin·Referer·Accept-Language)를 길이 제한과 함께 모으며, 비밀번호·세션 토큰·CSRF·일회용 코드 원문과 Cookie·Authorization 헤더는 넣지 않는다 |
| `src/server/core/uploads.ts` | `processMultipartFiles`, `streamToFile` | multipart의 파일당·총량·개수·시간·동시 요청·디스크 여유 제한과 중단 정리. 총량·동시성 조기 거부 시 요청 소켓을 `destroy()`해 아직 전송 중인 바디가 응답 전달을 막아 클라이언트가 pending으로 멈추는 것을 방지. `0600` 임시 파일 후 hard link no-replace 또는 명시적 overwrite로 원자 반영하고 크기·해시 반환 |
| `src/server/core/chat-busy.ts` | `setChatBusy` | 채팅별 "작업중" 여부를 `chats.busy` 컬럼에 영속화하고 `chat_busy` 웹소켓으로 알림(단일 진실 공급원 — 클라이언트는 값을 추측하지 않고 `/chats` 응답을 그대로 표시) |
| `src/server/providers/provider.ts` | `ProviderAdapter`, `TmuxIO`, `HistorySyncContext`, `HistorySyncDecision` | 공급자 실행·기록·승인·TUI 상호작용 공통 계약, 표시 라벨·대표 사용량 창·초기화 알림 대상 창 목록·창 ID별 공통 종류 이름(`usageWindowLabels`)·초기화 경계에서 일시적으로 숨는 창 목록·CLI 버전 명령, 준비/작업중 판정, 사용량 파싱과 저빈도 상세 메뉴 탐색, 모델 적용, 별도 추론 강도 명령, 공급자별 기록 동기화 정책(작업중 시작·해제·완료 알림), 입력창에 남은 미전송 초안을 읽는 `readPromptDraft` 선택 훅(빈 입력창은 빈 문자열, 입력창 자체가 없는 승인·메뉴 화면은 null), 공급자 내부용 기록 숨김 판정 선택 훅, 승인 화면의 선택지 번호가 도구마다 달라지는 공급자를 위한 화면 기반 `resolveApprovalInput` 선택 훅(확신 못 하면 null을 돌려 `approvalInput` 고정 매핑으로 되돌아감), 사용량 화면이 모달로 뜨는 공급자가 파싱 뒤 닫을 때 보낼 `usageScreenCloseInput` 선택 훅, `/model` 메뉴를 열지 않고도 모델 목록을 만들 수 있는 공급자용 `promptQuirks.modelOptionsWithoutMenu` |
| `src/server/providers/*-usage-collector.ts`, `src/server/providers/usage-collector.ts`, `src/server/providers/model-options-collector.ts` | 공급자별 direct 사용량·모델 카탈로그 수집기 | Claude OAuth usage·계정별 model-selector API(quick-select alias/정확한 버전 동적 분리와 최소 CLI 버전 필터), Codex app-server `account/rateLimits/read`·`model/list`, Grok billing API·비대화형 `grok models`를 공급자별로 분리한다. 비밀 토큰은 스냅샷에 넣지 않고 공통 사용량/모델 계약으로 정규화하며, 호출 실패는 `UsageMonitor`가 임시 PTY 폴백으로 넘긴다. |
| `src/server/providers/codex.ts` | `CodexAdapter` | Codex 실행/resume(생성자로 받은 WAM 훅 `-c` 인자와 `--dangerously-bypass-hook-trust`를 채팅 실행에만 붙이고, 사용량·모델 조회 PTY는 `createMonitorLaunch`로 훅 없이 실행 — 비관리 훅은 해시 신뢰 없이는 건너뛰어 사용자 결정(#92)으로 WAM 채팅에만 우회), JSONL 변환(event_msg 중복 제거, AGENTS.md 자동 주입 지침 분리, 내부 권한 상승 검토 세션 숨김, turn_context 기반 실제 모델·추론강도 감지, `token_count.last_token_usage`를 같은 턴 assistant 메시지의 입력·캐시·출력·추론 토큰으로 연결, task_complete/turn_aborted 기반 턴 종료 시각 기록), Codex TUI 준비/작업중 판정과 마지막 `›` composer의 미전송 초안 판독(순환 placeholder는 빈 문자열, 번호 선택 메뉴는 null), Codex 기록 동기화 정책(task_complete/turn_aborted 기반 완료·busy 해제), 붙여넣기 뒤 160ms 안정화·일반 프롬프트 제출 확인 특례, `/usage`·`/status` 파싱, `/model` 모델·effort 목록 파싱(Codex가 추천 모델에 붙이는 `(default)` 표시도 `(current)`·`(selected)`와 함께 인식하되 현재 선택값으로는 취급하지 않음 — 이 표시를 못 알아채던 예전 정규식은 해당 줄 전체가 파싱 실패해 그 모델이 목록에서 통째로 빠졌음)와 적용, slash 명령 자동완성 Enter 특례 선언, 번호 선택 메뉴가 실제로 있을 때만 승인으로 보는 TUI 승인 감지·입력(한도 임박 시 뜨는 경량 모델 전환 3지선다, 선택 메뉴 없이 `try again at`만 찍는 사용량 한도 화면 — 시각만 있는 기존 형식과 "Jul 13th, 2026 12:14 AM"처럼 요일 없는 전체 날짜까지 있는 새 형식을 둘 다 인식, 새 디렉터리 신뢰 확인 2지선다는 각각 별도 요청유형으로 감지·전용 키 매핑), tmux pane 캡처의 끝쪽 빈 줄을 먼저 제거한 뒤 마지막 줄만 검사, 시작 배너·하단 상태줄 모델 감지, 대표 사용량 구간은 Plus 플랜 계정엔 5시간 한도 자체가 없어 weekly로 표시. Pro 플랜 `/status`의 값 없는 모델 전용 헤더(`GPT-5.3-Codex-Spark limit:` 등) 아래 5h·주간 창은 플랜 한도가 아니므로 `stripCodexModelLimitSection`이 잘라내고 읽지 않는다(#87) |
| `src/server/providers/codex-rate-limits.ts` | `readCodexResetCredits`, `consumeCodexResetCredit`, `selectCodexResetCredit`, `parseCodexResetCreditConsumeOutcome`, `parseCodexResetCreditsScreen` | 현재 계정 설정 디렉터리의 Codex app-server 구조화 응답과 `/usage` 초기화권 상세 화면에서 잔여 개수·Full reset 기한을 추출한다. 사용 시 응답 순서상 첫 available ID(상세 생략 시 backend 다음 권리)를 멱등성 키와 함께 공식 consume 메서드에 전달하고 잔여량을 재조회하며, 사용 응답 유실도 실제 개수 감소로 복구한다. |
| `src/server/providers/claude.ts` | `ClaudeAdapter` | Claude 실행/resume, JSONL 변환(도구 결과·/exit 등 로컬 명령 user 턴을 tool/system으로 재분류, `/compact`·컨텍스트 한도 자동 압축이 CLI가 API 프로토콜상 어쩔 수 없이 "user" 역할 턴으로 주입하는 요약 전체(`isCompactSummary` 플래그로 식별)도 system(`compact_summary`)으로 재분류해 실제 사람이 그 긴 요약문을 직접 친 것처럼 채팅 말풍선에 보이지 않게 함, assistant content에 `tool_use` 블록이 있으면 `stop_reason`과 무관하게 완료가 아닌 `tool_call`로 표시, assistant `message.usage`의 입력·캐시 생성·캐시 읽기·출력 토큰을 공통 메시지에 보존, `stop_reason=end_turn` assistant를 완료 판정용 `turn_end`로 표시, 실제 사용자 메시지가 없는 상태 조회·local-command 전용 JSONL 제외 — 내용 기반 숨김 판정은 파일 전체를 읽어야 해서 mtime 캐시로 재검사를 최소화(실제 대화 확인된 파일은 영구 false), assistant 메시지 기반 실제 모델 감지), Claude screen-reader·일반 TUI 준비/작업중 판정, 입력창에 남은 미전송 초안을 읽는 `readPromptDraft`(선택 메뉴 커서 줄 `❯ 1. Yes`는 입력창이 아니라 null, dim 자동 제안과 "Try ..." 안내는 지울 글자가 없어 빈 문자열), 긴 bracketed paste 뒤 안정화 지연과 실제 제출 확인·Enter 1회 재시도, Claude 기록 동기화 정책(`turn_end` assistant 기반 완료·busy 해제), 진행 막대형 `/usage` 파싱과 stale 판정, 정렬형 `/model` 모델·effort 목록 파싱(`/model` 메뉴 선택 설명에서 현재 모델명을 보완), 모델 적용 시 채팅의 실제 최신 `/model` 메뉴를 다시 파싱해 캐시된 모델 ID와 일치하는 현재 번호를 선택(메뉴 순서가 바뀌어도 다른 모델을 고르지 않음), 최신 TUI의 `/effort <강도>` 별도 적용, hook 환경 연결, 훅 밖 TUI rate-limit 선택 화면 감지(스크롤백에 남은 문구가 아니라 "Enter selection [1-2]" 같은 한도 선택 화면 전용 표시로만 판정, 대형 세션 resume 선택 화면은 `Enter selection [1-3]` 또는 최신 `Enter to confirm · Esc to cancel` 푸터와 "Resume from summary" 문구를 함께 요구, 디렉터리 신뢰 확인 등 y/n 확인 화면은 "Enter y/n:"으로 별도 요청유형(confirm_yn) 감지 — 이 문구 자체가 기능을 설명하는 코드 주석·문서에 리터럴로 있어 grep/cat 도구 출력이나 이를 설명하는 assistant 텍스트에도 같은 화면에 찍혀 오탐했던 적이 있어, resume_session_prompt·generic_permission_prompt와 같은 방식으로 그 뒤에 esc to interrupt·"$" 프롬프트로 이미 넘어갔는지까지 확인하는 것에 더해, CODETREE.md 표의 매우 긴 한 줄(claude.ts 설명 자체)이 pane에서 여러 줄로 접혀 나오며 그 안의 "Enter y/n:"이 staleIndex보다 뒤로 밀리는 재오탐도 실제로 겪어 그 줄이 이 문구 하나만으로 이뤄져 있는지까지 요구하도록 좁힘, claude-in-chrome 등 브라우저 도구 권한 화면은 제목 문구 대신 실제 선택지 3개(Install extension/Not now/Don't ask again)의 구조와 화면 하단 위치로 판정해 `browser_permission_prompt`로 감지·전용 키 매핑(제목 문구 매칭 방식은 문구가 바뀌면 놓칠 수 있어 구조 기반으로 교체))·요청유형별 입력 매핑, tmux pane 캡처의 끝쪽 빈 줄을 먼저 제거한 뒤 마지막 줄만 검사, 시작 배너 모델 감지(대화가 길어져 배너가 화면 밖으로 스크롤되면 `/model` 변경 직후 뜨는 "Set model to X ..." 확인 메시지로 대체 감지), 표시용 제목(`displayTitle`)은 `claude --resume`·`claude agents --json`이 보여주는 것과 같은 소스인 `~/.claude/sessions/<pid>.json`의 `name`(문서화 안 된 내부 파일이라 실패하면 조용히 무시, sessionId당 가장 최근 것만, `kind:"bg"`는 세션 이름이 아니라 작업 설명이라 제외, `nameSource:"derived"`는 실제 `/rename`이 아니라 "작업디렉터리명-무작위2자" 자동 기본값이라 제외 — 진짜 `/rename`을 실행하면 이 필드 자체가 사라지는 것으로 확인함)을 최우선으로, 없으면 대화 중 계속 갱신되는 JSONL의 `aiTitle`로 대체 |
| `src/server/providers/claude.ts` | `isClaudeUsageRateLimited` | 조회 전용 PTY가 `/usage`를 자주 호출하면 `Per-model breakdown unavailable (rate limited)` 재시도 화면이 뜨고 Current session 숫자는 프로세스 시작 시점 캐시(실측 19%인데 1%)다. 이 화면은 성공 스냅샷으로 쓰지 않는다 |
| `src/server/providers/claude.ts` | `rateLimitPromptIndex`, `hasOnlyIdleChromeAfter` | 최신 Claude 리미트 메뉴에서 공통 푸터만 신뢰하지 않고 `What do you want to do?` 제목과 wait/upgrade 두 번호 선택지, 화면 하단 위치를 함께 검증해 `rate_limit_options`로 판정. 메뉴 뒤에 idle·응답 생성 표시가 있으면 지나간 화면으로 제외하고, 선택 메뉴 없는 세션 리밋 배너는 뒤에 유휴 프롬프트·상태줄만 남은 최신 화면에서만 감지한다. 예전 `Enter selection [1-2]` 형식은 `ClaudeAdapter.detectApproval`의 기존 경로로 병행 지원 |
| `src/server/providers/grok.ts` | `GrokAdapter` | Grok 실행/resume(`grok --resume <세션ID>`, 생성자로 받은 훅 토큰 환경을 실행에 실어 전역 훅 브리지가 쓰게 함 — #96), 세션 디렉터리(`~/.grok/sessions/<URL인코딩 cwd>/<세션UUID>/`) 기반 기록 변환. 대화 본문 `chat_history.jsonl`, 턴 종료 `events.jsonl`, 턴 토큰 `updates.jsonl`의 `turn_completed.usage`를 같은 세션의 입구로 받아들이고(어느 쪽으로 들어와도 `historyFile`은 `chat_history.jsonl`로 고정) `summary.json`에서 cwd·세션ID·모델(`current_model_id`)·생성/수정 시각·표시 제목(`generated_title`)을 읽는다. `turn_completed`는 사용자 턴 순서대로 그 턴의 마지막 내용 있는 assistant에만 붙이며, 캐시 읽기는 Codex처럼 입력의 부분집합(`cachedInputTokens`)으로 두고 합계는 다시 더하지 않는다. user 레코드는 사람이 친 `<user_query>` 블록만 남기고 CLI가 같은 타입으로 주입하는 `<user_info>`·`<git_status>`·`<system-reminder>`는 제외하며, `reasoning`(내부 사고 요약)·`system`·빈 assistant는 대화에서 뺀다. 상태줄로 준비/작업중 판정(`Shift+Tab:mode` 상태줄, 응답 중에만 붙는 `Esc:cancel`), 입력창 테두리 꼬리(`Grok 4.6 (high) · auto`)에서 모델·추론 강도·권한 모드를 한 번에 읽고, 모델 변경은 메뉴 탐색 없이 `/model <모델> [강도]` 한 줄로 적용한다. 사용량은 `/usage show`(인자 없이 보내면 show·manage 하위 선택에서 멈추고, `manage`는 결제 관리라 자동 조회가 골라선 안 된다)로 조회하고, 입력창을 덮는 모달이라 파싱 뒤 `usageScreenCloseInput`(Esc)으로 닫아야 다음 주기 명령이 입력창에 들어간다. 모델 목록은 `/model`을 보내지 않고 만든다(`modelOptionsWithoutMenu`) — Grok의 `/model`은 선택 메뉴가 아니라 인자 입력 대기라 Esc 뒤에도 입력이 남아 다음 사용량 조회가 `Unknown model: /usage show`로 실패했다 |
| `src/server/providers/grok.ts` | `parseGrokWindows`, `parseGrokSessionUsage` | `/usage show` 화면의 사용량 창을 제목이 아니라 막대·퍼센트 줄을 앵커로 찾는다 — 제목(`Weekly limit (SuperGrok)`)은 플랜마다 다른 데다 tmux 캡처가 간헐적으로 글자를 흘려(`Weekly  imit` 실측) 제목을 조건으로 걸면 그 주기 사용량이 통째로 파싱 실패로 떨어진다. 숫자와 `Resets:` 키워드로 찾고 제목은 위쪽에서 표시용으로만 주워오며, `limit`이 성하지 않으면 캡처 손상으로 보고 대표 창 표시명만 보정한다(플랜 이름은 화면 값 유지). 세션 토큰·호출·비용 줄은 한도가 아니라 참고 수치라 퍼센트 창이 아닌 활동 목록으로 남긴다 |
| `src/server/providers/grok.ts` | `readLastTurnEndedAt`, `readGrokTurnUsages`, `evaluateHistorySync` | Grok은 도구 호출 직전에도 설명 문장을 assistant 메시지로 남겨 "내용 있는 assistant"를 완료로 보면 턴 중간마다 완료 알림이 나간다. 실제 완료는 `events.jsonl`의 마지막 `turn_ended`뿐이라 그것만 완료·busy 해제로 인정한다. `chat_history.jsonl`의 mtime만 보면 마지막 메시지 기록과 `turn_ended` 기록 사이의 짧은 순간에 스캔이 걸렸을 때 턴 종료를 영영 놓치므로, `events.jsonl`도 세션 입구로 연다. 턴 토큰은 `updates.jsonl`이 갱신될 때 본문 캐시를 버려 말풍선에 붙인다(events.jsonl은 턴 중 phase가 수백 줄이라 같은 처리를 하지 않는다) |
| `src/server/providers/grok.ts` | `approvalScreenIndex`, `parseApprovalOptions`, `resolveApprovalInput` | 도구 실행 승인 화면을 푸터(`1/4:select`)와 라디오 선택지(`3 (○) Yes`)가 화면 하단에 함께 있을 때만 감지해, 문서·도구 출력에 섞인 비슷한 문구를 승인 요청으로 오탐하지 않는다. 선택지 구성이 도구마다 달라 고정 번호를 쓰지 않고 실제 화면 문구에서 번호를 찾는다(`Yes` 단독=1회 허용, `during this session`=세션 허용, `No`=거부). `approvalInput`은 빈 문자열을 돌려 화면을 못 본 상태에서는 아무 키도 보내지 않는다 |
| `src/server/providers/codex.ts`, `src/server/providers/claude.ts` | `isHiddenHistoryFile` | 내부 승인·상태 명령뿐 아니라 고정 사용량 최소 턴과 그 단답 응답만 있는 조회 PTY 기록도 일반 채팅 목록에서 숨긴다. Codex는 앞선 프로젝트 지침·슬래시 명령을 건너뛰고 첫 실제 프롬프트를 판정하며, Claude는 파일 mtime 변경 때 실제 사용자 대화가 추가됐는지 다시 확인한다 |
| `src/server/providers/history-utils.ts` | `extractContent`, `fallbackId` | 공급자 기록의 표시 텍스트와 안정 ID 생성 |
| `src/server/providers/usage-utils.ts` | `isExpiredResetTime`, `todayResetTime` | 시:분 추출은 `rate-limit-resume`의 `parseClockTime`을 공유해 정각 표기("6pm")까지 읽는다 — 예전에는 분을 필수로 요구해 정각이 전부 null이 됐고, 그 탓에 아래 만료·불가능 판정이 정각 표기에서만 통째로 무력화됐다(#53). `todayResetTime`은 날짜 없는 "시:분" 리셋 문구를 "오늘 그 시각"의 실제 Date로 환산(이미 지났어도 내일로 안 미룸 — rate-limit-resume의 `parseResetTime`은 반대로 항상 다음 도래 시각으로 미루므로 용도가 다름), `isExpiredResetTime`은 그 결과가 이미 지났는지만 판정하는 공급자 공용 유틸 |
| `src/server/providers/claude.ts`, `src/server/providers/usage-utils.ts` | Claude 자정 경계 사용량 판정 | 날짜 없는 `Current session` 리셋이 오늘 이미 지났어도 다음 발생이 5시간 안이면 익일 리셋으로 인정한다. 밤의 `Resets 1am`을 stale로 버리지 않되 오전의 오래된 `3:40am`은 계속 stale로 거른다. |
| `src/server/services/session-manager.ts` | `SessionManager` | 채팅의 전용 worktree 또는 프로젝트 공유 경로에서 tmux/PTY 생성(기본 256×36, 상태바 끔)·웹에서 수동 시작, 데스크톱 패널 요청에 따라 256열은 유지하고 PTY·tmux·headless 화면의 행 수만 함께 바꾸는 `resizeTerminal`, 입력 직렬화(system 자동화는 `user=null`로 호출 가능)와 첨부 변환까지 끝난 실제 터미널 전달 문구 반환, 일반 전송의 자동 resume와 실행 중 후속 입력 TUI 큐 전달, terminal map이 먼저 생긴 새 세션도 실제 입력 프롬프트까지 준비 대기, 일반 프롬프트 제출 확인(`confirmPromptSubmission`, #98 — 유휴·작업 중 모두. `waitForPastedDraft`로 붙여넣은 글이 입력창에 보인 뒤 Enter를 보내고, 성공 증거는 UserPromptSubmit 훅(`notePromptSubmitted`)·공백 무시 JSONL 새 user 기록·빈 입력창·입력창 없는 작업중 중 하나. 글이 입력창에 계속 남을 때만 Enter를 최대 3회 재시도하고 6초 뒤에도 남으면 실제 남은 초안 길이만큼 지우고 `busy`를 원복. 예전에는 기록 있는 Codex 채팅이 1.8초 안의 JSONL 기록만 인정해, 대기열·기록 지연으로 늦게 써진 실제 전송을 지우고 실패를 알렸다(실측 #1061 7.6초)), 전송 직전 입력창에 남은 미전송 초안을 `readPromptDraft` 길이만큼 지워 새 입력으로 덮어쓰기(초안이 남으면 준비·작업중 판정이 동시에 false가 되어 매 전송이 15초 뒤 실패하고 상태를 error로 덮어쓰는 교착이 됐음)와 준비 대기 중 승인 화면 감지 시 15초 대기 없이 즉시 이유를 알리는 거부, 종료·busy·실제 터미널 유실 시 자동 시작하지 않는 시스템 입력 전용 `sendPromptIfRunning`, 사용자 종료 시 리밋 대기 취소, 중지 복구용 원래 질문 보존, 어댑터 기반 준비 대기·작업중 판정·모델/추론 강도 적용, tmux 이름 있는 키 기반 응답 중단, 권한 모드 전환, 구독 즉시 현재 화면·커서 ANSI 프레임 전송, PTY 출력을 headless 현재 화면으로 재구성하고 출력 폭주를 500ms로 합치며 정적 화면은 5초 안전망으로 승인·busy·권한 모드를 한 스냅샷에서 판정, `supportsSessionRename`을 선언한 Claude·Codex만 `renameSession`을 허용하고 Grok에는 웹 버튼도 노출하지 않음, 작업 중 모델·이름 변경 즉시 거부, 웹 휠·스와이프를 tmux 기록 이동으로 옮기는 `scrollTerminal`(한 번에 200줄 제한)과 기록 보기 중 입력·중단·준비 대기 시 실시간 화면 자동 복귀·상태 스캔 보류 |
| `src/server/services/session-manager.ts` | `logStateCheck`, `clearChatCaches` | debug 상태 판정 로그는 화면 원문을 캐시에 보관하지 않고 SHA-256 지문·판정·마지막 기록 시각만 유지한다. 판정은 같고 스피너·경과 시간만 변하는 화면은 30초에 한 번만 상세 기록하며, 동일 화면은 반복 기록하지 않는다. 채팅 중지·PTY 종료 때 해당 채팅의 상태 판정·입력 큐·마지막 프롬프트 캐시를 제거하고 서버 종료 때 전체를 비워 장기 실행 메모리가 종료된 채팅 수에 비례해 남지 않게 한다. |
| `src/server/services/session-manager.ts` | `restartProviderTerminals` | CLI 업데이트 후 해당 공급자의 활성 채팅과 `error` 상태여도 실제 PTY가 붙은 채팅을 정상 종료한 뒤 저장된 공급자 세션으로 재개하며, 개별 실패를 모아 반환한다. PTY 종료 자체에 대한 외부 알림은 보내지 않는다. |
| `src/server/services/session-manager.ts` | 빈 채팅 종료 정리·history claim | 실제 history 파일과 내부 session ID가 일치할 때만 resume한다. 신규 첫 질문은 exact claim으로 남기며, 터미널 종료 시 user/assistant 기록 없는 채팅 row를 자동 삭제한다. 방금 제출한 질문은 30초 전체 발견 주기를 넘기는 35초 동안 유예해 기록 연결 경합으로 정상 대화를 지우지 않는다. |
| `src/server/providers/claude.ts`, `src/server/services/session-manager.ts` | `claudeQueuedPromptRecorded`, `ProviderAdapter.hasQueuedPrompt`, `historyFileState` | Claude 작업 중 후속 입력은 UserPromptSubmit 훅도 user 기록도 없이 JSONL `queue-operation enqueue`로만 즉시 남아, 전송 확인(`confirmPromptSubmission`)이 기록 파일이 바뀔 때마다 끝부분 256KB에서 보낸 시각 이후·같은 내용(공백 정규화)의 enqueue를 제출 증거로 인정한다. 전송 전부터 작업 중이던 채팅은 제출 확인이 실패해도 busy를 끄지 않는다(#103) |
| `src/server/services/chat-prompt.ts`, `src/shared/chat-prompt.ts` | `prepareChatPrompt`, `promptCharacterCount` | 1,000자를 넘는 일반 채팅 본문을 프로젝트와 전용 worktree의 채팅별 `.txt` 첨부로 원자 저장하고 TUI용 짧은 파일 읽기 지시를 구성한다. 슬래시·셸 명령은 원문을 유지하며, 실패한 제출은 방금 만든 파일만 정리한다. 클라이언트와 서버가 유니코드 코드 포인트 기준 임계값을 공유한다 |
| `src/shared/chat-attachments.ts` | `CHAT_ATTACHMENT_MAX_FILE_BYTES`, `CHAT_ATTACHMENT_MAX_TOTAL_BYTES`, `CHAT_ATTACHMENT_MAX_FILES` | 채팅 첨부 업로드 제한(파일당 25MB·총 50MB·5개) 공유 상수. 서버(`project-routes.ts`)와 클라이언트(`ChatView.tsx`)가 같은 값을 참조해 클라이언트가 제한을 넘는 파일을 애초에 전송하지 않게 한다 |
| `src/server/services/history-sync.ts` | `HistorySynchronizer` | 전역 JSONL 스캔, 공급자 내부용 숨김 기록 제외와 과거 등록 row 정리, 프로젝트·채팅 upsert, 어댑터 기록 동기화 정책에 따른 완료 알림·작업중 표시 갱신과 Claude 최종 응답 뒤 남은 PermissionRequest 훅 정리(외부에서 발견한 `stopped` 채팅과 오류로 남은 `error` 채팅은 완료 레코드 없는 user 메시지·도구 호출이 있어도 busy를 올리지 않고 옛 값도 정리 — `error + busy=1` 조합은 사용자가 전송도 복구도 못 하는 교착이었다), 등록된 알림 채널(Slack·ntfy) + 브라우저 알림용 `task_completed` 브로드캐스트, 메시지는 DB 미러링 없이 history-cache로 그때그때 파싱하되 usage가 있는 assistant 메시지는 숫자만 `TokenUsageLedger`에 멱등 저장, Grok `updates.jsonl` 같은 사이드카가 바뀌면 채팅 API가 읽는 본문(`chat_history.jsonl`) 캐시를 버려 턴 토큰이 말풍선에 나타나게 함 — 사이드카는 턴 중에도 계속 쌓이므로(실측 updates.jsonl 2.9MB, 본문 717KB) 바뀔 때마다 버리면 응답 내내 본문을 반복 파싱하게 되어, 토큰이 붙은 메시지 수가 실제로 늘었을 때만 무효화함, JSONL에 기록된 실제 모델명으로 채팅 모델 갱신·브로드캐스트, 채팅 제목은 `chats.title_source`가 'manual'(SessionManager.renameSession으로 사람이 직접 바꿈)이 아닌 한 매 동기화마다 공급자 `displayTitle`(있으면)·없으면 첫 메시지 기반 제목으로 계속 갱신. 사용자가 정리한(`active=0`) 프로젝트의 세션은 새 채팅으로 만들지 않아 삭제한 채팅이 다음 스캔에서 재등록되지 않음. 프로젝트 귀속은 `세션 cwd와 정확히 같은 등록 프로젝트 → 그 경로를 쓰는 worktree 소유 채팅의 프로젝트 → 가장 가까운 상위 등록 프로젝트 → 신규 discovered 등록` 순서로 정하고, 기존 채팅의 `project_id` 재귀속은 worktree 소유자가 확인된 경우에만 수행(세션 cwd는 레코드마다 갱신되어 에이전트가 `cd`만 해도 바뀌므로 일반 채팅을 매번 재귀속하면 채팅이 하위 디렉터리 프로젝트로 튕겨나가 자기 자신을 worktree 소유자로 찾는 고착에 빠진다). 계정마다 기록 루트가 갈라지므로 등록된 전 계정의 `historyRootFor(config_dir)`를 모두 스캔하고, 발견한 세션은 그 파일이 실제로 놓인 계정(`chats.account_id`)에 귀속 새 세션을 등록하는 INSERT 경로에는 MyAgent가 만든 적 없는 외부 CLI 세션만 들어오므로(웹에서 만든 채팅은 이미 행이 있어 UPDATE 경로를 탄다) `origin='delegation'`으로 넣어 일반 채팅 목록에서 제외한다 — 그러지 않으면 AI·스크립트가 CLI를 직접 띄울 때마다 목록에 쌓여 실제 채팅이 묻힌다(실측 #61: 목록 100건 중 77건). 사용자가 웹에서 그 채팅에 프롬프트를 보내면 `SessionManager`가 `user`로 승격해 다시 목록에 올린다. `origin`에 `CHECK(origin IN ('user','delegation'))` 제약이 있어 `external` 같은 새 값은 테이블 재생성이 필요해 기존 값을 재사용한다. 생성 경로는 `tmux_name`으로 사후 구분한다 — 웹 생성은 `web_agent_manager_chat_<id>`, 이 경로는 `web_agent_manager_<hash16>`. |
| `src/server/services/history-sync.ts` | `syncAll`, `syncActive`, `forgetHistoryFile` | `forgetHistoryFile`은 채팅에서 떼어 낸 기록 파일의 본 mtime을 지워 다음 전체 스캔에서 다시 반영하게 한다(훅 /clear 재연결용). 선택 인자 `hookObserver`에는 실시간 주기에서 찾은 실행 중 채팅의 턴 종료만 알린다(시작 스캔의 과거 턴 제외, #93). 이미 연결된 활성 채팅의 주 기록과 Grok 본문·events·updates 사이드카 묶음은 2초마다 mtime 증분 확인한다. 새 외부 세션을 찾는 전 계정 history root 재귀 탐색과 변경 없는 busy 고착 복구용 강제 재파싱은 시작 시 1회와 30초마다만 수행해, 약 675개 JSONL 열거·stat와 큰 진행 중 JSONL 전체 재파싱을 2초마다 반복하며 4 vCPU 기준 CPU 약 3~4%를 쓰던 낭비를 제거한다. `history_file`이 없고 starting·resuming이거나 실제 질문을 받아 running+busy인 채팅이 있으면 빠른 주기가 전체 탐색으로 승격해 최초 기록 연결은 늦추지 않는다. |
| `src/server/services/history-sync.ts` | `chat_history_claims` 귀속 | user 메시지 없는 session metadata는 FIFO로 빈 채팅에 붙이거나 외부 채팅으로 만들지 않는다. 첫 user 메시지의 정규화 본문·시각과 웹 전송 claim을 대조하며, 종료가 발견보다 먼저 온 stopped claim도 회수한다. |

| `src/server/services/token-usage-ledger.ts` | `TokenUsageLedger`, `tokenUsageSnapshotForChat` | 공급자·세션·assistant 메시지 ID를 안정 키로 입력·캐시·출력·추론·총 토큰과 당시 프로젝트·채팅·계정·모델 표시값을 영구 upsert. 채팅 삭제 표시는 row 삭제와 분리해 유지하고, 최근 기간 또는 전체를 일자·프로젝트·채팅·공급자·계정·모델별 고정 SQL 식으로 합산하며 브라우저 시간대의 일자 경계를 적용. 공급자 표시명은 Codex·Grok·Claude를 구분한다 |
| `src/server/services/history-cache.ts` | `HistoryCache` | 파일 mtime·크기 기준 세션 파싱 결과 캐시. append-only JSONL은 새 바이트의 완성 레코드만 공급자 증분 파서에 전달하고 파일 축소·덮어쓰기·불완전 레코드는 전체 파싱으로 복구, 메시지 조회의 단일 소스, 삭제·복원 시 캐시 무효화 |
| `src/server/services/session-backups.ts` | `SessionBackupService` | Codex·Claude JSONL 세션과 채팅 메타데이터 백업, 원본 삭제, 공급자 기록 저장소로 복원, `deleteBackup`으로 백업 사본(JSONL·메타데이터)만 지우고 원본 채팅·공급자 기록은 건드리지 않음. 백업·삭제 직전에 최신 숫자형 토큰 이벤트를 원장에 보존하고, 서버 시작 시 남은 기존 백업을 역수집해 기능 도입 전 삭제 채팅도 가능한 범위에서 복구 |
| `src/server/services/approval.ts` | `ApprovalService` | 훅 채팅 식별(`findHookChat`)은 브리지가 넘긴 채팅 ID(그 공급자 채팅일 때만)를 먼저 쓰고, 없을 때만 session_id → cwd의 최근 채팅 순으로 추측한다. 훅 승인 대기(`awaitHookDecision`)는 Claude·Codex 공용이며, `handleCodexHook`(#95)은 Codex 출력이 allow/deny뿐이라 세션 허용도 allow로 보내고 채팅을 못 찾으면 결정 없이 돌려줘 Codex 기본 승인 화면(화면 감지)으로 넘긴다. 결정 전달·닫기 판정은 공급자가 아니라 훅 대기자 유무로 가른다. 공급자·요청유형별 승인 저장, 웹 결정(터미널 결정 콜백에 요청유형 포함 전달), 승인 알림에 요청 요약 포함, timeout 기본 거부, `dismiss`(지나간 요청은 웹 목록만 정리하고, AI가 실제로 기다리는 중이면 막지 않고 `decline` 결정으로 전달해 즉시 풀어줌 — 답변 전송과 닫기밖에 없는 질문 카드에서 예전 방식은 사용자를 훅 9분 타임아웃까지 갇히게 했음. `cancel`은 훅에 `interrupt=true`로 나가 진행 중인 턴 자체를 중단시키고 터미널에는 Esc를 보내므로 쓰지 않는다 — 질문 하나를 닫았을 뿐인데 작업 전체가 취소되면 안 됨. 터미널 키 입력은 `terminalLiveCheckHandler`가 지금 화면에 그 프롬프트가 실제로 떠 있다고 확인한 경우에만 나감)와 `decide`를 분리, Claude 턴 완료·터미널 종료 시 잔여 요청 정리, `autoResolve`(사람 개입 없이 시스템이 즉시 결정, `notify=false`로 만들면 웹·알림 채널 없이 조용히 생성) |
| `src/server/services/runtime-files.ts` | `prepareRuntimeFiles` | 앱 소유 Claude hook 설정과 내부 토큰 파일 생성. `PermissionRequest`는 command 브리지(`scripts/claude-permission-bridge.ts`, `WEB_AGENT_MANAGER_CHAT_ID`를 `X-WAM-Chat-Id` 헤더로 전달. 인자 없으면 Claude 모드로 실패 시 거부, URL 인자를 주면 Codex 모드로 실패 시 아무것도 출력하지 않아 Codex 기본 승인 화면으로 넘김)로, `UserPromptSubmit`·`Stop`·`StopFailure`는 `/internal/claude/hook-event`로 가는 HTTP 훅으로 등록한다. Claude는 자격증명형 환경변수를 HTTP 훅 헤더에 치환하지 않아 토큰은 0600 설정 파일에 직접 두고 채팅 ID만 `allowedEnvVars`로 치환한다. `SessionStart`는 Claude가 HTTP 훅을 건너뛰어(실측) command 브리지(`scripts/hook-event-bridge.ts`)로 받는다. Codex용 `codexHookArgs`는 `-c hooks.<이벤트>=[...]` 인라인 TOML로 같은 브리지를 `/internal/codex/hook-event`에 연결하며 SessionStart와 PermissionRequest(승인 브리지 Codex 모드 → `/internal/codex/permission`, 600초)는 동기, 턴 이벤트는 async다. `grokHooks`는 Grok 전역 훅 파일 내용(SessionStart·UserPromptSubmit·Stop·StopFailure·StopCancelled를 브리지로 `/internal/grok/hook-event`에 연결)이고, `installGrokHooks`가 서버 시작 시 기본 Grok 홈의 `hooks/web-agent-manager.json`(0600, 같은 내용이면 재작성 안 함)에 설치한다(#96). Grok HTTP 훅은 헤더가 없어 토큰을 URL에 실어야 해 쓰지 않았고, 테스트가 실제 홈에 쓰지 않도록 생성과 설치를 나눴다. GROK_HOME 추가 계정은 TODO `bridgeCommand`가 운영(node+dist)·개발(tsx) 명령을 만든다 |
| `scripts/hook-event-bridge.ts` | 관찰 훅 command 브리지 | Claude SessionStart와 Codex 훅의 stdin JSON을 인자 URL로 POST한다(`WEB_AGENT_MANAGER_HOOK_TOKEN`·`WEB_AGENT_MANAGER_CHAT_ID`). 채팅 ID가 없는 세션(조회 PTY·직접 띄운 CLI)에서는 아무것도 하지 않고, SessionStart 출력이 모델 문맥에 들어가지 않게 stdout을 쓰지 않으며 실패해도 항상 0으로 끝낸다 |
| `src/server/services/agent-hook-events.ts` | `AgentHookEventService`, `HookObserver`, `parseHookChatId`, `HOOK_CHAT_ID_HEADER` | Claude·Codex·Grok 관찰 훅(#90·#91·#92·#96)을 받아(Grok은 턴 ID를 camelCase `promptId`로만 보내 `turn_id`로 정규화하고, `StopCancelled`도 턴 종료로 본다) 헤더 채팅 ID가 그 공급자 채팅일 때만 처리한다. `SessionStart`의 session_id가 채팅 값과 다르면(Codex 첫 세션, Claude /clear·세션 전환) `bindSession`이 채팅을 새 ID로 옮기고 history_file을 비워 기존 동기화가 새 JSONL을 session ID로 붙이게 하며, 다른 채팅이 쓰는 ID면 옮기지 않는다. 옛 JSONL은 바뀌지 않으면 스캔이 건너뛰므로 `HistorySynchronizer.forgetHistoryFile`로 잊게 해 다음 전체 스캔에서 숨김 외부 채팅으로 보존한다(운영 검증에서 이 처리 없이 옛 대화가 어느 채팅에도 등록되지 않은 것을 발견). `applyBusy`가 UserPromptSubmit이면 작업중, Stop·StopFailure면 해제를 바로 반영하며(#93) Codex async 훅은 최신 `turn_id`의 종료일 때만 내린다. 이어서 `HistorySynchronizer.syncActive`를 같은 틱 1회로 합쳐 HTTP 응답 뒤 요청해 메시지·완료 알림은 기존 동기화가 맡는다(JSONL 판정·2초 폴링·Codex claim/FIFO는 fallback). `StopFailure(rate_limit)`는 `rateLimitDelayMs`(5초, 화면 감지가 먼저 등록할 시간) 뒤 `onRateLimit`→`SessionManager.registerRateLimitWaitFromHook`을 불러, 대기가 아직 없을 때만 기존 `registerRateLimitWait`로 등록·알린다(#94 — 화면 감지 보완, error_details에서 리셋 시각을 못 읽으면 resume_after를 비워 사용량 회복 확인에 맡김). 선택 인자는 옵션 객체(`AgentHookEventOptions`)로 받는다. `HookObserver`는 폴링이 찾은 턴 종료마다 그 채팅의 Stop 훅이 15초 유예 안에 왔는지 대조해 matched/missed/unobserved(훅이 온 적 없는 채팅)를 세고 누락은 `turn_end_without_stop_hook` 경고로 남기며, 관리자 `GET /api/admin/hook-stats`로 본다. missed가 사실상 0이고 훅 없는 옛 세션이 모두 재시작되면 폴링 완화·claim/FIFO 삭제를 진행한다(TODO) |
| `src/server/services/usage-monitor.ts` | `UsageMonitor`, `mergeCodexResetCredits`, `detectUsageRegression`, `reconcileStaleClaudeSessionWindow`, `isUsageDetailsDue` | 계정별 direct 사용량 수집을 60초마다 실행하고 실패한 주기에만 임시 PTY로 `usageCommands`를 실행한 뒤 종료한다. direct와 PTY 결과 모두 같은 stale 회귀 방어·DB 저장·초기화 알림·keepalive 계약을 거친다. Codex app-server 결과의 초기화권은 매분 함께 저장하고 개수·가장 이른 기한 변화 시 알리며, direct 실패 시 무거운 `/usage` 상세 폴백은 최대 1시간에 한 번만 연다. 모델 옵션도 공급자 direct 수집기를 우선하고 실패 때만 TUI 메뉴를 읽어 캐시한다. `snapshot`은 토큰이 제거된 direct 구조화 원본 또는 PTY 화면을 메모리에만 보관한다. |
| `src/server/providers/claude.ts`, `src/server/services/usage-monitor.ts` | `claudeUsageFallbackCode`, `isClaudeUsageSnapshotComplete`, `isClaudeUsageFallbackError`, `claudeUsageBackoffMs`, `detectUsageRegression` | Claude 2.1.251의 live와 seeded headers·seeded persisted·refresh failure·loading·usage 상세 endpoint 제한을 숫자 파싱보다 먼저 구분한다. fallback·stale·unavailable 숫자는 저장·초기화 알림·keepalive에 채택하지 않고 마지막 직접 확인값을 보존한다. `Current session`이 퍼센트만 있고 Resets가 없으면 주간 창을 5시간 대표값으로 쓰지 않되, 0%·Resets 없음은 keepalive 재료로 남기고 저장된 세션 리셋이 지났으면 0%·5시간 뒤로 보정한다. fallback이 아닌 이 관측은 fresh가 아니어도 고정 턴을 보낸다. 과거 `shouldAdoptRejectedUsage`의 5회 강제 채택은 같은 fallback 1%가 반복돼 오염을 확정하므로 제거했으며, 감소 fresh 5회에는 PTY를 재생성해 reset 시각이 전진한 새 창인지 독립 확인한다. `decideFreshUsageAdoption`은 같은 창에서 0이 아닌 감소만 즉시 거절하고, 같은 날짜 0%는 2회 관측 뒤 PTY 재시작 후 재조회가 0%면 확정한다. 상세 조회 fallback은 사용자 quota 초과와 다른 상태 코드로 저장하고 자동 폴링을 2·4·8·최대 15분으로 백오프한다. |
| `src/server/services/usage-monitor.ts` | `isUsageDetailsDue`, `waitForUsageDetails`, `mergeCodexResetCredits`, `redeemResetCredit` | 사용량과 Codex 초기화권은 app-server direct 응답을 60초마다 함께 반영하고, 초기화권 개수·가장 이른 기한 변화는 외부 알림과 실시간 이벤트로 보낸다. direct 실패 시 기본 TUI 사용량은 즉시 폴백하되 무거운 Full reset 상세 메뉴는 최대 1시간에 한 번만 열고, 새 응답이 개수만 주면 같은 개수의 직전 정상 기한을 보존한다. 초기화권 사용은 저장 잔여량·계정별 동시 요청을 검증한 뒤 공식 consume 결과와 갱신 잔여량을 DB·실시간 화면에 반영한다. |
| `src/server/services/usage-monitor.ts`, `src/shared/usage-keepalive.ts`, `src/client/features/overview/Overview.tsx` | `detectUsageKeepaliveTrigger`, `pinnedCodexKeepaliveTrigger`, `evaluateCodexKeepaliveConfirmation`, `isSameUsageKeepaliveWindow`, `isUsageKeepaliveDue`, `reconcileKeepaliveAfterUsage`, `maybeSendUsageKeepalive`, `sendUsageKeepalivePrompt`, `runCodexKeepaliveExec`, `parseCodexKeepaliveExecOutput`, `usageKeepalivePrompt`, `keepaliveReasonLabel` | Claude 세션 창 없음·0% 또는 Codex 창의 0%를 정상 사용량 조회에서 감지해 유지 턴을 보낸다. Codex 5시간 고정은 계정 인증을 유지한 빈 임시 cwd의 격리 `codex exec --json`(read-only sandbox)로 보낸다. 대화형 TUI는 스레드만 열고 모델 턴을 제출하지 못해 exec로 되돌렸다. 확인 실패마다 요구 길이를 1500에서 최대 10회 만에 10000자까지 늘린다. 10회를 다 쓰면 현재 창을 기록하고 Slack·ntfy·FCM·브라우저로 세션 유지 실패 알림을 보낸다. 5시간 창이 이미 1% 이상이면 현재 창을 성공으로 남기고 더 보내지 않는다. 완결된 `agent_message`, 최소 글자 수, 명시적 `turn.completed`, 정상 프로세스 종료를 모두 확인해야 사후 사용량 검증으로 넘어간다. 임시 cwd는 항상 삭제하되 숨김 세션 JSONL은 감사용으로 보존하며, 과거 모든 Codex keepalive 문구 기록도 일반 채팅에서 숨긴다. Claude·Grok은 기존 `hi`와 별도 임시 PTY를 유지한다. Codex는 모델 응답만으로 DB 성공 기록을 남기지 않고 10초 뒤 사용량 조회를 당긴다. 대상 창 하나라도 정수 `usedPercent >= 1`이면 즉시 성공이며, 모두 0%이면 완료된 턴에 한해 75초 이상 떨어진 두 관측의 reset 문자열이 정확히 같을 때만 반올림된 성공으로 보조 판정한다. reset이 이동하거나 10분 안에 확인되지 않으면 성공 시각을 보존하지 않고 긴 문구로 재시도한다. 공급자·계정별 마지막 확인 성공과 창 키로 중복을 막으며, Codex의 여러 창이 동시에 0%여도 계정당 작업 하나만 둔다. 감지 1분 뒤 조회 PTY를 갈아타되 `keepaliveNotBeforeAt`을 보존해 60초 사용량 폴링의 busy 구간과 타이머가 겹쳐도 작업을 잃거나 새 1분을 반복하지 않는다(#75). |
| `src/server/services/usage-reset-notifier.ts` | `UsageResetNotifier`, `parseUsageResetMoment`, `resetEventKey` | Claude 5시간(`session`)·전체 모델 주간, Codex 5시간·주간, Grok 주간 창의 절대 초기화 시각을 복원하고 `(provider, account_id, window_id)`별 마지막 정상 에피소드를 DB에 유지한다. 예정 시각 타이머가 직접 알리던 방식은 낡은 예약으로 실제 전환 전 오발송했고, 0%에서 다음 리셋이 매분 이동하면 15분마다 새 창으로 오판해 반복 발송했다(#75). 이제 다음 리셋의 충분한 전진과 직전 양수 사용률, 사용률 하락 또는 예정 경계 통과가 정상 스냅샷에서 함께 확인돼야 원자적으로 새 에피소드를 선점하고 알린다. Claude `session`은 초기화 뒤 첫 채팅 전까지 창이 안 보이는 실제 동작을 반영해, 이전 양수 예약 경계 뒤 누락도 0% 관측과 같은 초기화로 한 번 선점한다. 직전 사용률이 이미 0%면 누락이나 리셋 시각 이동이 반복돼도 같은 빈 창으로 갱신하며, 허용 오차 밖 후퇴는 #52·#56의 옛 스냅샷으로 무시한다. 알림기는 공급자 조건문 대신 각 어댑터의 창 라벨 계약만 읽고, 모든 공급자에 동일한 `${공급자} ${5시간|주간} 사용량 초기화` 제목과 본문을 만들어 외부 채널과 브라우저에 전달한다. |
| `src/server/services/rate-limit-resume.ts` | `RateLimitResumeService`, `parseResetTime`, `parseClockTime`, `zonedTimeToUtc` | `rate_limit_waits`를 60초마다 확인해 예정 시각 도달 시 그 채팅이 실제로 쓰는 계정(`chats.account_id`)의 최신 `usage_status`로 실제 회복 여부를 재검증하고, 예고가 부정확하면 다음 리셋 시각으로 대기를 연장. 회복 뒤에는 DB가 running이고 busy가 아니며 SessionManager가 실제 관리 중인 터미널에만 "계속"을 보내고 전송 성공 때만 리셋 알림을 발송하며, 종료·오류·터미널 유실 채팅은 대기만 정리해 자동 시작하지 않음. `parseResetTime`은 타임존 유무·12/24시간·요일 없는 전체 날짜 형식을 실제 UTC 시각으로 환산. 시:분 추출은 `parseClockTime`이 맡아 `usage-utils`와 공유하며, 분 없는 정각은 am/pm이 붙은 경우만 인정하고 날짜가 붙은 문구는 예전처럼 콜론 있는 시:분만 읽어 CLI가 명시한 날짜를 무시하지 않는다. 날짜 판별은 월 이름으로만 한다 — 예전 `[A-Za-z]{3}\s+\d` 판별은 "resets 3am"의 "ets 3"을 날짜로 오인해 정각 리셋을 null로 만들었다(#94). 일부 날짜 붙은 형식(`Sep 12, 3:30am`, `15:00 on 17 Sep`)의 날짜를 무시하는 문제는 #97로 남아 있다 |
| `src/server/services/idle-chat-reaper.ts` | `IdleChatReaper` | 유휴 채팅 터미널 자동 종료. 10분 주기로 `status='running'`이면서 작업 중·리밋 대기·승인 대기가 아닌 채팅만 골라 기준 시간(DB 정책, 기본 24시간) 초과 시 종료하고 감사 로그를 남김. 마지막 활동 시각은 `chats.updated_at`과 기록 파일 mtime 중 최신값을 쓰며 SQLite CURRENT_TIMESTAMP 형식도 UTC로 정확히 해석 |
| `src/server/services/terminal-screen.ts` | `TerminalScreen` | 용도별 크기·scrollback으로 ANSI TUI를 headless 화면에 재구성하고 전체 기록·현재 viewport 텍스트와 새 xterm용 화면·커서 초기 프레임 제공, 웹 터미널과 동일한 행 수로 리사이즈 |
| `src/server/services/tmux-input.ts` | `pastePromptToTmux`, `sendTmuxEnter`, `sendTmuxEscape`, `sendTmuxBackspace`, `sendTmuxShiftTab`, `sendTmuxText`, `scrollTmuxHistory`, `isTmuxCopyMode`, `exitTmuxCopyMode`, `resizeTmuxWindow` | 웹 채팅 프롬프트를 tmux `paste-buffer -p`의 bracketed paste로 붙여 넣어 TUI의 빠른 키 burst 오인을 막고 실제 Enter 키 이벤트로 제출, Escape·Backspace·Shift+Tab(Claude Code의 기본·auto-accept edits·plan mode 순환)과 짧은 리터럴 키 입력도 tmux send-keys로 전달(attach된 클라이언트에 raw 제어 바이트를 직접 쓰면 이스케이프 시퀀스 파서에 걸려 실제로 반영되지 않을 수 있음). 웹 터미널의 휠·스와이프는 `copy-mode -e` 진입 후 `scroll-up`/`scroll-down`으로 tmux 기록을 옮기고(웹은 tmux attach 클라이언트라 xterm 자체 스크롤백이 늘 비어 있음), 기록 보기 여부 확인과 취소도 함께 제공. tmux 창의 256열·동적 행 크기도 적용 |
| `src/server/services/system-metrics.ts` | `SystemMetricsService`, `linkProcessesToChats` | 전체 CPU·메모리 5초, shell을 띄우는 네트워크 30초, `ps`와 전체 `/proc/PID/stat`을 읽는 프로세스·tmux PID 연결 60초, 디스크 60초로 비용별 캐시 주기를 분리한 호스트·채팅 상태 지표 수집과 중첩 실행 차단. Codex·Claude·Grok·Node·tmux 프로세스를 표시하며 tmux pane pid와 자식 프로세스를 실제 채팅·프로젝트에 연결한다. 프로세스는 tmux pane 자손으로 채팅을 전파한 뒤 남은 것 중 서버 자신·자손과 tmux 데몬을 시스템으로 분류해 채팅/시스템/기타 묶음 정보를 함께 내보냄(`classifySystemProcesses`, `processGroup`). 개발 모드의 `tsx watch`처럼 서버를 띄운 조상도 표시 대상 이름인 동안만 따라 올라가 시스템에 포함 |
| `src/server/services/realtime.ts` | `isSameOrigin`, `RealtimeHub`(`close`·`sweepHeartbeat` 포함) | Origin 호스트와 직접 TLS·명시적 외부 URL·`X-Forwarded-Proto` 기반 reverse proxy 프로토콜을 검증한 인증 WebSocket, 관리자 전용 채팅·CLI 인증 터미널 구독과 raw 입력·기록 스크롤(`terminal_scroll`)·세로 행 동기화(`terminal_resize`) 중계. 25초 ping/55초 timeout으로 모바일 좀비 연결을 정리하고, 1초 권한 검사에서 session/role/scope/device 변경과 절대·idle 만료를 강제하며 실제 메시지 활동은 idle 시간 절반·최대 5분 간격으로 DB에 반영한다. `close`는 종료 시 열린 소켓과 업그레이드 서버를 끊는다. `broadcast`의 `skipTemporary`는 일회용 임시 세션에 승인 payload가 새지 않게 한다 |
| `src/server/services/github-projects.ts` | `GithubProjectService`, `githubRepositoryName` | `gh api`로 로그인 사용자와 소속 조직을 조회하고 소유자별 구조화 저장소 목록을 중복 제거해 프로젝트 origin 연결 상태와 결합, 기존 프로젝트 재활성화, `소유자/저장소` 기본 경로의 허용 경계 clone. `createLocal`은 검증한 워크스페이스 바로 아래에 새 폴더와 main Git 저장소를 만들며, 새 프로젝트·기존 폴더 모두 `gh repo create --source --remote origin`으로 선택한 공개 범위의 원격을 연결 |
| `src/server/services/prompt-scheduler.ts` | `PromptScheduler`, `localScheduleStamp`, `isScheduleDue` | `run_date`가 있으면 그 현지 날짜의 시각(서버가 꺼져 놓쳤다면 이후 첫 확인)에 한 번만 실행하고 선점 UPDATE에서 함께 끄며, 지난 날짜·시각의 1회 예약은 저장·재활성화를 거부하고 `runNow`도 1회 예약을 소진·해제한다(#99). IANA 시간대의 현지 날짜·시각으로 매일 일정을 판정하고 실행일을 SQLite에서 원자 선점해 중복을 막는다. 매 실행마다 공급자·계정에 맞는 새 채팅을 생성하거나 지정 기존 채팅을 resume/queue해 시스템 프롬프트를 보내고 최근 상태·오류·채팅을 기록. 서버 시작의 누락 일정은 같은 현지 날짜 안에서 한 번 보충 실행 |
| `src/server/services/git-workspaces.ts` | `GitWorkspaceService`, `assertGitBranch`, `parseGitWorktrees` | 공유 checkout과 채팅별 앱 관리·외부 worktree의 브랜치·경로 영속화, 브랜치 목록·dirty 상태 조회, 종료 채팅의 기존/새 브랜치 전환, 실제 Git 등록 경로만 외부 연결, tmux 현재 경로 기반 자동 연결. 앱 관리 worktree만 깨끗할 때 제거하며 외부 worktree는 DB 연결만 해제. Diff 화면의 작업공간 선택을 위해 `listWorkspaces`로 고를 수 있는 목록을 주고, `resolveWorktreePath`가 요청 경로를 실제 worktree 목록과 대조해 검증(불일치는 거부). 전용 worktree를 만들면 `PROJECT_INSTRUCTION_FILES` 중 원본에만 있는 로컬 지침을 복사(이미 체크아웃된 파일은 유지, 복사 실패는 경고만). worktree 폴더는 브랜치 슬러그 기준이라 같은 브랜치 채팅들이 공유하고, 마지막 사용 채팅이 떠날 때만 폴더를 제거. `removeWorktree`는 실행 중 채팅·미커밋 변경을 확인한 뒤 force에서만 삭제하고 연결 채팅을 분리 |
| `src/server/services/cli-auth.ts` | `CliAuthManager`, `authSessionKey` | Codex device auth·Claude 계정 로그인·GitHub web auth 공식 CLI를 관리자 전용 PTY로 실행. Codex·Claude는 계정 슬롯마다 따로 로그인하며(설정 디렉터리 환경변수를 실어 그 계정 폴더에만 인증이 저장됨) 터미널 구독·입력 식별자는 `authSessionKey`가 만드는 `claude:3` 형태의 키를 쓴다(GitHub은 계정 구분이 없어 공급자 이름 그대로). 기본 계정 로그인 PTY는 설치 홈으로 `HOME`을 바꾸지 않고 채팅·상태 확인과 같은 프로세스 `HOME`을 쓴다 — 서버가 root로 뜰 때 두 홈이 다르면 로그인은 성공해도 상태 명령이 다른 디렉터리를 보고 계속 미인증으로 남는다. 인증 상태 명령은 서버 시작 시 한 번 캐시하고 로그인 PTY 종료 뒤 해당 대상만 재검사하며 제한된 출력 스냅샷을 중계. Grok(`grok login`)도 같은 방식으로 지원하되, 로그인하지 않아도 상태 명령이 정상 종료해 종료 코드만 보면 항상 "로그인됨"이 되므로 `unauthenticatedPattern`이 있는 공급자만 `commandOutput`으로 출력을 읽어 미인증 문구로 판정한다(출력은 판정에만 쓰고 밖으로 내보내지 않음) |
| `src/server/services/agent-accounts.ts` | `AgentAccountService`, `CONFIG_DIR_ENV`, `toAccountSlug`, `defaultCodexHome` | 공급자 인증 계정 슬롯의 등록·조회·삭제와 계정별 CLI 설정 디렉터리·실행 환경변수 계산. 계정 분리는 인증 파일 복사가 아니라 두 CLI가 공식 지원하는 환경변수(`CLAUDE_CONFIG_DIR`·`CODEX_HOME`)로 하며, 기본 Claude·Grok은 환경변수를 주입하지 않아 프로세스 홈의 기존 인증을 그대로 쓴다. 기본 Codex만 설치 계정 홈의 `~/.codex`를 `CODEX_HOME`으로 가리킨다 — 로그인 PTY가 그 경로에 인증을 저장하는데 프로세스 홈이 다르면 상태 확인·채팅이 미인증으로 남기 때문이다. 추가 계정 폴더는 데이터 디렉터리 아래(`agent-accounts/<공급자>/<슬러그>`, 0700)에만 만들고 삭제도 그 경로 안일 때만 허용하며(기본 계정·사용 중 계정은 거부), 인증 파일 내용은 어떤 경우에도 읽지 않는다. `monitorTargets`·`usageScope`·`setUsageScope`가 사용량 조회 범위(기본 계정만 / 전 계정) 설정을 담당 |
| `src/server/routes/agent-account-routes.ts` | `createAgentAccountRouter` | 계정 슬롯 CRUD(관리자 전용, 삭제는 되돌릴 수 없어 `requireTrustedNetwork` 적용), 계정별 로그인 상태·채팅 수를 합친 목록 조회, 사용량 조회 범위 변경(변경 즉시 `UsageMonitor.applyScopeChange`로 PTY 재구성), 채팅의 인증 계정 변경(`PUT /chats/:id/account`) |
| `src/server/services/notifier.ts` | `Notifier`, `NotificationHub` | 알림 채널 공통 계약과, 등록된 채널 전부에 동시에 보내는 허브(한 채널 실패가 다른 채널에 영향 없음) — 각 서비스는 이 허브 하나만 주입받으면 됨 |
| `src/server/services/slack.ts` | `SlackNotifier` | bot token 기반 중복 방지 Slack 알림, DB에 저장된 관리자 설정을 우선 쓰고 없으면 환경변수로 대체 |
| `src/server/services/ntfy.ts` | `NtfyNotifier` | ntfy(topic 기반 푸시) JSON 발행 API로 중복 방지 알림, DB에 저장된 topic·서버 URL을 우선 쓰고 없으면 환경변수로 대체, 이벤트 유형별 제목·이모지 태그·우선순위 매핑 |
| `src/server/routes/auth-routes.ts` | `createAuthRouter` | 입력 길이 검증, SQLite에 SHA-256 bucket으로 유지하는 IP 전체·계정 전체 로그인 제한과 상한 있는 실패 감사 로그, 로그아웃, 현재 사용자·최근 세션 목록(100개 상한)·다른 세션 폐기·비밀번호 변경 후 전체 폐기, 비밀번호+선택 MFA 중요 작업 재인증, 마지막 세션·웹 사용자별 채팅/터미널 화면 모드 저장 API. 사용자가 없을 때만 신뢰 네트워크에서 트랜잭션으로 첫 관리자 생성 |
| `src/server/core/mfa.ts` | `MfaService`, `totpCode` | RFC 6238 SHA-1/30초 TOTP, AES-256-GCM seed 저장과 소유자 전용 master key 기동 검증, 10분 등록 확인, 계정당 5분 로그인 challenge, time-step replay 차단과 SHA-256 일회용 복구 코드 10개를 제공 |
| `src/server/core/deployment-security.ts` | `diagnoseDeploymentSecurity`, `assertDeploymentSecurity` | PUBLIC_URL origin·HTTP/HTTPS·local/public host·Secure cookie/HSTS 일치·wildcard·trusted proxy를 순수 판정하고 production 외부 평문 HTTP는 fail-fast, 사설망/명시 우회와 proxy 누락은 구조화 warning으로 반환 |
| `src/server/routes/project-routes.ts` | `createProjectRouter`, `visibleChats` | `GET /chats?scope=active`는 활성 프로젝트의 user 채팅 중 starting·running·resuming·stopping·busy·리밋 대기인 것을 프로젝트 이름과 함께 준다(#100, 전체 프로젝트 실행 중 목록). 프로젝트·채팅 조회와 생명주기, GitHub 저장소 목록·clone 프로젝트 생성, 로컬 프로젝트의 GitHub 저장소 생성, JSONL 메시지 페이지네이션, 공유 프로젝트에 보존하고 전용 worktree에도 복제하는 첨부 업로드, 백업 API. 새 채팅에 공유 브랜치를 기록하고 삭제 전 앱 관리 worktree를 정리하며 프로젝트 등록 시 중앙 스킬 링크 자동 설치. `/chats/worktree`는 `worktreePath`가 오면 `attachWorktree`로 이미 있는 폴더에 새 채팅을 붙이고, 없으면 기존대로 브랜치 기준 `switchBranch`로 앱 관리 worktree를 만든다(앱 밖에서 만든 worktree는 앱 관리 경로에 없어 브랜치만으로는 "이미 체크아웃된 브랜치"로 실패했음) |
| `src/server/routes/prompt-schedule-routes.ts` | `createPromptScheduleRouter` | 관리자 전용 매일 프롬프트 일정 목록·생성·전체 수정·활성 전환·즉시 실행·삭제 API |
| `src/server/routes/cli-auth-routes.ts` | `createCliAuthRouter` | 관리자 전용 CLI 인증 상태 조회와 공급자별 로그인 PTY 시작·중단, 감사 로그 기록 |
| `src/server/routes/file-routes.ts` | `createFileRouter`, `previewKind`, `parseAttachmentPath`, `searchFiles` | 요청별 내부망 정책을 적용한 파일 목록·업로드·다운로드·형식별 미리보기와 텍스트 파일 편집 저장(`previewKind`가 text·markdown이고 미리보기 한도 안인 파일만). 목록 symlink 제외, 새 `.web-agent-manager-uploads`와 기존 `.myagent-uploads`의 프로젝트·채팅 소유권을 검증하는 전용 첨부 API, 매직 바이트·CSP·nosniff·no-store와 비활성 ZIP 다운로드 포함. `GET /projects/:id/files/search`(`searchFiles`)는 채팅 입력창의 "@파일" 멘션 자동완성용으로 프로젝트 트리를 이름 부분일치로 재귀 탐색하되 node_modules 등 무거운 디렉터리는 스킵하고 결과·스캔 수 상한을 둔다. 폴더도 이름이 일치하면 `{ path, directory: true }`로 결과에 포함해(재귀는 계속) 선택 시 파일 탭에서 그 폴더를 바로 열 수 있게 한다 |
| `src/server/core/instruction-files.ts` | `PROJECT_INSTRUCTION_FILES` | Claude·Codex 프로젝트 지침 파일 목록. 지침 편집 화면과 worktree 생성이 같은 목록을 참조해 한쪽에만 파일이 늘어나는 것을 막음 |
| `src/server/routes/instruction-routes.ts` | `createInstructionRouter`, `ensureClaudeImportsAgents` | 허용된 프로젝트/전역 지침 조회와 관리자 전용 편집 API, `CLAUDE.md`가 공통 `AGENTS.md`를 import하도록 보장하는 지침 통합 API |
| `src/server/routes/git-routes.ts` | `createGitRouter`, `requestWorkspacePath`, `parseStatus`, `parseLog`, `issueNumber`, `ghJson`, `listLimit`, `withHasMore` | `POST /git/discard`(관리자·신뢰 네트워크 전용)는 선택한 파일·폴더의 미커밋 변경을 되돌린다 — 선택 경로를 `git status`로 다시 나눠 추적 파일은 `git restore --source=HEAD --staged --worktree`로 복원(스테이지된 신규 파일도 HEAD에 없으므로 함께 제거됨), untracked 파일은 되돌릴 원본이 없어 `git clean -fd`로 삭제. `chatId`가 있으면 채팅 worktree에서 Git 상태·변경 파일·선택 diff·커밋 상세와 GitHub 이슈·PR·Actions 작업을 실행하고, 작업공간 상태·브랜치 전환·외부 worktree 연결 API와 관리자 전용 Git/GitHub 쓰기 API를 제공. `/git/diff`는 상태를 다시 조회해 untracked(`??`) 파일만 따로 `git diff --no-index`(exit code 1은 "차이 있음"이라 정상 처리하는 `runNoIndexDiff`)로 "전부 추가된" diff를 만들어 tracked 변경분과 합침(`git diff HEAD`는 untracked 파일을 원천적으로 못 보여줘 파일 목록엔 뜨는데 diff만 항상 비어 보였음) — 선택 파일이 전부 untracked라 tracked 목록이 우연히 비어도 `git diff --` 가 "제한 없음"으로 해석되지 않게 그 경우엔 호출 자체를 생략, 두 diff 호출 모두 `core.quotePath=false`로 비ASCII(한글) 파일명이 8진 이스케이프·따옴표로 깨지지 않게 함. `gh pr list`/`gh pr view`의 `statusCheckRollup` 필드는 PR마다 체크 상태를 추가로 조회해 응답이 느려지는데 화면 어디에도 안 써서 제외. 저장소 상태·변경 목록·GitHub 목록 조회는 `GitDataCache`를 거쳐 응답하고(`?refresh=1`이면 캐시 우회), GET이 아닌 요청이 성공하면 라우터 앞단 미들웨어가 해당 프로젝트 캐시를 버림. 이슈의 GitHub 연결 브랜치는 `gh issue view --json`이 지원하지 않아 GraphQL `linkedBranches`로 읽고, 없으면 `issueBranchName`이 `issue-<번호>-<슬러그>`를 제안. 워크플로 목록은 `/projects/:id/github/runs`로 분리했다 — `gh run list`가 셋 중 가장 느려서(WSS-Server 1.4초, 이슈 0.8초·PR 0.7초) 병렬 조회가 항상 거기 맞춰졌고, 덜 보는 Actions 때문에 탭 진입이 매번 붙잡혔다. 이슈·PR 상세 조회도 목록과 같은 캐시를 거친다(`gh` 한 번이 최소 0.5초라 같은 항목을 다시 열 때마다 그 비용을 냈다) — 쓰기 뒤에는 앞단 미들웨어의 프로젝트 캐시 무효화가 갱신을 보장한다. 커밋·이슈·PR·워크플로 목록 개수는 쿼리(`commits`/`issues`/`pulls`/`runs`)로 받아 화면의 "더 보기"만큼 늘리고(`listLimit`이 기본값 30/50/50/20과 상한 500으로 정규화), 다음 페이지 유무는 요청보다 하나 더 읽어 `withHasMore`가 잘라 내며 판단한다(`git log`·`gh list` 모두 전체 개수를 주지 않아 개수를 세는 추가 호출을 피함) — 늘어난 개수는 캐시 키에도 포함해 앞 페이지만 담긴 옛 결과가 재사용되지 않게 함 |
| `src/server/services/git-cache.ts` | `GitDataCache` | git·gh 조회 결과의 stale-while-revalidate 캐시. 첫 조회만 CLI를 기다리고 이후에는 저장값을 즉시 반환하며 60초가 지나면 응답을 막지 않고 뒤에서 갱신, 갱신 실패 시 마지막 성공값 유지, 프로젝트 단위 무효화와 새로고침용 강제 조회 지원. 첫 조회가 끝나기 전에 같은 키로 들어온 요청은 자리표시 엔트리의 빈 값을 받지 않고 진행 중인 조회에 합류해 결과를 기다린다(예전에는 `{cachedAt}`만 담긴 응답이 나가 GitHub 화면이 통째로 깨졌음) |
| `src/server/routes/tool-routes.ts` | `createToolRouter`, `listClaudeMcp`, `listCliMcp`, `parseCliMcpOutput`, `listTomlMcp`, `writeClaudeMcp`, `writeTomlMcp` | Claude/Codex/Grok commands·skills·marketplace·MCP 카탈로그 API와 관리자 전용 MCP 추가·수정·삭제·토글 API. `claude/codex mcp list` CLI 조회는 비동기 `execFile`로 실행하고 provider·경로별 60초(실패 시 10초) 캐시로 이벤트 루프 블로킹과 반복 실행을 방지. Claude는 프로젝트 `.mcp.json`의 `mcpServers`와 `claude mcp list`가 보고하는 연결 상태를 함께 보여주고, 공식 설정에 없는 enabled 토글은 거부. Codex와 Grok은 사용자 설정 `config.toml`의 `[mcp_servers.<이름>]` 블록 형식이 같아(HTTP 헤더 키만 Codex `http_headers`·Grok `headers`로 달라 쓰기는 `headerKey`로 나누고 읽기는 둘 다 받는다). 섹션 이름은 `parseMcpSectionName`이 서브테이블 접미사를 떼어 판정한다 — 따옴표 없는 이름에서 `[mcp_servers.<이름>.env]`의 `.env`까지 이름으로 삼키면 유령 서버가 목록에 오르는데, 우리 자동 연동이 쓰는 `grok mcp add -e`가 정확히 그 형식을 남긴다(실측). CLI가 긴 명령의 `args`를 여러 줄 배열로 쓰는 것도 이어 읽는다 — 한 줄만 읽으면 `args`가 빈 배열이 되고, 그 상태로 도구 화면에서 저장·토글하면 블록을 다시 쓰면서 실행 인자가 통째로 사라진다. 알려진 한계: 이 파서는 줄 단위라 배열 안에 주석이 섞이거나 배열이 닫히지 않은 손편집 파일은 정확히 읽지 못한다(`codex`·`grok` CLI가 쓰는 형식에는 없어 실사용 경로는 영향 없음) 읽기·쓰기(`readTomlMcpServers`·`writeTomlMcp`·`deleteTomlMcp`·`toggleTomlMcp`)를 공급자 인자로 공유하고, 각각 `~/.codex`·`~/.grok`과 프로젝트 `.codex`·`.grok` 설정 및 `codex/grok mcp list` 상태를 함께 읽으며 `enabled=false` 토글을 지원. env/header 값은 목록에 노출하지 않고 키 이름만 반환하며, 빈 입력으로 저장하면 기존 비밀 필드를 보존 |
| `src/server/routes/agent-integration-routes.ts` | `createAgentIntegrationRouter` | 관리자 전용 Codex·Claude CLI/전역 스킬/web-agent-manager MCP 시작 시점 상태 조회와 명시적 설치 API, 설치 감사 로그 |
| `src/server/routes/agent-delegation-routes.ts` | `createAgentDelegationRouter` | 관리자 전용 프로젝트 위임 목록과 새 자식 세션 생성 API. 대상 채팅의 실제 상태를 위임 기록과 함께 반환하고 생성은 `AgentBridge`를 통해 감사 로그로 기록 |
| `src/server/routes/operations-routes.ts` | `createOperationsRouter` | 승인, 공급자 메타·사용량·자원 조회, 관리자 전용 Codex 초기화권 사용·Slack·ntfy 테스트·설정, 서버 라우터 생성 시 한 번만 실행하고 결과를 재사용하는 CLI 버전 API, 관리자 전용 프로세스 종료(SIGTERM/SIGKILL, 자기 자신·pid 1 보호). 승인 목록은 요청 대상 채팅과 최신 활성 delegation의 source chat을 함께 반환해 부모 채팅에서 자식 요청을 전달 표시한다. 초기화권은 요청한 계정 ID를 검증하고 사용 결과·잔여량과 함께 감사 로그에 기록한다. 임시 로그인 세션은 승인 결정을 403으로 막고 목록도 빈 배열로 돌려준다(403이 아닌 이유는 클라이언트가 여러 조회를 한 번에 묶어 부르기 때문). |
| `src/server/routes/operations-routes.ts`, `src/server/services/provider-update-ledger.ts`, `src/server/services/provider-cli-backup.ts` | `POST /providers/:provider/update`, `POST /providers/:provider/updates/:runId/rollback`, `ProviderUpdateLedger`, `ProviderCliBackupService` | 최근 동일 버전의 10단계 passed canary만 고정 CLI update를 허용한다. 설치 전에 공급자 전체 session/task/profile 불변성, CLI binary와 credential 제외 설정 hash backup을 고정하고 설치 버전 일치 뒤에만 터미널을 재시작한다. update/rollback 상태와 제한된 evidence는 append-only event 원장에 남고 서버 중단 update는 재실행 없이 rollback_required가 된다. 관리자 원클릭 rollback은 모든 backup hash를 먼저 확인해 binary/config를 원자 복원하고 이전 버전·불변성을 재확인하며, 별도 멱등 키로 설치·복원·재시작 중복을 막는다. |
| `src/server/services/provider-canary.ts`, `src/server/services/provider-rollout.ts` | `createConfiguredProviderCanaryRunner`, `ProviderRolloutService` | 소유자 전용 서버 고정 harness와 dedicated credential seed를 run별 임시 HOME에 연결하며 부모 환경·원문 출력은 보존하지 않는다. 동일 candidate CLI의 version/hash를 고정해 quota 내 신규 채팅만 후보 command로 시작하고 기존·재개·quota 밖 채팅은 현재 CLI를 유지한다. 후보 chat 오류는 영속 관측해 승격을 차단하며 quota 완료·오류 0일 때만 전체 update를 허용하고 중단 시 다음 시작부터 현재 CLI로 되돌린다. |
| `src/server/routes/token-usage-routes.ts` | `createTokenUsageRouter` | 인증된 대시보드의 `/token-usage` 기간·그룹 쿼리를 허용 목록으로 검증하고 영구 원장의 전체·분류별 합계를 반환 |
| `src/server/routes/helpers.ts` | 경로 유틸, `writeFileAtomic` | 프로젝트 경계와 symlink 실제 경로 검증, 외부망에서만 적용하는 민감 경로·점 경로 차단 정책(내부망 요청은 `.env`·`.git` 등 민감 경로까지 일반 파일처럼 허용), 임시 파일 rename 교체로 원본 권한을 유지하는 공용 원자적 쓰기(지침 편집과 파일 편집이 함께 사용) |
| `src/server/services/agent-bridge.ts` | `AgentBridge` | `0600` Unix 소켓의 줄 단위 JSON API. 프로젝트·채팅 목록, 번호 기반 제한 문맥 조회, 7일 스냅샷, 멱등 작업 전달·상태, 공급자별 새 자식 생성, 실제 전달 문구(`history_prompt`) 기준 장문 첨부 위임 완료 대기·assistant 결과 영속화를 제공하고 모든 쓰기를 `SessionManager`와 감사 로그로 통과시킴. 실험 fixture 조회·생성 응답에서는 `groundTruth`를 빼 피험 에이전트가 정답을 읽지 못하게 한다. 위임 대상 공급자는 이름을 하드코딩하지 않고 `isSupportedProvider`가 등록된 어댑터 목록으로 판정한다(공급자를 늘려도 브리지 수정이 필요 없고, 오류 메시지도 실제 지원 목록을 그대로 보여줌) |
| `src/server/services/agent-skill-installer.ts` | `installProjectAgentSkills`, `installGlobalAgentSkills`, `globalAgentSkillsInstalled` | 등록 프로젝트와 공급자 사용자 전역 경로(`~/.codex/skills`·`~/.claude/skills`·`~/.grok/skills`)에 중앙 스킬 symlink를 설치하고 기존 항목은 보존. Grok은 `~/.claude/skills`도 호환으로 읽지만 Claude 설정 유무에 연동이 좌우되지 않도록 네이티브 경로에 설치·확인한다. 이 저장소의 이름 변경 전 원본을 정확히 가리키는 옛 symlink만 정리하고 현재 원본 연결 상태 판정 |
| `src/server/services/agent-integration.ts` | `AgentIntegrationManager`, `isAgentIntegrationProvider`, `mcpCheckArgs`, `mcpRemoveArgs`, `mcpAddArgs` | 서버 시작 시 저장되지 않은 Codex·Claude·Grok 연동만 공식 명령으로 한 번 판정하고 성공 상태를 DB에 영속화해 이후 시작·API 조회의 CLI 실행을 생략. 연결 버튼에서는 먼저 실제 상태를 재확인해 이미 정상이면 제거·재등록 없이 성공 저장하고, 필요할 때만 공식 `mcp add/remove`로 설치. 공급자마다 MCP 하위 명령 표면이 달라 argv를 `mcpCheckArgs`·`mcpRemoveArgs`·`mcpAddArgs`에 모아 두는데, 실측 기준 Codex는 `mcp get --json`·scope 인자 없음·`--env`, Claude는 `mcp get`·`--scope user`·`-e`, Grok은 **`mcp get`이 아예 없어** `mcp list --json`으로 확인한다 — 이 명령은 서버가 하나도 없어도 종료 코드 0이고, 출력 전체를 부분 문자열로 훑으면 남의 서버 실행 경로에 같은 이름이 들어 있을 때 오탐하므로 `grokMcpInstalled`가 JSON 항목의 `name` 일치만 인정한다. 설정 파일이나 비밀값을 읽지 않고 production/개발 브리지 진입점을 선택 |
| `src/client/lib/websocket-connection-manager.ts` | `createWebSocketConnectionManager` | 클라이언트 WebSocket 수명의 단일 상태기계. 각 연결에 generation을 부여해 옛 open/message/close와 지연된 인증 후 reconnect를 무효화하고, intentional close를 socket별 WeakSet으로 구분한다. `CONNECTING`이 15초를 넘으면 close 이벤트를 기다리지 않고 새 generation을 만들며 reconnect timer·dispose도 같은 소유권 경계를 따른다 |
| `src/client/main.tsx` | `App`, `createSocketWatchdog`, `createForegroundResume`, `navigationFromLocation`, `writeNavigation`, `selectChat`, `selectTab`, `refetchChats`, `rememberLastSession`, `changeChatViewMode`, `openProject`, `openProjectFile`, `openChatById`, `linkFileLineToChat` | 최상위 상태와 인증·WebSocket·세션 백업 조정. 공통 API 401 이벤트를 받으면 계정 메모리 상태를 지우고 즉시 로그인 화면으로 전환하며, WebSocket이 예기치 않게 끊기면 `/auth/me`로 인증 만료와 일시 장애를 구분한다. `createSocketWatchdog`은 서버 하트비트가 70초 넘게 끊기면 좀비 소켓으로 보고 기존 복귀 경로(`createForegroundResume.resume`)로 갈아끼운다. Android 복귀는 readyState 판정을 하지 않고 공통 connection manager에 generation 교체를 요청하며, 1초 중복 guard로 native/visibility 연속 훅만 합쳐 터미널이 새 소켓에 재구독되게 한다. 모바일 하단은 자주 쓰는 대시보드·채팅·파일·GitHub와 더보기 5개를 고정하고 보조 탭은 history 연동 더보기 시트에서 연다. 탭·프로젝트·채팅·파일 위치를 URL history에 기록·복원하고, 채팅별 작성 중 텍스트를 App 메모리에 보존해 내부 탭·채팅 왕복 때 복원하되 DB/localStorage에는 남기지 않고 전송·로그아웃 때 제거한다. 웹 사용자별 채팅/터미널 화면 모드를 낙관적으로 저장하며, 생성·재활성화한 GitHub 프로젝트를 최신 목록에서 선택해 채팅으로 이동하고 이전 비동기 응답의 선택 덮어쓰기를 차단. `openChatById`는 메시지 속 `chat:번호` 링크·파일 미리보기 줄 링크·실행 중 패널로 프로젝트가 달라도 그 채팅으로 이동하며 `selectChat`처럼 마지막 세션을 저장하고(#102, `loadCore`는 실제 목표 채팅이 있을 때만 그 프로젝트로 맞춰 옛 `last_project_id`로 되돌리지 않음), `openProjectFile`은 파일 탭의 특정 줄까지 이동하며, `linkFileLineToChat`은 선택한 줄 참조를 채팅 입력창에 채운다 |
| `src/client/api.ts` | `api`, `ApiError`, `AUTHENTICATION_REQUIRED_EVENT`, `setCsrfToken`, `getCsrfToken`, `uploadFile` | CSRF 토큰 보관과 인증 JSON API 호출 공통 유틸. JSON·업로드 응답의 401만 공통 인증 필요 이벤트로 전달하고 status가 보존된 오류를 던진다. `uploadFile`은 `fetch`에 없는 업로드 진행률(`XMLHttpRequest.upload.onprogress`)이 필요한 파일 첨부 전용 |
| `src/client/lib/logger.ts` | `initClientLogging`, `createLogger` | 클라이언트 상세 로깅. console 티로 기존 콘솔 로그와 전역 오류(error·unhandledrejection)를 배치(5초/30건)로 `/api/logs/client`에 전송, 숨김/이탈 시 keepalive flush, 로그인 전(CSRF 없음)에는 전송 보류 |
| `src/client/types.ts` | `Tab`, `Json`, `ApprovalAction`, `ChatActivity`, `ChatScrollState` | 클라이언트 전용 공통 타입 |
| `src/client/components/Login.tsx` | `Login` | 사용자 0명 상태 확인, 첫 관리자 생성과 일반·일회용 로그인, MFA 계정의 비밀번호 뒤 TOTP/복구 코드 두 번째 단계와 지연 중 중복 제출 차단 |
| `src/client/components/ProjectDialog.tsx` | `ProjectDialog` | `document.body` portal 팝업에서 워크스페이스 기반 새 Git 프로젝트 생성, 기존 로컬 절대 경로 등록, GitHub 저장소 clone을 세 탭으로 제공. 새 프로젝트·기존 폴더의 선택적 GitHub 저장소 생성·origin 연결과 Private/Public/Internal 선택 포함 |
| `src/client/features/schedules/SchedulesView.tsx` | `SchedulesView` | 새 채팅/기존 채팅 일일 입력 일정 편집 폼과 등록 일정 상태, 수정·일시정지·즉시 실행·최근 채팅·삭제 관리 UI |
| `src/client/components/GithubRepositoryList.tsx` | `GithubRepositoryList` | 개인·조직 소유자 필터와 저장소 검색·새로고침, 연결 프로젝트 채팅 이동, 미연결 저장소의 clone 경로 확인 팝업과 프로젝트 생성 |
| `src/client/components/GitBranchControl.tsx` | `GitBranchControl` | 선택 채팅의 현재 브랜치·공유/전용 상태를 작은 메뉴로 표시하고, 팝오버에서 종료 채팅의 기존·새 브랜치 전환과 발견된 미연결 외부 worktree 연결을 채팅·GitHub 화면에 공통 제공. 채팅 터미널 안에서 AI가 이 앱의 API를 거치지 않고 직접 브랜치를 바꿔도(`git checkout -b` 등) 따라잡도록 20초 주기로 다시 조회하되, 메뉴를 펼쳐 고르는 중이거나 전환 요청 중이면 건너뛴다 |
| `src/client/components/CliAuthPanel.tsx` | `CliAuthPanel` | 서버가 캐시한 세 CLI 설치·인증·PTY 실행 상태를 최초 진입과 로그인 종료 이벤트에 확인하고 미인증 CLI 발견 시 `document.body` portal의 관리자 인증 팝업 자동 표시 |
| `src/client/components/LoadingState.tsx` | `LoadingState` | 도구·GitHub 등 최초 서버 응답을 기다리는 영역의 공용 회전 스피너와 접근성 상태 문구 |
| `src/client/components/AgentIntegrationNotice.tsx` | `AgentIntegrationNotice` | 관리자 화면 진입 시 서버가 시작 때 한 번 확인해 둔 상태를 읽고, 설치됐지만 준비되지 않은 Codex·Claude의 전역 스킬+MCP 연결 버튼 제공 |
| `src/client/components/SubagentManager.tsx` | `SubagentManager`, `delegationActivity` | 관리자용 서브 에이전트 작업 서랍. 프로젝트 작업 수·실행 상태 요약, 접이식 새 작업 위임 폼, 상태 중심 대상 목록과 채팅 열기·응답 중단·터미널 종료·재시작 제공. 대상 채팅의 pending 승인은 `권한 요청` 상태와 실제 `ApprovalCard`로 함께 보여 그 자리에서 결정한다 |
| `src/client/features/chat/ChatView.tsx`, `src/client/lib/token-usage.ts` | `ChatView`, `MessageCard`, `detectMention`, `drillIntoFolder`, `compactTokenCount`, `tokenUsageLabel` | 채팅 목록 맨 위 `renderActiveChatsGroup`이 App이 `/chats?scope=active`로 400ms 묶어 불러온 전체 프로젝트 실행 중 채팅을 프로젝트별 소제목 아래 한 줄 행(상태 점·제목·제공자, 확인 필요 상태만 칩, 접힘 시 헤더에 확인 개수)으로 전역 승인 기준 상태와 함께 보여주고 `onOpenChat`으로 프로젝트 전환 이동한다(#100). 입력창에서 캐럿 기준 `@`(현재 프로젝트 파일·폴더, `GET /projects/:id/files/search`)·`#`(전체 프로젝트 채팅) 멘션 트리거를 감지해(`detectMention`) 자동완성 드롭다운을 띄우고 선택 시 `[라벨](경로)`/`[라벨](chat:번호)` 마크다운 링크를 삽입. 파일 검색 결과는 폴더도 포함해 "파일"/"폴더" 배지로 구분하고, 폴더 행을 누르면(또는 Tab) `drillIntoFolder`가 멘션 토큰을 `@폴더경로/`로 치환해 그 폴더 하위만 계속 검색하는 셸 자동완성식 탐색을 지원하며(메뉴는 유지), 행의 "+" 버튼이나 Enter는 폴더 자체를 링크로 선택한다. 입력 원문은 App의 채팅별 초안을 사용해 WAM 내부 탭·채팅 전환에서 유지하고, 선택 전환 시에는 첨부 미리보기·오류·열린 메뉴만 초기화한다. 공급자 메타 기반 새 채팅 버튼·라벨·대표 사용량 표시·모드 전환 지원 여부, 웹 사용자별 채팅/전체 PTY 모드 선택, 채팅 목록의 현재 브랜치와 채팅 상단 브랜치/worktree 제어, 모바일 메뉴·모델/사용량/추론 강도 선택, 채팅 이름 변경, 모바일·노트북 상태바 요약(접힌 상태에서도 `사용량 N% · 초기화 …`를 남기고 초기화 시각만 390px에서 숨김), 응답별 JSONL 토큰 사용량을 K·M 단위 작은 보조 문구로 표시, wheel·touch·pointer 입력 시 하단 정렬을 즉시 중단하는 자동 추적/과거 위치 보존 가상 스크롤, 관리자 서브 에이전트 패널, 입력 길이 자동 조절 컴포저와 첨부(서버와 공유하는 제한을 넘는 파일은 전송 전 사전 차단하고 `uploadFile` 진행률을 퍼센트로 표시), 1,000자를 넘는 일반 텍스트 붙여넣기의 UTF-8 `.txt` 자동 업로드, 도구·diff 상세·인라인 승인. 터미널 모드에서도 상태·사용량·승인과 세션 시작/종료 제어를 유지한다. 작업 중에도 후속 질문·명령 전송과 현재 응답 중지를 동시에 제공하고 중지 복구 질문은 큐 입력으로 덮지 않으며, 데스크톱 권한 요청 패널은 현재 프로젝트에 pending 요청이 있을 때만 렌더링. 채팅 목록을 작업공간별(`프로젝트 채팅`/`<브랜치> 워크트리`)로 묶어 접기·개수 표시와 묶음별 공급자 시작 버튼을 제공하고, 삭제 응답의 worktree 정리 결과를 알림으로 전달. 묶음은 채팅의 `worktree_path`뿐 아니라 `/projects/:id/git/workspaces`(30초 주기 재조회)의 실제 worktree 목록으로도 만들어, 채팅이 0개인 worktree도 빈 묶음으로 남긴다(공유 checkout은 `프로젝트 채팅`과 중복이라 제외). 빈 묶음에서 채팅을 시작할 때는 브랜치와 함께 `worktreePath`를 보내 새 폴더 생성 대신 그 폴더에 연결한다 |
| `src/client/features/terminal/TerminalPanel.tsx` | `TerminalPanel`, `terminalRowsForHeight`, `modifiedKey` | 선택 채팅의 tmux PTY를 xterm 화면과 WebSocket으로 연결하고 출력 리스너 등록 후 초기 화면을 구독한다. xterm 자체 `.terminal`과 호스트 클래스 충돌을 피하고, 모바일 터치를 잘린 고정 화면·tmux 기록 이동 또는 TUI mouse-wheel 입력으로 변환하면서 바깥 문서 스크롤을 차단한다. 데스크톱(701px 이상)은 13px 글자·정상 줄 간격·256열을 유지하면서 실제 셀 높이로 패널에 들어갈 행 수를 계산해 xterm과 서버에 `terminal_resize`로 함께 적용한다. 클라이언트는 스크롤백을 쌓지 않아(`scrollback: 0`, 서버 TerminalScreen과 같은 계약) 휠은 페이지·상자 스크롤 대신 `terminal_scroll`로 tmux 기록을 옮긴다. TUI가 mouse mode를 켠 동안에는 가로채지 않고 xterm의 mouse 프로토콜 입력으로 넘긴다. 화면 키보드 호출, Ctrl/Alt 일회성 modifier와 Esc·Tab·Shift+Tab·방향키·PageUp/Down·Enter·Ctrl+C/D 보조 키 바 제공 |
| `src/client/features/terminal/AuthTerminal.tsx` | `AuthTerminal` | 공급자 로그인 PTY를 xterm으로 표시하고 출력 조각에서 최근 인증 URL을 찾아 새 탭 링크 제공 |
| `src/client/features/overview/Overview.tsx` | `Overview`, `TokenUsageAnalytics`, `ResetCreditsSummary`, `redeemResetCredit` | 공급자 메타 기반 사용량 라벨, 호스트 자원·런타임·프로세스 대시보드와 프로세스 종료 UI. 영구 토큰 원장을 기간·프로젝트·채팅·일자·공급자·계정·모델별로 집계하고 Codex 초기화권, 개별 사용량 새로고침, direct 수집기 재시작과 비밀정보 없는 조회 원본을 제공한다. 변경형 환경 설정은 별도 설정 화면으로 분리한다. |
| `src/client/features/settings/SettingsView.tsx` | `SettingsView`, `SessionSecurityCard`, `MfaSettingsCard`, `BrowserNotificationCard`, `SlackSettingsCard`, `NtfySettingsCard`, `IdleChatSettingsCard`, `OneTimeLoginCard` | 현재·다른 로그인 세션 목록과 다른 세션 폐기, 비밀번호 변경·전체 로그아웃, TOTP 등록·복구 코드 1회 표시·MFA 해제, 브라우저 알림 권한, CLI 계정·인증 진입, Slack·ntfy 연결과 테스트, 유휴 채팅 자동 종료 정책, 일회용 로그인 코드 발급을 모은 관리자·사용자 설정 화면. 임시 로그인에는 보안 변경 폼을 노출하지 않는다. |
| `src/client/features/files/FilesView.tsx` | `FilesView`, `PreviewContent`, `LineNumberedPreview`, `LineChatLinkMenu`, `deleteEntry`, `fileDate`, `isEditable` | 목록 행과 미리보기 헤더 모두에 삭제 버튼(`deleteEntry`, `DELETE /projects/:id/files`, 관리자·신뢰 네트워크 전용)을 제공해 파일·폴더(하위 내용 포함)를 되돌릴 수 없다는 확인 뒤 지운다. 이름·크기·수정 시각·다운로드 열의 조밀한 프로젝트 파일 목록과 앱 스타일 파일 선택·업로드, 행 전체 단일 클릭과 키보드 폴더 진입·파일 선택, 채팅에서 전달된 경로 드러내기와 폴더·미리보기 탐색 이력 통지. 데스크톱은 선택 시 분할 미리보기, 모바일은 즉시 보이는 전체 오버레이를 사용하며 헤더 다운로드 버튼으로 현재 worktree 원본을 바로 저장한다. Markdown·sandbox HTML·이미지·영상·오디오·PDF는 각 형식대로 렌더링하고, 일반 텍스트는 `LineNumberedPreview`가 줄 단위 grid로 그려 채팅 메시지의 줄 링크(`#L줄` 프래그먼트)로 들어오면 그 줄로 스크롤·강조하며, 줄마다 호버 버튼으로 `LineChatLinkMenu`(전체 프로젝트 채팅 검색 + 공급자별 새 채팅 버튼, `onLinkFileLine` 콜백)를 띄워 그 줄을 채팅에 바로 연결할 수 있다. ZIP·EPUB은 압축파일 안내만 표시. 텍스트·Markdown이면서 잘리지 않은 파일은 연필 버튼으로 편집기를 열어 저장하며, 저장 후 목록 크기·수정 시각을 갱신하고 편집 중 다른 파일·폴더로 이동하거나 미리보기를 닫을 때는 변경 삭제 여부를 확인 |
| `src/client/features/instructions/InstructionsView.tsx` | `InstructionsView` | 프로젝트·전역 지침 파일 편집, `CLAUDE.md`에 `AGENTS.md` import를 추가하는 공통 지침 버튼 |
| `src/client/features/git/GitView.tsx` | `GitView`, `GitHubTab`, `IssueDetail`, `PullRequestDetail`, `LazyDiffSection`, `chatPath`, `chatBody`, `withQuery`, `loadGit`, `loadWorkspaceMeta`, `currentBranchFromStatus`, `isStackedLayout`, `scrollToDetail`, `revealCommitDetail`, `splitDiffSections`, `diffPreamble`, `toggleGroup`, `discardSelected` | 작업공간·커밋 내역 드롭다운의 기본 옵션은 "선택한 채팅 기준"·"현재 작업공간 브랜치" 같은 서술형 대신 실제 브랜치명을 보여준다 — 커밋 내역 쪽은 지금 보고 있는 대상(다른 worktree·브랜치를 골랐을 수 있음) 기준이라 이미 60초 주기로 새로고침되는 `git.status`에서 뽑고(`currentBranchFromStatus`), 작업공간 쪽 "선택한 채팅 기준으로" 옵션은 그 자체가 항상 기본(채팅) worktree를 가리키므로 별도로 유지하는 `loadWorkspaceMeta`(같은 60초 주기로 갱신)의 결과를 쓴다. 변경 파일 목록은 바로 위 폴더 하나로 묶어(`changeGroups`) 폴더 헤더 체크박스로 그 안 파일을 한 번에 선택할 수 있고(`toggleGroup`, 일부만 선택된 상태는 인디터미네이트로 표시), "선택 파일 롤백"(`discardSelected`, `POST /git/discard`)으로 선택한 파일·폴더의 미커밋 변경을 되돌린다(확인 후 실행, 커밋과 같이 다른 작업공간·브랜치를 보는 중에는 비활성화). 저장소 프로젝트 생성과 로컬·깃허브·저장소 탭, 선택 채팅 기준 미커밋 변경 파일·작업공간별 조회, 최근 커밋 상세 diff·커밋·push·PR 통합/분할 diff·이슈·PR·Actions 관리. 대형 PR은 펼친 파일의 줄 DOM만 지연 렌더링하고 GitHub 로딩·인증 필요·빈 상태를 구분한다. 프로젝트가 선택돼 있으면 로컬 탭으로 열고, 로컬·깃허브 탭에서 화면이 보이는 동안 60초 주기와 화면 복귀 시점에 상태를 다시 읽으며 헤더에 캐시 기준 시각을 표시한다(새로고침은 `refresh=1`로 캐시 우회) — 이 60초 polling effect의 의존성 배열에 `workspacePath`·`commitRef`가 빠져 있으면 작업공간 선택을 바꿔도 setInterval 콜백이 선택 전 시점의 `load()`를 계속 참조해, 1분 뒤 예전 작업공간 데이터로 되돌아가 방금 고른 작업공간 선택이 "안 먹는" 것처럼 보였다(실사용 보고로 발견·수정). 커밋과 이슈·PR·워크플로 목록은 각 목록 끝의 "더 보기"로 한 묶음(커밋 30, 이슈·PR 50, 워크플로 20)씩 늘려 이어 불러오며, 서버가 `hasMoreCommits`·`hasMore`로 다음 페이지가 있다고 알릴 때만 버튼을 보여준다. 조회는 `loadGit`(로컬)과 `loadGithub`(GitHub)으로 나뉘어 커밋 더 보기가 GitHub 목록까지 다시 읽지 않고, GitHub 개수만 늘어난 경우에는 결과를 비우지 않아 보던 목록이 사라졌다 나타나지 않는다. 프로젝트·채팅·작업공간·조회 브랜치가 바뀌면 렌더 단계에서 늘려 둔 개수를 기본값으로 되돌려 새 대상을 옛 개수로 한 번 더 조회하지 않는다. 좁은 화면에서 항목을 고르면 접이식 목록을 접고 상세로 이동한다(목록이 길수록 상세가 아래로 밀리던 문제) — 커밋은 `revealCommitDetail`이 700px 기준, GitHub 이슈·PR은 `github-list-toggle`과 함께 1100px 기준으로 동작하며 이동 자체는 공용 `scrollToDetail`이 맡는다. 목록을 접지 않고 스크롤만 하면 상세가 문서 끝이라 화면 하단에 걸친다. 접기와 이동은 두 단계로 나눠, 접힘이 DOM에 반영된 뒤 `useLayoutEffect`에서 이동한다(같은 프레임에 하면 목표 위치가 접히기 전 기준으로 잡혀 상세 중간에서 멈춘다). 커밋은 본문·diff를 나중에 받아 높이가 늘어나므로 로딩이 끝난 뒤 옮긴다. 넓은 화면은 2열이라 상세가 이미 옆에 있어 건드리지 않는다 |
| `src/client/features/git/GitView.tsx` | 로컬 diff 선택 effect | 아무 파일도 선택하지 않으면 diff 요청·본문을 비우고 안내만 표시하며, 명시적으로 선택한 여러 파일만 반복 `file` 쿼리로 조회한다. 선택 변경 뒤 늦게 도착한 이전 응답은 폐기하고 조회 실패는 화면에 원인을 표시 |
| `src/client/features/tools/ToolsView.tsx` | `ToolsView`, `McpEditor`, `parseJsonField` | 공급자별 commands·skills·marketplace·MCP 카탈로그와 MCP 추가·수정·삭제·Codex 활성화 토글. 최초 응답 전 로딩과 성공 후 빈 필터 결과를 구분하고 재조회 중 기존 결과를 유지하며, CLI 상태 전용 MCP는 read-only로 표시하고 기존 env/header 값은 노출하지 않음 |
| `src/client/lib/approvals.ts` | `approvalSummary`, `approvalActions`, `chatActivity` | 승인 요청 요약(Claude PermissionRequest 훅의 tool_name/tool_input에서 실제 명령·경로 추출)·버튼 라벨·리밋 대기 포함 채팅 활동 상태 계산 |
| `src/client/lib/schedules.ts` | `scheduleTimingLabel` | 예약 화면에서 실행 시점 문구("매일 HH:MM" / "YYYY-MM-DD HH:MM 1회")를 일관되게 만든다. |
| `src/client/lib/format.ts` | `usageWindows`, `usageResetCredits`, `bytes`, `usageErrorLabel`, `isUsageFallbackNotice` | 사용량 창·Codex 초기화권 세부 JSON을 안전하게 파싱하고 바이트 단위를 표시한다. Claude usage 상세 fallback을 사용자 quota 제한과 구분해 캐시 출처 안내로 바꾸고 마지막 직접 확인값을 보존한다. |
| `src/client/lib/notifications.ts` | `notificationPermission`, `requestNotificationPermission`, `showNotification` | 브라우저 Notification API 권한 조회·요청, 탭이 보이는 중엔 팝업을 띄우지 않고 조용히 무시 |
| `src/client/lib/dialog-history.ts` | `useDialogHistory` | 중첩 관리 팝업을 브라우저 history에 순서대로 쌓고 뒤로가기·명시적 닫기 후속 동작을 일관되게 처리 |
| `src/client/message-display.ts` | `splitMessageContent`, `mergeMessages`, `reconcileOptimisticMessages` | 일반 응답과 접힌 도구·diff 상세 분리, 페이지 병합·중복 제거, 작업 중 큐 입력은 실제 동일 user 기록이 새로 확인된 항목만 하나씩 낙관적 말풍선에서 확정 |
| `src/client/lib/attachments.tsx` | `isImagePath`, `attachmentUrl`, `AttachmentImage`, `CopyableCodeBlock`, `MessageBody`, `urlTransform` | 코드 블록은 `CopyableCodeBlock`이 오른쪽 위 복사 버튼(`copyText` 재사용, 끝 줄바꿈 제외, 1.5초 `복사됨`)을 붙여 그린다(#101). AI 응답의 프로젝트 상대·절대 `[첨부: 경로]`와 Markdown 이미지를 현재 채팅 작업공간의 파일 API로 정규화하고, 일반 프로젝트 링크는 파일 탭으로 연결하며(href의 `#L줄` 프래그먼트가 있으면 `onOpenProjectFile`에 줄 번호도 함께 전달) `chat:번호` pseudo-scheme 링크는 `onOpenChat`으로 프로젝트가 달라도 그 채팅으로 이동시킨다. react-markdown 기본 `defaultUrlTransform`은 허용 목록에 없는 URI scheme의 href를 지우므로, `urlTransform`에서 `chat:` 패턴만 예외로 통과시킨다. `.web-agent-manager-uploads` 첨부는 전용 소유권 검증 API로 inline/다운로드하며 이미지 실패는 고정 크기 안내로 가상 스크롤 변동 방지 |
| `src/client/lib/file-links.ts` | `projectFilePathFromHref`, `projectFileContentUrl`, `projectFileDownloadUrl` | 프로젝트·현재 worktree 경계 안의 절대/상대 링크를 프로젝트 상대경로로 정규화하고, 경로 세그먼트와 채팅 작업공간을 반영한 inline 콘텐츠·attachment 다운로드 URL 생성 |
| `src/client/lib/diff-parse.ts` | `parseDiffFiles`, `toSplitRows`, `hiddenLineCount`, `statusLabel` | 통합 diff를 파일→hunk→줄 구조로 파싱. `diff --git`·`index`·`new file mode`·`--- /dev/null` 같은 원문 헤더를 상태(added·deleted·renamed·modified·binary)·경로·증감 통계로 흡수하고, 따옴표·공백이 든 경로를 복원하며 이전·이후 줄 번호를 각각 매기고 분할 보기용 좌우 정렬과 hunk 사이 감춰진 줄 수 계산을 제공 |
| `src/client/lib/diff-view.tsx` | `DiffView`, `DiffFileCard`, `DiffModeToggle`, `looksLikeDiff` | 파싱 구조를 GitHub 화면 기준으로 렌더링. 상태 배지·경로·+/− 통계가 있는 접이식 파일 카드, hunk 사이 `N줄 펼치기`(서버 blob 조회로 원본 줄을 위/아래로 삽입), 통합·분할 보기, Shiki 문법 색상. 채팅 상세처럼 어두운 표면에서는 CSS로 색만 뒤집어 같은 구조를 재사용. 파일 경로는 디렉터리(축약 가능)와 파일명(고정)으로 나눠 좁은 화면에서도 파일명이 잘리지 않게 표시 |
| `src/client/lib/diff-highlight.ts` | `diffLanguage`, `diffPath`, `highlightDiffLines` | 파일 경로·git/Codex 패치 헤더의 언어 판정과 Shiki JavaScript 엔진 기반 밝은·어두운 GitHub 문법 토큰 생성. 코어·테마는 최초 diff에서 한 번, 언어 문법은 실제 사용 시 한 번만 동적 로드하며 미지원·바이너리 형식은 일반 텍스트로 대체 |
| `src/client/styles.css` | 반응형 디자인 시스템 | 네이비·바이올렛 색상 토큰, 공통 카드·버튼·입력·포커스 상태, 아이콘 탭 내비게이션과 모바일 프로젝트 바, 데스크톱 관리 화면·모바일 채팅 중심 레이아웃과 채팅/전체 터미널 모드 선택(터미널 모드는 `main-chat-terminal`로 페이지 위아래 여백을 줄여 세로 공간을 터미널에 넘기고, 채팅 레이아웃 높이는 실제로 위아래에 쌓이는 188px을 뺀 값이라 문서 자체에 스크롤이 생기지 않는다). 모바일 터미널의 터치 전용 overflow 경계·보조 키 바와 520px 이하 높이 압축 레이아웃을 포함한다. 브레이크포인트는 폭 기준 1100px·700px에 더해 노트북용 `(min-width: 701px) and (max-height: 900px)`를 두는데, 노트북은 폭이 아니라 세로가 병목이라 높이 기준으로 잡고 헤더·프로젝트 바·main 여백·workspace-head·모드 탭·컴포저를 함께 압축하며 채팅 사이드바를 240px로 줄인다. 이때 model-bar는 모바일과 같은 `.model-bar-summary` 요약 한 줄 + 펼치기를 재사용하므로(마크업·상태는 공용) 시각 스타일은 미디어쿼리 밖에 두고 표시 여부만 각 구간에서 바꾼다. 또한 작업 상태 배지·인라인 승인 카드·에이전트 연동 알림·서브 에이전트 관리 서랍·모바일 파일 미리보기 오버레이·설정 폼·세션 백업 목록을 같은 시각 체계로 구성 |
| `scripts/audit-chat-bindings.ts` | 전체 채팅 연결 감사 CLI | 모든 채팅의 provider/account/session ID/history 파일·workspace·tmux 프로세스·생성 시각·중복 소유·빈 예약 상태를 읽기 전용으로 대조해 JSON 보고서를 출력한다. `npm run audit:chats`로 실행한다. |
| `scripts/create-admin.ts` | 관리자 CLI | 환경변수로 관리자 생성/갱신 |
| `scripts/claude-permission-bridge.ts` | hook bridge | Claude stdin 요청을 내부 승인 API로 전달 |
| `scripts/web-agent-manager-agent.ts` | 로컬 브리지 CLI·MCP | Unix 소켓 요청 CLI와 프로젝트·채팅·문맥·스냅샷·비동기 전달, 새 자식 생성 후 완료 대기, 기존 전달 결과 대기 도구를 제공하는 줄 단위 stdio MCP 서버 |
| `scripts/myagent-agent.ts` | 기존 MCP 경로 호환 wrapper | 이름 변경 전 등록된 production MCP 명령이 새 `web-agent-manager-agent` 브리지로 계속 진입하도록 연결 |
| `scripts/install-agent-integrations.ts` | 공급자 연동 설치 진입점 | 설치된 Codex·Claude CLI를 감지해 기존 사용자 파일을 덮어쓰지 않고 전역 스킬 링크와 사용자 범위 `web-agent-manager` MCP를 설치·보정 |
| `electron/main.cjs`, `electron/preload.cjs`, `electron/setup.html` | Electron 데스크톱 셸 | sandbox 창과 제한 preload, 외부 이동 차단, 임의 localhost 포트 사용. 셸은 asar로 묶고 외부 Node 백엔드만 unpack해 최초 관리자 생성·연동 수행 |
| `android/app/src/main/java/com/webagentmanager/android/*` | `MainActivity`, `FloatingSettingsController`, `AppTrustManager`, `AuthenticatedApiClient`, `PushRegistration`, `UsageWidgetProvider` | 동일-origin WebView·시스템 inset, 인증 쿠키를 유지한 공용 Downloads 저장(Android 9 이하 권한 보류·재개 포함), 설정 톱니 드래그/가장자리 저장/축소 탭 복원, Android Keystore P-256 기기 등록·주소 전환 시 일회성 서명 로그인·기존 세션 승격·해제, 공통 쿠키/CSRF 네이티브 API, 선택적 FCM과 반응형 위젯 제공. 명시적 로그아웃은 origin별 자동 로그인 시도 기록으로 유지하고 HTTPS 인증서 검증은 유지하며 HTTP 주소는 사설망으로 제한. 파일 선택기 대기 중 시스템이 메모리 회수로 프로세스를 재생성하면 `fileChooserCallback`이 유실되는데, 이때 Logcat 경고와 재시도 안내 Toast를 띄우고 `onSaveInstanceState`/`webView.restoreState()`로 원래 보던 화면을 복원 |
| `android/app/src/main/res/layout/widget_*.xml`, `android/app/src/main/res/xml/usage_widget_info.xml` | Android 위젯 리소스 | 네이비·바이올렛 디자인의 모델 사용량/CPU/RAM 카드, 런처 크기 조절 계약과 30분 갱신 주기 정의 |
| `android/build.gradle`, `android/app/build.gradle`, `android/gradle/wrapper/*` | Android 빌드 | JDK 17·AGP 9.3·Gradle 9.5·Android API 36 프로젝트. Firebase Messaging SDK는 컴파일하되 `google-services.json`이 있을 때만 Google Services 플러그인을 적용해 인증 설정 없는 기본 APK도 빌드 가능 |
| `scripts/package-release.ts` | `createArchive`, `createSbom`, `createChecksums`, `main` | production 빌드·중앙 스킬·lockfile·플랫폼 진입점·Docker 구성과 전체 검증 스크린샷을 지원 플랫폼 ZIP으로 묶고 CycloneDX SBOM·SHA-256 목록 생성 |
| `scripts/create-release-checksums.mjs` | `writeChecksums` | 릴리즈 디렉터리의 일반 파일을 이름순으로 정렬해 SHA-256 무결성 목록 생성 |
| `scripts/check-public-build.mjs` | `checkPublicBuild` | 프로덕션 `dist`의 텍스트 산출물에 빌드 머신 절대 경로가 남으면 빌드를 실패 처리 |
| `scripts/run-server-supervised.sh` | `log`, `health_ok`, `stop_child`, `terminate` | systemd·cron·도커 재시작 정책을 쓸 수 없는 환경에서 tmux 안의 프로덕션 서버를 감시. 스크립트 위치와 PATH의 Node가 보고하는 실제 `process.execPath`를 이식 가능한 기본값으로 사용해 Volta shim 대신 실제 서버 PID를 추적하고 환경변수 override를 허용하며, 프로세스 생존과 `/health`를 함께 보고 종료·헬스 연속 실패 시 정상 종료 후 강제 종료로 정리해 재시작 |
| `packaging/*` | 간단 설치·관리자 생성·실행 진입점 | Linux `setup.sh`, macOS `setup.command`, Windows `setup-windows.cmd` 단일 진입점과 단계별 설치·관리자 생성·실행 스크립트. Windows는 WSL2로 동일 Linux 런타임 사용 |
| `.github/workflows/ci.yml`, `.github/dependabot.yml` | PR 검증·의존성 갱신 | 전체 npm 감사·타입·테스트·빌드, 자체 Vite 서버의 Chrome Playwright, 공식 wrapper 검증과 Android API 36 debug/release/lint를 병렬 강제하고 npm·Action·Docker 업데이트를 매주 제안 |
| `.github/workflows/release-desktop.yml` | Electron 교차 플랫폼 빌드 | 고정 SHA Action으로 사전 검증 후 Linux·Windows와 네이티브 Intel·Apple Silicon runner별 macOS 설치 파일, 간단 설치 ZIP을 GitHub 자산 이름으로 정규화·중복 검사하고 공개 저장소의 태그 Release에 SBOM·전체 체크섬과 함께 첨부. private 소스는 같은 패키지 버전을 사용하되 태그·Release를 만들지 않으며, Release와 중복되는 Actions 중간 아티팩트는 1일 보존. Android 서명 APK는 자동 자산 대상에서 제외 |
| `Dockerfile`, `docker-compose.yml`, `docker/entrypoint.sh` | Docker 배포 | 전용 builder 경로에서 공개 산출물을 검사하고 Node.js 22·tmux·Git·gh·고정 버전 Codex·Claude와 production 앱을 비루트 UID 10001로 실행하며 데이터·프로젝트·CLI 홈 볼륨을 영속화 |
| `.github/workflows/docker-image.yml` | GHCR 다중 아키텍처 이미지 | `main`·태그·수동 실행에서 linux/amd64·linux/arm64 이미지를 공개 `web-agent-manager-app` 패키지에 provenance·SBOM 증명과 함께 게시. 저장 공간을 제한하기 위해 Buildx 기록 아티팩트를 끄고 최종 이미지 레이어 중심 `mode=min` Actions 캐시 사용 |
| `skills/web-agent-manager-session-context/SKILL.md` | 세션 조회 스킬 | “채팅 160 참고” 같은 번호·프로젝트 요청을 구조화 문맥 조회로 변환하고 MCP가 없으면 CLI 사용 |
| `skills/web-agent-manager-delegate/SKILL.md` | 작업 전달 스킬 | 원본 문맥을 확인하고 대상 채팅 또는 새 반대 공급자 자식 채팅에 멱등 전달한 뒤 완료 결과를 회수·검토. 명시적 위임과 이점이 분명한 복잡한 교차검증에만 제한적으로 사용 |
| `scripts/check-usage-tui.ts` | 실제 TUI 검증 | Codex·Claude 사용량 화면 통합 테스트 |
| `scripts/check-session-lifecycle.ts` | 실제 resume 검증 | 새 세션 저장·종료·동일 ID resume 테스트 |
| `scripts/check-browser-resources.ts` | 브라우저 리소스 검증 | 주요 화면 120회 전환 전후 Chromium GC 기준 JS heap·DOM 노드·이벤트 리스너 누적 상한 검사 |
| `vite.config.ts` | `parseAllowedHosts`, `VITE_POLLING_INTERVAL_MS`·Vite 개발 서버 설정 | 환경변수의 추가 허용 Host 목록을 정규화하고 React 플러그인·클라이언트 루트·빌드 출력을 구성. Docker HMR 폴링은 1초로 제한해 상위 tsx watch의 300ms 설정이 프런트엔드 전체 감시에 전파되는 CPU 부하를 완화 |
| `.env.example` | 환경변수 예시 | 포트·데이터 경로·공개 URL·알림 토큰·선택적 FCM 활성화와 함께 내부망 정책 변수(`TRUSTED_NETWORKS`, `TRUSTED_PROXIES`)의 기본 동작·오설정 위험을 주석으로 안내 |
| `LICENSE`, `SECURITY.md`, `CONTRIBUTING.md`, `CHANGELOG.md` | 공개 운영 문서 | MIT 이용 조건, 비공개 취약점 제보, 기여·검증 기준과 버전별 사용자 영향 기록 |
| `tests/login-rate-limit.test.ts`, `tests/http-security.test.ts`, `tests/realtime-origin.test.ts` | 공개 경계 회귀 테스트 | 영속 IP·계정 잠금과 재시작 유지·원문 비저장·상한, HTTP 헤더·미리보기 예외, WebSocket 호스트·프로토콜 검증 |
| `tests/realtime-heartbeat.test.ts` | 하트비트·권한 폐기 회귀 테스트 | 실제 WebSocket으로 서버 주기 `ping`·클라이언트 `pong`, 침묵한 좀비 정리와 함께 DB 세션 삭제·role 변경·신뢰 기기 해제 시 입력 미처리와 1초 이내 연결 종료를 검증 |
| `tests/socket-watchdog.test.ts` | 소켓 침묵 감시 테스트 | `main.tsx`의 `createSocketWatchdog`를 소스에서 그대로 로드해 정상 침묵 무시·한계 초과 시 되살리기·중복 교체 방지·비가시 상태 제외를 검증 |
| `tests/websocket-connection-manager.test.ts` | WebSocket 상태 전이 테스트 | CONNECTING→OPEN, 모든 readyState의 강제 교체, 15초 연결 제한, unexpected close 재연결, 지연 인증, 구세대 이벤트, socket별 intentional close, dispose 전이를 fake timer로 전수 검증 |
| `tests/e2e/socket-heartbeat.spec.ts` | 하트비트 브라우저 검증 | 실제 브라우저가 서버 `ping`에 `pong`으로 답하는지와, 가상 시계로 침묵을 만들었을 때 소켓을 새로 연결하는지 검증 |
| `tests/realtime-shutdown.test.ts` | 종료 경로 회귀 테스트 | 실제 WebSocket을 붙인 HTTP 서버가 허브 정리 없이는 닫히지 않고, `RealtimeHub.close` 뒤에는 정상적으로 닫히며 클라이언트도 끊기는지 검증 |
| `tests/release-checksums.test.ts`, `tests/vite-config.test.ts`, `tests/logger.test.ts` | 배포 설정 회귀 테스트 | 재현 가능한 체크섬, 개발 허용 Host 파싱·Vite HMR 폴링 간격과 운영·개발 로그 기본값 검증, 로그 보존 기본 3일·환경변수 조절·잘못된 값 폴백과 날짜 형식 로그만 정리하는지 검증 |
| `TODO_LIST.md` | CLI/데스크톱 앱 parity TODO | Codex CLI/App, Claude Code CLI/Desktop 대비 남은 구현 후보와 우선순위 |
| `tests/usage-monitor.test.ts` | 파서 회귀 테스트 | 실측 축약 화면과 인증 오류, Codex 초기화권 상세의 24시간 주기·TUI 파싱·기한 보존 병합, 파싱 실패 시 사용량 상세 캐시 보존 계약, 세션 리셋 시각 만료(stale) 판정 검증, 정각 표기("6pm")의 시각 파싱·불가능 판정·만료 판정과 날짜 붙은 문구의 기존 동작 유지 검증, 조회 PTY 수명 판정 검증, 자정 넘겨 곧 돌아올 새벽 리셋을 옛 스냅샷으로 오판해 0%로 덮어쓰지 않는지·진짜 오래 지난 리셋은 여전히 0%로 보정하는지 검증 |
| `tests/usage-rejection-adopt.test.ts` | 연속 거부 탈출 테스트 | 같은 창에서 값이 줄어드는 응답이 계속 올 때 4회까지는 마지막 정상값과 stale을 지키고 5회째에 최신값을 fresh로 반영하는지, 거부가 끊기면 연속 횟수가 0으로 돌아가는지 실제 `UsageMonitor`와 임시 DB로 검증 |
| `tests/usage-keepalive.test.ts` | 사용량 최소 턴 테스트 | 계정별 초기화 창 DB 중복 방지, 기존 스키마·행 승계, 새 창 즉시 전송, 실패 원복과 고정 프롬프트·Enter의 분리 입력을 검증하며, 조건 판정과 공급자별 내부 기록 숨김은 사용량·기록 파서 테스트에서 함께 검증 |
| `tests/codex-rate-limits.test.ts` | Codex 초기화권 파서·사용 테스트 | app-server 응답과 `/usage` Full reset 화면의 잔여 개수·기한 추출, 첫 available 선택, consume 멱등성 키·잔여량 재조회·응답 유실 복구, 상세 Loading 완료 판정과 잘못된 응답 격리 검증 |
| `tests/usage-reset-notifier.test.ts` | 사용량 초기화 알림 테스트 | 예정 시각만 지난 상태의 무알림과 정상 스냅샷 전환 확인 후 발송, Claude 5시간 창의 경계 전 누락 보존·경계 후 누락 즉시 발송·0% 재등장 중복 방지, Claude·Codex의 5시간·주간 독립 알림과 Grok 단일 창의 주간 문구, 0% 리셋 시각이 13:50부터 14:08까지 매분 밀려도 한 번만 발송, 옛·새 값 교차와 재시작 중복 방지, 공급자 단일 키에서 계정·창 복합 키 마이그레이션, 날짜·타임존 절대 시각 변환을 검증 |
| `tests/rate-limit-resume.test.ts` | 리셋 시각 파싱 테스트 | `parseResetTime`의 24/12시간·타임존 유무·자정 경계·이미 지난 시각의 다음 날 처리·파싱 실패 검증, 요일 없는 전체 날짜 형식("Jul 13th, 2026 12:14 AM")을 오늘/내일 추측 없이 그대로 신뢰하는지 검증 |
| `tests/idle-chat-reaper.test.ts` | 유휴 채팅 자동 종료 테스트 | 기준 시간 초과 채팅만 종료, 작업 중·리밋 대기·승인 대기 제외, 실행 중이 아닌 상태 제외, 기록 파일 mtime 우선, SQLite 시각 형식 해석, 정책 끄기, 기준 시간 검증, 일부 실패 시 나머지 계속, 시스템 주체 감사 로그 검증 |
| `tests/rate-limit-resume-tick.test.ts` | 리밋 자동 재개 테스트 | 최신 사용량 회복 재검증과 다음 시각 연장, 정상 실행 터미널 재개, 종료·실제 터미널 유실 채팅의 자동 시작·잘못된 리셋 알림 차단 검증 |
| `tests/session-manager-rate-limit-stop.test.ts` | 리밋 대기 종료 테스트 | 사용자 터미널 종료 시 `rate_limit_waits` 삭제와 종료된 채팅의 시스템 `계속` 입력 거부 검증 |
| `tests/session-prompt-submission.test.ts` | Codex·Claude 채팅 제출 확인 테스트 | 첫 Enter 성공, 입력창에 남은 초안의 Enter 1회 재시도, 새 terminal map의 실제 준비 대기, 최종 실패 시 Unicode 문자 수만큼 초안 삭제와 `busy` 원복 검증 |
| `tests/system-metrics.test.ts` | 시스템 지표·프로세스 연결 테스트 | tmux pane pid 직접 연결·자식 프로세스 전파·미연결 케이스와 최초 전체 수집 후 빠른 지표 5초·네트워크 30초·프로세스/tmux PID 및 디스크 60초 호출과 캐시 스냅샷 유지 검증 |
| `tests/slack-settings.test.ts` | Slack 설정 테스트 | 환경변수 대체, DB 저장값 우선, 빈 값 갱신 시 기존값 유지 검증 |
| `tests/ntfy-settings.test.ts` | ntfy 설정·알림 허브 테스트 | 환경변수 대체, DB 저장값 우선, 빈 값 갱신 시 기존값 유지, topic 미설정 시 전송 안 함, 이벤트 ID 중복 방지, `NotificationHub`의 다중 채널 동시 전송·한 채널 실패 격리 검증 |
| `tests/mobile.test.ts` | Android 서버 연동 테스트 | 위젯 축약 응답의 사용량·CPU·RAM 계산, FCM 기기 등록과 이벤트 중복 전송 방지, 폐기 토큰 자동 비활성화 검증 |
| `tests/mobile-device-trust.test.ts` | Android 기기 신뢰 테스트 | 실제 P-256 키로 내부망 전용 등록, 기존 외부 세션 승격과 로그인 쿠키 없는 새 origin의 기기 서명 세션 발급, origin 미결합 서명·잘못된 키·서명·challenge 재사용 거부와 기기 해제 시 capability 회수 검증 |
| `tests/process-kill.test.ts` | 운영 API 테스트 | 공급자 메타 응답, 초기화권 사용의 관리자 성공·일반 사용자 거부·서비스 오류 전달, 자기 자신·pid 1 종료 거부, 이미 종료된 프로세스 안내 검증, 시스템 묶음 프로세스 종료 거부와 채팅 묶음은 통과하는지 검증 |
| `tests/message-display.test.ts` | 메시지 표시 테스트 | 도구·diff 분리와 상세 라벨, 페이지 병합, 실제 기록 전까지 작업 중 큐의 낙관적 입력 보존 검증 |
| `tests/diff-view.test.ts` | diff 표시 테스트 | git/unified/Codex 패치 판정, 분할 변환의 삭제·추가 블록 좌우 정렬·줄 번호 계산, 파일 언어·패치 경로 판정과 밝은·어두운 문법 토큰 생성 검증 |
| `tests/codex-history.test.ts` | Codex 기록 파싱 테스트 | event_msg 중복 제거, AGENTS.md 지침 분리, 내부 권한 상승 검토 세션 숨김과 일반 언급 오탐 방지, turn_context·하단 상태줄 모델 감지, `/model` 모델·effort 파싱, 턴 종료 시각 기록, 승인 감지 오탐 방지, 경량 모델 전환·디렉터리 신뢰 확인 화면 감지·입력 매핑 회귀 검증, 요일 없는 전체 날짜까지 찍는 새 사용량 한도 문구("try again at Jul 13th, 2026 12:14 AM")도 리밋 대기로 감지하는지 검증 |
| `tests/history-cache.test.ts` | JSONL 증분 캐시 테스트 | Codex·Claude 추가 레코드만 누적해 메시지·모델·제목을 갱신하고 쓰는 중인 불완전 레코드는 전체 파싱으로 복구하는지, Grok은 `updates.jsonl`만 바뀌면 본문 캐시를 버린 뒤에야 턴 토큰을 다시 읽는지 검증 |
| `tests/terminal-screen.test.ts` | headless 화면 테스트 | ANSI 커서 이동·scrollback viewport와 새 xterm용 화면 초기화·커서 프레임 검증 |
| `tests/claude-history.test.ts`, `tests/codex-history.test.ts`, `tests/grok-history.test.ts`, `tests/token-usage-display.test.ts` | 공급자 기록·토큰 표시 테스트 | Claude assistant usage, Codex token_count, Grok `turn_completed.usage`를 공통 응답 토큰 수치로 보존하고, K·M 축약 문구가 값 있는 입력·캐시·출력·추론 항목만 표시하는지 검증. 기존 도구 결과 재분류·완료 판정·제목·모델·승인 화면 파싱 회귀도 함께 검증 |
| `tests/token-usage-ledger.test.ts` | 영구 토큰 원장 테스트 | 같은 메시지 재동기화의 중복 방지, 프로젝트·채팅 합계, 삭제 표시, 브라우저 시간대 일자 경계와 최근 기간 필터 검증 |
| `tests/experiment-repository.test.ts` | 에이전트 실험 기반 원장 테스트 | run·node·event·checkpoint 불변식에 더해 평가 subject/blind map, 호출 상태·usage, judgment 멱등성, 동일 계열 provenance, 재시작 고아 평가 종결과 preset 버전 기반을 검증 |
| `tests/rubric-evaluation.test.ts` | 블라인드 복수 evaluator 테스트 | 가짜 Codex·Claude의 독립 judgment·호출별 usage, 동일/교차 계열, 비JSON evaluator의 partial 보존과 최종 답변·tracked diff 공급자 지문 마스킹을 검증 |
| `tests/graph-harness.test.ts` | 그래프 하네스 테스트 | 조정자·복수 worker·통합 부모 관계와 독립 usage 합산, 평가-개선 반복의 최소 점수 종료·loop 이벤트·체크포인트, Runtime 예외 node 종결을 가짜 Runtime으로 검증 |
| `tests/builtin-hooks.test.ts` | 실험 내장 훅 테스트 | tracked·untracked diff 줄 통계, Git diff whitespace validate 차단과 미등록 hook ID 거부를 검증 |
| `tests/agent-preset-service.test.ts` | Winner promotion 테스트 | 완료 run snapshot·accepted verdict·서버 계산 지표를 같은 preset의 불변 v1/v2로 누적하고 활성/비활성 버전 전환·미완료 run 거부를 검증 |
| `tests/experiment-runtime.test.ts` | 런타임 예산·HookBus 계약 테스트 | 미보고 usage의 null 보존, 시간·토큰·비용 상한, observe 중첩 payload 변조 차단, transform 순서, validate/block 오류 분리, timeout 실패 정책과 등록 제한 검증 |
| `tests/experiment-foundations.test.ts` | 실험 준비 기반 테스트 | 공급자별 스킬 scope 발견·realpath 중복 제거·SHA-256 manifest와 실제 Git 저장소의 detached worktree/monorepo 하위 경로 보존 검증 |
| `tests/experiment-routes.test.ts` | Agent Lab API 테스트 | 관리자 실험·Variant 생성/목록, run·복수 evaluation 흐름, Winner promotion·preset 목록, 감사 로그와 동시 실행 429를 검증 |
| `tests/single-harness.test.ts` | SingleHarness 테스트 | prompt 변환·환경 snapshot·node/event/usage/cost/checkpoint 성공, 시간·토큰 예산, 사용자 취소, 훅 차단, 공급자 max turn과 중복 실행 차단 검증 |
| `tests/path-security.test.ts` | 경로 보안 테스트 | traversal, symlink, 외부망 민감 경로·symlink로 해석된 민감 경로 차단, 내부망 민감 경로 허용, 내부망에서도 프로젝트 밖 symlink 차단, 업로드 파일명 정규화 차단 검증 |
| `tests/file-routes.test.ts` | 파일 API 테스트 | 형식별 미리보기, 외부망 점 파일·민감 파일 차단, 내부망 점 파일·민감 파일 목록·미리보기·다운로드 허용, 텍스트 파일 편집 저장과 이진 파일·디렉터리·크기 초과·외부망 민감 경로 저장 거부, symlink 제외, 첨부 소유권, 원자적 overwrite, 빈 업로드·위장 파일 차단, `@파일` 멘션용 파일 검색이 하위 폴더까지 찾되 node_modules·민감 경로는 건너뛰는지, 파일·폴더 삭제가 외부망에서는 403이고 내부망이라도 `.git` 등 민감 경로는 거부하는지 검증 |
| `tests/uploads.test.ts` | 업로드 저장 테스트 | no-replace·명시 overwrite와 파일 크기 초과 시 임시·대상 파일 정리 검증 |
| `tests/network.test.ts` | 신뢰 네트워크 테스트 | 사설망·CGNAT·IPv6·mapped 주소 경계, 잘못된 CIDR 거부, 신뢰 프록시 미설정 시 전달 헤더 요청의 내부망 판정 포기와 설정 시 실제 클라이언트 주소 판정 검증 |
| `tests/agent-bridge.test.ts` | 에이전트 연동 테스트 | 번호 기반 문맥 조회, 멱등 전달, 새 자식 작업의 assistant 완료 응답 대기·영속화, 소켓 `0600`·정리, Codex·Claude 프로젝트 스킬 링크와 기존 파일 보존 검증. Grok 자식 채팅 위임과 어댑터가 없는 공급자 거부도 함께 검증 |
| `tests/agent-delegation-routes.test.ts` | 서브 에이전트 API 테스트 | 관리자 위임 상태 조회, 대상 채팅 상태 결합, 새 자식 생성 브리지 호출과 감사 로그 검증 |
| `tests/tool-mcp-toml.test.ts` | MCP config.toml 파싱 테스트 | 실제 `grok mcp add`가 남긴 따옴표 없는 이름+서브테이블·여러 줄 `args`·이름에 점이 있는 경우, WAM이 쓰는 따옴표 있는 이름+inline 표기를 모두 같은 구조로 읽는지와, 서버 삭제가 그 서버의 서브테이블만 지우는지 검증 |
| `tests/credential-vault.test.ts` | 자격증명 vault 보안·통합·성능 테스트 | AES-GCM 평문 비저장, master key 권한·유실·AAD 변조 fail-fast, Slack legacy migration/실제 fetch 주입, Claude·Codex·Grok 공식 환경 참조, 프로젝트·공급자 범위, Codex env 충돌, MCP API·감사 로그 비노출과 100개 launch lease 1초 상한 검증 |
| `tests/credential-vault-cli.test.ts` | 설치 CLI vault 참조 smoke | 격리된 임시 HOME/config에서 실제 Codex `env_vars`·`env_http_headers`, Claude/Grok `${VAR}` 파싱을 확인하고, Grok 목록 명령의 확장 원문 출력 특성 때문에 WAM 카탈로그에는 vault 환경을 주입하지 않는 경계를 고정 |
| `tests/full-backup.test.ts`, `tests/full-backup-cli.test.ts` | 전체 백업·빈 환경 복구 QA | 암호문 비노출·변조/오암호·비어 있지 않은 대상 차단·관계/session/key 복구·로그인/runtime 폐기·관리 API 감사를 검증하고 실제 별도 CLI restore/drill과 5,000 chat 5초 회귀 상한을 고정 |
| `tests/task-board.test.ts`, `tests/task-board-routes.test.ts` | 작업 보드·라우팅·queue QA | 재시작 복원, 상태/rate-limit/idle/schedule 투영, metadata 멱등, 추천 근거와 승인 전 불변성, 적용 시 동시 상한 재검증, priority admission, 실제 Express 감사, 2,000 task 성능을 검증 |
| `tests/live-preview.test.ts`, `tests/workbench-artifact-migration.test.ts` | 실제 browser preview·visual·a11y 보안/성능 QA | 실제 별도 project cwd의 HTTP listener 자동 탐지, 설치 Chrome과 임시 loopback HTTP의 viewport PNG, local network/console redaction/외부 차단, 0·변경 pixel diff, axe 위반, DOM value 원문 0건, 0600/hash tamper, 10초 검사 상한과 기존 artifact schema 무손실 migration 검증 |
| `tests/remote-worker.test.ts`, `tests/remote-worker-routes.test.ts`, `tests/e2e/remote-worker.spec.ts`, `tests/e2e/remote-dispatch.spec.ts` | 원격 worker 보안·성능·실사용 QA | 실제 fake SSH child process의 0600 key/pin과 고정 probe/start/status argv, root escape·capability·멱등·timeout unknown·재시작 복구·최소 감사/API를 검증. 350ms UI 지연에서 500ms 이내 등록/probe/dispatch/status 피드백·요청 1회·secret 비렌더링·390px 무overflow 확인 |
| `tests/web-agent-manager-worker.test.ts` | 원격 worker CLI 실제 프로세스 QA | capabilities/start/status와 detached recipe를 실제 Node child process로 실행해 2초 ACK, owner-only 원장, 멱등·redaction, exact schema·symlink escape·ID 충돌·config 권한·동시성·timeout·shell 비사용을 검증 |
| `tests/github-auto-merge.test.ts`, `tests/github-auto-merge-routes.test.ts`, `tests/e2e/pr-auto-merge.spec.ts` | PR monitor·조건부 merge 보안/성능 QA | 실제 fake gh child process의 check 집계·head pin·고정 enable/disable argv와 모든 fail-closed 조건, 2초 상한, HTTP 신뢰망·감사/task event를 검증하고 350ms mutation의 500ms 피드백·15초 실제 poll·요청 1회·390px 무overflow를 확인 |
| `tests/git-hunk-review.test.ts`, `tests/git-hunk-review-routes.test.ts`, `tests/e2e/hunk-review.spec.ts` | hunk/라인 주석 실제 Git·HTTP·Chrome QA | 떨어진 hunk 부분 stage/역적용, untracked 유지, stale 무변경, 신뢰망·test_only·재인증 경계, 원문 없는 감사, 멱등 주석 payload, 350ms 지연 피드백과 390px overflow 검증 |
| `tests/agent-integration.test.ts` | 공급자 설치 관리 테스트 | Codex·Claude·Grok CLI 감지, 공급자별 공식 MCP 명령 인자(Grok은 `mcp get` 없이 `mcp list` 출력으로 판정), 사용자 전역 스킬 링크, CLI 미설치 시 무변경 실패, 시작 1회 검사·성공 DB 복원·이미 연결된 MCP 무변경 성공 검증 |
| `tests/cli-auth.test.ts` | CLI 인증 상태 캐시 테스트 | 서버 초기화 동시·반복 조회의 공급자별 1회 실행과 로그인 PTY 종료 뒤 해당 공급자만 재검사하는 계약 검증 |
| `tests/github-projects.test.ts` | GitHub 프로젝트 테스트 | HTTPS·SSH origin 정규화, 저장소 연결 상태, 비활성 프로젝트 재사용, 기본 경로 clone, 로컬 저장소 생성 명령 계약 검증 |
| `tests/prompt-scheduler.test.ts` | 영구 프롬프트 스케줄러 테스트 | 시간대 변환, 새 채팅·기존 채팅 실행, 일일 중복 방지, 다음 날 재실행, 저장 직후 과거 시각 실행 방지와 서버 재시작 중복 방지 검증 |
| `tests/history-sync.test.ts` | 히스토리 동기화 테스트 | Codex·Claude 어댑터별 기록 동기화 정책이 중간 assistant 조각은 완료 알림으로 보지 않고 각 공급자 완료 신호 기준으로만 완료 처리하는지 검증 |
| `tests/history-sync-project.test.ts` | 프로젝트 귀속 테스트 | 작업 중 cwd가 하위 디렉터리로 바뀌어도 채팅이 원래 프로젝트에 남고 새 프로젝트가 생기지 않는지, 등록된 프로젝트 하위(실험 worktree·평가 작업공간)의 새 세션이 상위 프로젝트로 귀속되는지, 기존 worktree 채팅 귀속은 그대로 동작하는지 검증 |
| `tests/auth-routes.test.ts` | 인증 라우트 테스트 | 로그인 실패 감사·시도 제한, 세션 발급·목록·현재 외 폐기, 현재 암호 검증·비밀번호 변경·전체 폐기, 대량 목록 성능, 임시 로그인 변경 차단, 로그인·`/me` 신뢰 네트워크 capability, 마지막 프로젝트·채팅과 사용자별 채팅 화면 모드 저장, 8자리 코드의 관리자 전용 발급·해시 저장·1회 소비·일반 사용자/내부망 차단·만료, 타이밍 세이프 비교 검증 |
| `tests/mfa.test.ts` | MFA 암호·HTTP 통합 테스트 | RFC TOTP 벡터, encrypted seed·hashed recovery·0600 master key, key 유실 fail-fast, 비밀번호만으로 세션 미발급, TOTP replay와 복구 코드 재사용 거부, MFA 해제 뒤 전체 세션 폐기를 검증 |
| `tests/rbac.test.ts` | RBAC 테스트 | 일반 사용자 관리자 전용 HTTP 변경 작업 거부, 채팅 프롬프트 허용, WebSocket 터미널 구독·raw 입력 거부 검증 |
| `tests/instruction-routes.test.ts` | 지침 파일 API 테스트 | `CLAUDE.md`가 `AGENTS.md`를 import하도록 생성하고 반복 호출 시 import를 중복하지 않는지 검증 |
| `tests/chat-attachments.test.ts` | 채팅 첨부 API 테스트 | 업로드 파일의 프로젝트 보존, 전용 worktree 상대경로 복제와 첨부 전용 디렉터리 symlink 탈출 거부 검증 |
| `tests/chat-prompt.test.ts` | 장문 프롬프트 테스트 | 1,000자 임계값, 원문 `.txt` 보존, worktree 복제, 슬래시·셸 명령 예외, Unicode 문자 수와 symlink 경계 검증 |
| `tests/attachments.test.ts` | 첨부 미리보기 유틸 테스트 | SVG 제외 이미지 확장자 판정, 업로드 다운로드 URL 인코딩, 프로젝트·worktree 상대/절대 이미지 첨부 렌더링과 프로젝트 밖 절대경로 차단, `chat:` 링크가 `onOpenChat` 유무에 따라 채팅 이동 링크·외부 링크로 갈리는지, 파일 줄 링크(`#L줄`)가 프로젝트 파일 링크로 인식되는지 검증 |
| `tests/file-links.test.ts` | 프로젝트 파일 링크 테스트 | 절대·상대 경로와 줄 번호 변환, Markdown 기준 디렉터리 해석, 외부 URL·프로젝트 밖 경로 거부, 인코딩된 채팅 작업공간 콘텐츠·다운로드 URL 검증 |
| `tests/message-pagination.test.ts` | 메시지 페이지네이션 테스트 | DB 없이 JSONL을 커서 기반으로 정확히 나누어 반환하는지 검증 |
| `tests/approvals.test.ts` | 승인 요약·버튼 라벨 테스트 | Claude PermissionRequest 훅(permission) 요약 추출, confirm_yn·trust_directory 2택 액션과 resume_session_prompt 3택 액션 검증 |
| `tests/session-backups.test.ts` | 세션 백업 테스트 | Codex·Claude JSONL 세션을 각각 백업, 삭제, 복원할 수 있는지 검증, 삭제 직전 토큰 이벤트·삭제 배지 보존과 기능 도입 전 백업 역수집, 백업 사본만 지우고 원본 채팅은 그대로 남는지·존재하지 않는 백업 삭제 시 실패하는지 검증 |
| `tests/github-detail-cache.test.ts` | GitHub 상세 캐시·Actions 분리 테스트 | 가짜 `gh` 실행 파일을 PATH 앞에 두고 호출 횟수를 세어, 같은 이슈·PR을 두 번 열어도 `gh`가 한 번만 실행되는지·다른 번호는 각각 조회하는지·목록 조회가 `run list`를 부르지 않는지·Actions 엔드포인트는 워크플로만 읽는지 검증 |
| `tests/git-cache-pending.test.ts` | Git 캐시 진행 중 조회 테스트 | 첫 조회가 끝나기 전 같은 키로 들어온 요청이 빈 값 대신 진행 중인 조회 결과를 기다려 받는지·저장 시각이 1970년이 아닌 실제 시각인지·첫 조회가 실패하면 뒤이은 요청도 같은 오류를 받는지·이미 끝난 조회는 기다리지 않고 저장값을 바로 받는지 검증(모두 CLI는 한 번만 실행) |
| `tests/git-commit-paging.test.ts` | Git 커밋 목록 페이징 테스트 | 커밋을 여러 개 쌓은 임시 저장소로 `createGitRouter`를 띄워, 기본 조회가 30개까지만 주면서 뒤에 더 있다고 알리는지·커밋 수가 요청 개수보다 적거나 정확히 같으면 더 있다고 하지 않는지·`commits` 쿼리로 개수를 늘리면 그만큼 더 내려오는지·잘못된 값과 상한 초과 값을 안전하게 처리하는지 검증 |
| `tests/git-diff-untracked.test.ts` | Git diff untracked 파일 테스트 | 실제 임시 git 저장소로 `createGitRouter`를 직접 띄워, untracked 새 파일이 전체 추가 diff로 나오는지·특정 untracked 파일만 선택해도 무관한 tracked 변경이 안 섞이는지·tracked·untracked가 섞였을 때 둘 다 나오는지 검증 |
| `tests/git-discard.test.ts` | Git 롤백(`/git/discard`) 테스트 | 실제 임시 git 저장소로 인증·신뢰 네트워크 미들웨어까지 갖춘 `createGitRouter`를 띄워, 수정·삭제된 추적 파일이 HEAD로 복원되는지·스테이지된 신규 파일과 순수 untracked 파일이 삭제되는지·폴더 단위로 고른 여러 파일만 되돌고 선택 밖 파일은 그대로인지·외부망에서는 403으로 거부되는지·빈 선택은 400인지 검증(감사 로그가 `users` FK를 요구해 테스트 DB에 관리자 사용자 행을 미리 넣어둠) |
| `tests/git-workspaces.test.ts` | 채팅별 Git 작업공간 테스트 | 실제 임시 Git 저장소로 공유 checkout 보존, 두 채팅의 독립 브랜치, 외부 AI 생성 worktree 발견·연결, 외부 worktree가 쓰는 브랜치는 브랜치 지정만으로 붙지 않고 경로 지정으로만 공유된다는 점, 실행 중·미커밋 전환 차단, `feat/#567` 브랜치명과 porcelain 파싱 검증 |
| `tests/git-cache.test.ts` | Git 조회 캐시 테스트 | 첫 조회만 실행·이후 캐시 반환, TTL 경과 시 stale 반환 후 백그라운드 갱신, 강제 갱신, 프로젝트 단위 무효화, 첫 조회 실패 시 재시도, 갱신 실패 시 마지막 성공값 유지 검증 |
| `tests/issue-branch-name.test.ts` | 이슈 브랜치명 테스트 | 영문 슬러그 변환, 사용 불가 문자 정리, 한글 전용 제목은 번호만, 길이 제한과 끝 하이픈 제거, 빈 제목 처리 검증 |
| `tests/tmux-input.test.ts` | tmux 입력 테스트 | bracketed paste 프롬프트 붙여넣기와 Enter 키 제출, 실패 시 버퍼 정리, 기록 스크롤의 copy-mode 진입·방향별 명령·모드 종료와 창 리사이즈 명령 검증 |
| `tests/terminal-history-scroll.test.ts` | 터미널 기록·크기 테스트 | 웹 스크롤 요청의 tmux 기록 이동 위임과 줄 수 제한, 기록 보기 상태에서 키 입력 시 실시간 화면 자동 복귀, 행 수의 PTY·tmux·headless 동기화와 상한 검증 |
| `tests/tui-readiness.test.ts` | TUI 준비 상태 테스트 | Codex 입력 프롬프트, 순환 placeholder 문구, 프롬프트 위/아래 작업중 상태 영역, Claude 프롬프트·생성 중 판정, 오래된 버퍼에 남은 문구 무시 검증 |
| `tests/supervisor-portability.test.ts` | supervisor 이식성 테스트 | 특정 호스트 절대경로가 남지 않고 공백이 있는 임의 설치 경로에서도 스크립트 위치를 앱 루트로 계산하며 PATH의 Node shim을 실제 `process.execPath`로 해석하는지 검증 |
| `tests/release-version.test.ts` | 릴리즈·Android 버전 테스트 | package와 Android versionName 일치, Gradle 9.5.0 배포 ZIP과 wrapper JAR의 공식 SHA-256 고정 검증 |
| `tests/e2e/ui.spec.ts` | Playwright 테스트 | 로그인·대시보드·채팅·파일·지침·GitHub·도구·설정·Agent Lab과 주요 팝업을 실제 Chromium에서 데스크톱·390px 모바일로 렌더링·캡처한다. 모바일 5개 하단 탭과 더보기 시트, 설정으로 이동한 알림·인증·운영 정책, 계정별 채팅/터미널·PTY 입력, history 복원, 브랜치/worktree, PR diff·사용량·프로세스 제어도 함께 검증한다. |
| `tests/e2e/git-list-paging.spec.ts` | Playwright 목록 더 보기·커밋 상세 테스트 | Git 화면에서 커밋 30개·PR 50개가 먼저 오고 "더 보기"를 누르면 늘어난 개수로 다시 조회해 나머지가 채워지는지, 남은 항목이 없으면 버튼이 사라지는지, 커밋 더 보기가 GitHub 목록까지 다시 읽지는 않는지 검증. 390px 모바일에서는 커밋을 고를 때 목록이 접히고 상세가 화면 안에 들어오는지, 커밋 설명이 자체 스크롤 없이(`overflow-y: visible`, 내용이 잘리지 않음) 페이지 흐름을 따르는지 검증하고 화면을 캡처. 이슈를 고르면 목록이 접히고 상세가 화면 맨 위(상단 200px 안)에서 시작하는지, 긴 이슈 본문이 자체 스크롤 없이 이어지는지, 토글로 목록을 다시 펼 수 있는지 검증. 저장소 정보가 없는 응답(`{cachedAt}`만 담긴 캐시 경쟁 사례)에서도 프로젝트를 바꿔가며 흰 화면 대신 안내가 뜨고 `pageerror`가 나지 않는지 검증 |
