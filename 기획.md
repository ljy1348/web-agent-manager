# Codex·Claude 웹 에이전트 관리자 기획

## 1. 개요

Linux 환경에서 Codex CLI와 Claude Code를 하나의 웹 UI로 관리하는 애플리케이션을 만든다.
각 AI 도구는 비대화형 명령으로 호출하지 않고, 백그라운드의 실제 인터랙티브 터미널(PTY)에서 실행한다.
사용자는 웹에서 새 채팅을 만들고, 질문과 슬래시 명령을 입력하며, 종료된 터미널의 기존 대화를 자동으로 재개할 수 있다.

## 2. 목표

- Codex CLI와 Claude Code를 동일한 웹 UI에서 관리한다.
- 각 채팅마다 독립된 인터랙티브 PTY를 생성한다.
- 브라우저를 닫아도 실행 중인 CLI 세션을 유지한다.
- 터미널이 종료된 채팅에 다시 입력하면 기존 CLI 세션을 자동으로 재개한다.
- 대화 내용은 터미널 화면을 파싱하지 않고 각 CLI의 전역 저장 기록을 기준으로 표시한다.
- 프로젝트 및 전역의 `AGENTS.md`, `CLAUDE.md` 등 지침 파일을 웹에서 관리한다.
- 인증된 사용자만 웹 애플리케이션과 프로젝트에 접근할 수 있게 한다.

## 3. 핵심 원칙

### 3.1 실제 인터랙티브 CLI 사용

- `claude -p`처럼 질문을 명령 인자로 전달하는 비대화형 방식은 사용하지 않는다.
- Codex와 Claude를 각각 실제 PTY에서 대화형 모드로 실행한다.
- 질문, 승인 응답, 슬래시 명령은 실행 중인 PTY에 키 입력으로 전달한다.
- resume에 필요한 실행 옵션은 사용할 수 있지만, 재개 후 질문은 PTY 입력으로 전달한다.

### 3.2 대화 기록과 터미널 출력 분리

- 웹의 대화 화면은 ANSI 터미널 출력을 파싱해 만들지 않는다.
- Claude와 Codex가 전역 저장소에 기록하는 프로젝트별 세션 파일을 읽어 대화를 구성한다.
- 사용자 메시지, AI 답변, 도구 호출 및 상태는 공급자별 기록 어댑터가 공통 형식으로 변환한다.
- 터미널 화면은 문제 해결이나 직접 조작이 필요한 경우에만 보조 패널로 제공한다.

### 3.3 정확한 세션 연결

- 웹 채팅과 CLI 세션 ID를 데이터베이스에 일대일로 연결한다.
- 동일 프로젝트에서 여러 채팅을 동시에 실행할 수 있어야 한다.
- 단순히 프로젝트의 가장 최근 기록을 선택하지 않고 저장된 세션 ID를 우선 사용한다.
- 세션 ID를 아직 감지하지 못한 초기 단계에서만 프로젝트 경로, 생성 시각, 프로세스 정보를 함께 사용해 연결한다.

## 4. 주요 사용자 흐름

### 4.1 새 채팅 생성

1. 사용자가 프로젝트와 Codex 또는 Claude를 선택한다.
2. 서버가 해당 프로젝트 경로를 작업 디렉터리로 설정한다.
3. 새로운 인터랙티브 PTY에서 선택한 CLI를 실행한다.
4. CLI가 생성한 세션 기록을 감지해 웹 채팅과 CLI 세션 ID를 연결한다.
5. 사용자의 입력을 PTY에 전달한다.
6. 전역 저장 기록의 변경을 감시해 웹 대화 화면을 갱신한다.

새 채팅은 기존 CLI 세션을 재사용하지 않고 항상 새로운 백그라운드 터미널과 CLI 세션을 만든다.

### 4.2 실행 중인 채팅에 입력

1. 사용자가 일반 질문 또는 슬래시 명령을 입력한다.
2. 서버가 채팅의 PTY 실행 상태를 확인한다.
3. 실행 중이면 입력 문자열과 Enter 키를 PTY에 그대로 전달한다.
4. 공급자별 전역 기록이 갱신되면 웹 대화 화면에 반영한다.

### 4.3 터미널 종료

1. 사용자가 터미널 종료를 선택한다.
2. 서버가 CLI에 정상 종료 신호를 전달한다.
3. 제한 시간 안에 종료되지 않으면 해당 PTY의 프로세스 그룹을 종료한다.
4. PTY 리소스는 정리하되 웹 채팅, CLI 세션 ID, 대화 기록 연결 정보는 보존한다.

터미널 종료는 채팅 삭제와 구분한다.

### 4.4 종료된 채팅 자동 재개

1. 종료된 채팅에 새 입력이 들어오면 입력을 대기열에 보관한다.
2. 데이터베이스에서 해당 채팅의 공급자와 정확한 CLI 세션 ID를 조회한다.
3. 원래 프로젝트 경로에서 공급자별 resume 명령으로 인터랙티브 PTY를 새로 실행한다.
4. CLI가 입력 가능한 상태가 된 것을 확인한다.
5. 대기 중인 입력을 PTY에 전달한다.
6. 이후에는 일반 실행 중 채팅과 동일하게 처리한다.

resume에 실패하면 새 대화를 임의로 생성하지 않는다. 실패 이유와 재시도 또는 새 채팅 생성 옵션을 사용자에게 표시한다.

## 5. 세션 상태

채팅의 터미널 상태는 다음과 같이 관리한다.

```text
시작 중 → 실행 중 → 종료 중 → 종료됨
                      ↓
                    오류

종료됨 → 재개 중 → 실행 중
          ↓
        오류
```

필요한 기본 상태:

- `starting`: 새 CLI 프로세스를 시작하는 중
- `running`: PTY와 CLI 프로세스가 실행 중
- `stopping`: 정상 종료 또는 강제 종료를 처리하는 중
- `stopped`: 터미널은 없지만 대화와 CLI 세션 ID는 보존된 상태
- `resuming`: 기존 CLI 세션을 새 PTY에서 재개하는 중
- `error`: 시작, 실행 또는 재개 과정에서 복구가 필요한 상태

## 6. 시스템 구성

### 6.1 웹 클라이언트

- 로그인 및 세션 유지
- 프로젝트 목록과 채팅 목록
- Codex·Claude 선택 및 새 채팅 생성
- 기록 기반 대화 화면
- 질문 및 슬래시 명령 입력
- 실행 상태와 오류 표시
- 필요할 때 여는 원본 터미널 패널
- 프로젝트 및 전역 지침 파일 편집기

후보 기술:

- Next.js
- xterm.js
- WebSocket

### 6.2 애플리케이션 서버

- 인증 및 권한 검사
- 프로젝트와 채팅 메타데이터 관리
- PTY 생성, 입력, 크기 변경 및 종료
- CLI 프로세스 그룹 관리
- 자동 resume와 입력 대기열 처리
- 공급자별 기록 저장소 감시
- 대화 기록 정규화 및 실시간 전송
- 지침 파일 읽기와 쓰기
- 감사 로그 기록

후보 기술:

- Node.js
- `node-pty`
- SQLite
- WebSocket

### 6.3 공급자 어댑터

Codex와 Claude의 차이를 공급자 어댑터로 분리한다.

각 어댑터의 역할:

- 새 대화형 CLI 실행 명령 구성
- 기존 세션 resume 명령 구성
- CLI 입력 가능 상태 판별
- 전역 기록 저장 위치와 프로젝트 매핑
- 세션 ID 감지
- 기록 파일을 공통 메시지 형식으로 변환
- 공급자별 오류를 공통 오류 형식으로 변환

CLI 버전별 저장 형식과 resume 방식이 바뀔 수 있으므로 어댑터 외부에서 공급자 전용 경로나 파일 형식에 의존하지 않는다.

## 7. 데이터 모델 초안

### 7.1 사용자

- 사용자 ID
- 로그인 식별자
- 비밀번호 해시
- 권한
- 생성 및 최근 로그인 시각

### 7.2 프로젝트

- 프로젝트 ID
- 표시 이름
- 실제 경로
- 활성 여부
- 생성 및 수정 시각

### 7.3 채팅

- 웹 채팅 ID
- 프로젝트 ID
- 공급자(`codex` 또는 `claude`)
- CLI 세션 ID
- 터미널 상태
- 제목
- 생성 및 최근 활동 시각
- 마지막 오류

### 7.4 PTY 실행 정보

- 웹 채팅 ID
- 서버 프로세스 ID
- 프로세스 그룹 ID
- 시작 시각
- 종료 시각
- 종료 코드

민감한 환경변수, 토큰, 인증 파일 내용은 저장하지 않는다.

### 7.5 메시지 인덱스

- 웹 채팅 ID
- 공급자 메시지 ID
- 역할
- 메시지 유형
- 생성 시각
- 원본 기록의 위치 식별자
- 정규화된 표시 데이터

원본 대화 기록은 CLI 전역 저장소를 기준 데이터로 유지한다. 데이터베이스에는 빠른 조회와 변경 감지를 위한 인덱스 또는 캐시만 저장한다.

## 8. 기록 동기화

- 공급자별 허용된 세션 기록 디렉터리와 파일 형식만 읽는다.
- 인증 파일, 토큰, 환경 파일, 쉘 히스토리는 읽지 않는다.
- 파일 변경 감시와 주기적 재검사를 함께 사용해 이벤트 누락을 복구한다.
- 파일 전체를 매번 다시 읽지 않고 마지막 처리 위치를 저장해 증분 반영한다.
- 기록 파일이 완전히 쓰이기 전에 읽을 수 있으므로 불완전한 마지막 레코드는 다음 변경 때 재처리한다.
- 중복 이벤트가 들어와도 같은 메시지가 중복 저장되지 않도록 공급자 메시지 ID 또는 안정적인 이벤트 키를 사용한다.
- 프로젝트 경로와 CLI 세션 ID가 모두 일치하는 기록만 해당 웹 채팅에 연결한다.

## 9. 지침 파일 관리

관리 대상 예시:

- 프로젝트 루트의 `AGENTS.md`
- 프로젝트 루트의 `CLAUDE.md`
- 프로젝트 내부에서 명시적으로 허용된 추가 지침 파일
- Codex 및 Claude가 지원하는 전역 지침 파일

보안 규칙:

- 등록된 프로젝트 루트와 명시적으로 허용된 전역 파일만 접근한다.
- `..` 경로 이동과 심볼릭 링크를 이용한 허용 범위 이탈을 차단한다.
- 저장 전 실제 경로를 다시 검증한다.
- 변경 전후와 변경 사용자를 감사 로그에 기록한다.
- 민감 정보가 포함될 수 있는 일반 설정 파일 편집 기능으로 확장하지 않는다.

## 10. 인증 및 보안

- 인증되지 않은 사용자의 모든 프로젝트, 채팅, WebSocket 접근을 차단한다.
- 비밀번호는 검증된 해시 알고리즘으로 저장한다.
- 세션은 `HttpOnly`, `Secure`, `SameSite` 쿠키를 사용한다.
- 상태 변경 요청에는 CSRF 방어를 적용한다.
- WebSocket 연결 시에도 로그인 세션과 프로젝트 권한을 다시 검증한다.
- 브라우저에 CLI 토큰, API 키, 인증 파일 내용 또는 전체 프로세스 환경을 전달하지 않는다.
- CLI 인증은 서버 운영체제에서 사용자가 미리 완료한 기존 인증 상태를 사용한다.
- 명령 실행은 공급자별 고정 실행 파일과 검증된 인자만 허용한다.
- 웹 입력을 셸 명령 문자열로 조합하지 않고 PTY의 표준 입력으로만 전달한다.
- 프로젝트 경로 접근, 터미널 생성·종료, 지침 파일 변경을 감사 로그로 남긴다.

## 11. 동시성 및 복구

- 한 채팅의 PTY 입력은 직렬화한다.
- 재개 중 들어온 여러 입력은 순서를 유지해 대기열에 저장한다.
- 중복 resume 요청이 동시에 들어와도 PTY는 하나만 생성한다.
- 동일 채팅을 여러 브라우저에서 열 경우 터미널 입력 권한 충돌 정책이 필요하다.
- 서버 재시작 시 실행 중이던 프로세스의 생존 여부를 확인하고 상태를 복구한다.
- 서버가 PTY 프로세스를 함께 종료하는 구조라면 재시작 후 기존 CLI 세션 ID로 자동 복구 가능한 상태로 표시한다.

## 12. 오류 처리

다음 오류를 사용자에게 구분해서 보여준다.

- CLI 실행 파일이 설치되지 않음
- CLI 인증이 완료되지 않음
- 프로젝트 경로가 없거나 접근할 수 없음
- CLI 세션 ID를 찾지 못함
- 저장된 세션을 resume할 수 없음
- 기록 파일 형식이 지원되지 않음
- PTY 프로세스 비정상 종료
- 지침 파일 접근 범위 위반

인증이 필요한 경우 웹에서 토큰을 요구하지 않고, 서버 운영자에게 해당 CLI의 공식 로그인 명령을 직접 실행하도록 안내한다.

## 13. MVP 범위

1. 웹 로그인, 사용자 권한 및 감사 로그
2. 서버 시작 시 기존 프로젝트와 CLI 세션 자동 등록
3. 로컬 프로젝트 등록 및 목록
4. 공급자별 기록 파서와 공통 대화 화면
5. Codex·Claude 새 인터랙티브 PTY 생성
6. 채팅별 백그라운드 터미널 유지
7. 질문과 슬래시 명령의 PTY 입력
8. 터미널 정상·강제 종료
9. 종료된 채팅 입력 시 기존 세션 ID 기반 별도 PTY 자동 resume
10. 웹 권한 승인 및 추가 사용자 입력 처리
11. 공급자별 상태 조회 PTY와 1분 주기 `/usage` 표시
12. 시스템 자원 및 공급자 프로세스 사용량 대시보드
13. 원본 터미널 패널
14. 프로젝트 및 전역 `AGENTS.md`, `CLAUDE.md` 관리
15. 안전한 파일 업로드 및 다운로드
16. Git 상태, diff, 브랜치, 커밋 및 push 관리
17. GitHub 이슈, Pull Request, 리뷰 및 CI 관리
18. Slack 권한 요청·완료·실패 알림
19. 모바일 대응 웹 UI
20. 오류 화면과 복구 동작

## 14. 후속 확장 후보

- 여러 Linux 서버를 중앙 웹에서 관리하는 에이전트 구조
- 사용자별 프로젝트 접근 권한
- 채팅 검색과 태그
- Slack 메시지 내 직접 승인 버튼
- GitHub merge, 브랜치 삭제 및 강제 push 등 고위험 작업
- 백업 및 복원

## 15. 미결정 사항

- 단일 Linux 서버 전용으로 먼저 구현할지, 여러 서버 관리까지 처음부터 포함할지
- 실행 중인 PTY를 서버 재시작 뒤에도 그대로 유지하기 위한 외부 세션 관리자 사용 여부
- 동일 채팅을 여러 브라우저에서 열었을 때 입력 권한 정책
- 전역 지침 파일의 정확한 허용 경로 목록
- 원본 기록을 데이터베이스에 캐시할 범위와 보존 기간

## 16. 구현 전 확인 항목

- 설치된 Codex CLI와 Claude Code의 대화형 실행 및 resume 명령 확인
- 각 CLI 버전별 전역 기록 경로와 레코드 구조 확인
- 새 세션 ID가 생성되는 시점과 프로젝트 경로 매핑 방법 검증
- CLI 시작 완료와 입력 가능 상태를 안정적으로 감지할 방법 검증
- 정상 종료, 강제 종료, 서버 재시작 상황의 복구 실험
- 기록 파일에 포함될 수 있는 민감 데이터의 필터링 범위 결정

## 17. 확정 추가 요구사항 및 사전 조사 결과

이 절의 항목은 앞의 MVP 범위에 추가하며, 충돌하는 기존 내용보다 우선한다.

### 17.1 기준 실행 환경

2026-07-06 현재 서버에서 확인한 기준 버전과 도구는 다음과 같다.

- Codex CLI: `0.142.5`
- Claude Code: `2.1.201`
- Node.js: `22.23.1`
- npm: `10.9.8`
- Git: `2.43.0`
- GitHub CLI: `2.45.0`
- tmux: `3.4`
- Linux: `x86_64`, 커널 `6.14`
- 네이티브 모듈 빌드 도구: `make`, `g++`, Python 사용 가능
- SQLite CLI: 미설치
- 개발 환경: Docker 컨테이너
- 실제 배포 대상: 일반 Linux 호스트

초기 구현과 테스트는 위 Codex·Claude 버전을 고정 기준으로 진행한다. 공급자 어댑터에는 감지한 실제 버전을 저장하고, 지원 범위를 벗어나면 조용히 오동작하지 않고 호환성 경고를 표시한다.

SQLite CLI가 없어도 Node.js SQLite 드라이버는 사용할 수 있다. 실제 배포는 일반 Linux 호스트의 systemd 서비스 구성을 우선한다.

### 17.2 웹 권한 요청 처리

웹에서 권한 요청을 확인하고 허용 또는 거부하는 기능은 구현 가능하다. 공급자별 안정적인 통합 방식은 다르게 구성한다.

#### Codex

현재 설치된 Codex의 `app-server`는 다음 구조화된 서버 요청을 제공한다.

- 명령 실행 승인: `item/commandExecution/requestApproval`
- 파일 변경 승인: `item/fileChange/requestApproval`
- 추가 파일시스템·네트워크 권한 승인: `item/permissions/requestApproval`
- 사용자 추가 입력: `item/tool/requestUserInput`

명령 실행과 파일 변경은 `accept`, `acceptForSession`, `decline`, `cancel` 결정을 지원한다. 명령, 작업 경로, 요청 이유, thread ID와 turn ID를 웹 승인 카드에 표시할 수 있다.

Codex는 실제 인터랙티브 TUI를 유지해야 한다는 요구사항이 있으므로 다음 순서로 기술 검증한다.

1. 로컬 Unix socket에서 `codex app-server`를 실행한다.
2. 인터랙티브 Codex TUI를 `--remote unix://...`로 연결한다.
3. 웹 서버도 같은 app-server에서 해당 thread의 승인 요청을 안정적으로 받을 수 있는지 검증한다.
4. 가능하면 app-server 이벤트를 권한 요청과 상태의 기준으로 사용하고 PTY TUI는 계속 유지한다.
5. 동일 thread의 다중 클라이언트 승인이 지원되지 않으면 PTY 화면 상태를 인식하고 승인 선택 키를 전달하는 호환 계층을 사용한다.

마지막 방식은 CLI 화면 변경에 취약하므로 Codex `0.142.5`의 PTY 스냅샷 회귀 테스트를 필수로 둔다. `codex exec` 같은 비대화형 실행으로 대체하지 않는다.

#### Claude

Claude Code의 `PermissionRequest` 훅은 권한 다이얼로그가 표시되기 직전에 실행된다. 훅 입력에는 세션 ID, 작업 경로, permission mode, 도구 이름, 도구 입력과 항상 허용 후보가 포함된다. 훅은 `allow` 또는 `deny` JSON 결정을 반환할 수 있다.

구현 방식:

1. 앱 전용 Claude 추가 설정에 `PermissionRequest` command hook을 등록한다.
2. 훅 브리지 프로세스가 stdin의 요청 JSON을 로컬 Unix socket으로 웹 서버에 전달한다.
3. 웹 서버가 DB에 대기 중인 승인 요청을 만들고 WebSocket과 Slack으로 알린다.
4. 인증된 사용자가 웹에서 허용 또는 거부한다.
5. 브리지가 결과를 Claude가 요구하는 JSON으로 stdout에 반환한다.
6. 시간 초과나 서버 연결 실패 시 기본 거부하여 무기한 대기나 암묵적 허용을 막는다.

`항상 허용`은 별도 선택지로 제공하되 기본 범위는 현재 세션으로 제한한다. 프로젝트 또는 사용자 설정에 지속 저장하는 선택은 적용 위치와 규칙을 명시하고 한 번 더 확인한다.

#### 공통 승인 보안

- 승인 요청은 공급자, 세션, turn, 요청 ID로 고유하게 식별한다.
- 이미 해결되거나 만료된 요청에는 다시 응답할 수 없다.
- 허용과 거부 모두 사용자, 시각, 대상 명령 또는 파일, 적용 범위를 감사 로그에 남긴다.
- 명령과 파일 경로는 이스케이프된 일반 텍스트로 표시하며 HTML로 렌더링하지 않는다.
- 로그인 세션과 CSRF 검증을 통과한 사용자만 결정을 제출할 수 있다.
- Slack 알림에서는 결정을 직접 수행하지 않고 인증이 필요한 웹 승인 화면으로 연결한다.

### 17.3 서버 시작 시 기존 세션 자동 등록

로컬 확인 결과 Codex와 Claude 모두 전역 세션 JSONL이 존재한다.

- Codex 세션 메타데이터에는 세션 ID, 작업 경로, CLI 버전, 모델 공급자 필드가 존재한다.
- Claude 기록에는 세션 ID, 작업 경로, 메시지 유형, 권한 모드, Git 브랜치 필드가 존재한다.
- Claude `agents --json`으로 활성 백그라운드 세션 목록도 조회할 수 있다.

서버 시작 동기화 순서:

1. Codex와 Claude의 설치 버전 및 실행 가능 상태를 확인한다.
2. 공급자별 허용된 전역 세션 디렉터리를 전체 스캔한다.
3. 공급자, 세션 ID, 정규화된 프로젝트 경로를 고유 키로 프로젝트와 채팅을 upsert한다.
4. 메시지 인덱스를 마지막 처리 위치부터 증분 생성한다.
5. Claude의 공식 활성 세션 목록과 앱이 소유한 PTY 레지스트리를 대조한다.
6. 초기 동기화가 끝난 뒤 파일 변경 감시와 주기적 재검사를 시작한다.

전역 기록으로 기존 대화는 자동 등록할 수 있지만, 앱이 생성하지 않은 임의의 외부 PTY에 안전하게 입력 스트림을 연결할 수 있는 것은 아니다. 이런 세션은 `외부 실행 중` 또는 `재개 가능`으로 표시한다. 입력이 들어오면 정확한 세션 ID로 새 인터랙티브 PTY를 실행해 resume한다.

허용된 프로젝트 루트 밖의 기록은 자동 노출하지 않고 관리자 승인 대기 상태로 둔다. 상태 조회 전용 세션은 자동 채팅 등록 대상에서 제외한다.

### 17.4 사용량과 공급자 상태

- 앱 시작 시 활성화된 공급자마다 상태 조회 전용 인터랙티브 PTY를 하나 실행한다.
- Codex와 Claude를 모두 활성화하면 상태 조회 PTY는 총 두 개다.
- 준비 완료 직후 첫 조회를 수행하고 이후 60초마다 `/usage`를 PTY 입력으로 전달한다.
- 이전 조회가 끝나지 않았으면 다음 주기를 건너뛰어 요청 중첩을 막는다.
- Codex는 `/usage daily`, `/usage weekly`, `/usage cumulative` 직접 조회도 지원하므로 어댑터에서 필요한 뷰를 선택한다.
- Claude `/usage`는 세션 비용, 요금제 사용 한도와 활동 통계를 제공한다.
- 현재 사용량, 남은 사용량, 초기화 시각, 마지막 성공 시각과 최신 여부를 표시한다.
- 상태 조회 PTY가 종료되면 지수 백오프로 재시작한다.
- 파싱 실패를 0으로 표시하지 않고 마지막 정상 값과 `오래된 정보` 상태를 보여준다.

상태값은 다음과 같이 분리한다.

- 모니터 상태: `starting`, `ready`, `refreshing`, `error`, `stopped`
- 데이터 상태: `fresh`, `stale`, `unavailable`
- 오류 코드: `auth_required`, `timeout`, `parse_failed`, `cli_exited`

일반 대화는 전역 기록을 기준으로 표시한다. 전용 PTY 출력 파싱은 `/usage`처럼 별도 구조화 저장소가 확인되지 않은 상태 정보에만 제한한다.

### 17.5 Git 및 GitHub 관리

MVP에서 다음 기능을 제공한다.

- 작업 트리, 현재 브랜치, ahead·behind, 최근 커밋과 원격 상태 조회
- 브랜치 생성 및 전환
- 변경 파일 선택, diff 확인 및 커밋
- 명시적 확인 뒤 push
- GitHub 이슈, Pull Request, 리뷰와 CI 상태 조회
- 이슈 및 Pull Request 생성

GitHub 작업은 서버에 설치된 `gh` CLI를 사용한다. 인증은 서버 운영체제에서 사용자가 미리 완료하며, 웹 앱은 토큰 값을 읽거나 저장하거나 표시하지 않는다. merge, 브랜치 삭제, 강제 push처럼 영향이 큰 작업은 MVP 기본 기능에서 제외하거나 별도의 강한 확인 절차를 적용한다.

모든 Git과 `gh` 실행은 셸 문자열이 아니라 검증된 인자 배열을 사용하고 프로젝트 저장소 경계를 확인한다.

### 17.6 서버 자원 사용량

웹 대시보드에 다음 항목을 표시한다.

- 전체 CPU 사용률과 load average
- 전체·사용·가용 메모리와 swap
- 루트 및 프로젝트가 위치한 파일시스템의 디스크 사용량
- 네트워크 송수신량
- 앱 uptime
- 전체 PTY 수와 실행 중·대기 중·오류 상태별 세션 수
- Codex·Claude·웹 서버 프로세스별 CPU와 메모리 사용량

서버 자원은 기본 2초 간격으로 수집해 WebSocket으로 전송한다. 장기 이력 저장은 초기 MVP에서 제외하고 최근 구간만 메모리 순환 버퍼로 유지한다. 브라우저가 없을 때 불필요한 고빈도 전송은 중단하되 임계치 알림용 저빈도 수집은 유지한다.

컨테이너 환경이 추가되면 호스트 지표와 컨테이너 제한 지표를 구분해 표시한다.

### 17.7 파일 업로드 및 다운로드

- 인증된 사용자가 등록 프로젝트 내부의 허용 경로에 파일을 업로드할 수 있다.
- 업로드는 메모리 전체 적재 없이 스트리밍하고 파일 크기 제한을 설정한다.
- 같은 이름이 있으면 자동 덮어쓰기하지 않고 덮어쓰기 또는 이름 변경을 확인한다.
- 업로드 완료 전에는 임시 파일에 쓰고 검증 후 원자적으로 이동한다.
- 다운로드는 단일 파일과 선택 파일 ZIP 묶음을 지원한다.
- 대용량 다운로드는 Range 요청과 스트리밍을 지원한다.
- 프로젝트 루트 밖 경로, `..`, 절대 경로, 심볼릭 링크 이탈과 ZIP Slip을 차단한다.
- `.git`, 인증 저장소, 토큰, 환경 파일과 전역 CLI 인증 파일은 일반 업로드·다운로드 대상에서 제외한다.
- `AGENTS.md`, `CLAUDE.md` 등 지침 파일은 전용 편집 기능의 감사 정책을 사용한다.
- 업로드·다운로드 사용자, 프로젝트, 경로, 크기, 해시와 시각을 감사 로그에 남긴다.

업로드된 파일을 채팅에 첨부할 때는 프로젝트 내부 경로로 저장한 후 해당 경로를 인터랙티브 CLI 입력에 전달한다.

### 17.8 Slack 알림

알림 이벤트:

- 권한 승인이 필요함
- AI가 추가 사용자 입력을 요청함
- 작업이 정상 완료됨
- 작업이 실패하거나 터미널이 비정상 종료됨
- 선택적으로 사용량 또는 시스템 자원 임계치에 도달함

설정은 값이 아닌 다음 환경변수 이름만 애플리케이션에서 참조한다.

- `SLACK_BOT_TOKEN`
- `SLACK_USER_TOKEN`
- `SLACK_CHANNEL_ID`

토큰은 웹 설정 화면, DB, 로그와 오류 응답에 노출하지 않는다. 기본 알림 전송에는 bot token과 `chat:write` scope를 사용한다. user token은 실제 사용자 명의가 필요한 별도 기능에만 사용하며 일반 알림의 fallback으로 자동 사용하지 않는다.

메시지는 `chat.postMessage`로 전송하고 프로젝트, 공급자, 채팅 제목, 이벤트 종류, 발생 시각과 인증이 필요한 웹 링크를 포함한다. 민감한 프롬프트 전문, 명령 전문, 파일 내용은 기본 메시지에 넣지 않는다.

같은 이벤트 ID는 한 번만 전송하고, 재시도는 Slack의 `Retry-After`와 지수 백오프를 따른다. 채널별 과도한 알림을 막기 위해 완료 이벤트를 묶거나 사용자별 알림 정책을 제공한다.

Slack에서 승인 버튼을 직접 제공하는 기능은 후속 범위로 둔다. 이를 구현하려면 별도의 Slack App 인터랙션 엔드포인트 또는 Socket Mode, 요청 서명 검증과 웹 사용자 연결 정책이 추가로 필요하다.

### 17.9 추가 데이터 모델

#### 권한 요청

- 요청 ID
- 공급자, 채팅, CLI 세션, turn 및 item ID
- 요청 종류와 대상
- 명령, 작업 경로 또는 파일 변경 요약
- 요청 이유와 허용 가능한 결정 목록
- 대기, 허용, 거부, 만료 상태
- 요청 및 해결 시각
- 결정 사용자와 적용 범위

#### 시스템 지표

- 수집 시각
- CPU, load, 메모리, swap, 디스크와 네트워크 지표
- 공급자·웹 서버 프로세스 지표
- 세션 상태별 개수

#### 알림 전송

- 이벤트 ID와 종류
- 대상 채널 ID
- 전송 상태와 시도 횟수
- 마지막 오류와 다음 재시도 시각
- Slack message timestamp

### 17.10 MVP 구현 우선순위

1. 인증, 권한 및 감사 로그
2. 서버 시작 시 기존 프로젝트·세션 자동 등록
3. 공급자별 기록 파서와 공통 대화 모델
4. 인터랙티브 PTY 생성, 종료 및 정확한 resume
5. 웹 권한 승인과 추가 입력 처리
6. 공급자별 상태 조회 PTY와 1분 주기 `/usage`
7. 시스템 자원 대시보드
8. 프로젝트 지침 파일 관리
9. 안전한 파일 업로드·다운로드
10. Git 및 GitHub 조회·기본 쓰기 작업
11. Slack 요청·완료·실패 알림
12. 원본 터미널 패널과 모바일 대응

### 17.11 구현 전 필수 기술 검증

- Codex app-server와 remote TUI를 함께 사용할 때 웹 클라이언트가 동일 thread의 승인 요청을 받을 수 있는지 확인
- 불가능할 경우 Codex TUI 승인 화면과 키 입력을 버전 고정 PTY 테스트로 검증
- Claude `PermissionRequest` command hook의 Unix socket 대기, 허용, 거부와 시간 초과 검증
- 두 CLI의 새 세션, 정확한 ID resume, 종료 후 자동 resume 검증
- 기존 전역 세션 전체 스캔의 중복 방지와 프로젝트 경로 매핑 검증
- 앱 소유 PTY, Claude background agent, 외부 실행 프로세스의 상태 구분 검증
- Codex와 Claude `/usage` 출력의 일반 화면 및 전체 화면 파싱 검증
- 업로드·다운로드 경로 이탈, 심볼릭 링크, ZIP Slip과 대용량 스트리밍 검증
- `gh` 인증이 없거나 권한이 부족할 때 토큰 노출 없이 오류를 표시하는지 검증
- Slack 토큰 미설정, scope 부족, 채널 미가입, rate limit과 재시도 검증
- 실제 UI에서 승인 요청, 파일 전송, 자원 차트와 알림 설정을 Playwright로 검증하고 스크린샷 보관

### 17.12 공식 조사 근거

- [Codex App Server](https://developers.openai.com/codex/app-server/)
- [Codex CLI 슬래시 명령](https://developers.openai.com/codex/cli/slash-commands/)
- [Codex CLI 명령줄 옵션](https://developers.openai.com/codex/cli/reference/)
- [Claude Code 훅 참조](https://code.claude.com/docs/en/hooks)
- [Claude Code 권한 모드](https://code.claude.com/docs/en/permission-modes)
- [Claude Code Remote Control](https://code.claude.com/docs/en/remote-control)
- [Claude Code 명령 목록](https://code.claude.com/docs/en/commands)
- [Slack `chat.postMessage`](https://docs.slack.dev/reference/methods/chat.postmessage)
- [Slack 토큰 유형](https://docs.slack.dev/authentication/tokens/)
- [Slack Web API rate limits](https://docs.slack.dev/apis/web-api/rate-limits/)

## 18. MVP 구현 상태

2026-07-06 기준 초기 MVP 구현을 완료했다.

- 인증, HttpOnly 세션, CSRF, WebSocket 인증과 감사 로그
- 시작 시 Codex·Claude 전역 JSONL 세션 및 프로젝트 자동 등록
- 공급자별 기록 어댑터와 기록 기반 공통 대화 화면
- 채팅별 tmux/PTY, 입력 직렬화, 정상·강제 종료, 정확한 세션 ID 자동 resume
- Codex TUI 승인 호환 계층과 Claude `PermissionRequest` 훅 승인
- 공급자별 상태 PTY, Codex `/usage weekly`·`/status`, Claude `/usage` 60초 조회
- 호스트 자원과 공급자·웹 프로세스 및 세션 상태 대시보드
- 지침 전용 편집, 파일 업로드·다운로드·ZIP과 경로 보안
- Git/GitHub 조회 및 브랜치·커밋·push·이슈·PR·리뷰·CI 재실행
- Slack 승인 요청·작업 완료·비정상 종료 알림
- 반응형 통합 UI, xterm 원본 터미널과 Playwright 검증
- 일반 Linux용 systemd 서비스 예시

실제 설치본 검증 결과:

- Codex 0.142.5와 Claude Code 2.1.201 실제 TUI 사용량 파싱 성공
- 두 공급자 모두 실제 대화형 새 세션 저장, 종료, 동일 세션 ID resume 성공
- TypeScript 타입 검사, Vitest 단위 테스트, 프로덕션 빌드 성공
- 프로덕션 서버 health·로그인·인증 API와 Chrome UI E2E 성공

버전 변경 시 우선 재실행할 회귀 검증은 `npm run test:tui`와 `npm run test:lifecycle`이다. Codex 승인 화면과 공급자 JSONL은 내부 형식 변경 가능성이 있으므로 호환성 경고와 어댑터 업데이트 대상으로 유지한다.
